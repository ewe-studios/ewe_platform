//! Auto-generated API clients for Google Cloud Platform - logging.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_get_cmek_settings_execute()` to send, or `logging_billing_accounts_get_cmek_settings` for simplest API.

pub fn logging_billing_accounts_get_cmek_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/cmekSettings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_get_cmek_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_get_cmek_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_get_cmek_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CmekSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_get_cmek_settings_builder()` + `logging_billing_accounts_get_cmek_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_get_cmek_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_get_cmek_settings_builder(client, name)?;
    logging_billing_accounts_get_cmek_settings_execute(builder)
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_get_settings_execute()` to send, or `logging_billing_accounts_get_settings` for simplest API.

pub fn logging_billing_accounts_get_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/settings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_get_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_get_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_get_settings_builder()` + `logging_billing_accounts_get_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_get_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_get_settings_builder(client, name)?;
    logging_billing_accounts_get_settings_execute(builder)
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_exclusions_create_execute()` to send, or `logging_billing_accounts_exclusions_create` for simplest API.

pub fn logging_billing_accounts_exclusions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &LogExclusion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/exclusions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_exclusions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_exclusions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_exclusions_create_builder()` + `logging_billing_accounts_exclusions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &LogExclusion,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_exclusions_create_builder(client, parent, body)?;
    logging_billing_accounts_exclusions_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_exclusions_delete_execute()` to send, or `logging_billing_accounts_exclusions_delete` for simplest API.

pub fn logging_billing_accounts_exclusions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_exclusions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_exclusions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_exclusions_delete_builder()` + `logging_billing_accounts_exclusions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_exclusions_delete_builder(client, name)?;
    logging_billing_accounts_exclusions_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_exclusions_get_execute()` to send, or `logging_billing_accounts_exclusions_get` for simplest API.

pub fn logging_billing_accounts_exclusions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_exclusions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_exclusions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_exclusions_get_builder()` + `logging_billing_accounts_exclusions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_exclusions_get_builder(client, name)?;
    logging_billing_accounts_exclusions_get_execute(builder)
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_exclusions_list_execute()` to send, or `logging_billing_accounts_exclusions_list` for simplest API.

pub fn logging_billing_accounts_exclusions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/exclusions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_exclusions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_exclusions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListExclusionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_exclusions_list_builder()` + `logging_billing_accounts_exclusions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_billing_accounts_exclusions_list_builder(client, parent, pageSize, pageToken)?;
    logging_billing_accounts_exclusions_list_execute(builder)
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_exclusions_patch_execute()` to send, or `logging_billing_accounts_exclusions_patch` for simplest API.

pub fn logging_billing_accounts_exclusions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogExclusion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_exclusions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_exclusions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_exclusions_patch_builder()` + `logging_billing_accounts_exclusions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogExclusion,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_billing_accounts_exclusions_patch_builder(client, name, updateMask, body)?;
    logging_billing_accounts_exclusions_patch_execute(builder)
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_get_execute()` to send, or `logging_billing_accounts_locations_get` for simplest API.

pub fn logging_billing_accounts_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_get_builder()` + `logging_billing_accounts_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_get_builder(client, name)?;
    logging_billing_accounts_locations_get_execute(builder)
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_list_execute()` to send, or `logging_billing_accounts_locations_list` for simplest API.

pub fn logging_billing_accounts_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/locations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extraLocationTypes",
            extraLocationTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_list_builder()` + `logging_billing_accounts_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    logging_billing_accounts_locations_list_execute(builder)
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_create_execute()` to send, or `logging_billing_accounts_locations_buckets_create` for simplest API.

pub fn logging_billing_accounts_locations_buckets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/buckets", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "bucketId",
            bucketId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_create_builder()` + `logging_billing_accounts_locations_buckets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_create(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_billing_accounts_locations_buckets_create_builder(client, parent, bucketId, body)?;
    logging_billing_accounts_locations_buckets_create_execute(builder)
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_create_async_execute()` to send, or `logging_billing_accounts_locations_buckets_create_async` for simplest API.

pub fn logging_billing_accounts_locations_buckets_create_async_builder(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/buckets:createAsync",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "bucketId",
            bucketId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_create_async_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_create_async_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_create_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_create_async_builder()` + `logging_billing_accounts_locations_buckets_create_async_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_create_async(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_create_async_builder(
        client, parent, bucketId, body,
    )?;
    logging_billing_accounts_locations_buckets_create_async_execute(builder)
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_delete_execute()` to send, or `logging_billing_accounts_locations_buckets_delete` for simplest API.

pub fn logging_billing_accounts_locations_buckets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_delete_builder()` + `logging_billing_accounts_locations_buckets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_delete_builder(client, name)?;
    logging_billing_accounts_locations_buckets_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_get_execute()` to send, or `logging_billing_accounts_locations_buckets_get` for simplest API.

pub fn logging_billing_accounts_locations_buckets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_get_builder()` + `logging_billing_accounts_locations_buckets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_get_builder(client, name)?;
    logging_billing_accounts_locations_buckets_get_execute(builder)
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_list_execute()` to send, or `logging_billing_accounts_locations_buckets_list` for simplest API.

pub fn logging_billing_accounts_locations_buckets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/buckets", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBucketsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_list_builder()` + `logging_billing_accounts_locations_buckets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    logging_billing_accounts_locations_buckets_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_patch_execute()` to send, or `logging_billing_accounts_locations_buckets_patch` for simplest API.

pub fn logging_billing_accounts_locations_buckets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_patch_builder()` + `logging_billing_accounts_locations_buckets_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_billing_accounts_locations_buckets_patch_builder(client, name, updateMask, body)?;
    logging_billing_accounts_locations_buckets_patch_execute(builder)
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_undelete_execute()` to send, or `logging_billing_accounts_locations_buckets_undelete` for simplest API.

pub fn logging_billing_accounts_locations_buckets_undelete_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndeleteBucketRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:undelete", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_undelete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_undelete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_undelete_builder()` + `logging_billing_accounts_locations_buckets_undelete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_undelete(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndeleteBucketRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_undelete_builder(client, name, body)?;
    logging_billing_accounts_locations_buckets_undelete_execute(builder)
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_update_async_execute()` to send, or `logging_billing_accounts_locations_buckets_update_async` for simplest API.

pub fn logging_billing_accounts_locations_buckets_update_async_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:updateAsync", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_update_async_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_update_async_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_update_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_update_async_builder()` + `logging_billing_accounts_locations_buckets_update_async_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_update_async(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_update_async_builder(
        client, name, updateMask, body,
    )?;
    logging_billing_accounts_locations_buckets_update_async_execute(builder)
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_links_create_execute()` to send, or `logging_billing_accounts_locations_buckets_links_create` for simplest API.

pub fn logging_billing_accounts_locations_buckets_links_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    linkId: Option<&str>,
    body: &Link,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/links", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "linkId",
            linkId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_links_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_links_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_links_create_builder()` + `logging_billing_accounts_locations_buckets_links_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_create(
    client: &SimpleHttpClient,
    parent: &str,
    linkId: Option<&str>,
    body: &Link,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_links_create_builder(
        client, parent, linkId, body,
    )?;
    logging_billing_accounts_locations_buckets_links_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_links_delete_execute()` to send, or `logging_billing_accounts_locations_buckets_links_delete` for simplest API.

pub fn logging_billing_accounts_locations_buckets_links_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_links_delete_builder()` + `logging_billing_accounts_locations_buckets_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_links_delete_builder(client, name)?;
    logging_billing_accounts_locations_buckets_links_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_links_get_execute()` to send, or `logging_billing_accounts_locations_buckets_links_get` for simplest API.

pub fn logging_billing_accounts_locations_buckets_links_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_links_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_links_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_links_get_builder()` + `logging_billing_accounts_locations_buckets_links_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_links_get_builder(client, name)?;
    logging_billing_accounts_locations_buckets_links_get_execute(builder)
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_links_list_execute()` to send, or `logging_billing_accounts_locations_buckets_links_list` for simplest API.

pub fn logging_billing_accounts_locations_buckets_links_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/links", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_links_list_builder()` + `logging_billing_accounts_locations_buckets_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_links_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    logging_billing_accounts_locations_buckets_links_list_execute(builder)
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_views_create_execute()` to send, or `logging_billing_accounts_locations_buckets_views_create` for simplest API.

pub fn logging_billing_accounts_locations_buckets_views_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    viewId: Option<&str>,
    body: &LogView,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/views", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "viewId",
            viewId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_views_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_views_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_views_create_builder()` + `logging_billing_accounts_locations_buckets_views_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_create(
    client: &SimpleHttpClient,
    parent: &str,
    viewId: Option<&str>,
    body: &LogView,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_views_create_builder(
        client, parent, viewId, body,
    )?;
    logging_billing_accounts_locations_buckets_views_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_views_delete_execute()` to send, or `logging_billing_accounts_locations_buckets_views_delete` for simplest API.

pub fn logging_billing_accounts_locations_buckets_views_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_views_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_views_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_views_delete_builder()` + `logging_billing_accounts_locations_buckets_views_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_views_delete_builder(client, name)?;
    logging_billing_accounts_locations_buckets_views_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_views_get_execute()` to send, or `logging_billing_accounts_locations_buckets_views_get` for simplest API.

pub fn logging_billing_accounts_locations_buckets_views_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_views_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_views_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_views_get_builder()` + `logging_billing_accounts_locations_buckets_views_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_views_get_builder(client, name)?;
    logging_billing_accounts_locations_buckets_views_get_execute(builder)
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_views_list_execute()` to send, or `logging_billing_accounts_locations_buckets_views_list` for simplest API.

pub fn logging_billing_accounts_locations_buckets_views_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/views", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_views_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_views_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListViewsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_views_list_builder()` + `logging_billing_accounts_locations_buckets_views_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_views_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    logging_billing_accounts_locations_buckets_views_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_views_patch_execute()` to send, or `logging_billing_accounts_locations_buckets_views_patch` for simplest API.

pub fn logging_billing_accounts_locations_buckets_views_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogView,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_views_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_views_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_views_patch_builder()` + `logging_billing_accounts_locations_buckets_views_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogView,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_views_patch_builder(
        client, name, updateMask, body,
    )?;
    logging_billing_accounts_locations_buckets_views_patch_execute(builder)
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_views_logs_list_execute()` to send, or `logging_billing_accounts_locations_buckets_views_logs_list` for simplest API.

pub fn logging_billing_accounts_locations_buckets_views_logs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resourceNames",
            resourceNames
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_buckets_views_logs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_buckets_views_logs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_views_logs_list_builder()` + `logging_billing_accounts_locations_buckets_views_logs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_logs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_views_logs_list_builder(
        client,
        parent,
        pageSize,
        pageToken,
        resourceNames,
    )?;
    logging_billing_accounts_locations_buckets_views_logs_list_execute(builder)
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_operations_cancel_execute()` to send, or `logging_billing_accounts_locations_operations_cancel` for simplest API.

pub fn logging_billing_accounts_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_operations_cancel_builder()` + `logging_billing_accounts_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_operations_cancel_builder(client, name, body)?;
    logging_billing_accounts_locations_operations_cancel_execute(builder)
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_operations_get_execute()` to send, or `logging_billing_accounts_locations_operations_get` for simplest API.

pub fn logging_billing_accounts_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_operations_get_builder()` + `logging_billing_accounts_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_operations_get_builder(client, name)?;
    logging_billing_accounts_locations_operations_get_execute(builder)
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_operations_list_execute()` to send, or `logging_billing_accounts_locations_operations_list` for simplest API.

pub fn logging_billing_accounts_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/operations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_operations_list_builder()` + `logging_billing_accounts_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    logging_billing_accounts_locations_operations_list_execute(builder)
}

/// GET v2/{+parent}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_recent_queries_list_execute()` to send, or `logging_billing_accounts_locations_recent_queries_list` for simplest API.

pub fn logging_billing_accounts_locations_recent_queries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/recentQueries", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_recent_queries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_recent_queries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_recent_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRecentQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_recent_queries_list_builder()` + `logging_billing_accounts_locations_recent_queries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_recent_queries_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_recent_queries_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    logging_billing_accounts_locations_recent_queries_list_execute(builder)
}

/// GET v2/{+parent}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_saved_queries_create_execute()` to send, or `logging_billing_accounts_locations_saved_queries_create` for simplest API.

pub fn logging_billing_accounts_locations_saved_queries_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    savedQueryId: Option<&str>,
    body: &SavedQuery,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/savedQueries", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "savedQueryId",
            savedQueryId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_saved_queries_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_saved_queries_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_saved_queries_create_builder()` + `logging_billing_accounts_locations_saved_queries_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_create(
    client: &SimpleHttpClient,
    parent: &str,
    savedQueryId: Option<&str>,
    body: &SavedQuery,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_saved_queries_create_builder(
        client,
        parent,
        savedQueryId,
        body,
    )?;
    logging_billing_accounts_locations_saved_queries_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_saved_queries_delete_execute()` to send, or `logging_billing_accounts_locations_saved_queries_delete` for simplest API.

pub fn logging_billing_accounts_locations_saved_queries_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_saved_queries_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_saved_queries_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_saved_queries_delete_builder()` + `logging_billing_accounts_locations_saved_queries_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_saved_queries_delete_builder(client, name)?;
    logging_billing_accounts_locations_saved_queries_delete_execute(builder)
}

/// GET v2/{+name}
/// Returns all data associated with the requested query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_saved_queries_get_execute()` to send, or `logging_billing_accounts_locations_saved_queries_get` for simplest API.

pub fn logging_billing_accounts_locations_saved_queries_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Returns all data associated with the requested query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_saved_queries_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_saved_queries_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Returns all data associated with the requested query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_saved_queries_get_builder()` + `logging_billing_accounts_locations_saved_queries_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_saved_queries_get_builder(client, name)?;
    logging_billing_accounts_locations_saved_queries_get_execute(builder)
}

/// GET v2/{+parent}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_saved_queries_list_execute()` to send, or `logging_billing_accounts_locations_saved_queries_list` for simplest API.

pub fn logging_billing_accounts_locations_saved_queries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/savedQueries", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_saved_queries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_saved_queries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSavedQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_saved_queries_list_builder()` + `logging_billing_accounts_locations_saved_queries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_saved_queries_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    logging_billing_accounts_locations_saved_queries_list_execute(builder)
}

/// GET v2/{+name}
/// Updates an existing SavedQuery.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_saved_queries_patch_execute()` to send, or `logging_billing_accounts_locations_saved_queries_patch` for simplest API.

pub fn logging_billing_accounts_locations_saved_queries_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SavedQuery,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates an existing SavedQuery.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_locations_saved_queries_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_locations_saved_queries_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates an existing SavedQuery.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_saved_queries_patch_builder()` + `logging_billing_accounts_locations_saved_queries_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SavedQuery,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_saved_queries_patch_builder(
        client, name, updateMask, body,
    )?;
    logging_billing_accounts_locations_saved_queries_patch_execute(builder)
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_logs_delete_execute()` to send, or `logging_billing_accounts_logs_delete` for simplest API.

pub fn logging_billing_accounts_logs_delete_builder(
    client: &SimpleHttpClient,
    logName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+logName}", logName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_logs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_logs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_logs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_logs_delete_builder()` + `logging_billing_accounts_logs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_logs_delete(
    client: &SimpleHttpClient,
    logName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_logs_delete_builder(client, logName)?;
    logging_billing_accounts_logs_delete_execute(builder)
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_logs_list_execute()` to send, or `logging_billing_accounts_logs_list` for simplest API.

pub fn logging_billing_accounts_logs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resourceNames",
            resourceNames
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_logs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_logs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_logs_list_builder()` + `logging_billing_accounts_logs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_logs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_logs_list_builder(
        client,
        parent,
        pageSize,
        pageToken,
        resourceNames,
    )?;
    logging_billing_accounts_logs_list_execute(builder)
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_sinks_create_execute()` to send, or `logging_billing_accounts_sinks_create` for simplest API.

pub fn logging_billing_accounts_sinks_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    body: &LogSink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/sinks", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customWriterIdentity",
            customWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "uniqueWriterIdentity",
            uniqueWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_sinks_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_sinks_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_sinks_create_builder()` + `logging_billing_accounts_sinks_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_create(
    client: &SimpleHttpClient,
    parent: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    body: &LogSink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_sinks_create_builder(
        client,
        parent,
        customWriterIdentity,
        uniqueWriterIdentity,
        body,
    )?;
    logging_billing_accounts_sinks_create_execute(builder)
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_sinks_delete_execute()` to send, or `logging_billing_accounts_sinks_delete` for simplest API.

pub fn logging_billing_accounts_sinks_delete_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_sinks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_sinks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_sinks_delete_builder()` + `logging_billing_accounts_sinks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_delete(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_sinks_delete_builder(client, sinkName)?;
    logging_billing_accounts_sinks_delete_execute(builder)
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_sinks_get_execute()` to send, or `logging_billing_accounts_sinks_get` for simplest API.

pub fn logging_billing_accounts_sinks_get_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_sinks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_sinks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_sinks_get_builder()` + `logging_billing_accounts_sinks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_get(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_sinks_get_builder(client, sinkName)?;
    logging_billing_accounts_sinks_get_execute(builder)
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_sinks_list_execute()` to send, or `logging_billing_accounts_sinks_list` for simplest API.

pub fn logging_billing_accounts_sinks_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/sinks", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_sinks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_sinks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_sinks_list_builder()` + `logging_billing_accounts_sinks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_billing_accounts_sinks_list_builder(client, parent, filter, pageSize, pageToken)?;
    logging_billing_accounts_sinks_list_execute(builder)
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_sinks_patch_execute()` to send, or `logging_billing_accounts_sinks_patch` for simplest API.

pub fn logging_billing_accounts_sinks_patch_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customWriterIdentity",
            customWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "uniqueWriterIdentity",
            uniqueWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_sinks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_sinks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_sinks_patch_builder()` + `logging_billing_accounts_sinks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_patch(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_sinks_patch_builder(
        client,
        sinkName,
        customWriterIdentity,
        uniqueWriterIdentity,
        updateMask,
        body,
    )?;
    logging_billing_accounts_sinks_patch_execute(builder)
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_sinks_update_execute()` to send, or `logging_billing_accounts_sinks_update` for simplest API.

pub fn logging_billing_accounts_sinks_update_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customWriterIdentity",
            customWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "uniqueWriterIdentity",
            uniqueWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_billing_accounts_sinks_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_billing_accounts_sinks_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_sinks_update_builder()` + `logging_billing_accounts_sinks_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_update(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_sinks_update_builder(
        client,
        sinkName,
        customWriterIdentity,
        uniqueWriterIdentity,
        updateMask,
        body,
    )?;
    logging_billing_accounts_sinks_update_execute(builder)
}

/// GET v2/entries:copy
/// Copies a set of log entries from a log bucket to a Cloud Storage bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_entries_copy_execute()` to send, or `logging_entries_copy` for simplest API.

pub fn logging_entries_copy_builder(
    client: &SimpleHttpClient,
    body: &CopyLogEntriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/entries:copy",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/entries:copy
/// Copies a set of log entries from a log bucket to a Cloud Storage bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_entries_copy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_entries_copy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_copy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/entries:copy
/// Copies a set of log entries from a log bucket to a Cloud Storage bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_entries_copy_builder()` + `logging_entries_copy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_copy(
    client: &SimpleHttpClient,
    body: &CopyLogEntriesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_entries_copy_builder(client, body)?;
    logging_entries_copy_execute(builder)
}

/// GET v2/entries:list
/// Lists log entries. Use this method to retrieve log entries that originated from a `project/folder/organization/billing` account. For ways to export log entries, see Routing overview (<https://docs.cloud.google.`com/logging/docs/routing/overview`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_entries_list_execute()` to send, or `logging_entries_list` for simplest API.

pub fn logging_entries_list_builder(
    client: &SimpleHttpClient,
    body: &ListLogEntriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/entries:list",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/entries:list
/// Lists log entries. Use this method to retrieve log entries that originated from a `project/folder/organization/billing` account. For ways to export log entries, see Routing overview (<https://docs.cloud.google.`com/logging/docs/routing/overview`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_entries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_entries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogEntriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogEntriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/entries:list
/// Lists log entries. Use this method to retrieve log entries that originated from a `project/folder/organization/billing` account. For ways to export log entries, see Routing overview (<https://docs.cloud.google.`com/logging/docs/routing/overview`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_entries_list_builder()` + `logging_entries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_list(
    client: &SimpleHttpClient,
    body: &ListLogEntriesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogEntriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_entries_list_builder(client, body)?;
    logging_entries_list_execute(builder)
}

/// GET v2/entries:tail
/// Streaming read of log entries as they are received. Until the stream is terminated, it will continue reading logs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_entries_tail_execute()` to send, or `logging_entries_tail` for simplest API.

pub fn logging_entries_tail_builder(
    client: &SimpleHttpClient,
    body: &TailLogEntriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/entries:tail",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/entries:tail
/// Streaming read of log entries as they are received. Until the stream is terminated, it will continue reading logs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_entries_tail_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_entries_tail_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_tail_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TailLogEntriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TailLogEntriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/entries:tail
/// Streaming read of log entries as they are received. Until the stream is terminated, it will continue reading logs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_entries_tail_builder()` + `logging_entries_tail_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_tail(
    client: &SimpleHttpClient,
    body: &TailLogEntriesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TailLogEntriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_entries_tail_builder(client, body)?;
    logging_entries_tail_execute(builder)
}

/// GET v2/entries:write
/// Writes log entries to Logging. This API method is the only way to send log entries to Logging. This method is used, directly or indirectly, by the Logging agent (fluentd) and all logging libraries configured to use Logging. A single request may contain log entries for a maximum of 1000 different resource names (projects, organizations, billing accounts or folders), where the resource name for a log entry is determined from its `logName` field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_entries_write_execute()` to send, or `logging_entries_write` for simplest API.

pub fn logging_entries_write_builder(
    client: &SimpleHttpClient,
    body: &WriteLogEntriesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/entries:write",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/entries:write
/// Writes log entries to Logging. This API method is the only way to send log entries to Logging. This method is used, directly or indirectly, by the Logging agent (fluentd) and all logging libraries configured to use Logging. A single request may contain log entries for a maximum of 1000 different resource names (projects, organizations, billing accounts or folders), where the resource name for a log entry is determined from its `logName` field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_entries_write_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_entries_write_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WriteLogEntriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WriteLogEntriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/entries:write
/// Writes log entries to Logging. This API method is the only way to send log entries to Logging. This method is used, directly or indirectly, by the Logging agent (fluentd) and all logging libraries configured to use Logging. A single request may contain log entries for a maximum of 1000 different resource names (projects, organizations, billing accounts or folders), where the resource name for a log entry is determined from its `logName` field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_entries_write_builder()` + `logging_entries_write_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_write(
    client: &SimpleHttpClient,
    body: &WriteLogEntriesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WriteLogEntriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_entries_write_builder(client, body)?;
    logging_entries_write_execute(builder)
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_exclusions_create_execute()` to send, or `logging_exclusions_create` for simplest API.

pub fn logging_exclusions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &LogExclusion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/exclusions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_exclusions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_exclusions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_exclusions_create_builder()` + `logging_exclusions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &LogExclusion,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_exclusions_create_builder(client, parent, body)?;
    logging_exclusions_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_exclusions_delete_execute()` to send, or `logging_exclusions_delete` for simplest API.

pub fn logging_exclusions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_exclusions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_exclusions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_exclusions_delete_builder()` + `logging_exclusions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_exclusions_delete_builder(client, name)?;
    logging_exclusions_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_exclusions_get_execute()` to send, or `logging_exclusions_get` for simplest API.

pub fn logging_exclusions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_exclusions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_exclusions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_exclusions_get_builder()` + `logging_exclusions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_exclusions_get_builder(client, name)?;
    logging_exclusions_get_execute(builder)
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_exclusions_list_execute()` to send, or `logging_exclusions_list` for simplest API.

pub fn logging_exclusions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/exclusions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_exclusions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_exclusions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListExclusionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_exclusions_list_builder()` + `logging_exclusions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_exclusions_list_builder(client, parent, pageSize, pageToken)?;
    logging_exclusions_list_execute(builder)
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_exclusions_patch_execute()` to send, or `logging_exclusions_patch` for simplest API.

pub fn logging_exclusions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogExclusion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_exclusions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_exclusions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_exclusions_patch_builder()` + `logging_exclusions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogExclusion,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_exclusions_patch_builder(client, name, updateMask, body)?;
    logging_exclusions_patch_execute(builder)
}

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_get_cmek_settings_execute()` to send, or `logging_folders_get_cmek_settings` for simplest API.

pub fn logging_folders_get_cmek_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/cmekSettings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_get_cmek_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_get_cmek_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_get_cmek_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CmekSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_get_cmek_settings_builder()` + `logging_folders_get_cmek_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_get_cmek_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_get_cmek_settings_builder(client, name)?;
    logging_folders_get_cmek_settings_execute(builder)
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_get_settings_execute()` to send, or `logging_folders_get_settings` for simplest API.

pub fn logging_folders_get_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/settings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_get_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_get_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_get_settings_builder()` + `logging_folders_get_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_get_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_get_settings_builder(client, name)?;
    logging_folders_get_settings_execute(builder)
}

/// GET v2/{+name}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn't supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_update_settings_execute()` to send, or `logging_folders_update_settings` for simplest API.

pub fn logging_folders_update_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Settings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/settings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn't supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_update_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_update_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_update_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn't supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_update_settings_builder()` + `logging_folders_update_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_update_settings(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Settings,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_update_settings_builder(client, name, updateMask, body)?;
    logging_folders_update_settings_execute(builder)
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_exclusions_create_execute()` to send, or `logging_folders_exclusions_create` for simplest API.

pub fn logging_folders_exclusions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &LogExclusion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/exclusions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_exclusions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_exclusions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_exclusions_create_builder()` + `logging_folders_exclusions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &LogExclusion,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_exclusions_create_builder(client, parent, body)?;
    logging_folders_exclusions_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_exclusions_delete_execute()` to send, or `logging_folders_exclusions_delete` for simplest API.

pub fn logging_folders_exclusions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_exclusions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_exclusions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_exclusions_delete_builder()` + `logging_folders_exclusions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_exclusions_delete_builder(client, name)?;
    logging_folders_exclusions_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_exclusions_get_execute()` to send, or `logging_folders_exclusions_get` for simplest API.

pub fn logging_folders_exclusions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_exclusions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_exclusions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_exclusions_get_builder()` + `logging_folders_exclusions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_exclusions_get_builder(client, name)?;
    logging_folders_exclusions_get_execute(builder)
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_exclusions_list_execute()` to send, or `logging_folders_exclusions_list` for simplest API.

pub fn logging_folders_exclusions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/exclusions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_exclusions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_exclusions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListExclusionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_exclusions_list_builder()` + `logging_folders_exclusions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_exclusions_list_builder(client, parent, pageSize, pageToken)?;
    logging_folders_exclusions_list_execute(builder)
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_exclusions_patch_execute()` to send, or `logging_folders_exclusions_patch` for simplest API.

pub fn logging_folders_exclusions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogExclusion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_exclusions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_exclusions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_exclusions_patch_builder()` + `logging_folders_exclusions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogExclusion,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_exclusions_patch_builder(client, name, updateMask, body)?;
    logging_folders_exclusions_patch_execute(builder)
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_get_execute()` to send, or `logging_folders_locations_get` for simplest API.

pub fn logging_folders_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_get_builder()` + `logging_folders_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_get_builder(client, name)?;
    logging_folders_locations_get_execute(builder)
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_list_execute()` to send, or `logging_folders_locations_list` for simplest API.

pub fn logging_folders_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/locations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extraLocationTypes",
            extraLocationTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_list_builder()` + `logging_folders_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    logging_folders_locations_list_execute(builder)
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_create_execute()` to send, or `logging_folders_locations_buckets_create` for simplest API.

pub fn logging_folders_locations_buckets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/buckets", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "bucketId",
            bucketId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_create_builder()` + `logging_folders_locations_buckets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_create(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_create_builder(client, parent, bucketId, body)?;
    logging_folders_locations_buckets_create_execute(builder)
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_create_async_execute()` to send, or `logging_folders_locations_buckets_create_async` for simplest API.

pub fn logging_folders_locations_buckets_create_async_builder(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/buckets:createAsync",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "bucketId",
            bucketId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_create_async_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_create_async_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_create_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_create_async_builder()` + `logging_folders_locations_buckets_create_async_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_create_async(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_create_async_builder(client, parent, bucketId, body)?;
    logging_folders_locations_buckets_create_async_execute(builder)
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_delete_execute()` to send, or `logging_folders_locations_buckets_delete` for simplest API.

pub fn logging_folders_locations_buckets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_delete_builder()` + `logging_folders_locations_buckets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_delete_builder(client, name)?;
    logging_folders_locations_buckets_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_get_execute()` to send, or `logging_folders_locations_buckets_get` for simplest API.

pub fn logging_folders_locations_buckets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_get_builder()` + `logging_folders_locations_buckets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_get_builder(client, name)?;
    logging_folders_locations_buckets_get_execute(builder)
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_list_execute()` to send, or `logging_folders_locations_buckets_list` for simplest API.

pub fn logging_folders_locations_buckets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/buckets", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBucketsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_list_builder()` + `logging_folders_locations_buckets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_list_builder(client, parent, pageSize, pageToken)?;
    logging_folders_locations_buckets_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_patch_execute()` to send, or `logging_folders_locations_buckets_patch` for simplest API.

pub fn logging_folders_locations_buckets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_patch_builder()` + `logging_folders_locations_buckets_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_patch_builder(client, name, updateMask, body)?;
    logging_folders_locations_buckets_patch_execute(builder)
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_undelete_execute()` to send, or `logging_folders_locations_buckets_undelete` for simplest API.

pub fn logging_folders_locations_buckets_undelete_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndeleteBucketRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:undelete", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_undelete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_undelete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_undelete_builder()` + `logging_folders_locations_buckets_undelete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_undelete(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndeleteBucketRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_undelete_builder(client, name, body)?;
    logging_folders_locations_buckets_undelete_execute(builder)
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_update_async_execute()` to send, or `logging_folders_locations_buckets_update_async` for simplest API.

pub fn logging_folders_locations_buckets_update_async_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:updateAsync", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_update_async_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_update_async_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_update_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_update_async_builder()` + `logging_folders_locations_buckets_update_async_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_update_async(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_update_async_builder(client, name, updateMask, body)?;
    logging_folders_locations_buckets_update_async_execute(builder)
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_links_create_execute()` to send, or `logging_folders_locations_buckets_links_create` for simplest API.

pub fn logging_folders_locations_buckets_links_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    linkId: Option<&str>,
    body: &Link,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/links", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "linkId",
            linkId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_links_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_links_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_links_create_builder()` + `logging_folders_locations_buckets_links_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_create(
    client: &SimpleHttpClient,
    parent: &str,
    linkId: Option<&str>,
    body: &Link,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_links_create_builder(client, parent, linkId, body)?;
    logging_folders_locations_buckets_links_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_links_delete_execute()` to send, or `logging_folders_locations_buckets_links_delete` for simplest API.

pub fn logging_folders_locations_buckets_links_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_links_delete_builder()` + `logging_folders_locations_buckets_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_links_delete_builder(client, name)?;
    logging_folders_locations_buckets_links_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_links_get_execute()` to send, or `logging_folders_locations_buckets_links_get` for simplest API.

pub fn logging_folders_locations_buckets_links_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_links_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_links_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_links_get_builder()` + `logging_folders_locations_buckets_links_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_links_get_builder(client, name)?;
    logging_folders_locations_buckets_links_get_execute(builder)
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_links_list_execute()` to send, or `logging_folders_locations_buckets_links_list` for simplest API.

pub fn logging_folders_locations_buckets_links_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/links", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_links_list_builder()` + `logging_folders_locations_buckets_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_links_list_builder(client, parent, pageSize, pageToken)?;
    logging_folders_locations_buckets_links_list_execute(builder)
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_create_execute()` to send, or `logging_folders_locations_buckets_views_create` for simplest API.

pub fn logging_folders_locations_buckets_views_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    viewId: Option<&str>,
    body: &LogView,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/views", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "viewId",
            viewId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_views_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_views_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_create_builder()` + `logging_folders_locations_buckets_views_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_create(
    client: &SimpleHttpClient,
    parent: &str,
    viewId: Option<&str>,
    body: &LogView,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_views_create_builder(client, parent, viewId, body)?;
    logging_folders_locations_buckets_views_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_delete_execute()` to send, or `logging_folders_locations_buckets_views_delete` for simplest API.

pub fn logging_folders_locations_buckets_views_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_views_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_views_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_delete_builder()` + `logging_folders_locations_buckets_views_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_views_delete_builder(client, name)?;
    logging_folders_locations_buckets_views_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_get_execute()` to send, or `logging_folders_locations_buckets_views_get` for simplest API.

pub fn logging_folders_locations_buckets_views_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_views_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_views_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_get_builder()` + `logging_folders_locations_buckets_views_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_views_get_builder(client, name)?;
    logging_folders_locations_buckets_views_get_execute(builder)
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_get_iam_policy_execute()` to send, or `logging_folders_locations_buckets_views_get_iam_policy` for simplest API.

pub fn logging_folders_locations_buckets_views_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_views_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_views_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_get_iam_policy_builder()` + `logging_folders_locations_buckets_views_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_views_get_iam_policy_builder(client, resource, body)?;
    logging_folders_locations_buckets_views_get_iam_policy_execute(builder)
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_list_execute()` to send, or `logging_folders_locations_buckets_views_list` for simplest API.

pub fn logging_folders_locations_buckets_views_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/views", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_views_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_views_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListViewsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_list_builder()` + `logging_folders_locations_buckets_views_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_views_list_builder(client, parent, pageSize, pageToken)?;
    logging_folders_locations_buckets_views_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_patch_execute()` to send, or `logging_folders_locations_buckets_views_patch` for simplest API.

pub fn logging_folders_locations_buckets_views_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogView,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_views_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_views_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_patch_builder()` + `logging_folders_locations_buckets_views_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogView,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_views_patch_builder(client, name, updateMask, body)?;
    logging_folders_locations_buckets_views_patch_execute(builder)
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_set_iam_policy_execute()` to send, or `logging_folders_locations_buckets_views_set_iam_policy` for simplest API.

pub fn logging_folders_locations_buckets_views_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_views_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_views_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_set_iam_policy_builder()` + `logging_folders_locations_buckets_views_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_views_set_iam_policy_builder(client, resource, body)?;
    logging_folders_locations_buckets_views_set_iam_policy_execute(builder)
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_test_iam_permissions_execute()` to send, or `logging_folders_locations_buckets_views_test_iam_permissions` for simplest API.

pub fn logging_folders_locations_buckets_views_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_views_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_views_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_test_iam_permissions_builder()` + `logging_folders_locations_buckets_views_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_views_test_iam_permissions_builder(
        client, resource, body,
    )?;
    logging_folders_locations_buckets_views_test_iam_permissions_execute(builder)
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_logs_list_execute()` to send, or `logging_folders_locations_buckets_views_logs_list` for simplest API.

pub fn logging_folders_locations_buckets_views_logs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resourceNames",
            resourceNames
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_buckets_views_logs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_buckets_views_logs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_logs_list_builder()` + `logging_folders_locations_buckets_views_logs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_logs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_views_logs_list_builder(
        client,
        parent,
        pageSize,
        pageToken,
        resourceNames,
    )?;
    logging_folders_locations_buckets_views_logs_list_execute(builder)
}

/// GET v2/{+parent}/logScopes
/// Creates a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_log_scopes_create_execute()` to send, or `logging_folders_locations_log_scopes_create` for simplest API.

pub fn logging_folders_locations_log_scopes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    logScopeId: Option<&str>,
    body: &LogScope,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logScopes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "logScopeId",
            logScopeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/logScopes
/// Creates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_log_scopes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_log_scopes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logScopes
/// Creates a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_log_scopes_create_builder()` + `logging_folders_locations_log_scopes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_create(
    client: &SimpleHttpClient,
    parent: &str,
    logScopeId: Option<&str>,
    body: &LogScope,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_log_scopes_create_builder(client, parent, logScopeId, body)?;
    logging_folders_locations_log_scopes_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_log_scopes_delete_execute()` to send, or `logging_folders_locations_log_scopes_delete` for simplest API.

pub fn logging_folders_locations_log_scopes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_log_scopes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_log_scopes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_log_scopes_delete_builder()` + `logging_folders_locations_log_scopes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_log_scopes_delete_builder(client, name)?;
    logging_folders_locations_log_scopes_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_log_scopes_get_execute()` to send, or `logging_folders_locations_log_scopes_get` for simplest API.

pub fn logging_folders_locations_log_scopes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_log_scopes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_log_scopes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_log_scopes_get_builder()` + `logging_folders_locations_log_scopes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_log_scopes_get_builder(client, name)?;
    logging_folders_locations_log_scopes_get_execute(builder)
}

/// GET v2/{+parent}/logScopes
/// Lists log scopes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_log_scopes_list_execute()` to send, or `logging_folders_locations_log_scopes_list` for simplest API.

pub fn logging_folders_locations_log_scopes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logScopes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/logScopes
/// Lists log scopes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_log_scopes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_log_scopes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogScopesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogScopesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logScopes
/// Lists log scopes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_log_scopes_list_builder()` + `logging_folders_locations_log_scopes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogScopesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_log_scopes_list_builder(client, parent, pageSize, pageToken)?;
    logging_folders_locations_log_scopes_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_log_scopes_patch_execute()` to send, or `logging_folders_locations_log_scopes_patch` for simplest API.

pub fn logging_folders_locations_log_scopes_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogScope,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_log_scopes_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_log_scopes_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_log_scopes_patch_builder()` + `logging_folders_locations_log_scopes_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogScope,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_log_scopes_patch_builder(client, name, updateMask, body)?;
    logging_folders_locations_log_scopes_patch_execute(builder)
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_operations_cancel_execute()` to send, or `logging_folders_locations_operations_cancel` for simplest API.

pub fn logging_folders_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_operations_cancel_builder()` + `logging_folders_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_operations_cancel_builder(client, name, body)?;
    logging_folders_locations_operations_cancel_execute(builder)
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_operations_get_execute()` to send, or `logging_folders_locations_operations_get` for simplest API.

pub fn logging_folders_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_operations_get_builder()` + `logging_folders_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_operations_get_builder(client, name)?;
    logging_folders_locations_operations_get_execute(builder)
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_operations_list_execute()` to send, or `logging_folders_locations_operations_list` for simplest API.

pub fn logging_folders_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/operations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_operations_list_builder()` + `logging_folders_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    logging_folders_locations_operations_list_execute(builder)
}

/// GET v2/{+parent}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_recent_queries_list_execute()` to send, or `logging_folders_locations_recent_queries_list` for simplest API.

pub fn logging_folders_locations_recent_queries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/recentQueries", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_recent_queries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_recent_queries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_recent_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRecentQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_recent_queries_list_builder()` + `logging_folders_locations_recent_queries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_recent_queries_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_recent_queries_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    logging_folders_locations_recent_queries_list_execute(builder)
}

/// GET v2/{+parent}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_saved_queries_create_execute()` to send, or `logging_folders_locations_saved_queries_create` for simplest API.

pub fn logging_folders_locations_saved_queries_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    savedQueryId: Option<&str>,
    body: &SavedQuery,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/savedQueries", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "savedQueryId",
            savedQueryId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_saved_queries_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_saved_queries_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_saved_queries_create_builder()` + `logging_folders_locations_saved_queries_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_create(
    client: &SimpleHttpClient,
    parent: &str,
    savedQueryId: Option<&str>,
    body: &SavedQuery,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_saved_queries_create_builder(client, parent, savedQueryId, body)?;
    logging_folders_locations_saved_queries_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_saved_queries_delete_execute()` to send, or `logging_folders_locations_saved_queries_delete` for simplest API.

pub fn logging_folders_locations_saved_queries_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_saved_queries_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_saved_queries_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_saved_queries_delete_builder()` + `logging_folders_locations_saved_queries_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_saved_queries_delete_builder(client, name)?;
    logging_folders_locations_saved_queries_delete_execute(builder)
}

/// GET v2/{+name}
/// Returns all data associated with the requested query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_saved_queries_get_execute()` to send, or `logging_folders_locations_saved_queries_get` for simplest API.

pub fn logging_folders_locations_saved_queries_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Returns all data associated with the requested query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_saved_queries_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_saved_queries_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Returns all data associated with the requested query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_saved_queries_get_builder()` + `logging_folders_locations_saved_queries_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_saved_queries_get_builder(client, name)?;
    logging_folders_locations_saved_queries_get_execute(builder)
}

/// GET v2/{+parent}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_saved_queries_list_execute()` to send, or `logging_folders_locations_saved_queries_list` for simplest API.

pub fn logging_folders_locations_saved_queries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/savedQueries", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_saved_queries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_saved_queries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSavedQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_saved_queries_list_builder()` + `logging_folders_locations_saved_queries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_saved_queries_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    logging_folders_locations_saved_queries_list_execute(builder)
}

/// GET v2/{+name}
/// Updates an existing SavedQuery.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_saved_queries_patch_execute()` to send, or `logging_folders_locations_saved_queries_patch` for simplest API.

pub fn logging_folders_locations_saved_queries_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SavedQuery,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates an existing SavedQuery.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_locations_saved_queries_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_locations_saved_queries_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates an existing SavedQuery.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_saved_queries_patch_builder()` + `logging_folders_locations_saved_queries_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SavedQuery,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_saved_queries_patch_builder(client, name, updateMask, body)?;
    logging_folders_locations_saved_queries_patch_execute(builder)
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_logs_delete_execute()` to send, or `logging_folders_logs_delete` for simplest API.

pub fn logging_folders_logs_delete_builder(
    client: &SimpleHttpClient,
    logName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+logName}", logName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_logs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_logs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_logs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_logs_delete_builder()` + `logging_folders_logs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_logs_delete(
    client: &SimpleHttpClient,
    logName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_logs_delete_builder(client, logName)?;
    logging_folders_logs_delete_execute(builder)
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_logs_list_execute()` to send, or `logging_folders_logs_list` for simplest API.

pub fn logging_folders_logs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resourceNames",
            resourceNames
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_logs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_logs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_logs_list_builder()` + `logging_folders_logs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_logs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_folders_logs_list_builder(client, parent, pageSize, pageToken, resourceNames)?;
    logging_folders_logs_list_execute(builder)
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_sinks_create_execute()` to send, or `logging_folders_sinks_create` for simplest API.

pub fn logging_folders_sinks_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    body: &LogSink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/sinks", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customWriterIdentity",
            customWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "uniqueWriterIdentity",
            uniqueWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_sinks_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_sinks_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_sinks_create_builder()` + `logging_folders_sinks_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_create(
    client: &SimpleHttpClient,
    parent: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    body: &LogSink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_sinks_create_builder(
        client,
        parent,
        customWriterIdentity,
        uniqueWriterIdentity,
        body,
    )?;
    logging_folders_sinks_create_execute(builder)
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_sinks_delete_execute()` to send, or `logging_folders_sinks_delete` for simplest API.

pub fn logging_folders_sinks_delete_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_sinks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_sinks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_sinks_delete_builder()` + `logging_folders_sinks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_delete(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_sinks_delete_builder(client, sinkName)?;
    logging_folders_sinks_delete_execute(builder)
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_sinks_get_execute()` to send, or `logging_folders_sinks_get` for simplest API.

pub fn logging_folders_sinks_get_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_sinks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_sinks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_sinks_get_builder()` + `logging_folders_sinks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_get(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_sinks_get_builder(client, sinkName)?;
    logging_folders_sinks_get_execute(builder)
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_sinks_list_execute()` to send, or `logging_folders_sinks_list` for simplest API.

pub fn logging_folders_sinks_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/sinks", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_sinks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_sinks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_sinks_list_builder()` + `logging_folders_sinks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_sinks_list_builder(client, parent, filter, pageSize, pageToken)?;
    logging_folders_sinks_list_execute(builder)
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_sinks_patch_execute()` to send, or `logging_folders_sinks_patch` for simplest API.

pub fn logging_folders_sinks_patch_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customWriterIdentity",
            customWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "uniqueWriterIdentity",
            uniqueWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_sinks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_sinks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_sinks_patch_builder()` + `logging_folders_sinks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_patch(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_sinks_patch_builder(
        client,
        sinkName,
        customWriterIdentity,
        uniqueWriterIdentity,
        updateMask,
        body,
    )?;
    logging_folders_sinks_patch_execute(builder)
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_sinks_update_execute()` to send, or `logging_folders_sinks_update` for simplest API.

pub fn logging_folders_sinks_update_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customWriterIdentity",
            customWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "uniqueWriterIdentity",
            uniqueWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_folders_sinks_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_folders_sinks_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_sinks_update_builder()` + `logging_folders_sinks_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_update(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_sinks_update_builder(
        client,
        sinkName,
        customWriterIdentity,
        uniqueWriterIdentity,
        updateMask,
        body,
    )?;
    logging_folders_sinks_update_execute(builder)
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_get_execute()` to send, or `logging_locations_get` for simplest API.

pub fn logging_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_get_builder()` + `logging_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_get_builder(client, name)?;
    logging_locations_get_execute(builder)
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_list_execute()` to send, or `logging_locations_list` for simplest API.

pub fn logging_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/locations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extraLocationTypes",
            extraLocationTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_list_builder()` + `logging_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    logging_locations_list_execute(builder)
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_create_execute()` to send, or `logging_locations_buckets_create` for simplest API.

pub fn logging_locations_buckets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/buckets", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "bucketId",
            bucketId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_create_builder()` + `logging_locations_buckets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_create(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_create_builder(client, parent, bucketId, body)?;
    logging_locations_buckets_create_execute(builder)
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_create_async_execute()` to send, or `logging_locations_buckets_create_async` for simplest API.

pub fn logging_locations_buckets_create_async_builder(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/buckets:createAsync",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "bucketId",
            bucketId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_create_async_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_create_async_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_create_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_create_async_builder()` + `logging_locations_buckets_create_async_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_create_async(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_create_async_builder(client, parent, bucketId, body)?;
    logging_locations_buckets_create_async_execute(builder)
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_delete_execute()` to send, or `logging_locations_buckets_delete` for simplest API.

pub fn logging_locations_buckets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_delete_builder()` + `logging_locations_buckets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_delete_builder(client, name)?;
    logging_locations_buckets_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_get_execute()` to send, or `logging_locations_buckets_get` for simplest API.

pub fn logging_locations_buckets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_get_builder()` + `logging_locations_buckets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_get_builder(client, name)?;
    logging_locations_buckets_get_execute(builder)
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_list_execute()` to send, or `logging_locations_buckets_list` for simplest API.

pub fn logging_locations_buckets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/buckets", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBucketsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_list_builder()` + `logging_locations_buckets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_list_builder(client, parent, pageSize, pageToken)?;
    logging_locations_buckets_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_patch_execute()` to send, or `logging_locations_buckets_patch` for simplest API.

pub fn logging_locations_buckets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_patch_builder()` + `logging_locations_buckets_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_patch_builder(client, name, updateMask, body)?;
    logging_locations_buckets_patch_execute(builder)
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_undelete_execute()` to send, or `logging_locations_buckets_undelete` for simplest API.

pub fn logging_locations_buckets_undelete_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndeleteBucketRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:undelete", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_undelete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_undelete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_undelete_builder()` + `logging_locations_buckets_undelete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_undelete(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndeleteBucketRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_undelete_builder(client, name, body)?;
    logging_locations_buckets_undelete_execute(builder)
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_update_async_execute()` to send, or `logging_locations_buckets_update_async` for simplest API.

pub fn logging_locations_buckets_update_async_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:updateAsync", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_update_async_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_update_async_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_update_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_update_async_builder()` + `logging_locations_buckets_update_async_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_update_async(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_update_async_builder(client, name, updateMask, body)?;
    logging_locations_buckets_update_async_execute(builder)
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_links_create_execute()` to send, or `logging_locations_buckets_links_create` for simplest API.

pub fn logging_locations_buckets_links_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    linkId: Option<&str>,
    body: &Link,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/links", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "linkId",
            linkId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_links_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_links_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_links_create_builder()` + `logging_locations_buckets_links_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_create(
    client: &SimpleHttpClient,
    parent: &str,
    linkId: Option<&str>,
    body: &Link,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_links_create_builder(client, parent, linkId, body)?;
    logging_locations_buckets_links_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_links_delete_execute()` to send, or `logging_locations_buckets_links_delete` for simplest API.

pub fn logging_locations_buckets_links_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_links_delete_builder()` + `logging_locations_buckets_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_links_delete_builder(client, name)?;
    logging_locations_buckets_links_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_links_get_execute()` to send, or `logging_locations_buckets_links_get` for simplest API.

pub fn logging_locations_buckets_links_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_links_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_links_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_links_get_builder()` + `logging_locations_buckets_links_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_links_get_builder(client, name)?;
    logging_locations_buckets_links_get_execute(builder)
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_links_list_execute()` to send, or `logging_locations_buckets_links_list` for simplest API.

pub fn logging_locations_buckets_links_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/links", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_links_list_builder()` + `logging_locations_buckets_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_locations_buckets_links_list_builder(client, parent, pageSize, pageToken)?;
    logging_locations_buckets_links_list_execute(builder)
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_create_execute()` to send, or `logging_locations_buckets_views_create` for simplest API.

pub fn logging_locations_buckets_views_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    viewId: Option<&str>,
    body: &LogView,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/views", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "viewId",
            viewId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_views_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_views_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_create_builder()` + `logging_locations_buckets_views_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_create(
    client: &SimpleHttpClient,
    parent: &str,
    viewId: Option<&str>,
    body: &LogView,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_views_create_builder(client, parent, viewId, body)?;
    logging_locations_buckets_views_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_delete_execute()` to send, or `logging_locations_buckets_views_delete` for simplest API.

pub fn logging_locations_buckets_views_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_views_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_views_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_delete_builder()` + `logging_locations_buckets_views_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_views_delete_builder(client, name)?;
    logging_locations_buckets_views_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_get_execute()` to send, or `logging_locations_buckets_views_get` for simplest API.

pub fn logging_locations_buckets_views_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_views_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_views_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_get_builder()` + `logging_locations_buckets_views_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_views_get_builder(client, name)?;
    logging_locations_buckets_views_get_execute(builder)
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_get_iam_policy_execute()` to send, or `logging_locations_buckets_views_get_iam_policy` for simplest API.

pub fn logging_locations_buckets_views_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_views_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_views_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_get_iam_policy_builder()` + `logging_locations_buckets_views_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_views_get_iam_policy_builder(client, resource, body)?;
    logging_locations_buckets_views_get_iam_policy_execute(builder)
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_list_execute()` to send, or `logging_locations_buckets_views_list` for simplest API.

pub fn logging_locations_buckets_views_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/views", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_views_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_views_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListViewsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_list_builder()` + `logging_locations_buckets_views_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_locations_buckets_views_list_builder(client, parent, pageSize, pageToken)?;
    logging_locations_buckets_views_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_patch_execute()` to send, or `logging_locations_buckets_views_patch` for simplest API.

pub fn logging_locations_buckets_views_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogView,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_views_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_views_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_patch_builder()` + `logging_locations_buckets_views_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogView,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_views_patch_builder(client, name, updateMask, body)?;
    logging_locations_buckets_views_patch_execute(builder)
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_set_iam_policy_execute()` to send, or `logging_locations_buckets_views_set_iam_policy` for simplest API.

pub fn logging_locations_buckets_views_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_views_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_views_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_set_iam_policy_builder()` + `logging_locations_buckets_views_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_views_set_iam_policy_builder(client, resource, body)?;
    logging_locations_buckets_views_set_iam_policy_execute(builder)
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_test_iam_permissions_execute()` to send, or `logging_locations_buckets_views_test_iam_permissions` for simplest API.

pub fn logging_locations_buckets_views_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_buckets_views_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_buckets_views_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_test_iam_permissions_builder()` + `logging_locations_buckets_views_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_locations_buckets_views_test_iam_permissions_builder(client, resource, body)?;
    logging_locations_buckets_views_test_iam_permissions_execute(builder)
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_operations_cancel_execute()` to send, or `logging_locations_operations_cancel` for simplest API.

pub fn logging_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_operations_cancel_builder()` + `logging_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_operations_cancel_builder(client, name, body)?;
    logging_locations_operations_cancel_execute(builder)
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_operations_get_execute()` to send, or `logging_locations_operations_get` for simplest API.

pub fn logging_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_operations_get_builder()` + `logging_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_operations_get_builder(client, name)?;
    logging_locations_operations_get_execute(builder)
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_operations_list_execute()` to send, or `logging_locations_operations_list` for simplest API.

pub fn logging_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/operations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_operations_list_builder()` + `logging_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    logging_locations_operations_list_execute(builder)
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_logs_delete_execute()` to send, or `logging_logs_delete` for simplest API.

pub fn logging_logs_delete_builder(
    client: &SimpleHttpClient,
    logName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+logName}", logName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_logs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_logs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_logs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_logs_delete_builder()` + `logging_logs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_logs_delete(
    client: &SimpleHttpClient,
    logName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_logs_delete_builder(client, logName)?;
    logging_logs_delete_execute(builder)
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_logs_list_execute()` to send, or `logging_logs_list` for simplest API.

pub fn logging_logs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resourceNames",
            resourceNames
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_logs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_logs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_logs_list_builder()` + `logging_logs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_logs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_logs_list_builder(client, parent, pageSize, pageToken, resourceNames)?;
    logging_logs_list_execute(builder)
}

/// GET v2/monitoredResourceDescriptors
/// Lists the descriptors for monitored resource types used by Logging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_monitored_resource_descriptors_list_execute()` to send, or `logging_monitored_resource_descriptors_list` for simplest API.

pub fn logging_monitored_resource_descriptors_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/monitoredResourceDescriptors",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/monitoredResourceDescriptors
/// Lists the descriptors for monitored resource types used by Logging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_monitored_resource_descriptors_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_monitored_resource_descriptors_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_monitored_resource_descriptors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMonitoredResourceDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMonitoredResourceDescriptorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/monitoredResourceDescriptors
/// Lists the descriptors for monitored resource types used by Logging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_monitored_resource_descriptors_list_builder()` + `logging_monitored_resource_descriptors_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_monitored_resource_descriptors_list(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMonitoredResourceDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = logging_monitored_resource_descriptors_list_builder(client, pageSize, pageToken)?;
    logging_monitored_resource_descriptors_list_execute(builder)
}

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_get_cmek_settings_execute()` to send, or `logging_organizations_get_cmek_settings` for simplest API.

pub fn logging_organizations_get_cmek_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/cmekSettings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_get_cmek_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_get_cmek_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_get_cmek_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CmekSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_get_cmek_settings_builder()` + `logging_organizations_get_cmek_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_get_cmek_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_get_cmek_settings_builder(client, name)?;
    logging_organizations_get_cmek_settings_execute(builder)
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_get_settings_execute()` to send, or `logging_organizations_get_settings` for simplest API.

pub fn logging_organizations_get_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/settings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_get_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_get_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_get_settings_builder()` + `logging_organizations_get_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_get_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_get_settings_builder(client, name)?;
    logging_organizations_get_settings_execute(builder)
}

/// GET v2/{+name}/cmekSettings
/// Updates the Log Router CMEK settings for the given resource.Note: CMEK for the Log Router can currently only be configured for Google Cloud organizations. Once configured, it applies to all projects and folders in the Google Cloud organization.UpdateCmekSettings fails when any of the following are `true`: The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_update_cmek_settings_execute()` to send, or `logging_organizations_update_cmek_settings` for simplest API.

pub fn logging_organizations_update_cmek_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &CmekSettings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/cmekSettings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}/cmekSettings
/// Updates the Log Router CMEK settings for the given resource.Note: CMEK for the Log Router can currently only be configured for Google Cloud organizations. Once configured, it applies to all projects and folders in the Google Cloud organization.UpdateCmekSettings fails when any of the following are `true`: The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_update_cmek_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_update_cmek_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_update_cmek_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CmekSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/cmekSettings
/// Updates the Log Router CMEK settings for the given resource.Note: CMEK for the Log Router can currently only be configured for Google Cloud organizations. Once configured, it applies to all projects and folders in the Google Cloud organization.UpdateCmekSettings fails when any of the following are `true`: The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_update_cmek_settings_builder()` + `logging_organizations_update_cmek_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_update_cmek_settings(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &CmekSettings,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_organizations_update_cmek_settings_builder(client, name, updateMask, body)?;
    logging_organizations_update_cmek_settings_execute(builder)
}

/// GET v2/{+name}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn't supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_update_settings_execute()` to send, or `logging_organizations_update_settings` for simplest API.

pub fn logging_organizations_update_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Settings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/settings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn't supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_update_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_update_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_update_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn't supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_update_settings_builder()` + `logging_organizations_update_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_update_settings(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Settings,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_update_settings_builder(client, name, updateMask, body)?;
    logging_organizations_update_settings_execute(builder)
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_exclusions_create_execute()` to send, or `logging_organizations_exclusions_create` for simplest API.

pub fn logging_organizations_exclusions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &LogExclusion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/exclusions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_exclusions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_exclusions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_exclusions_create_builder()` + `logging_organizations_exclusions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &LogExclusion,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_exclusions_create_builder(client, parent, body)?;
    logging_organizations_exclusions_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_exclusions_delete_execute()` to send, or `logging_organizations_exclusions_delete` for simplest API.

pub fn logging_organizations_exclusions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_exclusions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_exclusions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_exclusions_delete_builder()` + `logging_organizations_exclusions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_exclusions_delete_builder(client, name)?;
    logging_organizations_exclusions_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_exclusions_get_execute()` to send, or `logging_organizations_exclusions_get` for simplest API.

pub fn logging_organizations_exclusions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_exclusions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_exclusions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_exclusions_get_builder()` + `logging_organizations_exclusions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_exclusions_get_builder(client, name)?;
    logging_organizations_exclusions_get_execute(builder)
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_exclusions_list_execute()` to send, or `logging_organizations_exclusions_list` for simplest API.

pub fn logging_organizations_exclusions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/exclusions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_exclusions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_exclusions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListExclusionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_exclusions_list_builder()` + `logging_organizations_exclusions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_organizations_exclusions_list_builder(client, parent, pageSize, pageToken)?;
    logging_organizations_exclusions_list_execute(builder)
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_exclusions_patch_execute()` to send, or `logging_organizations_exclusions_patch` for simplest API.

pub fn logging_organizations_exclusions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogExclusion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_exclusions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_exclusions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_exclusions_patch_builder()` + `logging_organizations_exclusions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogExclusion,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_exclusions_patch_builder(client, name, updateMask, body)?;
    logging_organizations_exclusions_patch_execute(builder)
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_get_execute()` to send, or `logging_organizations_locations_get` for simplest API.

pub fn logging_organizations_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_get_builder()` + `logging_organizations_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_get_builder(client, name)?;
    logging_organizations_locations_get_execute(builder)
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_list_execute()` to send, or `logging_organizations_locations_list` for simplest API.

pub fn logging_organizations_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/locations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extraLocationTypes",
            extraLocationTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_list_builder()` + `logging_organizations_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    logging_organizations_locations_list_execute(builder)
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_create_execute()` to send, or `logging_organizations_locations_buckets_create` for simplest API.

pub fn logging_organizations_locations_buckets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/buckets", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "bucketId",
            bucketId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_create_builder()` + `logging_organizations_locations_buckets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_create(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_organizations_locations_buckets_create_builder(client, parent, bucketId, body)?;
    logging_organizations_locations_buckets_create_execute(builder)
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_create_async_execute()` to send, or `logging_organizations_locations_buckets_create_async` for simplest API.

pub fn logging_organizations_locations_buckets_create_async_builder(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/buckets:createAsync",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "bucketId",
            bucketId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_create_async_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_create_async_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_create_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_create_async_builder()` + `logging_organizations_locations_buckets_create_async_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_create_async(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_create_async_builder(
        client, parent, bucketId, body,
    )?;
    logging_organizations_locations_buckets_create_async_execute(builder)
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_delete_execute()` to send, or `logging_organizations_locations_buckets_delete` for simplest API.

pub fn logging_organizations_locations_buckets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_delete_builder()` + `logging_organizations_locations_buckets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_delete_builder(client, name)?;
    logging_organizations_locations_buckets_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_get_execute()` to send, or `logging_organizations_locations_buckets_get` for simplest API.

pub fn logging_organizations_locations_buckets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_get_builder()` + `logging_organizations_locations_buckets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_get_builder(client, name)?;
    logging_organizations_locations_buckets_get_execute(builder)
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_list_execute()` to send, or `logging_organizations_locations_buckets_list` for simplest API.

pub fn logging_organizations_locations_buckets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/buckets", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBucketsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_list_builder()` + `logging_organizations_locations_buckets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_organizations_locations_buckets_list_builder(client, parent, pageSize, pageToken)?;
    logging_organizations_locations_buckets_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_patch_execute()` to send, or `logging_organizations_locations_buckets_patch` for simplest API.

pub fn logging_organizations_locations_buckets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_patch_builder()` + `logging_organizations_locations_buckets_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_organizations_locations_buckets_patch_builder(client, name, updateMask, body)?;
    logging_organizations_locations_buckets_patch_execute(builder)
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_undelete_execute()` to send, or `logging_organizations_locations_buckets_undelete` for simplest API.

pub fn logging_organizations_locations_buckets_undelete_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndeleteBucketRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:undelete", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_undelete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_undelete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_undelete_builder()` + `logging_organizations_locations_buckets_undelete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_undelete(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndeleteBucketRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_undelete_builder(client, name, body)?;
    logging_organizations_locations_buckets_undelete_execute(builder)
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_update_async_execute()` to send, or `logging_organizations_locations_buckets_update_async` for simplest API.

pub fn logging_organizations_locations_buckets_update_async_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:updateAsync", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_update_async_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_update_async_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_update_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_update_async_builder()` + `logging_organizations_locations_buckets_update_async_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_update_async(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_update_async_builder(
        client, name, updateMask, body,
    )?;
    logging_organizations_locations_buckets_update_async_execute(builder)
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_links_create_execute()` to send, or `logging_organizations_locations_buckets_links_create` for simplest API.

pub fn logging_organizations_locations_buckets_links_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    linkId: Option<&str>,
    body: &Link,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/links", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "linkId",
            linkId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_links_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_links_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_links_create_builder()` + `logging_organizations_locations_buckets_links_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_create(
    client: &SimpleHttpClient,
    parent: &str,
    linkId: Option<&str>,
    body: &Link,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_organizations_locations_buckets_links_create_builder(client, parent, linkId, body)?;
    logging_organizations_locations_buckets_links_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_links_delete_execute()` to send, or `logging_organizations_locations_buckets_links_delete` for simplest API.

pub fn logging_organizations_locations_buckets_links_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_links_delete_builder()` + `logging_organizations_locations_buckets_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_links_delete_builder(client, name)?;
    logging_organizations_locations_buckets_links_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_links_get_execute()` to send, or `logging_organizations_locations_buckets_links_get` for simplest API.

pub fn logging_organizations_locations_buckets_links_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_links_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_links_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_links_get_builder()` + `logging_organizations_locations_buckets_links_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_links_get_builder(client, name)?;
    logging_organizations_locations_buckets_links_get_execute(builder)
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_links_list_execute()` to send, or `logging_organizations_locations_buckets_links_list` for simplest API.

pub fn logging_organizations_locations_buckets_links_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/links", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_links_list_builder()` + `logging_organizations_locations_buckets_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_links_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    logging_organizations_locations_buckets_links_list_execute(builder)
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_create_execute()` to send, or `logging_organizations_locations_buckets_views_create` for simplest API.

pub fn logging_organizations_locations_buckets_views_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    viewId: Option<&str>,
    body: &LogView,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/views", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "viewId",
            viewId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_views_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_views_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_create_builder()` + `logging_organizations_locations_buckets_views_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_create(
    client: &SimpleHttpClient,
    parent: &str,
    viewId: Option<&str>,
    body: &LogView,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_organizations_locations_buckets_views_create_builder(client, parent, viewId, body)?;
    logging_organizations_locations_buckets_views_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_delete_execute()` to send, or `logging_organizations_locations_buckets_views_delete` for simplest API.

pub fn logging_organizations_locations_buckets_views_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_views_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_views_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_delete_builder()` + `logging_organizations_locations_buckets_views_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_delete_builder(client, name)?;
    logging_organizations_locations_buckets_views_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_get_execute()` to send, or `logging_organizations_locations_buckets_views_get` for simplest API.

pub fn logging_organizations_locations_buckets_views_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_views_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_views_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_get_builder()` + `logging_organizations_locations_buckets_views_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_get_builder(client, name)?;
    logging_organizations_locations_buckets_views_get_execute(builder)
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_get_iam_policy_execute()` to send, or `logging_organizations_locations_buckets_views_get_iam_policy` for simplest API.

pub fn logging_organizations_locations_buckets_views_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_views_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_views_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_get_iam_policy_builder()` + `logging_organizations_locations_buckets_views_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_get_iam_policy_builder(
        client, resource, body,
    )?;
    logging_organizations_locations_buckets_views_get_iam_policy_execute(builder)
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_list_execute()` to send, or `logging_organizations_locations_buckets_views_list` for simplest API.

pub fn logging_organizations_locations_buckets_views_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/views", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_views_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_views_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListViewsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_list_builder()` + `logging_organizations_locations_buckets_views_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    logging_organizations_locations_buckets_views_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_patch_execute()` to send, or `logging_organizations_locations_buckets_views_patch` for simplest API.

pub fn logging_organizations_locations_buckets_views_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogView,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_views_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_views_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_patch_builder()` + `logging_organizations_locations_buckets_views_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogView,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_patch_builder(
        client, name, updateMask, body,
    )?;
    logging_organizations_locations_buckets_views_patch_execute(builder)
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_set_iam_policy_execute()` to send, or `logging_organizations_locations_buckets_views_set_iam_policy` for simplest API.

pub fn logging_organizations_locations_buckets_views_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_views_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_views_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_set_iam_policy_builder()` + `logging_organizations_locations_buckets_views_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_set_iam_policy_builder(
        client, resource, body,
    )?;
    logging_organizations_locations_buckets_views_set_iam_policy_execute(builder)
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_test_iam_permissions_execute()` to send, or `logging_organizations_locations_buckets_views_test_iam_permissions` for simplest API.

pub fn logging_organizations_locations_buckets_views_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_views_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_views_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_test_iam_permissions_builder()` + `logging_organizations_locations_buckets_views_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_test_iam_permissions_builder(
        client, resource, body,
    )?;
    logging_organizations_locations_buckets_views_test_iam_permissions_execute(builder)
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_logs_list_execute()` to send, or `logging_organizations_locations_buckets_views_logs_list` for simplest API.

pub fn logging_organizations_locations_buckets_views_logs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resourceNames",
            resourceNames
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_buckets_views_logs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_buckets_views_logs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_logs_list_builder()` + `logging_organizations_locations_buckets_views_logs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_logs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_logs_list_builder(
        client,
        parent,
        pageSize,
        pageToken,
        resourceNames,
    )?;
    logging_organizations_locations_buckets_views_logs_list_execute(builder)
}

/// GET v2/{+parent}/logScopes
/// Creates a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_log_scopes_create_execute()` to send, or `logging_organizations_locations_log_scopes_create` for simplest API.

pub fn logging_organizations_locations_log_scopes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    logScopeId: Option<&str>,
    body: &LogScope,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logScopes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "logScopeId",
            logScopeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/logScopes
/// Creates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_log_scopes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_log_scopes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logScopes
/// Creates a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_log_scopes_create_builder()` + `logging_organizations_locations_log_scopes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_create(
    client: &SimpleHttpClient,
    parent: &str,
    logScopeId: Option<&str>,
    body: &LogScope,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_log_scopes_create_builder(
        client, parent, logScopeId, body,
    )?;
    logging_organizations_locations_log_scopes_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_log_scopes_delete_execute()` to send, or `logging_organizations_locations_log_scopes_delete` for simplest API.

pub fn logging_organizations_locations_log_scopes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_log_scopes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_log_scopes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_log_scopes_delete_builder()` + `logging_organizations_locations_log_scopes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_log_scopes_delete_builder(client, name)?;
    logging_organizations_locations_log_scopes_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_log_scopes_get_execute()` to send, or `logging_organizations_locations_log_scopes_get` for simplest API.

pub fn logging_organizations_locations_log_scopes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_log_scopes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_log_scopes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_log_scopes_get_builder()` + `logging_organizations_locations_log_scopes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_log_scopes_get_builder(client, name)?;
    logging_organizations_locations_log_scopes_get_execute(builder)
}

/// GET v2/{+parent}/logScopes
/// Lists log scopes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_log_scopes_list_execute()` to send, or `logging_organizations_locations_log_scopes_list` for simplest API.

pub fn logging_organizations_locations_log_scopes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logScopes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/logScopes
/// Lists log scopes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_log_scopes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_log_scopes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogScopesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogScopesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logScopes
/// Lists log scopes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_log_scopes_list_builder()` + `logging_organizations_locations_log_scopes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogScopesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_log_scopes_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    logging_organizations_locations_log_scopes_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_log_scopes_patch_execute()` to send, or `logging_organizations_locations_log_scopes_patch` for simplest API.

pub fn logging_organizations_locations_log_scopes_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogScope,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_log_scopes_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_log_scopes_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_log_scopes_patch_builder()` + `logging_organizations_locations_log_scopes_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogScope,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_organizations_locations_log_scopes_patch_builder(client, name, updateMask, body)?;
    logging_organizations_locations_log_scopes_patch_execute(builder)
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_operations_cancel_execute()` to send, or `logging_organizations_locations_operations_cancel` for simplest API.

pub fn logging_organizations_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_operations_cancel_builder()` + `logging_organizations_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_operations_cancel_builder(client, name, body)?;
    logging_organizations_locations_operations_cancel_execute(builder)
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_operations_get_execute()` to send, or `logging_organizations_locations_operations_get` for simplest API.

pub fn logging_organizations_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_operations_get_builder()` + `logging_organizations_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_operations_get_builder(client, name)?;
    logging_organizations_locations_operations_get_execute(builder)
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_operations_list_execute()` to send, or `logging_organizations_locations_operations_list` for simplest API.

pub fn logging_organizations_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/operations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_operations_list_builder()` + `logging_organizations_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    logging_organizations_locations_operations_list_execute(builder)
}

/// GET v2/{+parent}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_recent_queries_list_execute()` to send, or `logging_organizations_locations_recent_queries_list` for simplest API.

pub fn logging_organizations_locations_recent_queries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/recentQueries", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_recent_queries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_recent_queries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_recent_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRecentQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_recent_queries_list_builder()` + `logging_organizations_locations_recent_queries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_recent_queries_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_recent_queries_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    logging_organizations_locations_recent_queries_list_execute(builder)
}

/// GET v2/{+parent}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_saved_queries_create_execute()` to send, or `logging_organizations_locations_saved_queries_create` for simplest API.

pub fn logging_organizations_locations_saved_queries_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    savedQueryId: Option<&str>,
    body: &SavedQuery,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/savedQueries", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "savedQueryId",
            savedQueryId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_saved_queries_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_saved_queries_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_saved_queries_create_builder()` + `logging_organizations_locations_saved_queries_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_create(
    client: &SimpleHttpClient,
    parent: &str,
    savedQueryId: Option<&str>,
    body: &SavedQuery,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_saved_queries_create_builder(
        client,
        parent,
        savedQueryId,
        body,
    )?;
    logging_organizations_locations_saved_queries_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_saved_queries_delete_execute()` to send, or `logging_organizations_locations_saved_queries_delete` for simplest API.

pub fn logging_organizations_locations_saved_queries_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_saved_queries_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_saved_queries_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_saved_queries_delete_builder()` + `logging_organizations_locations_saved_queries_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_saved_queries_delete_builder(client, name)?;
    logging_organizations_locations_saved_queries_delete_execute(builder)
}

/// GET v2/{+name}
/// Returns all data associated with the requested query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_saved_queries_get_execute()` to send, or `logging_organizations_locations_saved_queries_get` for simplest API.

pub fn logging_organizations_locations_saved_queries_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Returns all data associated with the requested query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_saved_queries_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_saved_queries_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Returns all data associated with the requested query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_saved_queries_get_builder()` + `logging_organizations_locations_saved_queries_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_saved_queries_get_builder(client, name)?;
    logging_organizations_locations_saved_queries_get_execute(builder)
}

/// GET v2/{+parent}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_saved_queries_list_execute()` to send, or `logging_organizations_locations_saved_queries_list` for simplest API.

pub fn logging_organizations_locations_saved_queries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/savedQueries", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_saved_queries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_saved_queries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSavedQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_saved_queries_list_builder()` + `logging_organizations_locations_saved_queries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_saved_queries_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    logging_organizations_locations_saved_queries_list_execute(builder)
}

/// GET v2/{+name}
/// Updates an existing SavedQuery.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_saved_queries_patch_execute()` to send, or `logging_organizations_locations_saved_queries_patch` for simplest API.

pub fn logging_organizations_locations_saved_queries_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SavedQuery,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates an existing SavedQuery.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_locations_saved_queries_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_locations_saved_queries_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates an existing SavedQuery.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_saved_queries_patch_builder()` + `logging_organizations_locations_saved_queries_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SavedQuery,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_saved_queries_patch_builder(
        client, name, updateMask, body,
    )?;
    logging_organizations_locations_saved_queries_patch_execute(builder)
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_logs_delete_execute()` to send, or `logging_organizations_logs_delete` for simplest API.

pub fn logging_organizations_logs_delete_builder(
    client: &SimpleHttpClient,
    logName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+logName}", logName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_logs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_logs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_logs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_logs_delete_builder()` + `logging_organizations_logs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_logs_delete(
    client: &SimpleHttpClient,
    logName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_logs_delete_builder(client, logName)?;
    logging_organizations_logs_delete_execute(builder)
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_logs_list_execute()` to send, or `logging_organizations_logs_list` for simplest API.

pub fn logging_organizations_logs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resourceNames",
            resourceNames
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_logs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_logs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_logs_list_builder()` + `logging_organizations_logs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_logs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_logs_list_builder(
        client,
        parent,
        pageSize,
        pageToken,
        resourceNames,
    )?;
    logging_organizations_logs_list_execute(builder)
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_sinks_create_execute()` to send, or `logging_organizations_sinks_create` for simplest API.

pub fn logging_organizations_sinks_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    body: &LogSink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/sinks", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customWriterIdentity",
            customWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "uniqueWriterIdentity",
            uniqueWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_sinks_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_sinks_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_sinks_create_builder()` + `logging_organizations_sinks_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_create(
    client: &SimpleHttpClient,
    parent: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    body: &LogSink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_sinks_create_builder(
        client,
        parent,
        customWriterIdentity,
        uniqueWriterIdentity,
        body,
    )?;
    logging_organizations_sinks_create_execute(builder)
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_sinks_delete_execute()` to send, or `logging_organizations_sinks_delete` for simplest API.

pub fn logging_organizations_sinks_delete_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_sinks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_sinks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_sinks_delete_builder()` + `logging_organizations_sinks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_delete(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_sinks_delete_builder(client, sinkName)?;
    logging_organizations_sinks_delete_execute(builder)
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_sinks_get_execute()` to send, or `logging_organizations_sinks_get` for simplest API.

pub fn logging_organizations_sinks_get_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_sinks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_sinks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_sinks_get_builder()` + `logging_organizations_sinks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_get(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_sinks_get_builder(client, sinkName)?;
    logging_organizations_sinks_get_execute(builder)
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_sinks_list_execute()` to send, or `logging_organizations_sinks_list` for simplest API.

pub fn logging_organizations_sinks_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/sinks", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_sinks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_sinks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_sinks_list_builder()` + `logging_organizations_sinks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_organizations_sinks_list_builder(client, parent, filter, pageSize, pageToken)?;
    logging_organizations_sinks_list_execute(builder)
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_sinks_patch_execute()` to send, or `logging_organizations_sinks_patch` for simplest API.

pub fn logging_organizations_sinks_patch_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customWriterIdentity",
            customWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "uniqueWriterIdentity",
            uniqueWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_sinks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_sinks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_sinks_patch_builder()` + `logging_organizations_sinks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_patch(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_sinks_patch_builder(
        client,
        sinkName,
        customWriterIdentity,
        uniqueWriterIdentity,
        updateMask,
        body,
    )?;
    logging_organizations_sinks_patch_execute(builder)
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_sinks_update_execute()` to send, or `logging_organizations_sinks_update` for simplest API.

pub fn logging_organizations_sinks_update_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customWriterIdentity",
            customWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "uniqueWriterIdentity",
            uniqueWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_organizations_sinks_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_organizations_sinks_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_sinks_update_builder()` + `logging_organizations_sinks_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_update(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_sinks_update_builder(
        client,
        sinkName,
        customWriterIdentity,
        uniqueWriterIdentity,
        updateMask,
        body,
    )?;
    logging_organizations_sinks_update_execute(builder)
}

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_get_cmek_settings_execute()` to send, or `logging_projects_get_cmek_settings` for simplest API.

pub fn logging_projects_get_cmek_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/cmekSettings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_get_cmek_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_get_cmek_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_get_cmek_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CmekSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_get_cmek_settings_builder()` + `logging_projects_get_cmek_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_get_cmek_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_get_cmek_settings_builder(client, name)?;
    logging_projects_get_cmek_settings_execute(builder)
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_get_settings_execute()` to send, or `logging_projects_get_settings` for simplest API.

pub fn logging_projects_get_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/settings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_get_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_get_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_get_settings_builder()` + `logging_projects_get_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_get_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_get_settings_builder(client, name)?;
    logging_projects_get_settings_execute(builder)
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_exclusions_create_execute()` to send, or `logging_projects_exclusions_create` for simplest API.

pub fn logging_projects_exclusions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &LogExclusion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/exclusions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_exclusions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_exclusions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_exclusions_create_builder()` + `logging_projects_exclusions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &LogExclusion,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_exclusions_create_builder(client, parent, body)?;
    logging_projects_exclusions_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_exclusions_delete_execute()` to send, or `logging_projects_exclusions_delete` for simplest API.

pub fn logging_projects_exclusions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_exclusions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_exclusions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_exclusions_delete_builder()` + `logging_projects_exclusions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_exclusions_delete_builder(client, name)?;
    logging_projects_exclusions_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_exclusions_get_execute()` to send, or `logging_projects_exclusions_get` for simplest API.

pub fn logging_projects_exclusions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_exclusions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_exclusions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the description of an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_exclusions_get_builder()` + `logging_projects_exclusions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_exclusions_get_builder(client, name)?;
    logging_projects_exclusions_get_execute(builder)
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_exclusions_list_execute()` to send, or `logging_projects_exclusions_list` for simplest API.

pub fn logging_projects_exclusions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/exclusions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_exclusions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_exclusions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListExclusionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_exclusions_list_builder()` + `logging_projects_exclusions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_exclusions_list_builder(client, parent, pageSize, pageToken)?;
    logging_projects_exclusions_list_execute(builder)
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_exclusions_patch_execute()` to send, or `logging_projects_exclusions_patch` for simplest API.

pub fn logging_projects_exclusions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogExclusion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_exclusions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_exclusions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_exclusions_patch_builder()` + `logging_projects_exclusions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogExclusion,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_exclusions_patch_builder(client, name, updateMask, body)?;
    logging_projects_exclusions_patch_execute(builder)
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_get_execute()` to send, or `logging_projects_locations_get` for simplest API.

pub fn logging_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_get_builder()` + `logging_projects_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_get_builder(client, name)?;
    logging_projects_locations_get_execute(builder)
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_list_execute()` to send, or `logging_projects_locations_list` for simplest API.

pub fn logging_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/locations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extraLocationTypes",
            extraLocationTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_list_builder()` + `logging_projects_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    logging_projects_locations_list_execute(builder)
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_create_execute()` to send, or `logging_projects_locations_buckets_create` for simplest API.

pub fn logging_projects_locations_buckets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/buckets", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "bucketId",
            bucketId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_create_builder()` + `logging_projects_locations_buckets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_create(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_create_builder(client, parent, bucketId, body)?;
    logging_projects_locations_buckets_create_execute(builder)
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_create_async_execute()` to send, or `logging_projects_locations_buckets_create_async` for simplest API.

pub fn logging_projects_locations_buckets_create_async_builder(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/buckets:createAsync",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "bucketId",
            bucketId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_create_async_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_create_async_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_create_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_create_async_builder()` + `logging_projects_locations_buckets_create_async_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_create_async(
    client: &SimpleHttpClient,
    parent: &str,
    bucketId: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_create_async_builder(client, parent, bucketId, body)?;
    logging_projects_locations_buckets_create_async_execute(builder)
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_delete_execute()` to send, or `logging_projects_locations_buckets_delete` for simplest API.

pub fn logging_projects_locations_buckets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log bucket.Changes the bucket's lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_delete_builder()` + `logging_projects_locations_buckets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_delete_builder(client, name)?;
    logging_projects_locations_buckets_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_get_execute()` to send, or `logging_projects_locations_buckets_get` for simplest API.

pub fn logging_projects_locations_buckets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_get_builder()` + `logging_projects_locations_buckets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_get_builder(client, name)?;
    logging_projects_locations_buckets_get_execute(builder)
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_list_execute()` to send, or `logging_projects_locations_buckets_list` for simplest API.

pub fn logging_projects_locations_buckets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/buckets", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBucketsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/buckets
/// Lists log buckets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_list_builder()` + `logging_projects_locations_buckets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_list_builder(client, parent, pageSize, pageToken)?;
    logging_projects_locations_buckets_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_patch_execute()` to send, or `logging_projects_locations_buckets_patch` for simplest API.

pub fn logging_projects_locations_buckets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_patch_builder()` + `logging_projects_locations_buckets_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_patch_builder(client, name, updateMask, body)?;
    logging_projects_locations_buckets_patch_execute(builder)
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_undelete_execute()` to send, or `logging_projects_locations_buckets_undelete` for simplest API.

pub fn logging_projects_locations_buckets_undelete_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndeleteBucketRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:undelete", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_undelete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_undelete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_undelete_builder()` + `logging_projects_locations_buckets_undelete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_undelete(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndeleteBucketRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_undelete_builder(client, name, body)?;
    logging_projects_locations_buckets_undelete_execute(builder)
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_update_async_execute()` to send, or `logging_projects_locations_buckets_update_async` for simplest API.

pub fn logging_projects_locations_buckets_update_async_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:updateAsync", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_update_async_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_update_async_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_update_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket's location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_update_async_builder()` + `logging_projects_locations_buckets_update_async_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_update_async(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogBucket,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_update_async_builder(client, name, updateMask, body)?;
    logging_projects_locations_buckets_update_async_execute(builder)
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_links_create_execute()` to send, or `logging_projects_locations_buckets_links_create` for simplest API.

pub fn logging_projects_locations_buckets_links_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    linkId: Option<&str>,
    body: &Link,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/links", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "linkId",
            linkId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_links_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_links_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_links_create_builder()` + `logging_projects_locations_buckets_links_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_create(
    client: &SimpleHttpClient,
    parent: &str,
    linkId: Option<&str>,
    body: &Link,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_links_create_builder(client, parent, linkId, body)?;
    logging_projects_locations_buckets_links_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_links_delete_execute()` to send, or `logging_projects_locations_buckets_links_delete` for simplest API.

pub fn logging_projects_locations_buckets_links_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_links_delete_builder()` + `logging_projects_locations_buckets_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_links_delete_builder(client, name)?;
    logging_projects_locations_buckets_links_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_links_get_execute()` to send, or `logging_projects_locations_buckets_links_get` for simplest API.

pub fn logging_projects_locations_buckets_links_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_links_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_links_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_links_get_builder()` + `logging_projects_locations_buckets_links_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_links_get_builder(client, name)?;
    logging_projects_locations_buckets_links_get_execute(builder)
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_links_list_execute()` to send, or `logging_projects_locations_buckets_links_list` for simplest API.

pub fn logging_projects_locations_buckets_links_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/links", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/links
/// Lists links.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_links_list_builder()` + `logging_projects_locations_buckets_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_links_list_builder(client, parent, pageSize, pageToken)?;
    logging_projects_locations_buckets_links_list_execute(builder)
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_create_execute()` to send, or `logging_projects_locations_buckets_views_create` for simplest API.

pub fn logging_projects_locations_buckets_views_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    viewId: Option<&str>,
    body: &LogView,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/views", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "viewId",
            viewId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_views_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_views_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_create_builder()` + `logging_projects_locations_buckets_views_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_create(
    client: &SimpleHttpClient,
    parent: &str,
    viewId: Option<&str>,
    body: &LogView,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_views_create_builder(client, parent, viewId, body)?;
    logging_projects_locations_buckets_views_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_delete_execute()` to send, or `logging_projects_locations_buckets_views_delete` for simplest API.

pub fn logging_projects_locations_buckets_views_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_views_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_views_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_delete_builder()` + `logging_projects_locations_buckets_views_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_views_delete_builder(client, name)?;
    logging_projects_locations_buckets_views_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_get_execute()` to send, or `logging_projects_locations_buckets_views_get` for simplest API.

pub fn logging_projects_locations_buckets_views_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_views_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_views_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a view on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_get_builder()` + `logging_projects_locations_buckets_views_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_views_get_builder(client, name)?;
    logging_projects_locations_buckets_views_get_execute(builder)
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_get_iam_policy_execute()` to send, or `logging_projects_locations_buckets_views_get_iam_policy` for simplest API.

pub fn logging_projects_locations_buckets_views_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_views_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_views_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_get_iam_policy_builder()` + `logging_projects_locations_buckets_views_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_views_get_iam_policy_builder(client, resource, body)?;
    logging_projects_locations_buckets_views_get_iam_policy_execute(builder)
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_list_execute()` to send, or `logging_projects_locations_buckets_views_list` for simplest API.

pub fn logging_projects_locations_buckets_views_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/views", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_views_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_views_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListViewsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/views
/// Lists views on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_list_builder()` + `logging_projects_locations_buckets_views_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_views_list_builder(client, parent, pageSize, pageToken)?;
    logging_projects_locations_buckets_views_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_patch_execute()` to send, or `logging_projects_locations_buckets_views_patch` for simplest API.

pub fn logging_projects_locations_buckets_views_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogView,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_views_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_views_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_patch_builder()` + `logging_projects_locations_buckets_views_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogView,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_views_patch_builder(client, name, updateMask, body)?;
    logging_projects_locations_buckets_views_patch_execute(builder)
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_set_iam_policy_execute()` to send, or `logging_projects_locations_buckets_views_set_iam_policy` for simplest API.

pub fn logging_projects_locations_buckets_views_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_views_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_views_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_set_iam_policy_builder()` + `logging_projects_locations_buckets_views_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_views_set_iam_policy_builder(client, resource, body)?;
    logging_projects_locations_buckets_views_set_iam_policy_execute(builder)
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_test_iam_permissions_execute()` to send, or `logging_projects_locations_buckets_views_test_iam_permissions` for simplest API.

pub fn logging_projects_locations_buckets_views_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_views_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_views_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_test_iam_permissions_builder()` + `logging_projects_locations_buckets_views_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_views_test_iam_permissions_builder(
        client, resource, body,
    )?;
    logging_projects_locations_buckets_views_test_iam_permissions_execute(builder)
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_logs_list_execute()` to send, or `logging_projects_locations_buckets_views_logs_list` for simplest API.

pub fn logging_projects_locations_buckets_views_logs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resourceNames",
            resourceNames
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_buckets_views_logs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_buckets_views_logs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_logs_list_builder()` + `logging_projects_locations_buckets_views_logs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_logs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_views_logs_list_builder(
        client,
        parent,
        pageSize,
        pageToken,
        resourceNames,
    )?;
    logging_projects_locations_buckets_views_logs_list_execute(builder)
}

/// GET v2/{+parent}/logScopes
/// Creates a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_log_scopes_create_execute()` to send, or `logging_projects_locations_log_scopes_create` for simplest API.

pub fn logging_projects_locations_log_scopes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    logScopeId: Option<&str>,
    body: &LogScope,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logScopes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "logScopeId",
            logScopeId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/logScopes
/// Creates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_log_scopes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_log_scopes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logScopes
/// Creates a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_log_scopes_create_builder()` + `logging_projects_locations_log_scopes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_create(
    client: &SimpleHttpClient,
    parent: &str,
    logScopeId: Option<&str>,
    body: &LogScope,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_log_scopes_create_builder(client, parent, logScopeId, body)?;
    logging_projects_locations_log_scopes_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_log_scopes_delete_execute()` to send, or `logging_projects_locations_log_scopes_delete` for simplest API.

pub fn logging_projects_locations_log_scopes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_log_scopes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_log_scopes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_log_scopes_delete_builder()` + `logging_projects_locations_log_scopes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_log_scopes_delete_builder(client, name)?;
    logging_projects_locations_log_scopes_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_log_scopes_get_execute()` to send, or `logging_projects_locations_log_scopes_get` for simplest API.

pub fn logging_projects_locations_log_scopes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_log_scopes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_log_scopes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_log_scopes_get_builder()` + `logging_projects_locations_log_scopes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_log_scopes_get_builder(client, name)?;
    logging_projects_locations_log_scopes_get_execute(builder)
}

/// GET v2/{+parent}/logScopes
/// Lists log scopes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_log_scopes_list_execute()` to send, or `logging_projects_locations_log_scopes_list` for simplest API.

pub fn logging_projects_locations_log_scopes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logScopes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/logScopes
/// Lists log scopes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_log_scopes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_log_scopes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogScopesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogScopesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logScopes
/// Lists log scopes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_log_scopes_list_builder()` + `logging_projects_locations_log_scopes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogScopesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_log_scopes_list_builder(client, parent, pageSize, pageToken)?;
    logging_projects_locations_log_scopes_list_execute(builder)
}

/// GET v2/{+name}
/// Updates a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_log_scopes_patch_execute()` to send, or `logging_projects_locations_log_scopes_patch` for simplest API.

pub fn logging_projects_locations_log_scopes_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogScope,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_log_scopes_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_log_scopes_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_log_scopes_patch_builder()` + `logging_projects_locations_log_scopes_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &LogScope,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_log_scopes_patch_builder(client, name, updateMask, body)?;
    logging_projects_locations_log_scopes_patch_execute(builder)
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_operations_cancel_execute()` to send, or `logging_projects_locations_operations_cancel` for simplest API.

pub fn logging_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_operations_cancel_builder()` + `logging_projects_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_operations_cancel_builder(client, name, body)?;
    logging_projects_locations_operations_cancel_execute(builder)
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_operations_get_execute()` to send, or `logging_projects_locations_operations_get` for simplest API.

pub fn logging_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_operations_get_builder()` + `logging_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_operations_get_builder(client, name)?;
    logging_projects_locations_operations_get_execute(builder)
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_operations_list_execute()` to send, or `logging_projects_locations_operations_list` for simplest API.

pub fn logging_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/operations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_operations_list_builder()` + `logging_projects_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    logging_projects_locations_operations_list_execute(builder)
}

/// GET v2/{+parent}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_recent_queries_list_execute()` to send, or `logging_projects_locations_recent_queries_list` for simplest API.

pub fn logging_projects_locations_recent_queries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/recentQueries", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_recent_queries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_recent_queries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_recent_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRecentQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_recent_queries_list_builder()` + `logging_projects_locations_recent_queries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_recent_queries_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_recent_queries_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    logging_projects_locations_recent_queries_list_execute(builder)
}

/// GET v2/{+parent}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_saved_queries_create_execute()` to send, or `logging_projects_locations_saved_queries_create` for simplest API.

pub fn logging_projects_locations_saved_queries_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    savedQueryId: Option<&str>,
    body: &SavedQuery,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/savedQueries", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "savedQueryId",
            savedQueryId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_saved_queries_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_saved_queries_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_saved_queries_create_builder()` + `logging_projects_locations_saved_queries_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_create(
    client: &SimpleHttpClient,
    parent: &str,
    savedQueryId: Option<&str>,
    body: &SavedQuery,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_saved_queries_create_builder(
        client,
        parent,
        savedQueryId,
        body,
    )?;
    logging_projects_locations_saved_queries_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_saved_queries_delete_execute()` to send, or `logging_projects_locations_saved_queries_delete` for simplest API.

pub fn logging_projects_locations_saved_queries_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_saved_queries_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_saved_queries_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_saved_queries_delete_builder()` + `logging_projects_locations_saved_queries_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_saved_queries_delete_builder(client, name)?;
    logging_projects_locations_saved_queries_delete_execute(builder)
}

/// GET v2/{+name}
/// Returns all data associated with the requested query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_saved_queries_get_execute()` to send, or `logging_projects_locations_saved_queries_get` for simplest API.

pub fn logging_projects_locations_saved_queries_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Returns all data associated with the requested query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_saved_queries_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_saved_queries_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Returns all data associated with the requested query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_saved_queries_get_builder()` + `logging_projects_locations_saved_queries_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_saved_queries_get_builder(client, name)?;
    logging_projects_locations_saved_queries_get_execute(builder)
}

/// GET v2/{+parent}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_saved_queries_list_execute()` to send, or `logging_projects_locations_saved_queries_list` for simplest API.

pub fn logging_projects_locations_saved_queries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/savedQueries", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_saved_queries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_saved_queries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSavedQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_saved_queries_list_builder()` + `logging_projects_locations_saved_queries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_saved_queries_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    logging_projects_locations_saved_queries_list_execute(builder)
}

/// GET v2/{+name}
/// Updates an existing SavedQuery.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_saved_queries_patch_execute()` to send, or `logging_projects_locations_saved_queries_patch` for simplest API.

pub fn logging_projects_locations_saved_queries_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SavedQuery,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates an existing SavedQuery.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_locations_saved_queries_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_locations_saved_queries_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates an existing SavedQuery.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_saved_queries_patch_builder()` + `logging_projects_locations_saved_queries_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SavedQuery,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_saved_queries_patch_builder(client, name, updateMask, body)?;
    logging_projects_locations_saved_queries_patch_execute(builder)
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_logs_delete_execute()` to send, or `logging_projects_logs_delete` for simplest API.

pub fn logging_projects_logs_delete_builder(
    client: &SimpleHttpClient,
    logName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+logName}", logName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_logs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_logs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_logs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+logName}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_logs_delete_builder()` + `logging_projects_logs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_logs_delete(
    client: &SimpleHttpClient,
    logName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_logs_delete_builder(client, logName)?;
    logging_projects_logs_delete_execute(builder)
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_logs_list_execute()` to send, or `logging_projects_logs_list` for simplest API.

pub fn logging_projects_logs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/logs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resourceNames",
            resourceNames
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_logs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_logs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_logs_list_builder()` + `logging_projects_logs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_logs_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resourceNames: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_projects_logs_list_builder(client, parent, pageSize, pageToken, resourceNames)?;
    logging_projects_logs_list_execute(builder)
}

/// GET v2/{+parent}/metrics
/// Creates a logs-based metric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_metrics_create_execute()` to send, or `logging_projects_metrics_create` for simplest API.

pub fn logging_projects_metrics_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &LogMetric,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/metrics", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/metrics
/// Creates a logs-based metric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_metrics_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_metrics_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogMetric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogMetric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/metrics
/// Creates a logs-based metric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_metrics_create_builder()` + `logging_projects_metrics_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &LogMetric,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogMetric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_metrics_create_builder(client, parent, body)?;
    logging_projects_metrics_create_execute(builder)
}

/// GET v2/{+metricName}
/// Deletes a logs-based metric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_metrics_delete_execute()` to send, or `logging_projects_metrics_delete` for simplest API.

pub fn logging_projects_metrics_delete_builder(
    client: &SimpleHttpClient,
    metricName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+metricName}", metricName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+metricName}
/// Deletes a logs-based metric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_metrics_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_metrics_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+metricName}
/// Deletes a logs-based metric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_metrics_delete_builder()` + `logging_projects_metrics_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_delete(
    client: &SimpleHttpClient,
    metricName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_metrics_delete_builder(client, metricName)?;
    logging_projects_metrics_delete_execute(builder)
}

/// GET v2/{+metricName}
/// Gets a logs-based metric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_metrics_get_execute()` to send, or `logging_projects_metrics_get` for simplest API.

pub fn logging_projects_metrics_get_builder(
    client: &SimpleHttpClient,
    metricName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+metricName}", metricName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+metricName}
/// Gets a logs-based metric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_metrics_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_metrics_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogMetric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogMetric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+metricName}
/// Gets a logs-based metric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_metrics_get_builder()` + `logging_projects_metrics_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_get(
    client: &SimpleHttpClient,
    metricName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogMetric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_metrics_get_builder(client, metricName)?;
    logging_projects_metrics_get_execute(builder)
}

/// GET v2/{+parent}/metrics
/// Lists logs-based metrics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_metrics_list_execute()` to send, or `logging_projects_metrics_list` for simplest API.

pub fn logging_projects_metrics_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/metrics", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/metrics
/// Lists logs-based metrics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_metrics_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_metrics_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogMetricsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogMetricsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/metrics
/// Lists logs-based metrics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_metrics_list_builder()` + `logging_projects_metrics_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogMetricsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_metrics_list_builder(client, parent, pageSize, pageToken)?;
    logging_projects_metrics_list_execute(builder)
}

/// GET v2/{+metricName}
/// Creates or updates a logs-based metric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_metrics_update_execute()` to send, or `logging_projects_metrics_update` for simplest API.

pub fn logging_projects_metrics_update_builder(
    client: &SimpleHttpClient,
    metricName: &str,
    body: &LogMetric,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+metricName}", metricName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+metricName}
/// Creates or updates a logs-based metric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_metrics_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_metrics_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogMetric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogMetric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+metricName}
/// Creates or updates a logs-based metric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_metrics_update_builder()` + `logging_projects_metrics_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_update(
    client: &SimpleHttpClient,
    metricName: &str,
    body: &LogMetric,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogMetric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_metrics_update_builder(client, metricName, body)?;
    logging_projects_metrics_update_execute(builder)
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_sinks_create_execute()` to send, or `logging_projects_sinks_create` for simplest API.

pub fn logging_projects_sinks_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    body: &LogSink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/sinks", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customWriterIdentity",
            customWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "uniqueWriterIdentity",
            uniqueWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_sinks_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_sinks_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_sinks_create_builder()` + `logging_projects_sinks_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_create(
    client: &SimpleHttpClient,
    parent: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    body: &LogSink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_sinks_create_builder(
        client,
        parent,
        customWriterIdentity,
        uniqueWriterIdentity,
        body,
    )?;
    logging_projects_sinks_create_execute(builder)
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_sinks_delete_execute()` to send, or `logging_projects_sinks_delete` for simplest API.

pub fn logging_projects_sinks_delete_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_sinks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_sinks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_sinks_delete_builder()` + `logging_projects_sinks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_delete(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_sinks_delete_builder(client, sinkName)?;
    logging_projects_sinks_delete_execute(builder)
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_sinks_get_execute()` to send, or `logging_projects_sinks_get` for simplest API.

pub fn logging_projects_sinks_get_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_sinks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_sinks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_sinks_get_builder()` + `logging_projects_sinks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_get(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_sinks_get_builder(client, sinkName)?;
    logging_projects_sinks_get_execute(builder)
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_sinks_list_execute()` to send, or `logging_projects_sinks_list` for simplest API.

pub fn logging_projects_sinks_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/sinks", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_sinks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_sinks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_sinks_list_builder()` + `logging_projects_sinks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_sinks_list_builder(client, parent, filter, pageSize, pageToken)?;
    logging_projects_sinks_list_execute(builder)
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_sinks_patch_execute()` to send, or `logging_projects_sinks_patch` for simplest API.

pub fn logging_projects_sinks_patch_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customWriterIdentity",
            customWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "uniqueWriterIdentity",
            uniqueWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_sinks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_sinks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_sinks_patch_builder()` + `logging_projects_sinks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_patch(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_sinks_patch_builder(
        client,
        sinkName,
        customWriterIdentity,
        uniqueWriterIdentity,
        updateMask,
        body,
    )?;
    logging_projects_sinks_patch_execute(builder)
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_sinks_update_execute()` to send, or `logging_projects_sinks_update` for simplest API.

pub fn logging_projects_sinks_update_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customWriterIdentity",
            customWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "uniqueWriterIdentity",
            uniqueWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_projects_sinks_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_projects_sinks_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_sinks_update_builder()` + `logging_projects_sinks_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_update(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_sinks_update_builder(
        client,
        sinkName,
        customWriterIdentity,
        uniqueWriterIdentity,
        updateMask,
        body,
    )?;
    logging_projects_sinks_update_execute(builder)
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_sinks_create_execute()` to send, or `logging_sinks_create` for simplest API.

pub fn logging_sinks_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    body: &LogSink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/sinks", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customWriterIdentity",
            customWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "uniqueWriterIdentity",
            uniqueWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_sinks_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_sinks_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink's writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_sinks_create_builder()` + `logging_sinks_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_create(
    client: &SimpleHttpClient,
    parent: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    body: &LogSink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_sinks_create_builder(
        client,
        parent,
        customWriterIdentity,
        uniqueWriterIdentity,
        body,
    )?;
    logging_sinks_create_execute(builder)
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_sinks_delete_execute()` to send, or `logging_sinks_delete` for simplest API.

pub fn logging_sinks_delete_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_sinks_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_sinks_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_sinks_delete_builder()` + `logging_sinks_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_delete(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_sinks_delete_builder(client, sinkName)?;
    logging_sinks_delete_execute(builder)
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_sinks_get_execute()` to send, or `logging_sinks_get` for simplest API.

pub fn logging_sinks_get_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_sinks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_sinks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Gets a sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_sinks_get_builder()` + `logging_sinks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_get(
    client: &SimpleHttpClient,
    sinkName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_sinks_get_builder(client, sinkName)?;
    logging_sinks_get_execute(builder)
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_sinks_list_execute()` to send, or `logging_sinks_list` for simplest API.

pub fn logging_sinks_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/sinks", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_sinks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_sinks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/sinks
/// Lists sinks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_sinks_list_builder()` + `logging_sinks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_sinks_list_builder(client, parent, filter, pageSize, pageToken)?;
    logging_sinks_list_execute(builder)
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_sinks_update_execute()` to send, or `logging_sinks_update` for simplest API.

pub fn logging_sinks_update_builder(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+sinkName}", sinkName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customWriterIdentity",
            customWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "uniqueWriterIdentity",
            uniqueWriterIdentity
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_sinks_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_sinks_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+sinkName}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_sinks_update_builder()` + `logging_sinks_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_update(
    client: &SimpleHttpClient,
    sinkName: &str,
    customWriterIdentity: Option<&str>,
    uniqueWriterIdentity: Option<bool>,
    updateMask: Option<&str>,
    body: &LogSink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_sinks_update_builder(
        client,
        sinkName,
        customWriterIdentity,
        uniqueWriterIdentity,
        updateMask,
        body,
    )?;
    logging_sinks_update_execute(builder)
}

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_get_cmek_settings_execute()` to send, or `logging_get_cmek_settings` for simplest API.

pub fn logging_get_cmek_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/cmekSettings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_get_cmek_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_get_cmek_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_get_cmek_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CmekSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_get_cmek_settings_builder()` + `logging_get_cmek_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_get_cmek_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_get_cmek_settings_builder(client, name)?;
    logging_get_cmek_settings_execute(builder)
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_get_settings_execute()` to send, or `logging_get_settings` for simplest API.

pub fn logging_get_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/settings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_get_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_get_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_get_settings_builder()` + `logging_get_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_get_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_get_settings_builder(client, name)?;
    logging_get_settings_execute(builder)
}

/// GET v2/{+name}/cmekSettings
/// Updates the Log Router CMEK settings for the given resource.Note: CMEK for the Log Router can currently only be configured for Google Cloud organizations. Once configured, it applies to all projects and folders in the Google Cloud organization.UpdateCmekSettings fails when any of the following are `true`: The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_update_cmek_settings_execute()` to send, or `logging_update_cmek_settings` for simplest API.

pub fn logging_update_cmek_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &CmekSettings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/cmekSettings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}/cmekSettings
/// Updates the Log Router CMEK settings for the given resource.Note: CMEK for the Log Router can currently only be configured for Google Cloud organizations. Once configured, it applies to all projects and folders in the Google Cloud organization.UpdateCmekSettings fails when any of the following are `true`: The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_update_cmek_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_update_cmek_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_update_cmek_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CmekSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/cmekSettings
/// Updates the Log Router CMEK settings for the given resource.Note: CMEK for the Log Router can currently only be configured for Google Cloud organizations. Once configured, it applies to all projects and folders in the Google Cloud organization.UpdateCmekSettings fails when any of the following are `true`: The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_update_cmek_settings_builder()` + `logging_update_cmek_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_update_cmek_settings(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &CmekSettings,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_update_cmek_settings_builder(client, name, updateMask, body)?;
    logging_update_cmek_settings_execute(builder)
}

/// GET v2/{+name}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn't supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_update_settings_execute()` to send, or `logging_update_settings` for simplest API.

pub fn logging_update_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Settings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/settings", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn't supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `logging_update_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from logging_update_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_update_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn't supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn't have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_update_settings_builder()` + `logging_update_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_update_settings(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Settings,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_update_settings_builder(client, name, updateMask, body)?;
    logging_update_settings_execute(builder)
}
