//! Auto-generated API clients for gcp/doubleclickbidmanager.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST queries
/// Creates a new query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclickbidmanager_queries_create_execute()` to send, or `doubleclickbidmanager_queries_create` for simplest API.

pub fn doubleclickbidmanager_queries_create_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://doubleclickbidmanager.googleapis.com/v2/queries",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST queries
/// Creates a new query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclickbidmanager_queries_create_execute()` or `doubleclickbidmanager_queries_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclickbidmanager_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Query>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Query = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST queries
/// Creates a new query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclickbidmanager_queries_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclickbidmanager_queries_create_task()`.
/// For the simplest API, use `doubleclickbidmanager_queries_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclickbidmanager_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclickbidmanager_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Query>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = doubleclickbidmanager_queries_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST queries
/// Creates a new query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclickbidmanager_queries_create_builder()` + `doubleclickbidmanager_queries_create_execute()`.
/// For task-level control, use `doubleclickbidmanager_queries_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_create(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Query>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclickbidmanager_queries_create_builder(client)?;
    doubleclickbidmanager_queries_create_execute(builder)
}

/// DELETE queries/{queryId}
/// Deletes an existing query as well as its generated reports.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclickbidmanager_queries_delete_execute()` to send, or `doubleclickbidmanager_queries_delete` for simplest API.

pub fn doubleclickbidmanager_queries_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    queryId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://doubleclickbidmanager.googleapis.com/v2/queries/{}",
        queryId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE queries/{queryId}
/// Deletes an existing query as well as its generated reports.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclickbidmanager_queries_delete_execute()` or `doubleclickbidmanager_queries_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclickbidmanager_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE queries/{queryId}
/// Deletes an existing query as well as its generated reports.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclickbidmanager_queries_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclickbidmanager_queries_delete_task()`.
/// For the simplest API, use `doubleclickbidmanager_queries_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclickbidmanager_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclickbidmanager_queries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = doubleclickbidmanager_queries_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`doubleclickbidmanager_queries_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DoubleclickbidmanagerQueriesDeleteArgs {
    /// Path parameter: queryId
    pub queryId: String,
}

/// DELETE queries/{queryId}
/// Deletes an existing query as well as its generated reports.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclickbidmanager_queries_delete_builder()` + `doubleclickbidmanager_queries_delete_execute()`.
/// For task-level control, use `doubleclickbidmanager_queries_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_delete(
    client: &SimpleHttpClient,
    args: &DoubleclickbidmanagerQueriesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclickbidmanager_queries_delete_builder(client, &args.queryId)?;
    doubleclickbidmanager_queries_delete_execute(builder)
}

/// GET queries/{queryId}
/// Retrieves a query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclickbidmanager_queries_get_execute()` to send, or `doubleclickbidmanager_queries_get` for simplest API.

pub fn doubleclickbidmanager_queries_get_builder<R>(
    client: &SimpleHttpClient<R>,
    queryId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://doubleclickbidmanager.googleapis.com/v2/queries/{}",
        queryId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET queries/{queryId}
/// Retrieves a query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclickbidmanager_queries_get_execute()` or `doubleclickbidmanager_queries_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclickbidmanager_queries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Query>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Query = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET queries/{queryId}
/// Retrieves a query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclickbidmanager_queries_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclickbidmanager_queries_get_task()`.
/// For the simplest API, use `doubleclickbidmanager_queries_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclickbidmanager_queries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclickbidmanager_queries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Query>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = doubleclickbidmanager_queries_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`doubleclickbidmanager_queries_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DoubleclickbidmanagerQueriesGetArgs {
    /// Path parameter: queryId
    pub queryId: String,
}

/// GET queries/{queryId}
/// Retrieves a query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclickbidmanager_queries_get_builder()` + `doubleclickbidmanager_queries_get_execute()`.
/// For task-level control, use `doubleclickbidmanager_queries_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_get(
    client: &SimpleHttpClient,
    args: &DoubleclickbidmanagerQueriesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Query>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclickbidmanager_queries_get_builder(client, &args.queryId)?;
    doubleclickbidmanager_queries_get_execute(builder)
}

/// GET queries
/// Lists queries created by the current user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclickbidmanager_queries_list_execute()` to send, or `doubleclickbidmanager_queries_list` for simplest API.

pub fn doubleclickbidmanager_queries_list_builder<R>(
    client: &SimpleHttpClient<R>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://doubleclickbidmanager.googleapis.com/v2/queries",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET queries
/// Lists queries created by the current user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclickbidmanager_queries_list_execute()` or `doubleclickbidmanager_queries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclickbidmanager_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListQueriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET queries
/// Lists queries created by the current user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclickbidmanager_queries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclickbidmanager_queries_list_task()`.
/// For the simplest API, use `doubleclickbidmanager_queries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclickbidmanager_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclickbidmanager_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = doubleclickbidmanager_queries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`doubleclickbidmanager_queries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DoubleclickbidmanagerQueriesListArgs {
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET queries
/// Lists queries created by the current user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclickbidmanager_queries_list_builder()` + `doubleclickbidmanager_queries_list_execute()`.
/// For task-level control, use `doubleclickbidmanager_queries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_list(
    client: &SimpleHttpClient,
    args: &DoubleclickbidmanagerQueriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = doubleclickbidmanager_queries_list_builder(
        client,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    doubleclickbidmanager_queries_list_execute(builder)
}

/// POST queries/{queryId}:run
/// Runs an existing query to generate a report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclickbidmanager_queries_run_execute()` to send, or `doubleclickbidmanager_queries_run` for simplest API.

pub fn doubleclickbidmanager_queries_run_builder<R>(
    client: &SimpleHttpClient<R>,
    queryId: &String,
    synchronous: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://doubleclickbidmanager.googleapis.com/v2/queries/{}:run",
        queryId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = synchronous.as_ref() {
        query_parts.push(format!("synchronous={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST queries/{queryId}:run
/// Runs an existing query to generate a report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclickbidmanager_queries_run_execute()` or `doubleclickbidmanager_queries_run`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclickbidmanager_queries_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_run_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST queries/{queryId}:run
/// Runs an existing query to generate a report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclickbidmanager_queries_run_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclickbidmanager_queries_run_task()`.
/// For the simplest API, use `doubleclickbidmanager_queries_run()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclickbidmanager_queries_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclickbidmanager_queries_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = doubleclickbidmanager_queries_run_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`doubleclickbidmanager_queries_run`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DoubleclickbidmanagerQueriesRunArgs {
    /// Path parameter: queryId
    pub queryId: String,
    /// Query parameter: synchronous
    pub synchronous: Option<String>,
}

/// POST queries/{queryId}:run
/// Runs an existing query to generate a report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclickbidmanager_queries_run_builder()` + `doubleclickbidmanager_queries_run_execute()`.
/// For task-level control, use `doubleclickbidmanager_queries_run_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_run(
    client: &SimpleHttpClient,
    args: &DoubleclickbidmanagerQueriesRunArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        doubleclickbidmanager_queries_run_builder(client, &args.queryId, &args.synchronous)?;
    doubleclickbidmanager_queries_run_execute(builder)
}

/// GET queries/{queryId}/reports/{reportId}
/// Retrieves a report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclickbidmanager_queries_reports_get_execute()` to send, or `doubleclickbidmanager_queries_reports_get` for simplest API.

pub fn doubleclickbidmanager_queries_reports_get_builder<R>(
    client: &SimpleHttpClient<R>,
    queryId: &String,
    reportId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://doubleclickbidmanager.googleapis.com/v2/queries/{}/reports/{}",
        queryId, reportId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET queries/{queryId}/reports/{reportId}
/// Retrieves a report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclickbidmanager_queries_reports_get_execute()` or `doubleclickbidmanager_queries_reports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclickbidmanager_queries_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_reports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET queries/{queryId}/reports/{reportId}
/// Retrieves a report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclickbidmanager_queries_reports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclickbidmanager_queries_reports_get_task()`.
/// For the simplest API, use `doubleclickbidmanager_queries_reports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclickbidmanager_queries_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclickbidmanager_queries_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = doubleclickbidmanager_queries_reports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`doubleclickbidmanager_queries_reports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DoubleclickbidmanagerQueriesReportsGetArgs {
    /// Path parameter: queryId
    pub queryId: String,
    /// Path parameter: reportId
    pub reportId: String,
}

/// GET queries/{queryId}/reports/{reportId}
/// Retrieves a report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclickbidmanager_queries_reports_get_builder()` + `doubleclickbidmanager_queries_reports_get_execute()`.
/// For task-level control, use `doubleclickbidmanager_queries_reports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_reports_get(
    client: &SimpleHttpClient,
    args: &DoubleclickbidmanagerQueriesReportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        doubleclickbidmanager_queries_reports_get_builder(client, &args.queryId, &args.reportId)?;
    doubleclickbidmanager_queries_reports_get_execute(builder)
}

/// GET queries/{queryId}/reports
/// Lists reports generated by the provided query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclickbidmanager_queries_reports_list_execute()` to send, or `doubleclickbidmanager_queries_reports_list` for simplest API.

pub fn doubleclickbidmanager_queries_reports_list_builder<R>(
    client: &SimpleHttpClient<R>,
    queryId: &String,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://doubleclickbidmanager.googleapis.com/v2/queries/{}/reports",
        queryId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET queries/{queryId}/reports
/// Lists reports generated by the provided query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclickbidmanager_queries_reports_list_execute()` or `doubleclickbidmanager_queries_reports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclickbidmanager_queries_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_reports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReportsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET queries/{queryId}/reports
/// Lists reports generated by the provided query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclickbidmanager_queries_reports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclickbidmanager_queries_reports_list_task()`.
/// For the simplest API, use `doubleclickbidmanager_queries_reports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclickbidmanager_queries_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclickbidmanager_queries_reports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = doubleclickbidmanager_queries_reports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`doubleclickbidmanager_queries_reports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DoubleclickbidmanagerQueriesReportsListArgs {
    /// Path parameter: queryId
    pub queryId: String,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET queries/{queryId}/reports
/// Lists reports generated by the provided query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclickbidmanager_queries_reports_list_builder()` + `doubleclickbidmanager_queries_reports_list_execute()`.
/// For task-level control, use `doubleclickbidmanager_queries_reports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_reports_list(
    client: &SimpleHttpClient,
    args: &DoubleclickbidmanagerQueriesReportsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = doubleclickbidmanager_queries_reports_list_builder(
        client,
        &args.queryId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    doubleclickbidmanager_queries_reports_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Query
// =============================================================================

/// ResourceIdentifier implementation for Query with DoubleclickbidmanagerQueriesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DoubleclickbidmanagerQueriesGetArgs> for Query {
    fn generate_resource_id(&self, input: &DoubleclickbidmanagerQueriesGetArgs) -> String {
        format!("gcp::doubleclickbidmanager::Query/{}", input.queryId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::doubleclickbidmanager::Query"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListQueriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListQueriesResponse with DoubleclickbidmanagerQueriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DoubleclickbidmanagerQueriesListArgs> for ListQueriesResponse {
    fn generate_resource_id(&self, input: &DoubleclickbidmanagerQueriesListArgs) -> String {
        "gcp::doubleclickbidmanager::ListQueriesResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::doubleclickbidmanager::ListQueriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Report
// =============================================================================

/// ResourceIdentifier implementation for Report with DoubleclickbidmanagerQueriesRunArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DoubleclickbidmanagerQueriesRunArgs> for Report {
    fn generate_resource_id(&self, input: &DoubleclickbidmanagerQueriesRunArgs) -> String {
        format!("gcp::doubleclickbidmanager::Report/{}", input.queryId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::doubleclickbidmanager::Report"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Report
// =============================================================================

/// ResourceIdentifier implementation for Report with DoubleclickbidmanagerQueriesReportsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DoubleclickbidmanagerQueriesReportsGetArgs> for Report {
    fn generate_resource_id(&self, input: &DoubleclickbidmanagerQueriesReportsGetArgs) -> String {
        format!(
            "gcp::doubleclickbidmanager::Report/{}/{}",
            input.queryId, input.reportId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::doubleclickbidmanager::Report"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListReportsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListReportsResponse with DoubleclickbidmanagerQueriesReportsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DoubleclickbidmanagerQueriesReportsListArgs> for ListReportsResponse {
    fn generate_resource_id(&self, input: &DoubleclickbidmanagerQueriesReportsListArgs) -> String {
        format!(
            "gcp::doubleclickbidmanager::ListReportsResponse/{}",
            input.queryId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::doubleclickbidmanager::ListReportsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
