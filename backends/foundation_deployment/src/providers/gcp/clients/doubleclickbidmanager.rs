//! Auto-generated API clients for gcp/doubleclickbidmanager.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/doubleclickbidmanager`

#![cfg(feature = "gcp/doubleclickbidmanager")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET queries
/// Creates a new query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclickbidmanager_queries_create_execute()` to send, or `doubleclickbidmanager_queries_create` for simplest API.

pub fn doubleclickbidmanager_queries_create_builder(
    client: &SimpleHttpClient,
    body: &Query,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://doubleclickbidmanager.googleapis.com/v2/queries",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET queries
/// Creates a new query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclickbidmanager_queries_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclickbidmanager_queries_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Query>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Query = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET queries
/// Creates a new query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclickbidmanager_queries_create_builder()` + `doubleclickbidmanager_queries_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_create(
    client: &SimpleHttpClient,
    body: &Query,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Query>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclickbidmanager_queries_create_builder(client, body)?;
    doubleclickbidmanager_queries_create_execute(builder)
}

/// GET queries/{queryId}
/// Deletes an existing query as well as its generated reports.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclickbidmanager_queries_delete_execute()` to send, or `doubleclickbidmanager_queries_delete` for simplest API.

pub fn doubleclickbidmanager_queries_delete_builder(
    client: &SimpleHttpClient,
    queryId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://doubleclickbidmanager.googleapis.com/v2/queries/{}",
        queryId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET queries/{queryId}
/// Deletes an existing query as well as its generated reports.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclickbidmanager_queries_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclickbidmanager_queries_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET queries/{queryId}
/// Deletes an existing query as well as its generated reports.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclickbidmanager_queries_delete_builder()` + `doubleclickbidmanager_queries_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_delete(
    client: &SimpleHttpClient,
    queryId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclickbidmanager_queries_delete_builder(client, queryId)?;
    doubleclickbidmanager_queries_delete_execute(builder)
}

/// GET queries/{queryId}
/// Retrieves a query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclickbidmanager_queries_get_execute()` to send, or `doubleclickbidmanager_queries_get` for simplest API.

pub fn doubleclickbidmanager_queries_get_builder(
    client: &SimpleHttpClient,
    queryId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://doubleclickbidmanager.googleapis.com/v2/queries/{}",
        queryId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET queries/{queryId}
/// Retrieves a query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclickbidmanager_queries_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclickbidmanager_queries_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Query>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Query = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET queries/{queryId}
/// Retrieves a query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclickbidmanager_queries_get_builder()` + `doubleclickbidmanager_queries_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_get(
    client: &SimpleHttpClient,
    queryId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Query>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclickbidmanager_queries_get_builder(client, queryId)?;
    doubleclickbidmanager_queries_get_execute(builder)
}

/// GET queries
/// Lists queries created by the current user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclickbidmanager_queries_list_execute()` to send, or `doubleclickbidmanager_queries_list` for simplest API.

pub fn doubleclickbidmanager_queries_list_builder(
    client: &SimpleHttpClient,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://doubleclickbidmanager.googleapis.com/v2/queries",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET queries
/// Lists queries created by the current user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclickbidmanager_queries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclickbidmanager_queries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET queries
/// Lists queries created by the current user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclickbidmanager_queries_list_builder()` + `doubleclickbidmanager_queries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_list(
    client: &SimpleHttpClient,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = doubleclickbidmanager_queries_list_builder(client, orderBy, pageSize, pageToken)?;
    doubleclickbidmanager_queries_list_execute(builder)
}

/// GET queries/{queryId}:run
/// Runs an existing query to generate a report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclickbidmanager_queries_run_execute()` to send, or `doubleclickbidmanager_queries_run` for simplest API.

pub fn doubleclickbidmanager_queries_run_builder(
    client: &SimpleHttpClient,
    queryId: &str,
    synchronous: Option<bool>,
    body: &RunQueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://doubleclickbidmanager.googleapis.com/v2/queries/{}:run",
        queryId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = synchronous {
        query_parts.push(format!("synchronous={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET queries/{queryId}:run
/// Runs an existing query to generate a report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclickbidmanager_queries_run_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclickbidmanager_queries_run_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET queries/{queryId}:run
/// Runs an existing query to generate a report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclickbidmanager_queries_run_builder()` + `doubleclickbidmanager_queries_run_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_run(
    client: &SimpleHttpClient,
    queryId: &str,
    synchronous: Option<bool>,
    body: &RunQueryRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclickbidmanager_queries_run_builder(client, queryId, synchronous, body)?;
    doubleclickbidmanager_queries_run_execute(builder)
}

/// GET queries/{queryId}/reports/{reportId}
/// Retrieves a report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclickbidmanager_queries_reports_get_execute()` to send, or `doubleclickbidmanager_queries_reports_get` for simplest API.

pub fn doubleclickbidmanager_queries_reports_get_builder(
    client: &SimpleHttpClient,
    queryId: &str,
    reportId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://doubleclickbidmanager.googleapis.com/v2/queries/{}/reports/{}",
        queryId, reportId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET queries/{queryId}/reports/{reportId}
/// Retrieves a report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclickbidmanager_queries_reports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclickbidmanager_queries_reports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET queries/{queryId}/reports/{reportId}
/// Retrieves a report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclickbidmanager_queries_reports_get_builder()` + `doubleclickbidmanager_queries_reports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_reports_get(
    client: &SimpleHttpClient,
    queryId: &str,
    reportId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclickbidmanager_queries_reports_get_builder(client, queryId, reportId)?;
    doubleclickbidmanager_queries_reports_get_execute(builder)
}

/// GET queries/{queryId}/reports
/// Lists reports generated by the provided query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclickbidmanager_queries_reports_list_execute()` to send, or `doubleclickbidmanager_queries_reports_list` for simplest API.

pub fn doubleclickbidmanager_queries_reports_list_builder(
    client: &SimpleHttpClient,
    queryId: &str,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://doubleclickbidmanager.googleapis.com/v2/queries/{}/reports",
        queryId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET queries/{queryId}/reports
/// Lists reports generated by the provided query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclickbidmanager_queries_reports_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclickbidmanager_queries_reports_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_reports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET queries/{queryId}/reports
/// Lists reports generated by the provided query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclickbidmanager_queries_reports_list_builder()` + `doubleclickbidmanager_queries_reports_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclickbidmanager_queries_reports_list(
    client: &SimpleHttpClient,
    queryId: &str,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = doubleclickbidmanager_queries_reports_list_builder(
        client, queryId, orderBy, pageSize, pageToken,
    )?;
    doubleclickbidmanager_queries_reports_list_execute(builder)
}
