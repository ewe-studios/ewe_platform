//! Auto-generated API clients for gcp/contactcenterinsights.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}:bulkDeleteFeedbackLabels
/// Delete feedback labels in bulk using a filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_bulk_delete_feedback_labels_execute()` to send, or `contactcenterinsights_projects_locations_bulk_delete_feedback_labels` for simplest API.

pub fn contactcenterinsights_projects_locations_bulk_delete_feedback_labels_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1BulkDeleteFeedbackLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}:bulkDeleteFeedbackLabels",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:bulkDeleteFeedbackLabels
/// Delete feedback labels in bulk using a filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_bulk_delete_feedback_labels_execute()` or `contactcenterinsights_projects_locations_bulk_delete_feedback_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_bulk_delete_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_bulk_delete_feedback_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:bulkDeleteFeedbackLabels
/// Delete feedback labels in bulk using a filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_bulk_delete_feedback_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_bulk_delete_feedback_labels_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_bulk_delete_feedback_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_bulk_delete_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_bulk_delete_feedback_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_bulk_delete_feedback_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_bulk_delete_feedback_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsBulkDeleteFeedbackLabelsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1BulkDeleteFeedbackLabelsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:bulkDeleteFeedbackLabels
/// Delete feedback labels in bulk using a filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_bulk_delete_feedback_labels_builder()` + `contactcenterinsights_projects_locations_bulk_delete_feedback_labels_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_bulk_delete_feedback_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_bulk_delete_feedback_labels(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsBulkDeleteFeedbackLabelsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_bulk_delete_feedback_labels_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_bulk_delete_feedback_labels_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:bulkDownloadFeedbackLabels
/// Download feedback labels in bulk from an external source. Currently supports exporting Quality AI example conversations with transcripts and question bodies.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_bulk_download_feedback_labels_execute()` to send, or `contactcenterinsights_projects_locations_bulk_download_feedback_labels` for simplest API.

pub fn contactcenterinsights_projects_locations_bulk_download_feedback_labels_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1BulkDownloadFeedbackLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}:bulkDownloadFeedbackLabels",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:bulkDownloadFeedbackLabels
/// Download feedback labels in bulk from an external source. Currently supports exporting Quality AI example conversations with transcripts and question bodies.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_bulk_download_feedback_labels_execute()` or `contactcenterinsights_projects_locations_bulk_download_feedback_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_bulk_download_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_bulk_download_feedback_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:bulkDownloadFeedbackLabels
/// Download feedback labels in bulk from an external source. Currently supports exporting Quality AI example conversations with transcripts and question bodies.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_bulk_download_feedback_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_bulk_download_feedback_labels_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_bulk_download_feedback_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_bulk_download_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_bulk_download_feedback_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_bulk_download_feedback_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_bulk_download_feedback_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsBulkDownloadFeedbackLabelsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1BulkDownloadFeedbackLabelsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:bulkDownloadFeedbackLabels
/// Download feedback labels in bulk from an external source. Currently supports exporting Quality AI example conversations with transcripts and question bodies.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_bulk_download_feedback_labels_builder()` + `contactcenterinsights_projects_locations_bulk_download_feedback_labels_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_bulk_download_feedback_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_bulk_download_feedback_labels(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsBulkDownloadFeedbackLabelsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_bulk_download_feedback_labels_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_bulk_download_feedback_labels_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:bulkUploadFeedbackLabels
/// Upload feedback labels from an external source in bulk. Currently supports labeling Quality AI example conversations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_bulk_upload_feedback_labels_execute()` to send, or `contactcenterinsights_projects_locations_bulk_upload_feedback_labels` for simplest API.

pub fn contactcenterinsights_projects_locations_bulk_upload_feedback_labels_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1BulkUploadFeedbackLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}:bulkUploadFeedbackLabels",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:bulkUploadFeedbackLabels
/// Upload feedback labels from an external source in bulk. Currently supports labeling Quality AI example conversations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_bulk_upload_feedback_labels_execute()` or `contactcenterinsights_projects_locations_bulk_upload_feedback_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_bulk_upload_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_bulk_upload_feedback_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:bulkUploadFeedbackLabels
/// Upload feedback labels from an external source in bulk. Currently supports labeling Quality AI example conversations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_bulk_upload_feedback_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_bulk_upload_feedback_labels_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_bulk_upload_feedback_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_bulk_upload_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_bulk_upload_feedback_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_bulk_upload_feedback_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_bulk_upload_feedback_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsBulkUploadFeedbackLabelsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1BulkUploadFeedbackLabelsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:bulkUploadFeedbackLabels
/// Upload feedback labels from an external source in bulk. Currently supports labeling Quality AI example conversations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_bulk_upload_feedback_labels_builder()` + `contactcenterinsights_projects_locations_bulk_upload_feedback_labels_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_bulk_upload_feedback_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_bulk_upload_feedback_labels(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsBulkUploadFeedbackLabelsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_bulk_upload_feedback_labels_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_bulk_upload_feedback_labels_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generativeInsights
/// Natural language based Insights which powers the next generation of dashboards in Insights. Next generation of QueryMetrics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_generative_insights_execute()` to send, or `contactcenterinsights_projects_locations_generative_insights` for simplest API.

pub fn contactcenterinsights_projects_locations_generative_insights_builder(
    client: &SimpleHttpClient,
    location: &str,
    body: &GoogleCloudContactcenterinsightsV1GenerativeInsightsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}:generativeInsights",
        location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generativeInsights
/// Natural language based Insights which powers the next generation of dashboards in Insights. Next generation of QueryMetrics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_generative_insights_execute()` or `contactcenterinsights_projects_locations_generative_insights`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_generative_insights_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_generative_insights_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generativeInsights
/// Natural language based Insights which powers the next generation of dashboards in Insights. Next generation of QueryMetrics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_generative_insights_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_generative_insights_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_generative_insights()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_generative_insights_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_generative_insights_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_generative_insights_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_generative_insights`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsGenerativeInsightsArgs {
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1GenerativeInsightsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:generativeInsights
/// Natural language based Insights which powers the next generation of dashboards in Insights. Next generation of QueryMetrics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_generative_insights_builder()` + `contactcenterinsights_projects_locations_generative_insights_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_generative_insights_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_generative_insights(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsGenerativeInsightsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_generative_insights_builder(
        client,
        &args.location,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_generative_insights_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/correlationConfig
/// Gets correlation config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_get_correlation_config_execute()` to send, or `contactcenterinsights_projects_locations_get_correlation_config` for simplest API.

pub fn contactcenterinsights_projects_locations_get_correlation_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/correlationConfig",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/correlationConfig
/// Gets correlation config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_get_correlation_config_execute()` or `contactcenterinsights_projects_locations_get_correlation_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_get_correlation_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_get_correlation_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1CorrelationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1CorrelationConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/correlationConfig
/// Gets correlation config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_get_correlation_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_get_correlation_config_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_get_correlation_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_get_correlation_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_get_correlation_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1CorrelationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_get_correlation_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_get_correlation_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsGetCorrelationConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/correlationConfig
/// Gets correlation config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_get_correlation_config_builder()` + `contactcenterinsights_projects_locations_get_correlation_config_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_get_correlation_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_get_correlation_config(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsGetCorrelationConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1CorrelationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_get_correlation_config_builder(
        client, &args.name,
    )?;
    contactcenterinsights_projects_locations_get_correlation_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/encryptionSpec
/// Gets location-level encryption key specification.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_get_encryption_spec_execute()` to send, or `contactcenterinsights_projects_locations_get_encryption_spec` for simplest API.

pub fn contactcenterinsights_projects_locations_get_encryption_spec_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/encryptionSpec",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/encryptionSpec
/// Gets location-level encryption key specification.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_get_encryption_spec_execute()` or `contactcenterinsights_projects_locations_get_encryption_spec`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_get_encryption_spec_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_get_encryption_spec_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1EncryptionSpec>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1EncryptionSpec =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/encryptionSpec
/// Gets location-level encryption key specification.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_get_encryption_spec_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_get_encryption_spec_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_get_encryption_spec()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_get_encryption_spec_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_get_encryption_spec_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1EncryptionSpec>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_get_encryption_spec_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_get_encryption_spec`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsGetEncryptionSpecArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/encryptionSpec
/// Gets location-level encryption key specification.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_get_encryption_spec_builder()` + `contactcenterinsights_projects_locations_get_encryption_spec_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_get_encryption_spec_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_get_encryption_spec(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsGetEncryptionSpecArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1EncryptionSpec>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_get_encryption_spec_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_get_encryption_spec_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Gets project-level settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_get_settings_execute()` to send, or `contactcenterinsights_projects_locations_get_settings` for simplest API.

pub fn contactcenterinsights_projects_locations_get_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/settings",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Gets project-level settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_get_settings_execute()` or `contactcenterinsights_projects_locations_get_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_get_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Settings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Settings =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Gets project-level settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_get_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_get_settings_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_get_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Settings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_get_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_get_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsGetSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Gets project-level settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_get_settings_builder()` + `contactcenterinsights_projects_locations_get_settings_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_get_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_get_settings(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsGetSettingsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Settings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_get_settings_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_get_settings_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:listAllFeedbackLabels
/// List all feedback labels by project number.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_list_all_feedback_labels_execute()` to send, or `contactcenterinsights_projects_locations_list_all_feedback_labels` for simplest API.

pub fn contactcenterinsights_projects_locations_list_all_feedback_labels_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}:listAllFeedbackLabels",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:listAllFeedbackLabels
/// List all feedback labels by project number.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_list_all_feedback_labels_execute()` or `contactcenterinsights_projects_locations_list_all_feedback_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_list_all_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_list_all_feedback_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAllFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListAllFeedbackLabelsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:listAllFeedbackLabels
/// List all feedback labels by project number.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_list_all_feedback_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_list_all_feedback_labels_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_list_all_feedback_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_list_all_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_list_all_feedback_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAllFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_list_all_feedback_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_list_all_feedback_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsListAllFeedbackLabelsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:listAllFeedbackLabels
/// List all feedback labels by project number.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_list_all_feedback_labels_builder()` + `contactcenterinsights_projects_locations_list_all_feedback_labels_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_list_all_feedback_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_list_all_feedback_labels(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsListAllFeedbackLabelsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAllFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_list_all_feedback_labels_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    contactcenterinsights_projects_locations_list_all_feedback_labels_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:queryMetrics
/// Query metrics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_query_metrics_execute()` to send, or `contactcenterinsights_projects_locations_query_metrics` for simplest API.

pub fn contactcenterinsights_projects_locations_query_metrics_builder(
    client: &SimpleHttpClient,
    location: &str,
    body: &GoogleCloudContactcenterinsightsV1QueryMetricsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}:queryMetrics",
        location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:queryMetrics
/// Query metrics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_query_metrics_execute()` or `contactcenterinsights_projects_locations_query_metrics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_query_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_query_metrics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:queryMetrics
/// Query metrics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_query_metrics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_query_metrics_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_query_metrics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_query_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_query_metrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_query_metrics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_query_metrics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQueryMetricsArgs {
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1QueryMetricsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:queryMetrics
/// Query metrics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_query_metrics_builder()` + `contactcenterinsights_projects_locations_query_metrics_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_query_metrics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_query_metrics(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQueryMetricsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_query_metrics_builder(
        client,
        &args.location,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_query_metrics_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:queryPerformanceOverview
/// Generates a summary of predefined performance metrics for a set of conversations. Conversations can be specified by specifying a time window and an agent id, for now. The summary includes a comparison of metrics computed for conversations in the previous time period, and also a comparison with peers in the same time period.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_query_performance_overview_execute()` to send, or `contactcenterinsights_projects_locations_query_performance_overview` for simplest API.

pub fn contactcenterinsights_projects_locations_query_performance_overview_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1QueryPerformanceOverviewRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}:queryPerformanceOverview",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:queryPerformanceOverview
/// Generates a summary of predefined performance metrics for a set of conversations. Conversations can be specified by specifying a time window and an agent id, for now. The summary includes a comparison of metrics computed for conversations in the previous time period, and also a comparison with peers in the same time period.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_query_performance_overview_execute()` or `contactcenterinsights_projects_locations_query_performance_overview`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_query_performance_overview_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_query_performance_overview_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:queryPerformanceOverview
/// Generates a summary of predefined performance metrics for a set of conversations. Conversations can be specified by specifying a time window and an agent id, for now. The summary includes a comparison of metrics computed for conversations in the previous time period, and also a comparison with peers in the same time period.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_query_performance_overview_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_query_performance_overview_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_query_performance_overview()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_query_performance_overview_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_query_performance_overview_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_query_performance_overview_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_query_performance_overview`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQueryPerformanceOverviewArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1QueryPerformanceOverviewRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:queryPerformanceOverview
/// Generates a summary of predefined performance metrics for a set of conversations. Conversations can be specified by specifying a time window and an agent id, for now. The summary includes a comparison of metrics computed for conversations in the previous time period, and also a comparison with peers in the same time period.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_query_performance_overview_builder()` + `contactcenterinsights_projects_locations_query_performance_overview_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_query_performance_overview_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_query_performance_overview(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQueryPerformanceOverviewArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_query_performance_overview_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_query_performance_overview_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:testCorrelationConfig
/// Tests correlation config on a conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_test_correlation_config_execute()` to send, or `contactcenterinsights_projects_locations_test_correlation_config` for simplest API.

pub fn contactcenterinsights_projects_locations_test_correlation_config_builder(
    client: &SimpleHttpClient,
    location: &str,
    body: &GoogleCloudContactcenterinsightsV1TestCorrelationConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}:testCorrelationConfig",
        location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:testCorrelationConfig
/// Tests correlation config on a conversation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_test_correlation_config_execute()` or `contactcenterinsights_projects_locations_test_correlation_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_test_correlation_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_test_correlation_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:testCorrelationConfig
/// Tests correlation config on a conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_test_correlation_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_test_correlation_config_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_test_correlation_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_test_correlation_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_test_correlation_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_test_correlation_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_test_correlation_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsTestCorrelationConfigArgs {
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1TestCorrelationConfigRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:testCorrelationConfig
/// Tests correlation config on a conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_test_correlation_config_builder()` + `contactcenterinsights_projects_locations_test_correlation_config_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_test_correlation_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_test_correlation_config(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsTestCorrelationConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_test_correlation_config_builder(
        client,
        &args.location,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_test_correlation_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/correlationConfig
/// Updates correlation config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_update_correlation_config_execute()` to send, or `contactcenterinsights_projects_locations_update_correlation_config` for simplest API.

pub fn contactcenterinsights_projects_locations_update_correlation_config_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1CorrelationConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/correlationConfig",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/correlationConfig
/// Updates correlation config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_update_correlation_config_execute()` or `contactcenterinsights_projects_locations_update_correlation_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_update_correlation_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_update_correlation_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1CorrelationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1CorrelationConfig =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/correlationConfig
/// Updates correlation config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_update_correlation_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_update_correlation_config_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_update_correlation_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_update_correlation_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_update_correlation_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1CorrelationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_update_correlation_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_update_correlation_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsUpdateCorrelationConfigArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1CorrelationConfig,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/correlationConfig
/// Updates correlation config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_update_correlation_config_builder()` + `contactcenterinsights_projects_locations_update_correlation_config_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_update_correlation_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_update_correlation_config(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsUpdateCorrelationConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1CorrelationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_update_correlation_config_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_update_correlation_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Updates project-level settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_update_settings_execute()` to send, or `contactcenterinsights_projects_locations_update_settings` for simplest API.

pub fn contactcenterinsights_projects_locations_update_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1Settings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/settings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Updates project-level settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_update_settings_execute()` or `contactcenterinsights_projects_locations_update_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_update_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Settings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Settings =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Updates project-level settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_update_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_update_settings_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_update_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_update_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Settings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_update_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_update_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsUpdateSettingsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Settings,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Updates project-level settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_update_settings_builder()` + `contactcenterinsights_projects_locations_update_settings_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_update_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_update_settings(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsUpdateSettingsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Settings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_update_settings_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_update_settings_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules
/// Creates a analysis rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_analysis_rules_create_execute()` to send, or `contactcenterinsights_projects_locations_analysis_rules_create` for simplest API.

pub fn contactcenterinsights_projects_locations_analysis_rules_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1AnalysisRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/analysisRules",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules
/// Creates a analysis rule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_analysis_rules_create_execute()` or `contactcenterinsights_projects_locations_analysis_rules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_analysis_rules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_analysis_rules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AnalysisRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AnalysisRule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules
/// Creates a analysis rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_analysis_rules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_analysis_rules_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_analysis_rules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_analysis_rules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_analysis_rules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AnalysisRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_analysis_rules_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_analysis_rules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAnalysisRulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1AnalysisRule,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules
/// Creates a analysis rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_analysis_rules_create_builder()` + `contactcenterinsights_projects_locations_analysis_rules_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_analysis_rules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_analysis_rules_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAnalysisRulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AnalysisRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_analysis_rules_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_analysis_rules_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules/{analysisRulesId}
/// Deletes a analysis rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_analysis_rules_delete_execute()` to send, or `contactcenterinsights_projects_locations_analysis_rules_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_analysis_rules_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/analysisRules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules/{analysisRulesId}
/// Deletes a analysis rule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_analysis_rules_delete_execute()` or `contactcenterinsights_projects_locations_analysis_rules_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_analysis_rules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_analysis_rules_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules/{analysisRulesId}
/// Deletes a analysis rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_analysis_rules_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_analysis_rules_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_analysis_rules_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_analysis_rules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_analysis_rules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_analysis_rules_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_analysis_rules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAnalysisRulesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules/{analysisRulesId}
/// Deletes a analysis rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_analysis_rules_delete_builder()` + `contactcenterinsights_projects_locations_analysis_rules_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_analysis_rules_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_analysis_rules_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAnalysisRulesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_analysis_rules_delete_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_analysis_rules_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules/{analysisRulesId}
/// Get a analysis rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_analysis_rules_get_execute()` to send, or `contactcenterinsights_projects_locations_analysis_rules_get` for simplest API.

pub fn contactcenterinsights_projects_locations_analysis_rules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/analysisRules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules/{analysisRulesId}
/// Get a analysis rule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_analysis_rules_get_execute()` or `contactcenterinsights_projects_locations_analysis_rules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_analysis_rules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_analysis_rules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AnalysisRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AnalysisRule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules/{analysisRulesId}
/// Get a analysis rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_analysis_rules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_analysis_rules_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_analysis_rules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_analysis_rules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_analysis_rules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AnalysisRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_analysis_rules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_analysis_rules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAnalysisRulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules/{analysisRulesId}
/// Get a analysis rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_analysis_rules_get_builder()` + `contactcenterinsights_projects_locations_analysis_rules_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_analysis_rules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_analysis_rules_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAnalysisRulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AnalysisRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_analysis_rules_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_analysis_rules_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules
/// Lists analysis rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_analysis_rules_list_execute()` to send, or `contactcenterinsights_projects_locations_analysis_rules_list` for simplest API.

pub fn contactcenterinsights_projects_locations_analysis_rules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/analysisRules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules
/// Lists analysis rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_analysis_rules_list_execute()` or `contactcenterinsights_projects_locations_analysis_rules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_analysis_rules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_analysis_rules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAnalysisRulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListAnalysisRulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules
/// Lists analysis rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_analysis_rules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_analysis_rules_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_analysis_rules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_analysis_rules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_analysis_rules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAnalysisRulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_analysis_rules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_analysis_rules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAnalysisRulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules
/// Lists analysis rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_analysis_rules_list_builder()` + `contactcenterinsights_projects_locations_analysis_rules_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_analysis_rules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_analysis_rules_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAnalysisRulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAnalysisRulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_analysis_rules_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    contactcenterinsights_projects_locations_analysis_rules_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules/{analysisRulesId}
/// Updates a analysis rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_analysis_rules_patch_execute()` to send, or `contactcenterinsights_projects_locations_analysis_rules_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_analysis_rules_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1AnalysisRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/analysisRules/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules/{analysisRulesId}
/// Updates a analysis rule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_analysis_rules_patch_execute()` or `contactcenterinsights_projects_locations_analysis_rules_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_analysis_rules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_analysis_rules_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AnalysisRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AnalysisRule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules/{analysisRulesId}
/// Updates a analysis rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_analysis_rules_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_analysis_rules_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_analysis_rules_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_analysis_rules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_analysis_rules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AnalysisRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_analysis_rules_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_analysis_rules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAnalysisRulesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1AnalysisRule,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/analysisRules/{analysisRulesId}
/// Updates a analysis rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_analysis_rules_patch_builder()` + `contactcenterinsights_projects_locations_analysis_rules_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_analysis_rules_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_analysis_rules_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAnalysisRulesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AnalysisRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_analysis_rules_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_analysis_rules_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules
/// Creates an assessment rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_assessment_rules_create_execute()` to send, or `contactcenterinsights_projects_locations_assessment_rules_create` for simplest API.

pub fn contactcenterinsights_projects_locations_assessment_rules_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    assessmentRuleId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1AssessmentRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/assessmentRules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = assessmentRuleId {
        query_parts.push(format!("assessmentRuleId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules
/// Creates an assessment rule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_assessment_rules_create_execute()` or `contactcenterinsights_projects_locations_assessment_rules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_assessment_rules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_assessment_rules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AssessmentRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AssessmentRule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules
/// Creates an assessment rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_assessment_rules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_assessment_rules_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_assessment_rules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_assessment_rules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_assessment_rules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AssessmentRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_assessment_rules_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_assessment_rules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAssessmentRulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: assessmentRuleId
    pub assessmentRuleId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1AssessmentRule,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules
/// Creates an assessment rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_assessment_rules_create_builder()` + `contactcenterinsights_projects_locations_assessment_rules_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_assessment_rules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_assessment_rules_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAssessmentRulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AssessmentRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_assessment_rules_create_builder(
        client,
        &args.parent,
        args.assessmentRuleId.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_assessment_rules_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules/{assessmentRulesId}
/// Deletes an assessment rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_assessment_rules_delete_execute()` to send, or `contactcenterinsights_projects_locations_assessment_rules_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_assessment_rules_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/assessmentRules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules/{assessmentRulesId}
/// Deletes an assessment rule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_assessment_rules_delete_execute()` or `contactcenterinsights_projects_locations_assessment_rules_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_assessment_rules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_assessment_rules_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules/{assessmentRulesId}
/// Deletes an assessment rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_assessment_rules_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_assessment_rules_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_assessment_rules_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_assessment_rules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_assessment_rules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_assessment_rules_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_assessment_rules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAssessmentRulesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules/{assessmentRulesId}
/// Deletes an assessment rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_assessment_rules_delete_builder()` + `contactcenterinsights_projects_locations_assessment_rules_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_assessment_rules_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_assessment_rules_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAssessmentRulesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_assessment_rules_delete_builder(
        client, &args.name,
    )?;
    contactcenterinsights_projects_locations_assessment_rules_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules/{assessmentRulesId}
/// Get an assessment rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_assessment_rules_get_execute()` to send, or `contactcenterinsights_projects_locations_assessment_rules_get` for simplest API.

pub fn contactcenterinsights_projects_locations_assessment_rules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/assessmentRules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules/{assessmentRulesId}
/// Get an assessment rule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_assessment_rules_get_execute()` or `contactcenterinsights_projects_locations_assessment_rules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_assessment_rules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_assessment_rules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AssessmentRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AssessmentRule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules/{assessmentRulesId}
/// Get an assessment rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_assessment_rules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_assessment_rules_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_assessment_rules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_assessment_rules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_assessment_rules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AssessmentRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_assessment_rules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_assessment_rules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAssessmentRulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules/{assessmentRulesId}
/// Get an assessment rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_assessment_rules_get_builder()` + `contactcenterinsights_projects_locations_assessment_rules_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_assessment_rules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_assessment_rules_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAssessmentRulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AssessmentRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_assessment_rules_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_assessment_rules_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules
/// Lists assessment rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_assessment_rules_list_execute()` to send, or `contactcenterinsights_projects_locations_assessment_rules_list` for simplest API.

pub fn contactcenterinsights_projects_locations_assessment_rules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/assessmentRules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules
/// Lists assessment rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_assessment_rules_list_execute()` or `contactcenterinsights_projects_locations_assessment_rules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_assessment_rules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_assessment_rules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAssessmentRulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListAssessmentRulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules
/// Lists assessment rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_assessment_rules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_assessment_rules_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_assessment_rules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_assessment_rules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_assessment_rules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAssessmentRulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_assessment_rules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_assessment_rules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAssessmentRulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules
/// Lists assessment rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_assessment_rules_list_builder()` + `contactcenterinsights_projects_locations_assessment_rules_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_assessment_rules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_assessment_rules_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAssessmentRulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAssessmentRulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_assessment_rules_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    contactcenterinsights_projects_locations_assessment_rules_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules/{assessmentRulesId}
/// Updates an assessment rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_assessment_rules_patch_execute()` to send, or `contactcenterinsights_projects_locations_assessment_rules_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_assessment_rules_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1AssessmentRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/assessmentRules/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules/{assessmentRulesId}
/// Updates an assessment rule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_assessment_rules_patch_execute()` or `contactcenterinsights_projects_locations_assessment_rules_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_assessment_rules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_assessment_rules_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AssessmentRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AssessmentRule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules/{assessmentRulesId}
/// Updates an assessment rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_assessment_rules_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_assessment_rules_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_assessment_rules_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_assessment_rules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_assessment_rules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AssessmentRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_assessment_rules_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_assessment_rules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAssessmentRulesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1AssessmentRule,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/assessmentRules/{assessmentRulesId}
/// Updates an assessment rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_assessment_rules_patch_builder()` + `contactcenterinsights_projects_locations_assessment_rules_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_assessment_rules_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_assessment_rules_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAssessmentRulesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AssessmentRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_assessment_rules_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_assessment_rules_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets
/// Create AuthorizedViewSet
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_create_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_create` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    authorizedViewSetId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1AuthorizedViewSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = authorizedViewSetId {
        query_parts.push(format!("authorizedViewSetId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets
/// Create AuthorizedViewSet
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_create_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedViewSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AuthorizedViewSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets
/// Create AuthorizedViewSet
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedViewSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: authorizedViewSetId
    pub authorizedViewSetId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1AuthorizedViewSet,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets
/// Create AuthorizedViewSet
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_create_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedViewSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_create_builder(
        client,
        &args.parent,
        args.authorizedViewSetId.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_authorized_view_sets_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}
/// Deletes an AuthorizedViewSet.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_delete_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}
/// Deletes an AuthorizedViewSet.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_delete_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}
/// Deletes an AuthorizedViewSet.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}
/// Deletes an AuthorizedViewSet.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_delete_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_delete_builder(
        client, &args.name, args.force,
    )?;
    contactcenterinsights_projects_locations_authorized_view_sets_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}
/// Get AuthorizedViewSet
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_get_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_get` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}
/// Get AuthorizedViewSet
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_get_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedViewSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AuthorizedViewSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}
/// Get AuthorizedViewSet
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedViewSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}
/// Get AuthorizedViewSet
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_get_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedViewSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_get_builder(
        client, &args.name,
    )?;
    contactcenterinsights_projects_locations_authorized_view_sets_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets
/// List AuthorizedViewSets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_list_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_list` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets
/// List AuthorizedViewSets
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_list_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAuthorizedViewSetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListAuthorizedViewSetsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets
/// List AuthorizedViewSets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAuthorizedViewSetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets
/// List AuthorizedViewSets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_list_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAuthorizedViewSetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    contactcenterinsights_projects_locations_authorized_view_sets_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}
/// Updates an AuthorizedViewSet.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_patch_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1AuthorizedViewSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}
/// Updates an AuthorizedViewSet.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_patch_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedViewSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AuthorizedViewSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}
/// Updates an AuthorizedViewSet.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedViewSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1AuthorizedViewSet,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}
/// Updates an AuthorizedViewSet.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_patch_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedViewSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_authorized_view_sets_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews
/// Create AuthorizedView
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    authorizedViewId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1AuthorizedView,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = authorizedViewId {
        query_parts.push(format!("authorizedViewId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews
/// Create AuthorizedView
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AuthorizedView =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews
/// Create AuthorizedView
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: authorizedViewId
    pub authorizedViewId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1AuthorizedView,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews
/// Create AuthorizedView
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_builder(client, &args.parent, args.authorizedViewId.as_deref(), &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}
/// Deletes an AuthorizedView.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}
/// Deletes an AuthorizedView.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}
/// Deletes an AuthorizedView.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}
/// Deletes an AuthorizedView.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:generativeInsights
/// Natural language based Insights which powers the next generation of dashboards in Insights. Next generation of QueryMetrics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_builder(
    client: &SimpleHttpClient,
    location: &str,
    body: &GoogleCloudContactcenterinsightsV1GenerativeInsightsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}:generativeInsights",
        location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:generativeInsights
/// Natural language based Insights which powers the next generation of dashboards in Insights. Next generation of QueryMetrics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:generativeInsights
/// Natural language based Insights which powers the next generation of dashboards in Insights. Next generation of QueryMetrics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsGenerativeInsightsArgs
{
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1GenerativeInsightsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:generativeInsights
/// Natural language based Insights which powers the next generation of dashboards in Insights. Next generation of QueryMetrics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsGenerativeInsightsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_builder(client, &args.location, &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}
/// Get AuthorizedView
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}
/// Get AuthorizedView
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AuthorizedView =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}
/// Get AuthorizedView
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}
/// Get AuthorizedView
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_builder(
            client, &args.name,
        )?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion {
        query_parts.push(format!("options_requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<i32>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_builder(client, &args.resource, args.options_requestedPolicyVersion)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews
/// List AuthorizedViewSets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews
/// List AuthorizedViewSets
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAuthorizedViewsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListAuthorizedViewsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews
/// List AuthorizedViewSets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAuthorizedViewsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews
/// List AuthorizedViewSets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAuthorizedViewsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_builder(client, &args.parent, args.filter.as_deref(), args.orderBy.as_deref(), args.pageSize, args.pageToken.as_deref())?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}
/// Updates an AuthorizedView.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1AuthorizedView,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}
/// Updates an AuthorizedView.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AuthorizedView =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}
/// Updates an AuthorizedView.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1AuthorizedView,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}
/// Updates an AuthorizedView.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AuthorizedView>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_builder(client, &args.name, args.updateMask.as_deref(), &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:queryMetrics
/// Query metrics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_builder(
    client: &SimpleHttpClient,
    location: &str,
    body: &GoogleCloudContactcenterinsightsV1QueryMetricsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}:queryMetrics",
        location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:queryMetrics
/// Query metrics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:queryMetrics
/// Query metrics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsQueryMetricsArgs {
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1QueryMetricsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:queryMetrics
/// Query metrics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsQueryMetricsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_builder(client, &args.location, &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:queryPerformanceOverview
/// Generates a summary of predefined performance metrics for a set of conversations. Conversations can be specified by specifying a time window and an agent id, for now. The summary includes a comparison of metrics computed for conversations in the previous time period, and also a comparison with peers in the same time period.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1QueryPerformanceOverviewRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}:queryPerformanceOverview",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:queryPerformanceOverview
/// Generates a summary of predefined performance metrics for a set of conversations. Conversations can be specified by specifying a time window and an agent id, for now. The summary includes a comparison of metrics computed for conversations in the previous time period, and also a comparison with peers in the same time period.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:queryPerformanceOverview
/// Generates a summary of predefined performance metrics for a set of conversations. Conversations can be specified by specifying a time window and an agent id, for now. The summary includes a comparison of metrics computed for conversations in the previous time period, and also a comparison with peers in the same time period.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsQueryPerformanceOverviewArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1QueryPerformanceOverviewRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:queryPerformanceOverview
/// Generates a summary of predefined performance metrics for a set of conversations. Conversations can be specified by specifying a time window and an agent id, for now. The summary includes a comparison of metrics computed for conversations in the previous time period, and also a comparison with peers in the same time period.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsQueryPerformanceOverviewArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_builder(client, &args.parent, &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews:search
/// SearchAuthorizedViewSets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews:search",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews:search
/// SearchAuthorizedViewSets
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1SearchAuthorizedViewsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1SearchAuthorizedViewsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews:search
/// SearchAuthorizedViewSets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1SearchAuthorizedViewsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsSearchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews:search
/// SearchAuthorizedViewSets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1SearchAuthorizedViewsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_builder(client, &args.parent, args.orderBy.as_deref(), args.pageSize, args.pageToken.as_deref(), args.query.as_deref())?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_search_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleIamV1SetIamPolicyRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_builder(client, &args.resource, &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsTestIamPermissionsArgs
{
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleIamV1TestIamPermissionsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_builder(client, &args.resource, &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations:calculateStats
/// Gets conversation statistics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_builder(
    client: &SimpleHttpClient,
    location: &str,
    filter: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations:calculateStats",
        location,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations:calculateStats
/// Gets conversation statistics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1CalculateStatsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1CalculateStatsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations:calculateStats
/// Gets conversation statistics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1CalculateStatsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsCalculateStatsArgs
{
    /// Path parameter: location
    pub location: String,
    /// Query parameter: filter
    pub filter: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations:calculateStats
/// Gets conversation statistics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsCalculateStatsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1CalculateStatsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_builder(client, &args.location, args.filter.as_deref())?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_calculate_stats_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}
/// Deletes a conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}
/// Deletes a conversation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}
/// Deletes a conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsDeleteArgs
{
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}
/// Deletes a conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_builder(client, &args.name, args.force)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}:generateSignedAudio
/// Gets the signed URI for the audio for the given conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}:generateSignedAudio",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}:generateSignedAudio
/// Gets the signed URI for the audio for the given conversation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudContactcenterinsightsV1GenerateConversationSignedAudioResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1GenerateConversationSignedAudioResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}:generateSignedAudio
/// Gets the signed URI for the audio for the given conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudContactcenterinsightsV1GenerateConversationSignedAudioResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsGenerateSignedAudioArgs
{
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}:generateSignedAudio
/// Gets the signed URI for the audio for the given conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsGenerateSignedAudioArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudContactcenterinsightsV1GenerateConversationSignedAudioResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_generate_signed_audio_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}
/// Gets a conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}
/// Gets a conversation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Conversation =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}
/// Gets a conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsGetArgs
{
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}
/// Gets a conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_builder(client, &args.name, args.view.as_deref())?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations
/// Lists conversations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations
/// Lists conversations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListConversationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations
/// Lists conversations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsListArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations
/// Lists conversations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_builder(client, &args.parent, args.filter.as_deref(), args.orderBy.as_deref(), args.pageSize, args.pageToken.as_deref(), args.view.as_deref())?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}:appeal
/// Appeal an Assessment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContactcenterinsightsV1AppealAssessmentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/assessments/{}:appeal",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}:appeal
/// Appeal an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Assessment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}:appeal
/// Appeal an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsAppealArgs
{
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1AppealAssessmentRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}:appeal
/// Appeal an Assessment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsAppealArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_builder(client, &args.name, &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments
/// Create Assessment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1Assessment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/assessments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments
/// Create Assessment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Assessment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments
/// Create Assessment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsCreateArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Assessment,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments
/// Create Assessment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_builder(client, &args.parent, &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Delete an Assessment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/assessments/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Delete an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Delete an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsDeleteArgs
{
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Delete an Assessment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_builder(client, &args.name, args.force)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}:finalize
/// Finalize an Assessment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContactcenterinsightsV1FinalizeAssessmentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/assessments/{}:finalize",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}:finalize
/// Finalize an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Assessment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}:finalize
/// Finalize an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsFinalizeArgs
{
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1FinalizeAssessmentRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}:finalize
/// Finalize an Assessment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsFinalizeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_builder(client, &args.name, &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Get Assessment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/assessments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Get Assessment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Assessment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Get Assessment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsGetArgs
{
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Get Assessment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments
/// List Assessments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/assessments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments
/// List Assessments.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAssessmentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListAssessmentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments
/// List Assessments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAssessmentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsListArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments
/// List Assessments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAssessmentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_builder(client, &args.parent, args.filter.as_deref(), args.pageSize, args.pageToken.as_deref())?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}:publish
/// Publish an Assessment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContactcenterinsightsV1PublishAssessmentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/assessments/{}:publish",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}:publish
/// Publish an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Assessment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}:publish
/// Publish an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsPublishArgs
{
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1PublishAssessmentRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}:publish
/// Publish an Assessment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsPublishArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_builder(client, &args.name, &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// Create Note.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1Note,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/assessments/{}/notes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// Create Note.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Note>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Note = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// Create Note.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Note>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsNotesCreateArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Note,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// Create Note.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsNotesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Note>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_builder(client, &args.parent, &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Deletes a Note.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/assessments/{}/notes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Deletes a Note.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Deletes a Note.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsNotesDeleteArgs
{
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Deletes a Note.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsNotesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// List Notes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/assessments/{}/notes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// List Notes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListNotesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListNotesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// List Notes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListNotesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsNotesListArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// List Notes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsNotesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListNotesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_builder(client, &args.parent, args.pageSize, args.pageToken.as_deref())?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Update Note.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1Note,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/assessments/{}/notes/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Update Note.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Note>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Note = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Update Note.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Note>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsNotesPatchArgs
{
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Note,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Update Note.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsNotesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Note>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_builder(client, &args.name, args.updateMask.as_deref(), &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels
/// Create feedback label.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    feedbackLabelId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1FeedbackLabel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/feedbackLabels",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = feedbackLabelId {
        query_parts.push(format!("feedbackLabelId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels
/// Create feedback label.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1FeedbackLabel =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels
/// Create feedback label.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsCreateArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: feedbackLabelId
    pub feedbackLabelId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1FeedbackLabel,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels
/// Create feedback label.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_builder(client, &args.parent, args.feedbackLabelId.as_deref(), &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Delete feedback label.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/feedbackLabels/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Delete feedback label.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Delete feedback label.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsDeleteArgs
{
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Delete feedback label.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Get feedback label.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/feedbackLabels/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Get feedback label.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1FeedbackLabel =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Get feedback label.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsGetArgs
{
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Get feedback label.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels
/// List feedback labels.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/feedbackLabels",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels
/// List feedback labels.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListFeedbackLabelsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels
/// List feedback labels.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsListArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels
/// List feedback labels.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_builder(client, &args.parent, args.filter.as_deref(), args.pageSize, args.pageToken.as_deref())?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Update feedback label.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1FeedbackLabel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/conversations/{}/feedbackLabels/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Update feedback label.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1FeedbackLabel =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Update feedback label.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsPatchArgs
{
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1FeedbackLabel,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Update feedback label.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_builder(client, &args.name, args.updateMask.as_deref(), &args.body)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsOperationsCancelArgs
{
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsOperationsGetArgs
{
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_execute()` to send, or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list` for simplest API.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/authorizedViewSets/{}/authorizedViews/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_execute()` or `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsOperationsListArgs
{
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizedViewSets/{authorizedViewSetsId}/authorizedViews/{authorizedViewsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_builder()` + `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_builder(client, &args.name, args.filter.as_deref(), args.pageSize, args.pageToken.as_deref(), args.returnPartialSuccess)?;
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules
/// Creates an auto labeling rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_auto_labeling_rules_create_execute()` to send, or `contactcenterinsights_projects_locations_auto_labeling_rules_create` for simplest API.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    autoLabelingRuleId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1AutoLabelingRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/autoLabelingRules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = autoLabelingRuleId {
        query_parts.push(format!("autoLabelingRuleId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules
/// Creates an auto labeling rule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_auto_labeling_rules_create_execute()` or `contactcenterinsights_projects_locations_auto_labeling_rules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_auto_labeling_rules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AutoLabelingRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AutoLabelingRule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules
/// Creates an auto labeling rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_auto_labeling_rules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_auto_labeling_rules_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_auto_labeling_rules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_auto_labeling_rules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AutoLabelingRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_auto_labeling_rules_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_auto_labeling_rules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAutoLabelingRulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: autoLabelingRuleId
    pub autoLabelingRuleId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1AutoLabelingRule,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules
/// Creates an auto labeling rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_auto_labeling_rules_create_builder()` + `contactcenterinsights_projects_locations_auto_labeling_rules_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_auto_labeling_rules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAutoLabelingRulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AutoLabelingRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_auto_labeling_rules_create_builder(
        client,
        &args.parent,
        args.autoLabelingRuleId.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_auto_labeling_rules_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules/{autoLabelingRulesId}
/// Deletes an auto labeling rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_auto_labeling_rules_delete_execute()` to send, or `contactcenterinsights_projects_locations_auto_labeling_rules_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/autoLabelingRules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules/{autoLabelingRulesId}
/// Deletes an auto labeling rule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_auto_labeling_rules_delete_execute()` or `contactcenterinsights_projects_locations_auto_labeling_rules_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_auto_labeling_rules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules/{autoLabelingRulesId}
/// Deletes an auto labeling rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_auto_labeling_rules_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_auto_labeling_rules_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_auto_labeling_rules_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_auto_labeling_rules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_auto_labeling_rules_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_auto_labeling_rules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAutoLabelingRulesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules/{autoLabelingRulesId}
/// Deletes an auto labeling rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_auto_labeling_rules_delete_builder()` + `contactcenterinsights_projects_locations_auto_labeling_rules_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_auto_labeling_rules_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAutoLabelingRulesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_auto_labeling_rules_delete_builder(
        client, &args.name,
    )?;
    contactcenterinsights_projects_locations_auto_labeling_rules_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules/{autoLabelingRulesId}
/// Gets an auto labeling rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_auto_labeling_rules_get_execute()` to send, or `contactcenterinsights_projects_locations_auto_labeling_rules_get` for simplest API.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/autoLabelingRules/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules/{autoLabelingRulesId}
/// Gets an auto labeling rule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_auto_labeling_rules_get_execute()` or `contactcenterinsights_projects_locations_auto_labeling_rules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_auto_labeling_rules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AutoLabelingRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AutoLabelingRule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules/{autoLabelingRulesId}
/// Gets an auto labeling rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_auto_labeling_rules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_auto_labeling_rules_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_auto_labeling_rules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_auto_labeling_rules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AutoLabelingRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_auto_labeling_rules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_auto_labeling_rules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAutoLabelingRulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules/{autoLabelingRulesId}
/// Gets an auto labeling rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_auto_labeling_rules_get_builder()` + `contactcenterinsights_projects_locations_auto_labeling_rules_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_auto_labeling_rules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAutoLabelingRulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AutoLabelingRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_auto_labeling_rules_get_builder(
        client, &args.name,
    )?;
    contactcenterinsights_projects_locations_auto_labeling_rules_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules
/// Lists auto labeling rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_auto_labeling_rules_list_execute()` to send, or `contactcenterinsights_projects_locations_auto_labeling_rules_list` for simplest API.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/autoLabelingRules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules
/// Lists auto labeling rules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_auto_labeling_rules_list_execute()` or `contactcenterinsights_projects_locations_auto_labeling_rules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_auto_labeling_rules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAutoLabelingRulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListAutoLabelingRulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules
/// Lists auto labeling rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_auto_labeling_rules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_auto_labeling_rules_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_auto_labeling_rules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_auto_labeling_rules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAutoLabelingRulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_auto_labeling_rules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_auto_labeling_rules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAutoLabelingRulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules
/// Lists auto labeling rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_auto_labeling_rules_list_builder()` + `contactcenterinsights_projects_locations_auto_labeling_rules_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_auto_labeling_rules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAutoLabelingRulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAutoLabelingRulesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_auto_labeling_rules_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    contactcenterinsights_projects_locations_auto_labeling_rules_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules/{autoLabelingRulesId}
/// Updates an auto labeling rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_auto_labeling_rules_patch_execute()` to send, or `contactcenterinsights_projects_locations_auto_labeling_rules_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1AutoLabelingRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/autoLabelingRules/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules/{autoLabelingRulesId}
/// Updates an auto labeling rule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_auto_labeling_rules_patch_execute()` or `contactcenterinsights_projects_locations_auto_labeling_rules_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_auto_labeling_rules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AutoLabelingRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1AutoLabelingRule =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules/{autoLabelingRulesId}
/// Updates an auto labeling rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_auto_labeling_rules_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_auto_labeling_rules_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_auto_labeling_rules_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_auto_labeling_rules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AutoLabelingRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_auto_labeling_rules_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_auto_labeling_rules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAutoLabelingRulesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1AutoLabelingRule,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules/{autoLabelingRulesId}
/// Updates an auto labeling rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_auto_labeling_rules_patch_builder()` + `contactcenterinsights_projects_locations_auto_labeling_rules_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_auto_labeling_rules_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAutoLabelingRulesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1AutoLabelingRule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_auto_labeling_rules_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_auto_labeling_rules_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules:test
/// Tests auto labeling rules against a conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_auto_labeling_rules_test_execute()` to send, or `contactcenterinsights_projects_locations_auto_labeling_rules_test` for simplest API.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_test_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1TestAutoLabelingRuleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/autoLabelingRules:test",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules:test
/// Tests auto labeling rules against a conversation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_auto_labeling_rules_test_execute()` or `contactcenterinsights_projects_locations_auto_labeling_rules_test`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_auto_labeling_rules_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_test_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1TestAutoLabelingRuleResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1TestAutoLabelingRuleResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules:test
/// Tests auto labeling rules against a conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_auto_labeling_rules_test_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_auto_labeling_rules_test_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_auto_labeling_rules_test()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_auto_labeling_rules_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1TestAutoLabelingRuleResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_auto_labeling_rules_test_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_auto_labeling_rules_test`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsAutoLabelingRulesTestArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1TestAutoLabelingRuleRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/autoLabelingRules:test
/// Tests auto labeling rules against a conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_auto_labeling_rules_test_builder()` + `contactcenterinsights_projects_locations_auto_labeling_rules_test_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_auto_labeling_rules_test_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_auto_labeling_rules_test(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsAutoLabelingRulesTestArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1TestAutoLabelingRuleResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_auto_labeling_rules_test_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_auto_labeling_rules_test_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:bulkAnalyze
/// Analyzes multiple conversations in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_bulk_analyze_execute()` to send, or `contactcenterinsights_projects_locations_conversations_bulk_analyze` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_bulk_analyze_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1BulkAnalyzeConversationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations:bulkAnalyze",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:bulkAnalyze
/// Analyzes multiple conversations in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_bulk_analyze_execute()` or `contactcenterinsights_projects_locations_conversations_bulk_analyze`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_bulk_analyze_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_bulk_analyze_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:bulkAnalyze
/// Analyzes multiple conversations in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_bulk_analyze_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_bulk_analyze_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_bulk_analyze()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_bulk_analyze_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_bulk_analyze_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_bulk_analyze_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_bulk_analyze`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsBulkAnalyzeArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1BulkAnalyzeConversationsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:bulkAnalyze
/// Analyzes multiple conversations in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_bulk_analyze_builder()` + `contactcenterinsights_projects_locations_conversations_bulk_analyze_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_bulk_analyze_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_bulk_analyze(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsBulkAnalyzeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_bulk_analyze_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_conversations_bulk_analyze_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:bulkDelete
/// Deletes multiple conversations in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_bulk_delete_execute()` to send, or `contactcenterinsights_projects_locations_conversations_bulk_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_bulk_delete_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1BulkDeleteConversationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations:bulkDelete",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:bulkDelete
/// Deletes multiple conversations in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_bulk_delete_execute()` or `contactcenterinsights_projects_locations_conversations_bulk_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_bulk_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_bulk_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:bulkDelete
/// Deletes multiple conversations in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_bulk_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_bulk_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_bulk_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_bulk_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_bulk_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_bulk_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_bulk_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsBulkDeleteArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1BulkDeleteConversationsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:bulkDelete
/// Deletes multiple conversations in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_bulk_delete_builder()` + `contactcenterinsights_projects_locations_conversations_bulk_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_bulk_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_bulk_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsBulkDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_bulk_delete_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_conversations_bulk_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:calculateStats
/// Gets conversation statistics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_calculate_stats_execute()` to send, or `contactcenterinsights_projects_locations_conversations_calculate_stats` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_calculate_stats_builder(
    client: &SimpleHttpClient,
    location: &str,
    filter: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations:calculateStats",
        location,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:calculateStats
/// Gets conversation statistics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_calculate_stats_execute()` or `contactcenterinsights_projects_locations_conversations_calculate_stats`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_calculate_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_calculate_stats_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1CalculateStatsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1CalculateStatsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:calculateStats
/// Gets conversation statistics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_calculate_stats_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_calculate_stats_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_calculate_stats()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_calculate_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_calculate_stats_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1CalculateStatsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_calculate_stats_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_calculate_stats`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsCalculateStatsArgs {
    /// Path parameter: location
    pub location: String,
    /// Query parameter: filter
    pub filter: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:calculateStats
/// Gets conversation statistics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_calculate_stats_builder()` + `contactcenterinsights_projects_locations_conversations_calculate_stats_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_calculate_stats_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_calculate_stats(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsCalculateStatsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1CalculateStatsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_calculate_stats_builder(
        client,
        &args.location,
        args.filter.as_deref(),
    )?;
    contactcenterinsights_projects_locations_conversations_calculate_stats_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations
/// Creates a conversation. Note that this method does not support audio transcription or redaction. Use conversations.upload instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_create_execute()` to send, or `contactcenterinsights_projects_locations_conversations_create` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    conversationId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1Conversation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = conversationId {
        query_parts.push(format!("conversationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations
/// Creates a conversation. Note that this method does not support audio transcription or redaction. Use conversations.upload instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_create_execute()` or `contactcenterinsights_projects_locations_conversations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Conversation =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations
/// Creates a conversation. Note that this method does not support audio transcription or redaction. Use conversations.upload instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: conversationId
    pub conversationId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Conversation,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations
/// Creates a conversation. Note that this method does not support audio transcription or redaction. Use conversations.upload instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_create_builder()` + `contactcenterinsights_projects_locations_conversations_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_create_builder(
        client,
        &args.parent,
        args.conversationId.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_conversations_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}
/// Deletes a conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_delete_execute()` to send, or `contactcenterinsights_projects_locations_conversations_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}
/// Deletes a conversation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_delete_execute()` or `contactcenterinsights_projects_locations_conversations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}
/// Deletes a conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}
/// Deletes a conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_delete_builder()` + `contactcenterinsights_projects_locations_conversations_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_delete_builder(
        client, &args.name, args.force,
    )?;
    contactcenterinsights_projects_locations_conversations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}:generateSignedAudio
/// Gets the signed URI for the audio for the given conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_generate_signed_audio_execute()` to send, or `contactcenterinsights_projects_locations_conversations_generate_signed_audio` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_generate_signed_audio_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}:generateSignedAudio",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}:generateSignedAudio
/// Gets the signed URI for the audio for the given conversation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_generate_signed_audio_execute()` or `contactcenterinsights_projects_locations_conversations_generate_signed_audio`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_generate_signed_audio_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_generate_signed_audio_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudContactcenterinsightsV1GenerateConversationSignedAudioResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1GenerateConversationSignedAudioResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}:generateSignedAudio
/// Gets the signed URI for the audio for the given conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_generate_signed_audio_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_generate_signed_audio_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_generate_signed_audio()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_generate_signed_audio_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_generate_signed_audio_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudContactcenterinsightsV1GenerateConversationSignedAudioResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_generate_signed_audio_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_generate_signed_audio`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsGenerateSignedAudioArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}:generateSignedAudio
/// Gets the signed URI for the audio for the given conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_generate_signed_audio_builder()` + `contactcenterinsights_projects_locations_conversations_generate_signed_audio_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_generate_signed_audio_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_generate_signed_audio(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsGenerateSignedAudioArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudContactcenterinsightsV1GenerateConversationSignedAudioResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_generate_signed_audio_builder(
            client, &args.name,
        )?;
    contactcenterinsights_projects_locations_conversations_generate_signed_audio_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}
/// Gets a conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_get_execute()` to send, or `contactcenterinsights_projects_locations_conversations_get` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}
/// Gets a conversation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_get_execute()` or `contactcenterinsights_projects_locations_conversations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Conversation =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}
/// Gets a conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}
/// Gets a conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_get_builder()` + `contactcenterinsights_projects_locations_conversations_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_get_builder(
        client,
        &args.name,
        args.view.as_deref(),
    )?;
    contactcenterinsights_projects_locations_conversations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:ingest
/// Imports conversations and processes them according to the user's configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_ingest_execute()` to send, or `contactcenterinsights_projects_locations_conversations_ingest` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_ingest_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1IngestConversationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations:ingest",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:ingest
/// Imports conversations and processes them according to the user's configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_ingest_execute()` or `contactcenterinsights_projects_locations_conversations_ingest`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_ingest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_ingest_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:ingest
/// Imports conversations and processes them according to the user's configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_ingest_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_ingest_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_ingest()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_ingest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_ingest_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_ingest_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_ingest`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsIngestArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1IngestConversationsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:ingest
/// Imports conversations and processes them according to the user's configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_ingest_builder()` + `contactcenterinsights_projects_locations_conversations_ingest_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_ingest_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_ingest(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsIngestArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_ingest_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_conversations_ingest_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations
/// Lists conversations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_list_execute()` to send, or `contactcenterinsights_projects_locations_conversations_list` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations
/// Lists conversations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_list_execute()` or `contactcenterinsights_projects_locations_conversations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListConversationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations
/// Lists conversations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations
/// Lists conversations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_list_builder()` + `contactcenterinsights_projects_locations_conversations_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.view.as_deref(),
    )?;
    contactcenterinsights_projects_locations_conversations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}
/// Updates a conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_patch_execute()` to send, or `contactcenterinsights_projects_locations_conversations_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    conversationAutoLabelingUpdateConfig_allowAutoLabelingUpdate: Option<bool>,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1Conversation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = conversationAutoLabelingUpdateConfig_allowAutoLabelingUpdate {
        query_parts.push(format!(
            "conversationAutoLabelingUpdateConfig_allowAutoLabelingUpdate={}",
            val
        ));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}
/// Updates a conversation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_patch_execute()` or `contactcenterinsights_projects_locations_conversations_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Conversation =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}
/// Updates a conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: conversationAutoLabelingUpdateConfig_allowAutoLabelingUpdate
    pub conversationAutoLabelingUpdateConfig_allowAutoLabelingUpdate: Option<bool>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Conversation,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}
/// Updates a conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_patch_builder()` + `contactcenterinsights_projects_locations_conversations_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_patch_builder(
        client,
        &args.name,
        args.allowMissing,
        args.conversationAutoLabelingUpdateConfig_allowAutoLabelingUpdate,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_conversations_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:sample
/// Samples conversations based on user configuration and handles the sampled conversations for different use cases.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_sample_execute()` to send, or `contactcenterinsights_projects_locations_conversations_sample` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_sample_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1SampleConversationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations:sample",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:sample
/// Samples conversations based on user configuration and handles the sampled conversations for different use cases.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_sample_execute()` or `contactcenterinsights_projects_locations_conversations_sample`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_sample_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_sample_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:sample
/// Samples conversations based on user configuration and handles the sampled conversations for different use cases.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_sample_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_sample_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_sample()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_sample_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_sample_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_sample_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_sample`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsSampleArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1SampleConversationsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:sample
/// Samples conversations based on user configuration and handles the sampled conversations for different use cases.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_sample_builder()` + `contactcenterinsights_projects_locations_conversations_sample_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_sample_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_sample(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsSampleArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_sample_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_conversations_sample_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:upload
/// Create a long-running conversation upload operation. This method differs from CreateConversation by allowing audio transcription and optional DLP redaction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_upload_execute()` to send, or `contactcenterinsights_projects_locations_conversations_upload` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_upload_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1UploadConversationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations:upload",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:upload
/// Create a long-running conversation upload operation. This method differs from CreateConversation by allowing audio transcription and optional DLP redaction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_upload_execute()` or `contactcenterinsights_projects_locations_conversations_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:upload
/// Create a long-running conversation upload operation. This method differs from CreateConversation by allowing audio transcription and optional DLP redaction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_upload_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsUploadArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1UploadConversationRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations:upload
/// Create a long-running conversation upload operation. This method differs from CreateConversation by allowing audio transcription and optional DLP redaction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_upload_builder()` + `contactcenterinsights_projects_locations_conversations_upload_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_upload(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsUploadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_upload_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_conversations_upload_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses
/// Creates an analysis. The long running operation is done when the analysis has completed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_analyses_create_execute()` to send, or `contactcenterinsights_projects_locations_conversations_analyses_create` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_analyses_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1Analysis,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/analyses",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses
/// Creates an analysis. The long running operation is done when the analysis has completed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_analyses_create_execute()` or `contactcenterinsights_projects_locations_conversations_analyses_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_analyses_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_analyses_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses
/// Creates an analysis. The long running operation is done when the analysis has completed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_analyses_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_analyses_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_analyses_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_analyses_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_analyses_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_analyses_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_analyses_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAnalysesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Analysis,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses
/// Creates an analysis. The long running operation is done when the analysis has completed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_analyses_create_builder()` + `contactcenterinsights_projects_locations_conversations_analyses_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_analyses_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_analyses_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAnalysesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_analyses_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_conversations_analyses_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses/{analysesId}
/// Deletes an analysis.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_analyses_delete_execute()` to send, or `contactcenterinsights_projects_locations_conversations_analyses_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_analyses_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/analyses/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses/{analysesId}
/// Deletes an analysis.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_analyses_delete_execute()` or `contactcenterinsights_projects_locations_conversations_analyses_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_analyses_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_analyses_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses/{analysesId}
/// Deletes an analysis.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_analyses_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_analyses_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_analyses_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_analyses_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_analyses_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_analyses_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_analyses_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAnalysesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses/{analysesId}
/// Deletes an analysis.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_analyses_delete_builder()` + `contactcenterinsights_projects_locations_conversations_analyses_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_analyses_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_analyses_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAnalysesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_analyses_delete_builder(
        client, &args.name,
    )?;
    contactcenterinsights_projects_locations_conversations_analyses_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses/{analysesId}
/// Gets an analysis.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_analyses_get_execute()` to send, or `contactcenterinsights_projects_locations_conversations_analyses_get` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_analyses_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/analyses/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses/{analysesId}
/// Gets an analysis.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_analyses_get_execute()` or `contactcenterinsights_projects_locations_conversations_analyses_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_analyses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_analyses_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Analysis>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Analysis =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses/{analysesId}
/// Gets an analysis.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_analyses_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_analyses_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_analyses_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_analyses_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_analyses_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Analysis>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_analyses_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_analyses_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAnalysesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses/{analysesId}
/// Gets an analysis.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_analyses_get_builder()` + `contactcenterinsights_projects_locations_conversations_analyses_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_analyses_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_analyses_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAnalysesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Analysis>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_analyses_get_builder(
        client, &args.name,
    )?;
    contactcenterinsights_projects_locations_conversations_analyses_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses
/// Lists analyses.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_analyses_list_execute()` to send, or `contactcenterinsights_projects_locations_conversations_analyses_list` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_analyses_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/analyses",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses
/// Lists analyses.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_analyses_list_execute()` or `contactcenterinsights_projects_locations_conversations_analyses_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_analyses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_analyses_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAnalysesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListAnalysesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses
/// Lists analyses.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_analyses_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_analyses_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_analyses_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_analyses_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_analyses_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAnalysesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_analyses_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_analyses_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAnalysesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/analyses
/// Lists analyses.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_analyses_list_builder()` + `contactcenterinsights_projects_locations_conversations_analyses_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_analyses_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_analyses_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAnalysesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAnalysesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_analyses_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    contactcenterinsights_projects_locations_conversations_analyses_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}:appeal
/// Appeal an Assessment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_assessments_appeal_execute()` to send, or `contactcenterinsights_projects_locations_conversations_assessments_appeal` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_assessments_appeal_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContactcenterinsightsV1AppealAssessmentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/assessments/{}:appeal",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}:appeal
/// Appeal an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_assessments_appeal_execute()` or `contactcenterinsights_projects_locations_conversations_assessments_appeal`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_appeal_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_appeal_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Assessment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}:appeal
/// Appeal an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_assessments_appeal_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_appeal_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_assessments_appeal()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_appeal_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_assessments_appeal_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_assessments_appeal_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_assessments_appeal`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAssessmentsAppealArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1AppealAssessmentRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}:appeal
/// Appeal an Assessment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_assessments_appeal_builder()` + `contactcenterinsights_projects_locations_conversations_assessments_appeal_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_appeal_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_appeal(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsAppealArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_assessments_appeal_builder(
            client, &args.name, &args.body,
        )?;
    contactcenterinsights_projects_locations_conversations_assessments_appeal_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments
/// Create Assessment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_assessments_create_execute()` to send, or `contactcenterinsights_projects_locations_conversations_assessments_create` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_assessments_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1Assessment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/assessments",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments
/// Create Assessment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_assessments_create_execute()` or `contactcenterinsights_projects_locations_conversations_assessments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Assessment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments
/// Create Assessment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_assessments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_assessments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_assessments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_assessments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_assessments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAssessmentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Assessment,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments
/// Create Assessment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_assessments_create_builder()` + `contactcenterinsights_projects_locations_conversations_assessments_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_assessments_create_builder(
            client,
            &args.parent,
            &args.body,
        )?;
    contactcenterinsights_projects_locations_conversations_assessments_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Delete an Assessment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_assessments_delete_execute()` to send, or `contactcenterinsights_projects_locations_conversations_assessments_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_assessments_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/assessments/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Delete an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_assessments_delete_execute()` or `contactcenterinsights_projects_locations_conversations_assessments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Delete an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_assessments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_assessments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_assessments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_assessments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_assessments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAssessmentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Delete an Assessment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_assessments_delete_builder()` + `contactcenterinsights_projects_locations_conversations_assessments_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_assessments_delete_builder(
            client, &args.name, args.force,
        )?;
    contactcenterinsights_projects_locations_conversations_assessments_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}:finalize
/// Finalize an Assessment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_assessments_finalize_execute()` to send, or `contactcenterinsights_projects_locations_conversations_assessments_finalize` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_assessments_finalize_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContactcenterinsightsV1FinalizeAssessmentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/assessments/{}:finalize",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}:finalize
/// Finalize an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_assessments_finalize_execute()` or `contactcenterinsights_projects_locations_conversations_assessments_finalize`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_finalize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_finalize_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Assessment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}:finalize
/// Finalize an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_assessments_finalize_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_finalize_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_assessments_finalize()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_finalize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_assessments_finalize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_assessments_finalize_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_assessments_finalize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAssessmentsFinalizeArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1FinalizeAssessmentRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}:finalize
/// Finalize an Assessment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_assessments_finalize_builder()` + `contactcenterinsights_projects_locations_conversations_assessments_finalize_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_finalize_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_finalize(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsFinalizeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_assessments_finalize_builder(
            client, &args.name, &args.body,
        )?;
    contactcenterinsights_projects_locations_conversations_assessments_finalize_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Get Assessment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_assessments_get_execute()` to send, or `contactcenterinsights_projects_locations_conversations_assessments_get` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_assessments_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/assessments/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Get Assessment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_assessments_get_execute()` or `contactcenterinsights_projects_locations_conversations_assessments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Assessment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Get Assessment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_assessments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_assessments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_assessments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_assessments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_assessments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAssessmentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}
/// Get Assessment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_assessments_get_builder()` + `contactcenterinsights_projects_locations_conversations_assessments_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_assessments_get_builder(
        client, &args.name,
    )?;
    contactcenterinsights_projects_locations_conversations_assessments_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments
/// List Assessments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_assessments_list_execute()` to send, or `contactcenterinsights_projects_locations_conversations_assessments_list` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_assessments_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/assessments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments
/// List Assessments.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_assessments_list_execute()` or `contactcenterinsights_projects_locations_conversations_assessments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAssessmentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListAssessmentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments
/// List Assessments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_assessments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_assessments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_assessments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAssessmentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_assessments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_assessments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAssessmentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments
/// List Assessments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_assessments_list_builder()` + `contactcenterinsights_projects_locations_conversations_assessments_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAssessmentsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_conversations_assessments_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    contactcenterinsights_projects_locations_conversations_assessments_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}:publish
/// Publish an Assessment.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_assessments_publish_execute()` to send, or `contactcenterinsights_projects_locations_conversations_assessments_publish` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_assessments_publish_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContactcenterinsightsV1PublishAssessmentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/assessments/{}:publish",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}:publish
/// Publish an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_assessments_publish_execute()` or `contactcenterinsights_projects_locations_conversations_assessments_publish`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_publish_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Assessment =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}:publish
/// Publish an Assessment.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_assessments_publish_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_publish_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_assessments_publish()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_publish_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_assessments_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_assessments_publish_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_assessments_publish`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAssessmentsPublishArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1PublishAssessmentRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}:publish
/// Publish an Assessment.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_assessments_publish_builder()` + `contactcenterinsights_projects_locations_conversations_assessments_publish_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_publish_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_publish(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsPublishArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Assessment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_assessments_publish_builder(
            client, &args.name, &args.body,
        )?;
    contactcenterinsights_projects_locations_conversations_assessments_publish_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// Create Note.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_assessments_notes_create_execute()` to send, or `contactcenterinsights_projects_locations_conversations_assessments_notes_create` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1Note,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/assessments/{}/notes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// Create Note.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_assessments_notes_create_execute()` or `contactcenterinsights_projects_locations_conversations_assessments_notes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_notes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Note>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Note = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// Create Note.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_assessments_notes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_notes_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_assessments_notes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_notes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Note>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_assessments_notes_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_assessments_notes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAssessmentsNotesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Note,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// Create Note.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_assessments_notes_create_builder()` + `contactcenterinsights_projects_locations_conversations_assessments_notes_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_notes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsNotesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Note>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_assessments_notes_create_builder(
            client,
            &args.parent,
            &args.body,
        )?;
    contactcenterinsights_projects_locations_conversations_assessments_notes_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Deletes a Note.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_assessments_notes_delete_execute()` to send, or `contactcenterinsights_projects_locations_conversations_assessments_notes_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/assessments/{}/notes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Deletes a Note.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_assessments_notes_delete_execute()` or `contactcenterinsights_projects_locations_conversations_assessments_notes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_notes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Deletes a Note.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_assessments_notes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_notes_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_assessments_notes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_notes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_assessments_notes_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_assessments_notes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAssessmentsNotesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Deletes a Note.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_assessments_notes_delete_builder()` + `contactcenterinsights_projects_locations_conversations_assessments_notes_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_notes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsNotesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_assessments_notes_delete_builder(
            client, &args.name,
        )?;
    contactcenterinsights_projects_locations_conversations_assessments_notes_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// List Notes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_assessments_notes_list_execute()` to send, or `contactcenterinsights_projects_locations_conversations_assessments_notes_list` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/assessments/{}/notes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// List Notes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_assessments_notes_list_execute()` or `contactcenterinsights_projects_locations_conversations_assessments_notes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_notes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListNotesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListNotesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// List Notes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_assessments_notes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_notes_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_assessments_notes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_notes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListNotesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_assessments_notes_list_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_assessments_notes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAssessmentsNotesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes
/// List Notes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_assessments_notes_list_builder()` + `contactcenterinsights_projects_locations_conversations_assessments_notes_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_notes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsNotesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListNotesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_assessments_notes_list_builder(
            client,
            &args.parent,
            args.pageSize,
            args.pageToken.as_deref(),
        )?;
    contactcenterinsights_projects_locations_conversations_assessments_notes_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Update Note.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_assessments_notes_patch_execute()` to send, or `contactcenterinsights_projects_locations_conversations_assessments_notes_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1Note,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/assessments/{}/notes/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Update Note.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_assessments_notes_patch_execute()` or `contactcenterinsights_projects_locations_conversations_assessments_notes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_notes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Note>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Note = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Update Note.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_assessments_notes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_notes_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_assessments_notes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_assessments_notes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Note>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_assessments_notes_patch_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_assessments_notes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsAssessmentsNotesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Note,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/assessments/{assessmentsId}/notes/{notesId}
/// Update Note.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_assessments_notes_patch_builder()` + `contactcenterinsights_projects_locations_conversations_assessments_notes_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_assessments_notes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsNotesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Note>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_assessments_notes_patch_builder(
            client,
            &args.name,
            args.updateMask.as_deref(),
            &args.body,
        )?;
    contactcenterinsights_projects_locations_conversations_assessments_notes_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels
/// Create feedback label.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_feedback_labels_create_execute()` to send, or `contactcenterinsights_projects_locations_conversations_feedback_labels_create` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    feedbackLabelId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1FeedbackLabel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/feedbackLabels",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = feedbackLabelId {
        query_parts.push(format!("feedbackLabelId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels
/// Create feedback label.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_feedback_labels_create_execute()` or `contactcenterinsights_projects_locations_conversations_feedback_labels_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_feedback_labels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1FeedbackLabel =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels
/// Create feedback label.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_feedback_labels_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_feedback_labels_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_feedback_labels_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_feedback_labels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_feedback_labels_create_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_feedback_labels_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: feedbackLabelId
    pub feedbackLabelId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1FeedbackLabel,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels
/// Create feedback label.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_feedback_labels_create_builder()` + `contactcenterinsights_projects_locations_conversations_feedback_labels_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_feedback_labels_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_feedback_labels_create_builder(
            client,
            &args.parent,
            args.feedbackLabelId.as_deref(),
            &args.body,
        )?;
    contactcenterinsights_projects_locations_conversations_feedback_labels_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Delete feedback label.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_feedback_labels_delete_execute()` to send, or `contactcenterinsights_projects_locations_conversations_feedback_labels_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/feedbackLabels/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Delete feedback label.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_feedback_labels_delete_execute()` or `contactcenterinsights_projects_locations_conversations_feedback_labels_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_feedback_labels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Delete feedback label.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_feedback_labels_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_feedback_labels_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_feedback_labels_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_feedback_labels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_conversations_feedback_labels_delete_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_feedback_labels_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Delete feedback label.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_feedback_labels_delete_builder()` + `contactcenterinsights_projects_locations_conversations_feedback_labels_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_feedback_labels_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_feedback_labels_delete_builder(
            client, &args.name,
        )?;
    contactcenterinsights_projects_locations_conversations_feedback_labels_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Get feedback label.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_feedback_labels_get_execute()` to send, or `contactcenterinsights_projects_locations_conversations_feedback_labels_get` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/feedbackLabels/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Get feedback label.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_feedback_labels_get_execute()` or `contactcenterinsights_projects_locations_conversations_feedback_labels_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_feedback_labels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1FeedbackLabel =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Get feedback label.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_feedback_labels_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_feedback_labels_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_feedback_labels_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_feedback_labels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_feedback_labels_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_feedback_labels_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Get feedback label.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_feedback_labels_get_builder()` + `contactcenterinsights_projects_locations_conversations_feedback_labels_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_feedback_labels_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_feedback_labels_get_builder(
            client, &args.name,
        )?;
    contactcenterinsights_projects_locations_conversations_feedback_labels_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels
/// List feedback labels.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_feedback_labels_list_execute()` to send, or `contactcenterinsights_projects_locations_conversations_feedback_labels_list` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/feedbackLabels",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels
/// List feedback labels.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_feedback_labels_list_execute()` or `contactcenterinsights_projects_locations_conversations_feedback_labels_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_feedback_labels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListFeedbackLabelsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels
/// List feedback labels.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_feedback_labels_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_feedback_labels_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_feedback_labels_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_feedback_labels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_feedback_labels_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_feedback_labels_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels
/// List feedback labels.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_feedback_labels_list_builder()` + `contactcenterinsights_projects_locations_conversations_feedback_labels_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_feedback_labels_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_feedback_labels_list_builder(
            client,
            &args.parent,
            args.filter.as_deref(),
            args.pageSize,
            args.pageToken.as_deref(),
        )?;
    contactcenterinsights_projects_locations_conversations_feedback_labels_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Update feedback label.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_feedback_labels_patch_execute()` to send, or `contactcenterinsights_projects_locations_conversations_feedback_labels_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1FeedbackLabel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/feedbackLabels/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Update feedback label.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_feedback_labels_patch_execute()` or `contactcenterinsights_projects_locations_conversations_feedback_labels_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_feedback_labels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1FeedbackLabel =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Update feedback label.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_feedback_labels_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_feedback_labels_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_feedback_labels_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_feedback_labels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_feedback_labels_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_feedback_labels_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1FeedbackLabel,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Update feedback label.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_feedback_labels_patch_builder()` + `contactcenterinsights_projects_locations_conversations_feedback_labels_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_feedback_labels_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_feedback_labels_patch_builder(
            client,
            &args.name,
            args.updateMask.as_deref(),
            &args.body,
        )?;
    contactcenterinsights_projects_locations_conversations_feedback_labels_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/segments:bulkAnalyze
/// Analyzes multiple conversations in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_execute()` to send, or `contactcenterinsights_projects_locations_conversations_segments_bulk_analyze` for simplest API.

pub fn contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1BulkAnalyzeConversationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/conversations/{}/segments:bulkAnalyze",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/segments:bulkAnalyze
/// Analyzes multiple conversations in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_execute()` or `contactcenterinsights_projects_locations_conversations_segments_bulk_analyze`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/segments:bulkAnalyze
/// Analyzes multiple conversations in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_conversations_segments_bulk_analyze()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_conversations_segments_bulk_analyze`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsConversationsSegmentsBulkAnalyzeArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1BulkAnalyzeConversationsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversations/{conversationsId}/segments:bulkAnalyze
/// Analyzes multiple conversations in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_builder()` + `contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_conversations_segments_bulk_analyze(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsConversationsSegmentsBulkAnalyzeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_builder(
            client,
            &args.parent,
            &args.body,
        )?;
    contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards
/// Creates a Dashboard.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_dashboards_create_execute()` to send, or `contactcenterinsights_projects_locations_dashboards_create` for simplest API.

pub fn contactcenterinsights_projects_locations_dashboards_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    dashboardId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1Dashboard,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/dashboards",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dashboardId {
        query_parts.push(format!("dashboardId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards
/// Creates a Dashboard.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_dashboards_create_execute()` or `contactcenterinsights_projects_locations_dashboards_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dashboard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Dashboard =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards
/// Creates a Dashboard.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_dashboards_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_dashboards_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_dashboards_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dashboard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_dashboards_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_dashboards_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDashboardsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: dashboardId
    pub dashboardId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Dashboard,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards
/// Creates a Dashboard.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_dashboards_create_builder()` + `contactcenterinsights_projects_locations_dashboards_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDashboardsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dashboard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_dashboards_create_builder(
        client,
        &args.parent,
        args.dashboardId.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_dashboards_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}
/// Deletes a Dashboard.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_dashboards_delete_execute()` to send, or `contactcenterinsights_projects_locations_dashboards_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_dashboards_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/dashboards/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}
/// Deletes a Dashboard.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_dashboards_delete_execute()` or `contactcenterinsights_projects_locations_dashboards_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}
/// Deletes a Dashboard.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_dashboards_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_dashboards_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_dashboards_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_dashboards_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_dashboards_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDashboardsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}
/// Deletes a Dashboard.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_dashboards_delete_builder()` + `contactcenterinsights_projects_locations_dashboards_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDashboardsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_dashboards_delete_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_dashboards_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}
/// Gets a Dashboard.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_dashboards_get_execute()` to send, or `contactcenterinsights_projects_locations_dashboards_get` for simplest API.

pub fn contactcenterinsights_projects_locations_dashboards_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/dashboards/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}
/// Gets a Dashboard.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_dashboards_get_execute()` or `contactcenterinsights_projects_locations_dashboards_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dashboard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Dashboard =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}
/// Gets a Dashboard.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_dashboards_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_dashboards_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_dashboards_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dashboard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_dashboards_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_dashboards_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDashboardsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}
/// Gets a Dashboard.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_dashboards_get_builder()` + `contactcenterinsights_projects_locations_dashboards_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDashboardsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dashboard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_dashboards_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_dashboards_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards
/// Lists Dashboards.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_dashboards_list_execute()` to send, or `contactcenterinsights_projects_locations_dashboards_list` for simplest API.

pub fn contactcenterinsights_projects_locations_dashboards_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/dashboards",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards
/// Lists Dashboards.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_dashboards_list_execute()` or `contactcenterinsights_projects_locations_dashboards_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListDashboardsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListDashboardsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards
/// Lists Dashboards.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_dashboards_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_dashboards_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_dashboards_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListDashboardsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_dashboards_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_dashboards_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDashboardsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards
/// Lists Dashboards.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_dashboards_list_builder()` + `contactcenterinsights_projects_locations_dashboards_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDashboardsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListDashboardsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_dashboards_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    contactcenterinsights_projects_locations_dashboards_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}
/// Updates a Dashboard.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_dashboards_patch_execute()` to send, or `contactcenterinsights_projects_locations_dashboards_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_dashboards_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1Dashboard,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/dashboards/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}
/// Updates a Dashboard.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_dashboards_patch_execute()` or `contactcenterinsights_projects_locations_dashboards_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dashboard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Dashboard =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}
/// Updates a Dashboard.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_dashboards_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_dashboards_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_dashboards_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dashboard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_dashboards_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_dashboards_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDashboardsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Dashboard,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}
/// Updates a Dashboard.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_dashboards_patch_builder()` + `contactcenterinsights_projects_locations_dashboards_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDashboardsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dashboard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_dashboards_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_dashboards_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts
/// Creates a Chart.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_dashboards_charts_create_execute()` to send, or `contactcenterinsights_projects_locations_dashboards_charts_create` for simplest API.

pub fn contactcenterinsights_projects_locations_dashboards_charts_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    chartId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1Chart,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/dashboards/{}/charts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = chartId {
        query_parts.push(format!("chartId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts
/// Creates a Chart.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_dashboards_charts_create_execute()` or `contactcenterinsights_projects_locations_dashboards_charts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_charts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_charts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Chart>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Chart =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts
/// Creates a Chart.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_dashboards_charts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_charts_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_dashboards_charts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_charts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_dashboards_charts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Chart>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_dashboards_charts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_dashboards_charts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDashboardsChartsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: chartId
    pub chartId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Chart,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts
/// Creates a Chart.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_dashboards_charts_create_builder()` + `contactcenterinsights_projects_locations_dashboards_charts_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_charts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_charts_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDashboardsChartsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Chart>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_dashboards_charts_create_builder(
        client,
        &args.parent,
        args.chartId.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_dashboards_charts_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts/{chartsId}
/// Deletes a Chart.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_dashboards_charts_delete_execute()` to send, or `contactcenterinsights_projects_locations_dashboards_charts_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_dashboards_charts_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/dashboards/{}/charts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts/{chartsId}
/// Deletes a Chart.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_dashboards_charts_delete_execute()` or `contactcenterinsights_projects_locations_dashboards_charts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_charts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_charts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts/{chartsId}
/// Deletes a Chart.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_dashboards_charts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_charts_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_dashboards_charts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_charts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_dashboards_charts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_dashboards_charts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_dashboards_charts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDashboardsChartsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts/{chartsId}
/// Deletes a Chart.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_dashboards_charts_delete_builder()` + `contactcenterinsights_projects_locations_dashboards_charts_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_charts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_charts_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDashboardsChartsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_dashboards_charts_delete_builder(
        client, &args.name,
    )?;
    contactcenterinsights_projects_locations_dashboards_charts_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts/{chartsId}
/// Gets a Chart.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_dashboards_charts_get_execute()` to send, or `contactcenterinsights_projects_locations_dashboards_charts_get` for simplest API.

pub fn contactcenterinsights_projects_locations_dashboards_charts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/dashboards/{}/charts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts/{chartsId}
/// Gets a Chart.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_dashboards_charts_get_execute()` or `contactcenterinsights_projects_locations_dashboards_charts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_charts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_charts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Chart>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Chart =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts/{chartsId}
/// Gets a Chart.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_dashboards_charts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_charts_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_dashboards_charts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_charts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_dashboards_charts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Chart>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_dashboards_charts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_dashboards_charts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDashboardsChartsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts/{chartsId}
/// Gets a Chart.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_dashboards_charts_get_builder()` + `contactcenterinsights_projects_locations_dashboards_charts_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_charts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_charts_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDashboardsChartsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Chart>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_dashboards_charts_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_dashboards_charts_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts
/// Lists Charts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_dashboards_charts_list_execute()` to send, or `contactcenterinsights_projects_locations_dashboards_charts_list` for simplest API.

pub fn contactcenterinsights_projects_locations_dashboards_charts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/dashboards/{}/charts",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts
/// Lists Charts.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_dashboards_charts_list_execute()` or `contactcenterinsights_projects_locations_dashboards_charts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_charts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_charts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListChartsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListChartsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts
/// Lists Charts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_dashboards_charts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_charts_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_dashboards_charts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_charts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_dashboards_charts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListChartsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_dashboards_charts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_dashboards_charts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDashboardsChartsListArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts
/// Lists Charts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_dashboards_charts_list_builder()` + `contactcenterinsights_projects_locations_dashboards_charts_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_charts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_charts_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDashboardsChartsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListChartsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_dashboards_charts_list_builder(
        client,
        &args.parent,
    )?;
    contactcenterinsights_projects_locations_dashboards_charts_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts/{chartsId}
/// Updates a Chart.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_dashboards_charts_patch_execute()` to send, or `contactcenterinsights_projects_locations_dashboards_charts_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_dashboards_charts_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1Chart,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/dashboards/{}/charts/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts/{chartsId}
/// Updates a Chart.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_dashboards_charts_patch_execute()` or `contactcenterinsights_projects_locations_dashboards_charts_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_charts_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_charts_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Chart>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Chart =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts/{chartsId}
/// Updates a Chart.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_dashboards_charts_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_charts_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_dashboards_charts_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_dashboards_charts_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_dashboards_charts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Chart>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_dashboards_charts_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_dashboards_charts_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDashboardsChartsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Chart,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dashboards/{dashboardsId}/charts/{chartsId}
/// Updates a Chart.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_dashboards_charts_patch_builder()` + `contactcenterinsights_projects_locations_dashboards_charts_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_dashboards_charts_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_dashboards_charts_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDashboardsChartsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Chart>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_dashboards_charts_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_dashboards_charts_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:bulkDeleteFeedbackLabels
/// Delete feedback labels in bulk using a filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_execute()` to send, or `contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1BulkDeleteFeedbackLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}:bulkDeleteFeedbackLabels",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:bulkDeleteFeedbackLabels
/// Delete feedback labels in bulk using a filter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_execute()` or `contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:bulkDeleteFeedbackLabels
/// Delete feedback labels in bulk using a filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsBulkDeleteFeedbackLabelsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1BulkDeleteFeedbackLabelsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:bulkDeleteFeedbackLabels
/// Delete feedback labels in bulk using a filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_builder()` + `contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsBulkDeleteFeedbackLabelsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_builder(
            client,
            &args.parent,
            &args.body,
        )?;
    contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:bulkDownloadFeedbackLabels
/// Download feedback labels in bulk from an external source. Currently supports exporting Quality AI example conversations with transcripts and question bodies.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_execute()` to send, or `contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1BulkDownloadFeedbackLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}:bulkDownloadFeedbackLabels",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:bulkDownloadFeedbackLabels
/// Download feedback labels in bulk from an external source. Currently supports exporting Quality AI example conversations with transcripts and question bodies.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_execute()` or `contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:bulkDownloadFeedbackLabels
/// Download feedback labels in bulk from an external source. Currently supports exporting Quality AI example conversations with transcripts and question bodies.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsBulkDownloadFeedbackLabelsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1BulkDownloadFeedbackLabelsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:bulkDownloadFeedbackLabels
/// Download feedback labels in bulk from an external source. Currently supports exporting Quality AI example conversations with transcripts and question bodies.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_builder()` + `contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsBulkDownloadFeedbackLabelsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_builder(
            client,
            &args.parent,
            &args.body,
        )?;
    contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:bulkUploadFeedbackLabels
/// Upload feedback labels from an external source in bulk. Currently supports labeling Quality AI example conversations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_execute()` to send, or `contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1BulkUploadFeedbackLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}:bulkUploadFeedbackLabels",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:bulkUploadFeedbackLabels
/// Upload feedback labels from an external source in bulk. Currently supports labeling Quality AI example conversations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_execute()` or `contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:bulkUploadFeedbackLabels
/// Upload feedback labels from an external source in bulk. Currently supports labeling Quality AI example conversations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsBulkUploadFeedbackLabelsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1BulkUploadFeedbackLabelsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:bulkUploadFeedbackLabels
/// Upload feedback labels from an external source in bulk. Currently supports labeling Quality AI example conversations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_builder()` + `contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsBulkUploadFeedbackLabelsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_builder(
            client,
            &args.parent,
            &args.body,
        )?;
    contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_create_execute()` to send, or `contactcenterinsights_projects_locations_datasets_create` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    datasetId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1Dataset,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = datasetId {
        query_parts.push(format!("datasetId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_create_execute()` or `contactcenterinsights_projects_locations_datasets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_datasets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: datasetId
    pub datasetId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Dataset,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_create_builder()` + `contactcenterinsights_projects_locations_datasets_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_create_builder(
        client,
        &args.parent,
        args.datasetId.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_datasets_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Delete a dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_delete_execute()` to send, or `contactcenterinsights_projects_locations_datasets_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Delete a dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_delete_execute()` or `contactcenterinsights_projects_locations_datasets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Delete a dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_datasets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Delete a dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_delete_builder()` + `contactcenterinsights_projects_locations_datasets_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_datasets_delete_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_datasets_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets a dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_get_execute()` to send, or `contactcenterinsights_projects_locations_datasets_get` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets a dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_get_execute()` or `contactcenterinsights_projects_locations_datasets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets a dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_datasets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets a dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_get_builder()` + `contactcenterinsights_projects_locations_datasets_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_datasets_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_datasets_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// List datasets matching the input.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_list_execute()` to send, or `contactcenterinsights_projects_locations_datasets_list` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// List datasets matching the input.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_list_execute()` or `contactcenterinsights_projects_locations_datasets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListDatasetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListDatasetsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// List datasets matching the input.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListDatasetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_datasets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets
/// List datasets matching the input.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_list_builder()` + `contactcenterinsights_projects_locations_datasets_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListDatasetsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    contactcenterinsights_projects_locations_datasets_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:listAllFeedbackLabels
/// List all feedback labels by project number.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_execute()` to send, or `contactcenterinsights_projects_locations_datasets_list_all_feedback_labels` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}:listAllFeedbackLabels",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:listAllFeedbackLabels
/// List all feedback labels by project number.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_execute()` or `contactcenterinsights_projects_locations_datasets_list_all_feedback_labels`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAllFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListAllFeedbackLabelsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:listAllFeedbackLabels
/// List all feedback labels by project number.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_list_all_feedback_labels()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAllFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_list_all_feedback_labels`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsListAllFeedbackLabelsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:listAllFeedbackLabels
/// List all feedback labels by project number.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_builder()` + `contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_list_all_feedback_labels(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsListAllFeedbackLabelsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListAllFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_builder(
            client,
            &args.parent,
            args.filter.as_deref(),
            args.pageSize,
            args.pageToken.as_deref(),
        )?;
    contactcenterinsights_projects_locations_datasets_list_all_feedback_labels_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Updates a dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_patch_execute()` to send, or `contactcenterinsights_projects_locations_datasets_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1Dataset,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Updates a dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_patch_execute()` or `contactcenterinsights_projects_locations_datasets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Updates a dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_datasets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Dataset,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Updates a dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_patch_builder()` + `contactcenterinsights_projects_locations_datasets_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_datasets_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:bulkDelete
/// Deletes multiple conversations in a single request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_execute()` to send, or `contactcenterinsights_projects_locations_datasets_conversations_bulk_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1BulkDeleteConversationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/conversations:bulkDelete",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:bulkDelete
/// Deletes multiple conversations in a single request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_execute()` or `contactcenterinsights_projects_locations_datasets_conversations_bulk_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:bulkDelete
/// Deletes multiple conversations in a single request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_conversations_bulk_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_conversations_bulk_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsConversationsBulkDeleteArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1BulkDeleteConversationsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:bulkDelete
/// Deletes multiple conversations in a single request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_builder()` + `contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_bulk_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsBulkDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_builder(
            client,
            &args.parent,
            &args.body,
        )?;
    contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:calculateStats
/// Gets conversation statistics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_execute()` to send, or `contactcenterinsights_projects_locations_datasets_conversations_calculate_stats` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_builder(
    client: &SimpleHttpClient,
    location: &str,
    body: &GoogleCloudContactcenterinsightsV1CalculateStatsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/conversations:calculateStats",
        location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:calculateStats
/// Gets conversation statistics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_execute()` or `contactcenterinsights_projects_locations_datasets_conversations_calculate_stats`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1CalculateStatsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1CalculateStatsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:calculateStats
/// Gets conversation statistics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_conversations_calculate_stats()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1CalculateStatsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_conversations_calculate_stats`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsConversationsCalculateStatsArgs {
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1CalculateStatsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:calculateStats
/// Gets conversation statistics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_builder()` + `contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_calculate_stats(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsCalculateStatsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1CalculateStatsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_builder(
            client,
            &args.location,
            &args.body,
        )?;
    contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}
/// Deletes a conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_conversations_delete_execute()` to send, or `contactcenterinsights_projects_locations_datasets_conversations_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_conversations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/conversations/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}
/// Deletes a conversation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_conversations_delete_execute()` or `contactcenterinsights_projects_locations_datasets_conversations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}
/// Deletes a conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_conversations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_conversations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_conversations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_datasets_conversations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_conversations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsConversationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}
/// Deletes a conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_conversations_delete_builder()` + `contactcenterinsights_projects_locations_datasets_conversations_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_conversations_delete_builder(
        client, &args.name, args.force,
    )?;
    contactcenterinsights_projects_locations_datasets_conversations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}:generateSignedAudio
/// Gets the signed URI for the audio for the given conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_execute()` to send, or `contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/conversations/{}:generateSignedAudio",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}:generateSignedAudio
/// Gets the signed URI for the audio for the given conversation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_execute()` or `contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudContactcenterinsightsV1GenerateConversationSignedAudioResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1GenerateConversationSignedAudioResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}:generateSignedAudio
/// Gets the signed URI for the audio for the given conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudContactcenterinsightsV1GenerateConversationSignedAudioResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsConversationsGenerateSignedAudioArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}:generateSignedAudio
/// Gets the signed URI for the audio for the given conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_builder()` + `contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsGenerateSignedAudioArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudContactcenterinsightsV1GenerateConversationSignedAudioResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_datasets_conversations_generate_signed_audio_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}
/// Gets a conversation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_conversations_get_execute()` to send, or `contactcenterinsights_projects_locations_datasets_conversations_get` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_conversations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/conversations/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}
/// Gets a conversation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_conversations_get_execute()` or `contactcenterinsights_projects_locations_datasets_conversations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Conversation =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}
/// Gets a conversation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_conversations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_conversations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_conversations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_datasets_conversations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_conversations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsConversationsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}
/// Gets a conversation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_conversations_get_builder()` + `contactcenterinsights_projects_locations_datasets_conversations_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Conversation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_conversations_get_builder(
        client,
        &args.name,
        args.view.as_deref(),
    )?;
    contactcenterinsights_projects_locations_datasets_conversations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:ingest
/// Imports conversations and processes them according to the user's configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_conversations_ingest_execute()` to send, or `contactcenterinsights_projects_locations_datasets_conversations_ingest` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_conversations_ingest_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1IngestConversationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/conversations:ingest",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:ingest
/// Imports conversations and processes them according to the user's configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_conversations_ingest_execute()` or `contactcenterinsights_projects_locations_datasets_conversations_ingest`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_ingest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_ingest_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:ingest
/// Imports conversations and processes them according to the user's configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_conversations_ingest_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_ingest_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_conversations_ingest()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_ingest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_conversations_ingest_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_datasets_conversations_ingest_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_conversations_ingest`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsConversationsIngestArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1IngestConversationsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:ingest
/// Imports conversations and processes them according to the user's configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_conversations_ingest_builder()` + `contactcenterinsights_projects_locations_datasets_conversations_ingest_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_ingest_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_ingest(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsIngestArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_conversations_ingest_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_datasets_conversations_ingest_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations
/// Lists conversations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_conversations_list_execute()` to send, or `contactcenterinsights_projects_locations_datasets_conversations_list` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_conversations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/conversations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations
/// Lists conversations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_conversations_list_execute()` or `contactcenterinsights_projects_locations_datasets_conversations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListConversationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations
/// Lists conversations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_conversations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_conversations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_conversations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_datasets_conversations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_conversations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsConversationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations
/// Lists conversations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_conversations_list_builder()` + `contactcenterinsights_projects_locations_datasets_conversations_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListConversationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_conversations_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.view.as_deref(),
    )?;
    contactcenterinsights_projects_locations_datasets_conversations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:sample
/// Samples conversations based on user configuration and handles the sampled conversations for different use cases.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_conversations_sample_execute()` to send, or `contactcenterinsights_projects_locations_datasets_conversations_sample` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_conversations_sample_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1SampleConversationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/conversations:sample",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:sample
/// Samples conversations based on user configuration and handles the sampled conversations for different use cases.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_conversations_sample_execute()` or `contactcenterinsights_projects_locations_datasets_conversations_sample`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_sample_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_sample_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:sample
/// Samples conversations based on user configuration and handles the sampled conversations for different use cases.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_conversations_sample_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_sample_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_conversations_sample()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_sample_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_conversations_sample_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_datasets_conversations_sample_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_conversations_sample`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsConversationsSampleArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1SampleConversationsRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations:sample
/// Samples conversations based on user configuration and handles the sampled conversations for different use cases.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_conversations_sample_builder()` + `contactcenterinsights_projects_locations_datasets_conversations_sample_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_sample_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_sample(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsSampleArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_conversations_sample_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_datasets_conversations_sample_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels
/// Create feedback label.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_execute()` to send, or `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    feedbackLabelId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1FeedbackLabel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/conversations/{}/feedbackLabels",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = feedbackLabelId {
        query_parts.push(format!("feedbackLabelId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels
/// Create feedback label.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_execute()` or `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1FeedbackLabel =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels
/// Create feedback label.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: feedbackLabelId
    pub feedbackLabelId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1FeedbackLabel,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels
/// Create feedback label.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_builder()` + `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_builder(client, &args.parent, args.feedbackLabelId.as_deref(), &args.body)?;
    contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Delete feedback label.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_execute()` to send, or `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/conversations/{}/feedbackLabels/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Delete feedback label.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_execute()` or `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Delete feedback label.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Delete feedback label.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_builder()` + `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Get feedback label.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_execute()` to send, or `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/conversations/{}/feedbackLabels/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Get feedback label.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_execute()` or `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1FeedbackLabel =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Get feedback label.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Get feedback label.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_builder()` + `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels
/// List feedback labels.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_execute()` to send, or `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/conversations/{}/feedbackLabels",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels
/// List feedback labels.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_execute()` or `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListFeedbackLabelsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels
/// List feedback labels.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels
/// List feedback labels.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_builder()` + `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListFeedbackLabelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_builder(client, &args.parent, args.filter.as_deref(), args.pageSize, args.pageToken.as_deref())?;
    contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Update feedback label.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_execute()` to send, or `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1FeedbackLabel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/conversations/{}/feedbackLabels/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Update feedback label.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_execute()` or `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1FeedbackLabel =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Update feedback label.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1FeedbackLabel,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/conversations/{conversationsId}/feedbackLabels/{feedbackLabelsId}
/// Update feedback label.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_builder()` + `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1FeedbackLabel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_builder(client, &args.name, args.updateMask.as_deref(), &args.body)?;
    contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/insightsdata:export
/// Export insights data to a destination defined in the request body.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_datasets_insightsdata_export_execute()` to send, or `contactcenterinsights_projects_locations_datasets_insightsdata_export` for simplest API.

pub fn contactcenterinsights_projects_locations_datasets_insightsdata_export_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1ExportInsightsDataRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/datasets/{}/insightsdata:export",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/insightsdata:export
/// Export insights data to a destination defined in the request body.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_datasets_insightsdata_export_execute()` or `contactcenterinsights_projects_locations_datasets_insightsdata_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_insightsdata_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_insightsdata_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/insightsdata:export
/// Export insights data to a destination defined in the request body.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_datasets_insightsdata_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_insightsdata_export_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_datasets_insightsdata_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_datasets_insightsdata_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_datasets_insightsdata_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_datasets_insightsdata_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_datasets_insightsdata_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsDatasetsInsightsdataExportArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1ExportInsightsDataRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/insightsdata:export
/// Export insights data to a destination defined in the request body.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_datasets_insightsdata_export_builder()` + `contactcenterinsights_projects_locations_datasets_insightsdata_export_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_datasets_insightsdata_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_datasets_insightsdata_export(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsDatasetsInsightsdataExportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_datasets_insightsdata_export_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_datasets_insightsdata_export_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/encryptionSpec:initialize
/// Initializes a location-level encryption key specification. An error will result if the location has resources already created before the initialization. After the encryption specification is initialized at a location, it is immutable and all newly created resources under the location will be encrypted with the existing specification.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_encryption_spec_initialize_execute()` to send, or `contactcenterinsights_projects_locations_encryption_spec_initialize` for simplest API.

pub fn contactcenterinsights_projects_locations_encryption_spec_initialize_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContactcenterinsightsV1InitializeEncryptionSpecRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/encryptionSpec:initialize",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/encryptionSpec:initialize
/// Initializes a location-level encryption key specification. An error will result if the location has resources already created before the initialization. After the encryption specification is initialized at a location, it is immutable and all newly created resources under the location will be encrypted with the existing specification.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_encryption_spec_initialize_execute()` or `contactcenterinsights_projects_locations_encryption_spec_initialize`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_encryption_spec_initialize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_encryption_spec_initialize_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/encryptionSpec:initialize
/// Initializes a location-level encryption key specification. An error will result if the location has resources already created before the initialization. After the encryption specification is initialized at a location, it is immutable and all newly created resources under the location will be encrypted with the existing specification.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_encryption_spec_initialize_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_encryption_spec_initialize_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_encryption_spec_initialize()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_encryption_spec_initialize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_encryption_spec_initialize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_encryption_spec_initialize_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_encryption_spec_initialize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsEncryptionSpecInitializeArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1InitializeEncryptionSpecRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/encryptionSpec:initialize
/// Initializes a location-level encryption key specification. An error will result if the location has resources already created before the initialization. After the encryption specification is initialized at a location, it is immutable and all newly created resources under the location will be encrypted with the existing specification.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_encryption_spec_initialize_builder()` + `contactcenterinsights_projects_locations_encryption_spec_initialize_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_encryption_spec_initialize_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_encryption_spec_initialize(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsEncryptionSpecInitializeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_encryption_spec_initialize_builder(
        client, &args.name, &args.body,
    )?;
    contactcenterinsights_projects_locations_encryption_spec_initialize_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightsdata:export
/// Export insights data to a destination defined in the request body.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_insightsdata_export_execute()` to send, or `contactcenterinsights_projects_locations_insightsdata_export` for simplest API.

pub fn contactcenterinsights_projects_locations_insightsdata_export_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1ExportInsightsDataRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/insightsdata:export",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightsdata:export
/// Export insights data to a destination defined in the request body.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_insightsdata_export_execute()` or `contactcenterinsights_projects_locations_insightsdata_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_insightsdata_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_insightsdata_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightsdata:export
/// Export insights data to a destination defined in the request body.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_insightsdata_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_insightsdata_export_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_insightsdata_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_insightsdata_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_insightsdata_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_insightsdata_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_insightsdata_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsInsightsdataExportArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1ExportInsightsDataRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/insightsdata:export
/// Export insights data to a destination defined in the request body.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_insightsdata_export_builder()` + `contactcenterinsights_projects_locations_insightsdata_export_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_insightsdata_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_insightsdata_export(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsInsightsdataExportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_insightsdata_export_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_insightsdata_export_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:calculateIssueModelStats
/// Gets an issue model's statistics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_builder(
    client: &SimpleHttpClient,
    issueModel: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels/{}:calculateIssueModelStats",
        issueModel,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:calculateIssueModelStats
/// Gets an issue model's statistics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_execute()` or `contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1CalculateIssueModelStatsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1CalculateIssueModelStatsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:calculateIssueModelStats
/// Gets an issue model's statistics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1CalculateIssueModelStatsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsCalculateIssueModelStatsArgs {
    /// Path parameter: issueModel
    pub issueModel: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:calculateIssueModelStats
/// Gets an issue model's statistics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_builder()` + `contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsCalculateIssueModelStatsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1CalculateIssueModelStatsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_builder(
            client,
            &args.issueModel,
        )?;
    contactcenterinsights_projects_locations_issue_models_calculate_issue_model_stats_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels
/// Creates an issue model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_create_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_create` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1IssueModel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels
/// Creates an issue model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_create_execute()` or `contactcenterinsights_projects_locations_issue_models_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels
/// Creates an issue model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_issue_models_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1IssueModel,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels
/// Creates an issue model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_create_builder()` + `contactcenterinsights_projects_locations_issue_models_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_issue_models_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_issue_models_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}
/// Deletes an issue model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_delete_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}
/// Deletes an issue model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_delete_execute()` or `contactcenterinsights_projects_locations_issue_models_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}
/// Deletes an issue model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_issue_models_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}
/// Deletes an issue model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_delete_builder()` + `contactcenterinsights_projects_locations_issue_models_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_issue_models_delete_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_issue_models_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:deploy
/// Deploys an issue model. Returns an error if a model is already deployed. An issue model can only be used in analysis after it has been deployed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_deploy_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_deploy` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_deploy_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContactcenterinsightsV1DeployIssueModelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels/{}:deploy",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:deploy
/// Deploys an issue model. Returns an error if a model is already deployed. An issue model can only be used in analysis after it has been deployed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_deploy_execute()` or `contactcenterinsights_projects_locations_issue_models_deploy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_deploy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:deploy
/// Deploys an issue model. Returns an error if a model is already deployed. An issue model can only be used in analysis after it has been deployed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_deploy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_deploy_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_deploy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_issue_models_deploy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_deploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsDeployArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1DeployIssueModelRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:deploy
/// Deploys an issue model. Returns an error if a model is already deployed. An issue model can only be used in analysis after it has been deployed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_deploy_builder()` + `contactcenterinsights_projects_locations_issue_models_deploy_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_deploy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_deploy(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsDeployArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_issue_models_deploy_builder(
        client, &args.name, &args.body,
    )?;
    contactcenterinsights_projects_locations_issue_models_deploy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:export
/// Exports an issue model to the provided destination.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_export_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_export` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_export_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContactcenterinsightsV1ExportIssueModelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels/{}:export",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:export
/// Exports an issue model to the provided destination.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_export_execute()` or `contactcenterinsights_projects_locations_issue_models_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:export
/// Exports an issue model to the provided destination.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_export_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_issue_models_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsExportArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1ExportIssueModelRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:export
/// Exports an issue model to the provided destination.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_export_builder()` + `contactcenterinsights_projects_locations_issue_models_export_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_export(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsExportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_issue_models_export_builder(
        client, &args.name, &args.body,
    )?;
    contactcenterinsights_projects_locations_issue_models_export_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}
/// Gets an issue model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_get_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_get` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}
/// Gets an issue model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_get_execute()` or `contactcenterinsights_projects_locations_issue_models_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1IssueModel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1IssueModel =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}
/// Gets an issue model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1IssueModel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_issue_models_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}
/// Gets an issue model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_get_builder()` + `contactcenterinsights_projects_locations_issue_models_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1IssueModel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_issue_models_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_issue_models_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels:import
/// Imports an issue model from a Cloud Storage bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_import_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_import` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_import_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1ImportIssueModelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels:import",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels:import
/// Imports an issue model from a Cloud Storage bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_import_execute()` or `contactcenterinsights_projects_locations_issue_models_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels:import
/// Imports an issue model from a Cloud Storage bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_import_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_issue_models_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsImportArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1ImportIssueModelRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels:import
/// Imports an issue model from a Cloud Storage bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_import_builder()` + `contactcenterinsights_projects_locations_issue_models_import_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_import(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_issue_models_import_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_issue_models_import_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels
/// Lists issue models.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_list_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_list` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels
/// Lists issue models.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_list_execute()` or `contactcenterinsights_projects_locations_issue_models_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListIssueModelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListIssueModelsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels
/// Lists issue models.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListIssueModelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_issue_models_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsListArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels
/// Lists issue models.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_list_builder()` + `contactcenterinsights_projects_locations_issue_models_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListIssueModelsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_issue_models_list_builder(client, &args.parent)?;
    contactcenterinsights_projects_locations_issue_models_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}
/// Updates an issue model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_patch_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1IssueModel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}
/// Updates an issue model.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_patch_execute()` or `contactcenterinsights_projects_locations_issue_models_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1IssueModel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1IssueModel =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}
/// Updates an issue model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1IssueModel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_issue_models_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1IssueModel,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}
/// Updates an issue model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_patch_builder()` + `contactcenterinsights_projects_locations_issue_models_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1IssueModel>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_issue_models_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_issue_models_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:undeploy
/// Undeploys an issue model. An issue model can not be used in analysis after it has been undeployed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_undeploy_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_undeploy` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_undeploy_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContactcenterinsightsV1UndeployIssueModelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels/{}:undeploy",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:undeploy
/// Undeploys an issue model. An issue model can not be used in analysis after it has been undeployed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_undeploy_execute()` or `contactcenterinsights_projects_locations_issue_models_undeploy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_undeploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_undeploy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:undeploy
/// Undeploys an issue model. An issue model can not be used in analysis after it has been undeployed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_undeploy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_undeploy_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_undeploy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_undeploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_undeploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_issue_models_undeploy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_undeploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsUndeployArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1UndeployIssueModelRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}:undeploy
/// Undeploys an issue model. An issue model can not be used in analysis after it has been undeployed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_undeploy_builder()` + `contactcenterinsights_projects_locations_issue_models_undeploy_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_undeploy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_undeploy(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsUndeployArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_issue_models_undeploy_builder(
        client, &args.name, &args.body,
    )?;
    contactcenterinsights_projects_locations_issue_models_undeploy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues
/// Creates an issue.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_issues_create_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_issues_create` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_issues_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1Issue,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels/{}/issues",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues
/// Creates an issue.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_issues_create_execute()` or `contactcenterinsights_projects_locations_issue_models_issues_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_issues_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_issues_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues
/// Creates an issue.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_issues_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_issues_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_issues_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_issues_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_issues_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_issue_models_issues_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_issues_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsIssuesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Issue,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues
/// Creates an issue.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_issues_create_builder()` + `contactcenterinsights_projects_locations_issue_models_issues_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_issues_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_issues_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsIssuesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_issue_models_issues_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_issue_models_issues_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues/{issuesId}
/// Deletes an issue.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_issues_delete_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_issues_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_issues_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels/{}/issues/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues/{issuesId}
/// Deletes an issue.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_issues_delete_execute()` or `contactcenterinsights_projects_locations_issue_models_issues_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_issues_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_issues_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues/{issuesId}
/// Deletes an issue.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_issues_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_issues_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_issues_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_issues_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_issues_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_issue_models_issues_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_issues_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsIssuesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues/{issuesId}
/// Deletes an issue.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_issues_delete_builder()` + `contactcenterinsights_projects_locations_issue_models_issues_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_issues_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_issues_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsIssuesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_issue_models_issues_delete_builder(
        client, &args.name,
    )?;
    contactcenterinsights_projects_locations_issue_models_issues_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues/{issuesId}
/// Gets an issue.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_issues_get_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_issues_get` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_issues_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels/{}/issues/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues/{issuesId}
/// Gets an issue.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_issues_get_execute()` or `contactcenterinsights_projects_locations_issue_models_issues_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_issues_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_issues_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Issue>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Issue =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues/{issuesId}
/// Gets an issue.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_issues_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_issues_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_issues_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_issues_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_issues_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Issue>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_issue_models_issues_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_issues_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsIssuesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues/{issuesId}
/// Gets an issue.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_issues_get_builder()` + `contactcenterinsights_projects_locations_issue_models_issues_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_issues_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_issues_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsIssuesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Issue>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_issue_models_issues_get_builder(
        client, &args.name,
    )?;
    contactcenterinsights_projects_locations_issue_models_issues_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues
/// Lists issues.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_issues_list_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_issues_list` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_issues_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels/{}/issues",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues
/// Lists issues.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_issues_list_execute()` or `contactcenterinsights_projects_locations_issue_models_issues_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_issues_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_issues_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListIssuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListIssuesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues
/// Lists issues.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_issues_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_issues_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_issues_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_issues_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_issues_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListIssuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_issue_models_issues_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_issues_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsIssuesListArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues
/// Lists issues.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_issues_list_builder()` + `contactcenterinsights_projects_locations_issue_models_issues_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_issues_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_issues_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsIssuesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListIssuesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_issue_models_issues_list_builder(
        client,
        &args.parent,
    )?;
    contactcenterinsights_projects_locations_issue_models_issues_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues/{issuesId}
/// Updates an issue.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_issue_models_issues_patch_execute()` to send, or `contactcenterinsights_projects_locations_issue_models_issues_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_issue_models_issues_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1Issue,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/issueModels/{}/issues/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues/{issuesId}
/// Updates an issue.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_issue_models_issues_patch_execute()` or `contactcenterinsights_projects_locations_issue_models_issues_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_issues_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_issues_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Issue>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Issue =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues/{issuesId}
/// Updates an issue.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_issue_models_issues_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_issues_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_issue_models_issues_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_issue_models_issues_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_issue_models_issues_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Issue>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_issue_models_issues_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_issue_models_issues_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsIssueModelsIssuesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1Issue,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/issueModels/{issueModelsId}/issues/{issuesId}
/// Updates an issue.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_issue_models_issues_patch_builder()` + `contactcenterinsights_projects_locations_issue_models_issues_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_issue_models_issues_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_issue_models_issues_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsIssueModelsIssuesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1Issue>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_issue_models_issues_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_issue_models_issues_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_operations_cancel_execute()` to send, or `contactcenterinsights_projects_locations_operations_cancel` for simplest API.

pub fn contactcenterinsights_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_operations_cancel_execute()` or `contactcenterinsights_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_operations_cancel_builder()` + `contactcenterinsights_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_operations_cancel_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_operations_get_execute()` to send, or `contactcenterinsights_projects_locations_operations_get` for simplest API.

pub fn contactcenterinsights_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_operations_get_execute()` or `contactcenterinsights_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_operations_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_operations_get_builder()` + `contactcenterinsights_projects_locations_operations_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_operations_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_operations_list_execute()` to send, or `contactcenterinsights_projects_locations_operations_list` for simplest API.

pub fn contactcenterinsights_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_operations_list_execute()` or `contactcenterinsights_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_operations_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_operations_list_builder()` + `contactcenterinsights_projects_locations_operations_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    contactcenterinsights_projects_locations_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers
/// Creates a phrase matcher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_phrase_matchers_create_execute()` to send, or `contactcenterinsights_projects_locations_phrase_matchers_create` for simplest API.

pub fn contactcenterinsights_projects_locations_phrase_matchers_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1PhraseMatcher,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/phraseMatchers",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers
/// Creates a phrase matcher.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_phrase_matchers_create_execute()` or `contactcenterinsights_projects_locations_phrase_matchers_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_phrase_matchers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_phrase_matchers_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1PhraseMatcher>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1PhraseMatcher =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers
/// Creates a phrase matcher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_phrase_matchers_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_phrase_matchers_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_phrase_matchers_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_phrase_matchers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_phrase_matchers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1PhraseMatcher>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_phrase_matchers_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_phrase_matchers_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsPhraseMatchersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1PhraseMatcher,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers
/// Creates a phrase matcher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_phrase_matchers_create_builder()` + `contactcenterinsights_projects_locations_phrase_matchers_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_phrase_matchers_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_phrase_matchers_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsPhraseMatchersCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1PhraseMatcher>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_phrase_matchers_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_phrase_matchers_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers/{phraseMatchersId}
/// Deletes a phrase matcher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_phrase_matchers_delete_execute()` to send, or `contactcenterinsights_projects_locations_phrase_matchers_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_phrase_matchers_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/phraseMatchers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers/{phraseMatchersId}
/// Deletes a phrase matcher.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_phrase_matchers_delete_execute()` or `contactcenterinsights_projects_locations_phrase_matchers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_phrase_matchers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_phrase_matchers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers/{phraseMatchersId}
/// Deletes a phrase matcher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_phrase_matchers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_phrase_matchers_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_phrase_matchers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_phrase_matchers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_phrase_matchers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_phrase_matchers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_phrase_matchers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsPhraseMatchersDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers/{phraseMatchersId}
/// Deletes a phrase matcher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_phrase_matchers_delete_builder()` + `contactcenterinsights_projects_locations_phrase_matchers_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_phrase_matchers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_phrase_matchers_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsPhraseMatchersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_phrase_matchers_delete_builder(
        client, &args.name,
    )?;
    contactcenterinsights_projects_locations_phrase_matchers_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers/{phraseMatchersId}
/// Gets a phrase matcher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_phrase_matchers_get_execute()` to send, or `contactcenterinsights_projects_locations_phrase_matchers_get` for simplest API.

pub fn contactcenterinsights_projects_locations_phrase_matchers_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/phraseMatchers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers/{phraseMatchersId}
/// Gets a phrase matcher.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_phrase_matchers_get_execute()` or `contactcenterinsights_projects_locations_phrase_matchers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_phrase_matchers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_phrase_matchers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1PhraseMatcher>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1PhraseMatcher =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers/{phraseMatchersId}
/// Gets a phrase matcher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_phrase_matchers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_phrase_matchers_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_phrase_matchers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_phrase_matchers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_phrase_matchers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1PhraseMatcher>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_phrase_matchers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_phrase_matchers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsPhraseMatchersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers/{phraseMatchersId}
/// Gets a phrase matcher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_phrase_matchers_get_builder()` + `contactcenterinsights_projects_locations_phrase_matchers_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_phrase_matchers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_phrase_matchers_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsPhraseMatchersGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1PhraseMatcher>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_phrase_matchers_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_phrase_matchers_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers
/// Lists phrase matchers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_phrase_matchers_list_execute()` to send, or `contactcenterinsights_projects_locations_phrase_matchers_list` for simplest API.

pub fn contactcenterinsights_projects_locations_phrase_matchers_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/phraseMatchers",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers
/// Lists phrase matchers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_phrase_matchers_list_execute()` or `contactcenterinsights_projects_locations_phrase_matchers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_phrase_matchers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_phrase_matchers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListPhraseMatchersResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListPhraseMatchersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers
/// Lists phrase matchers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_phrase_matchers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_phrase_matchers_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_phrase_matchers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_phrase_matchers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_phrase_matchers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListPhraseMatchersResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_phrase_matchers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_phrase_matchers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsPhraseMatchersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers
/// Lists phrase matchers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_phrase_matchers_list_builder()` + `contactcenterinsights_projects_locations_phrase_matchers_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_phrase_matchers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_phrase_matchers_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsPhraseMatchersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListPhraseMatchersResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_phrase_matchers_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    contactcenterinsights_projects_locations_phrase_matchers_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers/{phraseMatchersId}
/// Updates a phrase matcher.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_phrase_matchers_patch_execute()` to send, or `contactcenterinsights_projects_locations_phrase_matchers_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_phrase_matchers_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1PhraseMatcher,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/phraseMatchers/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers/{phraseMatchersId}
/// Updates a phrase matcher.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_phrase_matchers_patch_execute()` or `contactcenterinsights_projects_locations_phrase_matchers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_phrase_matchers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_phrase_matchers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1PhraseMatcher>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1PhraseMatcher =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers/{phraseMatchersId}
/// Updates a phrase matcher.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_phrase_matchers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_phrase_matchers_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_phrase_matchers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_phrase_matchers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_phrase_matchers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1PhraseMatcher>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_phrase_matchers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_phrase_matchers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsPhraseMatchersPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1PhraseMatcher,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/phraseMatchers/{phraseMatchersId}
/// Updates a phrase matcher.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_phrase_matchers_patch_builder()` + `contactcenterinsights_projects_locations_phrase_matchers_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_phrase_matchers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_phrase_matchers_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsPhraseMatchersPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1PhraseMatcher>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_phrase_matchers_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_phrase_matchers_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags
/// Creates a QaQuestionTag.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_question_tags_create_execute()` to send, or `contactcenterinsights_projects_locations_qa_question_tags_create` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_question_tags_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    qaQuestionTagId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1QaQuestionTag,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaQuestionTags",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = qaQuestionTagId {
        query_parts.push(format!("qaQuestionTagId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags
/// Creates a QaQuestionTag.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_question_tags_create_execute()` or `contactcenterinsights_projects_locations_qa_question_tags_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_question_tags_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_question_tags_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestionTag>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1QaQuestionTag =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags
/// Creates a QaQuestionTag.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_question_tags_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_question_tags_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_question_tags_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_question_tags_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_question_tags_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestionTag>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_qa_question_tags_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_question_tags_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaQuestionTagsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: qaQuestionTagId
    pub qaQuestionTagId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1QaQuestionTag,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags
/// Creates a QaQuestionTag.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_question_tags_create_builder()` + `contactcenterinsights_projects_locations_qa_question_tags_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_question_tags_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_question_tags_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaQuestionTagsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestionTag>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_question_tags_create_builder(
        client,
        &args.parent,
        args.qaQuestionTagId.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_qa_question_tags_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags/{qaQuestionTagsId}
/// Deletes a QaQuestionTag.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_question_tags_delete_execute()` to send, or `contactcenterinsights_projects_locations_qa_question_tags_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_question_tags_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaQuestionTags/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags/{qaQuestionTagsId}
/// Deletes a QaQuestionTag.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_question_tags_delete_execute()` or `contactcenterinsights_projects_locations_qa_question_tags_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_question_tags_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_question_tags_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags/{qaQuestionTagsId}
/// Deletes a QaQuestionTag.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_question_tags_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_question_tags_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_question_tags_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_question_tags_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_question_tags_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_qa_question_tags_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_question_tags_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaQuestionTagsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags/{qaQuestionTagsId}
/// Deletes a QaQuestionTag.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_question_tags_delete_builder()` + `contactcenterinsights_projects_locations_qa_question_tags_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_question_tags_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_question_tags_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaQuestionTagsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_question_tags_delete_builder(
        client, &args.name,
    )?;
    contactcenterinsights_projects_locations_qa_question_tags_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags/{qaQuestionTagsId}
/// Gets a QaQuestionTag.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_question_tags_get_execute()` to send, or `contactcenterinsights_projects_locations_qa_question_tags_get` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_question_tags_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaQuestionTags/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags/{qaQuestionTagsId}
/// Gets a QaQuestionTag.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_question_tags_get_execute()` or `contactcenterinsights_projects_locations_qa_question_tags_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_question_tags_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_question_tags_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestionTag>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1QaQuestionTag =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags/{qaQuestionTagsId}
/// Gets a QaQuestionTag.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_question_tags_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_question_tags_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_question_tags_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_question_tags_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_question_tags_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestionTag>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_qa_question_tags_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_question_tags_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaQuestionTagsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags/{qaQuestionTagsId}
/// Gets a QaQuestionTag.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_question_tags_get_builder()` + `contactcenterinsights_projects_locations_qa_question_tags_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_question_tags_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_question_tags_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaQuestionTagsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestionTag>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_qa_question_tags_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_qa_question_tags_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags
/// Lists the question tags.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_question_tags_list_execute()` to send, or `contactcenterinsights_projects_locations_qa_question_tags_list` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_question_tags_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaQuestionTags",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags
/// Lists the question tags.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_question_tags_list_execute()` or `contactcenterinsights_projects_locations_qa_question_tags_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_question_tags_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_question_tags_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListQaQuestionTagsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListQaQuestionTagsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags
/// Lists the question tags.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_question_tags_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_question_tags_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_question_tags_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_question_tags_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_question_tags_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListQaQuestionTagsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_qa_question_tags_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_question_tags_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaQuestionTagsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags
/// Lists the question tags.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_question_tags_list_builder()` + `contactcenterinsights_projects_locations_qa_question_tags_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_question_tags_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_question_tags_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaQuestionTagsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListQaQuestionTagsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_question_tags_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
    )?;
    contactcenterinsights_projects_locations_qa_question_tags_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags/{qaQuestionTagsId}
/// Updates a QaQuestionTag.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_question_tags_patch_execute()` to send, or `contactcenterinsights_projects_locations_qa_question_tags_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_question_tags_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1QaQuestionTag,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaQuestionTags/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags/{qaQuestionTagsId}
/// Updates a QaQuestionTag.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_question_tags_patch_execute()` or `contactcenterinsights_projects_locations_qa_question_tags_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_question_tags_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_question_tags_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags/{qaQuestionTagsId}
/// Updates a QaQuestionTag.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_question_tags_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_question_tags_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_question_tags_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_question_tags_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_question_tags_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_qa_question_tags_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_question_tags_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaQuestionTagsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1QaQuestionTag,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaQuestionTags/{qaQuestionTagsId}
/// Updates a QaQuestionTag.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_question_tags_patch_builder()` + `contactcenterinsights_projects_locations_qa_question_tags_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_question_tags_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_question_tags_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaQuestionTagsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_question_tags_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_qa_question_tags_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards
/// Create a QaScorecard.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_create_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_create` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    qaScorecardId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1QaScorecard,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = qaScorecardId {
        query_parts.push(format!("qaScorecardId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards
/// Create a QaScorecard.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_create_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1QaScorecard =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards
/// Create a QaScorecard.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_qa_scorecards_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: qaScorecardId
    pub qaScorecardId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1QaScorecard,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards
/// Create a QaScorecard.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_create_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_scorecards_create_builder(
        client,
        &args.parent,
        args.qaScorecardId.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_qa_scorecards_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}
/// Deletes a QaScorecard.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_delete_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}
/// Deletes a QaScorecard.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_delete_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}
/// Deletes a QaScorecard.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_qa_scorecards_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}
/// Deletes a QaScorecard.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_delete_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_scorecards_delete_builder(
        client, &args.name, args.force,
    )?;
    contactcenterinsights_projects_locations_qa_scorecards_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}
/// Gets a QaScorecard.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_get_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_get` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}
/// Gets a QaScorecard.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_get_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1QaScorecard =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}
/// Gets a QaScorecard.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_qa_scorecards_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}
/// Gets a QaScorecard.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_get_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_qa_scorecards_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_qa_scorecards_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards
/// Lists QaScorecards.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_list_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_list` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    qaScorecardSources: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = qaScorecardSources {
        query_parts.push(format!("qaScorecardSources={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards
/// Lists QaScorecards.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_list_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListQaScorecardsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListQaScorecardsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards
/// Lists QaScorecards.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListQaScorecardsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_qa_scorecards_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: qaScorecardSources
    pub qaScorecardSources: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards
/// Lists QaScorecards.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_list_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListQaScorecardsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_scorecards_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
        args.qaScorecardSources.as_deref(),
    )?;
    contactcenterinsights_projects_locations_qa_scorecards_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}
/// Updates a QaScorecard.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_patch_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1QaScorecard,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}
/// Updates a QaScorecard.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_patch_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1QaScorecard =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}
/// Updates a QaScorecard.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_qa_scorecards_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1QaScorecard,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}
/// Updates a QaScorecard.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_patch_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecard>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_scorecards_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_qa_scorecards_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions
/// Creates a QaScorecardRevision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_revisions_create_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_revisions_create` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    qaScorecardRevisionId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1QaScorecardRevision,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}/revisions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = qaScorecardRevisionId {
        query_parts.push(format!("qaScorecardRevisionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions
/// Creates a QaScorecardRevision.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_create_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_revisions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecardRevision>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1QaScorecardRevision =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions
/// Creates a QaScorecardRevision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecardRevision>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_qa_scorecards_revisions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_revisions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: qaScorecardRevisionId
    pub qaScorecardRevisionId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1QaScorecardRevision,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions
/// Creates a QaScorecardRevision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_create_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_revisions_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecardRevision>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_create_builder(
        client,
        &args.parent,
        args.qaScorecardRevisionId.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_qa_scorecards_revisions_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}
/// Deletes a QaScorecardRevision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_revisions_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}/revisions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}
/// Deletes a QaScorecardRevision.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_revisions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}
/// Deletes a QaScorecardRevision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_revisions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}
/// Deletes a QaScorecardRevision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_builder(
        client, &args.name, args.force,
    )?;
    contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}:deploy
/// Deploy a QaScorecardRevision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContactcenterinsightsV1DeployQaScorecardRevisionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}/revisions/{}:deploy",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}:deploy
/// Deploy a QaScorecardRevision.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecardRevision>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1QaScorecardRevision =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}:deploy
/// Deploy a QaScorecardRevision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecardRevision>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsDeployArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1DeployQaScorecardRevisionRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}:deploy
/// Deploy a QaScorecardRevision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsDeployArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecardRevision>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_builder(
        client, &args.name, &args.body,
    )?;
    contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}
/// Gets a QaScorecardRevision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_revisions_get_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_revisions_get` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}/revisions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}
/// Gets a QaScorecardRevision.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_get_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_revisions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecardRevision>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1QaScorecardRevision =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}
/// Gets a QaScorecardRevision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecardRevision>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_qa_scorecards_revisions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_revisions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}
/// Gets a QaScorecardRevision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_get_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_revisions_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecardRevision>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_get_builder(
        client, &args.name,
    )?;
    contactcenterinsights_projects_locations_qa_scorecards_revisions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions
/// Lists all revisions under the parent QaScorecard.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_revisions_list_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_revisions_list` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    qaScorecardSources: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}/revisions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = qaScorecardSources {
        query_parts.push(format!("qaScorecardSources={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions
/// Lists all revisions under the parent QaScorecard.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_list_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_revisions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListQaScorecardRevisionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListQaScorecardRevisionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions
/// Lists all revisions under the parent QaScorecard.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListQaScorecardRevisionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_qa_scorecards_revisions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_revisions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: qaScorecardSources
    pub qaScorecardSources: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions
/// Lists all revisions under the parent QaScorecard.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_list_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_revisions_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListQaScorecardRevisionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.qaScorecardSources.as_deref(),
    )?;
    contactcenterinsights_projects_locations_qa_scorecards_revisions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}:tuneQaScorecardRevision
/// Fine tune one or more QaModels.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1TuneQaScorecardRevisionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}/revisions/{}:tuneQaScorecardRevision",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}:tuneQaScorecardRevision
/// Fine tune one or more QaModels.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}:tuneQaScorecardRevision
/// Fine tune one or more QaModels.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsTuneQaScorecardRevisionArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1TuneQaScorecardRevisionRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}:tuneQaScorecardRevision
/// Fine tune one or more QaModels.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsTuneQaScorecardRevisionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_builder(client, &args.parent, &args.body)?;
    contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}:undeploy
/// Undeploy a QaScorecardRevision.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudContactcenterinsightsV1UndeployQaScorecardRevisionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}/revisions/{}:undeploy",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}:undeploy
/// Undeploy a QaScorecardRevision.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecardRevision>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1QaScorecardRevision =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}:undeploy
/// Undeploy a QaScorecardRevision.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecardRevision>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsUndeployArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1UndeployQaScorecardRevisionRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}:undeploy
/// Undeploy a QaScorecardRevision.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsUndeployArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1QaScorecardRevision>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_builder(
            client, &args.name, &args.body,
        )?;
    contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions
/// Create a QaQuestion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    qaQuestionId: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1QaQuestion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}/revisions/{}/qaQuestions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = qaQuestionId {
        query_parts.push(format!("qaQuestionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions
/// Create a QaQuestion.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1QaQuestion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions
/// Create a QaQuestion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: qaQuestionId
    pub qaQuestionId: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1QaQuestion,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions
/// Create a QaQuestion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_builder(client, &args.parent, args.qaQuestionId.as_deref(), &args.body)?;
    contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions/{qaQuestionsId}
/// Deletes a QaQuestion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}/revisions/{}/qaQuestions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions/{qaQuestionsId}
/// Deletes a QaQuestion.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions/{qaQuestionsId}
/// Deletes a QaQuestion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions/{qaQuestionsId}
/// Deletes a QaQuestion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions/{qaQuestionsId}
/// Gets a QaQuestion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}/revisions/{}/qaQuestions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions/{qaQuestionsId}
/// Gets a QaQuestion.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1QaQuestion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions/{qaQuestionsId}
/// Gets a QaQuestion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions/{qaQuestionsId}
/// Gets a QaQuestion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_builder(
            client, &args.name,
        )?;
    contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions
/// Lists QaQuestions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}/revisions/{}/qaQuestions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions
/// Lists QaQuestions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListQaQuestionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListQaQuestionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions
/// Lists QaQuestions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListQaQuestionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions
/// Lists QaQuestions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1ListQaQuestionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_builder(
            client,
            &args.parent,
            args.pageSize,
            args.pageToken.as_deref(),
        )?;
    contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_list_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions/{qaQuestionsId}
/// Updates a QaQuestion.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_execute()` to send, or `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1QaQuestion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/qaScorecards/{}/revisions/{}/qaQuestions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions/{qaQuestionsId}
/// Updates a QaQuestion.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_execute()` or `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1QaQuestion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions/{qaQuestionsId}
/// Updates a QaQuestion.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1QaQuestion,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/qaScorecards/{qaScorecardsId}/revisions/{revisionsId}/qaQuestions/{qaQuestionsId}
/// Updates a QaQuestion.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_builder()` + `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1QaQuestion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_builder(client, &args.name, args.updateMask.as_deref(), &args.body)?;
    contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views
/// Creates a view.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_views_create_execute()` to send, or `contactcenterinsights_projects_locations_views_create` for simplest API.

pub fn contactcenterinsights_projects_locations_views_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudContactcenterinsightsV1View,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/views",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views
/// Creates a view.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_views_create_execute()` or `contactcenterinsights_projects_locations_views_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_views_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1View>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1View = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views
/// Creates a view.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_views_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_views_create_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_views_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_views_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1View>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_views_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_views_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsViewsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1View,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views
/// Creates a view.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_views_create_builder()` + `contactcenterinsights_projects_locations_views_create_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_views_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_views_create(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsViewsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1View>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_views_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    contactcenterinsights_projects_locations_views_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views/{viewsId}
/// Deletes a view.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_views_delete_execute()` to send, or `contactcenterinsights_projects_locations_views_delete` for simplest API.

pub fn contactcenterinsights_projects_locations_views_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/views/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views/{viewsId}
/// Deletes a view.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_views_delete_execute()` or `contactcenterinsights_projects_locations_views_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_views_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views/{viewsId}
/// Deletes a view.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_views_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_views_delete_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_views_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_views_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_views_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_views_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsViewsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views/{viewsId}
/// Deletes a view.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_views_delete_builder()` + `contactcenterinsights_projects_locations_views_delete_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_views_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_views_delete(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsViewsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        contactcenterinsights_projects_locations_views_delete_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_views_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views/{viewsId}
/// Gets a view.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_views_get_execute()` to send, or `contactcenterinsights_projects_locations_views_get` for simplest API.

pub fn contactcenterinsights_projects_locations_views_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/views/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views/{viewsId}
/// Gets a view.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_views_get_execute()` or `contactcenterinsights_projects_locations_views_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_views_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1View>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1View = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views/{viewsId}
/// Gets a view.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_views_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_views_get_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_views_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1View>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_views_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_views_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsViewsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views/{viewsId}
/// Gets a view.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_views_get_builder()` + `contactcenterinsights_projects_locations_views_get_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_views_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_views_get(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsViewsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1View>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_views_get_builder(client, &args.name)?;
    contactcenterinsights_projects_locations_views_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views
/// Lists views.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_views_list_execute()` to send, or `contactcenterinsights_projects_locations_views_list` for simplest API.

pub fn contactcenterinsights_projects_locations_views_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/views",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views
/// Lists views.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_views_list_execute()` or `contactcenterinsights_projects_locations_views_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_views_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListViewsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1ListViewsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views
/// Lists views.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_views_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_views_list_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_views_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_views_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListViewsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_views_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_views_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsViewsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views
/// Lists views.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_views_list_builder()` + `contactcenterinsights_projects_locations_views_list_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_views_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_views_list(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsViewsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1ListViewsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_views_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    contactcenterinsights_projects_locations_views_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views/{viewsId}
/// Updates a view.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `contactcenterinsights_projects_locations_views_patch_execute()` to send, or `contactcenterinsights_projects_locations_views_patch` for simplest API.

pub fn contactcenterinsights_projects_locations_views_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudContactcenterinsightsV1View,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{}/locations/{}/views/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views/{viewsId}
/// Updates a view.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `contactcenterinsights_projects_locations_views_patch_execute()` or `contactcenterinsights_projects_locations_views_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_views_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1View>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1View = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views/{viewsId}
/// Updates a view.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `contactcenterinsights_projects_locations_views_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `contactcenterinsights_projects_locations_views_patch_task()`.
/// For the simplest API, use `contactcenterinsights_projects_locations_views_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `contactcenterinsights_projects_locations_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn contactcenterinsights_projects_locations_views_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1View>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = contactcenterinsights_projects_locations_views_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`contactcenterinsights_projects_locations_views_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsViewsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudContactcenterinsightsV1View,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/views/{viewsId}
/// Updates a view.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `contactcenterinsights_projects_locations_views_patch_builder()` + `contactcenterinsights_projects_locations_views_patch_execute()`.
/// For task-level control, use `contactcenterinsights_projects_locations_views_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn contactcenterinsights_projects_locations_views_patch(
    client: &SimpleHttpClient,
    args: &ContactcenterinsightsProjectsLocationsViewsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudContactcenterinsightsV1View>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = contactcenterinsights_projects_locations_views_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    contactcenterinsights_projects_locations_views_patch_execute(builder)
}
