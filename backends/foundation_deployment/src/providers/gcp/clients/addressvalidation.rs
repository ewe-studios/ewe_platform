//! Auto-generated API clients for gcp/addressvalidation.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/addressvalidation`

#![cfg(feature = "gcp/addressvalidation")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1:provideValidationFeedback
/// Feedback about the outcome of the sequence of validation attempts. This should be the last call made after a sequence of validation calls for the same address, and should be called once the transaction is concluded. This should only be sent once for the sequence of ValidateAddress requests needed to validate an address fully.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `addressvalidation_provide_validation_feedback_execute()` to send, or `addressvalidation_provide_validation_feedback` for simplest API.

pub fn addressvalidation_provide_validation_feedback_builder(
    client: &SimpleHttpClient,
    body: &GoogleMapsAddressvalidationV1ProvideValidationFeedbackRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://addressvalidation.googleapis.com/v1:provideValidationFeedback",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1:provideValidationFeedback
/// Feedback about the outcome of the sequence of validation attempts. This should be the last call made after a sequence of validation calls for the same address, and should be called once the transaction is concluded. This should only be sent once for the sequence of ValidateAddress requests needed to validate an address fully.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `addressvalidation_provide_validation_feedback_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from addressvalidation_provide_validation_feedback_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn addressvalidation_provide_validation_feedback_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleMapsAddressvalidationV1ProvideValidationFeedbackResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleMapsAddressvalidationV1ProvideValidationFeedbackResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1:provideValidationFeedback
/// Feedback about the outcome of the sequence of validation attempts. This should be the last call made after a sequence of validation calls for the same address, and should be called once the transaction is concluded. This should only be sent once for the sequence of ValidateAddress requests needed to validate an address fully.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `addressvalidation_provide_validation_feedback_builder()` + `addressvalidation_provide_validation_feedback_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn addressvalidation_provide_validation_feedback(
    client: &SimpleHttpClient,
    body: &GoogleMapsAddressvalidationV1ProvideValidationFeedbackRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleMapsAddressvalidationV1ProvideValidationFeedbackResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = addressvalidation_provide_validation_feedback_builder(client, body)?;
    addressvalidation_provide_validation_feedback_execute(builder)
}

/// GET v1:validateAddress
/// Validates an address.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `addressvalidation_validate_address_execute()` to send, or `addressvalidation_validate_address` for simplest API.

pub fn addressvalidation_validate_address_builder(
    client: &SimpleHttpClient,
    body: &GoogleMapsAddressvalidationV1ValidateAddressRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://addressvalidation.googleapis.com/v1:validateAddress",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1:validateAddress
/// Validates an address.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `addressvalidation_validate_address_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from addressvalidation_validate_address_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn addressvalidation_validate_address_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsAddressvalidationV1ValidateAddressResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleMapsAddressvalidationV1ValidateAddressResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1:validateAddress
/// Validates an address.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `addressvalidation_validate_address_builder()` + `addressvalidation_validate_address_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn addressvalidation_validate_address(
    client: &SimpleHttpClient,
    body: &GoogleMapsAddressvalidationV1ValidateAddressRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsAddressvalidationV1ValidateAddressResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = addressvalidation_validate_address_builder(client, body)?;
    addressvalidation_validate_address_execute(builder)
}
