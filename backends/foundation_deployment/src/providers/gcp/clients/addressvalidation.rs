//! Auto-generated API clients for gcp/addressvalidation.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1:provideValidationFeedback
/// Feedback about the outcome of the sequence of validation attempts. This should be the last call made after a sequence of validation calls for the same address, and should be called once the transaction is concluded. This should only be sent once for the sequence of ValidateAddress requests needed to validate an address fully.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `addressvalidation_provide_validation_feedback_execute()` to send, or `addressvalidation_provide_validation_feedback` for simplest API.

pub fn addressvalidation_provide_validation_feedback_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://addressvalidation.googleapis.com/v1:provideValidationFeedback",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1:provideValidationFeedback
/// Feedback about the outcome of the sequence of validation attempts. This should be the last call made after a sequence of validation calls for the same address, and should be called once the transaction is concluded. This should only be sent once for the sequence of ValidateAddress requests needed to validate an address fully.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `addressvalidation_provide_validation_feedback_execute()` or `addressvalidation_provide_validation_feedback`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `addressvalidation_provide_validation_feedback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn addressvalidation_provide_validation_feedback_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleMapsAddressvalidationV1ProvideValidationFeedbackResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleMapsAddressvalidationV1ProvideValidationFeedbackResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1:provideValidationFeedback
/// Feedback about the outcome of the sequence of validation attempts. This should be the last call made after a sequence of validation calls for the same address, and should be called once the transaction is concluded. This should only be sent once for the sequence of ValidateAddress requests needed to validate an address fully.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `addressvalidation_provide_validation_feedback_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `addressvalidation_provide_validation_feedback_task()`.
/// For the simplest API, use `addressvalidation_provide_validation_feedback()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `addressvalidation_provide_validation_feedback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn addressvalidation_provide_validation_feedback_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleMapsAddressvalidationV1ProvideValidationFeedbackResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = addressvalidation_provide_validation_feedback_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1:provideValidationFeedback
/// Feedback about the outcome of the sequence of validation attempts. This should be the last call made after a sequence of validation calls for the same address, and should be called once the transaction is concluded. This should only be sent once for the sequence of ValidateAddress requests needed to validate an address fully.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `addressvalidation_provide_validation_feedback_builder()` + `addressvalidation_provide_validation_feedback_execute()`.
/// For task-level control, use `addressvalidation_provide_validation_feedback_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn addressvalidation_provide_validation_feedback(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleMapsAddressvalidationV1ProvideValidationFeedbackResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = addressvalidation_provide_validation_feedback_builder(client)?;
    addressvalidation_provide_validation_feedback_execute(builder)
}

/// POST v1:validateAddress
/// Validates an address.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `addressvalidation_validate_address_execute()` to send, or `addressvalidation_validate_address` for simplest API.

pub fn addressvalidation_validate_address_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://addressvalidation.googleapis.com/v1:validateAddress",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1:validateAddress
/// Validates an address.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `addressvalidation_validate_address_execute()` or `addressvalidation_validate_address`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `addressvalidation_validate_address_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn addressvalidation_validate_address_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleMapsAddressvalidationV1ValidateAddressResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleMapsAddressvalidationV1ValidateAddressResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1:validateAddress
/// Validates an address.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `addressvalidation_validate_address_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `addressvalidation_validate_address_task()`.
/// For the simplest API, use `addressvalidation_validate_address()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `addressvalidation_validate_address_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn addressvalidation_validate_address_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsAddressvalidationV1ValidateAddressResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = addressvalidation_validate_address_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1:validateAddress
/// Validates an address.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `addressvalidation_validate_address_builder()` + `addressvalidation_validate_address_execute()`.
/// For task-level control, use `addressvalidation_validate_address_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn addressvalidation_validate_address(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsAddressvalidationV1ValidateAddressResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = addressvalidation_validate_address_builder(client)?;
    addressvalidation_validate_address_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleMapsAddressvalidationV1ProvideValidationFeedbackResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleMapsAddressvalidationV1ProvideValidationFeedbackResponse with AddressvalidationProvideValidationFeedbackArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AddressvalidationProvideValidationFeedbackArgs>
    for GoogleMapsAddressvalidationV1ProvideValidationFeedbackResponse
{
    fn generate_resource_id(
        &self,
        input: &AddressvalidationProvideValidationFeedbackArgs,
    ) -> String {
        "gcp::addressvalidation::GoogleMapsAddressvalidationV1ProvideValidationFeedbackResponse"
            .to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::addressvalidation::GoogleMapsAddressvalidationV1ProvideValidationFeedbackResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleMapsAddressvalidationV1ValidateAddressResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleMapsAddressvalidationV1ValidateAddressResponse with AddressvalidationValidateAddressArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AddressvalidationValidateAddressArgs>
    for GoogleMapsAddressvalidationV1ValidateAddressResponse
{
    fn generate_resource_id(&self, input: &AddressvalidationValidateAddressArgs) -> String {
        "gcp::addressvalidation::GoogleMapsAddressvalidationV1ValidateAddressResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::addressvalidation::GoogleMapsAddressvalidationV1ValidateAddressResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
