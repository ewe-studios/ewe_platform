//! Auto-generated API clients for gcp/ml.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}:explain
/// Performs explanation on the data in the request. {% dynamic include "/ai-`platform/includes/___explain-request`" %}
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `ml_projects_explain_execute()` to send, or `ml_projects_explain` for simplest API.

pub fn ml_projects_explain_builder(
    client: &SimpleHttpClient,
    name: &String,
    body: &GoogleCloudMlV1__ExplainRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://ml.googleapis.com/v1/projects/{}:explain",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:explain
/// Performs explanation on the data in the request. {% dynamic include "/ai-`platform/includes/___explain-request`" %}
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `ml_projects_explain_execute()` or `ml_projects_explain`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `ml_projects_explain_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn ml_projects_explain_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleApi__HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApi__HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}:explain
/// Performs explanation on the data in the request. {% dynamic include "/ai-`platform/includes/___explain-request`" %}
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `ml_projects_explain_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `ml_projects_explain_task()`.
/// For the simplest API, use `ml_projects_explain()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `ml_projects_explain_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn ml_projects_explain_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApi__HttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = ml_projects_explain_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`ml_projects_explain`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MlProjectsExplainArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudMlV1__ExplainRequest,
}

/// GET v1/projects/{projectsId}:explain
/// Performs explanation on the data in the request. {% dynamic include "/ai-`platform/includes/___explain-request`" %}
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `ml_projects_explain_builder()` + `ml_projects_explain_execute()`.
/// For task-level control, use `ml_projects_explain_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn ml_projects_explain(
    client: &SimpleHttpClient,
    args: &MlProjectsExplainArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApi__HttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = ml_projects_explain_builder(client, &args.name, &args.body)?;
    ml_projects_explain_execute(builder)
}

/// GET v1/projects/{projectsId}:getConfig
/// Get the service account information associated with your project. You need this information in order to grant the service account permissions for the Google Cloud Storage location where you put your model training code for training the model with Google Cloud Machine Learning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `ml_projects_get_config_execute()` to send, or `ml_projects_get_config` for simplest API.

pub fn ml_projects_get_config_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://ml.googleapis.com/v1/projects/{}:getConfig",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}:getConfig
/// Get the service account information associated with your project. You need this information in order to grant the service account permissions for the Google Cloud Storage location where you put your model training code for training the model with Google Cloud Machine Learning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `ml_projects_get_config_execute()` or `ml_projects_get_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `ml_projects_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn ml_projects_get_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudMlV1__GetConfigResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudMlV1__GetConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}:getConfig
/// Get the service account information associated with your project. You need this information in order to grant the service account permissions for the Google Cloud Storage location where you put your model training code for training the model with Google Cloud Machine Learning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `ml_projects_get_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `ml_projects_get_config_task()`.
/// For the simplest API, use `ml_projects_get_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `ml_projects_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn ml_projects_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudMlV1__GetConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = ml_projects_get_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`ml_projects_get_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MlProjectsGetConfigArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}:getConfig
/// Get the service account information associated with your project. You need this information in order to grant the service account permissions for the Google Cloud Storage location where you put your model training code for training the model with Google Cloud Machine Learning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `ml_projects_get_config_builder()` + `ml_projects_get_config_execute()`.
/// For task-level control, use `ml_projects_get_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn ml_projects_get_config(
    client: &SimpleHttpClient,
    args: &MlProjectsGetConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudMlV1__GetConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = ml_projects_get_config_builder(client, &args.name)?;
    ml_projects_get_config_execute(builder)
}

/// GET v1/projects/{projectsId}:predict
/// Performs online prediction on the data in the request. {% dynamic include "/ai-`platform/includes/___predict-request`" %}
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `ml_projects_predict_execute()` to send, or `ml_projects_predict` for simplest API.

pub fn ml_projects_predict_builder(
    client: &SimpleHttpClient,
    name: &String,
    body: &GoogleCloudMlV1__PredictRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://ml.googleapis.com/v1/projects/{}:predict",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:predict
/// Performs online prediction on the data in the request. {% dynamic include "/ai-`platform/includes/___predict-request`" %}
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `ml_projects_predict_execute()` or `ml_projects_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `ml_projects_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn ml_projects_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleApi__HttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApi__HttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}:predict
/// Performs online prediction on the data in the request. {% dynamic include "/ai-`platform/includes/___predict-request`" %}
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `ml_projects_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `ml_projects_predict_task()`.
/// For the simplest API, use `ml_projects_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `ml_projects_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn ml_projects_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApi__HttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = ml_projects_predict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`ml_projects_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MlProjectsPredictArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudMlV1__PredictRequest,
}

/// GET v1/projects/{projectsId}:predict
/// Performs online prediction on the data in the request. {% dynamic include "/ai-`platform/includes/___predict-request`" %}
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `ml_projects_predict_builder()` + `ml_projects_predict_execute()`.
/// For task-level control, use `ml_projects_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn ml_projects_predict(
    client: &SimpleHttpClient,
    args: &MlProjectsPredictArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApi__HttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = ml_projects_predict_builder(client, &args.name, &args.body)?;
    ml_projects_predict_execute(builder)
}

/// GET v1/projects/{projectsId}/jobs
/// Creates a training or a batch prediction job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `ml_projects_jobs_create_execute()` to send, or `ml_projects_jobs_create` for simplest API.

pub fn ml_projects_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudMlV1__Job,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://ml.googleapis.com/v1/projects/{}/jobs",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/jobs
/// Creates a training or a batch prediction job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `ml_projects_jobs_create_execute()` or `ml_projects_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `ml_projects_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn ml_projects_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudMlV1__Job>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudMlV1__Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/jobs
/// Creates a training or a batch prediction job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `ml_projects_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `ml_projects_jobs_create_task()`.
/// For the simplest API, use `ml_projects_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `ml_projects_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn ml_projects_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudMlV1__Job>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = ml_projects_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`ml_projects_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MlProjectsJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudMlV1__Job,
}

/// GET v1/projects/{projectsId}/jobs
/// Creates a training or a batch prediction job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `ml_projects_jobs_create_builder()` + `ml_projects_jobs_create_execute()`.
/// For task-level control, use `ml_projects_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn ml_projects_jobs_create(
    client: &SimpleHttpClient,
    args: &MlProjectsJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudMlV1__Job>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = ml_projects_jobs_create_builder(client, &args.parent, &args.body)?;
    ml_projects_jobs_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// List all locations that provides at least one type of CMLE capability.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `ml_projects_locations_list_execute()` to send, or `ml_projects_locations_list` for simplest API.

pub fn ml_projects_locations_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://ml.googleapis.com/v1/projects/{}/locations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// List all locations that provides at least one type of CMLE capability.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `ml_projects_locations_list_execute()` or `ml_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `ml_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn ml_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudMlV1__ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudMlV1__ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// List all locations that provides at least one type of CMLE capability.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `ml_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `ml_projects_locations_list_task()`.
/// For the simplest API, use `ml_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `ml_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn ml_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudMlV1__ListLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = ml_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`ml_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MlProjectsLocationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// List all locations that provides at least one type of CMLE capability.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `ml_projects_locations_list_builder()` + `ml_projects_locations_list_execute()`.
/// For task-level control, use `ml_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn ml_projects_locations_list(
    client: &SimpleHttpClient,
    args: &MlProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudMlV1__ListLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        ml_projects_locations_list_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    ml_projects_locations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/models
/// Creates a model which will later contain one or more versions. You must add at least one version before you can request predictions from the model. Add versions by calling projects.models.versions.create.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `ml_projects_models_create_execute()` to send, or `ml_projects_models_create` for simplest API.

pub fn ml_projects_models_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudMlV1__Model,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://ml.googleapis.com/v1/projects/{}/models",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/models
/// Creates a model which will later contain one or more versions. You must add at least one version before you can request predictions from the model. Add versions by calling projects.models.versions.create.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `ml_projects_models_create_execute()` or `ml_projects_models_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `ml_projects_models_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn ml_projects_models_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudMlV1__Model>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudMlV1__Model = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/models
/// Creates a model which will later contain one or more versions. You must add at least one version before you can request predictions from the model. Add versions by calling projects.models.versions.create.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `ml_projects_models_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `ml_projects_models_create_task()`.
/// For the simplest API, use `ml_projects_models_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `ml_projects_models_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn ml_projects_models_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudMlV1__Model>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = ml_projects_models_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`ml_projects_models_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MlProjectsModelsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudMlV1__Model,
}

/// GET v1/projects/{projectsId}/models
/// Creates a model which will later contain one or more versions. You must add at least one version before you can request predictions from the model. Add versions by calling projects.models.versions.create.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `ml_projects_models_create_builder()` + `ml_projects_models_create_execute()`.
/// For task-level control, use `ml_projects_models_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn ml_projects_models_create(
    client: &SimpleHttpClient,
    args: &MlProjectsModelsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleCloudMlV1__Model>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = ml_projects_models_create_builder(client, &args.parent, &args.body)?;
    ml_projects_models_create_execute(builder)
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `ml_projects_operations_list_execute()` to send, or `ml_projects_operations_list` for simplest API.

pub fn ml_projects_operations_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://ml.googleapis.com/v1/projects/{}/operations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `ml_projects_operations_list_execute()` or `ml_projects_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `ml_projects_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn ml_projects_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunning__ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunning__ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `ml_projects_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `ml_projects_operations_list_task()`.
/// For the simplest API, use `ml_projects_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `ml_projects_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn ml_projects_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunning__ListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = ml_projects_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`ml_projects_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MlProjectsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `ml_projects_operations_list_builder()` + `ml_projects_operations_list_execute()`.
/// For task-level control, use `ml_projects_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn ml_projects_operations_list(
    client: &SimpleHttpClient,
    args: &MlProjectsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunning__ListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = ml_projects_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    ml_projects_operations_list_execute(builder)
}
