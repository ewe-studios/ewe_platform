//! Auto-generated API clients for Google Cloud Platform - places.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/places:autocomplete
/// Returns predictions for the given input.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `places_places_autocomplete_execute()` to send, or `places_places_autocomplete` for simplest API.

pub fn places_places_autocomplete_builder(
    client: &SimpleHttpClient,
    body: &GoogleMapsPlacesV1AutocompletePlacesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/places:autocomplete",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/places:autocomplete
/// Returns predictions for the given input.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `places_places_autocomplete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from places_places_autocomplete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_autocomplete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1AutocompletePlacesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleMapsPlacesV1AutocompletePlacesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/places:autocomplete
/// Returns predictions for the given input.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `places_places_autocomplete_builder()` + `places_places_autocomplete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_autocomplete(
    client: &SimpleHttpClient,
    body: &GoogleMapsPlacesV1AutocompletePlacesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1AutocompletePlacesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = places_places_autocomplete_builder(client, body)?;
    places_places_autocomplete_execute(builder)
}

/// GET v1/{+name}
/// Get the details of a place based on its resource name, which is a string in the `places/{place_id}` format.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `places_places_get_execute()` to send, or `places_places_get` for simplest API.

pub fn places_places_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    languageCode: Option<&str>,
    regionCode: Option<&str>,
    sessionToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "languageCode",
            languageCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "regionCode",
            regionCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sessionToken",
            sessionToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Get the details of a place based on its resource name, which is a string in the `places/{place_id}` format.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `places_places_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from places_places_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleMapsPlacesV1Place>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleMapsPlacesV1Place = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get the details of a place based on its resource name, which is a string in the `places/{place_id}` format.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `places_places_get_builder()` + `places_places_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_get(
    client: &SimpleHttpClient,
    name: &str,
    languageCode: Option<&str>,
    regionCode: Option<&str>,
    sessionToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleMapsPlacesV1Place>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = places_places_get_builder(client, name, languageCode, regionCode, sessionToken)?;
    places_places_get_execute(builder)
}

/// GET v1/places:searchNearby
/// Search for places near locations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `places_places_search_nearby_execute()` to send, or `places_places_search_nearby` for simplest API.

pub fn places_places_search_nearby_builder(
    client: &SimpleHttpClient,
    body: &GoogleMapsPlacesV1SearchNearbyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/places:searchNearby",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/places:searchNearby
/// Search for places near locations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `places_places_search_nearby_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from places_places_search_nearby_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_search_nearby_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1SearchNearbyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleMapsPlacesV1SearchNearbyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/places:searchNearby
/// Search for places near locations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `places_places_search_nearby_builder()` + `places_places_search_nearby_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_search_nearby(
    client: &SimpleHttpClient,
    body: &GoogleMapsPlacesV1SearchNearbyRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1SearchNearbyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = places_places_search_nearby_builder(client, body)?;
    places_places_search_nearby_execute(builder)
}

/// GET v1/places:searchText
/// Text query based place search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `places_places_search_text_execute()` to send, or `places_places_search_text` for simplest API.

pub fn places_places_search_text_builder(
    client: &SimpleHttpClient,
    body: &GoogleMapsPlacesV1SearchTextRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/places:searchText",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/places:searchText
/// Text query based place search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `places_places_search_text_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from places_places_search_text_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_search_text_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1SearchTextResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleMapsPlacesV1SearchTextResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/places:searchText
/// Text query based place search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `places_places_search_text_builder()` + `places_places_search_text_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_search_text(
    client: &SimpleHttpClient,
    body: &GoogleMapsPlacesV1SearchTextRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1SearchTextResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = places_places_search_text_builder(client, body)?;
    places_places_search_text_execute(builder)
}

/// GET v1/{+name}
/// Get a photo media with a photo reference string.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `places_places_photos_get_media_execute()` to send, or `places_places_photos_get_media` for simplest API.

pub fn places_places_photos_get_media_builder(
    client: &SimpleHttpClient,
    name: &str,
    maxHeightPx: Option<i32>,
    maxWidthPx: Option<i32>,
    skipHttpRedirect: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxHeightPx",
            maxHeightPx
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxWidthPx",
            maxWidthPx
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "skipHttpRedirect",
            skipHttpRedirect
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Get a photo media with a photo reference string.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `places_places_photos_get_media_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from places_places_photos_get_media_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_photos_get_media_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1PhotoMedia>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleMapsPlacesV1PhotoMedia = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a photo media with a photo reference string.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `places_places_photos_get_media_builder()` + `places_places_photos_get_media_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_photos_get_media(
    client: &SimpleHttpClient,
    name: &str,
    maxHeightPx: Option<i32>,
    maxWidthPx: Option<i32>,
    skipHttpRedirect: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1PhotoMedia>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = places_places_photos_get_media_builder(
        client,
        name,
        maxHeightPx,
        maxWidthPx,
        skipHttpRedirect,
    )?;
    places_places_photos_get_media_execute(builder)
}
