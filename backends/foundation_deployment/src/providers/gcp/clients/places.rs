//! Auto-generated API clients for gcp/places.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/places:autocomplete
/// Returns predictions for the given input.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `places_places_autocomplete_execute()` to send, or `places_places_autocomplete` for simplest API.

pub fn places_places_autocomplete_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://places.googleapis.com/v1/places:autocomplete",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/places:autocomplete
/// Returns predictions for the given input.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `places_places_autocomplete_execute()` or `places_places_autocomplete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `places_places_autocomplete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_autocomplete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleMapsPlacesV1AutocompletePlacesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleMapsPlacesV1AutocompletePlacesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/places:autocomplete
/// Returns predictions for the given input.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `places_places_autocomplete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `places_places_autocomplete_task()`.
/// For the simplest API, use `places_places_autocomplete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `places_places_autocomplete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn places_places_autocomplete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1AutocompletePlacesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = places_places_autocomplete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/places:autocomplete
/// Returns predictions for the given input.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `places_places_autocomplete_builder()` + `places_places_autocomplete_execute()`.
/// For task-level control, use `places_places_autocomplete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_autocomplete(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1AutocompletePlacesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = places_places_autocomplete_builder(client)?;
    places_places_autocomplete_execute(builder)
}

/// GET v1/places/{placesId}
/// Get the details of a place based on its resource name, which is a string in the `places/{place_id}` format.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `places_places_get_execute()` to send, or `places_places_get` for simplest API.

pub fn places_places_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
    regionCode: &Option<String>,
    sessionToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://places.googleapis.com/v1/places/{}", name,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = regionCode.as_ref() {
        query_parts.push(format!("regionCode={}", val));
    }
    if let Some(val) = sessionToken.as_ref() {
        query_parts.push(format!("sessionToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/places/{placesId}
/// Get the details of a place based on its resource name, which is a string in the `places/{place_id}` format.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `places_places_get_execute()` or `places_places_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `places_places_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleMapsPlacesV1Place>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleMapsPlacesV1Place = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/places/{placesId}
/// Get the details of a place based on its resource name, which is a string in the `places/{place_id}` format.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `places_places_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `places_places_get_task()`.
/// For the simplest API, use `places_places_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `places_places_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn places_places_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleMapsPlacesV1Place>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = places_places_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`places_places_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlacesPlacesGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: regionCode
    pub regionCode: Option<String>,
    /// Query parameter: sessionToken
    pub sessionToken: Option<String>,
}

/// GET v1/places/{placesId}
/// Get the details of a place based on its resource name, which is a string in the `places/{place_id}` format.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `places_places_get_builder()` + `places_places_get_execute()`.
/// For task-level control, use `places_places_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_get(
    client: &SimpleHttpClient,
    args: &PlacesPlacesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleMapsPlacesV1Place>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = places_places_get_builder(
        client,
        &args.name,
        &args.languageCode,
        &args.regionCode,
        &args.sessionToken,
    )?;
    places_places_get_execute(builder)
}

/// POST v1/places:searchNearby
/// Search for places near locations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `places_places_search_nearby_execute()` to send, or `places_places_search_nearby` for simplest API.

pub fn places_places_search_nearby_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://places.googleapis.com/v1/places:searchNearby",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/places:searchNearby
/// Search for places near locations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `places_places_search_nearby_execute()` or `places_places_search_nearby`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `places_places_search_nearby_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_search_nearby_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleMapsPlacesV1SearchNearbyResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleMapsPlacesV1SearchNearbyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/places:searchNearby
/// Search for places near locations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `places_places_search_nearby_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `places_places_search_nearby_task()`.
/// For the simplest API, use `places_places_search_nearby()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `places_places_search_nearby_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn places_places_search_nearby_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1SearchNearbyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = places_places_search_nearby_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/places:searchNearby
/// Search for places near locations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `places_places_search_nearby_builder()` + `places_places_search_nearby_execute()`.
/// For task-level control, use `places_places_search_nearby_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_search_nearby(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1SearchNearbyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = places_places_search_nearby_builder(client)?;
    places_places_search_nearby_execute(builder)
}

/// POST v1/places:searchText
/// Text query based place search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `places_places_search_text_execute()` to send, or `places_places_search_text` for simplest API.

pub fn places_places_search_text_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://places.googleapis.com/v1/places:searchText",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/places:searchText
/// Text query based place search.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `places_places_search_text_execute()` or `places_places_search_text`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `places_places_search_text_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_search_text_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleMapsPlacesV1SearchTextResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleMapsPlacesV1SearchTextResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/places:searchText
/// Text query based place search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `places_places_search_text_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `places_places_search_text_task()`.
/// For the simplest API, use `places_places_search_text()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `places_places_search_text_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn places_places_search_text_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1SearchTextResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = places_places_search_text_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/places:searchText
/// Text query based place search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `places_places_search_text_builder()` + `places_places_search_text_execute()`.
/// For task-level control, use `places_places_search_text_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_search_text(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1SearchTextResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = places_places_search_text_builder(client)?;
    places_places_search_text_execute(builder)
}

/// GET v1/places/{placesId}/photos/{photosId}/media
/// Get a photo media with a photo reference string.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `places_places_photos_get_media_execute()` to send, or `places_places_photos_get_media` for simplest API.

pub fn places_places_photos_get_media_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    maxHeightPx: &Option<String>,
    maxWidthPx: &Option<String>,
    skipHttpRedirect: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://places.googleapis.com/v1/places/{}/photos/{photosId}/media",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxHeightPx.as_ref() {
        query_parts.push(format!("maxHeightPx={}", val));
    }
    if let Some(val) = maxWidthPx.as_ref() {
        query_parts.push(format!("maxWidthPx={}", val));
    }
    if let Some(val) = skipHttpRedirect.as_ref() {
        query_parts.push(format!("skipHttpRedirect={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/places/{placesId}/photos/{photosId}/media
/// Get a photo media with a photo reference string.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `places_places_photos_get_media_execute()` or `places_places_photos_get_media`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `places_places_photos_get_media_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_photos_get_media_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleMapsPlacesV1PhotoMedia>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleMapsPlacesV1PhotoMedia = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/places/{placesId}/photos/{photosId}/media
/// Get a photo media with a photo reference string.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `places_places_photos_get_media_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `places_places_photos_get_media_task()`.
/// For the simplest API, use `places_places_photos_get_media()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `places_places_photos_get_media_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn places_places_photos_get_media_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1PhotoMedia>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = places_places_photos_get_media_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`places_places_photos_get_media`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlacesPlacesPhotosGetMediaArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: maxHeightPx
    pub maxHeightPx: Option<String>,
    /// Query parameter: maxWidthPx
    pub maxWidthPx: Option<String>,
    /// Query parameter: skipHttpRedirect
    pub skipHttpRedirect: Option<String>,
}

/// GET v1/places/{placesId}/photos/{photosId}/media
/// Get a photo media with a photo reference string.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `places_places_photos_get_media_builder()` + `places_places_photos_get_media_execute()`.
/// For task-level control, use `places_places_photos_get_media_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn places_places_photos_get_media(
    client: &SimpleHttpClient,
    args: &PlacesPlacesPhotosGetMediaArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleMapsPlacesV1PhotoMedia>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = places_places_photos_get_media_builder(
        client,
        &args.name,
        &args.maxHeightPx,
        &args.maxWidthPx,
        &args.skipHttpRedirect,
    )?;
    places_places_photos_get_media_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleMapsPlacesV1Place
// =============================================================================

/// ResourceIdentifier implementation for GoogleMapsPlacesV1Place with PlacesPlacesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlacesPlacesGetArgs> for GoogleMapsPlacesV1Place {
    fn generate_resource_id(&self, input: &PlacesPlacesGetArgs) -> String {
        format!("gcp::places::GoogleMapsPlacesV1Place/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::places::GoogleMapsPlacesV1Place"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleMapsPlacesV1PhotoMedia
// =============================================================================

/// ResourceIdentifier implementation for GoogleMapsPlacesV1PhotoMedia with PlacesPlacesPhotosGetMediaArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PlacesPlacesPhotosGetMediaArgs> for GoogleMapsPlacesV1PhotoMedia {
    fn generate_resource_id(&self, input: &PlacesPlacesPhotosGetMediaArgs) -> String {
        format!("gcp::places::GoogleMapsPlacesV1PhotoMedia/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::places::GoogleMapsPlacesV1PhotoMedia"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
