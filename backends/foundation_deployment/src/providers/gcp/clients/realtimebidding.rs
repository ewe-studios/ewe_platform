//! Auto-generated API clients for gcp/realtimebidding.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/bidders/{biddersId}
/// Gets a bidder account by its name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_get_execute()` to send, or `realtimebidding_bidders_get` for simplest API.

pub fn realtimebidding_bidders_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://realtimebidding.googleapis.com/v1/bidders/{}", name,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/bidders/{biddersId}
/// Gets a bidder account by its name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_get_execute()` or `realtimebidding_bidders_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bidder>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Bidder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/bidders/{biddersId}
/// Gets a bidder account by its name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_get_task()`.
/// For the simplest API, use `realtimebidding_bidders_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bidder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/bidders/{biddersId}
/// Gets a bidder account by its name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_get_builder()` + `realtimebidding_bidders_get_execute()`.
/// For task-level control, use `realtimebidding_bidders_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_get(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bidder>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_get_builder(client, &args.name)?;
    realtimebidding_bidders_get_execute(builder)
}

/// GET v1/bidders
/// Lists all the bidder accounts that belong to the caller.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_list_execute()` to send, or `realtimebidding_bidders_list` for simplest API.

pub fn realtimebidding_bidders_list_builder<R>(
    client: &SimpleHttpClient<R>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://realtimebidding.googleapis.com/v1/bidders",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/bidders
/// Lists all the bidder accounts that belong to the caller.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_list_execute()` or `realtimebidding_bidders_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBiddersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBiddersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/bidders
/// Lists all the bidder accounts that belong to the caller.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_list_task()`.
/// For the simplest API, use `realtimebidding_bidders_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBiddersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/bidders
/// Lists all the bidder accounts that belong to the caller.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_list_builder()` + `realtimebidding_bidders_list_execute()`.
/// For task-level control, use `realtimebidding_bidders_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_list(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBiddersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_list_builder(client, &args.pageSize, &args.pageToken)?;
    realtimebidding_bidders_list_execute(builder)
}

/// GET v1/bidders/{biddersId}/creatives
/// Lists creatives as they are at the time of the initial request. This call may take multiple hours to complete. For large, paginated requests, this method returns a snapshot of creatives at the time of request for the first page. `lastStatusUpdate` and `creativeServingDecision` may be outdated for creatives on sequential pages. We recommend [Google Cloud P`ub/Sub`](//cloud.google.`com/pubsub/docs/overview`) to view the latest status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_creatives_list_execute()` to send, or `realtimebidding_bidders_creatives_list` for simplest API.

pub fn realtimebidding_bidders_creatives_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/creatives",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/bidders/{biddersId}/creatives
/// Lists creatives as they are at the time of the initial request. This call may take multiple hours to complete. For large, paginated requests, this method returns a snapshot of creatives at the time of request for the first page. `lastStatusUpdate` and `creativeServingDecision` may be outdated for creatives on sequential pages. We recommend [Google Cloud P`ub/Sub`](//cloud.google.`com/pubsub/docs/overview`) to view the latest status.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_creatives_list_execute()` or `realtimebidding_bidders_creatives_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_creatives_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_creatives_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCreativesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCreativesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/bidders/{biddersId}/creatives
/// Lists creatives as they are at the time of the initial request. This call may take multiple hours to complete. For large, paginated requests, this method returns a snapshot of creatives at the time of request for the first page. `lastStatusUpdate` and `creativeServingDecision` may be outdated for creatives on sequential pages. We recommend [Google Cloud P`ub/Sub`](//cloud.google.`com/pubsub/docs/overview`) to view the latest status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_creatives_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_creatives_list_task()`.
/// For the simplest API, use `realtimebidding_bidders_creatives_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_creatives_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_creatives_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCreativesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_creatives_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_creatives_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersCreativesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/bidders/{biddersId}/creatives
/// Lists creatives as they are at the time of the initial request. This call may take multiple hours to complete. For large, paginated requests, this method returns a snapshot of creatives at the time of request for the first page. `lastStatusUpdate` and `creativeServingDecision` may be outdated for creatives on sequential pages. We recommend [Google Cloud P`ub/Sub`](//cloud.google.`com/pubsub/docs/overview`) to view the latest status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_creatives_list_builder()` + `realtimebidding_bidders_creatives_list_execute()`.
/// For task-level control, use `realtimebidding_bidders_creatives_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_creatives_list(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersCreativesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCreativesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_creatives_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    realtimebidding_bidders_creatives_list_execute(builder)
}

/// POST v1/bidders/{biddersId}/creatives:watch
/// Watches all creatives pertaining to a bidder. It is sufficient to invoke this endpoint once per bidder. A P`ub/Sub` topic will be created and notifications will be pushed to the topic when any of the bidder''s creatives change status. All of the bidder''s service accounts will have access to read from the topic. Subsequent invocations of this method will return the existing P`ub/Sub` configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_creatives_watch_execute()` to send, or `realtimebidding_bidders_creatives_watch` for simplest API.

pub fn realtimebidding_bidders_creatives_watch_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/creatives:watch",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/bidders/{biddersId}/creatives:watch
/// Watches all creatives pertaining to a bidder. It is sufficient to invoke this endpoint once per bidder. A P`ub/Sub` topic will be created and notifications will be pushed to the topic when any of the bidder''s creatives change status. All of the bidder''s service accounts will have access to read from the topic. Subsequent invocations of this method will return the existing P`ub/Sub` configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_creatives_watch_execute()` or `realtimebidding_bidders_creatives_watch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_creatives_watch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_creatives_watch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WatchCreativesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WatchCreativesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/bidders/{biddersId}/creatives:watch
/// Watches all creatives pertaining to a bidder. It is sufficient to invoke this endpoint once per bidder. A P`ub/Sub` topic will be created and notifications will be pushed to the topic when any of the bidder''s creatives change status. All of the bidder''s service accounts will have access to read from the topic. Subsequent invocations of this method will return the existing P`ub/Sub` configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_creatives_watch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_creatives_watch_task()`.
/// For the simplest API, use `realtimebidding_bidders_creatives_watch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_creatives_watch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_creatives_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WatchCreativesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_creatives_watch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_creatives_watch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersCreativesWatchArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/bidders/{biddersId}/creatives:watch
/// Watches all creatives pertaining to a bidder. It is sufficient to invoke this endpoint once per bidder. A P`ub/Sub` topic will be created and notifications will be pushed to the topic when any of the bidder''s creatives change status. All of the bidder''s service accounts will have access to read from the topic. Subsequent invocations of this method will return the existing P`ub/Sub` configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_creatives_watch_builder()` + `realtimebidding_bidders_creatives_watch_execute()`.
/// For task-level control, use `realtimebidding_bidders_creatives_watch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_creatives_watch(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersCreativesWatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WatchCreativesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_creatives_watch_builder(client, &args.parent)?;
    realtimebidding_bidders_creatives_watch_execute(builder)
}

/// GET v1/bidders/{biddersId}/endpoints/{endpointsId}
/// Gets a bidder endpoint by its name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_endpoints_get_execute()` to send, or `realtimebidding_bidders_endpoints_get` for simplest API.

pub fn realtimebidding_bidders_endpoints_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/endpoints/{endpointsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/bidders/{biddersId}/endpoints/{endpointsId}
/// Gets a bidder endpoint by its name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_endpoints_get_execute()` or `realtimebidding_bidders_endpoints_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_endpoints_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_endpoints_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Endpoint>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Endpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/bidders/{biddersId}/endpoints/{endpointsId}
/// Gets a bidder endpoint by its name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_endpoints_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_endpoints_get_task()`.
/// For the simplest API, use `realtimebidding_bidders_endpoints_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_endpoints_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_endpoints_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Endpoint>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_endpoints_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_endpoints_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersEndpointsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/bidders/{biddersId}/endpoints/{endpointsId}
/// Gets a bidder endpoint by its name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_endpoints_get_builder()` + `realtimebidding_bidders_endpoints_get_execute()`.
/// For task-level control, use `realtimebidding_bidders_endpoints_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_endpoints_get(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersEndpointsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Endpoint>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_endpoints_get_builder(client, &args.name)?;
    realtimebidding_bidders_endpoints_get_execute(builder)
}

/// GET v1/bidders/{biddersId}/endpoints
/// Lists all the bidder''s endpoints.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_endpoints_list_execute()` to send, or `realtimebidding_bidders_endpoints_list` for simplest API.

pub fn realtimebidding_bidders_endpoints_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/endpoints",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/bidders/{biddersId}/endpoints
/// Lists all the bidder''s endpoints.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_endpoints_list_execute()` or `realtimebidding_bidders_endpoints_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_endpoints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_endpoints_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListEndpointsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEndpointsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/bidders/{biddersId}/endpoints
/// Lists all the bidder''s endpoints.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_endpoints_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_endpoints_list_task()`.
/// For the simplest API, use `realtimebidding_bidders_endpoints_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_endpoints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_endpoints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEndpointsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_endpoints_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_endpoints_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersEndpointsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/bidders/{biddersId}/endpoints
/// Lists all the bidder''s endpoints.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_endpoints_list_builder()` + `realtimebidding_bidders_endpoints_list_execute()`.
/// For task-level control, use `realtimebidding_bidders_endpoints_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_endpoints_list(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersEndpointsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEndpointsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_endpoints_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    realtimebidding_bidders_endpoints_list_execute(builder)
}

/// PATCH v1/bidders/{biddersId}/endpoints/{endpointsId}
/// Updates a bidder''s endpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_endpoints_patch_execute()` to send, or `realtimebidding_bidders_endpoints_patch` for simplest API.

pub fn realtimebidding_bidders_endpoints_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/endpoints/{endpointsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/bidders/{biddersId}/endpoints/{endpointsId}
/// Updates a bidder''s endpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_endpoints_patch_execute()` or `realtimebidding_bidders_endpoints_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_endpoints_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_endpoints_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Endpoint>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Endpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/bidders/{biddersId}/endpoints/{endpointsId}
/// Updates a bidder''s endpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_endpoints_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_endpoints_patch_task()`.
/// For the simplest API, use `realtimebidding_bidders_endpoints_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_endpoints_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_endpoints_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Endpoint>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_endpoints_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_endpoints_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersEndpointsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/bidders/{biddersId}/endpoints/{endpointsId}
/// Updates a bidder''s endpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_endpoints_patch_builder()` + `realtimebidding_bidders_endpoints_patch_execute()`.
/// For task-level control, use `realtimebidding_bidders_endpoints_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_endpoints_patch(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersEndpointsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Endpoint>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        realtimebidding_bidders_endpoints_patch_builder(client, &args.name, &args.updateMask)?;
    realtimebidding_bidders_endpoints_patch_execute(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:activate
/// Activates a pretargeting configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_pretargeting_configs_activate_execute()` to send, or `realtimebidding_bidders_pretargeting_configs_activate` for simplest API.

pub fn realtimebidding_bidders_pretargeting_configs_activate_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/pretargetingConfigs/{pretargetingConfigsId}:activate",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:activate
/// Activates a pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_pretargeting_configs_activate_execute()` or `realtimebidding_bidders_pretargeting_configs_activate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_activate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PretargetingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PretargetingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:activate
/// Activates a pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_pretargeting_configs_activate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_activate_task()`.
/// For the simplest API, use `realtimebidding_bidders_pretargeting_configs_activate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_pretargeting_configs_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_pretargeting_configs_activate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_pretargeting_configs_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPretargetingConfigsActivateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:activate
/// Activates a pretargeting configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_pretargeting_configs_activate_builder()` + `realtimebidding_bidders_pretargeting_configs_activate_execute()`.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_activate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_activate(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPretargetingConfigsActivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        realtimebidding_bidders_pretargeting_configs_activate_builder(client, &args.name)?;
    realtimebidding_bidders_pretargeting_configs_activate_execute(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedApps
/// Adds targeted apps to the pretargeting configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_pretargeting_configs_add_targeted_apps_execute()` to send, or `realtimebidding_bidders_pretargeting_configs_add_targeted_apps` for simplest API.

pub fn realtimebidding_bidders_pretargeting_configs_add_targeted_apps_builder<R>(
    client: &SimpleHttpClient<R>,
    pretargetingConfig: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedApps",
        pretargetingConfig,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedApps
/// Adds targeted apps to the pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_pretargeting_configs_add_targeted_apps_execute()` or `realtimebidding_bidders_pretargeting_configs_add_targeted_apps`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_add_targeted_apps_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_add_targeted_apps_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PretargetingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PretargetingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedApps
/// Adds targeted apps to the pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_pretargeting_configs_add_targeted_apps_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_add_targeted_apps_task()`.
/// For the simplest API, use `realtimebidding_bidders_pretargeting_configs_add_targeted_apps()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_add_targeted_apps_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_pretargeting_configs_add_targeted_apps_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_pretargeting_configs_add_targeted_apps_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_pretargeting_configs_add_targeted_apps`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPretargetingConfigsAddTargetedAppsArgs {
    /// Path parameter: pretargetingConfig
    pub pretargetingConfig: String,
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedApps
/// Adds targeted apps to the pretargeting configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_pretargeting_configs_add_targeted_apps_builder()` + `realtimebidding_bidders_pretargeting_configs_add_targeted_apps_execute()`.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_add_targeted_apps_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_add_targeted_apps(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPretargetingConfigsAddTargetedAppsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_pretargeting_configs_add_targeted_apps_builder(
        client,
        &args.pretargetingConfig,
    )?;
    realtimebidding_bidders_pretargeting_configs_add_targeted_apps_execute(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedPublishers
/// Adds targeted publishers to the pretargeting config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_execute()` to send, or `realtimebidding_bidders_pretargeting_configs_add_targeted_publishers` for simplest API.

pub fn realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_builder<R>(
    client: &SimpleHttpClient<R>,
    pretargetingConfig: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedPublishers",
        pretargetingConfig,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedPublishers
/// Adds targeted publishers to the pretargeting config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_execute()` or `realtimebidding_bidders_pretargeting_configs_add_targeted_publishers`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PretargetingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PretargetingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedPublishers
/// Adds targeted publishers to the pretargeting config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_task()`.
/// For the simplest API, use `realtimebidding_bidders_pretargeting_configs_add_targeted_publishers()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_pretargeting_configs_add_targeted_publishers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPretargetingConfigsAddTargetedPublishersArgs {
    /// Path parameter: pretargetingConfig
    pub pretargetingConfig: String,
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedPublishers
/// Adds targeted publishers to the pretargeting config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_builder()` + `realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_execute()`.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_add_targeted_publishers(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPretargetingConfigsAddTargetedPublishersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_builder(
        client,
        &args.pretargetingConfig,
    )?;
    realtimebidding_bidders_pretargeting_configs_add_targeted_publishers_execute(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedSites
/// Adds targeted sites to the pretargeting configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_pretargeting_configs_add_targeted_sites_execute()` to send, or `realtimebidding_bidders_pretargeting_configs_add_targeted_sites` for simplest API.

pub fn realtimebidding_bidders_pretargeting_configs_add_targeted_sites_builder<R>(
    client: &SimpleHttpClient<R>,
    pretargetingConfig: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedSites",
        pretargetingConfig,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedSites
/// Adds targeted sites to the pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_pretargeting_configs_add_targeted_sites_execute()` or `realtimebidding_bidders_pretargeting_configs_add_targeted_sites`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_add_targeted_sites_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_add_targeted_sites_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PretargetingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PretargetingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedSites
/// Adds targeted sites to the pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_pretargeting_configs_add_targeted_sites_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_add_targeted_sites_task()`.
/// For the simplest API, use `realtimebidding_bidders_pretargeting_configs_add_targeted_sites()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_add_targeted_sites_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_pretargeting_configs_add_targeted_sites_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_pretargeting_configs_add_targeted_sites_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_pretargeting_configs_add_targeted_sites`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPretargetingConfigsAddTargetedSitesArgs {
    /// Path parameter: pretargetingConfig
    pub pretargetingConfig: String,
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:addTargetedSites
/// Adds targeted sites to the pretargeting configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_pretargeting_configs_add_targeted_sites_builder()` + `realtimebidding_bidders_pretargeting_configs_add_targeted_sites_execute()`.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_add_targeted_sites_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_add_targeted_sites(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPretargetingConfigsAddTargetedSitesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_pretargeting_configs_add_targeted_sites_builder(
        client,
        &args.pretargetingConfig,
    )?;
    realtimebidding_bidders_pretargeting_configs_add_targeted_sites_execute(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs
/// Creates a pretargeting configuration. A pretargeting configuration''s state (PretargetingConfig.state) is active upon creation, and it will start to affect traffic shortly after. A bidder may create a maximum of 10 pretargeting configurations. Attempts to exceed this maximum results in a 400 bad request error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_pretargeting_configs_create_execute()` to send, or `realtimebidding_bidders_pretargeting_configs_create` for simplest API.

pub fn realtimebidding_bidders_pretargeting_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/pretargetingConfigs",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs
/// Creates a pretargeting configuration. A pretargeting configuration''s state (PretargetingConfig.state) is active upon creation, and it will start to affect traffic shortly after. A bidder may create a maximum of 10 pretargeting configurations. Attempts to exceed this maximum results in a 400 bad request error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_pretargeting_configs_create_execute()` or `realtimebidding_bidders_pretargeting_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PretargetingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PretargetingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs
/// Creates a pretargeting configuration. A pretargeting configuration''s state (PretargetingConfig.state) is active upon creation, and it will start to affect traffic shortly after. A bidder may create a maximum of 10 pretargeting configurations. Attempts to exceed this maximum results in a 400 bad request error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_pretargeting_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_create_task()`.
/// For the simplest API, use `realtimebidding_bidders_pretargeting_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_pretargeting_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_pretargeting_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_pretargeting_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPretargetingConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs
/// Creates a pretargeting configuration. A pretargeting configuration''s state (PretargetingConfig.state) is active upon creation, and it will start to affect traffic shortly after. A bidder may create a maximum of 10 pretargeting configurations. Attempts to exceed this maximum results in a 400 bad request error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_pretargeting_configs_create_builder()` + `realtimebidding_bidders_pretargeting_configs_create_execute()`.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_create(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPretargetingConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        realtimebidding_bidders_pretargeting_configs_create_builder(client, &args.parent)?;
    realtimebidding_bidders_pretargeting_configs_create_execute(builder)
}

/// DELETE v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}
/// Deletes a pretargeting configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_pretargeting_configs_delete_execute()` to send, or `realtimebidding_bidders_pretargeting_configs_delete` for simplest API.

pub fn realtimebidding_bidders_pretargeting_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/pretargetingConfigs/{pretargetingConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}
/// Deletes a pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_pretargeting_configs_delete_execute()` or `realtimebidding_bidders_pretargeting_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}
/// Deletes a pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_pretargeting_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_delete_task()`.
/// For the simplest API, use `realtimebidding_bidders_pretargeting_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_pretargeting_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_pretargeting_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_pretargeting_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPretargetingConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}
/// Deletes a pretargeting configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_pretargeting_configs_delete_builder()` + `realtimebidding_bidders_pretargeting_configs_delete_execute()`.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_delete(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPretargetingConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_pretargeting_configs_delete_builder(client, &args.name)?;
    realtimebidding_bidders_pretargeting_configs_delete_execute(builder)
}

/// GET v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}
/// Gets a pretargeting configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_pretargeting_configs_get_execute()` to send, or `realtimebidding_bidders_pretargeting_configs_get` for simplest API.

pub fn realtimebidding_bidders_pretargeting_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/pretargetingConfigs/{pretargetingConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}
/// Gets a pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_pretargeting_configs_get_execute()` or `realtimebidding_bidders_pretargeting_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PretargetingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PretargetingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}
/// Gets a pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_pretargeting_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_get_task()`.
/// For the simplest API, use `realtimebidding_bidders_pretargeting_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_pretargeting_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_pretargeting_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_pretargeting_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPretargetingConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}
/// Gets a pretargeting configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_pretargeting_configs_get_builder()` + `realtimebidding_bidders_pretargeting_configs_get_execute()`.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_get(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPretargetingConfigsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_pretargeting_configs_get_builder(client, &args.name)?;
    realtimebidding_bidders_pretargeting_configs_get_execute(builder)
}

/// GET v1/bidders/{biddersId}/pretargetingConfigs
/// Lists all pretargeting configurations for a single bidder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_pretargeting_configs_list_execute()` to send, or `realtimebidding_bidders_pretargeting_configs_list` for simplest API.

pub fn realtimebidding_bidders_pretargeting_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/pretargetingConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/bidders/{biddersId}/pretargetingConfigs
/// Lists all pretargeting configurations for a single bidder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_pretargeting_configs_list_execute()` or `realtimebidding_bidders_pretargeting_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPretargetingConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPretargetingConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/bidders/{biddersId}/pretargetingConfigs
/// Lists all pretargeting configurations for a single bidder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_pretargeting_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_list_task()`.
/// For the simplest API, use `realtimebidding_bidders_pretargeting_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_pretargeting_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPretargetingConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_pretargeting_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_pretargeting_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPretargetingConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/bidders/{biddersId}/pretargetingConfigs
/// Lists all pretargeting configurations for a single bidder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_pretargeting_configs_list_builder()` + `realtimebidding_bidders_pretargeting_configs_list_execute()`.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_list(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPretargetingConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPretargetingConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_pretargeting_configs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    realtimebidding_bidders_pretargeting_configs_list_execute(builder)
}

/// PATCH v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}
/// Updates a pretargeting configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_pretargeting_configs_patch_execute()` to send, or `realtimebidding_bidders_pretargeting_configs_patch` for simplest API.

pub fn realtimebidding_bidders_pretargeting_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/pretargetingConfigs/{pretargetingConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}
/// Updates a pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_pretargeting_configs_patch_execute()` or `realtimebidding_bidders_pretargeting_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PretargetingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PretargetingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}
/// Updates a pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_pretargeting_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_patch_task()`.
/// For the simplest API, use `realtimebidding_bidders_pretargeting_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_pretargeting_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_pretargeting_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_pretargeting_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPretargetingConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}
/// Updates a pretargeting configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_pretargeting_configs_patch_builder()` + `realtimebidding_bidders_pretargeting_configs_patch_execute()`.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_patch(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPretargetingConfigsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_pretargeting_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    realtimebidding_bidders_pretargeting_configs_patch_execute(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedApps
/// Removes targeted apps from the pretargeting configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_execute()` to send, or `realtimebidding_bidders_pretargeting_configs_remove_targeted_apps` for simplest API.

pub fn realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_builder<R>(
    client: &SimpleHttpClient<R>,
    pretargetingConfig: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedApps",
        pretargetingConfig,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedApps
/// Removes targeted apps from the pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_execute()` or `realtimebidding_bidders_pretargeting_configs_remove_targeted_apps`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PretargetingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PretargetingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedApps
/// Removes targeted apps from the pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_task()`.
/// For the simplest API, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_apps()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_pretargeting_configs_remove_targeted_apps`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPretargetingConfigsRemoveTargetedAppsArgs {
    /// Path parameter: pretargetingConfig
    pub pretargetingConfig: String,
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedApps
/// Removes targeted apps from the pretargeting configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_builder()` + `realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_execute()`.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_remove_targeted_apps(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPretargetingConfigsRemoveTargetedAppsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_builder(
        client,
        &args.pretargetingConfig,
    )?;
    realtimebidding_bidders_pretargeting_configs_remove_targeted_apps_execute(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedPublishers
/// Removes targeted publishers from the pretargeting config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_execute()` to send, or `realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers` for simplest API.

pub fn realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_builder<R>(
    client: &SimpleHttpClient<R>,
    pretargetingConfig: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedPublishers",
        pretargetingConfig,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedPublishers
/// Removes targeted publishers from the pretargeting config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_execute()` or `realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PretargetingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PretargetingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedPublishers
/// Removes targeted publishers from the pretargeting config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_task()`.
/// For the simplest API, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPretargetingConfigsRemoveTargetedPublishersArgs {
    /// Path parameter: pretargetingConfig
    pub pretargetingConfig: String,
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedPublishers
/// Removes targeted publishers from the pretargeting config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_builder()` + `realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_execute()`.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPretargetingConfigsRemoveTargetedPublishersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_builder(
        client,
        &args.pretargetingConfig,
    )?;
    realtimebidding_bidders_pretargeting_configs_remove_targeted_publishers_execute(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedSites
/// Removes targeted sites from the pretargeting configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_execute()` to send, or `realtimebidding_bidders_pretargeting_configs_remove_targeted_sites` for simplest API.

pub fn realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_builder<R>(
    client: &SimpleHttpClient<R>,
    pretargetingConfig: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedSites",
        pretargetingConfig,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedSites
/// Removes targeted sites from the pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_execute()` or `realtimebidding_bidders_pretargeting_configs_remove_targeted_sites`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PretargetingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PretargetingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedSites
/// Removes targeted sites from the pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_task()`.
/// For the simplest API, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_sites()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_pretargeting_configs_remove_targeted_sites`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPretargetingConfigsRemoveTargetedSitesArgs {
    /// Path parameter: pretargetingConfig
    pub pretargetingConfig: String,
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:removeTargetedSites
/// Removes targeted sites from the pretargeting configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_builder()` + `realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_execute()`.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_remove_targeted_sites(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPretargetingConfigsRemoveTargetedSitesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_builder(
        client,
        &args.pretargetingConfig,
    )?;
    realtimebidding_bidders_pretargeting_configs_remove_targeted_sites_execute(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:suspend
/// Suspends a pretargeting configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_pretargeting_configs_suspend_execute()` to send, or `realtimebidding_bidders_pretargeting_configs_suspend` for simplest API.

pub fn realtimebidding_bidders_pretargeting_configs_suspend_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/pretargetingConfigs/{pretargetingConfigsId}:suspend",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:suspend
/// Suspends a pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_pretargeting_configs_suspend_execute()` or `realtimebidding_bidders_pretargeting_configs_suspend`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_suspend_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_suspend_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PretargetingConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PretargetingConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:suspend
/// Suspends a pretargeting configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_pretargeting_configs_suspend_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_suspend_task()`.
/// For the simplest API, use `realtimebidding_bidders_pretargeting_configs_suspend()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_pretargeting_configs_suspend_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_pretargeting_configs_suspend_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_pretargeting_configs_suspend_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_pretargeting_configs_suspend`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPretargetingConfigsSuspendArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/bidders/{biddersId}/pretargetingConfigs/{pretargetingConfigsId}:suspend
/// Suspends a pretargeting configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_pretargeting_configs_suspend_builder()` + `realtimebidding_bidders_pretargeting_configs_suspend_execute()`.
/// For task-level control, use `realtimebidding_bidders_pretargeting_configs_suspend_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_pretargeting_configs_suspend(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPretargetingConfigsSuspendArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PretargetingConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_pretargeting_configs_suspend_builder(client, &args.name)?;
    realtimebidding_bidders_pretargeting_configs_suspend_execute(builder)
}

/// POST v1/bidders/{biddersId}/publisherConnections:batchApprove
/// Batch approves multiple publisher connections.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_publisher_connections_batch_approve_execute()` to send, or `realtimebidding_bidders_publisher_connections_batch_approve` for simplest API.

pub fn realtimebidding_bidders_publisher_connections_batch_approve_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/publisherConnections:batchApprove",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/bidders/{biddersId}/publisherConnections:batchApprove
/// Batch approves multiple publisher connections.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_publisher_connections_batch_approve_execute()` or `realtimebidding_bidders_publisher_connections_batch_approve`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_publisher_connections_batch_approve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_publisher_connections_batch_approve_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchApprovePublisherConnectionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchApprovePublisherConnectionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/bidders/{biddersId}/publisherConnections:batchApprove
/// Batch approves multiple publisher connections.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_publisher_connections_batch_approve_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_publisher_connections_batch_approve_task()`.
/// For the simplest API, use `realtimebidding_bidders_publisher_connections_batch_approve()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_publisher_connections_batch_approve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_publisher_connections_batch_approve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchApprovePublisherConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_publisher_connections_batch_approve_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_publisher_connections_batch_approve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPublisherConnectionsBatchApproveArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/bidders/{biddersId}/publisherConnections:batchApprove
/// Batch approves multiple publisher connections.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_publisher_connections_batch_approve_builder()` + `realtimebidding_bidders_publisher_connections_batch_approve_execute()`.
/// For task-level control, use `realtimebidding_bidders_publisher_connections_batch_approve_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_publisher_connections_batch_approve(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPublisherConnectionsBatchApproveArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchApprovePublisherConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        realtimebidding_bidders_publisher_connections_batch_approve_builder(client, &args.parent)?;
    realtimebidding_bidders_publisher_connections_batch_approve_execute(builder)
}

/// POST v1/bidders/{biddersId}/publisherConnections:batchReject
/// Batch rejects multiple publisher connections.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_publisher_connections_batch_reject_execute()` to send, or `realtimebidding_bidders_publisher_connections_batch_reject` for simplest API.

pub fn realtimebidding_bidders_publisher_connections_batch_reject_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/publisherConnections:batchReject",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/bidders/{biddersId}/publisherConnections:batchReject
/// Batch rejects multiple publisher connections.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_publisher_connections_batch_reject_execute()` or `realtimebidding_bidders_publisher_connections_batch_reject`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_publisher_connections_batch_reject_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_publisher_connections_batch_reject_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchRejectPublisherConnectionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchRejectPublisherConnectionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/bidders/{biddersId}/publisherConnections:batchReject
/// Batch rejects multiple publisher connections.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_publisher_connections_batch_reject_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_publisher_connections_batch_reject_task()`.
/// For the simplest API, use `realtimebidding_bidders_publisher_connections_batch_reject()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_publisher_connections_batch_reject_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_publisher_connections_batch_reject_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchRejectPublisherConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_publisher_connections_batch_reject_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_publisher_connections_batch_reject`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPublisherConnectionsBatchRejectArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/bidders/{biddersId}/publisherConnections:batchReject
/// Batch rejects multiple publisher connections.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_publisher_connections_batch_reject_builder()` + `realtimebidding_bidders_publisher_connections_batch_reject_execute()`.
/// For task-level control, use `realtimebidding_bidders_publisher_connections_batch_reject_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_publisher_connections_batch_reject(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPublisherConnectionsBatchRejectArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchRejectPublisherConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        realtimebidding_bidders_publisher_connections_batch_reject_builder(client, &args.parent)?;
    realtimebidding_bidders_publisher_connections_batch_reject_execute(builder)
}

/// GET v1/bidders/{biddersId}/publisherConnections/{publisherConnectionsId}
/// Gets a publisher connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_publisher_connections_get_execute()` to send, or `realtimebidding_bidders_publisher_connections_get` for simplest API.

pub fn realtimebidding_bidders_publisher_connections_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/publisherConnections/{publisherConnectionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/bidders/{biddersId}/publisherConnections/{publisherConnectionsId}
/// Gets a publisher connection.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_publisher_connections_get_execute()` or `realtimebidding_bidders_publisher_connections_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_publisher_connections_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_publisher_connections_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublisherConnection>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PublisherConnection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/bidders/{biddersId}/publisherConnections/{publisherConnectionsId}
/// Gets a publisher connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_publisher_connections_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_publisher_connections_get_task()`.
/// For the simplest API, use `realtimebidding_bidders_publisher_connections_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_publisher_connections_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_publisher_connections_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublisherConnection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_publisher_connections_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_publisher_connections_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPublisherConnectionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/bidders/{biddersId}/publisherConnections/{publisherConnectionsId}
/// Gets a publisher connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_publisher_connections_get_builder()` + `realtimebidding_bidders_publisher_connections_get_execute()`.
/// For task-level control, use `realtimebidding_bidders_publisher_connections_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_publisher_connections_get(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPublisherConnectionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PublisherConnection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_publisher_connections_get_builder(client, &args.name)?;
    realtimebidding_bidders_publisher_connections_get_execute(builder)
}

/// GET v1/bidders/{biddersId}/publisherConnections
/// Lists publisher connections for a given bidder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_bidders_publisher_connections_list_execute()` to send, or `realtimebidding_bidders_publisher_connections_list` for simplest API.

pub fn realtimebidding_bidders_publisher_connections_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/bidders/{}/publisherConnections",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/bidders/{biddersId}/publisherConnections
/// Lists publisher connections for a given bidder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_bidders_publisher_connections_list_execute()` or `realtimebidding_bidders_publisher_connections_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_publisher_connections_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_publisher_connections_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPublisherConnectionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPublisherConnectionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/bidders/{biddersId}/publisherConnections
/// Lists publisher connections for a given bidder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_bidders_publisher_connections_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_bidders_publisher_connections_list_task()`.
/// For the simplest API, use `realtimebidding_bidders_publisher_connections_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_bidders_publisher_connections_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_bidders_publisher_connections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPublisherConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_bidders_publisher_connections_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_bidders_publisher_connections_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBiddersPublisherConnectionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/bidders/{biddersId}/publisherConnections
/// Lists publisher connections for a given bidder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_bidders_publisher_connections_list_builder()` + `realtimebidding_bidders_publisher_connections_list_execute()`.
/// For task-level control, use `realtimebidding_bidders_publisher_connections_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_bidders_publisher_connections_list(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBiddersPublisherConnectionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPublisherConnectionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_bidders_publisher_connections_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    realtimebidding_bidders_publisher_connections_list_execute(builder)
}

/// GET v1/buyers/{buyersId}
/// Gets a buyer account by its name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_buyers_get_execute()` to send, or `realtimebidding_buyers_get` for simplest API.

pub fn realtimebidding_buyers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://realtimebidding.googleapis.com/v1/buyers/{}", name,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}
/// Gets a buyer account by its name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_buyers_get_execute()` or `realtimebidding_buyers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Buyer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Buyer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}
/// Gets a buyer account by its name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_buyers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_buyers_get_task()`.
/// For the simplest API, use `realtimebidding_buyers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_buyers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Buyer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = realtimebidding_buyers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_buyers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBuyersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/buyers/{buyersId}
/// Gets a buyer account by its name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_buyers_get_builder()` + `realtimebidding_buyers_get_execute()`.
/// For task-level control, use `realtimebidding_buyers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_get(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBuyersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Buyer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = realtimebidding_buyers_get_builder(client, &args.name)?;
    realtimebidding_buyers_get_execute(builder)
}

/// GET v1/buyers/{buyersId}:getRemarketingTag
/// This has been sunset as of October 2023, and will return an error response if called. For more information, see the release notes: <https://developers.google.`com/authorized-buyers/apis/relnotes`#real-time-bidding-api> Gets remarketing tag for a buyer. A remarketing tag is a piece of JavaScript code that can be placed on a web page. When a user visits a page containing a remarketing tag, Google adds the user to a user list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_buyers_get_remarketing_tag_execute()` to send, or `realtimebidding_buyers_get_remarketing_tag` for simplest API.

pub fn realtimebidding_buyers_get_remarketing_tag_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/buyers/{}:getRemarketingTag",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}:getRemarketingTag
/// This has been sunset as of October 2023, and will return an error response if called. For more information, see the release notes: <https://developers.google.`com/authorized-buyers/apis/relnotes`#real-time-bidding-api> Gets remarketing tag for a buyer. A remarketing tag is a piece of JavaScript code that can be placed on a web page. When a user visits a page containing a remarketing tag, Google adds the user to a user list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_buyers_get_remarketing_tag_execute()` or `realtimebidding_buyers_get_remarketing_tag`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_get_remarketing_tag_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_get_remarketing_tag_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetRemarketingTagResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetRemarketingTagResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}:getRemarketingTag
/// This has been sunset as of October 2023, and will return an error response if called. For more information, see the release notes: <https://developers.google.`com/authorized-buyers/apis/relnotes`#real-time-bidding-api> Gets remarketing tag for a buyer. A remarketing tag is a piece of JavaScript code that can be placed on a web page. When a user visits a page containing a remarketing tag, Google adds the user to a user list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_buyers_get_remarketing_tag_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_buyers_get_remarketing_tag_task()`.
/// For the simplest API, use `realtimebidding_buyers_get_remarketing_tag()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_get_remarketing_tag_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_buyers_get_remarketing_tag_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetRemarketingTagResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_buyers_get_remarketing_tag_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_buyers_get_remarketing_tag`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBuyersGetRemarketingTagArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/buyers/{buyersId}:getRemarketingTag
/// This has been sunset as of October 2023, and will return an error response if called. For more information, see the release notes: <https://developers.google.`com/authorized-buyers/apis/relnotes`#real-time-bidding-api> Gets remarketing tag for a buyer. A remarketing tag is a piece of JavaScript code that can be placed on a web page. When a user visits a page containing a remarketing tag, Google adds the user to a user list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_buyers_get_remarketing_tag_builder()` + `realtimebidding_buyers_get_remarketing_tag_execute()`.
/// For task-level control, use `realtimebidding_buyers_get_remarketing_tag_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_get_remarketing_tag(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBuyersGetRemarketingTagArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetRemarketingTagResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_buyers_get_remarketing_tag_builder(client, &args.name)?;
    realtimebidding_buyers_get_remarketing_tag_execute(builder)
}

/// GET v1/buyers
/// Lists all buyer account information the calling buyer user or service account is permissioned to manage.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_buyers_list_execute()` to send, or `realtimebidding_buyers_list` for simplest API.

pub fn realtimebidding_buyers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://realtimebidding.googleapis.com/v1/buyers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers
/// Lists all buyer account information the calling buyer user or service account is permissioned to manage.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_buyers_list_execute()` or `realtimebidding_buyers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBuyersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBuyersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers
/// Lists all buyer account information the calling buyer user or service account is permissioned to manage.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_buyers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_buyers_list_task()`.
/// For the simplest API, use `realtimebidding_buyers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_buyers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBuyersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_buyers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_buyers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBuyersListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/buyers
/// Lists all buyer account information the calling buyer user or service account is permissioned to manage.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_buyers_list_builder()` + `realtimebidding_buyers_list_execute()`.
/// For task-level control, use `realtimebidding_buyers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_list(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBuyersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBuyersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_buyers_list_builder(client, &args.pageSize, &args.pageToken)?;
    realtimebidding_buyers_list_execute(builder)
}

/// POST v1/buyers/{buyersId}/creatives
/// Creates a creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_buyers_creatives_create_execute()` to send, or `realtimebidding_buyers_creatives_create` for simplest API.

pub fn realtimebidding_buyers_creatives_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/buyers/{}/creatives",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/creatives
/// Creates a creative.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_buyers_creatives_create_execute()` or `realtimebidding_buyers_creatives_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_creatives_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_creatives_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/creatives
/// Creates a creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_buyers_creatives_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_buyers_creatives_create_task()`.
/// For the simplest API, use `realtimebidding_buyers_creatives_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_creatives_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_buyers_creatives_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = realtimebidding_buyers_creatives_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_buyers_creatives_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBuyersCreativesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/buyers/{buyersId}/creatives
/// Creates a creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_buyers_creatives_create_builder()` + `realtimebidding_buyers_creatives_create_execute()`.
/// For task-level control, use `realtimebidding_buyers_creatives_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_creatives_create(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBuyersCreativesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = realtimebidding_buyers_creatives_create_builder(client, &args.parent)?;
    realtimebidding_buyers_creatives_create_execute(builder)
}

/// GET v1/buyers/{buyersId}/creatives/{creativesId}
/// Gets a creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_buyers_creatives_get_execute()` to send, or `realtimebidding_buyers_creatives_get` for simplest API.

pub fn realtimebidding_buyers_creatives_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/buyers/{}/creatives/{creativesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/creatives/{creativesId}
/// Gets a creative.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_buyers_creatives_get_execute()` or `realtimebidding_buyers_creatives_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_creatives_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_creatives_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/creatives/{creativesId}
/// Gets a creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_buyers_creatives_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_buyers_creatives_get_task()`.
/// For the simplest API, use `realtimebidding_buyers_creatives_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_creatives_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_buyers_creatives_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = realtimebidding_buyers_creatives_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_buyers_creatives_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBuyersCreativesGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/buyers/{buyersId}/creatives/{creativesId}
/// Gets a creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_buyers_creatives_get_builder()` + `realtimebidding_buyers_creatives_get_execute()`.
/// For task-level control, use `realtimebidding_buyers_creatives_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_creatives_get(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBuyersCreativesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = realtimebidding_buyers_creatives_get_builder(client, &args.name, &args.view)?;
    realtimebidding_buyers_creatives_get_execute(builder)
}

/// GET v1/buyers/{buyersId}/creatives
/// Lists creatives as they are at the time of the initial request. This call may take multiple hours to complete. For large, paginated requests, this method returns a snapshot of creatives at the time of request for the first page. `lastStatusUpdate` and `creativeServingDecision` may be outdated for creatives on sequential pages. We recommend [Google Cloud P`ub/Sub`](//cloud.google.`com/pubsub/docs/overview`) to view the latest status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_buyers_creatives_list_execute()` to send, or `realtimebidding_buyers_creatives_list` for simplest API.

pub fn realtimebidding_buyers_creatives_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/buyers/{}/creatives",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/creatives
/// Lists creatives as they are at the time of the initial request. This call may take multiple hours to complete. For large, paginated requests, this method returns a snapshot of creatives at the time of request for the first page. `lastStatusUpdate` and `creativeServingDecision` may be outdated for creatives on sequential pages. We recommend [Google Cloud P`ub/Sub`](//cloud.google.`com/pubsub/docs/overview`) to view the latest status.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_buyers_creatives_list_execute()` or `realtimebidding_buyers_creatives_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_creatives_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_creatives_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCreativesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCreativesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/creatives
/// Lists creatives as they are at the time of the initial request. This call may take multiple hours to complete. For large, paginated requests, this method returns a snapshot of creatives at the time of request for the first page. `lastStatusUpdate` and `creativeServingDecision` may be outdated for creatives on sequential pages. We recommend [Google Cloud P`ub/Sub`](//cloud.google.`com/pubsub/docs/overview`) to view the latest status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_buyers_creatives_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_buyers_creatives_list_task()`.
/// For the simplest API, use `realtimebidding_buyers_creatives_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_creatives_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_buyers_creatives_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCreativesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_buyers_creatives_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_buyers_creatives_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBuyersCreativesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/buyers/{buyersId}/creatives
/// Lists creatives as they are at the time of the initial request. This call may take multiple hours to complete. For large, paginated requests, this method returns a snapshot of creatives at the time of request for the first page. `lastStatusUpdate` and `creativeServingDecision` may be outdated for creatives on sequential pages. We recommend [Google Cloud P`ub/Sub`](//cloud.google.`com/pubsub/docs/overview`) to view the latest status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_buyers_creatives_list_builder()` + `realtimebidding_buyers_creatives_list_execute()`.
/// For task-level control, use `realtimebidding_buyers_creatives_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_creatives_list(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBuyersCreativesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCreativesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_buyers_creatives_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    realtimebidding_buyers_creatives_list_execute(builder)
}

/// PATCH v1/buyers/{buyersId}/creatives/{creativesId}
/// Updates a creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_buyers_creatives_patch_execute()` to send, or `realtimebidding_buyers_creatives_patch` for simplest API.

pub fn realtimebidding_buyers_creatives_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/buyers/{}/creatives/{creativesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/buyers/{buyersId}/creatives/{creativesId}
/// Updates a creative.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_buyers_creatives_patch_execute()` or `realtimebidding_buyers_creatives_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_creatives_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_creatives_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/buyers/{buyersId}/creatives/{creativesId}
/// Updates a creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_buyers_creatives_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_buyers_creatives_patch_task()`.
/// For the simplest API, use `realtimebidding_buyers_creatives_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_creatives_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_buyers_creatives_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = realtimebidding_buyers_creatives_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_buyers_creatives_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBuyersCreativesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/buyers/{buyersId}/creatives/{creativesId}
/// Updates a creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_buyers_creatives_patch_builder()` + `realtimebidding_buyers_creatives_patch_execute()`.
/// For task-level control, use `realtimebidding_buyers_creatives_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_creatives_patch(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBuyersCreativesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        realtimebidding_buyers_creatives_patch_builder(client, &args.name, &args.updateMask)?;
    realtimebidding_buyers_creatives_patch_execute(builder)
}

/// POST v1/buyers/{buyersId}/userLists/{userListsId}:close
/// Changes the status of a user list to CLOSED. This prevents new users from being added to the user list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_buyers_user_lists_close_execute()` to send, or `realtimebidding_buyers_user_lists_close` for simplest API.

pub fn realtimebidding_buyers_user_lists_close_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/buyers/{}/userLists/{userListsId}:close",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/userLists/{userListsId}:close
/// Changes the status of a user list to CLOSED. This prevents new users from being added to the user list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_buyers_user_lists_close_execute()` or `realtimebidding_buyers_user_lists_close`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_user_lists_close_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_user_lists_close_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/userLists/{userListsId}:close
/// Changes the status of a user list to CLOSED. This prevents new users from being added to the user list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_buyers_user_lists_close_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_buyers_user_lists_close_task()`.
/// For the simplest API, use `realtimebidding_buyers_user_lists_close()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_user_lists_close_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_buyers_user_lists_close_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = realtimebidding_buyers_user_lists_close_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_buyers_user_lists_close`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBuyersUserListsCloseArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/buyers/{buyersId}/userLists/{userListsId}:close
/// Changes the status of a user list to CLOSED. This prevents new users from being added to the user list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_buyers_user_lists_close_builder()` + `realtimebidding_buyers_user_lists_close_execute()`.
/// For task-level control, use `realtimebidding_buyers_user_lists_close_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_user_lists_close(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBuyersUserListsCloseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = realtimebidding_buyers_user_lists_close_builder(client, &args.name)?;
    realtimebidding_buyers_user_lists_close_execute(builder)
}

/// POST v1/buyers/{buyersId}/userLists
/// Creates a new user list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_buyers_user_lists_create_execute()` to send, or `realtimebidding_buyers_user_lists_create` for simplest API.

pub fn realtimebidding_buyers_user_lists_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/buyers/{}/userLists",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/userLists
/// Creates a new user list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_buyers_user_lists_create_execute()` or `realtimebidding_buyers_user_lists_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_user_lists_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_user_lists_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/userLists
/// Creates a new user list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_buyers_user_lists_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_buyers_user_lists_create_task()`.
/// For the simplest API, use `realtimebidding_buyers_user_lists_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_user_lists_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_buyers_user_lists_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = realtimebidding_buyers_user_lists_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_buyers_user_lists_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBuyersUserListsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/buyers/{buyersId}/userLists
/// Creates a new user list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_buyers_user_lists_create_builder()` + `realtimebidding_buyers_user_lists_create_execute()`.
/// For task-level control, use `realtimebidding_buyers_user_lists_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_user_lists_create(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBuyersUserListsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = realtimebidding_buyers_user_lists_create_builder(client, &args.parent)?;
    realtimebidding_buyers_user_lists_create_execute(builder)
}

/// GET v1/buyers/{buyersId}/userLists/{userListsId}
/// Gets a user list by its name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_buyers_user_lists_get_execute()` to send, or `realtimebidding_buyers_user_lists_get` for simplest API.

pub fn realtimebidding_buyers_user_lists_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/buyers/{}/userLists/{userListsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/userLists/{userListsId}
/// Gets a user list by its name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_buyers_user_lists_get_execute()` or `realtimebidding_buyers_user_lists_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_user_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_user_lists_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/userLists/{userListsId}
/// Gets a user list by its name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_buyers_user_lists_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_buyers_user_lists_get_task()`.
/// For the simplest API, use `realtimebidding_buyers_user_lists_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_user_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_buyers_user_lists_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = realtimebidding_buyers_user_lists_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_buyers_user_lists_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBuyersUserListsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/buyers/{buyersId}/userLists/{userListsId}
/// Gets a user list by its name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_buyers_user_lists_get_builder()` + `realtimebidding_buyers_user_lists_get_execute()`.
/// For task-level control, use `realtimebidding_buyers_user_lists_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_user_lists_get(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBuyersUserListsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = realtimebidding_buyers_user_lists_get_builder(client, &args.name)?;
    realtimebidding_buyers_user_lists_get_execute(builder)
}

/// GET v1/buyers/{buyersId}/userLists/{userListsId}:getRemarketingTag
/// This has been sunset as of October 2023, and will return an error response if called. For more information, see the release notes: <https://developers.google.`com/authorized-buyers/apis/relnotes`#real-time-bidding-api> Gets remarketing tag for a buyer. A remarketing tag is a piece of JavaScript code that can be placed on a web page. When a user visits a page containing a remarketing tag, Google adds the user to a user list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_buyers_user_lists_get_remarketing_tag_execute()` to send, or `realtimebidding_buyers_user_lists_get_remarketing_tag` for simplest API.

pub fn realtimebidding_buyers_user_lists_get_remarketing_tag_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/buyers/{}/userLists/{userListsId}:getRemarketingTag",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/userLists/{userListsId}:getRemarketingTag
/// This has been sunset as of October 2023, and will return an error response if called. For more information, see the release notes: <https://developers.google.`com/authorized-buyers/apis/relnotes`#real-time-bidding-api> Gets remarketing tag for a buyer. A remarketing tag is a piece of JavaScript code that can be placed on a web page. When a user visits a page containing a remarketing tag, Google adds the user to a user list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_buyers_user_lists_get_remarketing_tag_execute()` or `realtimebidding_buyers_user_lists_get_remarketing_tag`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_user_lists_get_remarketing_tag_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_user_lists_get_remarketing_tag_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetRemarketingTagResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetRemarketingTagResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/userLists/{userListsId}:getRemarketingTag
/// This has been sunset as of October 2023, and will return an error response if called. For more information, see the release notes: <https://developers.google.`com/authorized-buyers/apis/relnotes`#real-time-bidding-api> Gets remarketing tag for a buyer. A remarketing tag is a piece of JavaScript code that can be placed on a web page. When a user visits a page containing a remarketing tag, Google adds the user to a user list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_buyers_user_lists_get_remarketing_tag_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_buyers_user_lists_get_remarketing_tag_task()`.
/// For the simplest API, use `realtimebidding_buyers_user_lists_get_remarketing_tag()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_user_lists_get_remarketing_tag_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_buyers_user_lists_get_remarketing_tag_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetRemarketingTagResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_buyers_user_lists_get_remarketing_tag_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_buyers_user_lists_get_remarketing_tag`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBuyersUserListsGetRemarketingTagArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/buyers/{buyersId}/userLists/{userListsId}:getRemarketingTag
/// This has been sunset as of October 2023, and will return an error response if called. For more information, see the release notes: <https://developers.google.`com/authorized-buyers/apis/relnotes`#real-time-bidding-api> Gets remarketing tag for a buyer. A remarketing tag is a piece of JavaScript code that can be placed on a web page. When a user visits a page containing a remarketing tag, Google adds the user to a user list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_buyers_user_lists_get_remarketing_tag_builder()` + `realtimebidding_buyers_user_lists_get_remarketing_tag_execute()`.
/// For task-level control, use `realtimebidding_buyers_user_lists_get_remarketing_tag_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_user_lists_get_remarketing_tag(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBuyersUserListsGetRemarketingTagArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetRemarketingTagResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        realtimebidding_buyers_user_lists_get_remarketing_tag_builder(client, &args.name)?;
    realtimebidding_buyers_user_lists_get_remarketing_tag_execute(builder)
}

/// GET v1/buyers/{buyersId}/userLists
/// Lists the user lists visible to the current user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_buyers_user_lists_list_execute()` to send, or `realtimebidding_buyers_user_lists_list` for simplest API.

pub fn realtimebidding_buyers_user_lists_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/buyers/{}/userLists",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/buyers/{buyersId}/userLists
/// Lists the user lists visible to the current user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_buyers_user_lists_list_execute()` or `realtimebidding_buyers_user_lists_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_user_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_user_lists_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUserListsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUserListsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/buyers/{buyersId}/userLists
/// Lists the user lists visible to the current user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_buyers_user_lists_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_buyers_user_lists_list_task()`.
/// For the simplest API, use `realtimebidding_buyers_user_lists_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_user_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_buyers_user_lists_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUserListsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = realtimebidding_buyers_user_lists_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_buyers_user_lists_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBuyersUserListsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/buyers/{buyersId}/userLists
/// Lists the user lists visible to the current user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_buyers_user_lists_list_builder()` + `realtimebidding_buyers_user_lists_list_execute()`.
/// For task-level control, use `realtimebidding_buyers_user_lists_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_user_lists_list(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBuyersUserListsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUserListsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = realtimebidding_buyers_user_lists_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    realtimebidding_buyers_user_lists_list_execute(builder)
}

/// POST v1/buyers/{buyersId}/userLists/{userListsId}:open
/// Changes the status of a user list to OPEN. This allows new users to be added to the user list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_buyers_user_lists_open_execute()` to send, or `realtimebidding_buyers_user_lists_open` for simplest API.

pub fn realtimebidding_buyers_user_lists_open_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/buyers/{}/userLists/{userListsId}:open",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/buyers/{buyersId}/userLists/{userListsId}:open
/// Changes the status of a user list to OPEN. This allows new users to be added to the user list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_buyers_user_lists_open_execute()` or `realtimebidding_buyers_user_lists_open`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_user_lists_open_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_user_lists_open_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/buyers/{buyersId}/userLists/{userListsId}:open
/// Changes the status of a user list to OPEN. This allows new users to be added to the user list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_buyers_user_lists_open_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_buyers_user_lists_open_task()`.
/// For the simplest API, use `realtimebidding_buyers_user_lists_open()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_user_lists_open_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_buyers_user_lists_open_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = realtimebidding_buyers_user_lists_open_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_buyers_user_lists_open`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBuyersUserListsOpenArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/buyers/{buyersId}/userLists/{userListsId}:open
/// Changes the status of a user list to OPEN. This allows new users to be added to the user list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_buyers_user_lists_open_builder()` + `realtimebidding_buyers_user_lists_open_execute()`.
/// For task-level control, use `realtimebidding_buyers_user_lists_open_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_user_lists_open(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBuyersUserListsOpenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = realtimebidding_buyers_user_lists_open_builder(client, &args.name)?;
    realtimebidding_buyers_user_lists_open_execute(builder)
}

/// PUT v1/buyers/{buyersId}/userLists/{userListsId}
/// Updates the given user list. Only user lists with URLRestrictions can be updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `realtimebidding_buyers_user_lists_update_execute()` to send, or `realtimebidding_buyers_user_lists_update` for simplest API.

pub fn realtimebidding_buyers_user_lists_update_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://realtimebidding.googleapis.com/v1/buyers/{}/userLists/{userListsId}",
        name,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/buyers/{buyersId}/userLists/{userListsId}
/// Updates the given user list. Only user lists with URLRestrictions can be updated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `realtimebidding_buyers_user_lists_update_execute()` or `realtimebidding_buyers_user_lists_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_user_lists_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_user_lists_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/buyers/{buyersId}/userLists/{userListsId}
/// Updates the given user list. Only user lists with URLRestrictions can be updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `realtimebidding_buyers_user_lists_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `realtimebidding_buyers_user_lists_update_task()`.
/// For the simplest API, use `realtimebidding_buyers_user_lists_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `realtimebidding_buyers_user_lists_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn realtimebidding_buyers_user_lists_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = realtimebidding_buyers_user_lists_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`realtimebidding_buyers_user_lists_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RealtimebiddingBuyersUserListsUpdateArgs {
    /// Path parameter: name
    pub name: String,
}

/// PUT v1/buyers/{buyersId}/userLists/{userListsId}
/// Updates the given user list. Only user lists with URLRestrictions can be updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `realtimebidding_buyers_user_lists_update_builder()` + `realtimebidding_buyers_user_lists_update_execute()`.
/// For task-level control, use `realtimebidding_buyers_user_lists_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn realtimebidding_buyers_user_lists_update(
    client: &SimpleHttpClient,
    args: &RealtimebiddingBuyersUserListsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = realtimebidding_buyers_user_lists_update_builder(client, &args.name)?;
    realtimebidding_buyers_user_lists_update_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Bidder
// =============================================================================

/// ResourceIdentifier implementation for Bidder with RealtimebiddingBiddersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersGetArgs> for Bidder {
    fn generate_resource_id(&self, input: &RealtimebiddingBiddersGetArgs) -> String {
        format!("gcp::realtimebidding::Bidder/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::Bidder"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBiddersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBiddersResponse with RealtimebiddingBiddersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersListArgs> for ListBiddersResponse {
    fn generate_resource_id(&self, input: &RealtimebiddingBiddersListArgs) -> String {
        "gcp::realtimebidding::ListBiddersResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::ListBiddersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCreativesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCreativesResponse with RealtimebiddingBiddersCreativesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersCreativesListArgs> for ListCreativesResponse {
    fn generate_resource_id(&self, input: &RealtimebiddingBiddersCreativesListArgs) -> String {
        format!(
            "gcp::realtimebidding::ListCreativesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::ListCreativesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WatchCreativesResponse
// =============================================================================

/// ResourceIdentifier implementation for WatchCreativesResponse with RealtimebiddingBiddersCreativesWatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersCreativesWatchArgs> for WatchCreativesResponse {
    fn generate_resource_id(&self, input: &RealtimebiddingBiddersCreativesWatchArgs) -> String {
        format!(
            "gcp::realtimebidding::WatchCreativesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::WatchCreativesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Endpoint
// =============================================================================

/// ResourceIdentifier implementation for Endpoint with RealtimebiddingBiddersEndpointsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersEndpointsGetArgs> for Endpoint {
    fn generate_resource_id(&self, input: &RealtimebiddingBiddersEndpointsGetArgs) -> String {
        format!("gcp::realtimebidding::Endpoint/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::Endpoint"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListEndpointsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListEndpointsResponse with RealtimebiddingBiddersEndpointsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersEndpointsListArgs> for ListEndpointsResponse {
    fn generate_resource_id(&self, input: &RealtimebiddingBiddersEndpointsListArgs) -> String {
        format!(
            "gcp::realtimebidding::ListEndpointsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::ListEndpointsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Endpoint
// =============================================================================

/// ResourceIdentifier implementation for Endpoint with RealtimebiddingBiddersEndpointsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersEndpointsPatchArgs> for Endpoint {
    fn generate_resource_id(&self, input: &RealtimebiddingBiddersEndpointsPatchArgs) -> String {
        format!("gcp::realtimebidding::Endpoint/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::Endpoint"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PretargetingConfig
// =============================================================================

/// ResourceIdentifier implementation for PretargetingConfig with RealtimebiddingBiddersPretargetingConfigsActivateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPretargetingConfigsActivateArgs>
    for PretargetingConfig
{
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPretargetingConfigsActivateArgs,
    ) -> String {
        format!("gcp::realtimebidding::PretargetingConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::PretargetingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PretargetingConfig
// =============================================================================

/// ResourceIdentifier implementation for PretargetingConfig with RealtimebiddingBiddersPretargetingConfigsAddTargetedAppsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPretargetingConfigsAddTargetedAppsArgs>
    for PretargetingConfig
{
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPretargetingConfigsAddTargetedAppsArgs,
    ) -> String {
        format!(
            "gcp::realtimebidding::PretargetingConfig/{}",
            input.pretargetingConfig
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::PretargetingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PretargetingConfig
// =============================================================================

/// ResourceIdentifier implementation for PretargetingConfig with RealtimebiddingBiddersPretargetingConfigsAddTargetedPublishersArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPretargetingConfigsAddTargetedPublishersArgs>
    for PretargetingConfig
{
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPretargetingConfigsAddTargetedPublishersArgs,
    ) -> String {
        format!(
            "gcp::realtimebidding::PretargetingConfig/{}",
            input.pretargetingConfig
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::PretargetingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PretargetingConfig
// =============================================================================

/// ResourceIdentifier implementation for PretargetingConfig with RealtimebiddingBiddersPretargetingConfigsAddTargetedSitesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPretargetingConfigsAddTargetedSitesArgs>
    for PretargetingConfig
{
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPretargetingConfigsAddTargetedSitesArgs,
    ) -> String {
        format!(
            "gcp::realtimebidding::PretargetingConfig/{}",
            input.pretargetingConfig
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::PretargetingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PretargetingConfig
// =============================================================================

/// ResourceIdentifier implementation for PretargetingConfig with RealtimebiddingBiddersPretargetingConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPretargetingConfigsCreateArgs>
    for PretargetingConfig
{
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPretargetingConfigsCreateArgs,
    ) -> String {
        format!("gcp::realtimebidding::PretargetingConfig/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::PretargetingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with RealtimebiddingBiddersPretargetingConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPretargetingConfigsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPretargetingConfigsDeleteArgs,
    ) -> String {
        format!("gcp::realtimebidding::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PretargetingConfig
// =============================================================================

/// ResourceIdentifier implementation for PretargetingConfig with RealtimebiddingBiddersPretargetingConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPretargetingConfigsGetArgs> for PretargetingConfig {
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPretargetingConfigsGetArgs,
    ) -> String {
        format!("gcp::realtimebidding::PretargetingConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::PretargetingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListPretargetingConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListPretargetingConfigsResponse with RealtimebiddingBiddersPretargetingConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPretargetingConfigsListArgs>
    for ListPretargetingConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPretargetingConfigsListArgs,
    ) -> String {
        format!(
            "gcp::realtimebidding::ListPretargetingConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::ListPretargetingConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PretargetingConfig
// =============================================================================

/// ResourceIdentifier implementation for PretargetingConfig with RealtimebiddingBiddersPretargetingConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPretargetingConfigsPatchArgs> for PretargetingConfig {
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPretargetingConfigsPatchArgs,
    ) -> String {
        format!("gcp::realtimebidding::PretargetingConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::PretargetingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PretargetingConfig
// =============================================================================

/// ResourceIdentifier implementation for PretargetingConfig with RealtimebiddingBiddersPretargetingConfigsRemoveTargetedAppsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPretargetingConfigsRemoveTargetedAppsArgs>
    for PretargetingConfig
{
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPretargetingConfigsRemoveTargetedAppsArgs,
    ) -> String {
        format!(
            "gcp::realtimebidding::PretargetingConfig/{}",
            input.pretargetingConfig
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::PretargetingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PretargetingConfig
// =============================================================================

/// ResourceIdentifier implementation for PretargetingConfig with RealtimebiddingBiddersPretargetingConfigsRemoveTargetedPublishersArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPretargetingConfigsRemoveTargetedPublishersArgs>
    for PretargetingConfig
{
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPretargetingConfigsRemoveTargetedPublishersArgs,
    ) -> String {
        format!(
            "gcp::realtimebidding::PretargetingConfig/{}",
            input.pretargetingConfig
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::PretargetingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PretargetingConfig
// =============================================================================

/// ResourceIdentifier implementation for PretargetingConfig with RealtimebiddingBiddersPretargetingConfigsRemoveTargetedSitesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPretargetingConfigsRemoveTargetedSitesArgs>
    for PretargetingConfig
{
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPretargetingConfigsRemoveTargetedSitesArgs,
    ) -> String {
        format!(
            "gcp::realtimebidding::PretargetingConfig/{}",
            input.pretargetingConfig
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::PretargetingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PretargetingConfig
// =============================================================================

/// ResourceIdentifier implementation for PretargetingConfig with RealtimebiddingBiddersPretargetingConfigsSuspendArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPretargetingConfigsSuspendArgs>
    for PretargetingConfig
{
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPretargetingConfigsSuspendArgs,
    ) -> String {
        format!("gcp::realtimebidding::PretargetingConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::PretargetingConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BatchApprovePublisherConnectionsResponse
// =============================================================================

/// ResourceIdentifier implementation for BatchApprovePublisherConnectionsResponse with RealtimebiddingBiddersPublisherConnectionsBatchApproveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPublisherConnectionsBatchApproveArgs>
    for BatchApprovePublisherConnectionsResponse
{
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPublisherConnectionsBatchApproveArgs,
    ) -> String {
        format!(
            "gcp::realtimebidding::BatchApprovePublisherConnectionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::BatchApprovePublisherConnectionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BatchRejectPublisherConnectionsResponse
// =============================================================================

/// ResourceIdentifier implementation for BatchRejectPublisherConnectionsResponse with RealtimebiddingBiddersPublisherConnectionsBatchRejectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPublisherConnectionsBatchRejectArgs>
    for BatchRejectPublisherConnectionsResponse
{
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPublisherConnectionsBatchRejectArgs,
    ) -> String {
        format!(
            "gcp::realtimebidding::BatchRejectPublisherConnectionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::BatchRejectPublisherConnectionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PublisherConnection
// =============================================================================

/// ResourceIdentifier implementation for PublisherConnection with RealtimebiddingBiddersPublisherConnectionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPublisherConnectionsGetArgs> for PublisherConnection {
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPublisherConnectionsGetArgs,
    ) -> String {
        format!("gcp::realtimebidding::PublisherConnection/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::PublisherConnection"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListPublisherConnectionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListPublisherConnectionsResponse with RealtimebiddingBiddersPublisherConnectionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBiddersPublisherConnectionsListArgs>
    for ListPublisherConnectionsResponse
{
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBiddersPublisherConnectionsListArgs,
    ) -> String {
        format!(
            "gcp::realtimebidding::ListPublisherConnectionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::ListPublisherConnectionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Buyer
// =============================================================================

/// ResourceIdentifier implementation for Buyer with RealtimebiddingBuyersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBuyersGetArgs> for Buyer {
    fn generate_resource_id(&self, input: &RealtimebiddingBuyersGetArgs) -> String {
        format!("gcp::realtimebidding::Buyer/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::Buyer"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetRemarketingTagResponse
// =============================================================================

/// ResourceIdentifier implementation for GetRemarketingTagResponse with RealtimebiddingBuyersGetRemarketingTagArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBuyersGetRemarketingTagArgs> for GetRemarketingTagResponse {
    fn generate_resource_id(&self, input: &RealtimebiddingBuyersGetRemarketingTagArgs) -> String {
        format!(
            "gcp::realtimebidding::GetRemarketingTagResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::GetRemarketingTagResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBuyersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBuyersResponse with RealtimebiddingBuyersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBuyersListArgs> for ListBuyersResponse {
    fn generate_resource_id(&self, input: &RealtimebiddingBuyersListArgs) -> String {
        "gcp::realtimebidding::ListBuyersResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::ListBuyersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Creative
// =============================================================================

/// ResourceIdentifier implementation for Creative with RealtimebiddingBuyersCreativesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBuyersCreativesCreateArgs> for Creative {
    fn generate_resource_id(&self, input: &RealtimebiddingBuyersCreativesCreateArgs) -> String {
        format!("gcp::realtimebidding::Creative/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::Creative"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Creative
// =============================================================================

/// ResourceIdentifier implementation for Creative with RealtimebiddingBuyersCreativesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBuyersCreativesGetArgs> for Creative {
    fn generate_resource_id(&self, input: &RealtimebiddingBuyersCreativesGetArgs) -> String {
        format!("gcp::realtimebidding::Creative/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::Creative"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCreativesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCreativesResponse with RealtimebiddingBuyersCreativesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBuyersCreativesListArgs> for ListCreativesResponse {
    fn generate_resource_id(&self, input: &RealtimebiddingBuyersCreativesListArgs) -> String {
        format!(
            "gcp::realtimebidding::ListCreativesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::ListCreativesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Creative
// =============================================================================

/// ResourceIdentifier implementation for Creative with RealtimebiddingBuyersCreativesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBuyersCreativesPatchArgs> for Creative {
    fn generate_resource_id(&self, input: &RealtimebiddingBuyersCreativesPatchArgs) -> String {
        format!("gcp::realtimebidding::Creative/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::Creative"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserList
// =============================================================================

/// ResourceIdentifier implementation for UserList with RealtimebiddingBuyersUserListsCloseArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBuyersUserListsCloseArgs> for UserList {
    fn generate_resource_id(&self, input: &RealtimebiddingBuyersUserListsCloseArgs) -> String {
        format!("gcp::realtimebidding::UserList/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::UserList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserList
// =============================================================================

/// ResourceIdentifier implementation for UserList with RealtimebiddingBuyersUserListsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBuyersUserListsCreateArgs> for UserList {
    fn generate_resource_id(&self, input: &RealtimebiddingBuyersUserListsCreateArgs) -> String {
        format!("gcp::realtimebidding::UserList/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::UserList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserList
// =============================================================================

/// ResourceIdentifier implementation for UserList with RealtimebiddingBuyersUserListsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBuyersUserListsGetArgs> for UserList {
    fn generate_resource_id(&self, input: &RealtimebiddingBuyersUserListsGetArgs) -> String {
        format!("gcp::realtimebidding::UserList/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::UserList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetRemarketingTagResponse
// =============================================================================

/// ResourceIdentifier implementation for GetRemarketingTagResponse with RealtimebiddingBuyersUserListsGetRemarketingTagArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBuyersUserListsGetRemarketingTagArgs>
    for GetRemarketingTagResponse
{
    fn generate_resource_id(
        &self,
        input: &RealtimebiddingBuyersUserListsGetRemarketingTagArgs,
    ) -> String {
        format!(
            "gcp::realtimebidding::GetRemarketingTagResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::GetRemarketingTagResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListUserListsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListUserListsResponse with RealtimebiddingBuyersUserListsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBuyersUserListsListArgs> for ListUserListsResponse {
    fn generate_resource_id(&self, input: &RealtimebiddingBuyersUserListsListArgs) -> String {
        format!(
            "gcp::realtimebidding::ListUserListsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::ListUserListsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserList
// =============================================================================

/// ResourceIdentifier implementation for UserList with RealtimebiddingBuyersUserListsOpenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBuyersUserListsOpenArgs> for UserList {
    fn generate_resource_id(&self, input: &RealtimebiddingBuyersUserListsOpenArgs) -> String {
        format!("gcp::realtimebidding::UserList/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::UserList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserList
// =============================================================================

/// ResourceIdentifier implementation for UserList with RealtimebiddingBuyersUserListsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RealtimebiddingBuyersUserListsUpdateArgs> for UserList {
    fn generate_resource_id(&self, input: &RealtimebiddingBuyersUserListsUpdateArgs) -> String {
        format!("gcp::realtimebidding::UserList/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::realtimebidding::UserList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
