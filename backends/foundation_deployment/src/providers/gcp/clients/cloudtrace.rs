//! Auto-generated API clients for Google Cloud Platform - cloudtrace.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v2/{+name}/traces:batchWrite
/// Batch writes new spans to new or existing traces. You cannot update existing spans. If a span ID already exists, an additional copy of the span will be stored.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudtrace_projects_traces_batch_write_execute()` to send, or `cloudtrace_projects_traces_batch_write` for simplest API.

pub fn cloudtrace_projects_traces_batch_write_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &BatchWriteSpansRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/traces:batchWrite", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}/traces:batchWrite
/// Batch writes new spans to new or existing traces. You cannot update existing spans. If a span ID already exists, an additional copy of the span will be stored.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudtrace_projects_traces_batch_write_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudtrace_projects_traces_batch_write_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudtrace_projects_traces_batch_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/traces:batchWrite
/// Batch writes new spans to new or existing traces. You cannot update existing spans. If a span ID already exists, an additional copy of the span will be stored.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudtrace_projects_traces_batch_write_builder()` + `cloudtrace_projects_traces_batch_write_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudtrace_projects_traces_batch_write(
    client: &SimpleHttpClient,
    name: &str,
    body: &BatchWriteSpansRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudtrace_projects_traces_batch_write_builder(client, name, body)?;
    cloudtrace_projects_traces_batch_write_execute(builder)
}

/// GET v2/{+name}
/// Creates a new span. If a span ID already exists, an additional copy of the span will be stored.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudtrace_projects_traces_spans_create_span_execute()` to send, or `cloudtrace_projects_traces_spans_create_span` for simplest API.

pub fn cloudtrace_projects_traces_spans_create_span_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &Span,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Creates a new span. If a span ID already exists, an additional copy of the span will be stored.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudtrace_projects_traces_spans_create_span_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudtrace_projects_traces_spans_create_span_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudtrace_projects_traces_spans_create_span_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Span>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Span = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Creates a new span. If a span ID already exists, an additional copy of the span will be stored.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudtrace_projects_traces_spans_create_span_builder()` + `cloudtrace_projects_traces_spans_create_span_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudtrace_projects_traces_spans_create_span(
    client: &SimpleHttpClient,
    name: &str,
    body: &Span,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Span>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudtrace_projects_traces_spans_create_span_builder(client, name, body)?;
    cloudtrace_projects_traces_spans_create_span_execute(builder)
}
