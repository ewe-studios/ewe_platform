//! Auto-generated API clients for gcp/youtube.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET youtube/v3/captions/{id}
/// Downloads a caption track.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_captions_download_execute()` to send, or `youtube_captions_download` for simplest API.

pub fn youtube_captions_download_builder(
    client: &SimpleHttpClient,
    id: String,
    onBehalfOf: Option<String>,
    onBehalfOfContentOwner: Option<String>,
    tfmt: Option<String>,
    tlang: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://youtube.googleapis.com/youtube/v3/captions/{}",
        id.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = onBehalfOf {
        query_parts.push(format!("onBehalfOf={}", val));
    }
    if let Some(val) = onBehalfOfContentOwner {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }
    if let Some(val) = tfmt {
        query_parts.push(format!("tfmt={}", val));
    }
    if let Some(val) = tlang {
        query_parts.push(format!("tlang={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET youtube/v3/captions/{id}
/// Downloads a caption track.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtube_captions_download_execute()` or `youtube_captions_download`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_captions_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_captions_download_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET youtube/v3/captions/{id}
/// Downloads a caption track.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtube_captions_download_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtube_captions_download_task()`.
/// For the simplest API, use `youtube_captions_download()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_captions_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtube_captions_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = youtube_captions_download_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtube_captions_download`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubeCaptionsDownloadArgs {
    /// Path parameter: id
    pub id: String,
    /// Query parameter: onBehalfOf
    pub onBehalfOf: Option<String>,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<String>,
    /// Query parameter: tfmt
    pub tfmt: Option<String>,
    /// Query parameter: tlang
    pub tlang: Option<String>,
}

/// GET youtube/v3/captions/{id}
/// Downloads a caption track.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_captions_download_builder()` + `youtube_captions_download_execute()`.
/// For task-level control, use `youtube_captions_download_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_captions_download(
    client: &SimpleHttpClient,
    args: &YoutubeCaptionsDownloadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_captions_download_builder(
        client,
        args.id.clone(),
        args.onBehalfOf.clone(),
        args.onBehalfOfContentOwner.clone(),
        args.tfmt.clone(),
        args.tlang.clone(),
    )?;
    youtube_captions_download_execute(builder)
}

/// GET youtube/v3/channelBanners/insert
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_channel_banners_insert_execute()` to send, or `youtube_channel_banners_insert` for simplest API.

pub fn youtube_channel_banners_insert_builder(
    client: &SimpleHttpClient,
    channelId: Option<String>,
    onBehalfOfContentOwner: Option<String>,
    onBehalfOfContentOwnerChannel: Option<String>,
    body: &ChannelBannerResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/channelBanners/insert",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = channelId {
        query_parts.push(format!("channelId={}", val));
    }
    if let Some(val) = onBehalfOfContentOwner {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }
    if let Some(val) = onBehalfOfContentOwnerChannel {
        query_parts.push(format!("onBehalfOfContentOwnerChannel={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/channelBanners/insert
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtube_channel_banners_insert_execute()` or `youtube_channel_banners_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_channel_banners_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channel_banners_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ChannelBannerResource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ChannelBannerResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET youtube/v3/channelBanners/insert
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtube_channel_banners_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtube_channel_banners_insert_task()`.
/// For the simplest API, use `youtube_channel_banners_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_channel_banners_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtube_channel_banners_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChannelBannerResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = youtube_channel_banners_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtube_channel_banners_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubeChannelBannersInsertArgs {
    /// Query parameter: channelId
    pub channelId: Option<String>,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<String>,
    /// Query parameter: onBehalfOfContentOwnerChannel
    pub onBehalfOfContentOwnerChannel: Option<String>,
    /// Request body.
    pub body: ChannelBannerResource,
}

/// GET youtube/v3/channelBanners/insert
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_channel_banners_insert_builder()` + `youtube_channel_banners_insert_execute()`.
/// For task-level control, use `youtube_channel_banners_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channel_banners_insert(
    client: &SimpleHttpClient,
    args: &YoutubeChannelBannersInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChannelBannerResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_channel_banners_insert_builder(
        client,
        args.channelId.clone(),
        args.onBehalfOfContentOwner.clone(),
        args.onBehalfOfContentOwnerChannel.clone(),
        &args.body,
    )?;
    youtube_channel_banners_insert_execute(builder)
}

/// GET youtube/v3/liveBroadcasts/cuepoint
/// Insert cuepoints in a broadcast
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_broadcasts_insert_cuepoint_execute()` to send, or `youtube_live_broadcasts_insert_cuepoint` for simplest API.

pub fn youtube_live_broadcasts_insert_cuepoint_builder(
    client: &SimpleHttpClient,
    id: Option<String>,
    onBehalfOfContentOwner: Option<String>,
    onBehalfOfContentOwnerChannel: Option<String>,
    part: Option<String>,
    body: &Cuepoint,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://youtube.googleapis.com/youtube/v3/liveBroadcasts/cuepoint",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id {
        query_parts.push(format!("id={}", val));
    }
    if let Some(val) = onBehalfOfContentOwner {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }
    if let Some(val) = onBehalfOfContentOwnerChannel {
        query_parts.push(format!("onBehalfOfContentOwnerChannel={}", val));
    }
    if let Some(val) = part {
        query_parts.push(format!("part={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveBroadcasts/cuepoint
/// Insert cuepoints in a broadcast
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtube_live_broadcasts_insert_cuepoint_execute()` or `youtube_live_broadcasts_insert_cuepoint`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_live_broadcasts_insert_cuepoint_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_insert_cuepoint_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Cuepoint>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Cuepoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET youtube/v3/liveBroadcasts/cuepoint
/// Insert cuepoints in a broadcast
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtube_live_broadcasts_insert_cuepoint_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtube_live_broadcasts_insert_cuepoint_task()`.
/// For the simplest API, use `youtube_live_broadcasts_insert_cuepoint()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_live_broadcasts_insert_cuepoint_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtube_live_broadcasts_insert_cuepoint_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cuepoint>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = youtube_live_broadcasts_insert_cuepoint_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtube_live_broadcasts_insert_cuepoint`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubeLiveBroadcastsInsertCuepointArgs {
    /// Query parameter: id
    pub id: Option<String>,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<String>,
    /// Query parameter: onBehalfOfContentOwnerChannel
    pub onBehalfOfContentOwnerChannel: Option<String>,
    /// Query parameter: part
    pub part: Option<String>,
    /// Request body.
    pub body: Cuepoint,
}

/// GET youtube/v3/liveBroadcasts/cuepoint
/// Insert cuepoints in a broadcast
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_broadcasts_insert_cuepoint_builder()` + `youtube_live_broadcasts_insert_cuepoint_execute()`.
/// For task-level control, use `youtube_live_broadcasts_insert_cuepoint_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_insert_cuepoint(
    client: &SimpleHttpClient,
    args: &YoutubeLiveBroadcastsInsertCuepointArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cuepoint>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_live_broadcasts_insert_cuepoint_builder(
        client,
        args.id.clone(),
        args.onBehalfOfContentOwner.clone(),
        args.onBehalfOfContentOwnerChannel.clone(),
        args.part.clone(),
        &args.body,
    )?;
    youtube_live_broadcasts_insert_cuepoint_execute(builder)
}

/// GET youtube/v3/liveChat/messages/transition
/// Transition a durable chat event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_chat_messages_transition_execute()` to send, or `youtube_live_chat_messages_transition` for simplest API.

pub fn youtube_live_chat_messages_transition_builder(
    client: &SimpleHttpClient,
    id: Option<String>,
    status: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://youtube.googleapis.com/youtube/v3/liveChat/messages/transition",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id {
        query_parts.push(format!("id={}", val));
    }
    if let Some(val) = status {
        query_parts.push(format!("status={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET youtube/v3/liveChat/messages/transition
/// Transition a durable chat event.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtube_live_chat_messages_transition_execute()` or `youtube_live_chat_messages_transition`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_live_chat_messages_transition_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_messages_transition_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveChatMessage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveChatMessage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET youtube/v3/liveChat/messages/transition
/// Transition a durable chat event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtube_live_chat_messages_transition_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtube_live_chat_messages_transition_task()`.
/// For the simplest API, use `youtube_live_chat_messages_transition()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_live_chat_messages_transition_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtube_live_chat_messages_transition_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveChatMessage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = youtube_live_chat_messages_transition_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtube_live_chat_messages_transition`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubeLiveChatMessagesTransitionArgs {
    /// Query parameter: id
    pub id: Option<String>,
    /// Query parameter: status
    pub status: Option<String>,
}

/// GET youtube/v3/liveChat/messages/transition
/// Transition a durable chat event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_chat_messages_transition_builder()` + `youtube_live_chat_messages_transition_execute()`.
/// For task-level control, use `youtube_live_chat_messages_transition_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_messages_transition(
    client: &SimpleHttpClient,
    args: &YoutubeLiveChatMessagesTransitionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveChatMessage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_live_chat_messages_transition_builder(
        client,
        args.id.clone(),
        args.status.clone(),
    )?;
    youtube_live_chat_messages_transition_execute(builder)
}

/// GET youtube/v3/playlistImages
/// Deletes a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_playlist_images_delete_execute()` to send, or `youtube_playlist_images_delete` for simplest API.

pub fn youtube_playlist_images_delete_builder(
    client: &SimpleHttpClient,
    id: Option<String>,
    onBehalfOfContentOwner: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/playlistImages",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id {
        query_parts.push(format!("id={}", val));
    }
    if let Some(val) = onBehalfOfContentOwner {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET youtube/v3/playlistImages
/// Deletes a resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtube_playlist_images_delete_execute()` or `youtube_playlist_images_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_playlist_images_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_images_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET youtube/v3/playlistImages
/// Deletes a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtube_playlist_images_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtube_playlist_images_delete_task()`.
/// For the simplest API, use `youtube_playlist_images_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_playlist_images_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtube_playlist_images_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = youtube_playlist_images_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtube_playlist_images_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubePlaylistImagesDeleteArgs {
    /// Query parameter: id
    pub id: Option<String>,
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<String>,
}

/// GET youtube/v3/playlistImages
/// Deletes a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_playlist_images_delete_builder()` + `youtube_playlist_images_delete_execute()`.
/// For task-level control, use `youtube_playlist_images_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_images_delete(
    client: &SimpleHttpClient,
    args: &YoutubePlaylistImagesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_playlist_images_delete_builder(
        client,
        args.id.clone(),
        args.onBehalfOfContentOwner.clone(),
    )?;
    youtube_playlist_images_delete_execute(builder)
}

/// GET youtube/v3/videoTrainability
/// Returns the trainability status of a video.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_video_trainability_get_execute()` to send, or `youtube_video_trainability_get` for simplest API.

pub fn youtube_video_trainability_get_builder(
    client: &SimpleHttpClient,
    id: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/videoTrainability",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = id {
        query_parts.push(format!("id={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET youtube/v3/videoTrainability
/// Returns the trainability status of a video.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtube_video_trainability_get_execute()` or `youtube_video_trainability_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_video_trainability_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_video_trainability_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VideoTrainability>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VideoTrainability = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET youtube/v3/videoTrainability
/// Returns the trainability status of a video.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtube_video_trainability_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtube_video_trainability_get_task()`.
/// For the simplest API, use `youtube_video_trainability_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_video_trainability_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtube_video_trainability_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VideoTrainability>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = youtube_video_trainability_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtube_video_trainability_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubeVideoTrainabilityGetArgs {
    /// Query parameter: id
    pub id: Option<String>,
}

/// GET youtube/v3/videoTrainability
/// Returns the trainability status of a video.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_video_trainability_get_builder()` + `youtube_video_trainability_get_execute()`.
/// For task-level control, use `youtube_video_trainability_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_video_trainability_get(
    client: &SimpleHttpClient,
    args: &YoutubeVideoTrainabilityGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VideoTrainability>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_video_trainability_get_builder(client, args.id.clone())?;
    youtube_video_trainability_get_execute(builder)
}

/// GET youtube/v3/videos/reportAbuse
/// Report abuse for a video.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_videos_report_abuse_execute()` to send, or `youtube_videos_report_abuse` for simplest API.

pub fn youtube_videos_report_abuse_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<String>,
    body: &VideoAbuseReport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/videos/reportAbuse",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = onBehalfOfContentOwner {
        query_parts.push(format!("onBehalfOfContentOwner={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/videos/reportAbuse
/// Report abuse for a video.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtube_videos_report_abuse_execute()` or `youtube_videos_report_abuse`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_videos_report_abuse_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_report_abuse_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET youtube/v3/videos/reportAbuse
/// Report abuse for a video.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtube_videos_report_abuse_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtube_videos_report_abuse_task()`.
/// For the simplest API, use `youtube_videos_report_abuse()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_videos_report_abuse_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtube_videos_report_abuse_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = youtube_videos_report_abuse_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtube_videos_report_abuse`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubeVideosReportAbuseArgs {
    /// Query parameter: onBehalfOfContentOwner
    pub onBehalfOfContentOwner: Option<String>,
    /// Request body.
    pub body: VideoAbuseReport,
}

/// GET youtube/v3/videos/reportAbuse
/// Report abuse for a video.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_videos_report_abuse_builder()` + `youtube_videos_report_abuse_execute()`.
/// For task-level control, use `youtube_videos_report_abuse_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_report_abuse(
    client: &SimpleHttpClient,
    args: &YoutubeVideosReportAbuseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_videos_report_abuse_builder(
        client,
        args.onBehalfOfContentOwner.clone(),
        &args.body,
    )?;
    youtube_videos_report_abuse_execute(builder)
}

/// GET youtube/v3/commentThreads
/// Updates an existing resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_youtube_v3_update_comment_threads_execute()` to send, or `youtube_youtube_v3_update_comment_threads` for simplest API.

pub fn youtube_youtube_v3_update_comment_threads_builder(
    client: &SimpleHttpClient,
    part: Option<String>,
    body: &CommentThread,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/commentThreads",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = part {
        query_parts.push(format!("part={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/commentThreads
/// Updates an existing resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtube_youtube_v3_update_comment_threads_execute()` or `youtube_youtube_v3_update_comment_threads`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_youtube_v3_update_comment_threads_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_youtube_v3_update_comment_threads_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommentThread>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommentThread = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET youtube/v3/commentThreads
/// Updates an existing resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtube_youtube_v3_update_comment_threads_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtube_youtube_v3_update_comment_threads_task()`.
/// For the simplest API, use `youtube_youtube_v3_update_comment_threads()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_youtube_v3_update_comment_threads_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtube_youtube_v3_update_comment_threads_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentThread>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = youtube_youtube_v3_update_comment_threads_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtube_youtube_v3_update_comment_threads`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubeYoutubeV3UpdateCommentThreadsArgs {
    /// Query parameter: part
    pub part: Option<String>,
    /// Request body.
    pub body: CommentThread,
}

/// GET youtube/v3/commentThreads
/// Updates an existing resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_youtube_v3_update_comment_threads_builder()` + `youtube_youtube_v3_update_comment_threads_execute()`.
/// For task-level control, use `youtube_youtube_v3_update_comment_threads_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_youtube_v3_update_comment_threads(
    client: &SimpleHttpClient,
    args: &YoutubeYoutubeV3UpdateCommentThreadsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentThread>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        youtube_youtube_v3_update_comment_threads_builder(client, args.part.clone(), &args.body)?;
    youtube_youtube_v3_update_comment_threads_execute(builder)
}

/// GET youtube/v3/liveChat/messages/stream
/// Allows a user to load live chat through a server-streamed RPC.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_youtube_v3_live_chat_messages_stream_execute()` to send, or `youtube_youtube_v3_live_chat_messages_stream` for simplest API.

pub fn youtube_youtube_v3_live_chat_messages_stream_builder(
    client: &SimpleHttpClient,
    hl: Option<String>,
    liveChatId: Option<String>,
    maxResults: Option<i32>,
    pageToken: Option<String>,
    part: Option<String>,
    profileImageSize: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://youtube.googleapis.com/youtube/v3/liveChat/messages/stream",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = hl {
        query_parts.push(format!("hl={}", val));
    }
    if let Some(val) = liveChatId {
        query_parts.push(format!("liveChatId={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = part {
        query_parts.push(format!("part={}", val));
    }
    if let Some(val) = profileImageSize {
        query_parts.push(format!("profileImageSize={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET youtube/v3/liveChat/messages/stream
/// Allows a user to load live chat through a server-streamed RPC.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `youtube_youtube_v3_live_chat_messages_stream_execute()` or `youtube_youtube_v3_live_chat_messages_stream`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_youtube_v3_live_chat_messages_stream_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_youtube_v3_live_chat_messages_stream_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveChatMessageListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveChatMessageListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET youtube/v3/liveChat/messages/stream
/// Allows a user to load live chat through a server-streamed RPC.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `youtube_youtube_v3_live_chat_messages_stream_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `youtube_youtube_v3_live_chat_messages_stream_task()`.
/// For the simplest API, use `youtube_youtube_v3_live_chat_messages_stream()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `youtube_youtube_v3_live_chat_messages_stream_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn youtube_youtube_v3_live_chat_messages_stream_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiveChatMessageListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = youtube_youtube_v3_live_chat_messages_stream_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`youtube_youtube_v3_live_chat_messages_stream`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct YoutubeYoutubeV3LiveChatMessagesStreamArgs {
    /// Query parameter: hl
    pub hl: Option<String>,
    /// Query parameter: liveChatId
    pub liveChatId: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: part
    pub part: Option<String>,
    /// Query parameter: profileImageSize
    pub profileImageSize: Option<i32>,
}

/// GET youtube/v3/liveChat/messages/stream
/// Allows a user to load live chat through a server-streamed RPC.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_youtube_v3_live_chat_messages_stream_builder()` + `youtube_youtube_v3_live_chat_messages_stream_execute()`.
/// For task-level control, use `youtube_youtube_v3_live_chat_messages_stream_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_youtube_v3_live_chat_messages_stream(
    client: &SimpleHttpClient,
    args: &YoutubeYoutubeV3LiveChatMessagesStreamArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiveChatMessageListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_youtube_v3_live_chat_messages_stream_builder(
        client,
        args.hl.clone(),
        args.liveChatId.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
        args.part.clone(),
        args.profileImageSize.clone(),
    )?;
    youtube_youtube_v3_live_chat_messages_stream_execute(builder)
}
