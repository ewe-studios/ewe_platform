//! Auto-generated API clients for Google Cloud Platform - youtube.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET youtube/v3/abuseReports
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_abuse_reports_insert_execute()` to send, or `youtube_abuse_reports_insert` for simplest API.

pub fn youtube_abuse_reports_insert_builder(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &AbuseReport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/abuseReports",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/abuseReports
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_abuse_reports_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_abuse_reports_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_abuse_reports_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AbuseReport>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AbuseReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/abuseReports
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_abuse_reports_insert_builder()` + `youtube_abuse_reports_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_abuse_reports_insert(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &AbuseReport,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AbuseReport>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_abuse_reports_insert_builder(client, part, body)?;
    youtube_abuse_reports_insert_execute(builder)
}

/// GET youtube/v3/activities
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_activities_list_execute()` to send, or `youtube_activities_list` for simplest API.

pub fn youtube_activities_list_builder(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    home: Option<bool>,
    maxResults: Option<i32>,
    mine: Option<bool>,
    pageToken: Option<&str>,
    part: Option<&str>,
    publishedAfter: Option<&str>,
    publishedBefore: Option<&str>,
    regionCode: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/activities",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "channelId",
            channelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "home",
            home.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "mine",
            mine.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "publishedAfter",
            publishedAfter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "publishedBefore",
            publishedBefore
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "regionCode",
            regionCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/activities
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_activities_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_activities_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_activities_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ActivityListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ActivityListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/activities
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_activities_list_builder()` + `youtube_activities_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_activities_list(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    home: Option<bool>,
    maxResults: Option<i32>,
    mine: Option<bool>,
    pageToken: Option<&str>,
    part: Option<&str>,
    publishedAfter: Option<&str>,
    publishedBefore: Option<&str>,
    regionCode: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ActivityListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_activities_list_builder(
        client,
        channelId,
        home,
        maxResults,
        mine,
        pageToken,
        part,
        publishedAfter,
        publishedBefore,
        regionCode,
    )?;
    youtube_activities_list_execute(builder)
}

/// GET youtube/v3/captions
/// Deletes a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_captions_delete_execute()` to send, or `youtube_captions_delete` for simplest API.

pub fn youtube_captions_delete_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOf: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/captions",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOf",
            onBehalfOf
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/captions
/// Deletes a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_captions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_captions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_captions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/captions
/// Deletes a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_captions_delete_builder()` + `youtube_captions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_captions_delete(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOf: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_captions_delete_builder(client, id, onBehalfOf, onBehalfOfContentOwner)?;
    youtube_captions_delete_execute(builder)
}

/// GET youtube/v3/captions/{id}
/// Downloads a caption track.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_captions_download_execute()` to send, or `youtube_captions_download` for simplest API.

pub fn youtube_captions_download_builder(
    client: &SimpleHttpClient,
    id: &str,
    onBehalfOf: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    tfmt: Option<&str>,
    tlang: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/captions/{}", id,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOf",
            onBehalfOf
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "tfmt",
            tfmt.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "tlang",
            tlang
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/captions/{id}
/// Downloads a caption track.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_captions_download_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_captions_download_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_captions_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/captions/{id}
/// Downloads a caption track.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_captions_download_builder()` + `youtube_captions_download_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_captions_download(
    client: &SimpleHttpClient,
    id: &str,
    onBehalfOf: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    tfmt: Option<&str>,
    tlang: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_captions_download_builder(
        client,
        id,
        onBehalfOf,
        onBehalfOfContentOwner,
        tfmt,
        tlang,
    )?;
    youtube_captions_download_execute(builder)
}

/// GET youtube/v3/captions
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_captions_insert_execute()` to send, or `youtube_captions_insert` for simplest API.

pub fn youtube_captions_insert_builder(
    client: &SimpleHttpClient,
    onBehalfOf: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    sync: Option<bool>,
    body: &Caption,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/captions",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOf",
            onBehalfOf
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sync",
            sync.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/captions
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_captions_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_captions_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_captions_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Caption>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Caption = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/captions
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_captions_insert_builder()` + `youtube_captions_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_captions_insert(
    client: &SimpleHttpClient,
    onBehalfOf: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    sync: Option<bool>,
    body: &Caption,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Caption>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_captions_insert_builder(
        client,
        onBehalfOf,
        onBehalfOfContentOwner,
        part,
        sync,
        body,
    )?;
    youtube_captions_insert_execute(builder)
}

/// GET youtube/v3/captions
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_captions_list_execute()` to send, or `youtube_captions_list` for simplest API.

pub fn youtube_captions_list_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOf: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    videoId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/captions",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOf",
            onBehalfOf
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoId",
            videoId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/captions
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_captions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_captions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_captions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CaptionListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CaptionListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/captions
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_captions_list_builder()` + `youtube_captions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_captions_list(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOf: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    videoId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CaptionListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_captions_list_builder(
        client,
        id,
        onBehalfOf,
        onBehalfOfContentOwner,
        part,
        videoId,
    )?;
    youtube_captions_list_execute(builder)
}

/// GET youtube/v3/captions
/// Updates an existing resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_captions_update_execute()` to send, or `youtube_captions_update` for simplest API.

pub fn youtube_captions_update_builder(
    client: &SimpleHttpClient,
    onBehalfOf: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    sync: Option<bool>,
    body: &Caption,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/captions",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOf",
            onBehalfOf
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sync",
            sync.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/captions
/// Updates an existing resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_captions_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_captions_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_captions_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Caption>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Caption = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/captions
/// Updates an existing resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_captions_update_builder()` + `youtube_captions_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_captions_update(
    client: &SimpleHttpClient,
    onBehalfOf: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    sync: Option<bool>,
    body: &Caption,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Caption>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_captions_update_builder(
        client,
        onBehalfOf,
        onBehalfOfContentOwner,
        part,
        sync,
        body,
    )?;
    youtube_captions_update_execute(builder)
}

/// GET youtube/v3/channelBanners/insert
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_channel_banners_insert_execute()` to send, or `youtube_channel_banners_insert` for simplest API.

pub fn youtube_channel_banners_insert_builder(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    body: &ChannelBannerResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/channelBanners/insert",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "channelId",
            channelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/channelBanners/insert
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_channel_banners_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_channel_banners_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channel_banners_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChannelBannerResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ChannelBannerResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/channelBanners/insert
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_channel_banners_insert_builder()` + `youtube_channel_banners_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channel_banners_insert(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    body: &ChannelBannerResource,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChannelBannerResource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_channel_banners_insert_builder(
        client,
        channelId,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        body,
    )?;
    youtube_channel_banners_insert_execute(builder)
}

/// GET youtube/v3/channelSections
/// Deletes a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_channel_sections_delete_execute()` to send, or `youtube_channel_sections_delete` for simplest API.

pub fn youtube_channel_sections_delete_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/channelSections",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/channelSections
/// Deletes a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_channel_sections_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_channel_sections_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channel_sections_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/channelSections
/// Deletes a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_channel_sections_delete_builder()` + `youtube_channel_sections_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channel_sections_delete(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_channel_sections_delete_builder(client, id, onBehalfOfContentOwner)?;
    youtube_channel_sections_delete_execute(builder)
}

/// GET youtube/v3/channelSections
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_channel_sections_insert_execute()` to send, or `youtube_channel_sections_insert` for simplest API.

pub fn youtube_channel_sections_insert_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &ChannelSection,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/channelSections",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/channelSections
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_channel_sections_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_channel_sections_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channel_sections_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChannelSection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ChannelSection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/channelSections
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_channel_sections_insert_builder()` + `youtube_channel_sections_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channel_sections_insert(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &ChannelSection,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChannelSection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_channel_sections_insert_builder(
        client,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        part,
        body,
    )?;
    youtube_channel_sections_insert_execute(builder)
}

/// GET youtube/v3/channelSections
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_channel_sections_list_execute()` to send, or `youtube_channel_sections_list` for simplest API.

pub fn youtube_channel_sections_list_builder(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    hl: Option<&str>,
    id: Option<&str>,
    mine: Option<bool>,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/channelSections",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "channelId",
            channelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "hl",
            hl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "mine",
            mine.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/channelSections
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_channel_sections_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_channel_sections_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channel_sections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ChannelSectionListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ChannelSectionListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/channelSections
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_channel_sections_list_builder()` + `youtube_channel_sections_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channel_sections_list(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    hl: Option<&str>,
    id: Option<&str>,
    mine: Option<bool>,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ChannelSectionListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_channel_sections_list_builder(
        client,
        channelId,
        hl,
        id,
        mine,
        onBehalfOfContentOwner,
        part,
    )?;
    youtube_channel_sections_list_execute(builder)
}

/// GET youtube/v3/channelSections
/// Updates an existing resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_channel_sections_update_execute()` to send, or `youtube_channel_sections_update` for simplest API.

pub fn youtube_channel_sections_update_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    body: &ChannelSection,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/channelSections",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/channelSections
/// Updates an existing resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_channel_sections_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_channel_sections_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channel_sections_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChannelSection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ChannelSection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/channelSections
/// Updates an existing resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_channel_sections_update_builder()` + `youtube_channel_sections_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channel_sections_update(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    body: &ChannelSection,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChannelSection>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        youtube_channel_sections_update_builder(client, onBehalfOfContentOwner, part, body)?;
    youtube_channel_sections_update_execute(builder)
}

/// GET youtube/v3/channels
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_channels_list_execute()` to send, or `youtube_channels_list` for simplest API.

pub fn youtube_channels_list_builder(
    client: &SimpleHttpClient,
    categoryId: Option<&str>,
    forHandle: Option<&str>,
    forUsername: Option<&str>,
    hl: Option<&str>,
    id: Option<&str>,
    managedByMe: Option<bool>,
    maxResults: Option<i32>,
    mine: Option<bool>,
    mySubscribers: Option<bool>,
    onBehalfOfContentOwner: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/channels",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "categoryId",
            categoryId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "forHandle",
            forHandle
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "forUsername",
            forUsername
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "hl",
            hl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "managedByMe",
            managedByMe
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "mine",
            mine.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "mySubscribers",
            mySubscribers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/channels
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_channels_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_channels_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChannelListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ChannelListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/channels
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_channels_list_builder()` + `youtube_channels_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channels_list(
    client: &SimpleHttpClient,
    categoryId: Option<&str>,
    forHandle: Option<&str>,
    forUsername: Option<&str>,
    hl: Option<&str>,
    id: Option<&str>,
    managedByMe: Option<bool>,
    maxResults: Option<i32>,
    mine: Option<bool>,
    mySubscribers: Option<bool>,
    onBehalfOfContentOwner: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChannelListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_channels_list_builder(
        client,
        categoryId,
        forHandle,
        forUsername,
        hl,
        id,
        managedByMe,
        maxResults,
        mine,
        mySubscribers,
        onBehalfOfContentOwner,
        pageToken,
        part,
    )?;
    youtube_channels_list_execute(builder)
}

/// GET youtube/v3/channels
/// Updates an existing resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_channels_update_execute()` to send, or `youtube_channels_update` for simplest API.

pub fn youtube_channels_update_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/channels",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/channels
/// Updates an existing resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_channels_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_channels_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channels_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/channels
/// Updates an existing resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_channels_update_builder()` + `youtube_channels_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_channels_update(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    body: &Channel,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_channels_update_builder(client, onBehalfOfContentOwner, part, body)?;
    youtube_channels_update_execute(builder)
}

/// GET youtube/v3/commentThreads
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_comment_threads_insert_execute()` to send, or `youtube_comment_threads_insert` for simplest API.

pub fn youtube_comment_threads_insert_builder(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &CommentThread,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/commentThreads",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/commentThreads
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_comment_threads_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_comment_threads_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comment_threads_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentThread>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommentThread = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/commentThreads
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_comment_threads_insert_builder()` + `youtube_comment_threads_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comment_threads_insert(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &CommentThread,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentThread>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_comment_threads_insert_builder(client, part, body)?;
    youtube_comment_threads_insert_execute(builder)
}

/// GET youtube/v3/commentThreads
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_comment_threads_list_execute()` to send, or `youtube_comment_threads_list` for simplest API.

pub fn youtube_comment_threads_list_builder(
    client: &SimpleHttpClient,
    allThreadsRelatedToChannelId: Option<&str>,
    channelId: Option<&str>,
    id: Option<&str>,
    maxResults: Option<i32>,
    moderationStatus: Option<&str>,
    order: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
    postId: Option<&str>,
    searchTerms: Option<&str>,
    textFormat: Option<&str>,
    videoId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/commentThreads",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "allThreadsRelatedToChannelId",
            allThreadsRelatedToChannelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "channelId",
            channelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "moderationStatus",
            moderationStatus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "order",
            order
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "postId",
            postId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "searchTerms",
            searchTerms
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "textFormat",
            textFormat
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoId",
            videoId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/commentThreads
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_comment_threads_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_comment_threads_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comment_threads_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentThreadListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommentThreadListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/commentThreads
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_comment_threads_list_builder()` + `youtube_comment_threads_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comment_threads_list(
    client: &SimpleHttpClient,
    allThreadsRelatedToChannelId: Option<&str>,
    channelId: Option<&str>,
    id: Option<&str>,
    maxResults: Option<i32>,
    moderationStatus: Option<&str>,
    order: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
    postId: Option<&str>,
    searchTerms: Option<&str>,
    textFormat: Option<&str>,
    videoId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentThreadListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_comment_threads_list_builder(
        client,
        allThreadsRelatedToChannelId,
        channelId,
        id,
        maxResults,
        moderationStatus,
        order,
        pageToken,
        part,
        postId,
        searchTerms,
        textFormat,
        videoId,
    )?;
    youtube_comment_threads_list_execute(builder)
}

/// GET youtube/v3/comments
/// Deletes a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_comments_delete_execute()` to send, or `youtube_comments_delete` for simplest API.

pub fn youtube_comments_delete_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/comments",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/comments
/// Deletes a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_comments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_comments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/comments
/// Deletes a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_comments_delete_builder()` + `youtube_comments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comments_delete(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_comments_delete_builder(client, id)?;
    youtube_comments_delete_execute(builder)
}

/// GET youtube/v3/comments
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_comments_insert_execute()` to send, or `youtube_comments_insert` for simplest API.

pub fn youtube_comments_insert_builder(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &Comment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/comments",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/comments
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_comments_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_comments_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comments_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Comment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/comments
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_comments_insert_builder()` + `youtube_comments_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comments_insert(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &Comment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_comments_insert_builder(client, part, body)?;
    youtube_comments_insert_execute(builder)
}

/// GET youtube/v3/comments
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_comments_list_execute()` to send, or `youtube_comments_list` for simplest API.

pub fn youtube_comments_list_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    parentId: Option<&str>,
    part: Option<&str>,
    textFormat: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/comments",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parentId",
            parentId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "textFormat",
            textFormat
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/comments
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_comments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_comments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommentListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/comments
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_comments_list_builder()` + `youtube_comments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comments_list(
    client: &SimpleHttpClient,
    id: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    parentId: Option<&str>,
    part: Option<&str>,
    textFormat: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_comments_list_builder(
        client, id, maxResults, pageToken, parentId, part, textFormat,
    )?;
    youtube_comments_list_execute(builder)
}

/// GET youtube/v3/comments/markAsSpam
/// Expresses the caller's opinion that one or more comments should be flagged as spam.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_comments_mark_as_spam_execute()` to send, or `youtube_comments_mark_as_spam` for simplest API.

pub fn youtube_comments_mark_as_spam_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/comments/markAsSpam",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/comments/markAsSpam
/// Expresses the caller's opinion that one or more comments should be flagged as spam.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_comments_mark_as_spam_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_comments_mark_as_spam_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comments_mark_as_spam_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/comments/markAsSpam
/// Expresses the caller's opinion that one or more comments should be flagged as spam.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_comments_mark_as_spam_builder()` + `youtube_comments_mark_as_spam_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comments_mark_as_spam(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_comments_mark_as_spam_builder(client, id)?;
    youtube_comments_mark_as_spam_execute(builder)
}

/// GET youtube/v3/comments/setModerationStatus
/// Sets the moderation status of one or more comments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_comments_set_moderation_status_execute()` to send, or `youtube_comments_set_moderation_status` for simplest API.

pub fn youtube_comments_set_moderation_status_builder(
    client: &SimpleHttpClient,
    banAuthor: Option<bool>,
    id: Option<&str>,
    moderationStatus: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/comments/setModerationStatus",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "banAuthor",
            banAuthor
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "moderationStatus",
            moderationStatus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/comments/setModerationStatus
/// Sets the moderation status of one or more comments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_comments_set_moderation_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_comments_set_moderation_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comments_set_moderation_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/comments/setModerationStatus
/// Sets the moderation status of one or more comments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_comments_set_moderation_status_builder()` + `youtube_comments_set_moderation_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comments_set_moderation_status(
    client: &SimpleHttpClient,
    banAuthor: Option<bool>,
    id: Option<&str>,
    moderationStatus: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        youtube_comments_set_moderation_status_builder(client, banAuthor, id, moderationStatus)?;
    youtube_comments_set_moderation_status_execute(builder)
}

/// GET youtube/v3/comments
/// Updates an existing resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_comments_update_execute()` to send, or `youtube_comments_update` for simplest API.

pub fn youtube_comments_update_builder(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &Comment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/comments",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/comments
/// Updates an existing resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_comments_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_comments_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comments_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Comment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/comments
/// Updates an existing resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_comments_update_builder()` + `youtube_comments_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_comments_update(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &Comment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_comments_update_builder(client, part, body)?;
    youtube_comments_update_execute(builder)
}

/// GET youtube/v3/i18nLanguages
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_i18n_languages_list_execute()` to send, or `youtube_i18n_languages_list` for simplest API.

pub fn youtube_i18n_languages_list_builder(
    client: &SimpleHttpClient,
    hl: Option<&str>,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/i18nLanguages",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "hl",
            hl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/i18nLanguages
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_i18n_languages_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_i18n_languages_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_i18n_languages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<I18nLanguageListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: I18nLanguageListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/i18nLanguages
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_i18n_languages_list_builder()` + `youtube_i18n_languages_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_i18n_languages_list(
    client: &SimpleHttpClient,
    hl: Option<&str>,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<I18nLanguageListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_i18n_languages_list_builder(client, hl, part)?;
    youtube_i18n_languages_list_execute(builder)
}

/// GET youtube/v3/i18nRegions
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_i18n_regions_list_execute()` to send, or `youtube_i18n_regions_list` for simplest API.

pub fn youtube_i18n_regions_list_builder(
    client: &SimpleHttpClient,
    hl: Option<&str>,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/i18nRegions",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "hl",
            hl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/i18nRegions
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_i18n_regions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_i18n_regions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_i18n_regions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<I18nRegionListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: I18nRegionListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/i18nRegions
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_i18n_regions_list_builder()` + `youtube_i18n_regions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_i18n_regions_list(
    client: &SimpleHttpClient,
    hl: Option<&str>,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<I18nRegionListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_i18n_regions_list_builder(client, hl, part)?;
    youtube_i18n_regions_list_execute(builder)
}

/// GET youtube/v3/liveBroadcasts/bind
/// Bind a broadcast to a stream.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_broadcasts_bind_execute()` to send, or `youtube_live_broadcasts_bind` for simplest API.

pub fn youtube_live_broadcasts_bind_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    streamId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveBroadcasts/bind",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "streamId",
            streamId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/liveBroadcasts/bind
/// Bind a broadcast to a stream.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_broadcasts_bind_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_broadcasts_bind_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_bind_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveBroadcast>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveBroadcast = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveBroadcasts/bind
/// Bind a broadcast to a stream.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_broadcasts_bind_builder()` + `youtube_live_broadcasts_bind_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_bind(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    streamId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveBroadcast>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_live_broadcasts_bind_builder(
        client,
        id,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        part,
        streamId,
    )?;
    youtube_live_broadcasts_bind_execute(builder)
}

/// GET youtube/v3/liveBroadcasts
/// Delete a given broadcast.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_broadcasts_delete_execute()` to send, or `youtube_live_broadcasts_delete` for simplest API.

pub fn youtube_live_broadcasts_delete_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveBroadcasts",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/liveBroadcasts
/// Delete a given broadcast.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_broadcasts_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_broadcasts_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveBroadcasts
/// Delete a given broadcast.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_broadcasts_delete_builder()` + `youtube_live_broadcasts_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_delete(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_live_broadcasts_delete_builder(
        client,
        id,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
    )?;
    youtube_live_broadcasts_delete_execute(builder)
}

/// GET youtube/v3/liveBroadcasts
/// Inserts a new stream for the authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_broadcasts_insert_execute()` to send, or `youtube_live_broadcasts_insert` for simplest API.

pub fn youtube_live_broadcasts_insert_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &LiveBroadcast,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveBroadcasts",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/liveBroadcasts
/// Inserts a new stream for the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_broadcasts_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_broadcasts_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveBroadcast>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveBroadcast = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveBroadcasts
/// Inserts a new stream for the authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_broadcasts_insert_builder()` + `youtube_live_broadcasts_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_insert(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &LiveBroadcast,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveBroadcast>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_live_broadcasts_insert_builder(
        client,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        part,
        body,
    )?;
    youtube_live_broadcasts_insert_execute(builder)
}

/// GET youtube/v3/liveBroadcasts/cuepoint
/// Insert cuepoints in a broadcast
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_broadcasts_insert_cuepoint_execute()` to send, or `youtube_live_broadcasts_insert_cuepoint` for simplest API.

pub fn youtube_live_broadcasts_insert_cuepoint_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &Cuepoint,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveBroadcasts/cuepoint",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/liveBroadcasts/cuepoint
/// Insert cuepoints in a broadcast
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_broadcasts_insert_cuepoint_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_broadcasts_insert_cuepoint_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_insert_cuepoint_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cuepoint>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Cuepoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveBroadcasts/cuepoint
/// Insert cuepoints in a broadcast
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_broadcasts_insert_cuepoint_builder()` + `youtube_live_broadcasts_insert_cuepoint_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_insert_cuepoint(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &Cuepoint,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cuepoint>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_live_broadcasts_insert_cuepoint_builder(
        client,
        id,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        part,
        body,
    )?;
    youtube_live_broadcasts_insert_cuepoint_execute(builder)
}

/// GET youtube/v3/liveBroadcasts
/// Retrieve the list of broadcasts associated with the given channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_broadcasts_list_execute()` to send, or `youtube_live_broadcasts_list` for simplest API.

pub fn youtube_live_broadcasts_list_builder(
    client: &SimpleHttpClient,
    broadcastStatus: Option<&str>,
    broadcastType: Option<&str>,
    id: Option<&str>,
    maxResults: Option<i32>,
    mine: Option<bool>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveBroadcasts",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "broadcastStatus",
            broadcastStatus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "broadcastType",
            broadcastType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "mine",
            mine.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/liveBroadcasts
/// Retrieve the list of broadcasts associated with the given channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_broadcasts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_broadcasts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveBroadcastListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveBroadcastListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveBroadcasts
/// Retrieve the list of broadcasts associated with the given channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_broadcasts_list_builder()` + `youtube_live_broadcasts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_list(
    client: &SimpleHttpClient,
    broadcastStatus: Option<&str>,
    broadcastType: Option<&str>,
    id: Option<&str>,
    maxResults: Option<i32>,
    mine: Option<bool>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveBroadcastListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_live_broadcasts_list_builder(
        client,
        broadcastStatus,
        broadcastType,
        id,
        maxResults,
        mine,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        pageToken,
        part,
    )?;
    youtube_live_broadcasts_list_execute(builder)
}

/// GET youtube/v3/liveBroadcasts/transition
/// Transition a broadcast to a given status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_broadcasts_transition_execute()` to send, or `youtube_live_broadcasts_transition` for simplest API.

pub fn youtube_live_broadcasts_transition_builder(
    client: &SimpleHttpClient,
    broadcastStatus: Option<&str>,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveBroadcasts/transition",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "broadcastStatus",
            broadcastStatus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/liveBroadcasts/transition
/// Transition a broadcast to a given status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_broadcasts_transition_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_broadcasts_transition_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_transition_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveBroadcast>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveBroadcast = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveBroadcasts/transition
/// Transition a broadcast to a given status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_broadcasts_transition_builder()` + `youtube_live_broadcasts_transition_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_transition(
    client: &SimpleHttpClient,
    broadcastStatus: Option<&str>,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveBroadcast>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_live_broadcasts_transition_builder(
        client,
        broadcastStatus,
        id,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        part,
    )?;
    youtube_live_broadcasts_transition_execute(builder)
}

/// GET youtube/v3/liveBroadcasts
/// Updates an existing broadcast for the authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_broadcasts_update_execute()` to send, or `youtube_live_broadcasts_update` for simplest API.

pub fn youtube_live_broadcasts_update_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &LiveBroadcast,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveBroadcasts",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/liveBroadcasts
/// Updates an existing broadcast for the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_broadcasts_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_broadcasts_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveBroadcast>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveBroadcast = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveBroadcasts
/// Updates an existing broadcast for the authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_broadcasts_update_builder()` + `youtube_live_broadcasts_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_broadcasts_update(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &LiveBroadcast,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveBroadcast>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_live_broadcasts_update_builder(
        client,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        part,
        body,
    )?;
    youtube_live_broadcasts_update_execute(builder)
}

/// GET youtube/v3/liveChat/bans
/// Deletes a chat ban.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_chat_bans_delete_execute()` to send, or `youtube_live_chat_bans_delete` for simplest API.

pub fn youtube_live_chat_bans_delete_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveChat/bans",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/liveChat/bans
/// Deletes a chat ban.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_chat_bans_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_chat_bans_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_bans_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveChat/bans
/// Deletes a chat ban.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_chat_bans_delete_builder()` + `youtube_live_chat_bans_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_bans_delete(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_live_chat_bans_delete_builder(client, id)?;
    youtube_live_chat_bans_delete_execute(builder)
}

/// GET youtube/v3/liveChat/bans
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_chat_bans_insert_execute()` to send, or `youtube_live_chat_bans_insert` for simplest API.

pub fn youtube_live_chat_bans_insert_builder(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &LiveChatBan,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveChat/bans",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/liveChat/bans
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_chat_bans_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_chat_bans_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_bans_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveChatBan>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveChatBan = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveChat/bans
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_chat_bans_insert_builder()` + `youtube_live_chat_bans_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_bans_insert(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &LiveChatBan,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveChatBan>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_live_chat_bans_insert_builder(client, part, body)?;
    youtube_live_chat_bans_insert_execute(builder)
}

/// GET youtube/v3/liveChat/messages
/// Deletes a chat message.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_chat_messages_delete_execute()` to send, or `youtube_live_chat_messages_delete` for simplest API.

pub fn youtube_live_chat_messages_delete_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveChat/messages",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/liveChat/messages
/// Deletes a chat message.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_chat_messages_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_chat_messages_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_messages_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveChat/messages
/// Deletes a chat message.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_chat_messages_delete_builder()` + `youtube_live_chat_messages_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_messages_delete(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_live_chat_messages_delete_builder(client, id)?;
    youtube_live_chat_messages_delete_execute(builder)
}

/// GET youtube/v3/liveChat/messages
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_chat_messages_insert_execute()` to send, or `youtube_live_chat_messages_insert` for simplest API.

pub fn youtube_live_chat_messages_insert_builder(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &LiveChatMessage,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveChat/messages",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/liveChat/messages
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_chat_messages_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_chat_messages_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_messages_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveChatMessage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveChatMessage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveChat/messages
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_chat_messages_insert_builder()` + `youtube_live_chat_messages_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_messages_insert(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &LiveChatMessage,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveChatMessage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_live_chat_messages_insert_builder(client, part, body)?;
    youtube_live_chat_messages_insert_execute(builder)
}

/// GET youtube/v3/liveChat/messages
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_chat_messages_list_execute()` to send, or `youtube_live_chat_messages_list` for simplest API.

pub fn youtube_live_chat_messages_list_builder(
    client: &SimpleHttpClient,
    hl: Option<&str>,
    liveChatId: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    part: Option<&str>,
    profileImageSize: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveChat/messages",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "hl",
            hl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "liveChatId",
            liveChatId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "profileImageSize",
            profileImageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/liveChat/messages
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_chat_messages_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_chat_messages_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_messages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiveChatMessageListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveChatMessageListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveChat/messages
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_chat_messages_list_builder()` + `youtube_live_chat_messages_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_messages_list(
    client: &SimpleHttpClient,
    hl: Option<&str>,
    liveChatId: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    part: Option<&str>,
    profileImageSize: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiveChatMessageListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_live_chat_messages_list_builder(
        client,
        hl,
        liveChatId,
        maxResults,
        pageToken,
        part,
        profileImageSize,
    )?;
    youtube_live_chat_messages_list_execute(builder)
}

/// GET youtube/v3/liveChat/messages/transition
/// Transition a durable chat event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_chat_messages_transition_execute()` to send, or `youtube_live_chat_messages_transition` for simplest API.

pub fn youtube_live_chat_messages_transition_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    status: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveChat/messages/transition",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "status",
            status
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/liveChat/messages/transition
/// Transition a durable chat event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_chat_messages_transition_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_chat_messages_transition_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_messages_transition_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveChatMessage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveChatMessage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveChat/messages/transition
/// Transition a durable chat event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_chat_messages_transition_builder()` + `youtube_live_chat_messages_transition_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_messages_transition(
    client: &SimpleHttpClient,
    id: Option<&str>,
    status: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveChatMessage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_live_chat_messages_transition_builder(client, id, status)?;
    youtube_live_chat_messages_transition_execute(builder)
}

/// GET youtube/v3/liveChat/moderators
/// Deletes a chat moderator.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_chat_moderators_delete_execute()` to send, or `youtube_live_chat_moderators_delete` for simplest API.

pub fn youtube_live_chat_moderators_delete_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveChat/moderators",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/liveChat/moderators
/// Deletes a chat moderator.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_chat_moderators_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_chat_moderators_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_moderators_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveChat/moderators
/// Deletes a chat moderator.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_chat_moderators_delete_builder()` + `youtube_live_chat_moderators_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_moderators_delete(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_live_chat_moderators_delete_builder(client, id)?;
    youtube_live_chat_moderators_delete_execute(builder)
}

/// GET youtube/v3/liveChat/moderators
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_chat_moderators_insert_execute()` to send, or `youtube_live_chat_moderators_insert` for simplest API.

pub fn youtube_live_chat_moderators_insert_builder(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &LiveChatModerator,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveChat/moderators",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/liveChat/moderators
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_chat_moderators_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_chat_moderators_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_moderators_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveChatModerator>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveChatModerator = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveChat/moderators
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_chat_moderators_insert_builder()` + `youtube_live_chat_moderators_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_moderators_insert(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &LiveChatModerator,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveChatModerator>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_live_chat_moderators_insert_builder(client, part, body)?;
    youtube_live_chat_moderators_insert_execute(builder)
}

/// GET youtube/v3/liveChat/moderators
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_chat_moderators_list_execute()` to send, or `youtube_live_chat_moderators_list` for simplest API.

pub fn youtube_live_chat_moderators_list_builder(
    client: &SimpleHttpClient,
    liveChatId: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveChat/moderators",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "liveChatId",
            liveChatId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/liveChat/moderators
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_chat_moderators_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_chat_moderators_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_moderators_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiveChatModeratorListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveChatModeratorListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveChat/moderators
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_chat_moderators_list_builder()` + `youtube_live_chat_moderators_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_chat_moderators_list(
    client: &SimpleHttpClient,
    liveChatId: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiveChatModeratorListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        youtube_live_chat_moderators_list_builder(client, liveChatId, maxResults, pageToken, part)?;
    youtube_live_chat_moderators_list_execute(builder)
}

/// GET youtube/v3/liveStreams
/// Deletes an existing stream for the authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_streams_delete_execute()` to send, or `youtube_live_streams_delete` for simplest API.

pub fn youtube_live_streams_delete_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveStreams",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/liveStreams
/// Deletes an existing stream for the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_streams_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_streams_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_streams_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveStreams
/// Deletes an existing stream for the authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_streams_delete_builder()` + `youtube_live_streams_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_streams_delete(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_live_streams_delete_builder(
        client,
        id,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
    )?;
    youtube_live_streams_delete_execute(builder)
}

/// GET youtube/v3/liveStreams
/// Inserts a new stream for the authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_streams_insert_execute()` to send, or `youtube_live_streams_insert` for simplest API.

pub fn youtube_live_streams_insert_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &LiveStream,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveStreams",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/liveStreams
/// Inserts a new stream for the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_streams_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_streams_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_streams_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveStream>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveStream = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveStreams
/// Inserts a new stream for the authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_streams_insert_builder()` + `youtube_live_streams_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_streams_insert(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &LiveStream,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveStream>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_live_streams_insert_builder(
        client,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        part,
        body,
    )?;
    youtube_live_streams_insert_execute(builder)
}

/// GET youtube/v3/liveStreams
/// Retrieve the list of streams associated with the given channel. --
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_streams_list_execute()` to send, or `youtube_live_streams_list` for simplest API.

pub fn youtube_live_streams_list_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    maxResults: Option<i32>,
    mine: Option<bool>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveStreams",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "mine",
            mine.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/liveStreams
/// Retrieve the list of streams associated with the given channel. --
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_streams_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_streams_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_streams_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveStreamListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveStreamListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveStreams
/// Retrieve the list of streams associated with the given channel. --
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_streams_list_builder()` + `youtube_live_streams_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_streams_list(
    client: &SimpleHttpClient,
    id: Option<&str>,
    maxResults: Option<i32>,
    mine: Option<bool>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveStreamListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_live_streams_list_builder(
        client,
        id,
        maxResults,
        mine,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        pageToken,
        part,
    )?;
    youtube_live_streams_list_execute(builder)
}

/// GET youtube/v3/liveStreams
/// Updates an existing stream for the authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_live_streams_update_execute()` to send, or `youtube_live_streams_update` for simplest API.

pub fn youtube_live_streams_update_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &LiveStream,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveStreams",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/liveStreams
/// Updates an existing stream for the authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_live_streams_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_live_streams_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_streams_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveStream>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveStream = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveStreams
/// Updates an existing stream for the authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_live_streams_update_builder()` + `youtube_live_streams_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_live_streams_update(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &LiveStream,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LiveStream>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_live_streams_update_builder(
        client,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        part,
        body,
    )?;
    youtube_live_streams_update_execute(builder)
}

/// GET youtube/v3/members
/// Retrieves a list of members that match the request criteria for a channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_members_list_execute()` to send, or `youtube_members_list` for simplest API.

pub fn youtube_members_list_builder(
    client: &SimpleHttpClient,
    filterByMemberChannelId: Option<&str>,
    hasAccessToLevel: Option<&str>,
    maxResults: Option<i32>,
    mode: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/members",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filterByMemberChannelId",
            filterByMemberChannelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "hasAccessToLevel",
            hasAccessToLevel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "mode",
            mode.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/members
/// Retrieves a list of members that match the request criteria for a channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_members_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_members_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_members_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MemberListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MemberListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/members
/// Retrieves a list of members that match the request criteria for a channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_members_list_builder()` + `youtube_members_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_members_list(
    client: &SimpleHttpClient,
    filterByMemberChannelId: Option<&str>,
    hasAccessToLevel: Option<&str>,
    maxResults: Option<i32>,
    mode: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MemberListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_members_list_builder(
        client,
        filterByMemberChannelId,
        hasAccessToLevel,
        maxResults,
        mode,
        pageToken,
        part,
    )?;
    youtube_members_list_execute(builder)
}

/// GET youtube/v3/membershipsLevels
/// Retrieves a list of all pricing levels offered by a creator to the fans.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_memberships_levels_list_execute()` to send, or `youtube_memberships_levels_list` for simplest API.

pub fn youtube_memberships_levels_list_builder(
    client: &SimpleHttpClient,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/membershipsLevels",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/membershipsLevels
/// Retrieves a list of all pricing levels offered by a creator to the fans.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_memberships_levels_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_memberships_levels_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_memberships_levels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MembershipsLevelListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MembershipsLevelListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/membershipsLevels
/// Retrieves a list of all pricing levels offered by a creator to the fans.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_memberships_levels_list_builder()` + `youtube_memberships_levels_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_memberships_levels_list(
    client: &SimpleHttpClient,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MembershipsLevelListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_memberships_levels_list_builder(client, part)?;
    youtube_memberships_levels_list_execute(builder)
}

/// GET youtube/v3/playlistImages
/// Deletes a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_playlist_images_delete_execute()` to send, or `youtube_playlist_images_delete` for simplest API.

pub fn youtube_playlist_images_delete_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/playlistImages",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/playlistImages
/// Deletes a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_playlist_images_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_playlist_images_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_images_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/playlistImages
/// Deletes a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_playlist_images_delete_builder()` + `youtube_playlist_images_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_images_delete(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_playlist_images_delete_builder(client, id, onBehalfOfContentOwner)?;
    youtube_playlist_images_delete_execute(builder)
}

/// GET youtube/v3/playlistImages
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_playlist_images_insert_execute()` to send, or `youtube_playlist_images_insert` for simplest API.

pub fn youtube_playlist_images_insert_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &PlaylistImage,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/playlistImages",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/playlistImages
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_playlist_images_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_playlist_images_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_images_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaylistImage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlaylistImage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/playlistImages
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_playlist_images_insert_builder()` + `youtube_playlist_images_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_images_insert(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &PlaylistImage,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaylistImage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_playlist_images_insert_builder(
        client,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        part,
        body,
    )?;
    youtube_playlist_images_insert_execute(builder)
}

/// GET youtube/v3/playlistImages
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_playlist_images_list_execute()` to send, or `youtube_playlist_images_list` for simplest API.

pub fn youtube_playlist_images_list_builder(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/playlistImages",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "parent",
            parent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/playlistImages
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_playlist_images_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_playlist_images_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_images_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaylistImageListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlaylistImageListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/playlistImages
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_playlist_images_list_builder()` + `youtube_playlist_images_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_images_list(
    client: &SimpleHttpClient,
    maxResults: Option<i32>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaylistImageListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_playlist_images_list_builder(
        client,
        maxResults,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        pageToken,
        parent,
        part,
    )?;
    youtube_playlist_images_list_execute(builder)
}

/// GET youtube/v3/playlistImages
/// Updates an existing resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_playlist_images_update_execute()` to send, or `youtube_playlist_images_update` for simplest API.

pub fn youtube_playlist_images_update_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    body: &PlaylistImage,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/playlistImages",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/playlistImages
/// Updates an existing resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_playlist_images_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_playlist_images_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_images_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaylistImage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlaylistImage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/playlistImages
/// Updates an existing resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_playlist_images_update_builder()` + `youtube_playlist_images_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_images_update(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    body: &PlaylistImage,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaylistImage>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        youtube_playlist_images_update_builder(client, onBehalfOfContentOwner, part, body)?;
    youtube_playlist_images_update_execute(builder)
}

/// GET youtube/v3/playlistItems
/// Deletes a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_playlist_items_delete_execute()` to send, or `youtube_playlist_items_delete` for simplest API.

pub fn youtube_playlist_items_delete_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/playlistItems",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/playlistItems
/// Deletes a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_playlist_items_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_playlist_items_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_items_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/playlistItems
/// Deletes a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_playlist_items_delete_builder()` + `youtube_playlist_items_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_items_delete(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_playlist_items_delete_builder(client, id, onBehalfOfContentOwner)?;
    youtube_playlist_items_delete_execute(builder)
}

/// GET youtube/v3/playlistItems
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_playlist_items_insert_execute()` to send, or `youtube_playlist_items_insert` for simplest API.

pub fn youtube_playlist_items_insert_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    body: &PlaylistItem,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/playlistItems",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/playlistItems
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_playlist_items_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_playlist_items_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_items_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaylistItem>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlaylistItem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/playlistItems
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_playlist_items_insert_builder()` + `youtube_playlist_items_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_items_insert(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    body: &PlaylistItem,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaylistItem>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        youtube_playlist_items_insert_builder(client, onBehalfOfContentOwner, part, body)?;
    youtube_playlist_items_insert_execute(builder)
}

/// GET youtube/v3/playlistItems
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_playlist_items_list_execute()` to send, or `youtube_playlist_items_list` for simplest API.

pub fn youtube_playlist_items_list_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    maxResults: Option<i32>,
    onBehalfOfContentOwner: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
    playlistId: Option<&str>,
    videoId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/playlistItems",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "playlistId",
            playlistId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoId",
            videoId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/playlistItems
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_playlist_items_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_playlist_items_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_items_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaylistItemListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlaylistItemListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/playlistItems
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_playlist_items_list_builder()` + `youtube_playlist_items_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_items_list(
    client: &SimpleHttpClient,
    id: Option<&str>,
    maxResults: Option<i32>,
    onBehalfOfContentOwner: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
    playlistId: Option<&str>,
    videoId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaylistItemListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_playlist_items_list_builder(
        client,
        id,
        maxResults,
        onBehalfOfContentOwner,
        pageToken,
        part,
        playlistId,
        videoId,
    )?;
    youtube_playlist_items_list_execute(builder)
}

/// GET youtube/v3/playlistItems
/// Updates an existing resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_playlist_items_update_execute()` to send, or `youtube_playlist_items_update` for simplest API.

pub fn youtube_playlist_items_update_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    body: &PlaylistItem,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/playlistItems",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/playlistItems
/// Updates an existing resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_playlist_items_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_playlist_items_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_items_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaylistItem>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlaylistItem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/playlistItems
/// Updates an existing resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_playlist_items_update_builder()` + `youtube_playlist_items_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlist_items_update(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    body: &PlaylistItem,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaylistItem>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        youtube_playlist_items_update_builder(client, onBehalfOfContentOwner, part, body)?;
    youtube_playlist_items_update_execute(builder)
}

/// GET youtube/v3/playlists
/// Deletes a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_playlists_delete_execute()` to send, or `youtube_playlists_delete` for simplest API.

pub fn youtube_playlists_delete_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/playlists",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/playlists
/// Deletes a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_playlists_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_playlists_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlists_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/playlists
/// Deletes a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_playlists_delete_builder()` + `youtube_playlists_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlists_delete(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_playlists_delete_builder(client, id, onBehalfOfContentOwner)?;
    youtube_playlists_delete_execute(builder)
}

/// GET youtube/v3/playlists
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_playlists_insert_execute()` to send, or `youtube_playlists_insert` for simplest API.

pub fn youtube_playlists_insert_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &Playlist,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/playlists",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/playlists
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_playlists_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_playlists_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlists_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Playlist>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Playlist = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/playlists
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_playlists_insert_builder()` + `youtube_playlists_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlists_insert(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &Playlist,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Playlist>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_playlists_insert_builder(
        client,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        part,
        body,
    )?;
    youtube_playlists_insert_execute(builder)
}

/// GET youtube/v3/playlists
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_playlists_list_execute()` to send, or `youtube_playlists_list` for simplest API.

pub fn youtube_playlists_list_builder(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    hl: Option<&str>,
    id: Option<&str>,
    maxResults: Option<i32>,
    mine: Option<bool>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/playlists",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "channelId",
            channelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "hl",
            hl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "mine",
            mine.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/playlists
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_playlists_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_playlists_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlists_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaylistListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlaylistListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/playlists
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_playlists_list_builder()` + `youtube_playlists_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlists_list(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    hl: Option<&str>,
    id: Option<&str>,
    maxResults: Option<i32>,
    mine: Option<bool>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlaylistListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_playlists_list_builder(
        client,
        channelId,
        hl,
        id,
        maxResults,
        mine,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        pageToken,
        part,
    )?;
    youtube_playlists_list_execute(builder)
}

/// GET youtube/v3/playlists
/// Updates an existing resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_playlists_update_execute()` to send, or `youtube_playlists_update` for simplest API.

pub fn youtube_playlists_update_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    body: &Playlist,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/playlists",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/playlists
/// Updates an existing resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_playlists_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_playlists_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlists_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Playlist>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Playlist = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/playlists
/// Updates an existing resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_playlists_update_builder()` + `youtube_playlists_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_playlists_update(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    body: &Playlist,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Playlist>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_playlists_update_builder(client, onBehalfOfContentOwner, part, body)?;
    youtube_playlists_update_execute(builder)
}

/// GET youtube/v3/search
/// Retrieves a list of search resources
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_search_list_execute()` to send, or `youtube_search_list` for simplest API.

pub fn youtube_search_list_builder(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    channelType: Option<&str>,
    eventType: Option<&str>,
    forContentOwner: Option<bool>,
    forDeveloper: Option<bool>,
    forMine: Option<bool>,
    location: Option<&str>,
    locationRadius: Option<&str>,
    maxResults: Option<i32>,
    onBehalfOfContentOwner: Option<&str>,
    order: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
    publishedAfter: Option<&str>,
    publishedBefore: Option<&str>,
    q: Option<&str>,
    regionCode: Option<&str>,
    relevanceLanguage: Option<&str>,
    safeSearch: Option<&str>,
    topicId: Option<&str>,
    type_rs: Option<&str>,
    videoCaption: Option<&str>,
    videoCategoryId: Option<&str>,
    videoDefinition: Option<&str>,
    videoDimension: Option<&str>,
    videoDuration: Option<&str>,
    videoEmbeddable: Option<&str>,
    videoLicense: Option<&str>,
    videoPaidProductPlacement: Option<&str>,
    videoSyndicated: Option<&str>,
    videoType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/search",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "channelId",
            channelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "channelType",
            channelType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "eventType",
            eventType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "forContentOwner",
            forContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "forDeveloper",
            forDeveloper
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "forMine",
            forMine
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "location",
            location
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locationRadius",
            locationRadius
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "order",
            order
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "publishedAfter",
            publishedAfter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "publishedBefore",
            publishedBefore
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "q",
            q.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "regionCode",
            regionCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "relevanceLanguage",
            relevanceLanguage
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "safeSearch",
            safeSearch
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "topicId",
            topicId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoCaption",
            videoCaption
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoCategoryId",
            videoCategoryId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoDefinition",
            videoDefinition
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoDimension",
            videoDimension
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoDuration",
            videoDuration
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoEmbeddable",
            videoEmbeddable
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoLicense",
            videoLicense
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoPaidProductPlacement",
            videoPaidProductPlacement
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoSyndicated",
            videoSyndicated
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoType",
            videoType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/search
/// Retrieves a list of search resources
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_search_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_search_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_search_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/search
/// Retrieves a list of search resources
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_search_list_builder()` + `youtube_search_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_search_list(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    channelType: Option<&str>,
    eventType: Option<&str>,
    forContentOwner: Option<bool>,
    forDeveloper: Option<bool>,
    forMine: Option<bool>,
    location: Option<&str>,
    locationRadius: Option<&str>,
    maxResults: Option<i32>,
    onBehalfOfContentOwner: Option<&str>,
    order: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
    publishedAfter: Option<&str>,
    publishedBefore: Option<&str>,
    q: Option<&str>,
    regionCode: Option<&str>,
    relevanceLanguage: Option<&str>,
    safeSearch: Option<&str>,
    topicId: Option<&str>,
    type_rs: Option<&str>,
    videoCaption: Option<&str>,
    videoCategoryId: Option<&str>,
    videoDefinition: Option<&str>,
    videoDimension: Option<&str>,
    videoDuration: Option<&str>,
    videoEmbeddable: Option<&str>,
    videoLicense: Option<&str>,
    videoPaidProductPlacement: Option<&str>,
    videoSyndicated: Option<&str>,
    videoType: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_search_list_builder(
        client,
        channelId,
        channelType,
        eventType,
        forContentOwner,
        forDeveloper,
        forMine,
        location,
        locationRadius,
        maxResults,
        onBehalfOfContentOwner,
        order,
        pageToken,
        part,
        publishedAfter,
        publishedBefore,
        q,
        regionCode,
        relevanceLanguage,
        safeSearch,
        topicId,
        type_rs,
        videoCaption,
        videoCategoryId,
        videoDefinition,
        videoDimension,
        videoDuration,
        videoEmbeddable,
        videoLicense,
        videoPaidProductPlacement,
        videoSyndicated,
        videoType,
    )?;
    youtube_search_list_execute(builder)
}

/// GET youtube/v3/subscriptions
/// Deletes a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_subscriptions_delete_execute()` to send, or `youtube_subscriptions_delete` for simplest API.

pub fn youtube_subscriptions_delete_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/subscriptions",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/subscriptions
/// Deletes a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_subscriptions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_subscriptions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_subscriptions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/subscriptions
/// Deletes a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_subscriptions_delete_builder()` + `youtube_subscriptions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_subscriptions_delete(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_subscriptions_delete_builder(client, id)?;
    youtube_subscriptions_delete_execute(builder)
}

/// GET youtube/v3/subscriptions
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_subscriptions_insert_execute()` to send, or `youtube_subscriptions_insert` for simplest API.

pub fn youtube_subscriptions_insert_builder(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &Subscription,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/subscriptions",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/subscriptions
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_subscriptions_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_subscriptions_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_subscriptions_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/subscriptions
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_subscriptions_insert_builder()` + `youtube_subscriptions_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_subscriptions_insert(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &Subscription,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_subscriptions_insert_builder(client, part, body)?;
    youtube_subscriptions_insert_execute(builder)
}

/// GET youtube/v3/subscriptions
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_subscriptions_list_execute()` to send, or `youtube_subscriptions_list` for simplest API.

pub fn youtube_subscriptions_list_builder(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    forChannelId: Option<&str>,
    id: Option<&str>,
    maxResults: Option<i32>,
    mine: Option<bool>,
    myRecentSubscribers: Option<bool>,
    mySubscribers: Option<bool>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    order: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/subscriptions",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "channelId",
            channelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "forChannelId",
            forChannelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "mine",
            mine.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "myRecentSubscribers",
            myRecentSubscribers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "mySubscribers",
            mySubscribers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "order",
            order
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/subscriptions
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_subscriptions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_subscriptions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_subscriptions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/subscriptions
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_subscriptions_list_builder()` + `youtube_subscriptions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_subscriptions_list(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    forChannelId: Option<&str>,
    id: Option<&str>,
    maxResults: Option<i32>,
    mine: Option<bool>,
    myRecentSubscribers: Option<bool>,
    mySubscribers: Option<bool>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    order: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_subscriptions_list_builder(
        client,
        channelId,
        forChannelId,
        id,
        maxResults,
        mine,
        myRecentSubscribers,
        mySubscribers,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        order,
        pageToken,
        part,
    )?;
    youtube_subscriptions_list_execute(builder)
}

/// GET youtube/v3/superChatEvents
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_super_chat_events_list_execute()` to send, or `youtube_super_chat_events_list` for simplest API.

pub fn youtube_super_chat_events_list_builder(
    client: &SimpleHttpClient,
    hl: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/superChatEvents",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "hl",
            hl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/superChatEvents
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_super_chat_events_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_super_chat_events_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_super_chat_events_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SuperChatEventListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SuperChatEventListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/superChatEvents
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_super_chat_events_list_builder()` + `youtube_super_chat_events_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_super_chat_events_list(
    client: &SimpleHttpClient,
    hl: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SuperChatEventListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_super_chat_events_list_builder(client, hl, maxResults, pageToken, part)?;
    youtube_super_chat_events_list_execute(builder)
}

/// GET youtube/v3/tests
/// POST method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_tests_insert_execute()` to send, or `youtube_tests_insert` for simplest API.

pub fn youtube_tests_insert_builder(
    client: &SimpleHttpClient,
    externalChannelId: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &TestItem,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/tests",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "externalChannelId",
            externalChannelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/tests
/// POST method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_tests_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_tests_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_tests_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestItem>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestItem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/tests
/// POST method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_tests_insert_builder()` + `youtube_tests_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_tests_insert(
    client: &SimpleHttpClient,
    externalChannelId: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    body: &TestItem,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestItem>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_tests_insert_builder(
        client,
        externalChannelId,
        onBehalfOfContentOwnerChannel,
        part,
        body,
    )?;
    youtube_tests_insert_execute(builder)
}

/// GET youtube/v3/thirdPartyLinks
/// Deletes a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_third_party_links_delete_execute()` to send, or `youtube_third_party_links_delete` for simplest API.

pub fn youtube_third_party_links_delete_builder(
    client: &SimpleHttpClient,
    externalChannelId: Option<&str>,
    linkingToken: Option<&str>,
    part: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/thirdPartyLinks",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "externalChannelId",
            externalChannelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "linkingToken",
            linkingToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/thirdPartyLinks
/// Deletes a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_third_party_links_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_third_party_links_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_third_party_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/thirdPartyLinks
/// Deletes a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_third_party_links_delete_builder()` + `youtube_third_party_links_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_third_party_links_delete(
    client: &SimpleHttpClient,
    externalChannelId: Option<&str>,
    linkingToken: Option<&str>,
    part: Option<&str>,
    type_rs: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_third_party_links_delete_builder(
        client,
        externalChannelId,
        linkingToken,
        part,
        type_rs,
    )?;
    youtube_third_party_links_delete_execute(builder)
}

/// GET youtube/v3/thirdPartyLinks
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_third_party_links_insert_execute()` to send, or `youtube_third_party_links_insert` for simplest API.

pub fn youtube_third_party_links_insert_builder(
    client: &SimpleHttpClient,
    externalChannelId: Option<&str>,
    part: Option<&str>,
    body: &ThirdPartyLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/thirdPartyLinks",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "externalChannelId",
            externalChannelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/thirdPartyLinks
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_third_party_links_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_third_party_links_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_third_party_links_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ThirdPartyLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ThirdPartyLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/thirdPartyLinks
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_third_party_links_insert_builder()` + `youtube_third_party_links_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_third_party_links_insert(
    client: &SimpleHttpClient,
    externalChannelId: Option<&str>,
    part: Option<&str>,
    body: &ThirdPartyLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ThirdPartyLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_third_party_links_insert_builder(client, externalChannelId, part, body)?;
    youtube_third_party_links_insert_execute(builder)
}

/// GET youtube/v3/thirdPartyLinks
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_third_party_links_list_execute()` to send, or `youtube_third_party_links_list` for simplest API.

pub fn youtube_third_party_links_list_builder(
    client: &SimpleHttpClient,
    externalChannelId: Option<&str>,
    linkingToken: Option<&str>,
    part: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/thirdPartyLinks",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "externalChannelId",
            externalChannelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "linkingToken",
            linkingToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/thirdPartyLinks
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_third_party_links_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_third_party_links_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_third_party_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ThirdPartyLinkListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ThirdPartyLinkListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/thirdPartyLinks
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_third_party_links_list_builder()` + `youtube_third_party_links_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_third_party_links_list(
    client: &SimpleHttpClient,
    externalChannelId: Option<&str>,
    linkingToken: Option<&str>,
    part: Option<&str>,
    type_rs: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ThirdPartyLinkListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_third_party_links_list_builder(
        client,
        externalChannelId,
        linkingToken,
        part,
        type_rs,
    )?;
    youtube_third_party_links_list_execute(builder)
}

/// GET youtube/v3/thirdPartyLinks
/// Updates an existing resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_third_party_links_update_execute()` to send, or `youtube_third_party_links_update` for simplest API.

pub fn youtube_third_party_links_update_builder(
    client: &SimpleHttpClient,
    externalChannelId: Option<&str>,
    part: Option<&str>,
    body: &ThirdPartyLink,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/thirdPartyLinks",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "externalChannelId",
            externalChannelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/thirdPartyLinks
/// Updates an existing resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_third_party_links_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_third_party_links_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_third_party_links_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ThirdPartyLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ThirdPartyLink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/thirdPartyLinks
/// Updates an existing resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_third_party_links_update_builder()` + `youtube_third_party_links_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_third_party_links_update(
    client: &SimpleHttpClient,
    externalChannelId: Option<&str>,
    part: Option<&str>,
    body: &ThirdPartyLink,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ThirdPartyLink>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_third_party_links_update_builder(client, externalChannelId, part, body)?;
    youtube_third_party_links_update_execute(builder)
}

/// GET youtube/v3/thumbnails/set
/// As this is not an insert in a strict sense (it supports `uploading/setting` of a thumbnail for multiple videos, which doesn't result in creation of a single resource), I use a custom verb here.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_thumbnails_set_execute()` to send, or `youtube_thumbnails_set` for simplest API.

pub fn youtube_thumbnails_set_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    videoId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/thumbnails/set",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoId",
            videoId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/thumbnails/set
/// As this is not an insert in a strict sense (it supports `uploading/setting` of a thumbnail for multiple videos, which doesn't result in creation of a single resource), I use a custom verb here.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_thumbnails_set_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_thumbnails_set_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_thumbnails_set_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ThumbnailSetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ThumbnailSetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/thumbnails/set
/// As this is not an insert in a strict sense (it supports `uploading/setting` of a thumbnail for multiple videos, which doesn't result in creation of a single resource), I use a custom verb here.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_thumbnails_set_builder()` + `youtube_thumbnails_set_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_thumbnails_set(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    videoId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ThumbnailSetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_thumbnails_set_builder(client, onBehalfOfContentOwner, videoId)?;
    youtube_thumbnails_set_execute(builder)
}

/// GET youtube/v3/videoAbuseReportReasons
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_video_abuse_report_reasons_list_execute()` to send, or `youtube_video_abuse_report_reasons_list` for simplest API.

pub fn youtube_video_abuse_report_reasons_list_builder(
    client: &SimpleHttpClient,
    hl: Option<&str>,
    part: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/videoAbuseReportReasons",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "hl",
            hl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/videoAbuseReportReasons
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_video_abuse_report_reasons_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_video_abuse_report_reasons_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_video_abuse_report_reasons_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VideoAbuseReportReasonListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VideoAbuseReportReasonListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/videoAbuseReportReasons
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_video_abuse_report_reasons_list_builder()` + `youtube_video_abuse_report_reasons_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_video_abuse_report_reasons_list(
    client: &SimpleHttpClient,
    hl: Option<&str>,
    part: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VideoAbuseReportReasonListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_video_abuse_report_reasons_list_builder(client, hl, part)?;
    youtube_video_abuse_report_reasons_list_execute(builder)
}

/// GET youtube/v3/videoCategories
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_video_categories_list_execute()` to send, or `youtube_video_categories_list` for simplest API.

pub fn youtube_video_categories_list_builder(
    client: &SimpleHttpClient,
    hl: Option<&str>,
    id: Option<&str>,
    part: Option<&str>,
    regionCode: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/videoCategories",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "hl",
            hl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "regionCode",
            regionCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/videoCategories
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_video_categories_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_video_categories_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_video_categories_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VideoCategoryListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VideoCategoryListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/videoCategories
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_video_categories_list_builder()` + `youtube_video_categories_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_video_categories_list(
    client: &SimpleHttpClient,
    hl: Option<&str>,
    id: Option<&str>,
    part: Option<&str>,
    regionCode: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VideoCategoryListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_video_categories_list_builder(client, hl, id, part, regionCode)?;
    youtube_video_categories_list_execute(builder)
}

/// GET youtube/v3/videoTrainability
/// Returns the trainability status of a video.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_video_trainability_get_execute()` to send, or `youtube_video_trainability_get` for simplest API.

pub fn youtube_video_trainability_get_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/videoTrainability",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/videoTrainability
/// Returns the trainability status of a video.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_video_trainability_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_video_trainability_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_video_trainability_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VideoTrainability>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VideoTrainability = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/videoTrainability
/// Returns the trainability status of a video.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_video_trainability_get_builder()` + `youtube_video_trainability_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_video_trainability_get(
    client: &SimpleHttpClient,
    id: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VideoTrainability>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_video_trainability_get_builder(client, id)?;
    youtube_video_trainability_get_execute(builder)
}

/// GET youtube/v3/videos
/// Deletes a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_videos_delete_execute()` to send, or `youtube_videos_delete` for simplest API.

pub fn youtube_videos_delete_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/videos",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/videos
/// Deletes a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_videos_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_videos_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/videos
/// Deletes a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_videos_delete_builder()` + `youtube_videos_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_delete(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_videos_delete_builder(client, id, onBehalfOfContentOwner)?;
    youtube_videos_delete_execute(builder)
}

/// GET youtube/v3/videos/getRating
/// Retrieves the ratings that the authorized user gave to a list of specified videos.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_videos_get_rating_execute()` to send, or `youtube_videos_get_rating` for simplest API.

pub fn youtube_videos_get_rating_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/videos/getRating",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/videos/getRating
/// Retrieves the ratings that the authorized user gave to a list of specified videos.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_videos_get_rating_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_videos_get_rating_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_get_rating_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VideoGetRatingResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VideoGetRatingResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/videos/getRating
/// Retrieves the ratings that the authorized user gave to a list of specified videos.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_videos_get_rating_builder()` + `youtube_videos_get_rating_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_get_rating(
    client: &SimpleHttpClient,
    id: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VideoGetRatingResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_videos_get_rating_builder(client, id, onBehalfOfContentOwner)?;
    youtube_videos_get_rating_execute(builder)
}

/// GET youtube/v3/videos
/// Inserts a new resource into this collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_videos_insert_execute()` to send, or `youtube_videos_insert` for simplest API.

pub fn youtube_videos_insert_builder(
    client: &SimpleHttpClient,
    autoLevels: Option<bool>,
    notifySubscribers: Option<bool>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    stabilize: Option<bool>,
    body: &Video,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/videos",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "autoLevels",
            autoLevels
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "notifySubscribers",
            notifySubscribers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwnerChannel",
            onBehalfOfContentOwnerChannel
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "stabilize",
            stabilize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/videos
/// Inserts a new resource into this collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_videos_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_videos_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Video>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Video = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/videos
/// Inserts a new resource into this collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_videos_insert_builder()` + `youtube_videos_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_insert(
    client: &SimpleHttpClient,
    autoLevels: Option<bool>,
    notifySubscribers: Option<bool>,
    onBehalfOfContentOwner: Option<&str>,
    onBehalfOfContentOwnerChannel: Option<&str>,
    part: Option<&str>,
    stabilize: Option<bool>,
    body: &Video,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Video>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_videos_insert_builder(
        client,
        autoLevels,
        notifySubscribers,
        onBehalfOfContentOwner,
        onBehalfOfContentOwnerChannel,
        part,
        stabilize,
        body,
    )?;
    youtube_videos_insert_execute(builder)
}

/// GET youtube/v3/videos
/// Retrieves a list of resources, possibly filtered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_videos_list_execute()` to send, or `youtube_videos_list` for simplest API.

pub fn youtube_videos_list_builder(
    client: &SimpleHttpClient,
    chart: Option<&str>,
    hl: Option<&str>,
    id: Option<&str>,
    locale: Option<&str>,
    maxHeight: Option<i32>,
    maxResults: Option<i32>,
    maxWidth: Option<i32>,
    myRating: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
    regionCode: Option<&str>,
    videoCategoryId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/videos",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "chart",
            chart
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "hl",
            hl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "locale",
            locale
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxHeight",
            maxHeight
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxWidth",
            maxWidth
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "myRating",
            myRating
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "regionCode",
            regionCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "videoCategoryId",
            videoCategoryId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/videos
/// Retrieves a list of resources, possibly filtered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_videos_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_videos_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VideoListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VideoListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/videos
/// Retrieves a list of resources, possibly filtered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_videos_list_builder()` + `youtube_videos_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_list(
    client: &SimpleHttpClient,
    chart: Option<&str>,
    hl: Option<&str>,
    id: Option<&str>,
    locale: Option<&str>,
    maxHeight: Option<i32>,
    maxResults: Option<i32>,
    maxWidth: Option<i32>,
    myRating: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    pageToken: Option<&str>,
    part: Option<&str>,
    regionCode: Option<&str>,
    videoCategoryId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VideoListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_videos_list_builder(
        client,
        chart,
        hl,
        id,
        locale,
        maxHeight,
        maxResults,
        maxWidth,
        myRating,
        onBehalfOfContentOwner,
        pageToken,
        part,
        regionCode,
        videoCategoryId,
    )?;
    youtube_videos_list_execute(builder)
}

/// GET youtube/v3/videos/rate
/// Adds a like or dislike rating to a video or removes a rating from a video.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_videos_rate_execute()` to send, or `youtube_videos_rate` for simplest API.

pub fn youtube_videos_rate_builder(
    client: &SimpleHttpClient,
    id: Option<&str>,
    rating: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/videos/rate",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "id",
            id.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "rating",
            rating
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/videos/rate
/// Adds a like or dislike rating to a video or removes a rating from a video.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_videos_rate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_videos_rate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_rate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/videos/rate
/// Adds a like or dislike rating to a video or removes a rating from a video.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_videos_rate_builder()` + `youtube_videos_rate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_rate(
    client: &SimpleHttpClient,
    id: Option<&str>,
    rating: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_videos_rate_builder(client, id, rating)?;
    youtube_videos_rate_execute(builder)
}

/// GET youtube/v3/videos/reportAbuse
/// Report abuse for a video.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_videos_report_abuse_execute()` to send, or `youtube_videos_report_abuse` for simplest API.

pub fn youtube_videos_report_abuse_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    body: &VideoAbuseReport,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/videos/reportAbuse",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/videos/reportAbuse
/// Report abuse for a video.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_videos_report_abuse_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_videos_report_abuse_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_report_abuse_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/videos/reportAbuse
/// Report abuse for a video.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_videos_report_abuse_builder()` + `youtube_videos_report_abuse_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_report_abuse(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    body: &VideoAbuseReport,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_videos_report_abuse_builder(client, onBehalfOfContentOwner, body)?;
    youtube_videos_report_abuse_execute(builder)
}

/// GET youtube/v3/videos
/// Updates an existing resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_videos_update_execute()` to send, or `youtube_videos_update` for simplest API.

pub fn youtube_videos_update_builder(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    body: &Video,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/videos",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/videos
/// Updates an existing resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_videos_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_videos_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Video>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Video = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/videos
/// Updates an existing resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_videos_update_builder()` + `youtube_videos_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_videos_update(
    client: &SimpleHttpClient,
    onBehalfOfContentOwner: Option<&str>,
    part: Option<&str>,
    body: &Video,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Video>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_videos_update_builder(client, onBehalfOfContentOwner, part, body)?;
    youtube_videos_update_execute(builder)
}

/// GET youtube/v3/watermarks/set
/// Allows upload of watermark image and setting it for a channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_watermarks_set_execute()` to send, or `youtube_watermarks_set` for simplest API.

pub fn youtube_watermarks_set_builder(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    body: &InvideoBranding,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/watermarks/set",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "channelId",
            channelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/watermarks/set
/// Allows upload of watermark image and setting it for a channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_watermarks_set_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_watermarks_set_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_watermarks_set_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/watermarks/set
/// Allows upload of watermark image and setting it for a channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_watermarks_set_builder()` + `youtube_watermarks_set_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_watermarks_set(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
    body: &InvideoBranding,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_watermarks_set_builder(client, channelId, onBehalfOfContentOwner, body)?;
    youtube_watermarks_set_execute(builder)
}

/// GET youtube/v3/watermarks/unset
/// Allows removal of channel watermark.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_watermarks_unset_execute()` to send, or `youtube_watermarks_unset` for simplest API.

pub fn youtube_watermarks_unset_builder(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/watermarks/unset",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "channelId",
            channelId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "onBehalfOfContentOwner",
            onBehalfOfContentOwner
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/watermarks/unset
/// Allows removal of channel watermark.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_watermarks_unset_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_watermarks_unset_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_watermarks_unset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/watermarks/unset
/// Allows removal of channel watermark.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_watermarks_unset_builder()` + `youtube_watermarks_unset_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_watermarks_unset(
    client: &SimpleHttpClient,
    channelId: Option<&str>,
    onBehalfOfContentOwner: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = youtube_watermarks_unset_builder(client, channelId, onBehalfOfContentOwner)?;
    youtube_watermarks_unset_execute(builder)
}

/// GET youtube/v3/commentThreads
/// Updates an existing resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_youtube_v3_update_comment_threads_execute()` to send, or `youtube_youtube_v3_update_comment_threads` for simplest API.

pub fn youtube_youtube_v3_update_comment_threads_builder(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &CommentThread,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/commentThreads",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET youtube/v3/commentThreads
/// Updates an existing resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_youtube_v3_update_comment_threads_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_youtube_v3_update_comment_threads_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_youtube_v3_update_comment_threads_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentThread>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommentThread = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/commentThreads
/// Updates an existing resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_youtube_v3_update_comment_threads_builder()` + `youtube_youtube_v3_update_comment_threads_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_youtube_v3_update_comment_threads(
    client: &SimpleHttpClient,
    part: Option<&str>,
    body: &CommentThread,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentThread>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_youtube_v3_update_comment_threads_builder(client, part, body)?;
    youtube_youtube_v3_update_comment_threads_execute(builder)
}

/// GET youtube/v3/liveChat/messages/stream
/// Allows a user to load live chat through a server-streamed RPC.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `youtube_youtube_v3_live_chat_messages_stream_execute()` to send, or `youtube_youtube_v3_live_chat_messages_stream` for simplest API.

pub fn youtube_youtube_v3_live_chat_messages_stream_builder(
    client: &SimpleHttpClient,
    hl: Option<&str>,
    liveChatId: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    part: Option<&str>,
    profileImageSize: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comyoutube/v3/liveChat/messages/stream",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "hl",
            hl.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "liveChatId",
            liveChatId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "part",
            part.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "profileImageSize",
            profileImageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET youtube/v3/liveChat/messages/stream
/// Allows a user to load live chat through a server-streamed RPC.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `youtube_youtube_v3_live_chat_messages_stream_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from youtube_youtube_v3_live_chat_messages_stream_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_youtube_v3_live_chat_messages_stream_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiveChatMessageListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LiveChatMessageListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET youtube/v3/liveChat/messages/stream
/// Allows a user to load live chat through a server-streamed RPC.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `youtube_youtube_v3_live_chat_messages_stream_builder()` + `youtube_youtube_v3_live_chat_messages_stream_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn youtube_youtube_v3_live_chat_messages_stream(
    client: &SimpleHttpClient,
    hl: Option<&str>,
    liveChatId: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    part: Option<&str>,
    profileImageSize: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LiveChatMessageListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = youtube_youtube_v3_live_chat_messages_stream_builder(
        client,
        hl,
        liveChatId,
        maxResults,
        pageToken,
        part,
        profileImageSize,
    )?;
    youtube_youtube_v3_live_chat_messages_stream_execute(builder)
}
