//! Auto-generated API clients for gcp/cloudsearch.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/debug/{+name}:checkAccess
/// Checks whether an item is accessible by specified principal. Principal must be a user; groups and domain values aren't supported. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_debug_datasources_items_check_access_execute()` to send, or `cloudsearch_debug_datasources_items_check_access` for simplest API.

pub fn cloudsearch_debug_datasources_items_check_access_builder(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
    body: &Principal,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/debug/{+name}:checkAccess", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/debug/{+name}:checkAccess
/// Checks whether an item is accessible by specified principal. Principal must be a user; groups and domain values aren't supported. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_debug_datasources_items_check_access_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_debug_datasources_items_check_access_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_datasources_items_check_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/debug/{+name}:checkAccess
/// Checks whether an item is accessible by specified principal. Principal must be a user; groups and domain values aren't supported. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_debug_datasources_items_check_access_builder()` + `cloudsearch_debug_datasources_items_check_access_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_datasources_items_check_access(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
    body: &Principal,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_debug_datasources_items_check_access_builder(
        client,
        name,
        debugOptions_enableDebugging,
        body,
    )?;
    cloudsearch_debug_datasources_items_check_access_execute(builder)
}

/// GET v1/debug/{+name}/items:searchByViewUrl
/// Fetches the item whose `viewUrl` exactly matches that of the URL provided in the request. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_debug_datasources_items_search_by_view_url_execute()` to send, or `cloudsearch_debug_datasources_items_search_by_view_url` for simplest API.

pub fn cloudsearch_debug_datasources_items_search_by_view_url_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SearchItemsByViewUrlRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/debug/{+name}/items:searchByViewUrl",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/debug/{+name}/items:searchByViewUrl
/// Fetches the item whose `viewUrl` exactly matches that of the URL provided in the request. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_debug_datasources_items_search_by_view_url_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_debug_datasources_items_search_by_view_url_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_datasources_items_search_by_view_url_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchItemsByViewUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchItemsByViewUrlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/debug/{+name}/items:searchByViewUrl
/// Fetches the item whose `viewUrl` exactly matches that of the URL provided in the request. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_debug_datasources_items_search_by_view_url_builder()` + `cloudsearch_debug_datasources_items_search_by_view_url_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_datasources_items_search_by_view_url(
    client: &SimpleHttpClient,
    name: &str,
    body: &SearchItemsByViewUrlRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchItemsByViewUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudsearch_debug_datasources_items_search_by_view_url_builder(client, name, body)?;
    cloudsearch_debug_datasources_items_search_by_view_url_execute(builder)
}

/// GET v1/debug/{+parent}/unmappedids
/// List all unmapped identities for a specific item. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_debug_datasources_items_unmappedids_list_execute()` to send, or `cloudsearch_debug_datasources_items_unmappedids_list` for simplest API.

pub fn cloudsearch_debug_datasources_items_unmappedids_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    debugOptions_enableDebugging: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/debug/{+parent}/unmappedids",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/debug/{+parent}/unmappedids
/// List all unmapped identities for a specific item. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_debug_datasources_items_unmappedids_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_debug_datasources_items_unmappedids_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_datasources_items_unmappedids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUnmappedIdentitiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUnmappedIdentitiesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/debug/{+parent}/unmappedids
/// List all unmapped identities for a specific item. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_debug_datasources_items_unmappedids_list_builder()` + `cloudsearch_debug_datasources_items_unmappedids_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_datasources_items_unmappedids_list(
    client: &SimpleHttpClient,
    parent: &str,
    debugOptions_enableDebugging: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUnmappedIdentitiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_debug_datasources_items_unmappedids_list_builder(
        client,
        parent,
        debugOptions_enableDebugging,
        pageSize,
        pageToken,
    )?;
    cloudsearch_debug_datasources_items_unmappedids_list_execute(builder)
}

/// GET v1/debug/{+parent}/items:forunmappedidentity
/// Lists names of items associated with an unmapped identity. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_debug_identitysources_items_list_forunmappedidentity_execute()` to send, or `cloudsearch_debug_identitysources_items_list_forunmappedidentity` for simplest API.

pub fn cloudsearch_debug_identitysources_items_list_forunmappedidentity_builder(
    client: &SimpleHttpClient,
    parent: &str,
    debugOptions_enableDebugging: Option<bool>,
    groupResourceName: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    userResourceName: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/debug/{+parent}/items:forunmappedidentity",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "groupResourceName",
            groupResourceName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "userResourceName",
            userResourceName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/debug/{+parent}/items:forunmappedidentity
/// Lists names of items associated with an unmapped identity. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_debug_identitysources_items_list_forunmappedidentity_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_debug_identitysources_items_list_forunmappedidentity_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_identitysources_items_list_forunmappedidentity_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListItemNamesForUnmappedIdentityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListItemNamesForUnmappedIdentityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/debug/{+parent}/items:forunmappedidentity
/// Lists names of items associated with an unmapped identity. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_debug_identitysources_items_list_forunmappedidentity_builder()` + `cloudsearch_debug_identitysources_items_list_forunmappedidentity_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_identitysources_items_list_forunmappedidentity(
    client: &SimpleHttpClient,
    parent: &str,
    debugOptions_enableDebugging: Option<bool>,
    groupResourceName: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    userResourceName: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListItemNamesForUnmappedIdentityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_debug_identitysources_items_list_forunmappedidentity_builder(
        client,
        parent,
        debugOptions_enableDebugging,
        groupResourceName,
        pageSize,
        pageToken,
        userResourceName,
    )?;
    cloudsearch_debug_identitysources_items_list_forunmappedidentity_execute(builder)
}

/// GET v1/debug/{+parent}/unmappedids
/// Lists unmapped user identities for an identity source. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_debug_identitysources_unmappedids_list_execute()` to send, or `cloudsearch_debug_identitysources_unmappedids_list` for simplest API.

pub fn cloudsearch_debug_identitysources_unmappedids_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    debugOptions_enableDebugging: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resolutionStatusCode: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/debug/{+parent}/unmappedids",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resolutionStatusCode",
            resolutionStatusCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/debug/{+parent}/unmappedids
/// Lists unmapped user identities for an identity source. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_debug_identitysources_unmappedids_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_debug_identitysources_unmappedids_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_identitysources_unmappedids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUnmappedIdentitiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUnmappedIdentitiesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/debug/{+parent}/unmappedids
/// Lists unmapped user identities for an identity source. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_debug_identitysources_unmappedids_list_builder()` + `cloudsearch_debug_identitysources_unmappedids_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_identitysources_unmappedids_list(
    client: &SimpleHttpClient,
    parent: &str,
    debugOptions_enableDebugging: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    resolutionStatusCode: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUnmappedIdentitiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_debug_identitysources_unmappedids_list_builder(
        client,
        parent,
        debugOptions_enableDebugging,
        pageSize,
        pageToken,
        resolutionStatusCode,
    )?;
    cloudsearch_debug_identitysources_unmappedids_list_execute(builder)
}

/// GET v1/indexing/{+name}/schema
/// Deletes the schema of a data source. **Note:** This API requires an admin or service account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_delete_schema_execute()` to send, or `cloudsearch_indexing_datasources_delete_schema` for simplest API.

pub fn cloudsearch_indexing_datasources_delete_schema_builder(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/indexing/{+name}/schema", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/indexing/{+name}/schema
/// Deletes the schema of a data source. **Note:** This API requires an admin or service account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_indexing_datasources_delete_schema_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_indexing_datasources_delete_schema_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_delete_schema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/indexing/{+name}/schema
/// Deletes the schema of a data source. **Note:** This API requires an admin or service account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_delete_schema_builder()` + `cloudsearch_indexing_datasources_delete_schema_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_delete_schema(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_delete_schema_builder(
        client,
        name,
        debugOptions_enableDebugging,
    )?;
    cloudsearch_indexing_datasources_delete_schema_execute(builder)
}

/// GET v1/indexing/{+name}/schema
/// Gets the schema of a data source. **Note:** This API requires an admin or service account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_get_schema_execute()` to send, or `cloudsearch_indexing_datasources_get_schema` for simplest API.

pub fn cloudsearch_indexing_datasources_get_schema_builder(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/indexing/{+name}/schema", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/indexing/{+name}/schema
/// Gets the schema of a data source. **Note:** This API requires an admin or service account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_indexing_datasources_get_schema_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_indexing_datasources_get_schema_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_get_schema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Schema>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Schema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/indexing/{+name}/schema
/// Gets the schema of a data source. **Note:** This API requires an admin or service account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_get_schema_builder()` + `cloudsearch_indexing_datasources_get_schema_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_get_schema(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Schema>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_get_schema_builder(
        client,
        name,
        debugOptions_enableDebugging,
    )?;
    cloudsearch_indexing_datasources_get_schema_execute(builder)
}

/// GET v1/indexing/{+name}/schema
/// Updates the schema of a data source. This method does not perform incremental updates to the schema. Instead, this method updates the schema by overwriting the entire schema. **Note:** This API requires an admin or service account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_update_schema_execute()` to send, or `cloudsearch_indexing_datasources_update_schema` for simplest API.

pub fn cloudsearch_indexing_datasources_update_schema_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpdateSchemaRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/indexing/{+name}/schema", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/indexing/{+name}/schema
/// Updates the schema of a data source. This method does not perform incremental updates to the schema. Instead, this method updates the schema by overwriting the entire schema. **Note:** This API requires an admin or service account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_indexing_datasources_update_schema_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_indexing_datasources_update_schema_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_update_schema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/indexing/{+name}/schema
/// Updates the schema of a data source. This method does not perform incremental updates to the schema. Instead, this method updates the schema by overwriting the entire schema. **Note:** This API requires an admin or service account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_update_schema_builder()` + `cloudsearch_indexing_datasources_update_schema_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_update_schema(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpdateSchemaRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_update_schema_builder(client, name, body)?;
    cloudsearch_indexing_datasources_update_schema_execute(builder)
}

/// GET v1/indexing/{+name}
/// Deletes Item resource for the specified resource name. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_delete_execute()` to send, or `cloudsearch_indexing_datasources_items_delete` for simplest API.

pub fn cloudsearch_indexing_datasources_items_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    connectorName: Option<&str>,
    debugOptions_enableDebugging: Option<bool>,
    mode: Option<&str>,
    version: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/indexing/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "connectorName",
            connectorName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "mode",
            mode.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "version",
            version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/indexing/{+name}
/// Deletes Item resource for the specified resource name. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_indexing_datasources_items_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_indexing_datasources_items_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/indexing/{+name}
/// Deletes Item resource for the specified resource name. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_delete_builder()` + `cloudsearch_indexing_datasources_items_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_delete(
    client: &SimpleHttpClient,
    name: &str,
    connectorName: Option<&str>,
    debugOptions_enableDebugging: Option<bool>,
    mode: Option<&str>,
    version: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_delete_builder(
        client,
        name,
        connectorName,
        debugOptions_enableDebugging,
        mode,
        version,
    )?;
    cloudsearch_indexing_datasources_items_delete_execute(builder)
}

/// GET v1/indexing/{+name}/items:deleteQueueItems
/// Deletes all items in a queue. This method is useful for deleting stale items. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_delete_queue_items_execute()` to send, or `cloudsearch_indexing_datasources_items_delete_queue_items` for simplest API.

pub fn cloudsearch_indexing_datasources_items_delete_queue_items_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DeleteQueueItemsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/indexing/{+name}/items:deleteQueueItems",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/indexing/{+name}/items:deleteQueueItems
/// Deletes all items in a queue. This method is useful for deleting stale items. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_indexing_datasources_items_delete_queue_items_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_indexing_datasources_items_delete_queue_items_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_delete_queue_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/indexing/{+name}/items:deleteQueueItems
/// Deletes all items in a queue. This method is useful for deleting stale items. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_delete_queue_items_builder()` + `cloudsearch_indexing_datasources_items_delete_queue_items_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_delete_queue_items(
    client: &SimpleHttpClient,
    name: &str,
    body: &DeleteQueueItemsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudsearch_indexing_datasources_items_delete_queue_items_builder(client, name, body)?;
    cloudsearch_indexing_datasources_items_delete_queue_items_execute(builder)
}

/// GET v1/indexing/{+name}
/// Gets Item resource by item name. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_get_execute()` to send, or `cloudsearch_indexing_datasources_items_get` for simplest API.

pub fn cloudsearch_indexing_datasources_items_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    connectorName: Option<&str>,
    debugOptions_enableDebugging: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/indexing/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "connectorName",
            connectorName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/indexing/{+name}
/// Gets Item resource by item name. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_indexing_datasources_items_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_indexing_datasources_items_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Item>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Item = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/indexing/{+name}
/// Gets Item resource by item name. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_get_builder()` + `cloudsearch_indexing_datasources_items_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_get(
    client: &SimpleHttpClient,
    name: &str,
    connectorName: Option<&str>,
    debugOptions_enableDebugging: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Item>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_get_builder(
        client,
        name,
        connectorName,
        debugOptions_enableDebugging,
    )?;
    cloudsearch_indexing_datasources_items_get_execute(builder)
}

/// GET v1/indexing/{+name}:index
/// Updates Item ACL, metadata, and content. It will insert the Item if it does not exist. This method does not support partial updates. Fields with no provided values are cleared out in the Cloud Search index. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_index_execute()` to send, or `cloudsearch_indexing_datasources_items_index` for simplest API.

pub fn cloudsearch_indexing_datasources_items_index_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &IndexItemRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/indexing/{+name}:index", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/indexing/{+name}:index
/// Updates Item ACL, metadata, and content. It will insert the Item if it does not exist. This method does not support partial updates. Fields with no provided values are cleared out in the Cloud Search index. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_indexing_datasources_items_index_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_indexing_datasources_items_index_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_index_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/indexing/{+name}:index
/// Updates Item ACL, metadata, and content. It will insert the Item if it does not exist. This method does not support partial updates. Fields with no provided values are cleared out in the Cloud Search index. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_index_builder()` + `cloudsearch_indexing_datasources_items_index_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_index(
    client: &SimpleHttpClient,
    name: &str,
    body: &IndexItemRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_index_builder(client, name, body)?;
    cloudsearch_indexing_datasources_items_index_execute(builder)
}

/// GET v1/indexing/{+name}/items
/// Lists all or a subset of Item resources. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_list_execute()` to send, or `cloudsearch_indexing_datasources_items_list` for simplest API.

pub fn cloudsearch_indexing_datasources_items_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    brief: Option<bool>,
    connectorName: Option<&str>,
    debugOptions_enableDebugging: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/indexing/{+name}/items", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "brief",
            brief
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "connectorName",
            connectorName
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/indexing/{+name}/items
/// Lists all or a subset of Item resources. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_indexing_datasources_items_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_indexing_datasources_items_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListItemsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListItemsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/indexing/{+name}/items
/// Lists all or a subset of Item resources. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_list_builder()` + `cloudsearch_indexing_datasources_items_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_list(
    client: &SimpleHttpClient,
    name: &str,
    brief: Option<bool>,
    connectorName: Option<&str>,
    debugOptions_enableDebugging: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListItemsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_list_builder(
        client,
        name,
        brief,
        connectorName,
        debugOptions_enableDebugging,
        pageSize,
        pageToken,
    )?;
    cloudsearch_indexing_datasources_items_list_execute(builder)
}

/// GET v1/indexing/{+name}/items:poll
/// Polls for unreserved items from the indexing queue and marks a set as reserved, starting with items that have the oldest timestamp from the highest priority ItemStatus. The priority order is as follows: `ERROR` MODIFIED NEW_ITEM ACCEPTED Reserving items ensures that polling from other threads cannot create overlapping sets. After handling the reserved items, the client should put items back into the unreserved state, either by calling index, or by calling push with the type REQUEUE. Items automatically become available (unreserved) after 4 hours even if no update or push method is called. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_poll_execute()` to send, or `cloudsearch_indexing_datasources_items_poll` for simplest API.

pub fn cloudsearch_indexing_datasources_items_poll_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &PollItemsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/indexing/{+name}/items:poll",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/indexing/{+name}/items:poll
/// Polls for unreserved items from the indexing queue and marks a set as reserved, starting with items that have the oldest timestamp from the highest priority ItemStatus. The priority order is as follows: `ERROR` MODIFIED NEW_ITEM ACCEPTED Reserving items ensures that polling from other threads cannot create overlapping sets. After handling the reserved items, the client should put items back into the unreserved state, either by calling index, or by calling push with the type REQUEUE. Items automatically become available (unreserved) after 4 hours even if no update or push method is called. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_indexing_datasources_items_poll_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_indexing_datasources_items_poll_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_poll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PollItemsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PollItemsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/indexing/{+name}/items:poll
/// Polls for unreserved items from the indexing queue and marks a set as reserved, starting with items that have the oldest timestamp from the highest priority ItemStatus. The priority order is as follows: `ERROR` MODIFIED NEW_ITEM ACCEPTED Reserving items ensures that polling from other threads cannot create overlapping sets. After handling the reserved items, the client should put items back into the unreserved state, either by calling index, or by calling push with the type REQUEUE. Items automatically become available (unreserved) after 4 hours even if no update or push method is called. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_poll_builder()` + `cloudsearch_indexing_datasources_items_poll_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_poll(
    client: &SimpleHttpClient,
    name: &str,
    body: &PollItemsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PollItemsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_poll_builder(client, name, body)?;
    cloudsearch_indexing_datasources_items_poll_execute(builder)
}

/// GET v1/indexing/{+name}:push
/// Pushes an item onto a queue for later polling and updating. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_push_execute()` to send, or `cloudsearch_indexing_datasources_items_push` for simplest API.

pub fn cloudsearch_indexing_datasources_items_push_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &PushItemRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/indexing/{+name}:push", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/indexing/{+name}:push
/// Pushes an item onto a queue for later polling and updating. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_indexing_datasources_items_push_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_indexing_datasources_items_push_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_push_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Item>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Item = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/indexing/{+name}:push
/// Pushes an item onto a queue for later polling and updating. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_push_builder()` + `cloudsearch_indexing_datasources_items_push_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_push(
    client: &SimpleHttpClient,
    name: &str,
    body: &PushItemRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Item>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_push_builder(client, name, body)?;
    cloudsearch_indexing_datasources_items_push_execute(builder)
}

/// GET v1/indexing/{+name}/items:unreserve
/// Unreserves all items from a queue, making them all eligible to be polled. This method is useful for resetting the indexing queue after a connector has been restarted. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_unreserve_execute()` to send, or `cloudsearch_indexing_datasources_items_unreserve` for simplest API.

pub fn cloudsearch_indexing_datasources_items_unreserve_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UnreserveItemsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/indexing/{+name}/items:unreserve",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/indexing/{+name}/items:unreserve
/// Unreserves all items from a queue, making them all eligible to be polled. This method is useful for resetting the indexing queue after a connector has been restarted. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_indexing_datasources_items_unreserve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_indexing_datasources_items_unreserve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_unreserve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/indexing/{+name}/items:unreserve
/// Unreserves all items from a queue, making them all eligible to be polled. This method is useful for resetting the indexing queue after a connector has been restarted. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_unreserve_builder()` + `cloudsearch_indexing_datasources_items_unreserve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_unreserve(
    client: &SimpleHttpClient,
    name: &str,
    body: &UnreserveItemsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_unreserve_builder(client, name, body)?;
    cloudsearch_indexing_datasources_items_unreserve_execute(builder)
}

/// GET v1/indexing/{+name}:upload
/// Creates an upload session for uploading item content. For items smaller than 100 KB, it's easier to embed the content inline within an index request. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_upload_execute()` to send, or `cloudsearch_indexing_datasources_items_upload` for simplest API.

pub fn cloudsearch_indexing_datasources_items_upload_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &StartUploadItemRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/indexing/{+name}:upload", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/indexing/{+name}:upload
/// Creates an upload session for uploading item content. For items smaller than 100 KB, it's easier to embed the content inline within an index request. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_indexing_datasources_items_upload_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_indexing_datasources_items_upload_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UploadItemRef>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UploadItemRef = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/indexing/{+name}:upload
/// Creates an upload session for uploading item content. For items smaller than 100 KB, it's easier to embed the content inline within an index request. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_upload_builder()` + `cloudsearch_indexing_datasources_items_upload_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_upload(
    client: &SimpleHttpClient,
    name: &str,
    body: &StartUploadItemRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UploadItemRef>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_upload_builder(client, name, body)?;
    cloudsearch_indexing_datasources_items_upload_execute(builder)
}

/// GET v1/media/{+resourceName}
/// Uploads media for indexing. The upload endpoint supports direct and resumable upload protocols and is intended for large items that can not be [inlined during index requests](<https://developers.google.`com/workspace/cloud-search/docs/reference/rest/v1/indexing`.datasources.items#itemcontent>). To index large content: 1. Call indexing.datasources.items.upload with the item name to begin an upload session and retrieve the UploadItemRef. 1. Call media.upload to upload the content, as a streaming request, using the same resource name from the UploadItemRef from step 1. 1. Call indexing.datasources.items.index to index the item. Populate the [ItemContent](/cloud-`search/docs/reference/rest/v1/indexing`.datasources.items#ItemContent) with the UploadItemRef from step 1. For additional information, see [Create a content connector using the REST API](<https://developers.google.`com/workspace/cloud-search/docs/guides/content-connector`#rest>). **Note:** This API requires a service account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_media_upload_execute()` to send, or `cloudsearch_media_upload` for simplest API.

pub fn cloudsearch_media_upload_builder(
    client: &SimpleHttpClient,
    resourceName: &str,
    body: &Media,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/media/{+resourceName}",
        resourceName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/media/{+resourceName}
/// Uploads media for indexing. The upload endpoint supports direct and resumable upload protocols and is intended for large items that can not be [inlined during index requests](<https://developers.google.`com/workspace/cloud-search/docs/reference/rest/v1/indexing`.datasources.items#itemcontent>). To index large content: 1. Call indexing.datasources.items.upload with the item name to begin an upload session and retrieve the UploadItemRef. 1. Call media.upload to upload the content, as a streaming request, using the same resource name from the UploadItemRef from step 1. 1. Call indexing.datasources.items.index to index the item. Populate the [ItemContent](/cloud-`search/docs/reference/rest/v1/indexing`.datasources.items#ItemContent) with the UploadItemRef from step 1. For additional information, see [Create a content connector using the REST API](<https://developers.google.`com/workspace/cloud-search/docs/guides/content-connector`#rest>). **Note:** This API requires a service account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_media_upload_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_media_upload_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_media_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Media>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Media = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/media/{+resourceName}
/// Uploads media for indexing. The upload endpoint supports direct and resumable upload protocols and is intended for large items that can not be [inlined during index requests](<https://developers.google.`com/workspace/cloud-search/docs/reference/rest/v1/indexing`.datasources.items#itemcontent>). To index large content: 1. Call indexing.datasources.items.upload with the item name to begin an upload session and retrieve the UploadItemRef. 1. Call media.upload to upload the content, as a streaming request, using the same resource name from the UploadItemRef from step 1. 1. Call indexing.datasources.items.index to index the item. Populate the [ItemContent](/cloud-`search/docs/reference/rest/v1/indexing`.datasources.items#ItemContent) with the UploadItemRef from step 1. For additional information, see [Create a content connector using the REST API](<https://developers.google.`com/workspace/cloud-search/docs/guides/content-connector`#rest>). **Note:** This API requires a service account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_media_upload_builder()` + `cloudsearch_media_upload_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_media_upload(
    client: &SimpleHttpClient,
    resourceName: &str,
    body: &Media,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Media>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_media_upload_builder(client, resourceName, body)?;
    cloudsearch_media_upload_execute(builder)
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_operations_get_execute()` to send, or `cloudsearch_operations_get` for simplest API.

pub fn cloudsearch_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_operations_get_builder()` + `cloudsearch_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_operations_get_builder(client, name)?;
    cloudsearch_operations_get_execute(builder)
}

/// GET v1/{+name}/lro
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_operations_lro_list_execute()` to send, or `cloudsearch_operations_lro_list` for simplest API.

pub fn cloudsearch_operations_lro_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/lro", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}/lro
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_operations_lro_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_operations_lro_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_operations_lro_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/lro
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_operations_lro_list_builder()` + `cloudsearch_operations_lro_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_operations_lro_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_operations_lro_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    cloudsearch_operations_lro_list_execute(builder)
}

/// GET v1/query:removeActivity
/// Provides functionality to remove logged activity for a user. Currently to be used only for Chat 1p clients **Note:** This API requires a standard end user account to execute. A service account can't perform Remove Activity requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_query_remove_activity_execute()` to send, or `cloudsearch_query_remove_activity` for simplest API.

pub fn cloudsearch_query_remove_activity_builder(
    client: &SimpleHttpClient,
    body: &RemoveActivityRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/query:removeActivity",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/query:removeActivity
/// Provides functionality to remove logged activity for a user. Currently to be used only for Chat 1p clients **Note:** This API requires a standard end user account to execute. A service account can't perform Remove Activity requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_query_remove_activity_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_query_remove_activity_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_remove_activity_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemoveActivityResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemoveActivityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/query:removeActivity
/// Provides functionality to remove logged activity for a user. Currently to be used only for Chat 1p clients **Note:** This API requires a standard end user account to execute. A service account can't perform Remove Activity requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_query_remove_activity_builder()` + `cloudsearch_query_remove_activity_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_remove_activity(
    client: &SimpleHttpClient,
    body: &RemoveActivityRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemoveActivityResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_query_remove_activity_builder(client, body)?;
    cloudsearch_query_remove_activity_execute(builder)
}

/// GET v1/query/search
/// The Cloud Search Query API provides the search method, which returns the most relevant results from a user query. The results can come from Google Workspace apps, such as Gmail or Google Drive, or they can come from data that you have indexed from a third party. **Note:** This API requires a standard end user account to execute. A service account can't perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_query_search_execute()` to send, or `cloudsearch_query_search` for simplest API.

pub fn cloudsearch_query_search_builder(
    client: &SimpleHttpClient,
    body: &SearchRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/query/search",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/query/search
/// The Cloud Search Query API provides the search method, which returns the most relevant results from a user query. The results can come from Google Workspace apps, such as Gmail or Google Drive, or they can come from data that you have indexed from a third party. **Note:** This API requires a standard end user account to execute. A service account can't perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_query_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_query_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/query/search
/// The Cloud Search Query API provides the search method, which returns the most relevant results from a user query. The results can come from Google Workspace apps, such as Gmail or Google Drive, or they can come from data that you have indexed from a third party. **Note:** This API requires a standard end user account to execute. A service account can't perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_query_search_builder()` + `cloudsearch_query_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_search(
    client: &SimpleHttpClient,
    body: &SearchRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_query_search_builder(client, body)?;
    cloudsearch_query_search_execute(builder)
}

/// GET v1/query/suggest
/// Provides suggestions for autocompleting the query. **Note:** This API requires a standard end user account to execute. A service account can't perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_query_suggest_execute()` to send, or `cloudsearch_query_suggest` for simplest API.

pub fn cloudsearch_query_suggest_builder(
    client: &SimpleHttpClient,
    body: &SuggestRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/query/suggest",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/query/suggest
/// Provides suggestions for autocompleting the query. **Note:** This API requires a standard end user account to execute. A service account can't perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_query_suggest_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_query_suggest_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_suggest_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SuggestResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SuggestResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/query/suggest
/// Provides suggestions for autocompleting the query. **Note:** This API requires a standard end user account to execute. A service account can't perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_query_suggest_builder()` + `cloudsearch_query_suggest_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_suggest(
    client: &SimpleHttpClient,
    body: &SuggestRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SuggestResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_query_suggest_builder(client, body)?;
    cloudsearch_query_suggest_execute(builder)
}

/// GET v1/query/sources
/// Returns list of sources that user can use for Search and Suggest APIs. **Note:** This API requires a standard end user account to execute. A service account can't perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_query_sources_list_execute()` to send, or `cloudsearch_query_sources_list` for simplest API.

pub fn cloudsearch_query_sources_list_builder(
    client: &SimpleHttpClient,
    pageToken: Option<&str>,
    requestOptions_debugOptions_enableDebugging: Option<bool>,
    requestOptions_languageCode: Option<&str>,
    requestOptions_searchApplicationId: Option<&str>,
    requestOptions_timeZone: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/query/sources",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestOptions_debugOptions_enableDebugging",
            requestOptions_debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestOptions_languageCode",
            requestOptions_languageCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestOptions_searchApplicationId",
            requestOptions_searchApplicationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestOptions_timeZone",
            requestOptions_timeZone
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/query/sources
/// Returns list of sources that user can use for Search and Suggest APIs. **Note:** This API requires a standard end user account to execute. A service account can't perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_query_sources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_query_sources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListQuerySourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListQuerySourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/query/sources
/// Returns list of sources that user can use for Search and Suggest APIs. **Note:** This API requires a standard end user account to execute. A service account can't perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_query_sources_list_builder()` + `cloudsearch_query_sources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_sources_list(
    client: &SimpleHttpClient,
    pageToken: Option<&str>,
    requestOptions_debugOptions_enableDebugging: Option<bool>,
    requestOptions_languageCode: Option<&str>,
    requestOptions_searchApplicationId: Option<&str>,
    requestOptions_timeZone: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListQuerySourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_query_sources_list_builder(
        client,
        pageToken,
        requestOptions_debugOptions_enableDebugging,
        requestOptions_languageCode,
        requestOptions_searchApplicationId,
        requestOptions_timeZone,
    )?;
    cloudsearch_query_sources_list_execute(builder)
}

/// GET v1/settings/customer
/// Get customer settings. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_get_customer_execute()` to send, or `cloudsearch_settings_get_customer` for simplest API.

pub fn cloudsearch_settings_get_customer_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/customer",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/customer
/// Get customer settings. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_get_customer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_get_customer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_get_customer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomerSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomerSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/customer
/// Get customer settings. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_get_customer_builder()` + `cloudsearch_settings_get_customer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_get_customer(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomerSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_get_customer_builder(client)?;
    cloudsearch_settings_get_customer_execute(builder)
}

/// GET v1/settings/customer
/// Update customer settings. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_update_customer_execute()` to send, or `cloudsearch_settings_update_customer` for simplest API.

pub fn cloudsearch_settings_update_customer_builder(
    client: &SimpleHttpClient,
    updateMask: Option<&str>,
    body: &CustomerSettings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/customer",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/settings/customer
/// Update customer settings. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_update_customer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_update_customer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_update_customer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/customer
/// Update customer settings. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_update_customer_builder()` + `cloudsearch_settings_update_customer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_update_customer(
    client: &SimpleHttpClient,
    updateMask: Option<&str>,
    body: &CustomerSettings,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_update_customer_builder(client, updateMask, body)?;
    cloudsearch_settings_update_customer_execute(builder)
}

/// GET v1/settings/datasources
/// Creates a datasource. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_datasources_create_execute()` to send, or `cloudsearch_settings_datasources_create` for simplest API.

pub fn cloudsearch_settings_datasources_create_builder(
    client: &SimpleHttpClient,
    body: &DataSource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/datasources",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/settings/datasources
/// Creates a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_datasources_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_datasources_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/datasources
/// Creates a datasource. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_datasources_create_builder()` + `cloudsearch_settings_datasources_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_create(
    client: &SimpleHttpClient,
    body: &DataSource,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_datasources_create_builder(client, body)?;
    cloudsearch_settings_datasources_create_execute(builder)
}

/// GET v1/settings/{+name}
/// Deletes a datasource. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_datasources_delete_execute()` to send, or `cloudsearch_settings_datasources_delete` for simplest API.

pub fn cloudsearch_settings_datasources_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/settings/{+name}
/// Deletes a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_datasources_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_datasources_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/{+name}
/// Deletes a datasource. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_datasources_delete_builder()` + `cloudsearch_settings_datasources_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_delete(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_datasources_delete_builder(
        client,
        name,
        debugOptions_enableDebugging,
    )?;
    cloudsearch_settings_datasources_delete_execute(builder)
}

/// GET v1/settings/{+name}
/// Gets a datasource. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_datasources_get_execute()` to send, or `cloudsearch_settings_datasources_get` for simplest API.

pub fn cloudsearch_settings_datasources_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/settings/{+name}
/// Gets a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_datasources_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_datasources_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataSource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/{+name}
/// Gets a datasource. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_datasources_get_builder()` + `cloudsearch_settings_datasources_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_get(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudsearch_settings_datasources_get_builder(client, name, debugOptions_enableDebugging)?;
    cloudsearch_settings_datasources_get_execute(builder)
}

/// GET v1/settings/datasources
/// Lists datasources. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_datasources_list_execute()` to send, or `cloudsearch_settings_datasources_list` for simplest API.

pub fn cloudsearch_settings_datasources_list_builder(
    client: &SimpleHttpClient,
    debugOptions_enableDebugging: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/datasources",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/settings/datasources
/// Lists datasources. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_datasources_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_datasources_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataSourceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDataSourceResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/datasources
/// Lists datasources. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_datasources_list_builder()` + `cloudsearch_settings_datasources_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_list(
    client: &SimpleHttpClient,
    debugOptions_enableDebugging: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataSourceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_datasources_list_builder(
        client,
        debugOptions_enableDebugging,
        pageSize,
        pageToken,
    )?;
    cloudsearch_settings_datasources_list_execute(builder)
}

/// GET v1/settings/{+name}
/// Updates a datasource. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_datasources_patch_execute()` to send, or `cloudsearch_settings_datasources_patch` for simplest API.

pub fn cloudsearch_settings_datasources_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
    updateMask: Option<&str>,
    body: &DataSource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/settings/{+name}
/// Updates a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_datasources_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_datasources_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/{+name}
/// Updates a datasource. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_datasources_patch_builder()` + `cloudsearch_settings_datasources_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_patch(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
    updateMask: Option<&str>,
    body: &DataSource,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_datasources_patch_builder(
        client,
        name,
        debugOptions_enableDebugging,
        updateMask,
        body,
    )?;
    cloudsearch_settings_datasources_patch_execute(builder)
}

/// GET v1/settings/{+name}
/// Updates a datasource. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_datasources_update_execute()` to send, or `cloudsearch_settings_datasources_update` for simplest API.

pub fn cloudsearch_settings_datasources_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpdateDataSourceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/settings/{+name}
/// Updates a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_datasources_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_datasources_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/{+name}
/// Updates a datasource. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_datasources_update_builder()` + `cloudsearch_settings_datasources_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_update(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpdateDataSourceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_datasources_update_builder(client, name, body)?;
    cloudsearch_settings_datasources_update_execute(builder)
}

/// GET v1/settings/searchapplications
/// Creates a search application. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_searchapplications_create_execute()` to send, or `cloudsearch_settings_searchapplications_create` for simplest API.

pub fn cloudsearch_settings_searchapplications_create_builder(
    client: &SimpleHttpClient,
    body: &SearchApplication,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/searchapplications",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/settings/searchapplications
/// Creates a search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_searchapplications_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_searchapplications_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/searchapplications
/// Creates a search application. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_searchapplications_create_builder()` + `cloudsearch_settings_searchapplications_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_create(
    client: &SimpleHttpClient,
    body: &SearchApplication,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_searchapplications_create_builder(client, body)?;
    cloudsearch_settings_searchapplications_create_execute(builder)
}

/// GET v1/settings/{+name}
/// Deletes a search application. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_searchapplications_delete_execute()` to send, or `cloudsearch_settings_searchapplications_delete` for simplest API.

pub fn cloudsearch_settings_searchapplications_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/settings/{+name}
/// Deletes a search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_searchapplications_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_searchapplications_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/{+name}
/// Deletes a search application. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_searchapplications_delete_builder()` + `cloudsearch_settings_searchapplications_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_delete(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_searchapplications_delete_builder(
        client,
        name,
        debugOptions_enableDebugging,
    )?;
    cloudsearch_settings_searchapplications_delete_execute(builder)
}

/// GET v1/settings/{+name}
/// Gets the specified search application. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_searchapplications_get_execute()` to send, or `cloudsearch_settings_searchapplications_get` for simplest API.

pub fn cloudsearch_settings_searchapplications_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/settings/{+name}
/// Gets the specified search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_searchapplications_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_searchapplications_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchApplication>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchApplication = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/{+name}
/// Gets the specified search application. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_searchapplications_get_builder()` + `cloudsearch_settings_searchapplications_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_get(
    client: &SimpleHttpClient,
    name: &str,
    debugOptions_enableDebugging: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchApplication>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_searchapplications_get_builder(
        client,
        name,
        debugOptions_enableDebugging,
    )?;
    cloudsearch_settings_searchapplications_get_execute(builder)
}

/// GET v1/settings/searchapplications
/// Lists all search applications. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_searchapplications_list_execute()` to send, or `cloudsearch_settings_searchapplications_list` for simplest API.

pub fn cloudsearch_settings_searchapplications_list_builder(
    client: &SimpleHttpClient,
    debugOptions_enableDebugging: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/searchapplications",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "debugOptions_enableDebugging",
            debugOptions_enableDebugging
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/settings/searchapplications
/// Lists all search applications. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_searchapplications_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_searchapplications_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSearchApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSearchApplicationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/searchapplications
/// Lists all search applications. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_searchapplications_list_builder()` + `cloudsearch_settings_searchapplications_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_list(
    client: &SimpleHttpClient,
    debugOptions_enableDebugging: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSearchApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_searchapplications_list_builder(
        client,
        debugOptions_enableDebugging,
        pageSize,
        pageToken,
    )?;
    cloudsearch_settings_searchapplications_list_execute(builder)
}

/// GET v1/settings/{+name}
/// Updates a search application. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_searchapplications_patch_execute()` to send, or `cloudsearch_settings_searchapplications_patch` for simplest API.

pub fn cloudsearch_settings_searchapplications_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SearchApplication,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/settings/{+name}
/// Updates a search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_searchapplications_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_searchapplications_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/{+name}
/// Updates a search application. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_searchapplications_patch_builder()` + `cloudsearch_settings_searchapplications_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SearchApplication,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudsearch_settings_searchapplications_patch_builder(client, name, updateMask, body)?;
    cloudsearch_settings_searchapplications_patch_execute(builder)
}

/// GET v1/settings/{+name}:reset
/// Resets a search application to default settings. This will return an empty response. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_searchapplications_reset_execute()` to send, or `cloudsearch_settings_searchapplications_reset` for simplest API.

pub fn cloudsearch_settings_searchapplications_reset_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResetSearchApplicationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/{+name}:reset", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/settings/{+name}:reset
/// Resets a search application to default settings. This will return an empty response. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_searchapplications_reset_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_searchapplications_reset_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/{+name}:reset
/// Resets a search application to default settings. This will return an empty response. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_searchapplications_reset_builder()` + `cloudsearch_settings_searchapplications_reset_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_reset(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResetSearchApplicationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_searchapplications_reset_builder(client, name, body)?;
    cloudsearch_settings_searchapplications_reset_execute(builder)
}

/// GET v1/settings/{+name}
/// Updates a search application. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_searchapplications_update_execute()` to send, or `cloudsearch_settings_searchapplications_update` for simplest API.

pub fn cloudsearch_settings_searchapplications_update_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SearchApplication,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/settings/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/settings/{+name}
/// Updates a search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_settings_searchapplications_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_settings_searchapplications_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/{+name}
/// Updates a search application. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_searchapplications_update_builder()` + `cloudsearch_settings_searchapplications_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_update(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SearchApplication,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudsearch_settings_searchapplications_update_builder(client, name, updateMask, body)?;
    cloudsearch_settings_searchapplications_update_execute(builder)
}

/// GET v1/stats/index
/// Gets indexed item statistics aggreggated across all data sources. This API only returns statistics for previous dates; it doesn't return statistics for the current day. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_get_index_execute()` to send, or `cloudsearch_stats_get_index` for simplest API.

pub fn cloudsearch_stats_get_index_builder(
    client: &SimpleHttpClient,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/stats/index",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fromDate_day",
            fromDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_month",
            fromDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_year",
            fromDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_day",
            toDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_month",
            toDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_year",
            toDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/stats/index
/// Gets indexed item statistics aggreggated across all data sources. This API only returns statistics for previous dates; it doesn't return statistics for the current day. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_stats_get_index_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_stats_get_index_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_index_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerIndexStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetCustomerIndexStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/stats/index
/// Gets indexed item statistics aggreggated across all data sources. This API only returns statistics for previous dates; it doesn't return statistics for the current day. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_get_index_builder()` + `cloudsearch_stats_get_index_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_index(
    client: &SimpleHttpClient,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerIndexStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_get_index_builder(
        client,
        fromDate_day,
        fromDate_month,
        fromDate_year,
        toDate_day,
        toDate_month,
        toDate_year,
    )?;
    cloudsearch_stats_get_index_execute(builder)
}

/// GET v1/stats/query
/// Get the query statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_get_query_execute()` to send, or `cloudsearch_stats_get_query` for simplest API.

pub fn cloudsearch_stats_get_query_builder(
    client: &SimpleHttpClient,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/stats/query",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fromDate_day",
            fromDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_month",
            fromDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_year",
            fromDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_day",
            toDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_month",
            toDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_year",
            toDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/stats/query
/// Get the query statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_stats_get_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_stats_get_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerQueryStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetCustomerQueryStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/stats/query
/// Get the query statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_get_query_builder()` + `cloudsearch_stats_get_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_query(
    client: &SimpleHttpClient,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerQueryStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_get_query_builder(
        client,
        fromDate_day,
        fromDate_month,
        fromDate_year,
        toDate_day,
        toDate_month,
        toDate_year,
    )?;
    cloudsearch_stats_get_query_execute(builder)
}

/// GET v1/stats/searchapplication
/// Get search application stats for customer. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_get_searchapplication_execute()` to send, or `cloudsearch_stats_get_searchapplication` for simplest API.

pub fn cloudsearch_stats_get_searchapplication_builder(
    client: &SimpleHttpClient,
    endDate_day: Option<i32>,
    endDate_month: Option<i32>,
    endDate_year: Option<i32>,
    startDate_day: Option<i32>,
    startDate_month: Option<i32>,
    startDate_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/stats/searchapplication",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "endDate_day",
            endDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "endDate_month",
            endDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "endDate_year",
            endDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startDate_day",
            startDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startDate_month",
            startDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startDate_year",
            startDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/stats/searchapplication
/// Get search application stats for customer. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_stats_get_searchapplication_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_stats_get_searchapplication_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_searchapplication_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerSearchApplicationStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetCustomerSearchApplicationStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/stats/searchapplication
/// Get search application stats for customer. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_get_searchapplication_builder()` + `cloudsearch_stats_get_searchapplication_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_searchapplication(
    client: &SimpleHttpClient,
    endDate_day: Option<i32>,
    endDate_month: Option<i32>,
    endDate_year: Option<i32>,
    startDate_day: Option<i32>,
    startDate_month: Option<i32>,
    startDate_year: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerSearchApplicationStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_get_searchapplication_builder(
        client,
        endDate_day,
        endDate_month,
        endDate_year,
        startDate_day,
        startDate_month,
        startDate_year,
    )?;
    cloudsearch_stats_get_searchapplication_execute(builder)
}

/// GET v1/stats/session
/// Get the # of search sessions, % of successful sessions with a click query statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_get_session_execute()` to send, or `cloudsearch_stats_get_session` for simplest API.

pub fn cloudsearch_stats_get_session_builder(
    client: &SimpleHttpClient,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/stats/session",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fromDate_day",
            fromDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_month",
            fromDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_year",
            fromDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_day",
            toDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_month",
            toDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_year",
            toDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/stats/session
/// Get the # of search sessions, % of successful sessions with a click query statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_stats_get_session_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_stats_get_session_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_session_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerSessionStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetCustomerSessionStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/stats/session
/// Get the # of search sessions, % of successful sessions with a click query statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_get_session_builder()` + `cloudsearch_stats_get_session_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_session(
    client: &SimpleHttpClient,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerSessionStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_get_session_builder(
        client,
        fromDate_day,
        fromDate_month,
        fromDate_year,
        toDate_day,
        toDate_month,
        toDate_year,
    )?;
    cloudsearch_stats_get_session_execute(builder)
}

/// GET v1/stats/user
/// Get the users statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_get_user_execute()` to send, or `cloudsearch_stats_get_user` for simplest API.

pub fn cloudsearch_stats_get_user_builder(
    client: &SimpleHttpClient,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/stats/user",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fromDate_day",
            fromDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_month",
            fromDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_year",
            fromDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_day",
            toDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_month",
            toDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_year",
            toDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/stats/user
/// Get the users statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_stats_get_user_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_stats_get_user_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerUserStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetCustomerUserStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/stats/user
/// Get the users statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_get_user_builder()` + `cloudsearch_stats_get_user_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_user(
    client: &SimpleHttpClient,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerUserStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_get_user_builder(
        client,
        fromDate_day,
        fromDate_month,
        fromDate_year,
        toDate_day,
        toDate_month,
        toDate_year,
    )?;
    cloudsearch_stats_get_user_execute(builder)
}

/// GET v1/stats/index/{+name}
/// Gets indexed item statistics for a single data source. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_index_datasources_get_execute()` to send, or `cloudsearch_stats_index_datasources_get` for simplest API.

pub fn cloudsearch_stats_index_datasources_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/stats/index/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fromDate_day",
            fromDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_month",
            fromDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_year",
            fromDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_day",
            toDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_month",
            toDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_year",
            toDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/stats/index/{+name}
/// Gets indexed item statistics for a single data source. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_stats_index_datasources_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_stats_index_datasources_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_index_datasources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetDataSourceIndexStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetDataSourceIndexStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/stats/index/{+name}
/// Gets indexed item statistics for a single data source. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_index_datasources_get_builder()` + `cloudsearch_stats_index_datasources_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_index_datasources_get(
    client: &SimpleHttpClient,
    name: &str,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetDataSourceIndexStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_index_datasources_get_builder(
        client,
        name,
        fromDate_day,
        fromDate_month,
        fromDate_year,
        toDate_day,
        toDate_month,
        toDate_year,
    )?;
    cloudsearch_stats_index_datasources_get_execute(builder)
}

/// GET v1/stats/query/{+name}
/// Get the query statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_query_searchapplications_get_execute()` to send, or `cloudsearch_stats_query_searchapplications_get` for simplest API.

pub fn cloudsearch_stats_query_searchapplications_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/stats/query/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fromDate_day",
            fromDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_month",
            fromDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_year",
            fromDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_day",
            toDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_month",
            toDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_year",
            toDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/stats/query/{+name}
/// Get the query statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_stats_query_searchapplications_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_stats_query_searchapplications_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_query_searchapplications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetSearchApplicationQueryStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetSearchApplicationQueryStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/stats/query/{+name}
/// Get the query statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_query_searchapplications_get_builder()` + `cloudsearch_stats_query_searchapplications_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_query_searchapplications_get(
    client: &SimpleHttpClient,
    name: &str,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetSearchApplicationQueryStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_query_searchapplications_get_builder(
        client,
        name,
        fromDate_day,
        fromDate_month,
        fromDate_year,
        toDate_day,
        toDate_month,
        toDate_year,
    )?;
    cloudsearch_stats_query_searchapplications_get_execute(builder)
}

/// GET v1/stats/session/{+name}
/// Get the # of search sessions, % of successful sessions with a click query statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_session_searchapplications_get_execute()` to send, or `cloudsearch_stats_session_searchapplications_get` for simplest API.

pub fn cloudsearch_stats_session_searchapplications_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/stats/session/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fromDate_day",
            fromDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_month",
            fromDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_year",
            fromDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_day",
            toDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_month",
            toDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_year",
            toDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/stats/session/{+name}
/// Get the # of search sessions, % of successful sessions with a click query statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_stats_session_searchapplications_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_stats_session_searchapplications_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_session_searchapplications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetSearchApplicationSessionStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetSearchApplicationSessionStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/stats/session/{+name}
/// Get the # of search sessions, % of successful sessions with a click query statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_session_searchapplications_get_builder()` + `cloudsearch_stats_session_searchapplications_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_session_searchapplications_get(
    client: &SimpleHttpClient,
    name: &str,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetSearchApplicationSessionStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_session_searchapplications_get_builder(
        client,
        name,
        fromDate_day,
        fromDate_month,
        fromDate_year,
        toDate_day,
        toDate_month,
        toDate_year,
    )?;
    cloudsearch_stats_session_searchapplications_get_execute(builder)
}

/// GET v1/stats/user/{+name}
/// Get the users statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_user_searchapplications_get_execute()` to send, or `cloudsearch_stats_user_searchapplications_get` for simplest API.

pub fn cloudsearch_stats_user_searchapplications_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/stats/user/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "fromDate_day",
            fromDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_month",
            fromDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fromDate_year",
            fromDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_day",
            toDate_day
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_month",
            toDate_month
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "toDate_year",
            toDate_year
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/stats/user/{+name}
/// Get the users statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_stats_user_searchapplications_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_stats_user_searchapplications_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_user_searchapplications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetSearchApplicationUserStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetSearchApplicationUserStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/stats/user/{+name}
/// Get the users statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_user_searchapplications_get_builder()` + `cloudsearch_stats_user_searchapplications_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_user_searchapplications_get(
    client: &SimpleHttpClient,
    name: &str,
    fromDate_day: Option<i32>,
    fromDate_month: Option<i32>,
    fromDate_year: Option<i32>,
    toDate_day: Option<i32>,
    toDate_month: Option<i32>,
    toDate_year: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetSearchApplicationUserStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_user_searchapplications_get_builder(
        client,
        name,
        fromDate_day,
        fromDate_month,
        fromDate_year,
        toDate_day,
        toDate_month,
        toDate_year,
    )?;
    cloudsearch_stats_user_searchapplications_get_execute(builder)
}

/// GET v1:initializeCustomer
/// Enables third party support in Google Cloud Search. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_initialize_customer_execute()` to send, or `cloudsearch_initialize_customer` for simplest API.

pub fn cloudsearch_initialize_customer_builder(
    client: &SimpleHttpClient,
    body: &InitializeCustomerRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1:initializeCustomer",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1:initializeCustomer
/// Enables third party support in Google Cloud Search. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudsearch_initialize_customer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudsearch_initialize_customer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_initialize_customer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1:initializeCustomer
/// Enables third party support in Google Cloud Search. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_initialize_customer_builder()` + `cloudsearch_initialize_customer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_initialize_customer(
    client: &SimpleHttpClient,
    body: &InitializeCustomerRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_initialize_customer_builder(client, body)?;
    cloudsearch_initialize_customer_execute(builder)
}
