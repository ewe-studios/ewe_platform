//! Auto-generated API clients for gcp/cloudsearch.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/debug/datasources/{datasourcesId}/items/{itemsId}:checkAccess
/// Checks whether an item is accessible by specified principal. Principal must be a user; groups and domain values aren''t supported. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_debug_datasources_items_check_access_execute()` to send, or `cloudsearch_debug_datasources_items_check_access` for simplest API.

pub fn cloudsearch_debug_datasources_items_check_access_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    debugOptions_enableDebugging: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/debug/datasources/{}/items/{itemsId}:checkAccess",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/debug/datasources/{datasourcesId}/items/{itemsId}:checkAccess
/// Checks whether an item is accessible by specified principal. Principal must be a user; groups and domain values aren''t supported. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_debug_datasources_items_check_access_execute()` or `cloudsearch_debug_datasources_items_check_access`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_debug_datasources_items_check_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_datasources_items_check_access_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CheckAccessResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckAccessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/debug/datasources/{datasourcesId}/items/{itemsId}:checkAccess
/// Checks whether an item is accessible by specified principal. Principal must be a user; groups and domain values aren''t supported. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_debug_datasources_items_check_access_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_debug_datasources_items_check_access_task()`.
/// For the simplest API, use `cloudsearch_debug_datasources_items_check_access()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_debug_datasources_items_check_access_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_debug_datasources_items_check_access_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_debug_datasources_items_check_access_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_debug_datasources_items_check_access`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchDebugDatasourcesItemsCheckAccessArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
}

/// POST v1/debug/datasources/{datasourcesId}/items/{itemsId}:checkAccess
/// Checks whether an item is accessible by specified principal. Principal must be a user; groups and domain values aren''t supported. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_debug_datasources_items_check_access_builder()` + `cloudsearch_debug_datasources_items_check_access_execute()`.
/// For task-level control, use `cloudsearch_debug_datasources_items_check_access_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_datasources_items_check_access(
    client: &SimpleHttpClient,
    args: &CloudsearchDebugDatasourcesItemsCheckAccessArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckAccessResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_debug_datasources_items_check_access_builder(
        client,
        &args.name,
        &args.debugOptions_enableDebugging,
    )?;
    cloudsearch_debug_datasources_items_check_access_execute(builder)
}

/// POST v1/debug/datasources/{datasourcesId}/items:searchByViewUrl
/// Fetches the item whose `viewUrl` exactly matches that of the URL provided in the request. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_debug_datasources_items_search_by_view_url_execute()` to send, or `cloudsearch_debug_datasources_items_search_by_view_url` for simplest API.

pub fn cloudsearch_debug_datasources_items_search_by_view_url_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/debug/datasources/{}/items:searchByViewUrl",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/debug/datasources/{datasourcesId}/items:searchByViewUrl
/// Fetches the item whose `viewUrl` exactly matches that of the URL provided in the request. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_debug_datasources_items_search_by_view_url_execute()` or `cloudsearch_debug_datasources_items_search_by_view_url`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_debug_datasources_items_search_by_view_url_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_datasources_items_search_by_view_url_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchItemsByViewUrlResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchItemsByViewUrlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/debug/datasources/{datasourcesId}/items:searchByViewUrl
/// Fetches the item whose `viewUrl` exactly matches that of the URL provided in the request. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_debug_datasources_items_search_by_view_url_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_debug_datasources_items_search_by_view_url_task()`.
/// For the simplest API, use `cloudsearch_debug_datasources_items_search_by_view_url()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_debug_datasources_items_search_by_view_url_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_debug_datasources_items_search_by_view_url_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchItemsByViewUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_debug_datasources_items_search_by_view_url_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_debug_datasources_items_search_by_view_url`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchDebugDatasourcesItemsSearchByViewUrlArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/debug/datasources/{datasourcesId}/items:searchByViewUrl
/// Fetches the item whose `viewUrl` exactly matches that of the URL provided in the request. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_debug_datasources_items_search_by_view_url_builder()` + `cloudsearch_debug_datasources_items_search_by_view_url_execute()`.
/// For task-level control, use `cloudsearch_debug_datasources_items_search_by_view_url_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_datasources_items_search_by_view_url(
    client: &SimpleHttpClient,
    args: &CloudsearchDebugDatasourcesItemsSearchByViewUrlArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchItemsByViewUrlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudsearch_debug_datasources_items_search_by_view_url_builder(client, &args.name)?;
    cloudsearch_debug_datasources_items_search_by_view_url_execute(builder)
}

/// GET v1/debug/datasources/{datasourcesId}/items/{itemsId}/unmappedids
/// List all unmapped identities for a specific item. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_debug_datasources_items_unmappedids_list_execute()` to send, or `cloudsearch_debug_datasources_items_unmappedids_list` for simplest API.

pub fn cloudsearch_debug_datasources_items_unmappedids_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    debugOptions_enableDebugging: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/debug/datasources/{}/items/{itemsId}/unmappedids",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/debug/datasources/{datasourcesId}/items/{itemsId}/unmappedids
/// List all unmapped identities for a specific item. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_debug_datasources_items_unmappedids_list_execute()` or `cloudsearch_debug_datasources_items_unmappedids_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_debug_datasources_items_unmappedids_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_datasources_items_unmappedids_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUnmappedIdentitiesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUnmappedIdentitiesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/debug/datasources/{datasourcesId}/items/{itemsId}/unmappedids
/// List all unmapped identities for a specific item. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_debug_datasources_items_unmappedids_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_debug_datasources_items_unmappedids_list_task()`.
/// For the simplest API, use `cloudsearch_debug_datasources_items_unmappedids_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_debug_datasources_items_unmappedids_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_debug_datasources_items_unmappedids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUnmappedIdentitiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_debug_datasources_items_unmappedids_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_debug_datasources_items_unmappedids_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchDebugDatasourcesItemsUnmappedidsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/debug/datasources/{datasourcesId}/items/{itemsId}/unmappedids
/// List all unmapped identities for a specific item. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_debug_datasources_items_unmappedids_list_builder()` + `cloudsearch_debug_datasources_items_unmappedids_list_execute()`.
/// For task-level control, use `cloudsearch_debug_datasources_items_unmappedids_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_datasources_items_unmappedids_list(
    client: &SimpleHttpClient,
    args: &CloudsearchDebugDatasourcesItemsUnmappedidsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUnmappedIdentitiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_debug_datasources_items_unmappedids_list_builder(
        client,
        &args.parent,
        &args.debugOptions_enableDebugging,
        &args.pageSize,
        &args.pageToken,
    )?;
    cloudsearch_debug_datasources_items_unmappedids_list_execute(builder)
}

/// GET v1/debug/identitysources/{identitysourcesId}/items:forunmappedidentity
/// Lists names of items associated with an unmapped identity. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_debug_identitysources_items_list_forunmappedidentity_execute()` to send, or `cloudsearch_debug_identitysources_items_list_forunmappedidentity` for simplest API.

pub fn cloudsearch_debug_identitysources_items_list_forunmappedidentity_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    debugOptions_enableDebugging: &Option<Option<String>>,
    groupResourceName: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    userResourceName: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/debug/identitysources/{}/items:forunmappedidentity",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }
    if let Some(val) = groupResourceName.as_ref() {
        query_parts.push(format!("groupResourceName={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = userResourceName.as_ref() {
        query_parts.push(format!("userResourceName={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/debug/identitysources/{identitysourcesId}/items:forunmappedidentity
/// Lists names of items associated with an unmapped identity. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_debug_identitysources_items_list_forunmappedidentity_execute()` or `cloudsearch_debug_identitysources_items_list_forunmappedidentity`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_debug_identitysources_items_list_forunmappedidentity_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_identitysources_items_list_forunmappedidentity_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListItemNamesForUnmappedIdentityResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListItemNamesForUnmappedIdentityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/debug/identitysources/{identitysourcesId}/items:forunmappedidentity
/// Lists names of items associated with an unmapped identity. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_debug_identitysources_items_list_forunmappedidentity_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_debug_identitysources_items_list_forunmappedidentity_task()`.
/// For the simplest API, use `cloudsearch_debug_identitysources_items_list_forunmappedidentity()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_debug_identitysources_items_list_forunmappedidentity_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_debug_identitysources_items_list_forunmappedidentity_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListItemNamesForUnmappedIdentityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_debug_identitysources_items_list_forunmappedidentity_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_debug_identitysources_items_list_forunmappedidentity`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchDebugIdentitysourcesItemsListForunmappedidentityArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
    /// Query parameter: groupResourceName
    pub groupResourceName: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: userResourceName
    pub userResourceName: Option<Option<String>>,
}

/// GET v1/debug/identitysources/{identitysourcesId}/items:forunmappedidentity
/// Lists names of items associated with an unmapped identity. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_debug_identitysources_items_list_forunmappedidentity_builder()` + `cloudsearch_debug_identitysources_items_list_forunmappedidentity_execute()`.
/// For task-level control, use `cloudsearch_debug_identitysources_items_list_forunmappedidentity_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_identitysources_items_list_forunmappedidentity(
    client: &SimpleHttpClient,
    args: &CloudsearchDebugIdentitysourcesItemsListForunmappedidentityArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListItemNamesForUnmappedIdentityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_debug_identitysources_items_list_forunmappedidentity_builder(
        client,
        &args.parent,
        &args.debugOptions_enableDebugging,
        &args.groupResourceName,
        &args.pageSize,
        &args.pageToken,
        &args.userResourceName,
    )?;
    cloudsearch_debug_identitysources_items_list_forunmappedidentity_execute(builder)
}

/// GET v1/debug/identitysources/{identitysourcesId}/unmappedids
/// Lists unmapped user identities for an identity source. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_debug_identitysources_unmappedids_list_execute()` to send, or `cloudsearch_debug_identitysources_unmappedids_list` for simplest API.

pub fn cloudsearch_debug_identitysources_unmappedids_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    debugOptions_enableDebugging: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    resolutionStatusCode: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/debug/identitysources/{}/unmappedids",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resolutionStatusCode.as_ref() {
        query_parts.push(format!("resolutionStatusCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/debug/identitysources/{identitysourcesId}/unmappedids
/// Lists unmapped user identities for an identity source. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_debug_identitysources_unmappedids_list_execute()` or `cloudsearch_debug_identitysources_unmappedids_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_debug_identitysources_unmappedids_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_identitysources_unmappedids_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUnmappedIdentitiesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUnmappedIdentitiesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/debug/identitysources/{identitysourcesId}/unmappedids
/// Lists unmapped user identities for an identity source. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_debug_identitysources_unmappedids_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_debug_identitysources_unmappedids_list_task()`.
/// For the simplest API, use `cloudsearch_debug_identitysources_unmappedids_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_debug_identitysources_unmappedids_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_debug_identitysources_unmappedids_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUnmappedIdentitiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_debug_identitysources_unmappedids_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_debug_identitysources_unmappedids_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchDebugIdentitysourcesUnmappedidsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: resolutionStatusCode
    pub resolutionStatusCode: Option<Option<String>>,
}

/// GET v1/debug/identitysources/{identitysourcesId}/unmappedids
/// Lists unmapped user identities for an identity source. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_debug_identitysources_unmappedids_list_builder()` + `cloudsearch_debug_identitysources_unmappedids_list_execute()`.
/// For task-level control, use `cloudsearch_debug_identitysources_unmappedids_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_debug_identitysources_unmappedids_list(
    client: &SimpleHttpClient,
    args: &CloudsearchDebugIdentitysourcesUnmappedidsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUnmappedIdentitiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_debug_identitysources_unmappedids_list_builder(
        client,
        &args.parent,
        &args.debugOptions_enableDebugging,
        &args.pageSize,
        &args.pageToken,
        &args.resolutionStatusCode,
    )?;
    cloudsearch_debug_identitysources_unmappedids_list_execute(builder)
}

/// DELETE v1/indexing/datasources/{datasourcesId}/schema
/// Deletes the schema of a data source. **Note:** This API requires an admin or service account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_delete_schema_execute()` to send, or `cloudsearch_indexing_datasources_delete_schema` for simplest API.

pub fn cloudsearch_indexing_datasources_delete_schema_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    debugOptions_enableDebugging: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/datasources/{}/schema",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/indexing/datasources/{datasourcesId}/schema
/// Deletes the schema of a data source. **Note:** This API requires an admin or service account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_indexing_datasources_delete_schema_execute()` or `cloudsearch_indexing_datasources_delete_schema`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_delete_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_delete_schema_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/indexing/datasources/{datasourcesId}/schema
/// Deletes the schema of a data source. **Note:** This API requires an admin or service account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_indexing_datasources_delete_schema_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_indexing_datasources_delete_schema_task()`.
/// For the simplest API, use `cloudsearch_indexing_datasources_delete_schema()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_delete_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_indexing_datasources_delete_schema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_indexing_datasources_delete_schema_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_indexing_datasources_delete_schema`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesDeleteSchemaArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
}

/// DELETE v1/indexing/datasources/{datasourcesId}/schema
/// Deletes the schema of a data source. **Note:** This API requires an admin or service account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_delete_schema_builder()` + `cloudsearch_indexing_datasources_delete_schema_execute()`.
/// For task-level control, use `cloudsearch_indexing_datasources_delete_schema_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_delete_schema(
    client: &SimpleHttpClient,
    args: &CloudsearchIndexingDatasourcesDeleteSchemaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_delete_schema_builder(
        client,
        &args.name,
        &args.debugOptions_enableDebugging,
    )?;
    cloudsearch_indexing_datasources_delete_schema_execute(builder)
}

/// GET v1/indexing/datasources/{datasourcesId}/schema
/// Gets the schema of a data source. **Note:** This API requires an admin or service account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_get_schema_execute()` to send, or `cloudsearch_indexing_datasources_get_schema` for simplest API.

pub fn cloudsearch_indexing_datasources_get_schema_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    debugOptions_enableDebugging: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/datasources/{}/schema",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/indexing/datasources/{datasourcesId}/schema
/// Gets the schema of a data source. **Note:** This API requires an admin or service account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_indexing_datasources_get_schema_execute()` or `cloudsearch_indexing_datasources_get_schema`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_get_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_get_schema_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Schema>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Schema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/indexing/datasources/{datasourcesId}/schema
/// Gets the schema of a data source. **Note:** This API requires an admin or service account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_indexing_datasources_get_schema_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_indexing_datasources_get_schema_task()`.
/// For the simplest API, use `cloudsearch_indexing_datasources_get_schema()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_get_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_indexing_datasources_get_schema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Schema>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_indexing_datasources_get_schema_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_indexing_datasources_get_schema`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesGetSchemaArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
}

/// GET v1/indexing/datasources/{datasourcesId}/schema
/// Gets the schema of a data source. **Note:** This API requires an admin or service account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_get_schema_builder()` + `cloudsearch_indexing_datasources_get_schema_execute()`.
/// For task-level control, use `cloudsearch_indexing_datasources_get_schema_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_get_schema(
    client: &SimpleHttpClient,
    args: &CloudsearchIndexingDatasourcesGetSchemaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Schema>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_get_schema_builder(
        client,
        &args.name,
        &args.debugOptions_enableDebugging,
    )?;
    cloudsearch_indexing_datasources_get_schema_execute(builder)
}

/// PUT v1/indexing/datasources/{datasourcesId}/schema
/// Updates the schema of a data source. This method does not perform incremental updates to the schema. Instead, this method updates the schema by overwriting the entire schema. **Note:** This API requires an admin or service account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_update_schema_execute()` to send, or `cloudsearch_indexing_datasources_update_schema` for simplest API.

pub fn cloudsearch_indexing_datasources_update_schema_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/datasources/{}/schema",
        name,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/indexing/datasources/{datasourcesId}/schema
/// Updates the schema of a data source. This method does not perform incremental updates to the schema. Instead, this method updates the schema by overwriting the entire schema. **Note:** This API requires an admin or service account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_indexing_datasources_update_schema_execute()` or `cloudsearch_indexing_datasources_update_schema`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_update_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_update_schema_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/indexing/datasources/{datasourcesId}/schema
/// Updates the schema of a data source. This method does not perform incremental updates to the schema. Instead, this method updates the schema by overwriting the entire schema. **Note:** This API requires an admin or service account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_indexing_datasources_update_schema_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_indexing_datasources_update_schema_task()`.
/// For the simplest API, use `cloudsearch_indexing_datasources_update_schema()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_update_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_indexing_datasources_update_schema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_indexing_datasources_update_schema_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_indexing_datasources_update_schema`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesUpdateSchemaArgs {
    /// Path parameter: name
    pub name: String,
}

/// PUT v1/indexing/datasources/{datasourcesId}/schema
/// Updates the schema of a data source. This method does not perform incremental updates to the schema. Instead, this method updates the schema by overwriting the entire schema. **Note:** This API requires an admin or service account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_update_schema_builder()` + `cloudsearch_indexing_datasources_update_schema_execute()`.
/// For task-level control, use `cloudsearch_indexing_datasources_update_schema_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_update_schema(
    client: &SimpleHttpClient,
    args: &CloudsearchIndexingDatasourcesUpdateSchemaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_update_schema_builder(client, &args.name)?;
    cloudsearch_indexing_datasources_update_schema_execute(builder)
}

/// DELETE v1/indexing/datasources/{datasourcesId}/items/{itemsId}
/// Deletes Item resource for the specified resource name. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_delete_execute()` to send, or `cloudsearch_indexing_datasources_items_delete` for simplest API.

pub fn cloudsearch_indexing_datasources_items_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    connectorName: &Option<Option<String>>,
    debugOptions_enableDebugging: &Option<Option<String>>,
    mode: &Option<Option<String>>,
    version: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/datasources/{}/items/{itemsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = connectorName.as_ref() {
        query_parts.push(format!("connectorName={}", val));
    }
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }
    if let Some(val) = mode.as_ref() {
        query_parts.push(format!("mode={}", val));
    }
    if let Some(val) = version.as_ref() {
        query_parts.push(format!("version={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/indexing/datasources/{datasourcesId}/items/{itemsId}
/// Deletes Item resource for the specified resource name. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_indexing_datasources_items_delete_execute()` or `cloudsearch_indexing_datasources_items_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/indexing/datasources/{datasourcesId}/items/{itemsId}
/// Deletes Item resource for the specified resource name. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_indexing_datasources_items_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_indexing_datasources_items_delete_task()`.
/// For the simplest API, use `cloudsearch_indexing_datasources_items_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_indexing_datasources_items_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_indexing_datasources_items_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_indexing_datasources_items_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: connectorName
    pub connectorName: Option<Option<String>>,
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
    /// Query parameter: mode
    pub mode: Option<Option<String>>,
    /// Query parameter: version
    pub version: Option<Option<String>>,
}

/// DELETE v1/indexing/datasources/{datasourcesId}/items/{itemsId}
/// Deletes Item resource for the specified resource name. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_delete_builder()` + `cloudsearch_indexing_datasources_items_delete_execute()`.
/// For task-level control, use `cloudsearch_indexing_datasources_items_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_delete(
    client: &SimpleHttpClient,
    args: &CloudsearchIndexingDatasourcesItemsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_delete_builder(
        client,
        &args.name,
        &args.connectorName,
        &args.debugOptions_enableDebugging,
        &args.mode,
        &args.version,
    )?;
    cloudsearch_indexing_datasources_items_delete_execute(builder)
}

/// POST v1/indexing/datasources/{datasourcesId}/items:deleteQueueItems
/// Deletes all items in a queue. This method is useful for deleting stale items. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_delete_queue_items_execute()` to send, or `cloudsearch_indexing_datasources_items_delete_queue_items` for simplest API.

pub fn cloudsearch_indexing_datasources_items_delete_queue_items_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/datasources/{}/items:deleteQueueItems",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/indexing/datasources/{datasourcesId}/items:deleteQueueItems
/// Deletes all items in a queue. This method is useful for deleting stale items. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_indexing_datasources_items_delete_queue_items_execute()` or `cloudsearch_indexing_datasources_items_delete_queue_items`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_delete_queue_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_delete_queue_items_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/indexing/datasources/{datasourcesId}/items:deleteQueueItems
/// Deletes all items in a queue. This method is useful for deleting stale items. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_indexing_datasources_items_delete_queue_items_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_indexing_datasources_items_delete_queue_items_task()`.
/// For the simplest API, use `cloudsearch_indexing_datasources_items_delete_queue_items()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_delete_queue_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_indexing_datasources_items_delete_queue_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_indexing_datasources_items_delete_queue_items_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_indexing_datasources_items_delete_queue_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsDeleteQueueItemsArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/indexing/datasources/{datasourcesId}/items:deleteQueueItems
/// Deletes all items in a queue. This method is useful for deleting stale items. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_delete_queue_items_builder()` + `cloudsearch_indexing_datasources_items_delete_queue_items_execute()`.
/// For task-level control, use `cloudsearch_indexing_datasources_items_delete_queue_items_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_delete_queue_items(
    client: &SimpleHttpClient,
    args: &CloudsearchIndexingDatasourcesItemsDeleteQueueItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudsearch_indexing_datasources_items_delete_queue_items_builder(client, &args.name)?;
    cloudsearch_indexing_datasources_items_delete_queue_items_execute(builder)
}

/// GET v1/indexing/datasources/{datasourcesId}/items/{itemsId}
/// Gets Item resource by item name. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_get_execute()` to send, or `cloudsearch_indexing_datasources_items_get` for simplest API.

pub fn cloudsearch_indexing_datasources_items_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    connectorName: &Option<Option<String>>,
    debugOptions_enableDebugging: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/datasources/{}/items/{itemsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = connectorName.as_ref() {
        query_parts.push(format!("connectorName={}", val));
    }
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/indexing/datasources/{datasourcesId}/items/{itemsId}
/// Gets Item resource by item name. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_indexing_datasources_items_get_execute()` or `cloudsearch_indexing_datasources_items_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Item>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Item = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/indexing/datasources/{datasourcesId}/items/{itemsId}
/// Gets Item resource by item name. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_indexing_datasources_items_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_indexing_datasources_items_get_task()`.
/// For the simplest API, use `cloudsearch_indexing_datasources_items_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_indexing_datasources_items_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Item>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_indexing_datasources_items_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_indexing_datasources_items_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: connectorName
    pub connectorName: Option<Option<String>>,
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
}

/// GET v1/indexing/datasources/{datasourcesId}/items/{itemsId}
/// Gets Item resource by item name. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_get_builder()` + `cloudsearch_indexing_datasources_items_get_execute()`.
/// For task-level control, use `cloudsearch_indexing_datasources_items_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_get(
    client: &SimpleHttpClient,
    args: &CloudsearchIndexingDatasourcesItemsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Item>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_get_builder(
        client,
        &args.name,
        &args.connectorName,
        &args.debugOptions_enableDebugging,
    )?;
    cloudsearch_indexing_datasources_items_get_execute(builder)
}

/// POST v1/indexing/datasources/{datasourcesId}/items/{itemsId}:index
/// Updates Item ACL, metadata, and content. It will insert the Item if it does not exist. This method does not support partial updates. Fields with no provided values are cleared out in the Cloud Search index. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_index_execute()` to send, or `cloudsearch_indexing_datasources_items_index` for simplest API.

pub fn cloudsearch_indexing_datasources_items_index_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/datasources/{}/items/{itemsId}:index",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/indexing/datasources/{datasourcesId}/items/{itemsId}:index
/// Updates Item ACL, metadata, and content. It will insert the Item if it does not exist. This method does not support partial updates. Fields with no provided values are cleared out in the Cloud Search index. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_indexing_datasources_items_index_execute()` or `cloudsearch_indexing_datasources_items_index`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_index_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_index_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/indexing/datasources/{datasourcesId}/items/{itemsId}:index
/// Updates Item ACL, metadata, and content. It will insert the Item if it does not exist. This method does not support partial updates. Fields with no provided values are cleared out in the Cloud Search index. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_indexing_datasources_items_index_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_indexing_datasources_items_index_task()`.
/// For the simplest API, use `cloudsearch_indexing_datasources_items_index()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_index_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_indexing_datasources_items_index_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_indexing_datasources_items_index_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_indexing_datasources_items_index`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsIndexArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/indexing/datasources/{datasourcesId}/items/{itemsId}:index
/// Updates Item ACL, metadata, and content. It will insert the Item if it does not exist. This method does not support partial updates. Fields with no provided values are cleared out in the Cloud Search index. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_index_builder()` + `cloudsearch_indexing_datasources_items_index_execute()`.
/// For task-level control, use `cloudsearch_indexing_datasources_items_index_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_index(
    client: &SimpleHttpClient,
    args: &CloudsearchIndexingDatasourcesItemsIndexArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_index_builder(client, &args.name)?;
    cloudsearch_indexing_datasources_items_index_execute(builder)
}

/// GET v1/indexing/datasources/{datasourcesId}/items
/// Lists all or a subset of Item resources. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_list_execute()` to send, or `cloudsearch_indexing_datasources_items_list` for simplest API.

pub fn cloudsearch_indexing_datasources_items_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    brief: &Option<Option<String>>,
    connectorName: &Option<Option<String>>,
    debugOptions_enableDebugging: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/datasources/{}/items",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = brief.as_ref() {
        query_parts.push(format!("brief={}", val));
    }
    if let Some(val) = connectorName.as_ref() {
        query_parts.push(format!("connectorName={}", val));
    }
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/indexing/datasources/{datasourcesId}/items
/// Lists all or a subset of Item resources. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_indexing_datasources_items_list_execute()` or `cloudsearch_indexing_datasources_items_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListItemsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListItemsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/indexing/datasources/{datasourcesId}/items
/// Lists all or a subset of Item resources. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_indexing_datasources_items_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_indexing_datasources_items_list_task()`.
/// For the simplest API, use `cloudsearch_indexing_datasources_items_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_indexing_datasources_items_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListItemsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_indexing_datasources_items_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_indexing_datasources_items_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: brief
    pub brief: Option<Option<String>>,
    /// Query parameter: connectorName
    pub connectorName: Option<Option<String>>,
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/indexing/datasources/{datasourcesId}/items
/// Lists all or a subset of Item resources. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_list_builder()` + `cloudsearch_indexing_datasources_items_list_execute()`.
/// For task-level control, use `cloudsearch_indexing_datasources_items_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_list(
    client: &SimpleHttpClient,
    args: &CloudsearchIndexingDatasourcesItemsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListItemsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_list_builder(
        client,
        &args.name,
        &args.brief,
        &args.connectorName,
        &args.debugOptions_enableDebugging,
        &args.pageSize,
        &args.pageToken,
    )?;
    cloudsearch_indexing_datasources_items_list_execute(builder)
}

/// POST v1/indexing/datasources/{datasourcesId}/items:poll
/// Polls for unreserved items from the indexing queue and marks a set as reserved, starting with items that have the oldest timestamp from the highest priority ItemStatus. The priority order is as follows: `ERROR` MODIFIED NEW_ITEM ACCEPTED Reserving items ensures that polling from other threads cannot create overlapping sets. After handling the reserved items, the client should put items back into the unreserved state, either by calling index, or by calling push with the type REQUEUE. Items automatically become available (unreserved) after 4 hours even if no update or push method is called. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_poll_execute()` to send, or `cloudsearch_indexing_datasources_items_poll` for simplest API.

pub fn cloudsearch_indexing_datasources_items_poll_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/datasources/{}/items:poll",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/indexing/datasources/{datasourcesId}/items:poll
/// Polls for unreserved items from the indexing queue and marks a set as reserved, starting with items that have the oldest timestamp from the highest priority ItemStatus. The priority order is as follows: `ERROR` MODIFIED NEW_ITEM ACCEPTED Reserving items ensures that polling from other threads cannot create overlapping sets. After handling the reserved items, the client should put items back into the unreserved state, either by calling index, or by calling push with the type REQUEUE. Items automatically become available (unreserved) after 4 hours even if no update or push method is called. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_indexing_datasources_items_poll_execute()` or `cloudsearch_indexing_datasources_items_poll`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_poll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_poll_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PollItemsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PollItemsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/indexing/datasources/{datasourcesId}/items:poll
/// Polls for unreserved items from the indexing queue and marks a set as reserved, starting with items that have the oldest timestamp from the highest priority ItemStatus. The priority order is as follows: `ERROR` MODIFIED NEW_ITEM ACCEPTED Reserving items ensures that polling from other threads cannot create overlapping sets. After handling the reserved items, the client should put items back into the unreserved state, either by calling index, or by calling push with the type REQUEUE. Items automatically become available (unreserved) after 4 hours even if no update or push method is called. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_indexing_datasources_items_poll_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_indexing_datasources_items_poll_task()`.
/// For the simplest API, use `cloudsearch_indexing_datasources_items_poll()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_poll_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_indexing_datasources_items_poll_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PollItemsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_indexing_datasources_items_poll_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_indexing_datasources_items_poll`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsPollArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/indexing/datasources/{datasourcesId}/items:poll
/// Polls for unreserved items from the indexing queue and marks a set as reserved, starting with items that have the oldest timestamp from the highest priority ItemStatus. The priority order is as follows: `ERROR` MODIFIED NEW_ITEM ACCEPTED Reserving items ensures that polling from other threads cannot create overlapping sets. After handling the reserved items, the client should put items back into the unreserved state, either by calling index, or by calling push with the type REQUEUE. Items automatically become available (unreserved) after 4 hours even if no update or push method is called. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_poll_builder()` + `cloudsearch_indexing_datasources_items_poll_execute()`.
/// For task-level control, use `cloudsearch_indexing_datasources_items_poll_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_poll(
    client: &SimpleHttpClient,
    args: &CloudsearchIndexingDatasourcesItemsPollArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PollItemsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_poll_builder(client, &args.name)?;
    cloudsearch_indexing_datasources_items_poll_execute(builder)
}

/// POST v1/indexing/datasources/{datasourcesId}/items/{itemsId}:push
/// Pushes an item onto a queue for later polling and updating. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_push_execute()` to send, or `cloudsearch_indexing_datasources_items_push` for simplest API.

pub fn cloudsearch_indexing_datasources_items_push_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/datasources/{}/items/{itemsId}:push",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/indexing/datasources/{datasourcesId}/items/{itemsId}:push
/// Pushes an item onto a queue for later polling and updating. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_indexing_datasources_items_push_execute()` or `cloudsearch_indexing_datasources_items_push`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_push_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_push_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Item>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Item = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/indexing/datasources/{datasourcesId}/items/{itemsId}:push
/// Pushes an item onto a queue for later polling and updating. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_indexing_datasources_items_push_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_indexing_datasources_items_push_task()`.
/// For the simplest API, use `cloudsearch_indexing_datasources_items_push()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_push_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_indexing_datasources_items_push_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Item>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_indexing_datasources_items_push_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_indexing_datasources_items_push`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsPushArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/indexing/datasources/{datasourcesId}/items/{itemsId}:push
/// Pushes an item onto a queue for later polling and updating. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_push_builder()` + `cloudsearch_indexing_datasources_items_push_execute()`.
/// For task-level control, use `cloudsearch_indexing_datasources_items_push_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_push(
    client: &SimpleHttpClient,
    args: &CloudsearchIndexingDatasourcesItemsPushArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Item>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_push_builder(client, &args.name)?;
    cloudsearch_indexing_datasources_items_push_execute(builder)
}

/// POST v1/indexing/datasources/{datasourcesId}/items:unreserve
/// Unreserves all items from a queue, making them all eligible to be polled. This method is useful for resetting the indexing queue after a connector has been restarted. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_unreserve_execute()` to send, or `cloudsearch_indexing_datasources_items_unreserve` for simplest API.

pub fn cloudsearch_indexing_datasources_items_unreserve_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/datasources/{}/items:unreserve",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/indexing/datasources/{datasourcesId}/items:unreserve
/// Unreserves all items from a queue, making them all eligible to be polled. This method is useful for resetting the indexing queue after a connector has been restarted. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_indexing_datasources_items_unreserve_execute()` or `cloudsearch_indexing_datasources_items_unreserve`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_unreserve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_unreserve_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/indexing/datasources/{datasourcesId}/items:unreserve
/// Unreserves all items from a queue, making them all eligible to be polled. This method is useful for resetting the indexing queue after a connector has been restarted. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_indexing_datasources_items_unreserve_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_indexing_datasources_items_unreserve_task()`.
/// For the simplest API, use `cloudsearch_indexing_datasources_items_unreserve()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_unreserve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_indexing_datasources_items_unreserve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_indexing_datasources_items_unreserve_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_indexing_datasources_items_unreserve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsUnreserveArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/indexing/datasources/{datasourcesId}/items:unreserve
/// Unreserves all items from a queue, making them all eligible to be polled. This method is useful for resetting the indexing queue after a connector has been restarted. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_unreserve_builder()` + `cloudsearch_indexing_datasources_items_unreserve_execute()`.
/// For task-level control, use `cloudsearch_indexing_datasources_items_unreserve_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_unreserve(
    client: &SimpleHttpClient,
    args: &CloudsearchIndexingDatasourcesItemsUnreserveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_unreserve_builder(client, &args.name)?;
    cloudsearch_indexing_datasources_items_unreserve_execute(builder)
}

/// POST v1/indexing/datasources/{datasourcesId}/items/{itemsId}:upload
/// Creates an upload session for uploading item content. For items smaller than 100 KB, it''s easier to embed the content inline within an index request. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_indexing_datasources_items_upload_execute()` to send, or `cloudsearch_indexing_datasources_items_upload` for simplest API.

pub fn cloudsearch_indexing_datasources_items_upload_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/datasources/{}/items/{itemsId}:upload",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/indexing/datasources/{datasourcesId}/items/{itemsId}:upload
/// Creates an upload session for uploading item content. For items smaller than 100 KB, it''s easier to embed the content inline within an index request. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_indexing_datasources_items_upload_execute()` or `cloudsearch_indexing_datasources_items_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UploadItemRef>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UploadItemRef = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/indexing/datasources/{datasourcesId}/items/{itemsId}:upload
/// Creates an upload session for uploading item content. For items smaller than 100 KB, it''s easier to embed the content inline within an index request. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_indexing_datasources_items_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_indexing_datasources_items_upload_task()`.
/// For the simplest API, use `cloudsearch_indexing_datasources_items_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_indexing_datasources_items_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_indexing_datasources_items_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UploadItemRef>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_indexing_datasources_items_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_indexing_datasources_items_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsUploadArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/indexing/datasources/{datasourcesId}/items/{itemsId}:upload
/// Creates an upload session for uploading item content. For items smaller than 100 KB, it''s easier to embed the content inline within an index request. This API requires an admin or service account to execute. The service account used is the one whitelisted in the corresponding data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_indexing_datasources_items_upload_builder()` + `cloudsearch_indexing_datasources_items_upload_execute()`.
/// For task-level control, use `cloudsearch_indexing_datasources_items_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_indexing_datasources_items_upload(
    client: &SimpleHttpClient,
    args: &CloudsearchIndexingDatasourcesItemsUploadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UploadItemRef>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_indexing_datasources_items_upload_builder(client, &args.name)?;
    cloudsearch_indexing_datasources_items_upload_execute(builder)
}

/// POST v1/media/{mediaId}
/// Uploads media for indexing. The upload endpoint supports direct and resumable upload protocols and is intended for large items that can not be [inlined during index requests](<https://developers.google.`com/workspace/cloud-search/docs/reference/rest/v1/indexing`.datasources.items#itemcontent>). To index large content: 1. Call indexing.datasources.items.upload with the item name to begin an upload session and retrieve the UploadItemRef. 1. Call media.upload to upload the content, as a streaming request, using the same resource name from the UploadItemRef from step 1. 1. Call indexing.datasources.items.index to index the item. Populate the [ItemContent](/cloud-`search/docs/reference/rest/v1/indexing`.datasources.items#ItemContent) with the UploadItemRef from step 1. For additional information, see [Create a content connector using the REST API](<https://developers.google.`com/workspace/cloud-search/docs/guides/content-connector`#rest>). **Note:** This API requires a service account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_media_upload_execute()` to send, or `cloudsearch_media_upload` for simplest API.

pub fn cloudsearch_media_upload_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/media/{}",
        resourceName,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/media/{mediaId}
/// Uploads media for indexing. The upload endpoint supports direct and resumable upload protocols and is intended for large items that can not be [inlined during index requests](<https://developers.google.`com/workspace/cloud-search/docs/reference/rest/v1/indexing`.datasources.items#itemcontent>). To index large content: 1. Call indexing.datasources.items.upload with the item name to begin an upload session and retrieve the UploadItemRef. 1. Call media.upload to upload the content, as a streaming request, using the same resource name from the UploadItemRef from step 1. 1. Call indexing.datasources.items.index to index the item. Populate the [ItemContent](/cloud-`search/docs/reference/rest/v1/indexing`.datasources.items#ItemContent) with the UploadItemRef from step 1. For additional information, see [Create a content connector using the REST API](<https://developers.google.`com/workspace/cloud-search/docs/guides/content-connector`#rest>). **Note:** This API requires a service account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_media_upload_execute()` or `cloudsearch_media_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_media_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_media_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Media>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Media = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/media/{mediaId}
/// Uploads media for indexing. The upload endpoint supports direct and resumable upload protocols and is intended for large items that can not be [inlined during index requests](<https://developers.google.`com/workspace/cloud-search/docs/reference/rest/v1/indexing`.datasources.items#itemcontent>). To index large content: 1. Call indexing.datasources.items.upload with the item name to begin an upload session and retrieve the UploadItemRef. 1. Call media.upload to upload the content, as a streaming request, using the same resource name from the UploadItemRef from step 1. 1. Call indexing.datasources.items.index to index the item. Populate the [ItemContent](/cloud-`search/docs/reference/rest/v1/indexing`.datasources.items#ItemContent) with the UploadItemRef from step 1. For additional information, see [Create a content connector using the REST API](<https://developers.google.`com/workspace/cloud-search/docs/guides/content-connector`#rest>). **Note:** This API requires a service account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_media_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_media_upload_task()`.
/// For the simplest API, use `cloudsearch_media_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_media_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_media_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Media>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_media_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_media_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchMediaUploadArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
}

/// POST v1/media/{mediaId}
/// Uploads media for indexing. The upload endpoint supports direct and resumable upload protocols and is intended for large items that can not be [inlined during index requests](<https://developers.google.`com/workspace/cloud-search/docs/reference/rest/v1/indexing`.datasources.items#itemcontent>). To index large content: 1. Call indexing.datasources.items.upload with the item name to begin an upload session and retrieve the UploadItemRef. 1. Call media.upload to upload the content, as a streaming request, using the same resource name from the UploadItemRef from step 1. 1. Call indexing.datasources.items.index to index the item. Populate the [ItemContent](/cloud-`search/docs/reference/rest/v1/indexing`.datasources.items#ItemContent) with the UploadItemRef from step 1. For additional information, see [Create a content connector using the REST API](<https://developers.google.`com/workspace/cloud-search/docs/guides/content-connector`#rest>). **Note:** This API requires a service account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_media_upload_builder()` + `cloudsearch_media_upload_execute()`.
/// For task-level control, use `cloudsearch_media_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_media_upload(
    client: &SimpleHttpClient,
    args: &CloudsearchMediaUploadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Media>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_media_upload_builder(client, &args.resourceName)?;
    cloudsearch_media_upload_execute(builder)
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_operations_get_execute()` to send, or `cloudsearch_operations_get` for simplest API.

pub fn cloudsearch_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/operations/{}", name,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_operations_get_execute()` or `cloudsearch_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_operations_get_task()`.
/// For the simplest API, use `cloudsearch_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_operations_get_builder()` + `cloudsearch_operations_get_execute()`.
/// For task-level control, use `cloudsearch_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_operations_get(
    client: &SimpleHttpClient,
    args: &CloudsearchOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_operations_get_builder(client, &args.name)?;
    cloudsearch_operations_get_execute(builder)
}

/// GET v1/operations/{operationsId}/lro
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_operations_lro_list_execute()` to send, or `cloudsearch_operations_lro_list` for simplest API.

pub fn cloudsearch_operations_lro_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/operations/{}/lro",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}/lro
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_operations_lro_list_execute()` or `cloudsearch_operations_lro_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_operations_lro_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_operations_lro_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}/lro
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_operations_lro_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_operations_lro_list_task()`.
/// For the simplest API, use `cloudsearch_operations_lro_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_operations_lro_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_operations_lro_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_operations_lro_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_operations_lro_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchOperationsLroListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/operations/{operationsId}/lro
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_operations_lro_list_builder()` + `cloudsearch_operations_lro_list_execute()`.
/// For task-level control, use `cloudsearch_operations_lro_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_operations_lro_list(
    client: &SimpleHttpClient,
    args: &CloudsearchOperationsLroListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_operations_lro_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    cloudsearch_operations_lro_list_execute(builder)
}

/// POST v1/query:removeActivity
/// Provides functionality to remove logged activity for a user. Currently to be used only for Chat 1p clients **Note:** This API requires a standard end user account to execute. A service account can''t perform Remove Activity requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_query_remove_activity_execute()` to send, or `cloudsearch_query_remove_activity` for simplest API.

pub fn cloudsearch_query_remove_activity_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/query:removeActivity",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/query:removeActivity
/// Provides functionality to remove logged activity for a user. Currently to be used only for Chat 1p clients **Note:** This API requires a standard end user account to execute. A service account can''t perform Remove Activity requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_query_remove_activity_execute()` or `cloudsearch_query_remove_activity`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_query_remove_activity_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_remove_activity_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemoveActivityResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemoveActivityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/query:removeActivity
/// Provides functionality to remove logged activity for a user. Currently to be used only for Chat 1p clients **Note:** This API requires a standard end user account to execute. A service account can''t perform Remove Activity requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_query_remove_activity_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_query_remove_activity_task()`.
/// For the simplest API, use `cloudsearch_query_remove_activity()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_query_remove_activity_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_query_remove_activity_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemoveActivityResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_query_remove_activity_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/query:removeActivity
/// Provides functionality to remove logged activity for a user. Currently to be used only for Chat 1p clients **Note:** This API requires a standard end user account to execute. A service account can''t perform Remove Activity requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_query_remove_activity_builder()` + `cloudsearch_query_remove_activity_execute()`.
/// For task-level control, use `cloudsearch_query_remove_activity_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_remove_activity(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemoveActivityResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_query_remove_activity_builder(client)?;
    cloudsearch_query_remove_activity_execute(builder)
}

/// POST v1/query/search
/// The Cloud Search Query API provides the search method, which returns the most relevant results from a user query. The results can come from Google Workspace apps, such as Gmail or Google Drive, or they can come from data that you have indexed from a third party. **Note:** This API requires a standard end user account to execute. A service account can''t perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_query_search_execute()` to send, or `cloudsearch_query_search` for simplest API.

pub fn cloudsearch_query_search_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/query/search",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/query/search
/// The Cloud Search Query API provides the search method, which returns the most relevant results from a user query. The results can come from Google Workspace apps, such as Gmail or Google Drive, or they can come from data that you have indexed from a third party. **Note:** This API requires a standard end user account to execute. A service account can''t perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_query_search_execute()` or `cloudsearch_query_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_query_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/query/search
/// The Cloud Search Query API provides the search method, which returns the most relevant results from a user query. The results can come from Google Workspace apps, such as Gmail or Google Drive, or they can come from data that you have indexed from a third party. **Note:** This API requires a standard end user account to execute. A service account can''t perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_query_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_query_search_task()`.
/// For the simplest API, use `cloudsearch_query_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_query_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_query_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_query_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/query/search
/// The Cloud Search Query API provides the search method, which returns the most relevant results from a user query. The results can come from Google Workspace apps, such as Gmail or Google Drive, or they can come from data that you have indexed from a third party. **Note:** This API requires a standard end user account to execute. A service account can''t perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_query_search_builder()` + `cloudsearch_query_search_execute()`.
/// For task-level control, use `cloudsearch_query_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_search(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_query_search_builder(client)?;
    cloudsearch_query_search_execute(builder)
}

/// POST v1/query/suggest
/// Provides suggestions for autocompleting the query. **Note:** This API requires a standard end user account to execute. A service account can''t perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_query_suggest_execute()` to send, or `cloudsearch_query_suggest` for simplest API.

pub fn cloudsearch_query_suggest_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/query/suggest",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/query/suggest
/// Provides suggestions for autocompleting the query. **Note:** This API requires a standard end user account to execute. A service account can''t perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_query_suggest_execute()` or `cloudsearch_query_suggest`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_query_suggest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_suggest_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SuggestResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SuggestResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/query/suggest
/// Provides suggestions for autocompleting the query. **Note:** This API requires a standard end user account to execute. A service account can''t perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_query_suggest_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_query_suggest_task()`.
/// For the simplest API, use `cloudsearch_query_suggest()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_query_suggest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_query_suggest_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SuggestResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_query_suggest_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/query/suggest
/// Provides suggestions for autocompleting the query. **Note:** This API requires a standard end user account to execute. A service account can''t perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_query_suggest_builder()` + `cloudsearch_query_suggest_execute()`.
/// For task-level control, use `cloudsearch_query_suggest_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_suggest(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SuggestResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_query_suggest_builder(client)?;
    cloudsearch_query_suggest_execute(builder)
}

/// GET v1/query/sources
/// Returns list of sources that user can use for Search and Suggest APIs. **Note:** This API requires a standard end user account to execute. A service account can''t perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_query_sources_list_execute()` to send, or `cloudsearch_query_sources_list` for simplest API.

pub fn cloudsearch_query_sources_list_builder<R>(
    client: &SimpleHttpClient<R>,
    pageToken: &Option<Option<String>>,
    requestOptions_debugOptions_enableDebugging: &Option<Option<String>>,
    requestOptions_languageCode: &Option<Option<String>>,
    requestOptions_searchApplicationId: &Option<Option<String>>,
    requestOptions_timeZone: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/query/sources",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = requestOptions_debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!(
            "requestOptions.debugOptions.enableDebugging={}",
            val
        ));
    }
    if let Some(val) = requestOptions_languageCode.as_ref() {
        query_parts.push(format!("requestOptions.languageCode={}", val));
    }
    if let Some(val) = requestOptions_searchApplicationId.as_ref() {
        query_parts.push(format!("requestOptions.searchApplicationId={}", val));
    }
    if let Some(val) = requestOptions_timeZone.as_ref() {
        query_parts.push(format!("requestOptions.timeZone={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/query/sources
/// Returns list of sources that user can use for Search and Suggest APIs. **Note:** This API requires a standard end user account to execute. A service account can''t perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_query_sources_list_execute()` or `cloudsearch_query_sources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_query_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_sources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListQuerySourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListQuerySourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/query/sources
/// Returns list of sources that user can use for Search and Suggest APIs. **Note:** This API requires a standard end user account to execute. A service account can''t perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_query_sources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_query_sources_list_task()`.
/// For the simplest API, use `cloudsearch_query_sources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_query_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_query_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListQuerySourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_query_sources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_query_sources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchQuerySourcesListArgs {
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: requestOptions_debugOptions_enableDebugging
    pub requestOptions_debugOptions_enableDebugging: Option<Option<String>>,
    /// Query parameter: requestOptions_languageCode
    pub requestOptions_languageCode: Option<Option<String>>,
    /// Query parameter: requestOptions_searchApplicationId
    pub requestOptions_searchApplicationId: Option<Option<String>>,
    /// Query parameter: requestOptions_timeZone
    pub requestOptions_timeZone: Option<Option<String>>,
}

/// GET v1/query/sources
/// Returns list of sources that user can use for Search and Suggest APIs. **Note:** This API requires a standard end user account to execute. A service account can''t perform Query API requests directly; to use a service account to perform queries, set up [Google Workspace domain-wide delegation of authority](<https://developers.google.`com/workspace/cloud-search/docs/guides/delegation/`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_query_sources_list_builder()` + `cloudsearch_query_sources_list_execute()`.
/// For task-level control, use `cloudsearch_query_sources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_query_sources_list(
    client: &SimpleHttpClient,
    args: &CloudsearchQuerySourcesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListQuerySourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_query_sources_list_builder(
        client,
        &args.pageToken,
        &args.requestOptions_debugOptions_enableDebugging,
        &args.requestOptions_languageCode,
        &args.requestOptions_searchApplicationId,
        &args.requestOptions_timeZone,
    )?;
    cloudsearch_query_sources_list_execute(builder)
}

/// GET v1/settings/customer
/// Get customer settings. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_get_customer_execute()` to send, or `cloudsearch_settings_get_customer` for simplest API.

pub fn cloudsearch_settings_get_customer_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/settings/customer",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/settings/customer
/// Get customer settings. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_get_customer_execute()` or `cloudsearch_settings_get_customer`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_get_customer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_get_customer_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomerSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomerSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/settings/customer
/// Get customer settings. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_get_customer_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_get_customer_task()`.
/// For the simplest API, use `cloudsearch_settings_get_customer()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_get_customer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_get_customer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomerSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_get_customer_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/settings/customer
/// Get customer settings. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_get_customer_builder()` + `cloudsearch_settings_get_customer_execute()`.
/// For task-level control, use `cloudsearch_settings_get_customer_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_get_customer(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomerSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_get_customer_builder(client)?;
    cloudsearch_settings_get_customer_execute(builder)
}

/// PATCH v1/settings/customer
/// Update customer settings. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_update_customer_execute()` to send, or `cloudsearch_settings_update_customer` for simplest API.

pub fn cloudsearch_settings_update_customer_builder<R>(
    client: &SimpleHttpClient<R>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/settings/customer",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/settings/customer
/// Update customer settings. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_update_customer_execute()` or `cloudsearch_settings_update_customer`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_update_customer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_update_customer_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/settings/customer
/// Update customer settings. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_update_customer_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_update_customer_task()`.
/// For the simplest API, use `cloudsearch_settings_update_customer()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_update_customer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_update_customer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_update_customer_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_settings_update_customer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchSettingsUpdateCustomerArgs {
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/settings/customer
/// Update customer settings. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_update_customer_builder()` + `cloudsearch_settings_update_customer_execute()`.
/// For task-level control, use `cloudsearch_settings_update_customer_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_update_customer(
    client: &SimpleHttpClient,
    args: &CloudsearchSettingsUpdateCustomerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_update_customer_builder(client, &args.updateMask)?;
    cloudsearch_settings_update_customer_execute(builder)
}

/// POST v1/settings/datasources
/// Creates a datasource. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_datasources_create_execute()` to send, or `cloudsearch_settings_datasources_create` for simplest API.

pub fn cloudsearch_settings_datasources_create_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/settings/datasources",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/settings/datasources
/// Creates a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_datasources_create_execute()` or `cloudsearch_settings_datasources_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_datasources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/settings/datasources
/// Creates a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_datasources_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_datasources_create_task()`.
/// For the simplest API, use `cloudsearch_settings_datasources_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_datasources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_datasources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_datasources_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/settings/datasources
/// Creates a datasource. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_datasources_create_builder()` + `cloudsearch_settings_datasources_create_execute()`.
/// For task-level control, use `cloudsearch_settings_datasources_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_create(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_datasources_create_builder(client)?;
    cloudsearch_settings_datasources_create_execute(builder)
}

/// DELETE v1/settings/datasources/{datasourcesId}
/// Deletes a datasource. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_datasources_delete_execute()` to send, or `cloudsearch_settings_datasources_delete` for simplest API.

pub fn cloudsearch_settings_datasources_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    debugOptions_enableDebugging: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/datasources/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/settings/datasources/{datasourcesId}
/// Deletes a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_datasources_delete_execute()` or `cloudsearch_settings_datasources_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_datasources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/settings/datasources/{datasourcesId}
/// Deletes a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_datasources_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_datasources_delete_task()`.
/// For the simplest API, use `cloudsearch_settings_datasources_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_datasources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_datasources_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_datasources_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_settings_datasources_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchSettingsDatasourcesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
}

/// DELETE v1/settings/datasources/{datasourcesId}
/// Deletes a datasource. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_datasources_delete_builder()` + `cloudsearch_settings_datasources_delete_execute()`.
/// For task-level control, use `cloudsearch_settings_datasources_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_delete(
    client: &SimpleHttpClient,
    args: &CloudsearchSettingsDatasourcesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_datasources_delete_builder(
        client,
        &args.name,
        &args.debugOptions_enableDebugging,
    )?;
    cloudsearch_settings_datasources_delete_execute(builder)
}

/// GET v1/settings/datasources/{datasourcesId}
/// Gets a datasource. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_datasources_get_execute()` to send, or `cloudsearch_settings_datasources_get` for simplest API.

pub fn cloudsearch_settings_datasources_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    debugOptions_enableDebugging: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/datasources/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/settings/datasources/{datasourcesId}
/// Gets a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_datasources_get_execute()` or `cloudsearch_settings_datasources_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_datasources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataSource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataSource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/settings/datasources/{datasourcesId}
/// Gets a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_datasources_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_datasources_get_task()`.
/// For the simplest API, use `cloudsearch_settings_datasources_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_datasources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_datasources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_datasources_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_settings_datasources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchSettingsDatasourcesGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
}

/// GET v1/settings/datasources/{datasourcesId}
/// Gets a datasource. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_datasources_get_builder()` + `cloudsearch_settings_datasources_get_execute()`.
/// For task-level control, use `cloudsearch_settings_datasources_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_get(
    client: &SimpleHttpClient,
    args: &CloudsearchSettingsDatasourcesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_datasources_get_builder(
        client,
        &args.name,
        &args.debugOptions_enableDebugging,
    )?;
    cloudsearch_settings_datasources_get_execute(builder)
}

/// GET v1/settings/datasources
/// Lists datasources. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_datasources_list_execute()` to send, or `cloudsearch_settings_datasources_list` for simplest API.

pub fn cloudsearch_settings_datasources_list_builder<R>(
    client: &SimpleHttpClient<R>,
    debugOptions_enableDebugging: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/settings/datasources",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/settings/datasources
/// Lists datasources. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_datasources_list_execute()` or `cloudsearch_settings_datasources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_datasources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDataSourceResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDataSourceResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/settings/datasources
/// Lists datasources. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_datasources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_datasources_list_task()`.
/// For the simplest API, use `cloudsearch_settings_datasources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_datasources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_datasources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataSourceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_datasources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_settings_datasources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchSettingsDatasourcesListArgs {
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/settings/datasources
/// Lists datasources. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_datasources_list_builder()` + `cloudsearch_settings_datasources_list_execute()`.
/// For task-level control, use `cloudsearch_settings_datasources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_list(
    client: &SimpleHttpClient,
    args: &CloudsearchSettingsDatasourcesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataSourceResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_datasources_list_builder(
        client,
        &args.debugOptions_enableDebugging,
        &args.pageSize,
        &args.pageToken,
    )?;
    cloudsearch_settings_datasources_list_execute(builder)
}

/// PATCH v1/settings/datasources/{datasourcesId}
/// Updates a datasource. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_datasources_patch_execute()` to send, or `cloudsearch_settings_datasources_patch` for simplest API.

pub fn cloudsearch_settings_datasources_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    debugOptions_enableDebugging: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/datasources/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/settings/datasources/{datasourcesId}
/// Updates a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_datasources_patch_execute()` or `cloudsearch_settings_datasources_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_datasources_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/settings/datasources/{datasourcesId}
/// Updates a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_datasources_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_datasources_patch_task()`.
/// For the simplest API, use `cloudsearch_settings_datasources_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_datasources_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_datasources_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_datasources_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_settings_datasources_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchSettingsDatasourcesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/settings/datasources/{datasourcesId}
/// Updates a datasource. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_datasources_patch_builder()` + `cloudsearch_settings_datasources_patch_execute()`.
/// For task-level control, use `cloudsearch_settings_datasources_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_patch(
    client: &SimpleHttpClient,
    args: &CloudsearchSettingsDatasourcesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_datasources_patch_builder(
        client,
        &args.name,
        &args.debugOptions_enableDebugging,
        &args.updateMask,
    )?;
    cloudsearch_settings_datasources_patch_execute(builder)
}

/// PUT v1/settings/datasources/{datasourcesId}
/// Updates a datasource. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_datasources_update_execute()` to send, or `cloudsearch_settings_datasources_update` for simplest API.

pub fn cloudsearch_settings_datasources_update_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/datasources/{}",
        name,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/settings/datasources/{datasourcesId}
/// Updates a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_datasources_update_execute()` or `cloudsearch_settings_datasources_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_datasources_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/settings/datasources/{datasourcesId}
/// Updates a datasource. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_datasources_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_datasources_update_task()`.
/// For the simplest API, use `cloudsearch_settings_datasources_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_datasources_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_datasources_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_datasources_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_settings_datasources_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchSettingsDatasourcesUpdateArgs {
    /// Path parameter: name
    pub name: String,
}

/// PUT v1/settings/datasources/{datasourcesId}
/// Updates a datasource. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_datasources_update_builder()` + `cloudsearch_settings_datasources_update_execute()`.
/// For task-level control, use `cloudsearch_settings_datasources_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_datasources_update(
    client: &SimpleHttpClient,
    args: &CloudsearchSettingsDatasourcesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_datasources_update_builder(client, &args.name)?;
    cloudsearch_settings_datasources_update_execute(builder)
}

/// POST v1/settings/searchapplications
/// Creates a search application. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_searchapplications_create_execute()` to send, or `cloudsearch_settings_searchapplications_create` for simplest API.

pub fn cloudsearch_settings_searchapplications_create_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://cloudsearch.googleapis.com/v1/settings/searchapplications",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/settings/searchapplications
/// Creates a search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_searchapplications_create_execute()` or `cloudsearch_settings_searchapplications_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_searchapplications_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/settings/searchapplications
/// Creates a search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_searchapplications_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_searchapplications_create_task()`.
/// For the simplest API, use `cloudsearch_settings_searchapplications_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_searchapplications_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_searchapplications_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_searchapplications_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/settings/searchapplications
/// Creates a search application. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_searchapplications_create_builder()` + `cloudsearch_settings_searchapplications_create_execute()`.
/// For task-level control, use `cloudsearch_settings_searchapplications_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_create(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_searchapplications_create_builder(client)?;
    cloudsearch_settings_searchapplications_create_execute(builder)
}

/// DELETE v1/settings/searchapplications/{searchapplicationsId}
/// Deletes a search application. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_searchapplications_delete_execute()` to send, or `cloudsearch_settings_searchapplications_delete` for simplest API.

pub fn cloudsearch_settings_searchapplications_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    debugOptions_enableDebugging: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/searchapplications/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/settings/searchapplications/{searchapplicationsId}
/// Deletes a search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_searchapplications_delete_execute()` or `cloudsearch_settings_searchapplications_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_searchapplications_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/settings/searchapplications/{searchapplicationsId}
/// Deletes a search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_searchapplications_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_searchapplications_delete_task()`.
/// For the simplest API, use `cloudsearch_settings_searchapplications_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_searchapplications_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_searchapplications_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_searchapplications_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_settings_searchapplications_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchSettingsSearchapplicationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
}

/// DELETE v1/settings/searchapplications/{searchapplicationsId}
/// Deletes a search application. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_searchapplications_delete_builder()` + `cloudsearch_settings_searchapplications_delete_execute()`.
/// For task-level control, use `cloudsearch_settings_searchapplications_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_delete(
    client: &SimpleHttpClient,
    args: &CloudsearchSettingsSearchapplicationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_searchapplications_delete_builder(
        client,
        &args.name,
        &args.debugOptions_enableDebugging,
    )?;
    cloudsearch_settings_searchapplications_delete_execute(builder)
}

/// GET v1/settings/searchapplications/{searchapplicationsId}
/// Gets the specified search application. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_searchapplications_get_execute()` to send, or `cloudsearch_settings_searchapplications_get` for simplest API.

pub fn cloudsearch_settings_searchapplications_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    debugOptions_enableDebugging: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/searchapplications/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/settings/searchapplications/{searchapplicationsId}
/// Gets the specified search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_searchapplications_get_execute()` or `cloudsearch_settings_searchapplications_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_searchapplications_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchApplication>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchApplication = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/settings/searchapplications/{searchapplicationsId}
/// Gets the specified search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_searchapplications_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_searchapplications_get_task()`.
/// For the simplest API, use `cloudsearch_settings_searchapplications_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_searchapplications_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_searchapplications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchApplication>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_searchapplications_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_settings_searchapplications_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchSettingsSearchapplicationsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
}

/// GET v1/settings/searchapplications/{searchapplicationsId}
/// Gets the specified search application. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_searchapplications_get_builder()` + `cloudsearch_settings_searchapplications_get_execute()`.
/// For task-level control, use `cloudsearch_settings_searchapplications_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_get(
    client: &SimpleHttpClient,
    args: &CloudsearchSettingsSearchapplicationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchApplication>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_searchapplications_get_builder(
        client,
        &args.name,
        &args.debugOptions_enableDebugging,
    )?;
    cloudsearch_settings_searchapplications_get_execute(builder)
}

/// GET v1/settings/searchapplications
/// Lists all search applications. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_searchapplications_list_execute()` to send, or `cloudsearch_settings_searchapplications_list` for simplest API.

pub fn cloudsearch_settings_searchapplications_list_builder<R>(
    client: &SimpleHttpClient<R>,
    debugOptions_enableDebugging: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://cloudsearch.googleapis.com/v1/settings/searchapplications",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = debugOptions_enableDebugging.as_ref() {
        query_parts.push(format!("debugOptions.enableDebugging={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/settings/searchapplications
/// Lists all search applications. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_searchapplications_list_execute()` or `cloudsearch_settings_searchapplications_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_searchapplications_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSearchApplicationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSearchApplicationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/settings/searchapplications
/// Lists all search applications. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_searchapplications_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_searchapplications_list_task()`.
/// For the simplest API, use `cloudsearch_settings_searchapplications_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_searchapplications_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_searchapplications_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSearchApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_searchapplications_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_settings_searchapplications_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchSettingsSearchapplicationsListArgs {
    /// Query parameter: debugOptions_enableDebugging
    pub debugOptions_enableDebugging: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/settings/searchapplications
/// Lists all search applications. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_searchapplications_list_builder()` + `cloudsearch_settings_searchapplications_list_execute()`.
/// For task-level control, use `cloudsearch_settings_searchapplications_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_list(
    client: &SimpleHttpClient,
    args: &CloudsearchSettingsSearchapplicationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSearchApplicationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_searchapplications_list_builder(
        client,
        &args.debugOptions_enableDebugging,
        &args.pageSize,
        &args.pageToken,
    )?;
    cloudsearch_settings_searchapplications_list_execute(builder)
}

/// PATCH v1/settings/searchapplications/{searchapplicationsId}
/// Updates a search application. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_searchapplications_patch_execute()` to send, or `cloudsearch_settings_searchapplications_patch` for simplest API.

pub fn cloudsearch_settings_searchapplications_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/searchapplications/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/settings/searchapplications/{searchapplicationsId}
/// Updates a search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_searchapplications_patch_execute()` or `cloudsearch_settings_searchapplications_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_searchapplications_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/settings/searchapplications/{searchapplicationsId}
/// Updates a search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_searchapplications_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_searchapplications_patch_task()`.
/// For the simplest API, use `cloudsearch_settings_searchapplications_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_searchapplications_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_searchapplications_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_searchapplications_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_settings_searchapplications_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchSettingsSearchapplicationsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/settings/searchapplications/{searchapplicationsId}
/// Updates a search application. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_searchapplications_patch_builder()` + `cloudsearch_settings_searchapplications_patch_execute()`.
/// For task-level control, use `cloudsearch_settings_searchapplications_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_patch(
    client: &SimpleHttpClient,
    args: &CloudsearchSettingsSearchapplicationsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_searchapplications_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    cloudsearch_settings_searchapplications_patch_execute(builder)
}

/// POST v1/settings/searchapplications/{searchapplicationsId}:reset
/// Resets a search application to default settings. This will return an empty response. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_searchapplications_reset_execute()` to send, or `cloudsearch_settings_searchapplications_reset` for simplest API.

pub fn cloudsearch_settings_searchapplications_reset_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/searchapplications/{}:reset",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/settings/searchapplications/{searchapplicationsId}:reset
/// Resets a search application to default settings. This will return an empty response. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_searchapplications_reset_execute()` or `cloudsearch_settings_searchapplications_reset`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_searchapplications_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_reset_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/settings/searchapplications/{searchapplicationsId}:reset
/// Resets a search application to default settings. This will return an empty response. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_searchapplications_reset_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_searchapplications_reset_task()`.
/// For the simplest API, use `cloudsearch_settings_searchapplications_reset()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_searchapplications_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_searchapplications_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_searchapplications_reset_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_settings_searchapplications_reset`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchSettingsSearchapplicationsResetArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/settings/searchapplications/{searchapplicationsId}:reset
/// Resets a search application to default settings. This will return an empty response. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_searchapplications_reset_builder()` + `cloudsearch_settings_searchapplications_reset_execute()`.
/// For task-level control, use `cloudsearch_settings_searchapplications_reset_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_reset(
    client: &SimpleHttpClient,
    args: &CloudsearchSettingsSearchapplicationsResetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_searchapplications_reset_builder(client, &args.name)?;
    cloudsearch_settings_searchapplications_reset_execute(builder)
}

/// PUT v1/settings/searchapplications/{searchapplicationsId}
/// Updates a search application. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_settings_searchapplications_update_execute()` to send, or `cloudsearch_settings_searchapplications_update` for simplest API.

pub fn cloudsearch_settings_searchapplications_update_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/searchapplications/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/settings/searchapplications/{searchapplicationsId}
/// Updates a search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_settings_searchapplications_update_execute()` or `cloudsearch_settings_searchapplications_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_searchapplications_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/settings/searchapplications/{searchapplicationsId}
/// Updates a search application. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_settings_searchapplications_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_settings_searchapplications_update_task()`.
/// For the simplest API, use `cloudsearch_settings_searchapplications_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_settings_searchapplications_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_settings_searchapplications_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_settings_searchapplications_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_settings_searchapplications_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchSettingsSearchapplicationsUpdateArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PUT v1/settings/searchapplications/{searchapplicationsId}
/// Updates a search application. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_settings_searchapplications_update_builder()` + `cloudsearch_settings_searchapplications_update_execute()`.
/// For task-level control, use `cloudsearch_settings_searchapplications_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_settings_searchapplications_update(
    client: &SimpleHttpClient,
    args: &CloudsearchSettingsSearchapplicationsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_settings_searchapplications_update_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    cloudsearch_settings_searchapplications_update_execute(builder)
}

/// GET v1/stats/index
/// Gets indexed item statistics aggreggated across all data sources. This API only returns statistics for previous dates; it doesn''t return statistics for the current day. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_get_index_execute()` to send, or `cloudsearch_stats_get_index` for simplest API.

pub fn cloudsearch_stats_get_index_builder<R>(
    client: &SimpleHttpClient<R>,
    fromDate_day: &Option<Option<String>>,
    fromDate_month: &Option<Option<String>>,
    fromDate_year: &Option<Option<String>>,
    toDate_day: &Option<Option<String>>,
    toDate_month: &Option<Option<String>>,
    toDate_year: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/stats/index",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fromDate_day.as_ref() {
        query_parts.push(format!("fromDate.day={}", val));
    }
    if let Some(val) = fromDate_month.as_ref() {
        query_parts.push(format!("fromDate.month={}", val));
    }
    if let Some(val) = fromDate_year.as_ref() {
        query_parts.push(format!("fromDate.year={}", val));
    }
    if let Some(val) = toDate_day.as_ref() {
        query_parts.push(format!("toDate.day={}", val));
    }
    if let Some(val) = toDate_month.as_ref() {
        query_parts.push(format!("toDate.month={}", val));
    }
    if let Some(val) = toDate_year.as_ref() {
        query_parts.push(format!("toDate.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/stats/index
/// Gets indexed item statistics aggreggated across all data sources. This API only returns statistics for previous dates; it doesn''t return statistics for the current day. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_stats_get_index_execute()` or `cloudsearch_stats_get_index`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_get_index_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_index_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetCustomerIndexStatsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetCustomerIndexStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/stats/index
/// Gets indexed item statistics aggreggated across all data sources. This API only returns statistics for previous dates; it doesn''t return statistics for the current day. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_stats_get_index_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_stats_get_index_task()`.
/// For the simplest API, use `cloudsearch_stats_get_index()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_get_index_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_stats_get_index_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerIndexStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_stats_get_index_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_stats_get_index`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchStatsGetIndexArgs {
    /// Query parameter: fromDate_day
    pub fromDate_day: Option<Option<String>>,
    /// Query parameter: fromDate_month
    pub fromDate_month: Option<Option<String>>,
    /// Query parameter: fromDate_year
    pub fromDate_year: Option<Option<String>>,
    /// Query parameter: toDate_day
    pub toDate_day: Option<Option<String>>,
    /// Query parameter: toDate_month
    pub toDate_month: Option<Option<String>>,
    /// Query parameter: toDate_year
    pub toDate_year: Option<Option<String>>,
}

/// GET v1/stats/index
/// Gets indexed item statistics aggreggated across all data sources. This API only returns statistics for previous dates; it doesn''t return statistics for the current day. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_get_index_builder()` + `cloudsearch_stats_get_index_execute()`.
/// For task-level control, use `cloudsearch_stats_get_index_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_index(
    client: &SimpleHttpClient,
    args: &CloudsearchStatsGetIndexArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerIndexStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_get_index_builder(
        client,
        &args.fromDate_day,
        &args.fromDate_month,
        &args.fromDate_year,
        &args.toDate_day,
        &args.toDate_month,
        &args.toDate_year,
    )?;
    cloudsearch_stats_get_index_execute(builder)
}

/// GET v1/stats/query
/// Get the query statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_get_query_execute()` to send, or `cloudsearch_stats_get_query` for simplest API.

pub fn cloudsearch_stats_get_query_builder<R>(
    client: &SimpleHttpClient<R>,
    fromDate_day: &Option<Option<String>>,
    fromDate_month: &Option<Option<String>>,
    fromDate_year: &Option<Option<String>>,
    toDate_day: &Option<Option<String>>,
    toDate_month: &Option<Option<String>>,
    toDate_year: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/stats/query",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fromDate_day.as_ref() {
        query_parts.push(format!("fromDate.day={}", val));
    }
    if let Some(val) = fromDate_month.as_ref() {
        query_parts.push(format!("fromDate.month={}", val));
    }
    if let Some(val) = fromDate_year.as_ref() {
        query_parts.push(format!("fromDate.year={}", val));
    }
    if let Some(val) = toDate_day.as_ref() {
        query_parts.push(format!("toDate.day={}", val));
    }
    if let Some(val) = toDate_month.as_ref() {
        query_parts.push(format!("toDate.month={}", val));
    }
    if let Some(val) = toDate_year.as_ref() {
        query_parts.push(format!("toDate.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/stats/query
/// Get the query statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_stats_get_query_execute()` or `cloudsearch_stats_get_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_get_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetCustomerQueryStatsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetCustomerQueryStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/stats/query
/// Get the query statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_stats_get_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_stats_get_query_task()`.
/// For the simplest API, use `cloudsearch_stats_get_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_get_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_stats_get_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerQueryStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_stats_get_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_stats_get_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchStatsGetQueryArgs {
    /// Query parameter: fromDate_day
    pub fromDate_day: Option<Option<String>>,
    /// Query parameter: fromDate_month
    pub fromDate_month: Option<Option<String>>,
    /// Query parameter: fromDate_year
    pub fromDate_year: Option<Option<String>>,
    /// Query parameter: toDate_day
    pub toDate_day: Option<Option<String>>,
    /// Query parameter: toDate_month
    pub toDate_month: Option<Option<String>>,
    /// Query parameter: toDate_year
    pub toDate_year: Option<Option<String>>,
}

/// GET v1/stats/query
/// Get the query statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_get_query_builder()` + `cloudsearch_stats_get_query_execute()`.
/// For task-level control, use `cloudsearch_stats_get_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_query(
    client: &SimpleHttpClient,
    args: &CloudsearchStatsGetQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerQueryStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_get_query_builder(
        client,
        &args.fromDate_day,
        &args.fromDate_month,
        &args.fromDate_year,
        &args.toDate_day,
        &args.toDate_month,
        &args.toDate_year,
    )?;
    cloudsearch_stats_get_query_execute(builder)
}

/// GET v1/stats/searchapplication
/// Get search application stats for customer. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_get_searchapplication_execute()` to send, or `cloudsearch_stats_get_searchapplication` for simplest API.

pub fn cloudsearch_stats_get_searchapplication_builder<R>(
    client: &SimpleHttpClient<R>,
    endDate_day: &Option<Option<String>>,
    endDate_month: &Option<Option<String>>,
    endDate_year: &Option<Option<String>>,
    startDate_day: &Option<Option<String>>,
    startDate_month: &Option<Option<String>>,
    startDate_year: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/stats/searchapplication",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endDate_day.as_ref() {
        query_parts.push(format!("endDate.day={}", val));
    }
    if let Some(val) = endDate_month.as_ref() {
        query_parts.push(format!("endDate.month={}", val));
    }
    if let Some(val) = endDate_year.as_ref() {
        query_parts.push(format!("endDate.year={}", val));
    }
    if let Some(val) = startDate_day.as_ref() {
        query_parts.push(format!("startDate.day={}", val));
    }
    if let Some(val) = startDate_month.as_ref() {
        query_parts.push(format!("startDate.month={}", val));
    }
    if let Some(val) = startDate_year.as_ref() {
        query_parts.push(format!("startDate.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/stats/searchapplication
/// Get search application stats for customer. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_stats_get_searchapplication_execute()` or `cloudsearch_stats_get_searchapplication`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_get_searchapplication_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_searchapplication_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetCustomerSearchApplicationStatsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetCustomerSearchApplicationStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/stats/searchapplication
/// Get search application stats for customer. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_stats_get_searchapplication_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_stats_get_searchapplication_task()`.
/// For the simplest API, use `cloudsearch_stats_get_searchapplication()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_get_searchapplication_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_stats_get_searchapplication_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerSearchApplicationStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_stats_get_searchapplication_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_stats_get_searchapplication`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchStatsGetSearchapplicationArgs {
    /// Query parameter: endDate_day
    pub endDate_day: Option<Option<String>>,
    /// Query parameter: endDate_month
    pub endDate_month: Option<Option<String>>,
    /// Query parameter: endDate_year
    pub endDate_year: Option<Option<String>>,
    /// Query parameter: startDate_day
    pub startDate_day: Option<Option<String>>,
    /// Query parameter: startDate_month
    pub startDate_month: Option<Option<String>>,
    /// Query parameter: startDate_year
    pub startDate_year: Option<Option<String>>,
}

/// GET v1/stats/searchapplication
/// Get search application stats for customer. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_get_searchapplication_builder()` + `cloudsearch_stats_get_searchapplication_execute()`.
/// For task-level control, use `cloudsearch_stats_get_searchapplication_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_searchapplication(
    client: &SimpleHttpClient,
    args: &CloudsearchStatsGetSearchapplicationArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerSearchApplicationStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_get_searchapplication_builder(
        client,
        &args.endDate_day,
        &args.endDate_month,
        &args.endDate_year,
        &args.startDate_day,
        &args.startDate_month,
        &args.startDate_year,
    )?;
    cloudsearch_stats_get_searchapplication_execute(builder)
}

/// GET v1/stats/session
/// Get the # of search sessions, % of successful sessions with a click query statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_get_session_execute()` to send, or `cloudsearch_stats_get_session` for simplest API.

pub fn cloudsearch_stats_get_session_builder<R>(
    client: &SimpleHttpClient<R>,
    fromDate_day: &Option<Option<String>>,
    fromDate_month: &Option<Option<String>>,
    fromDate_year: &Option<Option<String>>,
    toDate_day: &Option<Option<String>>,
    toDate_month: &Option<Option<String>>,
    toDate_year: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/stats/session",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fromDate_day.as_ref() {
        query_parts.push(format!("fromDate.day={}", val));
    }
    if let Some(val) = fromDate_month.as_ref() {
        query_parts.push(format!("fromDate.month={}", val));
    }
    if let Some(val) = fromDate_year.as_ref() {
        query_parts.push(format!("fromDate.year={}", val));
    }
    if let Some(val) = toDate_day.as_ref() {
        query_parts.push(format!("toDate.day={}", val));
    }
    if let Some(val) = toDate_month.as_ref() {
        query_parts.push(format!("toDate.month={}", val));
    }
    if let Some(val) = toDate_year.as_ref() {
        query_parts.push(format!("toDate.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/stats/session
/// Get the # of search sessions, % of successful sessions with a click query statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_stats_get_session_execute()` or `cloudsearch_stats_get_session`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_get_session_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_session_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetCustomerSessionStatsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetCustomerSessionStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/stats/session
/// Get the # of search sessions, % of successful sessions with a click query statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_stats_get_session_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_stats_get_session_task()`.
/// For the simplest API, use `cloudsearch_stats_get_session()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_get_session_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_stats_get_session_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerSessionStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_stats_get_session_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_stats_get_session`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchStatsGetSessionArgs {
    /// Query parameter: fromDate_day
    pub fromDate_day: Option<Option<String>>,
    /// Query parameter: fromDate_month
    pub fromDate_month: Option<Option<String>>,
    /// Query parameter: fromDate_year
    pub fromDate_year: Option<Option<String>>,
    /// Query parameter: toDate_day
    pub toDate_day: Option<Option<String>>,
    /// Query parameter: toDate_month
    pub toDate_month: Option<Option<String>>,
    /// Query parameter: toDate_year
    pub toDate_year: Option<Option<String>>,
}

/// GET v1/stats/session
/// Get the # of search sessions, % of successful sessions with a click query statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_get_session_builder()` + `cloudsearch_stats_get_session_execute()`.
/// For task-level control, use `cloudsearch_stats_get_session_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_session(
    client: &SimpleHttpClient,
    args: &CloudsearchStatsGetSessionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerSessionStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_get_session_builder(
        client,
        &args.fromDate_day,
        &args.fromDate_month,
        &args.fromDate_year,
        &args.toDate_day,
        &args.toDate_month,
        &args.toDate_year,
    )?;
    cloudsearch_stats_get_session_execute(builder)
}

/// GET v1/stats/user
/// Get the users statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_get_user_execute()` to send, or `cloudsearch_stats_get_user` for simplest API.

pub fn cloudsearch_stats_get_user_builder<R>(
    client: &SimpleHttpClient<R>,
    fromDate_day: &Option<Option<String>>,
    fromDate_month: &Option<Option<String>>,
    fromDate_year: &Option<Option<String>>,
    toDate_day: &Option<Option<String>>,
    toDate_month: &Option<Option<String>>,
    toDate_year: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/stats/user",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fromDate_day.as_ref() {
        query_parts.push(format!("fromDate.day={}", val));
    }
    if let Some(val) = fromDate_month.as_ref() {
        query_parts.push(format!("fromDate.month={}", val));
    }
    if let Some(val) = fromDate_year.as_ref() {
        query_parts.push(format!("fromDate.year={}", val));
    }
    if let Some(val) = toDate_day.as_ref() {
        query_parts.push(format!("toDate.day={}", val));
    }
    if let Some(val) = toDate_month.as_ref() {
        query_parts.push(format!("toDate.month={}", val));
    }
    if let Some(val) = toDate_year.as_ref() {
        query_parts.push(format!("toDate.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/stats/user
/// Get the users statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_stats_get_user_execute()` or `cloudsearch_stats_get_user`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_get_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_user_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetCustomerUserStatsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetCustomerUserStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/stats/user
/// Get the users statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_stats_get_user_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_stats_get_user_task()`.
/// For the simplest API, use `cloudsearch_stats_get_user()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_get_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_stats_get_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerUserStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_stats_get_user_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_stats_get_user`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchStatsGetUserArgs {
    /// Query parameter: fromDate_day
    pub fromDate_day: Option<Option<String>>,
    /// Query parameter: fromDate_month
    pub fromDate_month: Option<Option<String>>,
    /// Query parameter: fromDate_year
    pub fromDate_year: Option<Option<String>>,
    /// Query parameter: toDate_day
    pub toDate_day: Option<Option<String>>,
    /// Query parameter: toDate_month
    pub toDate_month: Option<Option<String>>,
    /// Query parameter: toDate_year
    pub toDate_year: Option<Option<String>>,
}

/// GET v1/stats/user
/// Get the users statistics for customer. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_get_user_builder()` + `cloudsearch_stats_get_user_execute()`.
/// For task-level control, use `cloudsearch_stats_get_user_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_get_user(
    client: &SimpleHttpClient,
    args: &CloudsearchStatsGetUserArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetCustomerUserStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_get_user_builder(
        client,
        &args.fromDate_day,
        &args.fromDate_month,
        &args.fromDate_year,
        &args.toDate_day,
        &args.toDate_month,
        &args.toDate_year,
    )?;
    cloudsearch_stats_get_user_execute(builder)
}

/// GET v1/stats/index/datasources/{datasourcesId}
/// Gets indexed item statistics for a single data source. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_index_datasources_get_execute()` to send, or `cloudsearch_stats_index_datasources_get` for simplest API.

pub fn cloudsearch_stats_index_datasources_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    fromDate_day: &Option<Option<String>>,
    fromDate_month: &Option<Option<String>>,
    fromDate_year: &Option<Option<String>>,
    toDate_day: &Option<Option<String>>,
    toDate_month: &Option<Option<String>>,
    toDate_year: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/stats/index/datasources/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fromDate_day.as_ref() {
        query_parts.push(format!("fromDate.day={}", val));
    }
    if let Some(val) = fromDate_month.as_ref() {
        query_parts.push(format!("fromDate.month={}", val));
    }
    if let Some(val) = fromDate_year.as_ref() {
        query_parts.push(format!("fromDate.year={}", val));
    }
    if let Some(val) = toDate_day.as_ref() {
        query_parts.push(format!("toDate.day={}", val));
    }
    if let Some(val) = toDate_month.as_ref() {
        query_parts.push(format!("toDate.month={}", val));
    }
    if let Some(val) = toDate_year.as_ref() {
        query_parts.push(format!("toDate.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/stats/index/datasources/{datasourcesId}
/// Gets indexed item statistics for a single data source. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_stats_index_datasources_get_execute()` or `cloudsearch_stats_index_datasources_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_index_datasources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_index_datasources_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetDataSourceIndexStatsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetDataSourceIndexStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/stats/index/datasources/{datasourcesId}
/// Gets indexed item statistics for a single data source. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_stats_index_datasources_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_stats_index_datasources_get_task()`.
/// For the simplest API, use `cloudsearch_stats_index_datasources_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_index_datasources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_stats_index_datasources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetDataSourceIndexStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_stats_index_datasources_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_stats_index_datasources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchStatsIndexDatasourcesGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: fromDate_day
    pub fromDate_day: Option<Option<String>>,
    /// Query parameter: fromDate_month
    pub fromDate_month: Option<Option<String>>,
    /// Query parameter: fromDate_year
    pub fromDate_year: Option<Option<String>>,
    /// Query parameter: toDate_day
    pub toDate_day: Option<Option<String>>,
    /// Query parameter: toDate_month
    pub toDate_month: Option<Option<String>>,
    /// Query parameter: toDate_year
    pub toDate_year: Option<Option<String>>,
}

/// GET v1/stats/index/datasources/{datasourcesId}
/// Gets indexed item statistics for a single data source. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_index_datasources_get_builder()` + `cloudsearch_stats_index_datasources_get_execute()`.
/// For task-level control, use `cloudsearch_stats_index_datasources_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_index_datasources_get(
    client: &SimpleHttpClient,
    args: &CloudsearchStatsIndexDatasourcesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetDataSourceIndexStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_index_datasources_get_builder(
        client,
        &args.name,
        &args.fromDate_day,
        &args.fromDate_month,
        &args.fromDate_year,
        &args.toDate_day,
        &args.toDate_month,
        &args.toDate_year,
    )?;
    cloudsearch_stats_index_datasources_get_execute(builder)
}

/// GET v1/stats/query/searchapplications/{searchapplicationsId}
/// Get the query statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_query_searchapplications_get_execute()` to send, or `cloudsearch_stats_query_searchapplications_get` for simplest API.

pub fn cloudsearch_stats_query_searchapplications_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    fromDate_day: &Option<Option<String>>,
    fromDate_month: &Option<Option<String>>,
    fromDate_year: &Option<Option<String>>,
    toDate_day: &Option<Option<String>>,
    toDate_month: &Option<Option<String>>,
    toDate_year: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/stats/query/searchapplications/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fromDate_day.as_ref() {
        query_parts.push(format!("fromDate.day={}", val));
    }
    if let Some(val) = fromDate_month.as_ref() {
        query_parts.push(format!("fromDate.month={}", val));
    }
    if let Some(val) = fromDate_year.as_ref() {
        query_parts.push(format!("fromDate.year={}", val));
    }
    if let Some(val) = toDate_day.as_ref() {
        query_parts.push(format!("toDate.day={}", val));
    }
    if let Some(val) = toDate_month.as_ref() {
        query_parts.push(format!("toDate.month={}", val));
    }
    if let Some(val) = toDate_year.as_ref() {
        query_parts.push(format!("toDate.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/stats/query/searchapplications/{searchapplicationsId}
/// Get the query statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_stats_query_searchapplications_get_execute()` or `cloudsearch_stats_query_searchapplications_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_query_searchapplications_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_query_searchapplications_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetSearchApplicationQueryStatsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetSearchApplicationQueryStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/stats/query/searchapplications/{searchapplicationsId}
/// Get the query statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_stats_query_searchapplications_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_stats_query_searchapplications_get_task()`.
/// For the simplest API, use `cloudsearch_stats_query_searchapplications_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_query_searchapplications_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_stats_query_searchapplications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetSearchApplicationQueryStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_stats_query_searchapplications_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_stats_query_searchapplications_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchStatsQuerySearchapplicationsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: fromDate_day
    pub fromDate_day: Option<Option<String>>,
    /// Query parameter: fromDate_month
    pub fromDate_month: Option<Option<String>>,
    /// Query parameter: fromDate_year
    pub fromDate_year: Option<Option<String>>,
    /// Query parameter: toDate_day
    pub toDate_day: Option<Option<String>>,
    /// Query parameter: toDate_month
    pub toDate_month: Option<Option<String>>,
    /// Query parameter: toDate_year
    pub toDate_year: Option<Option<String>>,
}

/// GET v1/stats/query/searchapplications/{searchapplicationsId}
/// Get the query statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_query_searchapplications_get_builder()` + `cloudsearch_stats_query_searchapplications_get_execute()`.
/// For task-level control, use `cloudsearch_stats_query_searchapplications_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_query_searchapplications_get(
    client: &SimpleHttpClient,
    args: &CloudsearchStatsQuerySearchapplicationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetSearchApplicationQueryStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_query_searchapplications_get_builder(
        client,
        &args.name,
        &args.fromDate_day,
        &args.fromDate_month,
        &args.fromDate_year,
        &args.toDate_day,
        &args.toDate_month,
        &args.toDate_year,
    )?;
    cloudsearch_stats_query_searchapplications_get_execute(builder)
}

/// GET v1/stats/session/searchapplications/{searchapplicationsId}
/// Get the # of search sessions, % of successful sessions with a click query statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_session_searchapplications_get_execute()` to send, or `cloudsearch_stats_session_searchapplications_get` for simplest API.

pub fn cloudsearch_stats_session_searchapplications_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    fromDate_day: &Option<Option<String>>,
    fromDate_month: &Option<Option<String>>,
    fromDate_year: &Option<Option<String>>,
    toDate_day: &Option<Option<String>>,
    toDate_month: &Option<Option<String>>,
    toDate_year: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/stats/session/searchapplications/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fromDate_day.as_ref() {
        query_parts.push(format!("fromDate.day={}", val));
    }
    if let Some(val) = fromDate_month.as_ref() {
        query_parts.push(format!("fromDate.month={}", val));
    }
    if let Some(val) = fromDate_year.as_ref() {
        query_parts.push(format!("fromDate.year={}", val));
    }
    if let Some(val) = toDate_day.as_ref() {
        query_parts.push(format!("toDate.day={}", val));
    }
    if let Some(val) = toDate_month.as_ref() {
        query_parts.push(format!("toDate.month={}", val));
    }
    if let Some(val) = toDate_year.as_ref() {
        query_parts.push(format!("toDate.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/stats/session/searchapplications/{searchapplicationsId}
/// Get the # of search sessions, % of successful sessions with a click query statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_stats_session_searchapplications_get_execute()` or `cloudsearch_stats_session_searchapplications_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_session_searchapplications_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_session_searchapplications_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetSearchApplicationSessionStatsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetSearchApplicationSessionStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/stats/session/searchapplications/{searchapplicationsId}
/// Get the # of search sessions, % of successful sessions with a click query statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_stats_session_searchapplications_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_stats_session_searchapplications_get_task()`.
/// For the simplest API, use `cloudsearch_stats_session_searchapplications_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_session_searchapplications_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_stats_session_searchapplications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetSearchApplicationSessionStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_stats_session_searchapplications_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_stats_session_searchapplications_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchStatsSessionSearchapplicationsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: fromDate_day
    pub fromDate_day: Option<Option<String>>,
    /// Query parameter: fromDate_month
    pub fromDate_month: Option<Option<String>>,
    /// Query parameter: fromDate_year
    pub fromDate_year: Option<Option<String>>,
    /// Query parameter: toDate_day
    pub toDate_day: Option<Option<String>>,
    /// Query parameter: toDate_month
    pub toDate_month: Option<Option<String>>,
    /// Query parameter: toDate_year
    pub toDate_year: Option<Option<String>>,
}

/// GET v1/stats/session/searchapplications/{searchapplicationsId}
/// Get the # of search sessions, % of successful sessions with a click query statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_session_searchapplications_get_builder()` + `cloudsearch_stats_session_searchapplications_get_execute()`.
/// For task-level control, use `cloudsearch_stats_session_searchapplications_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_session_searchapplications_get(
    client: &SimpleHttpClient,
    args: &CloudsearchStatsSessionSearchapplicationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetSearchApplicationSessionStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_session_searchapplications_get_builder(
        client,
        &args.name,
        &args.fromDate_day,
        &args.fromDate_month,
        &args.fromDate_year,
        &args.toDate_day,
        &args.toDate_month,
        &args.toDate_year,
    )?;
    cloudsearch_stats_session_searchapplications_get_execute(builder)
}

/// GET v1/stats/user/searchapplications/{searchapplicationsId}
/// Get the users statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_stats_user_searchapplications_get_execute()` to send, or `cloudsearch_stats_user_searchapplications_get` for simplest API.

pub fn cloudsearch_stats_user_searchapplications_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    fromDate_day: &Option<Option<String>>,
    fromDate_month: &Option<Option<String>>,
    fromDate_year: &Option<Option<String>>,
    toDate_day: &Option<Option<String>>,
    toDate_month: &Option<Option<String>>,
    toDate_year: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/stats/user/searchapplications/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fromDate_day.as_ref() {
        query_parts.push(format!("fromDate.day={}", val));
    }
    if let Some(val) = fromDate_month.as_ref() {
        query_parts.push(format!("fromDate.month={}", val));
    }
    if let Some(val) = fromDate_year.as_ref() {
        query_parts.push(format!("fromDate.year={}", val));
    }
    if let Some(val) = toDate_day.as_ref() {
        query_parts.push(format!("toDate.day={}", val));
    }
    if let Some(val) = toDate_month.as_ref() {
        query_parts.push(format!("toDate.month={}", val));
    }
    if let Some(val) = toDate_year.as_ref() {
        query_parts.push(format!("toDate.year={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/stats/user/searchapplications/{searchapplicationsId}
/// Get the users statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_stats_user_searchapplications_get_execute()` or `cloudsearch_stats_user_searchapplications_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_user_searchapplications_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_user_searchapplications_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetSearchApplicationUserStatsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetSearchApplicationUserStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/stats/user/searchapplications/{searchapplicationsId}
/// Get the users statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_stats_user_searchapplications_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_stats_user_searchapplications_get_task()`.
/// For the simplest API, use `cloudsearch_stats_user_searchapplications_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_stats_user_searchapplications_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_stats_user_searchapplications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetSearchApplicationUserStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudsearch_stats_user_searchapplications_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudsearch_stats_user_searchapplications_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudsearchStatsUserSearchapplicationsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: fromDate_day
    pub fromDate_day: Option<Option<String>>,
    /// Query parameter: fromDate_month
    pub fromDate_month: Option<Option<String>>,
    /// Query parameter: fromDate_year
    pub fromDate_year: Option<Option<String>>,
    /// Query parameter: toDate_day
    pub toDate_day: Option<Option<String>>,
    /// Query parameter: toDate_month
    pub toDate_month: Option<Option<String>>,
    /// Query parameter: toDate_year
    pub toDate_year: Option<Option<String>>,
}

/// GET v1/stats/user/searchapplications/{searchapplicationsId}
/// Get the users statistics for search application. **Note:** This API requires a standard end user account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_stats_user_searchapplications_get_builder()` + `cloudsearch_stats_user_searchapplications_get_execute()`.
/// For task-level control, use `cloudsearch_stats_user_searchapplications_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_stats_user_searchapplications_get(
    client: &SimpleHttpClient,
    args: &CloudsearchStatsUserSearchapplicationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetSearchApplicationUserStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudsearch_stats_user_searchapplications_get_builder(
        client,
        &args.name,
        &args.fromDate_day,
        &args.fromDate_month,
        &args.fromDate_year,
        &args.toDate_day,
        &args.toDate_month,
        &args.toDate_year,
    )?;
    cloudsearch_stats_user_searchapplications_get_execute(builder)
}

/// POST v1:initializeCustomer
/// Enables third party support in Google Cloud Search. **Note:** This API requires an admin account to execute.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudsearch_initialize_customer_execute()` to send, or `cloudsearch_initialize_customer` for simplest API.

pub fn cloudsearch_initialize_customer_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1:initializeCustomer",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1:initializeCustomer
/// Enables third party support in Google Cloud Search. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudsearch_initialize_customer_execute()` or `cloudsearch_initialize_customer`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_initialize_customer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_initialize_customer_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1:initializeCustomer
/// Enables third party support in Google Cloud Search. **Note:** This API requires an admin account to execute.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudsearch_initialize_customer_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudsearch_initialize_customer_task()`.
/// For the simplest API, use `cloudsearch_initialize_customer()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudsearch_initialize_customer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudsearch_initialize_customer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudsearch_initialize_customer_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1:initializeCustomer
/// Enables third party support in Google Cloud Search. **Note:** This API requires an admin account to execute.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudsearch_initialize_customer_builder()` + `cloudsearch_initialize_customer_execute()`.
/// For task-level control, use `cloudsearch_initialize_customer_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudsearch_initialize_customer(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudsearch_initialize_customer_builder(client)?;
    cloudsearch_initialize_customer_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for CheckAccessResponse
// =============================================================================

/// ResourceIdentifier implementation for CheckAccessResponse with CloudsearchDebugDatasourcesItemsCheckAccessArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchDebugDatasourcesItemsCheckAccessArgs> for CheckAccessResponse {
    fn generate_resource_id(
        &self,
        input: &CloudsearchDebugDatasourcesItemsCheckAccessArgs,
    ) -> String {
        format!("gcp::cloudsearch::CheckAccessResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::CheckAccessResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchItemsByViewUrlResponse
// =============================================================================

/// ResourceIdentifier implementation for SearchItemsByViewUrlResponse with CloudsearchDebugDatasourcesItemsSearchByViewUrlArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchDebugDatasourcesItemsSearchByViewUrlArgs>
    for SearchItemsByViewUrlResponse
{
    fn generate_resource_id(
        &self,
        input: &CloudsearchDebugDatasourcesItemsSearchByViewUrlArgs,
    ) -> String {
        format!(
            "gcp::cloudsearch::SearchItemsByViewUrlResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::SearchItemsByViewUrlResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListUnmappedIdentitiesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListUnmappedIdentitiesResponse with CloudsearchDebugDatasourcesItemsUnmappedidsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchDebugDatasourcesItemsUnmappedidsListArgs>
    for ListUnmappedIdentitiesResponse
{
    fn generate_resource_id(
        &self,
        input: &CloudsearchDebugDatasourcesItemsUnmappedidsListArgs,
    ) -> String {
        format!(
            "gcp::cloudsearch::ListUnmappedIdentitiesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::ListUnmappedIdentitiesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListItemNamesForUnmappedIdentityResponse
// =============================================================================

/// ResourceIdentifier implementation for ListItemNamesForUnmappedIdentityResponse with CloudsearchDebugIdentitysourcesItemsListForunmappedidentityArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchDebugIdentitysourcesItemsListForunmappedidentityArgs>
    for ListItemNamesForUnmappedIdentityResponse
{
    fn generate_resource_id(
        &self,
        input: &CloudsearchDebugIdentitysourcesItemsListForunmappedidentityArgs,
    ) -> String {
        format!(
            "gcp::cloudsearch::ListItemNamesForUnmappedIdentityResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::ListItemNamesForUnmappedIdentityResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListUnmappedIdentitiesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListUnmappedIdentitiesResponse with CloudsearchDebugIdentitysourcesUnmappedidsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchDebugIdentitysourcesUnmappedidsListArgs>
    for ListUnmappedIdentitiesResponse
{
    fn generate_resource_id(
        &self,
        input: &CloudsearchDebugIdentitysourcesUnmappedidsListArgs,
    ) -> String {
        format!(
            "gcp::cloudsearch::ListUnmappedIdentitiesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::ListUnmappedIdentitiesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchIndexingDatasourcesDeleteSchemaArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchIndexingDatasourcesDeleteSchemaArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CloudsearchIndexingDatasourcesDeleteSchemaArgs,
    ) -> String {
        format!("gcp::cloudsearch::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Schema
// =============================================================================

/// ResourceIdentifier implementation for Schema with CloudsearchIndexingDatasourcesGetSchemaArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchIndexingDatasourcesGetSchemaArgs> for Schema {
    fn generate_resource_id(&self, input: &CloudsearchIndexingDatasourcesGetSchemaArgs) -> String {
        format!("gcp::cloudsearch::Schema/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Schema"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchIndexingDatasourcesUpdateSchemaArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchIndexingDatasourcesUpdateSchemaArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CloudsearchIndexingDatasourcesUpdateSchemaArgs,
    ) -> String {
        format!("gcp::cloudsearch::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchIndexingDatasourcesItemsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchIndexingDatasourcesItemsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CloudsearchIndexingDatasourcesItemsDeleteArgs,
    ) -> String {
        format!("gcp::cloudsearch::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchIndexingDatasourcesItemsDeleteQueueItemsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchIndexingDatasourcesItemsDeleteQueueItemsArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CloudsearchIndexingDatasourcesItemsDeleteQueueItemsArgs,
    ) -> String {
        format!("gcp::cloudsearch::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Item
// =============================================================================

/// ResourceIdentifier implementation for Item with CloudsearchIndexingDatasourcesItemsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchIndexingDatasourcesItemsGetArgs> for Item {
    fn generate_resource_id(&self, input: &CloudsearchIndexingDatasourcesItemsGetArgs) -> String {
        format!("gcp::cloudsearch::Item/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Item"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchIndexingDatasourcesItemsIndexArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchIndexingDatasourcesItemsIndexArgs> for Operation {
    fn generate_resource_id(&self, input: &CloudsearchIndexingDatasourcesItemsIndexArgs) -> String {
        format!("gcp::cloudsearch::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListItemsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListItemsResponse with CloudsearchIndexingDatasourcesItemsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchIndexingDatasourcesItemsListArgs> for ListItemsResponse {
    fn generate_resource_id(&self, input: &CloudsearchIndexingDatasourcesItemsListArgs) -> String {
        format!("gcp::cloudsearch::ListItemsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::ListItemsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PollItemsResponse
// =============================================================================

/// ResourceIdentifier implementation for PollItemsResponse with CloudsearchIndexingDatasourcesItemsPollArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchIndexingDatasourcesItemsPollArgs> for PollItemsResponse {
    fn generate_resource_id(&self, input: &CloudsearchIndexingDatasourcesItemsPollArgs) -> String {
        format!("gcp::cloudsearch::PollItemsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::PollItemsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Item
// =============================================================================

/// ResourceIdentifier implementation for Item with CloudsearchIndexingDatasourcesItemsPushArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchIndexingDatasourcesItemsPushArgs> for Item {
    fn generate_resource_id(&self, input: &CloudsearchIndexingDatasourcesItemsPushArgs) -> String {
        format!("gcp::cloudsearch::Item/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Item"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchIndexingDatasourcesItemsUnreserveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchIndexingDatasourcesItemsUnreserveArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CloudsearchIndexingDatasourcesItemsUnreserveArgs,
    ) -> String {
        format!("gcp::cloudsearch::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UploadItemRef
// =============================================================================

/// ResourceIdentifier implementation for UploadItemRef with CloudsearchIndexingDatasourcesItemsUploadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchIndexingDatasourcesItemsUploadArgs> for UploadItemRef {
    fn generate_resource_id(
        &self,
        input: &CloudsearchIndexingDatasourcesItemsUploadArgs,
    ) -> String {
        format!("gcp::cloudsearch::UploadItemRef/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::UploadItemRef"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Media
// =============================================================================

/// ResourceIdentifier implementation for Media with CloudsearchMediaUploadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchMediaUploadArgs> for Media {
    fn generate_resource_id(&self, input: &CloudsearchMediaUploadArgs) -> String {
        format!("gcp::cloudsearch::Media/{}", input.resourceName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Media"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &CloudsearchOperationsGetArgs) -> String {
        format!("gcp::cloudsearch::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with CloudsearchOperationsLroListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchOperationsLroListArgs> for ListOperationsResponse {
    fn generate_resource_id(&self, input: &CloudsearchOperationsLroListArgs) -> String {
        format!("gcp::cloudsearch::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RemoveActivityResponse
// =============================================================================

/// ResourceIdentifier implementation for RemoveActivityResponse with CloudsearchQueryRemoveActivityArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchQueryRemoveActivityArgs> for RemoveActivityResponse {
    fn generate_resource_id(&self, input: &CloudsearchQueryRemoveActivityArgs) -> String {
        "gcp::cloudsearch::RemoveActivityResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::RemoveActivityResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchResponse
// =============================================================================

/// ResourceIdentifier implementation for SearchResponse with CloudsearchQuerySearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchQuerySearchArgs> for SearchResponse {
    fn generate_resource_id(&self, input: &CloudsearchQuerySearchArgs) -> String {
        "gcp::cloudsearch::SearchResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::SearchResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SuggestResponse
// =============================================================================

/// ResourceIdentifier implementation for SuggestResponse with CloudsearchQuerySuggestArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchQuerySuggestArgs> for SuggestResponse {
    fn generate_resource_id(&self, input: &CloudsearchQuerySuggestArgs) -> String {
        "gcp::cloudsearch::SuggestResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::SuggestResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListQuerySourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListQuerySourcesResponse with CloudsearchQuerySourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchQuerySourcesListArgs> for ListQuerySourcesResponse {
    fn generate_resource_id(&self, input: &CloudsearchQuerySourcesListArgs) -> String {
        "gcp::cloudsearch::ListQuerySourcesResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::ListQuerySourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CustomerSettings
// =============================================================================

/// ResourceIdentifier implementation for CustomerSettings with CloudsearchSettingsGetCustomerArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsGetCustomerArgs> for CustomerSettings {
    fn generate_resource_id(&self, input: &CloudsearchSettingsGetCustomerArgs) -> String {
        "gcp::cloudsearch::CustomerSettings".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::CustomerSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchSettingsUpdateCustomerArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsUpdateCustomerArgs> for Operation {
    fn generate_resource_id(&self, input: &CloudsearchSettingsUpdateCustomerArgs) -> String {
        "gcp::cloudsearch::Operation".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchSettingsDatasourcesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsDatasourcesCreateArgs> for Operation {
    fn generate_resource_id(&self, input: &CloudsearchSettingsDatasourcesCreateArgs) -> String {
        "gcp::cloudsearch::Operation".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchSettingsDatasourcesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsDatasourcesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &CloudsearchSettingsDatasourcesDeleteArgs) -> String {
        format!("gcp::cloudsearch::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DataSource
// =============================================================================

/// ResourceIdentifier implementation for DataSource with CloudsearchSettingsDatasourcesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsDatasourcesGetArgs> for DataSource {
    fn generate_resource_id(&self, input: &CloudsearchSettingsDatasourcesGetArgs) -> String {
        format!("gcp::cloudsearch::DataSource/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::DataSource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDataSourceResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDataSourceResponse with CloudsearchSettingsDatasourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsDatasourcesListArgs> for ListDataSourceResponse {
    fn generate_resource_id(&self, input: &CloudsearchSettingsDatasourcesListArgs) -> String {
        "gcp::cloudsearch::ListDataSourceResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::ListDataSourceResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchSettingsDatasourcesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsDatasourcesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &CloudsearchSettingsDatasourcesPatchArgs) -> String {
        format!("gcp::cloudsearch::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchSettingsDatasourcesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsDatasourcesUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &CloudsearchSettingsDatasourcesUpdateArgs) -> String {
        format!("gcp::cloudsearch::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchSettingsSearchapplicationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsSearchapplicationsCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CloudsearchSettingsSearchapplicationsCreateArgs,
    ) -> String {
        "gcp::cloudsearch::Operation".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchSettingsSearchapplicationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsSearchapplicationsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CloudsearchSettingsSearchapplicationsDeleteArgs,
    ) -> String {
        format!("gcp::cloudsearch::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchApplication
// =============================================================================

/// ResourceIdentifier implementation for SearchApplication with CloudsearchSettingsSearchapplicationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsSearchapplicationsGetArgs> for SearchApplication {
    fn generate_resource_id(&self, input: &CloudsearchSettingsSearchapplicationsGetArgs) -> String {
        format!("gcp::cloudsearch::SearchApplication/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::SearchApplication"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSearchApplicationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSearchApplicationsResponse with CloudsearchSettingsSearchapplicationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsSearchapplicationsListArgs>
    for ListSearchApplicationsResponse
{
    fn generate_resource_id(
        &self,
        input: &CloudsearchSettingsSearchapplicationsListArgs,
    ) -> String {
        "gcp::cloudsearch::ListSearchApplicationsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::ListSearchApplicationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchSettingsSearchapplicationsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsSearchapplicationsPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CloudsearchSettingsSearchapplicationsPatchArgs,
    ) -> String {
        format!("gcp::cloudsearch::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchSettingsSearchapplicationsResetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsSearchapplicationsResetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CloudsearchSettingsSearchapplicationsResetArgs,
    ) -> String {
        format!("gcp::cloudsearch::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchSettingsSearchapplicationsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchSettingsSearchapplicationsUpdateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &CloudsearchSettingsSearchapplicationsUpdateArgs,
    ) -> String {
        format!("gcp::cloudsearch::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetCustomerIndexStatsResponse
// =============================================================================

/// ResourceIdentifier implementation for GetCustomerIndexStatsResponse with CloudsearchStatsGetIndexArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchStatsGetIndexArgs> for GetCustomerIndexStatsResponse {
    fn generate_resource_id(&self, input: &CloudsearchStatsGetIndexArgs) -> String {
        "gcp::cloudsearch::GetCustomerIndexStatsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::GetCustomerIndexStatsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetCustomerQueryStatsResponse
// =============================================================================

/// ResourceIdentifier implementation for GetCustomerQueryStatsResponse with CloudsearchStatsGetQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchStatsGetQueryArgs> for GetCustomerQueryStatsResponse {
    fn generate_resource_id(&self, input: &CloudsearchStatsGetQueryArgs) -> String {
        "gcp::cloudsearch::GetCustomerQueryStatsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::GetCustomerQueryStatsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetCustomerSearchApplicationStatsResponse
// =============================================================================

/// ResourceIdentifier implementation for GetCustomerSearchApplicationStatsResponse with CloudsearchStatsGetSearchapplicationArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchStatsGetSearchapplicationArgs>
    for GetCustomerSearchApplicationStatsResponse
{
    fn generate_resource_id(&self, input: &CloudsearchStatsGetSearchapplicationArgs) -> String {
        "gcp::cloudsearch::GetCustomerSearchApplicationStatsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::GetCustomerSearchApplicationStatsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetCustomerSessionStatsResponse
// =============================================================================

/// ResourceIdentifier implementation for GetCustomerSessionStatsResponse with CloudsearchStatsGetSessionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchStatsGetSessionArgs> for GetCustomerSessionStatsResponse {
    fn generate_resource_id(&self, input: &CloudsearchStatsGetSessionArgs) -> String {
        "gcp::cloudsearch::GetCustomerSessionStatsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::GetCustomerSessionStatsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetCustomerUserStatsResponse
// =============================================================================

/// ResourceIdentifier implementation for GetCustomerUserStatsResponse with CloudsearchStatsGetUserArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchStatsGetUserArgs> for GetCustomerUserStatsResponse {
    fn generate_resource_id(&self, input: &CloudsearchStatsGetUserArgs) -> String {
        "gcp::cloudsearch::GetCustomerUserStatsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::GetCustomerUserStatsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetDataSourceIndexStatsResponse
// =============================================================================

/// ResourceIdentifier implementation for GetDataSourceIndexStatsResponse with CloudsearchStatsIndexDatasourcesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchStatsIndexDatasourcesGetArgs>
    for GetDataSourceIndexStatsResponse
{
    fn generate_resource_id(&self, input: &CloudsearchStatsIndexDatasourcesGetArgs) -> String {
        format!(
            "gcp::cloudsearch::GetDataSourceIndexStatsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::GetDataSourceIndexStatsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetSearchApplicationQueryStatsResponse
// =============================================================================

/// ResourceIdentifier implementation for GetSearchApplicationQueryStatsResponse with CloudsearchStatsQuerySearchapplicationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchStatsQuerySearchapplicationsGetArgs>
    for GetSearchApplicationQueryStatsResponse
{
    fn generate_resource_id(
        &self,
        input: &CloudsearchStatsQuerySearchapplicationsGetArgs,
    ) -> String {
        format!(
            "gcp::cloudsearch::GetSearchApplicationQueryStatsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::GetSearchApplicationQueryStatsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetSearchApplicationSessionStatsResponse
// =============================================================================

/// ResourceIdentifier implementation for GetSearchApplicationSessionStatsResponse with CloudsearchStatsSessionSearchapplicationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchStatsSessionSearchapplicationsGetArgs>
    for GetSearchApplicationSessionStatsResponse
{
    fn generate_resource_id(
        &self,
        input: &CloudsearchStatsSessionSearchapplicationsGetArgs,
    ) -> String {
        format!(
            "gcp::cloudsearch::GetSearchApplicationSessionStatsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::GetSearchApplicationSessionStatsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetSearchApplicationUserStatsResponse
// =============================================================================

/// ResourceIdentifier implementation for GetSearchApplicationUserStatsResponse with CloudsearchStatsUserSearchapplicationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchStatsUserSearchapplicationsGetArgs>
    for GetSearchApplicationUserStatsResponse
{
    fn generate_resource_id(
        &self,
        input: &CloudsearchStatsUserSearchapplicationsGetArgs,
    ) -> String {
        format!(
            "gcp::cloudsearch::GetSearchApplicationUserStatsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::GetSearchApplicationUserStatsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with CloudsearchInitializeCustomerArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CloudsearchInitializeCustomerArgs> for Operation {
    fn generate_resource_id(&self, input: &CloudsearchInitializeCustomerArgs) -> String {
        "gcp::cloudsearch::Operation".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::cloudsearch::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
