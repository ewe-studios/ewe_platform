//! Auto-generated API clients for gcp/firestore.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/projects/{projectsId}/databases/{databasesId}:bulkDeleteDocuments
/// Bulk deletes a subset of documents from Google Cloud Firestore. Documents created or updated after the underlying system starts to process the request will not be deleted. The bulk delete occurs in the background and its progress can be monitored and managed via the Operation resource that is created. For more details on bulk delete behavior, refer to: <https://cloud.google.`com/firestore/docs/manage-data/bulk-delete`>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_bulk_delete_documents_execute()` to send, or `firestore_projects_databases_bulk_delete_documents` for simplest API.

pub fn firestore_projects_databases_bulk_delete_documents_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}:bulkDeleteDocuments",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}:bulkDeleteDocuments
/// Bulk deletes a subset of documents from Google Cloud Firestore. Documents created or updated after the underlying system starts to process the request will not be deleted. The bulk delete occurs in the background and its progress can be monitored and managed via the Operation resource that is created. For more details on bulk delete behavior, refer to: <https://cloud.google.`com/firestore/docs/manage-data/bulk-delete`>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_bulk_delete_documents_execute()` or `firestore_projects_databases_bulk_delete_documents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_bulk_delete_documents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_bulk_delete_documents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}:bulkDeleteDocuments
/// Bulk deletes a subset of documents from Google Cloud Firestore. Documents created or updated after the underlying system starts to process the request will not be deleted. The bulk delete occurs in the background and its progress can be monitored and managed via the Operation resource that is created. For more details on bulk delete behavior, refer to: <https://cloud.google.`com/firestore/docs/manage-data/bulk-delete`>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_bulk_delete_documents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_bulk_delete_documents_task()`.
/// For the simplest API, use `firestore_projects_databases_bulk_delete_documents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_bulk_delete_documents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_bulk_delete_documents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_bulk_delete_documents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_bulk_delete_documents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesBulkDeleteDocumentsArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}:bulkDeleteDocuments
/// Bulk deletes a subset of documents from Google Cloud Firestore. Documents created or updated after the underlying system starts to process the request will not be deleted. The bulk delete occurs in the background and its progress can be monitored and managed via the Operation resource that is created. For more details on bulk delete behavior, refer to: <https://cloud.google.`com/firestore/docs/manage-data/bulk-delete`>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_bulk_delete_documents_builder()` + `firestore_projects_databases_bulk_delete_documents_execute()`.
/// For task-level control, use `firestore_projects_databases_bulk_delete_documents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_bulk_delete_documents(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesBulkDeleteDocumentsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_bulk_delete_documents_builder(client, &args.name)?;
    firestore_projects_databases_bulk_delete_documents_execute(builder)
}

/// POST v1/projects/{projectsId}/databases:clone
/// Creates a new database by cloning an existing one. The new database must be in the same cloud region or multi-region location as the existing database. This behaves similar to FirestoreAdmin.CreateDatabase except instead of creating a new empty database, a new database is created with the database type, index configuration, and documents from an existing database. The long-running operation can be used to track the progress of the clone, with the Operation's metadata field type being the CloneDatabaseMetadata. The response type is the Database if the clone was successful. The new database is not readable or writeable until the LRO has completed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_clone_execute()` to send, or `firestore_projects_databases_clone` for simplest API.

pub fn firestore_projects_databases_clone_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases:clone",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases:clone
/// Creates a new database by cloning an existing one. The new database must be in the same cloud region or multi-region location as the existing database. This behaves similar to FirestoreAdmin.CreateDatabase except instead of creating a new empty database, a new database is created with the database type, index configuration, and documents from an existing database. The long-running operation can be used to track the progress of the clone, with the Operation's metadata field type being the CloneDatabaseMetadata. The response type is the Database if the clone was successful. The new database is not readable or writeable until the LRO has completed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_clone_execute()` or `firestore_projects_databases_clone`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_clone_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_clone_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases:clone
/// Creates a new database by cloning an existing one. The new database must be in the same cloud region or multi-region location as the existing database. This behaves similar to FirestoreAdmin.CreateDatabase except instead of creating a new empty database, a new database is created with the database type, index configuration, and documents from an existing database. The long-running operation can be used to track the progress of the clone, with the Operation's metadata field type being the CloneDatabaseMetadata. The response type is the Database if the clone was successful. The new database is not readable or writeable until the LRO has completed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_clone_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_clone_task()`.
/// For the simplest API, use `firestore_projects_databases_clone()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_clone_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_clone_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_clone_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_clone`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesCloneArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/databases:clone
/// Creates a new database by cloning an existing one. The new database must be in the same cloud region or multi-region location as the existing database. This behaves similar to FirestoreAdmin.CreateDatabase except instead of creating a new empty database, a new database is created with the database type, index configuration, and documents from an existing database. The long-running operation can be used to track the progress of the clone, with the Operation's metadata field type being the CloneDatabaseMetadata. The response type is the Database if the clone was successful. The new database is not readable or writeable until the LRO has completed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_clone_builder()` + `firestore_projects_databases_clone_execute()`.
/// For task-level control, use `firestore_projects_databases_clone_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_clone(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesCloneArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_clone_builder(client, &args.parent)?;
    firestore_projects_databases_clone_execute(builder)
}

/// POST v1/projects/{projectsId}/databases
/// Create a database.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_create_execute()` to send, or `firestore_projects_databases_create` for simplest API.

pub fn firestore_projects_databases_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    databaseId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = databaseId.as_ref() {
        query_parts.push(format!("databaseId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases
/// Create a database.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_create_execute()` or `firestore_projects_databases_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases
/// Create a database.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_create_task()`.
/// For the simplest API, use `firestore_projects_databases_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: databaseId
    pub databaseId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/databases
/// Create a database.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_create_builder()` + `firestore_projects_databases_create_execute()`.
/// For task-level control, use `firestore_projects_databases_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_create(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firestore_projects_databases_create_builder(client, &args.parent, &args.databaseId)?;
    firestore_projects_databases_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}
/// Deletes a database.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_delete_execute()` to send, or `firestore_projects_databases_delete` for simplest API.

pub fn firestore_projects_databases_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
    etag: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}
/// Deletes a database.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_delete_execute()` or `firestore_projects_databases_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}
/// Deletes a database.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_delete_task()`.
/// For the simplest API, use `firestore_projects_databases_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}
/// Deletes a database.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_delete_builder()` + `firestore_projects_databases_delete_execute()`.
/// For task-level control, use `firestore_projects_databases_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_delete(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_delete_builder(client, &args.name, &args.etag)?;
    firestore_projects_databases_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}:exportDocuments
/// Exports a copy of all or a subset of documents from Google Cloud Firestore to another storage system, such as Google Cloud Storage. Recent updates to documents may not be reflected in the export. The export occurs in the background and its progress can be monitored and managed via the Operation resource that is created. The output of an export may only be used once the associated operation is done. If an export operation is cancelled before completion it may leave partial data behind in Google Cloud Storage. For more details on export behavior and output format, refer to: <https://cloud.google.`com/firestore/docs/manage-data/export-import`>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_export_documents_execute()` to send, or `firestore_projects_databases_export_documents` for simplest API.

pub fn firestore_projects_databases_export_documents_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}:exportDocuments",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}:exportDocuments
/// Exports a copy of all or a subset of documents from Google Cloud Firestore to another storage system, such as Google Cloud Storage. Recent updates to documents may not be reflected in the export. The export occurs in the background and its progress can be monitored and managed via the Operation resource that is created. The output of an export may only be used once the associated operation is done. If an export operation is cancelled before completion it may leave partial data behind in Google Cloud Storage. For more details on export behavior and output format, refer to: <https://cloud.google.`com/firestore/docs/manage-data/export-import`>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_export_documents_execute()` or `firestore_projects_databases_export_documents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_export_documents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_export_documents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}:exportDocuments
/// Exports a copy of all or a subset of documents from Google Cloud Firestore to another storage system, such as Google Cloud Storage. Recent updates to documents may not be reflected in the export. The export occurs in the background and its progress can be monitored and managed via the Operation resource that is created. The output of an export may only be used once the associated operation is done. If an export operation is cancelled before completion it may leave partial data behind in Google Cloud Storage. For more details on export behavior and output format, refer to: <https://cloud.google.`com/firestore/docs/manage-data/export-import`>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_export_documents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_export_documents_task()`.
/// For the simplest API, use `firestore_projects_databases_export_documents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_export_documents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_export_documents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_export_documents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_export_documents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesExportDocumentsArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}:exportDocuments
/// Exports a copy of all or a subset of documents from Google Cloud Firestore to another storage system, such as Google Cloud Storage. Recent updates to documents may not be reflected in the export. The export occurs in the background and its progress can be monitored and managed via the Operation resource that is created. The output of an export may only be used once the associated operation is done. If an export operation is cancelled before completion it may leave partial data behind in Google Cloud Storage. For more details on export behavior and output format, refer to: <https://cloud.google.`com/firestore/docs/manage-data/export-import`>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_export_documents_builder()` + `firestore_projects_databases_export_documents_execute()`.
/// For task-level control, use `firestore_projects_databases_export_documents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_export_documents(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesExportDocumentsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_export_documents_builder(client, &args.name)?;
    firestore_projects_databases_export_documents_execute(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}
/// Gets information about a database.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_get_execute()` to send, or `firestore_projects_databases_get` for simplest API.

pub fn firestore_projects_databases_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}
/// Gets information about a database.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_get_execute()` or `firestore_projects_databases_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1Database>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1Database = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases/{databasesId}
/// Gets information about a database.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_get_task()`.
/// For the simplest API, use `firestore_projects_databases_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1Database>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/databases/{databasesId}
/// Gets information about a database.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_get_builder()` + `firestore_projects_databases_get_execute()`.
/// For task-level control, use `firestore_projects_databases_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_get(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1Database>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_get_builder(client, &args.name)?;
    firestore_projects_databases_get_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}:importDocuments
/// Imports documents into Google Cloud Firestore. Existing documents with the same name are overwritten. The import occurs in the background and its progress can be monitored and managed via the Operation resource that is created. If an ImportDocuments operation is cancelled, it is possible that a subset of the data has already been imported to Cloud Firestore.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_import_documents_execute()` to send, or `firestore_projects_databases_import_documents` for simplest API.

pub fn firestore_projects_databases_import_documents_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}:importDocuments",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}:importDocuments
/// Imports documents into Google Cloud Firestore. Existing documents with the same name are overwritten. The import occurs in the background and its progress can be monitored and managed via the Operation resource that is created. If an ImportDocuments operation is cancelled, it is possible that a subset of the data has already been imported to Cloud Firestore.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_import_documents_execute()` or `firestore_projects_databases_import_documents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_import_documents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_import_documents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}:importDocuments
/// Imports documents into Google Cloud Firestore. Existing documents with the same name are overwritten. The import occurs in the background and its progress can be monitored and managed via the Operation resource that is created. If an ImportDocuments operation is cancelled, it is possible that a subset of the data has already been imported to Cloud Firestore.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_import_documents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_import_documents_task()`.
/// For the simplest API, use `firestore_projects_databases_import_documents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_import_documents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_import_documents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_import_documents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_import_documents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesImportDocumentsArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}:importDocuments
/// Imports documents into Google Cloud Firestore. Existing documents with the same name are overwritten. The import occurs in the background and its progress can be monitored and managed via the Operation resource that is created. If an ImportDocuments operation is cancelled, it is possible that a subset of the data has already been imported to Cloud Firestore.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_import_documents_builder()` + `firestore_projects_databases_import_documents_execute()`.
/// For task-level control, use `firestore_projects_databases_import_documents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_import_documents(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesImportDocumentsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_import_documents_builder(client, &args.name)?;
    firestore_projects_databases_import_documents_execute(builder)
}

/// GET v1/projects/{projectsId}/databases
/// List all the databases in the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_list_execute()` to send, or `firestore_projects_databases_list` for simplest API.

pub fn firestore_projects_databases_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    showDeleted: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = showDeleted.as_ref() {
        query_parts.push(format!("showDeleted={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases
/// List all the databases in the project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_list_execute()` or `firestore_projects_databases_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1ListDatabasesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1ListDatabasesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases
/// List all the databases in the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_list_task()`.
/// For the simplest API, use `firestore_projects_databases_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1ListDatabasesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: showDeleted
    pub showDeleted: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/databases
/// List all the databases in the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_list_builder()` + `firestore_projects_databases_list_execute()`.
/// For task-level control, use `firestore_projects_databases_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_list(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1ListDatabasesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firestore_projects_databases_list_builder(client, &args.parent, &args.showDeleted)?;
    firestore_projects_databases_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}
/// Updates a database.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_patch_execute()` to send, or `firestore_projects_databases_patch` for simplest API.

pub fn firestore_projects_databases_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}
/// Updates a database.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_patch_execute()` or `firestore_projects_databases_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}
/// Updates a database.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_patch_task()`.
/// For the simplest API, use `firestore_projects_databases_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}
/// Updates a database.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_patch_builder()` + `firestore_projects_databases_patch_execute()`.
/// For task-level control, use `firestore_projects_databases_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_patch(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_patch_builder(client, &args.name, &args.updateMask)?;
    firestore_projects_databases_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/databases:restore
/// Creates a new database by restoring from an existing backup. The new database must be in the same cloud region or multi-region location as the existing backup. This behaves similar to FirestoreAdmin.CreateDatabase except instead of creating a new empty database, a new database is created with the database type, index configuration, and documents from an existing backup. The long-running operation can be used to track the progress of the restore, with the Operation's metadata field type being the RestoreDatabaseMetadata. The response type is the Database if the restore was successful. The new database is not readable or writeable until the LRO has completed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_restore_execute()` to send, or `firestore_projects_databases_restore` for simplest API.

pub fn firestore_projects_databases_restore_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases:restore",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases:restore
/// Creates a new database by restoring from an existing backup. The new database must be in the same cloud region or multi-region location as the existing backup. This behaves similar to FirestoreAdmin.CreateDatabase except instead of creating a new empty database, a new database is created with the database type, index configuration, and documents from an existing backup. The long-running operation can be used to track the progress of the restore, with the Operation's metadata field type being the RestoreDatabaseMetadata. The response type is the Database if the restore was successful. The new database is not readable or writeable until the LRO has completed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_restore_execute()` or `firestore_projects_databases_restore`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_restore_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases:restore
/// Creates a new database by restoring from an existing backup. The new database must be in the same cloud region or multi-region location as the existing backup. This behaves similar to FirestoreAdmin.CreateDatabase except instead of creating a new empty database, a new database is created with the database type, index configuration, and documents from an existing backup. The long-running operation can be used to track the progress of the restore, with the Operation's metadata field type being the RestoreDatabaseMetadata. The response type is the Database if the restore was successful. The new database is not readable or writeable until the LRO has completed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_restore_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_restore_task()`.
/// For the simplest API, use `firestore_projects_databases_restore()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_restore_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_restore`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesRestoreArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/databases:restore
/// Creates a new database by restoring from an existing backup. The new database must be in the same cloud region or multi-region location as the existing backup. This behaves similar to FirestoreAdmin.CreateDatabase except instead of creating a new empty database, a new database is created with the database type, index configuration, and documents from an existing backup. The long-running operation can be used to track the progress of the restore, with the Operation's metadata field type being the RestoreDatabaseMetadata. The response type is the Database if the restore was successful. The new database is not readable or writeable until the LRO has completed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_restore_builder()` + `firestore_projects_databases_restore_execute()`.
/// For task-level control, use `firestore_projects_databases_restore_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_restore(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesRestoreArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_restore_builder(client, &args.parent)?;
    firestore_projects_databases_restore_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/backupSchedules
/// Creates a backup schedule on a database. At most two backup schedules can be configured on a database, one daily backup schedule and one weekly backup schedule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_backup_schedules_create_execute()` to send, or `firestore_projects_databases_backup_schedules_create` for simplest API.

pub fn firestore_projects_databases_backup_schedules_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/backupSchedules",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/backupSchedules
/// Creates a backup schedule on a database. At most two backup schedules can be configured on a database, one daily backup schedule and one weekly backup schedule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_backup_schedules_create_execute()` or `firestore_projects_databases_backup_schedules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_backup_schedules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_backup_schedules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1BackupSchedule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1BackupSchedule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/backupSchedules
/// Creates a backup schedule on a database. At most two backup schedules can be configured on a database, one daily backup schedule and one weekly backup schedule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_backup_schedules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_backup_schedules_create_task()`.
/// For the simplest API, use `firestore_projects_databases_backup_schedules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_backup_schedules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_backup_schedules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1BackupSchedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_backup_schedules_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_backup_schedules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesBackupSchedulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/backupSchedules
/// Creates a backup schedule on a database. At most two backup schedules can be configured on a database, one daily backup schedule and one weekly backup schedule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_backup_schedules_create_builder()` + `firestore_projects_databases_backup_schedules_create_execute()`.
/// For task-level control, use `firestore_projects_databases_backup_schedules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_backup_schedules_create(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesBackupSchedulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1BackupSchedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firestore_projects_databases_backup_schedules_create_builder(client, &args.parent)?;
    firestore_projects_databases_backup_schedules_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/backupSchedules/{backupSchedulesId}
/// Deletes a backup schedule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_backup_schedules_delete_execute()` to send, or `firestore_projects_databases_backup_schedules_delete` for simplest API.

pub fn firestore_projects_databases_backup_schedules_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/backupSchedules/{backupSchedulesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/backupSchedules/{backupSchedulesId}
/// Deletes a backup schedule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_backup_schedules_delete_execute()` or `firestore_projects_databases_backup_schedules_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_backup_schedules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_backup_schedules_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/backupSchedules/{backupSchedulesId}
/// Deletes a backup schedule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_backup_schedules_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_backup_schedules_delete_task()`.
/// For the simplest API, use `firestore_projects_databases_backup_schedules_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_backup_schedules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_backup_schedules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_backup_schedules_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_backup_schedules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesBackupSchedulesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/backupSchedules/{backupSchedulesId}
/// Deletes a backup schedule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_backup_schedules_delete_builder()` + `firestore_projects_databases_backup_schedules_delete_execute()`.
/// For task-level control, use `firestore_projects_databases_backup_schedules_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_backup_schedules_delete(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesBackupSchedulesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_backup_schedules_delete_builder(client, &args.name)?;
    firestore_projects_databases_backup_schedules_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/backupSchedules/{backupSchedulesId}
/// Gets information about a backup schedule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_backup_schedules_get_execute()` to send, or `firestore_projects_databases_backup_schedules_get` for simplest API.

pub fn firestore_projects_databases_backup_schedules_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/backupSchedules/{backupSchedulesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/backupSchedules/{backupSchedulesId}
/// Gets information about a backup schedule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_backup_schedules_get_execute()` or `firestore_projects_databases_backup_schedules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_backup_schedules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_backup_schedules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1BackupSchedule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1BackupSchedule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/backupSchedules/{backupSchedulesId}
/// Gets information about a backup schedule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_backup_schedules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_backup_schedules_get_task()`.
/// For the simplest API, use `firestore_projects_databases_backup_schedules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_backup_schedules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_backup_schedules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1BackupSchedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_backup_schedules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_backup_schedules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesBackupSchedulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/backupSchedules/{backupSchedulesId}
/// Gets information about a backup schedule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_backup_schedules_get_builder()` + `firestore_projects_databases_backup_schedules_get_execute()`.
/// For task-level control, use `firestore_projects_databases_backup_schedules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_backup_schedules_get(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesBackupSchedulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1BackupSchedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_backup_schedules_get_builder(client, &args.name)?;
    firestore_projects_databases_backup_schedules_get_execute(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/backupSchedules
/// List backup schedules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_backup_schedules_list_execute()` to send, or `firestore_projects_databases_backup_schedules_list` for simplest API.

pub fn firestore_projects_databases_backup_schedules_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/backupSchedules",
        parent,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/backupSchedules
/// List backup schedules.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_backup_schedules_list_execute()` or `firestore_projects_databases_backup_schedules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_backup_schedules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_backup_schedules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleFirestoreAdminV1ListBackupSchedulesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1ListBackupSchedulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/backupSchedules
/// List backup schedules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_backup_schedules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_backup_schedules_list_task()`.
/// For the simplest API, use `firestore_projects_databases_backup_schedules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_backup_schedules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_backup_schedules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1ListBackupSchedulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_backup_schedules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_backup_schedules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesBackupSchedulesListArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/backupSchedules
/// List backup schedules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_backup_schedules_list_builder()` + `firestore_projects_databases_backup_schedules_list_execute()`.
/// For task-level control, use `firestore_projects_databases_backup_schedules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_backup_schedules_list(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesBackupSchedulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1ListBackupSchedulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_backup_schedules_list_builder(client, &args.parent)?;
    firestore_projects_databases_backup_schedules_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}/backupSchedules/{backupSchedulesId}
/// Updates a backup schedule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_backup_schedules_patch_execute()` to send, or `firestore_projects_databases_backup_schedules_patch` for simplest API.

pub fn firestore_projects_databases_backup_schedules_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/backupSchedules/{backupSchedulesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}/backupSchedules/{backupSchedulesId}
/// Updates a backup schedule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_backup_schedules_patch_execute()` or `firestore_projects_databases_backup_schedules_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_backup_schedules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_backup_schedules_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1BackupSchedule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1BackupSchedule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}/backupSchedules/{backupSchedulesId}
/// Updates a backup schedule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_backup_schedules_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_backup_schedules_patch_task()`.
/// For the simplest API, use `firestore_projects_databases_backup_schedules_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_backup_schedules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_backup_schedules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1BackupSchedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_backup_schedules_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_backup_schedules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesBackupSchedulesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}/backupSchedules/{backupSchedulesId}
/// Updates a backup schedule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_backup_schedules_patch_builder()` + `firestore_projects_databases_backup_schedules_patch_execute()`.
/// For task-level control, use `firestore_projects_databases_backup_schedules_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_backup_schedules_patch(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesBackupSchedulesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1BackupSchedule>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_backup_schedules_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    firestore_projects_databases_backup_schedules_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields/{fieldsId}
/// Gets the metadata and configuration for a Field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_collection_groups_fields_get_execute()` to send, or `firestore_projects_databases_collection_groups_fields_get` for simplest API.

pub fn firestore_projects_databases_collection_groups_fields_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields/{fieldsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields/{fieldsId}
/// Gets the metadata and configuration for a Field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_collection_groups_fields_get_execute()` or `firestore_projects_databases_collection_groups_fields_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_collection_groups_fields_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_collection_groups_fields_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1Field>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1Field = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields/{fieldsId}
/// Gets the metadata and configuration for a Field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_collection_groups_fields_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_collection_groups_fields_get_task()`.
/// For the simplest API, use `firestore_projects_databases_collection_groups_fields_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_collection_groups_fields_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_collection_groups_fields_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1Field>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_collection_groups_fields_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_collection_groups_fields_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesCollectionGroupsFieldsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields/{fieldsId}
/// Gets the metadata and configuration for a Field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_collection_groups_fields_get_builder()` + `firestore_projects_databases_collection_groups_fields_get_execute()`.
/// For task-level control, use `firestore_projects_databases_collection_groups_fields_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_collection_groups_fields_get(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesCollectionGroupsFieldsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1Field>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firestore_projects_databases_collection_groups_fields_get_builder(client, &args.name)?;
    firestore_projects_databases_collection_groups_fields_get_execute(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields
/// Lists the field configuration and metadata for this database. Currently, FirestoreAdmin.ListFields only supports listing fields that have been explicitly overridden. To issue this query, call FirestoreAdmin.ListFields with the filter set to `indexConfig`.`usesAncestorConfig`:`false` or `ttlConfig`:*.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_collection_groups_fields_list_execute()` to send, or `firestore_projects_databases_collection_groups_fields_list` for simplest API.

pub fn firestore_projects_databases_collection_groups_fields_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields
/// Lists the field configuration and metadata for this database. Currently, FirestoreAdmin.ListFields only supports listing fields that have been explicitly overridden. To issue this query, call FirestoreAdmin.ListFields with the filter set to `indexConfig`.`usesAncestorConfig`:`false` or `ttlConfig`:*.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_collection_groups_fields_list_execute()` or `firestore_projects_databases_collection_groups_fields_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_collection_groups_fields_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_collection_groups_fields_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1ListFieldsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1ListFieldsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields
/// Lists the field configuration and metadata for this database. Currently, FirestoreAdmin.ListFields only supports listing fields that have been explicitly overridden. To issue this query, call FirestoreAdmin.ListFields with the filter set to `indexConfig`.`usesAncestorConfig`:`false` or `ttlConfig`:*.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_collection_groups_fields_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_collection_groups_fields_list_task()`.
/// For the simplest API, use `firestore_projects_databases_collection_groups_fields_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_collection_groups_fields_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_collection_groups_fields_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1ListFieldsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_collection_groups_fields_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_collection_groups_fields_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesCollectionGroupsFieldsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields
/// Lists the field configuration and metadata for this database. Currently, FirestoreAdmin.ListFields only supports listing fields that have been explicitly overridden. To issue this query, call FirestoreAdmin.ListFields with the filter set to `indexConfig`.`usesAncestorConfig`:`false` or `ttlConfig`:*.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_collection_groups_fields_list_builder()` + `firestore_projects_databases_collection_groups_fields_list_execute()`.
/// For task-level control, use `firestore_projects_databases_collection_groups_fields_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_collection_groups_fields_list(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesCollectionGroupsFieldsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1ListFieldsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_collection_groups_fields_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    firestore_projects_databases_collection_groups_fields_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields/{fieldsId}
/// Updates a field configuration. Currently, field updates apply only to single field index configuration. However, calls to FirestoreAdmin.UpdateField should provide a field mask to avoid changing any configuration that the caller isn't aware of. The field mask should be specified as: { paths: "index_config" }. This call returns a google.longrunning.Operation which may be used to track the status of the field update. The metadata for the operation will be the type FieldOperationMetadata. To configure the default field settings for the database, use the special Field with resource name: `projects/{project_id}/databases/{database_id}/`collectionGroups`/__default__/fields/`*.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_collection_groups_fields_patch_execute()` to send, or `firestore_projects_databases_collection_groups_fields_patch` for simplest API.

pub fn firestore_projects_databases_collection_groups_fields_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields/{fieldsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields/{fieldsId}
/// Updates a field configuration. Currently, field updates apply only to single field index configuration. However, calls to FirestoreAdmin.UpdateField should provide a field mask to avoid changing any configuration that the caller isn't aware of. The field mask should be specified as: { paths: "index_config" }. This call returns a google.longrunning.Operation which may be used to track the status of the field update. The metadata for the operation will be the type FieldOperationMetadata. To configure the default field settings for the database, use the special Field with resource name: `projects/{project_id}/databases/{database_id}/`collectionGroups`/__default__/fields/`*.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_collection_groups_fields_patch_execute()` or `firestore_projects_databases_collection_groups_fields_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_collection_groups_fields_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_collection_groups_fields_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields/{fieldsId}
/// Updates a field configuration. Currently, field updates apply only to single field index configuration. However, calls to FirestoreAdmin.UpdateField should provide a field mask to avoid changing any configuration that the caller isn't aware of. The field mask should be specified as: { paths: "index_config" }. This call returns a google.longrunning.Operation which may be used to track the status of the field update. The metadata for the operation will be the type FieldOperationMetadata. To configure the default field settings for the database, use the special Field with resource name: `projects/{project_id}/databases/{database_id}/`collectionGroups`/__default__/fields/`*.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_collection_groups_fields_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_collection_groups_fields_patch_task()`.
/// For the simplest API, use `firestore_projects_databases_collection_groups_fields_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_collection_groups_fields_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_collection_groups_fields_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_collection_groups_fields_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_collection_groups_fields_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesCollectionGroupsFieldsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/fields/{fieldsId}
/// Updates a field configuration. Currently, field updates apply only to single field index configuration. However, calls to FirestoreAdmin.UpdateField should provide a field mask to avoid changing any configuration that the caller isn't aware of. The field mask should be specified as: { paths: "index_config" }. This call returns a google.longrunning.Operation which may be used to track the status of the field update. The metadata for the operation will be the type FieldOperationMetadata. To configure the default field settings for the database, use the special Field with resource name: `projects/{project_id}/databases/{database_id}/`collectionGroups`/__default__/fields/`*.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_collection_groups_fields_patch_builder()` + `firestore_projects_databases_collection_groups_fields_patch_execute()`.
/// For task-level control, use `firestore_projects_databases_collection_groups_fields_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_collection_groups_fields_patch(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesCollectionGroupsFieldsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_collection_groups_fields_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    firestore_projects_databases_collection_groups_fields_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes
/// Creates a composite index. This returns a google.longrunning.Operation which may be used to track the status of the creation. The metadata for the operation will be the type IndexOperationMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_collection_groups_indexes_create_execute()` to send, or `firestore_projects_databases_collection_groups_indexes_create` for simplest API.

pub fn firestore_projects_databases_collection_groups_indexes_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes
/// Creates a composite index. This returns a google.longrunning.Operation which may be used to track the status of the creation. The metadata for the operation will be the type IndexOperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_collection_groups_indexes_create_execute()` or `firestore_projects_databases_collection_groups_indexes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_collection_groups_indexes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_collection_groups_indexes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes
/// Creates a composite index. This returns a google.longrunning.Operation which may be used to track the status of the creation. The metadata for the operation will be the type IndexOperationMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_collection_groups_indexes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_collection_groups_indexes_create_task()`.
/// For the simplest API, use `firestore_projects_databases_collection_groups_indexes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_collection_groups_indexes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_collection_groups_indexes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_collection_groups_indexes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_collection_groups_indexes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesCollectionGroupsIndexesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes
/// Creates a composite index. This returns a google.longrunning.Operation which may be used to track the status of the creation. The metadata for the operation will be the type IndexOperationMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_collection_groups_indexes_create_builder()` + `firestore_projects_databases_collection_groups_indexes_create_execute()`.
/// For task-level control, use `firestore_projects_databases_collection_groups_indexes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_collection_groups_indexes_create(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesCollectionGroupsIndexesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_collection_groups_indexes_create_builder(
        client,
        &args.parent,
    )?;
    firestore_projects_databases_collection_groups_indexes_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes/{indexesId}
/// Deletes a composite index.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_collection_groups_indexes_delete_execute()` to send, or `firestore_projects_databases_collection_groups_indexes_delete` for simplest API.

pub fn firestore_projects_databases_collection_groups_indexes_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes/{indexesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes/{indexesId}
/// Deletes a composite index.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_collection_groups_indexes_delete_execute()` or `firestore_projects_databases_collection_groups_indexes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_collection_groups_indexes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_collection_groups_indexes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes/{indexesId}
/// Deletes a composite index.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_collection_groups_indexes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_collection_groups_indexes_delete_task()`.
/// For the simplest API, use `firestore_projects_databases_collection_groups_indexes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_collection_groups_indexes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_collection_groups_indexes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_collection_groups_indexes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_collection_groups_indexes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesCollectionGroupsIndexesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes/{indexesId}
/// Deletes a composite index.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_collection_groups_indexes_delete_builder()` + `firestore_projects_databases_collection_groups_indexes_delete_execute()`.
/// For task-level control, use `firestore_projects_databases_collection_groups_indexes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_collection_groups_indexes_delete(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesCollectionGroupsIndexesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        firestore_projects_databases_collection_groups_indexes_delete_builder(client, &args.name)?;
    firestore_projects_databases_collection_groups_indexes_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes/{indexesId}
/// Gets a composite index.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_collection_groups_indexes_get_execute()` to send, or `firestore_projects_databases_collection_groups_indexes_get` for simplest API.

pub fn firestore_projects_databases_collection_groups_indexes_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes/{indexesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes/{indexesId}
/// Gets a composite index.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_collection_groups_indexes_get_execute()` or `firestore_projects_databases_collection_groups_indexes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_collection_groups_indexes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_collection_groups_indexes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1Index>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1Index = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes/{indexesId}
/// Gets a composite index.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_collection_groups_indexes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_collection_groups_indexes_get_task()`.
/// For the simplest API, use `firestore_projects_databases_collection_groups_indexes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_collection_groups_indexes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_collection_groups_indexes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1Index>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_collection_groups_indexes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_collection_groups_indexes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesCollectionGroupsIndexesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes/{indexesId}
/// Gets a composite index.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_collection_groups_indexes_get_builder()` + `firestore_projects_databases_collection_groups_indexes_get_execute()`.
/// For task-level control, use `firestore_projects_databases_collection_groups_indexes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_collection_groups_indexes_get(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesCollectionGroupsIndexesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1Index>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firestore_projects_databases_collection_groups_indexes_get_builder(client, &args.name)?;
    firestore_projects_databases_collection_groups_indexes_get_execute(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes
/// Lists composite indexes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_collection_groups_indexes_list_execute()` to send, or `firestore_projects_databases_collection_groups_indexes_list` for simplest API.

pub fn firestore_projects_databases_collection_groups_indexes_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes
/// Lists composite indexes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_collection_groups_indexes_list_execute()` or `firestore_projects_databases_collection_groups_indexes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_collection_groups_indexes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_collection_groups_indexes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1ListIndexesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1ListIndexesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes
/// Lists composite indexes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_collection_groups_indexes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_collection_groups_indexes_list_task()`.
/// For the simplest API, use `firestore_projects_databases_collection_groups_indexes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_collection_groups_indexes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_collection_groups_indexes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1ListIndexesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_collection_groups_indexes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_collection_groups_indexes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesCollectionGroupsIndexesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/collectionGroups/{collectionGroupsId}/indexes
/// Lists composite indexes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_collection_groups_indexes_list_builder()` + `firestore_projects_databases_collection_groups_indexes_list_execute()`.
/// For task-level control, use `firestore_projects_databases_collection_groups_indexes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_collection_groups_indexes_list(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesCollectionGroupsIndexesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1ListIndexesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_collection_groups_indexes_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    firestore_projects_databases_collection_groups_indexes_list_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:batchGet
/// Gets multiple documents. Documents returned by this method are not guaranteed to be returned in the same order that they were requested.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_batch_get_execute()` to send, or `firestore_projects_databases_documents_batch_get` for simplest API.

pub fn firestore_projects_databases_documents_batch_get_builder(
    client: &SimpleHttpClient,
    database: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents:batchGet",
        database,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:batchGet
/// Gets multiple documents. Documents returned by this method are not guaranteed to be returned in the same order that they were requested.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_batch_get_execute()` or `firestore_projects_databases_documents_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchGetDocumentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetDocumentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:batchGet
/// Gets multiple documents. Documents returned by this method are not guaranteed to be returned in the same order that they were requested.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_batch_get_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchGetDocumentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsBatchGetArgs {
    /// Path parameter: database
    pub database: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:batchGet
/// Gets multiple documents. Documents returned by this method are not guaranteed to be returned in the same order that they were requested.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_batch_get_builder()` + `firestore_projects_databases_documents_batch_get_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_batch_get(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsBatchGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchGetDocumentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_documents_batch_get_builder(client, &args.database)?;
    firestore_projects_databases_documents_batch_get_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:batchWrite
/// Applies a batch of write operations. The BatchWrite method does not apply the write operations atomically and can apply them out of order. Method does not allow more than one write per document. Each write succeeds or fails independently. See the BatchWriteResponse for the success status of each write. If you require an atomically applied set of writes, use Commit instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_batch_write_execute()` to send, or `firestore_projects_databases_documents_batch_write` for simplest API.

pub fn firestore_projects_databases_documents_batch_write_builder(
    client: &SimpleHttpClient,
    database: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents:batchWrite",
        database,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:batchWrite
/// Applies a batch of write operations. The BatchWrite method does not apply the write operations atomically and can apply them out of order. Method does not allow more than one write per document. Each write succeeds or fails independently. See the BatchWriteResponse for the success status of each write. If you require an atomically applied set of writes, use Commit instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_batch_write_execute()` or `firestore_projects_databases_documents_batch_write`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_batch_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_batch_write_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchWriteResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchWriteResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:batchWrite
/// Applies a batch of write operations. The BatchWrite method does not apply the write operations atomically and can apply them out of order. Method does not allow more than one write per document. Each write succeeds or fails independently. See the BatchWriteResponse for the success status of each write. If you require an atomically applied set of writes, use Commit instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_batch_write_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_batch_write_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_batch_write()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_batch_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_batch_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchWriteResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_batch_write_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_batch_write`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsBatchWriteArgs {
    /// Path parameter: database
    pub database: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:batchWrite
/// Applies a batch of write operations. The BatchWrite method does not apply the write operations atomically and can apply them out of order. Method does not allow more than one write per document. Each write succeeds or fails independently. See the BatchWriteResponse for the success status of each write. If you require an atomically applied set of writes, use Commit instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_batch_write_builder()` + `firestore_projects_databases_documents_batch_write_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_batch_write_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_batch_write(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsBatchWriteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchWriteResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        firestore_projects_databases_documents_batch_write_builder(client, &args.database)?;
    firestore_projects_databases_documents_batch_write_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:beginTransaction
/// Starts a new transaction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_begin_transaction_execute()` to send, or `firestore_projects_databases_documents_begin_transaction` for simplest API.

pub fn firestore_projects_databases_documents_begin_transaction_builder(
    client: &SimpleHttpClient,
    database: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents:beginTransaction",
        database,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:beginTransaction
/// Starts a new transaction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_begin_transaction_execute()` or `firestore_projects_databases_documents_begin_transaction`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_begin_transaction_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_begin_transaction_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BeginTransactionResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BeginTransactionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:beginTransaction
/// Starts a new transaction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_begin_transaction_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_begin_transaction_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_begin_transaction()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_begin_transaction_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_begin_transaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BeginTransactionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_begin_transaction_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_begin_transaction`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsBeginTransactionArgs {
    /// Path parameter: database
    pub database: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:beginTransaction
/// Starts a new transaction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_begin_transaction_builder()` + `firestore_projects_databases_documents_begin_transaction_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_begin_transaction_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_begin_transaction(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsBeginTransactionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BeginTransactionResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        firestore_projects_databases_documents_begin_transaction_builder(client, &args.database)?;
    firestore_projects_databases_documents_begin_transaction_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:commit
/// Commits a transaction, while optionally updating documents.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_commit_execute()` to send, or `firestore_projects_databases_documents_commit` for simplest API.

pub fn firestore_projects_databases_documents_commit_builder(
    client: &SimpleHttpClient,
    database: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents:commit",
        database,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:commit
/// Commits a transaction, while optionally updating documents.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_commit_execute()` or `firestore_projects_databases_documents_commit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_commit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_commit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommitResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommitResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:commit
/// Commits a transaction, while optionally updating documents.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_commit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_commit_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_commit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_commit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_commit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommitResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_commit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_commit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsCommitArgs {
    /// Path parameter: database
    pub database: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:commit
/// Commits a transaction, while optionally updating documents.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_commit_builder()` + `firestore_projects_databases_documents_commit_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_commit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_commit(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsCommitArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommitResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_documents_commit_builder(client, &args.database)?;
    firestore_projects_databases_documents_commit_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{collectionId}
/// Creates a new document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_create_document_execute()` to send, or `firestore_projects_databases_documents_create_document` for simplest API.

pub fn firestore_projects_databases_documents_create_document_builder(
    client: &SimpleHttpClient,
    parent: &String,
    collectionId: &String,
    documentId: &Option<Option<String>>,
    mask_fieldPaths: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{}/documents/{documentsId}/{collectionId}",
        parent,
        collectionId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = documentId.as_ref() {
        query_parts.push(format!("documentId={}", val));
    }
    if let Some(val) = mask_fieldPaths.as_ref() {
        query_parts.push(format!("mask.fieldPaths={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{collectionId}
/// Creates a new document.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_create_document_execute()` or `firestore_projects_databases_documents_create_document`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_create_document_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_create_document_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Document>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Document = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{collectionId}
/// Creates a new document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_create_document_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_create_document_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_create_document()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_create_document_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_create_document_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Document>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_create_document_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_create_document`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsCreateDocumentArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: collectionId
    pub collectionId: String,
    /// Query parameter: documentId
    pub documentId: Option<Option<String>>,
    /// Query parameter: mask_fieldPaths
    pub mask_fieldPaths: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{collectionId}
/// Creates a new document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_create_document_builder()` + `firestore_projects_databases_documents_create_document_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_create_document_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_create_document(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsCreateDocumentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Document>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_documents_create_document_builder(
        client,
        &args.parent,
        &args.collectionId,
        &args.documentId,
        &args.mask_fieldPaths,
    )?;
    firestore_projects_databases_documents_create_document_execute(builder)
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}
/// Deletes a document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_delete_execute()` to send, or `firestore_projects_databases_documents_delete` for simplest API.

pub fn firestore_projects_databases_documents_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
    currentDocument_exists: &Option<Option<String>>,
    currentDocument_updateTime: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents/{documentsId}/{documentsId1}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = currentDocument_exists.as_ref() {
        query_parts.push(format!("currentDocument.exists={}", val));
    }
    if let Some(val) = currentDocument_updateTime.as_ref() {
        query_parts.push(format!("currentDocument.updateTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}
/// Deletes a document.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_delete_execute()` or `firestore_projects_databases_documents_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}
/// Deletes a document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_delete_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: currentDocument_exists
    pub currentDocument_exists: Option<Option<String>>,
    /// Query parameter: currentDocument_updateTime
    pub currentDocument_updateTime: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}
/// Deletes a document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_delete_builder()` + `firestore_projects_databases_documents_delete_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_delete(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_documents_delete_builder(
        client,
        &args.name,
        &args.currentDocument_exists,
        &args.currentDocument_updateTime,
    )?;
    firestore_projects_databases_documents_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:executePipeline
/// Executes a pipeline query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_execute_pipeline_execute()` to send, or `firestore_projects_databases_documents_execute_pipeline` for simplest API.

pub fn firestore_projects_databases_documents_execute_pipeline_builder(
    client: &SimpleHttpClient,
    database: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents:executePipeline",
        database,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:executePipeline
/// Executes a pipeline query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_execute_pipeline_execute()` or `firestore_projects_databases_documents_execute_pipeline`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_execute_pipeline_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_execute_pipeline_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExecutePipelineResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExecutePipelineResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:executePipeline
/// Executes a pipeline query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_execute_pipeline_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_execute_pipeline_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_execute_pipeline()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_execute_pipeline_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_execute_pipeline_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecutePipelineResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_execute_pipeline_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_execute_pipeline`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsExecutePipelineArgs {
    /// Path parameter: database
    pub database: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:executePipeline
/// Executes a pipeline query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_execute_pipeline_builder()` + `firestore_projects_databases_documents_execute_pipeline_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_execute_pipeline_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_execute_pipeline(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsExecutePipelineArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExecutePipelineResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        firestore_projects_databases_documents_execute_pipeline_builder(client, &args.database)?;
    firestore_projects_databases_documents_execute_pipeline_execute(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}
/// Gets a single document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_get_execute()` to send, or `firestore_projects_databases_documents_get` for simplest API.

pub fn firestore_projects_databases_documents_get_builder(
    client: &SimpleHttpClient,
    name: &String,
    mask_fieldPaths: &Option<Option<String>>,
    readTime: &Option<Option<String>>,
    transaction: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents/{documentsId}/{documentsId1}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = mask_fieldPaths.as_ref() {
        query_parts.push(format!("mask.fieldPaths={}", val));
    }
    if let Some(val) = readTime.as_ref() {
        query_parts.push(format!("readTime={}", val));
    }
    if let Some(val) = transaction.as_ref() {
        query_parts.push(format!("transaction={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}
/// Gets a single document.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_get_execute()` or `firestore_projects_databases_documents_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Document>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Document = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}
/// Gets a single document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_get_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Document>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: mask_fieldPaths
    pub mask_fieldPaths: Option<Option<String>>,
    /// Query parameter: readTime
    pub readTime: Option<Option<String>>,
    /// Query parameter: transaction
    pub transaction: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}
/// Gets a single document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_get_builder()` + `firestore_projects_databases_documents_get_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_get(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Document>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_documents_get_builder(
        client,
        &args.name,
        &args.mask_fieldPaths,
        &args.readTime,
        &args.transaction,
    )?;
    firestore_projects_databases_documents_get_execute(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}/{collectionId}
/// Lists documents.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_list_execute()` to send, or `firestore_projects_databases_documents_list` for simplest API.

pub fn firestore_projects_databases_documents_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    collectionId: &String,
    mask_fieldPaths: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readTime: &Option<Option<String>>,
    showMissing: &Option<Option<String>>,
    transaction: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{}/documents/{documentsId}/{documentsId1}/{collectionId}",
        parent,
        collectionId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = mask_fieldPaths.as_ref() {
        query_parts.push(format!("mask.fieldPaths={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime.as_ref() {
        query_parts.push(format!("readTime={}", val));
    }
    if let Some(val) = showMissing.as_ref() {
        query_parts.push(format!("showMissing={}", val));
    }
    if let Some(val) = transaction.as_ref() {
        query_parts.push(format!("transaction={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}/{collectionId}
/// Lists documents.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_list_execute()` or `firestore_projects_databases_documents_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDocumentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDocumentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}/{collectionId}
/// Lists documents.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_list_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDocumentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: collectionId
    pub collectionId: String,
    /// Query parameter: mask_fieldPaths
    pub mask_fieldPaths: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readTime
    pub readTime: Option<Option<String>>,
    /// Query parameter: showMissing
    pub showMissing: Option<Option<String>>,
    /// Query parameter: transaction
    pub transaction: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}/{collectionId}
/// Lists documents.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_list_builder()` + `firestore_projects_databases_documents_list_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_list(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDocumentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_documents_list_builder(
        client,
        &args.parent,
        &args.collectionId,
        &args.mask_fieldPaths,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.readTime,
        &args.showMissing,
        &args.transaction,
    )?;
    firestore_projects_databases_documents_list_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:listCollectionIds
/// Lists all the collection IDs underneath a document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_list_collection_ids_execute()` to send, or `firestore_projects_databases_documents_list_collection_ids` for simplest API.

pub fn firestore_projects_databases_documents_list_collection_ids_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:listCollectionIds",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:listCollectionIds
/// Lists all the collection IDs underneath a document.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_list_collection_ids_execute()` or `firestore_projects_databases_documents_list_collection_ids`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_list_collection_ids_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_list_collection_ids_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCollectionIdsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCollectionIdsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:listCollectionIds
/// Lists all the collection IDs underneath a document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_list_collection_ids_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_list_collection_ids_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_list_collection_ids()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_list_collection_ids_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_list_collection_ids_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCollectionIdsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_list_collection_ids_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_list_collection_ids`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsListCollectionIdsArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:listCollectionIds
/// Lists all the collection IDs underneath a document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_list_collection_ids_builder()` + `firestore_projects_databases_documents_list_collection_ids_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_list_collection_ids_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_list_collection_ids(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsListCollectionIdsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCollectionIdsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        firestore_projects_databases_documents_list_collection_ids_builder(client, &args.parent)?;
    firestore_projects_databases_documents_list_collection_ids_execute(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/documents/{collectionId}
/// Lists documents.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_list_documents_execute()` to send, or `firestore_projects_databases_documents_list_documents` for simplest API.

pub fn firestore_projects_databases_documents_list_documents_builder(
    client: &SimpleHttpClient,
    parent: &String,
    collectionId: &String,
    mask_fieldPaths: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readTime: &Option<Option<String>>,
    showMissing: &Option<Option<String>>,
    transaction: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{}/documents/{collectionId}",
        parent, collectionId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = mask_fieldPaths.as_ref() {
        query_parts.push(format!("mask.fieldPaths={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime.as_ref() {
        query_parts.push(format!("readTime={}", val));
    }
    if let Some(val) = showMissing.as_ref() {
        query_parts.push(format!("showMissing={}", val));
    }
    if let Some(val) = transaction.as_ref() {
        query_parts.push(format!("transaction={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/documents/{collectionId}
/// Lists documents.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_list_documents_execute()` or `firestore_projects_databases_documents_list_documents`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_list_documents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_list_documents_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDocumentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDocumentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/documents/{collectionId}
/// Lists documents.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_list_documents_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_list_documents_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_list_documents()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_list_documents_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_list_documents_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDocumentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_list_documents_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_list_documents`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsListDocumentsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Path parameter: collectionId
    pub collectionId: String,
    /// Query parameter: mask_fieldPaths
    pub mask_fieldPaths: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readTime
    pub readTime: Option<Option<String>>,
    /// Query parameter: showMissing
    pub showMissing: Option<Option<String>>,
    /// Query parameter: transaction
    pub transaction: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/documents/{collectionId}
/// Lists documents.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_list_documents_builder()` + `firestore_projects_databases_documents_list_documents_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_list_documents_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_list_documents(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsListDocumentsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDocumentsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_documents_list_documents_builder(
        client,
        &args.parent,
        &args.collectionId,
        &args.mask_fieldPaths,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.readTime,
        &args.showMissing,
        &args.transaction,
    )?;
    firestore_projects_databases_documents_list_documents_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:listen
/// Listens to changes. This method is only available via `gRPC` or WebChannel (not REST).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_listen_execute()` to send, or `firestore_projects_databases_documents_listen` for simplest API.

pub fn firestore_projects_databases_documents_listen_builder(
    client: &SimpleHttpClient,
    database: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents:listen",
        database,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:listen
/// Listens to changes. This method is only available via `gRPC` or WebChannel (not REST).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_listen_execute()` or `firestore_projects_databases_documents_listen`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_listen_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_listen_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListenResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:listen
/// Listens to changes. This method is only available via `gRPC` or WebChannel (not REST).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_listen_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_listen_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_listen()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_listen_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_listen_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_listen_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_listen`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsListenArgs {
    /// Path parameter: database
    pub database: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:listen
/// Listens to changes. This method is only available via `gRPC` or WebChannel (not REST).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_listen_builder()` + `firestore_projects_databases_documents_listen_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_listen_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_listen(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsListenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_documents_listen_builder(client, &args.database)?;
    firestore_projects_databases_documents_listen_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:partitionQuery
/// Partitions a query by returning partition cursors that can be used to run the query in parallel. The returned partition cursors are split points that can be used by RunQuery as `starting/end` points for the query results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_partition_query_execute()` to send, or `firestore_projects_databases_documents_partition_query` for simplest API.

pub fn firestore_projects_databases_documents_partition_query_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:partitionQuery",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:partitionQuery
/// Partitions a query by returning partition cursors that can be used to run the query in parallel. The returned partition cursors are split points that can be used by RunQuery as `starting/end` points for the query results.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_partition_query_execute()` or `firestore_projects_databases_documents_partition_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_partition_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_partition_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PartitionQueryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PartitionQueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:partitionQuery
/// Partitions a query by returning partition cursors that can be used to run the query in parallel. The returned partition cursors are split points that can be used by RunQuery as `starting/end` points for the query results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_partition_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_partition_query_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_partition_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_partition_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_partition_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PartitionQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_partition_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_partition_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsPartitionQueryArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:partitionQuery
/// Partitions a query by returning partition cursors that can be used to run the query in parallel. The returned partition cursors are split points that can be used by RunQuery as `starting/end` points for the query results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_partition_query_builder()` + `firestore_projects_databases_documents_partition_query_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_partition_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_partition_query(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsPartitionQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PartitionQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        firestore_projects_databases_documents_partition_query_builder(client, &args.parent)?;
    firestore_projects_databases_documents_partition_query_execute(builder)
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}
/// Updates or inserts a document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_patch_execute()` to send, or `firestore_projects_databases_documents_patch` for simplest API.

pub fn firestore_projects_databases_documents_patch_builder(
    client: &SimpleHttpClient,
    name: &String,
    currentDocument_exists: &Option<Option<String>>,
    currentDocument_updateTime: &Option<Option<String>>,
    mask_fieldPaths: &Option<Option<String>>,
    updateMask_fieldPaths: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents/{documentsId}/{documentsId1}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = currentDocument_exists.as_ref() {
        query_parts.push(format!("currentDocument.exists={}", val));
    }
    if let Some(val) = currentDocument_updateTime.as_ref() {
        query_parts.push(format!("currentDocument.updateTime={}", val));
    }
    if let Some(val) = mask_fieldPaths.as_ref() {
        query_parts.push(format!("mask.fieldPaths={}", val));
    }
    if let Some(val) = updateMask_fieldPaths.as_ref() {
        query_parts.push(format!("updateMask.fieldPaths={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}
/// Updates or inserts a document.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_patch_execute()` or `firestore_projects_databases_documents_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Document>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Document = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}
/// Updates or inserts a document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_patch_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Document>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: currentDocument_exists
    pub currentDocument_exists: Option<Option<String>>,
    /// Query parameter: currentDocument_updateTime
    pub currentDocument_updateTime: Option<Option<String>>,
    /// Query parameter: mask_fieldPaths
    pub mask_fieldPaths: Option<Option<String>>,
    /// Query parameter: updateMask_fieldPaths
    pub updateMask_fieldPaths: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}
/// Updates or inserts a document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_patch_builder()` + `firestore_projects_databases_documents_patch_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_patch(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Document>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_documents_patch_builder(
        client,
        &args.name,
        &args.currentDocument_exists,
        &args.currentDocument_updateTime,
        &args.mask_fieldPaths,
        &args.updateMask_fieldPaths,
    )?;
    firestore_projects_databases_documents_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:rollback
/// Rolls back a transaction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_rollback_execute()` to send, or `firestore_projects_databases_documents_rollback` for simplest API.

pub fn firestore_projects_databases_documents_rollback_builder(
    client: &SimpleHttpClient,
    database: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents:rollback",
        database,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:rollback
/// Rolls back a transaction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_rollback_execute()` or `firestore_projects_databases_documents_rollback`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_rollback_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:rollback
/// Rolls back a transaction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_rollback_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_rollback_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_rollback()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_rollback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_rollback_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_rollback_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_rollback`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsRollbackArgs {
    /// Path parameter: database
    pub database: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:rollback
/// Rolls back a transaction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_rollback_builder()` + `firestore_projects_databases_documents_rollback_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_rollback_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_rollback(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsRollbackArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_documents_rollback_builder(client, &args.database)?;
    firestore_projects_databases_documents_rollback_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:runAggregationQuery
/// Runs an aggregation query. Rather than producing Document results like Firestore.RunQuery, this API allows running an aggregation to produce a series of AggregationResult server-side. High-Level Example:  -- Return the number of documents in table given a filter. SELECT COUNT(*) FROM ( SELECT * FROM k where a = `true` );
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_run_aggregation_query_execute()` to send, or `firestore_projects_databases_documents_run_aggregation_query` for simplest API.

pub fn firestore_projects_databases_documents_run_aggregation_query_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:runAggregationQuery",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:runAggregationQuery
/// Runs an aggregation query. Rather than producing Document results like Firestore.RunQuery, this API allows running an aggregation to produce a series of AggregationResult server-side. High-Level Example:  -- Return the number of documents in table given a filter. SELECT COUNT(*) FROM ( SELECT * FROM k where a = `true` );
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_run_aggregation_query_execute()` or `firestore_projects_databases_documents_run_aggregation_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_run_aggregation_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_run_aggregation_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RunAggregationQueryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RunAggregationQueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:runAggregationQuery
/// Runs an aggregation query. Rather than producing Document results like Firestore.RunQuery, this API allows running an aggregation to produce a series of AggregationResult server-side. High-Level Example:  -- Return the number of documents in table given a filter. SELECT COUNT(*) FROM ( SELECT * FROM k where a = `true` );
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_run_aggregation_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_run_aggregation_query_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_run_aggregation_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_run_aggregation_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_run_aggregation_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RunAggregationQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_run_aggregation_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_run_aggregation_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsRunAggregationQueryArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:runAggregationQuery
/// Runs an aggregation query. Rather than producing Document results like Firestore.RunQuery, this API allows running an aggregation to produce a series of AggregationResult server-side. High-Level Example:  -- Return the number of documents in table given a filter. SELECT COUNT(*) FROM ( SELECT * FROM k where a = `true` );
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_run_aggregation_query_builder()` + `firestore_projects_databases_documents_run_aggregation_query_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_run_aggregation_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_run_aggregation_query(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsRunAggregationQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RunAggregationQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firestore_projects_databases_documents_run_aggregation_query_builder(client, &args.parent)?;
    firestore_projects_databases_documents_run_aggregation_query_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:runQuery
/// Runs a query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_run_query_execute()` to send, or `firestore_projects_databases_documents_run_query` for simplest API.

pub fn firestore_projects_databases_documents_run_query_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:runQuery",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:runQuery
/// Runs a query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_run_query_execute()` or `firestore_projects_databases_documents_run_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_run_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_run_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RunQueryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RunQueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:runQuery
/// Runs a query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_run_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_run_query_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_run_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_run_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_run_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RunQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_run_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_run_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsRunQueryArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents/{documentsId}/{documentsId1}:runQuery
/// Runs a query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_run_query_builder()` + `firestore_projects_databases_documents_run_query_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_run_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_run_query(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsRunQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RunQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_documents_run_query_builder(client, &args.parent)?;
    firestore_projects_databases_documents_run_query_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:write
/// Streams batches of document updates and deletes, in order. This method is only available via `gRPC` or WebChannel (not REST).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_documents_write_execute()` to send, or `firestore_projects_databases_documents_write` for simplest API.

pub fn firestore_projects_databases_documents_write_builder(
    client: &SimpleHttpClient,
    database: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/documents:write",
        database,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:write
/// Streams batches of document updates and deletes, in order. This method is only available via `gRPC` or WebChannel (not REST).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_documents_write_execute()` or `firestore_projects_databases_documents_write`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_write_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WriteResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WriteResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:write
/// Streams batches of document updates and deletes, in order. This method is only available via `gRPC` or WebChannel (not REST).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_documents_write_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_documents_write_task()`.
/// For the simplest API, use `firestore_projects_databases_documents_write()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_documents_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_documents_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WriteResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_documents_write_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_documents_write`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesDocumentsWriteArgs {
    /// Path parameter: database
    pub database: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/documents:write
/// Streams batches of document updates and deletes, in order. This method is only available via `gRPC` or WebChannel (not REST).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_documents_write_builder()` + `firestore_projects_databases_documents_write_execute()`.
/// For task-level control, use `firestore_projects_databases_documents_write_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_documents_write(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesDocumentsWriteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WriteResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_documents_write_builder(client, &args.database)?;
    firestore_projects_databases_documents_write_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_operations_cancel_execute()` to send, or `firestore_projects_databases_operations_cancel` for simplest API.

pub fn firestore_projects_databases_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_operations_cancel_execute()` or `firestore_projects_databases_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_operations_cancel_task()`.
/// For the simplest API, use `firestore_projects_databases_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_operations_cancel_builder()` + `firestore_projects_databases_operations_cancel_execute()`.
/// For task-level control, use `firestore_projects_databases_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_operations_cancel(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_operations_cancel_builder(client, &args.name)?;
    firestore_projects_databases_operations_cancel_execute(builder)
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_operations_delete_execute()` to send, or `firestore_projects_databases_operations_delete` for simplest API.

pub fn firestore_projects_databases_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_operations_delete_execute()` or `firestore_projects_databases_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_operations_delete_task()`.
/// For the simplest API, use `firestore_projects_databases_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_operations_delete_builder()` + `firestore_projects_databases_operations_delete_execute()`.
/// For task-level control, use `firestore_projects_databases_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_operations_delete(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_operations_delete_builder(client, &args.name)?;
    firestore_projects_databases_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_operations_get_execute()` to send, or `firestore_projects_databases_operations_get` for simplest API.

pub fn firestore_projects_databases_operations_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_operations_get_execute()` or `firestore_projects_databases_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_operations_get_task()`.
/// For the simplest API, use `firestore_projects_databases_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_operations_get_builder()` + `firestore_projects_databases_operations_get_execute()`.
/// For task-level control, use `firestore_projects_databases_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_operations_get(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_operations_get_builder(client, &args.name)?;
    firestore_projects_databases_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_operations_list_execute()` to send, or `firestore_projects_databases_operations_list` for simplest API.

pub fn firestore_projects_databases_operations_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_operations_list_execute()` or `firestore_projects_databases_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_operations_list_task()`.
/// For the simplest API, use `firestore_projects_databases_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_operations_list_builder()` + `firestore_projects_databases_operations_list_execute()`.
/// For task-level control, use `firestore_projects_databases_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_operations_list(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    firestore_projects_databases_operations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds
/// Create a user creds.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_user_creds_create_execute()` to send, or `firestore_projects_databases_user_creds_create` for simplest API.

pub fn firestore_projects_databases_user_creds_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    userCredsId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/userCreds",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = userCredsId.as_ref() {
        query_parts.push(format!("userCredsId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds
/// Create a user creds.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_user_creds_create_execute()` or `firestore_projects_databases_user_creds_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_user_creds_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_user_creds_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1UserCreds = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds
/// Create a user creds.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_user_creds_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_user_creds_create_task()`.
/// For the simplest API, use `firestore_projects_databases_user_creds_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_user_creds_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_user_creds_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_user_creds_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_user_creds_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesUserCredsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: userCredsId
    pub userCredsId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds
/// Create a user creds.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_user_creds_create_builder()` + `firestore_projects_databases_user_creds_create_execute()`.
/// For task-level control, use `firestore_projects_databases_user_creds_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_user_creds_create(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesUserCredsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_user_creds_create_builder(
        client,
        &args.parent,
        &args.userCredsId,
    )?;
    firestore_projects_databases_user_creds_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}
/// Deletes a user creds.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_user_creds_delete_execute()` to send, or `firestore_projects_databases_user_creds_delete` for simplest API.

pub fn firestore_projects_databases_user_creds_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/userCreds/{userCredsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}
/// Deletes a user creds.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_user_creds_delete_execute()` or `firestore_projects_databases_user_creds_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_user_creds_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_user_creds_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}
/// Deletes a user creds.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_user_creds_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_user_creds_delete_task()`.
/// For the simplest API, use `firestore_projects_databases_user_creds_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_user_creds_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_user_creds_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_user_creds_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_user_creds_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesUserCredsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}
/// Deletes a user creds.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_user_creds_delete_builder()` + `firestore_projects_databases_user_creds_delete_execute()`.
/// For task-level control, use `firestore_projects_databases_user_creds_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_user_creds_delete(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesUserCredsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_user_creds_delete_builder(client, &args.name)?;
    firestore_projects_databases_user_creds_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}:disable
/// Disables a user creds. No-op if the user creds are already disabled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_user_creds_disable_execute()` to send, or `firestore_projects_databases_user_creds_disable` for simplest API.

pub fn firestore_projects_databases_user_creds_disable_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/userCreds/{userCredsId}:disable",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}:disable
/// Disables a user creds. No-op if the user creds are already disabled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_user_creds_disable_execute()` or `firestore_projects_databases_user_creds_disable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_user_creds_disable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_user_creds_disable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1UserCreds = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}:disable
/// Disables a user creds. No-op if the user creds are already disabled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_user_creds_disable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_user_creds_disable_task()`.
/// For the simplest API, use `firestore_projects_databases_user_creds_disable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_user_creds_disable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_user_creds_disable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_user_creds_disable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_user_creds_disable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesUserCredsDisableArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}:disable
/// Disables a user creds. No-op if the user creds are already disabled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_user_creds_disable_builder()` + `firestore_projects_databases_user_creds_disable_execute()`.
/// For task-level control, use `firestore_projects_databases_user_creds_disable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_user_creds_disable(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesUserCredsDisableArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_user_creds_disable_builder(client, &args.name)?;
    firestore_projects_databases_user_creds_disable_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}:enable
/// Enables a user creds. No-op if the user creds are already enabled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_user_creds_enable_execute()` to send, or `firestore_projects_databases_user_creds_enable` for simplest API.

pub fn firestore_projects_databases_user_creds_enable_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/userCreds/{userCredsId}:enable",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}:enable
/// Enables a user creds. No-op if the user creds are already enabled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_user_creds_enable_execute()` or `firestore_projects_databases_user_creds_enable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_user_creds_enable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_user_creds_enable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1UserCreds = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}:enable
/// Enables a user creds. No-op if the user creds are already enabled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_user_creds_enable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_user_creds_enable_task()`.
/// For the simplest API, use `firestore_projects_databases_user_creds_enable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_user_creds_enable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_user_creds_enable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_user_creds_enable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_user_creds_enable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesUserCredsEnableArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}:enable
/// Enables a user creds. No-op if the user creds are already enabled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_user_creds_enable_builder()` + `firestore_projects_databases_user_creds_enable_execute()`.
/// For task-level control, use `firestore_projects_databases_user_creds_enable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_user_creds_enable(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesUserCredsEnableArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_user_creds_enable_builder(client, &args.name)?;
    firestore_projects_databases_user_creds_enable_execute(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}
/// Gets a user creds resource. Note that the returned resource does not contain the secret value itself.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_user_creds_get_execute()` to send, or `firestore_projects_databases_user_creds_get` for simplest API.

pub fn firestore_projects_databases_user_creds_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/userCreds/{userCredsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}
/// Gets a user creds resource. Note that the returned resource does not contain the secret value itself.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_user_creds_get_execute()` or `firestore_projects_databases_user_creds_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_user_creds_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_user_creds_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1UserCreds = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}
/// Gets a user creds resource. Note that the returned resource does not contain the secret value itself.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_user_creds_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_user_creds_get_task()`.
/// For the simplest API, use `firestore_projects_databases_user_creds_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_user_creds_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_user_creds_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_user_creds_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_user_creds_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesUserCredsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}
/// Gets a user creds resource. Note that the returned resource does not contain the secret value itself.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_user_creds_get_builder()` + `firestore_projects_databases_user_creds_get_execute()`.
/// For task-level control, use `firestore_projects_databases_user_creds_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_user_creds_get(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesUserCredsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_user_creds_get_builder(client, &args.name)?;
    firestore_projects_databases_user_creds_get_execute(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/userCreds
/// List all user creds in the database. Note that the returned resource does not contain the secret value itself.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_user_creds_list_execute()` to send, or `firestore_projects_databases_user_creds_list` for simplest API.

pub fn firestore_projects_databases_user_creds_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/userCreds",
        parent,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/userCreds
/// List all user creds in the database. Note that the returned resource does not contain the secret value itself.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_user_creds_list_execute()` or `firestore_projects_databases_user_creds_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_user_creds_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_user_creds_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1ListUserCredsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1ListUserCredsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/userCreds
/// List all user creds in the database. Note that the returned resource does not contain the secret value itself.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_user_creds_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_user_creds_list_task()`.
/// For the simplest API, use `firestore_projects_databases_user_creds_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_user_creds_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_user_creds_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1ListUserCredsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_user_creds_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_user_creds_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesUserCredsListArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1/projects/{projectsId}/databases/{databasesId}/userCreds
/// List all user creds in the database. Note that the returned resource does not contain the secret value itself.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_user_creds_list_builder()` + `firestore_projects_databases_user_creds_list_execute()`.
/// For task-level control, use `firestore_projects_databases_user_creds_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_user_creds_list(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesUserCredsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1ListUserCredsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_databases_user_creds_list_builder(client, &args.parent)?;
    firestore_projects_databases_user_creds_list_execute(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}:resetPassword
/// Resets the password of a user creds.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_databases_user_creds_reset_password_execute()` to send, or `firestore_projects_databases_user_creds_reset_password` for simplest API.

pub fn firestore_projects_databases_user_creds_reset_password_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/databases/{databasesId}/userCreds/{userCredsId}:resetPassword",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}:resetPassword
/// Resets the password of a user creds.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_databases_user_creds_reset_password_execute()` or `firestore_projects_databases_user_creds_reset_password`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_user_creds_reset_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_user_creds_reset_password_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1UserCreds = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}:resetPassword
/// Resets the password of a user creds.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_databases_user_creds_reset_password_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_databases_user_creds_reset_password_task()`.
/// For the simplest API, use `firestore_projects_databases_user_creds_reset_password()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_databases_user_creds_reset_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_databases_user_creds_reset_password_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_databases_user_creds_reset_password_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_databases_user_creds_reset_password`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsDatabasesUserCredsResetPasswordArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/databases/{databasesId}/userCreds/{userCredsId}:resetPassword
/// Resets the password of a user creds.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_databases_user_creds_reset_password_builder()` + `firestore_projects_databases_user_creds_reset_password_execute()`.
/// For task-level control, use `firestore_projects_databases_user_creds_reset_password_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_databases_user_creds_reset_password(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsDatabasesUserCredsResetPasswordArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1UserCreds>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firestore_projects_databases_user_creds_reset_password_builder(client, &args.name)?;
    firestore_projects_databases_user_creds_reset_password_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_locations_get_execute()` to send, or `firestore_projects_locations_get` for simplest API.

pub fn firestore_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_locations_get_execute()` or `firestore_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_locations_get_task()`.
/// For the simplest API, use `firestore_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firestore_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_locations_get_builder()` + `firestore_projects_locations_get_execute()`.
/// For task-level control, use `firestore_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_locations_get(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firestore_projects_locations_get_builder(client, &args.name)?;
    firestore_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_locations_list_execute()` to send, or `firestore_projects_locations_list` for simplest API.

pub fn firestore_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    extraLocationTypes: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_locations_list_execute()` or `firestore_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_locations_list_task()`.
/// For the simplest API, use `firestore_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_locations_list_builder()` + `firestore_projects_locations_list_execute()`.
/// For task-level control, use `firestore_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_locations_list(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    firestore_projects_locations_list_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/backups/{backupsId}
/// Deletes a backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_locations_backups_delete_execute()` to send, or `firestore_projects_locations_backups_delete` for simplest API.

pub fn firestore_projects_locations_backups_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/locations/{locationsId}/backups/{backupsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/backups/{backupsId}
/// Deletes a backup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_locations_backups_delete_execute()` or `firestore_projects_locations_backups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_locations_backups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_locations_backups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/backups/{backupsId}
/// Deletes a backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_locations_backups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_locations_backups_delete_task()`.
/// For the simplest API, use `firestore_projects_locations_backups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_locations_backups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_locations_backups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firestore_projects_locations_backups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_locations_backups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsLocationsBackupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/backups/{backupsId}
/// Deletes a backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_locations_backups_delete_builder()` + `firestore_projects_locations_backups_delete_execute()`.
/// For task-level control, use `firestore_projects_locations_backups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_locations_backups_delete(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsLocationsBackupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firestore_projects_locations_backups_delete_builder(client, &args.name)?;
    firestore_projects_locations_backups_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backups/{backupsId}
/// Gets information about a backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_locations_backups_get_execute()` to send, or `firestore_projects_locations_backups_get` for simplest API.

pub fn firestore_projects_locations_backups_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/locations/{locationsId}/backups/{backupsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backups/{backupsId}
/// Gets information about a backup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_locations_backups_get_execute()` or `firestore_projects_locations_backups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_locations_backups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_locations_backups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1Backup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1Backup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backups/{backupsId}
/// Gets information about a backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_locations_backups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_locations_backups_get_task()`.
/// For the simplest API, use `firestore_projects_locations_backups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_locations_backups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_locations_backups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1Backup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_locations_backups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_locations_backups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsLocationsBackupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backups/{backupsId}
/// Gets information about a backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_locations_backups_get_builder()` + `firestore_projects_locations_backups_get_execute()`.
/// For task-level control, use `firestore_projects_locations_backups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_locations_backups_get(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsLocationsBackupsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1Backup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = firestore_projects_locations_backups_get_builder(client, &args.name)?;
    firestore_projects_locations_backups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backups
/// Lists all the backups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firestore_projects_locations_backups_list_execute()` to send, or `firestore_projects_locations_backups_list` for simplest API.

pub fn firestore_projects_locations_backups_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://firestore.googleapis.com/v1/projects/{}/locations/{locationsId}/backups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backups
/// Lists all the backups.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firestore_projects_locations_backups_list_execute()` or `firestore_projects_locations_backups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_locations_backups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_locations_backups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleFirestoreAdminV1ListBackupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleFirestoreAdminV1ListBackupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backups
/// Lists all the backups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firestore_projects_locations_backups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firestore_projects_locations_backups_list_task()`.
/// For the simplest API, use `firestore_projects_locations_backups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firestore_projects_locations_backups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firestore_projects_locations_backups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1ListBackupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = firestore_projects_locations_backups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firestore_projects_locations_backups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirestoreProjectsLocationsBackupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backups
/// Lists all the backups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firestore_projects_locations_backups_list_builder()` + `firestore_projects_locations_backups_list_execute()`.
/// For task-level control, use `firestore_projects_locations_backups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firestore_projects_locations_backups_list(
    client: &SimpleHttpClient,
    args: &FirestoreProjectsLocationsBackupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleFirestoreAdminV1ListBackupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firestore_projects_locations_backups_list_builder(client, &args.parent, &args.filter)?;
    firestore_projects_locations_backups_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with FirestoreProjectsDatabasesBulkDeleteDocumentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesBulkDeleteDocumentsArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesBulkDeleteDocumentsArgs,
    ) -> String {
        format!("gcp::firestore::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with FirestoreProjectsDatabasesCloneArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesCloneArgs> for GoogleLongrunningOperation {
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesCloneArgs) -> String {
        format!(
            "gcp::firestore::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with FirestoreProjectsDatabasesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesCreateArgs> for GoogleLongrunningOperation {
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesCreateArgs) -> String {
        format!(
            "gcp::firestore::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with FirestoreProjectsDatabasesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDeleteArgs> for GoogleLongrunningOperation {
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesDeleteArgs) -> String {
        format!("gcp::firestore::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with FirestoreProjectsDatabasesExportDocumentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesExportDocumentsArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesExportDocumentsArgs,
    ) -> String {
        format!("gcp::firestore::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1Database
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1Database with FirestoreProjectsDatabasesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesGetArgs> for GoogleFirestoreAdminV1Database {
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesGetArgs) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1Database/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1Database"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with FirestoreProjectsDatabasesImportDocumentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesImportDocumentsArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesImportDocumentsArgs,
    ) -> String {
        format!("gcp::firestore::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1ListDatabasesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1ListDatabasesResponse with FirestoreProjectsDatabasesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesListArgs>
    for GoogleFirestoreAdminV1ListDatabasesResponse
{
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesListArgs) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1ListDatabasesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1ListDatabasesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with FirestoreProjectsDatabasesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesPatchArgs> for GoogleLongrunningOperation {
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesPatchArgs) -> String {
        format!("gcp::firestore::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with FirestoreProjectsDatabasesRestoreArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesRestoreArgs> for GoogleLongrunningOperation {
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesRestoreArgs) -> String {
        format!(
            "gcp::firestore::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1BackupSchedule
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1BackupSchedule with FirestoreProjectsDatabasesBackupSchedulesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesBackupSchedulesCreateArgs>
    for GoogleFirestoreAdminV1BackupSchedule
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesBackupSchedulesCreateArgs,
    ) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1BackupSchedule/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1BackupSchedule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with FirestoreProjectsDatabasesBackupSchedulesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesBackupSchedulesDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesBackupSchedulesDeleteArgs,
    ) -> String {
        format!("gcp::firestore::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1BackupSchedule
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1BackupSchedule with FirestoreProjectsDatabasesBackupSchedulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesBackupSchedulesGetArgs>
    for GoogleFirestoreAdminV1BackupSchedule
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesBackupSchedulesGetArgs,
    ) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1BackupSchedule/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1BackupSchedule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1ListBackupSchedulesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1ListBackupSchedulesResponse with FirestoreProjectsDatabasesBackupSchedulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesBackupSchedulesListArgs>
    for GoogleFirestoreAdminV1ListBackupSchedulesResponse
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesBackupSchedulesListArgs,
    ) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1ListBackupSchedulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1ListBackupSchedulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1BackupSchedule
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1BackupSchedule with FirestoreProjectsDatabasesBackupSchedulesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesBackupSchedulesPatchArgs>
    for GoogleFirestoreAdminV1BackupSchedule
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesBackupSchedulesPatchArgs,
    ) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1BackupSchedule/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1BackupSchedule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1Field
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1Field with FirestoreProjectsDatabasesCollectionGroupsFieldsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesCollectionGroupsFieldsGetArgs>
    for GoogleFirestoreAdminV1Field
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesCollectionGroupsFieldsGetArgs,
    ) -> String {
        format!("gcp::firestore::GoogleFirestoreAdminV1Field/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1Field"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1ListFieldsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1ListFieldsResponse with FirestoreProjectsDatabasesCollectionGroupsFieldsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesCollectionGroupsFieldsListArgs>
    for GoogleFirestoreAdminV1ListFieldsResponse
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesCollectionGroupsFieldsListArgs,
    ) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1ListFieldsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1ListFieldsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with FirestoreProjectsDatabasesCollectionGroupsFieldsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesCollectionGroupsFieldsPatchArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesCollectionGroupsFieldsPatchArgs,
    ) -> String {
        format!("gcp::firestore::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with FirestoreProjectsDatabasesCollectionGroupsIndexesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesCollectionGroupsIndexesCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesCollectionGroupsIndexesCreateArgs,
    ) -> String {
        format!(
            "gcp::firestore::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with FirestoreProjectsDatabasesCollectionGroupsIndexesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesCollectionGroupsIndexesDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesCollectionGroupsIndexesDeleteArgs,
    ) -> String {
        format!("gcp::firestore::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1Index
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1Index with FirestoreProjectsDatabasesCollectionGroupsIndexesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesCollectionGroupsIndexesGetArgs>
    for GoogleFirestoreAdminV1Index
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesCollectionGroupsIndexesGetArgs,
    ) -> String {
        format!("gcp::firestore::GoogleFirestoreAdminV1Index/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1Index"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1ListIndexesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1ListIndexesResponse with FirestoreProjectsDatabasesCollectionGroupsIndexesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesCollectionGroupsIndexesListArgs>
    for GoogleFirestoreAdminV1ListIndexesResponse
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesCollectionGroupsIndexesListArgs,
    ) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1ListIndexesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1ListIndexesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BatchGetDocumentsResponse
// =============================================================================

/// ResourceIdentifier implementation for BatchGetDocumentsResponse with FirestoreProjectsDatabasesDocumentsBatchGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsBatchGetArgs>
    for BatchGetDocumentsResponse
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesDocumentsBatchGetArgs,
    ) -> String {
        format!(
            "gcp::firestore::BatchGetDocumentsResponse/{}",
            input.database
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::BatchGetDocumentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BatchWriteResponse
// =============================================================================

/// ResourceIdentifier implementation for BatchWriteResponse with FirestoreProjectsDatabasesDocumentsBatchWriteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsBatchWriteArgs> for BatchWriteResponse {
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesDocumentsBatchWriteArgs,
    ) -> String {
        format!("gcp::firestore::BatchWriteResponse/{}", input.database)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::BatchWriteResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BeginTransactionResponse
// =============================================================================

/// ResourceIdentifier implementation for BeginTransactionResponse with FirestoreProjectsDatabasesDocumentsBeginTransactionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsBeginTransactionArgs>
    for BeginTransactionResponse
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesDocumentsBeginTransactionArgs,
    ) -> String {
        format!(
            "gcp::firestore::BeginTransactionResponse/{}",
            input.database
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::BeginTransactionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CommitResponse
// =============================================================================

/// ResourceIdentifier implementation for CommitResponse with FirestoreProjectsDatabasesDocumentsCommitArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsCommitArgs> for CommitResponse {
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesDocumentsCommitArgs,
    ) -> String {
        format!("gcp::firestore::CommitResponse/{}", input.database)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::CommitResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Document
// =============================================================================

/// ResourceIdentifier implementation for Document with FirestoreProjectsDatabasesDocumentsCreateDocumentArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsCreateDocumentArgs> for Document {
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesDocumentsCreateDocumentArgs,
    ) -> String {
        format!(
            "gcp::firestore::Document/{}/{}",
            input.parent, input.collectionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::Document"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with FirestoreProjectsDatabasesDocumentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesDocumentsDeleteArgs,
    ) -> String {
        format!("gcp::firestore::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ExecutePipelineResponse
// =============================================================================

/// ResourceIdentifier implementation for ExecutePipelineResponse with FirestoreProjectsDatabasesDocumentsExecutePipelineArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsExecutePipelineArgs>
    for ExecutePipelineResponse
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesDocumentsExecutePipelineArgs,
    ) -> String {
        format!("gcp::firestore::ExecutePipelineResponse/{}", input.database)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::ExecutePipelineResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Document
// =============================================================================

/// ResourceIdentifier implementation for Document with FirestoreProjectsDatabasesDocumentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsGetArgs> for Document {
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesDocumentsGetArgs) -> String {
        format!("gcp::firestore::Document/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::Document"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDocumentsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDocumentsResponse with FirestoreProjectsDatabasesDocumentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsListArgs> for ListDocumentsResponse {
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesDocumentsListArgs) -> String {
        format!(
            "gcp::firestore::ListDocumentsResponse/{}/{}",
            input.parent, input.collectionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::ListDocumentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCollectionIdsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCollectionIdsResponse with FirestoreProjectsDatabasesDocumentsListCollectionIdsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsListCollectionIdsArgs>
    for ListCollectionIdsResponse
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesDocumentsListCollectionIdsArgs,
    ) -> String {
        format!("gcp::firestore::ListCollectionIdsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::ListCollectionIdsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDocumentsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDocumentsResponse with FirestoreProjectsDatabasesDocumentsListDocumentsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsListDocumentsArgs>
    for ListDocumentsResponse
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesDocumentsListDocumentsArgs,
    ) -> String {
        format!(
            "gcp::firestore::ListDocumentsResponse/{}/{}",
            input.parent, input.collectionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::ListDocumentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListenResponse
// =============================================================================

/// ResourceIdentifier implementation for ListenResponse with FirestoreProjectsDatabasesDocumentsListenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsListenArgs> for ListenResponse {
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesDocumentsListenArgs,
    ) -> String {
        format!("gcp::firestore::ListenResponse/{}", input.database)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::ListenResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PartitionQueryResponse
// =============================================================================

/// ResourceIdentifier implementation for PartitionQueryResponse with FirestoreProjectsDatabasesDocumentsPartitionQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsPartitionQueryArgs>
    for PartitionQueryResponse
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesDocumentsPartitionQueryArgs,
    ) -> String {
        format!("gcp::firestore::PartitionQueryResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::PartitionQueryResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Document
// =============================================================================

/// ResourceIdentifier implementation for Document with FirestoreProjectsDatabasesDocumentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsPatchArgs> for Document {
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesDocumentsPatchArgs) -> String {
        format!("gcp::firestore::Document/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::Document"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with FirestoreProjectsDatabasesDocumentsRollbackArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsRollbackArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesDocumentsRollbackArgs,
    ) -> String {
        format!("gcp::firestore::Empty/{}", input.database)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RunAggregationQueryResponse
// =============================================================================

/// ResourceIdentifier implementation for RunAggregationQueryResponse with FirestoreProjectsDatabasesDocumentsRunAggregationQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsRunAggregationQueryArgs>
    for RunAggregationQueryResponse
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesDocumentsRunAggregationQueryArgs,
    ) -> String {
        format!(
            "gcp::firestore::RunAggregationQueryResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::RunAggregationQueryResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RunQueryResponse
// =============================================================================

/// ResourceIdentifier implementation for RunQueryResponse with FirestoreProjectsDatabasesDocumentsRunQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsRunQueryArgs> for RunQueryResponse {
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesDocumentsRunQueryArgs,
    ) -> String {
        format!("gcp::firestore::RunQueryResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::RunQueryResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WriteResponse
// =============================================================================

/// ResourceIdentifier implementation for WriteResponse with FirestoreProjectsDatabasesDocumentsWriteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesDocumentsWriteArgs> for WriteResponse {
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesDocumentsWriteArgs) -> String {
        format!("gcp::firestore::WriteResponse/{}", input.database)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::WriteResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with FirestoreProjectsDatabasesOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesOperationsCancelArgs,
    ) -> String {
        format!("gcp::firestore::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with FirestoreProjectsDatabasesOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesOperationsDeleteArgs,
    ) -> String {
        format!("gcp::firestore::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with FirestoreProjectsDatabasesOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesOperationsGetArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesOperationsGetArgs) -> String {
        format!("gcp::firestore::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse with FirestoreProjectsDatabasesOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesOperationsListArgs>
    for GoogleLongrunningListOperationsResponse
{
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesOperationsListArgs) -> String {
        format!(
            "gcp::firestore::GoogleLongrunningListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleLongrunningListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1UserCreds
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1UserCreds with FirestoreProjectsDatabasesUserCredsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesUserCredsCreateArgs>
    for GoogleFirestoreAdminV1UserCreds
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesUserCredsCreateArgs,
    ) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1UserCreds/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1UserCreds"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with FirestoreProjectsDatabasesUserCredsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesUserCredsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesUserCredsDeleteArgs,
    ) -> String {
        format!("gcp::firestore::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1UserCreds
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1UserCreds with FirestoreProjectsDatabasesUserCredsDisableArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesUserCredsDisableArgs>
    for GoogleFirestoreAdminV1UserCreds
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesUserCredsDisableArgs,
    ) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1UserCreds/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1UserCreds"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1UserCreds
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1UserCreds with FirestoreProjectsDatabasesUserCredsEnableArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesUserCredsEnableArgs>
    for GoogleFirestoreAdminV1UserCreds
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesUserCredsEnableArgs,
    ) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1UserCreds/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1UserCreds"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1UserCreds
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1UserCreds with FirestoreProjectsDatabasesUserCredsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesUserCredsGetArgs>
    for GoogleFirestoreAdminV1UserCreds
{
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesUserCredsGetArgs) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1UserCreds/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1UserCreds"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1ListUserCredsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1ListUserCredsResponse with FirestoreProjectsDatabasesUserCredsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesUserCredsListArgs>
    for GoogleFirestoreAdminV1ListUserCredsResponse
{
    fn generate_resource_id(&self, input: &FirestoreProjectsDatabasesUserCredsListArgs) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1ListUserCredsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1ListUserCredsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1UserCreds
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1UserCreds with FirestoreProjectsDatabasesUserCredsResetPasswordArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsDatabasesUserCredsResetPasswordArgs>
    for GoogleFirestoreAdminV1UserCreds
{
    fn generate_resource_id(
        &self,
        input: &FirestoreProjectsDatabasesUserCredsResetPasswordArgs,
    ) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1UserCreds/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1UserCreds"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with FirestoreProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &FirestoreProjectsLocationsGetArgs) -> String {
        format!("gcp::firestore::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with FirestoreProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &FirestoreProjectsLocationsListArgs) -> String {
        format!("gcp::firestore::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with FirestoreProjectsLocationsBackupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsLocationsBackupsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &FirestoreProjectsLocationsBackupsDeleteArgs) -> String {
        format!("gcp::firestore::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1Backup
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1Backup with FirestoreProjectsLocationsBackupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsLocationsBackupsGetArgs> for GoogleFirestoreAdminV1Backup {
    fn generate_resource_id(&self, input: &FirestoreProjectsLocationsBackupsGetArgs) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1Backup/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1Backup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleFirestoreAdminV1ListBackupsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleFirestoreAdminV1ListBackupsResponse with FirestoreProjectsLocationsBackupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FirestoreProjectsLocationsBackupsListArgs>
    for GoogleFirestoreAdminV1ListBackupsResponse
{
    fn generate_resource_id(&self, input: &FirestoreProjectsLocationsBackupsListArgs) -> String {
        format!(
            "gcp::firestore::GoogleFirestoreAdminV1ListBackupsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::firestore::GoogleFirestoreAdminV1ListBackupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
