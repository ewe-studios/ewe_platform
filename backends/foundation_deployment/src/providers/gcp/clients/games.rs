//! Auto-generated API clients for gcp/games.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/games`

#![cfg(feature = "gcp/games")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET games/v1/accesstokens/generatePlayGroupingApiToken
/// Generates a Play Grouping API token for the PGS user identified by the attached credential.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_accesstokens_generate_play_grouping_api_token_execute()` to send, or `games_accesstokens_generate_play_grouping_api_token` for simplest API.

pub fn games_accesstokens_generate_play_grouping_api_token_builder(
    client: &SimpleHttpClient,
    packageName: Option<&str>,
    persona: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url =
        format!("https://games.googleapis.com/games/v1/accesstokens/generatePlayGroupingApiToken",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = packageName {
        query_parts.push(format!("packageName={}", val));
    }
    if let Some(val) = persona {
        query_parts.push(format!("persona={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/accesstokens/generatePlayGroupingApiToken
/// Generates a Play Grouping API token for the PGS user identified by the attached credential.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_accesstokens_generate_play_grouping_api_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_accesstokens_generate_play_grouping_api_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_accesstokens_generate_play_grouping_api_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GeneratePlayGroupingApiTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GeneratePlayGroupingApiTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/accesstokens/generatePlayGroupingApiToken
/// Generates a Play Grouping API token for the PGS user identified by the attached credential.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_accesstokens_generate_play_grouping_api_token_builder()` + `games_accesstokens_generate_play_grouping_api_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_accesstokens_generate_play_grouping_api_token(
    client: &SimpleHttpClient,
    packageName: Option<&str>,
    persona: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GeneratePlayGroupingApiTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        games_accesstokens_generate_play_grouping_api_token_builder(client, packageName, persona)?;
    games_accesstokens_generate_play_grouping_api_token_execute(builder)
}

/// GET games/v1/accesstokens/generateRecallPlayGroupingApiToken
/// Generates a Play Grouping API token for the PGS user identified by the Recall session ID provided in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_accesstokens_generate_recall_play_grouping_api_token_execute()` to send, or `games_accesstokens_generate_recall_play_grouping_api_token` for simplest API.

pub fn games_accesstokens_generate_recall_play_grouping_api_token_builder(
    client: &SimpleHttpClient,
    packageName: Option<&str>,
    persona: Option<&str>,
    recallSessionId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/accesstokens/generateRecallPlayGroupingApiToken",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = packageName {
        query_parts.push(format!("packageName={}", val));
    }
    if let Some(val) = persona {
        query_parts.push(format!("persona={}", val));
    }
    if let Some(val) = recallSessionId {
        query_parts.push(format!("recallSessionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/accesstokens/generateRecallPlayGroupingApiToken
/// Generates a Play Grouping API token for the PGS user identified by the Recall session ID provided in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_accesstokens_generate_recall_play_grouping_api_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_accesstokens_generate_recall_play_grouping_api_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_accesstokens_generate_recall_play_grouping_api_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateRecallPlayGroupingApiTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateRecallPlayGroupingApiTokenResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/accesstokens/generateRecallPlayGroupingApiToken
/// Generates a Play Grouping API token for the PGS user identified by the Recall session ID provided in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_accesstokens_generate_recall_play_grouping_api_token_builder()` + `games_accesstokens_generate_recall_play_grouping_api_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_accesstokens_generate_recall_play_grouping_api_token(
    client: &SimpleHttpClient,
    packageName: Option<&str>,
    persona: Option<&str>,
    recallSessionId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateRecallPlayGroupingApiTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_accesstokens_generate_recall_play_grouping_api_token_builder(
        client,
        packageName,
        persona,
        recallSessionId,
    )?;
    games_accesstokens_generate_recall_play_grouping_api_token_execute(builder)
}

/// GET games/v1/achievements
/// Lists all the achievement definitions for your application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_achievement_definitions_list_execute()` to send, or `games_achievement_definitions_list` for simplest API.

pub fn games_achievement_definitions_list_builder(
    client: &SimpleHttpClient,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/achievements",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/achievements
/// Lists all the achievement definitions for your application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_achievement_definitions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_achievement_definitions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievement_definitions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementDefinitionsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementDefinitionsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/achievements
/// Lists all the achievement definitions for your application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_achievement_definitions_list_builder()` + `games_achievement_definitions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievement_definitions_list(
    client: &SimpleHttpClient,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementDefinitionsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        games_achievement_definitions_list_builder(client, language, maxResults, pageToken)?;
    games_achievement_definitions_list_execute(builder)
}

/// GET games/v1/achievements/{achievementId}/increment
/// Increments the steps of the achievement with the given ID for the currently authenticated player.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_achievements_increment_execute()` to send, or `games_achievements_increment` for simplest API.

pub fn games_achievements_increment_builder(
    client: &SimpleHttpClient,
    achievementId: &str,
    stepsToIncrement: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/achievements/{}/increment",
        achievementId, stepsToIncrement,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/achievements/{achievementId}/increment
/// Increments the steps of the achievement with the given ID for the currently authenticated player.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_achievements_increment_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_achievements_increment_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_increment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementIncrementResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementIncrementResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/achievements/{achievementId}/increment
/// Increments the steps of the achievement with the given ID for the currently authenticated player.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_achievements_increment_builder()` + `games_achievements_increment_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_increment(
    client: &SimpleHttpClient,
    achievementId: &str,
    stepsToIncrement: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementIncrementResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        games_achievements_increment_builder(client, achievementId, stepsToIncrement, requestId)?;
    games_achievements_increment_execute(builder)
}

/// GET games/v1/players/{playerId}/achievements
/// Lists the progress for all your application's achievements for the currently authenticated player.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_achievements_list_execute()` to send, or `games_achievements_list` for simplest API.

pub fn games_achievements_list_builder(
    client: &SimpleHttpClient,
    playerId: &str,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    state: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/players/{}/achievements",
        playerId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = state {
        query_parts.push(format!("state={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/{playerId}/achievements
/// Lists the progress for all your application's achievements for the currently authenticated player.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_achievements_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_achievements_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlayerAchievementListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlayerAchievementListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/players/{playerId}/achievements
/// Lists the progress for all your application's achievements for the currently authenticated player.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_achievements_list_builder()` + `games_achievements_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_list(
    client: &SimpleHttpClient,
    playerId: &str,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    state: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlayerAchievementListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        games_achievements_list_builder(client, playerId, language, maxResults, pageToken, state)?;
    games_achievements_list_execute(builder)
}

/// GET games/v1/achievements/{achievementId}/reveal
/// Sets the state of the achievement with the given ID to REVEALED for the currently authenticated player.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_achievements_reveal_execute()` to send, or `games_achievements_reveal` for simplest API.

pub fn games_achievements_reveal_builder(
    client: &SimpleHttpClient,
    achievementId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/achievements/{}/reveal",
        achievementId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/achievements/{achievementId}/reveal
/// Sets the state of the achievement with the given ID to REVEALED for the currently authenticated player.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_achievements_reveal_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_achievements_reveal_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_reveal_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementRevealResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementRevealResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/achievements/{achievementId}/reveal
/// Sets the state of the achievement with the given ID to REVEALED for the currently authenticated player.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_achievements_reveal_builder()` + `games_achievements_reveal_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_reveal(
    client: &SimpleHttpClient,
    achievementId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementRevealResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_achievements_reveal_builder(client, achievementId)?;
    games_achievements_reveal_execute(builder)
}

/// GET games/v1/achievements/{achievementId}/setStepsAtLeast
/// Sets the steps for the currently authenticated player towards unlocking an achievement. If the steps parameter is less than the current number of steps that the player already gained for the achievement, the achievement is not modified.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_achievements_set_steps_at_least_execute()` to send, or `games_achievements_set_steps_at_least` for simplest API.

pub fn games_achievements_set_steps_at_least_builder(
    client: &SimpleHttpClient,
    achievementId: &str,
    steps: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/achievements/{}/setStepsAtLeast",
        achievementId, steps,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/achievements/{achievementId}/setStepsAtLeast
/// Sets the steps for the currently authenticated player towards unlocking an achievement. If the steps parameter is less than the current number of steps that the player already gained for the achievement, the achievement is not modified.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_achievements_set_steps_at_least_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_achievements_set_steps_at_least_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_set_steps_at_least_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementSetStepsAtLeastResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementSetStepsAtLeastResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/achievements/{achievementId}/setStepsAtLeast
/// Sets the steps for the currently authenticated player towards unlocking an achievement. If the steps parameter is less than the current number of steps that the player already gained for the achievement, the achievement is not modified.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_achievements_set_steps_at_least_builder()` + `games_achievements_set_steps_at_least_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_set_steps_at_least(
    client: &SimpleHttpClient,
    achievementId: &str,
    steps: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementSetStepsAtLeastResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_achievements_set_steps_at_least_builder(client, achievementId, steps)?;
    games_achievements_set_steps_at_least_execute(builder)
}

/// GET games/v1/achievements/{achievementId}/unlock
/// Unlocks this achievement for the currently authenticated player.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_achievements_unlock_execute()` to send, or `games_achievements_unlock` for simplest API.

pub fn games_achievements_unlock_builder(
    client: &SimpleHttpClient,
    achievementId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/achievements/{}/unlock",
        achievementId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/achievements/{achievementId}/unlock
/// Unlocks this achievement for the currently authenticated player.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_achievements_unlock_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_achievements_unlock_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_unlock_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementUnlockResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementUnlockResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/achievements/{achievementId}/unlock
/// Unlocks this achievement for the currently authenticated player.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_achievements_unlock_builder()` + `games_achievements_unlock_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_unlock(
    client: &SimpleHttpClient,
    achievementId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementUnlockResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_achievements_unlock_builder(client, achievementId)?;
    games_achievements_unlock_execute(builder)
}

/// GET games/v1/achievements/updateMultiple
/// Updates multiple achievements for the currently authenticated player.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_achievements_update_multiple_execute()` to send, or `games_achievements_update_multiple` for simplest API.

pub fn games_achievements_update_multiple_builder(
    client: &SimpleHttpClient,
    body: &AchievementUpdateMultipleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/achievements/updateMultiple",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/achievements/updateMultiple
/// Updates multiple achievements for the currently authenticated player.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_achievements_update_multiple_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_achievements_update_multiple_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_update_multiple_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementUpdateMultipleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementUpdateMultipleResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/achievements/updateMultiple
/// Updates multiple achievements for the currently authenticated player.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_achievements_update_multiple_builder()` + `games_achievements_update_multiple_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_update_multiple(
    client: &SimpleHttpClient,
    body: &AchievementUpdateMultipleRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementUpdateMultipleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_achievements_update_multiple_builder(client, body)?;
    games_achievements_update_multiple_execute(builder)
}

/// GET games/v1/applications/{applicationId}
/// Retrieves the metadata of the application with the given ID. If the requested application is not available for the specified `platformType`, the returned response will not include any instance data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_applications_get_execute()` to send, or `games_applications_get` for simplest API.

pub fn games_applications_get_builder(
    client: &SimpleHttpClient,
    applicationId: &str,
    language: Option<&str>,
    platformType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/applications/{}",
        applicationId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = platformType {
        query_parts.push(format!("platformType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/applications/{applicationId}
/// Retrieves the metadata of the application with the given ID. If the requested application is not available for the specified `platformType`, the returned response will not include any instance data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_applications_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_applications_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Application>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Application = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/applications/{applicationId}
/// Retrieves the metadata of the application with the given ID. If the requested application is not available for the specified `platformType`, the returned response will not include any instance data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_applications_get_builder()` + `games_applications_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_get(
    client: &SimpleHttpClient,
    applicationId: &str,
    language: Option<&str>,
    platformType: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Application>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_applications_get_builder(client, applicationId, language, platformType)?;
    games_applications_get_execute(builder)
}

/// GET games/v1/applications/getEndPoint
/// Returns a URL for the requested end point type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_applications_get_end_point_execute()` to send, or `games_applications_get_end_point` for simplest API.

pub fn games_applications_get_end_point_builder(
    client: &SimpleHttpClient,
    applicationId: Option<&str>,
    endPointType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/applications/getEndPoint",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = applicationId {
        query_parts.push(format!("applicationId={}", val));
    }
    if let Some(val) = endPointType {
        query_parts.push(format!("endPointType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/applications/getEndPoint
/// Returns a URL for the requested end point type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_applications_get_end_point_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_applications_get_end_point_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_get_end_point_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EndPoint>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EndPoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/applications/getEndPoint
/// Returns a URL for the requested end point type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_applications_get_end_point_builder()` + `games_applications_get_end_point_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_get_end_point(
    client: &SimpleHttpClient,
    applicationId: Option<&str>,
    endPointType: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EndPoint>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_applications_get_end_point_builder(client, applicationId, endPointType)?;
    games_applications_get_end_point_execute(builder)
}

/// GET games/v1/applications/played
/// Indicate that the currently authenticated user is playing your application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_applications_played_execute()` to send, or `games_applications_played` for simplest API.

pub fn games_applications_played_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/applications/played",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/applications/played
/// Indicate that the currently authenticated user is playing your application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_applications_played_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_applications_played_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_played_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/applications/played
/// Indicate that the currently authenticated user is playing your application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_applications_played_builder()` + `games_applications_played_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_played(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_applications_played_builder(client)?;
    games_applications_played_execute(builder)
}

/// GET games/v1/applications/{applicationId}/verify
/// Verifies the auth token provided with this request is for the application with the specified ID, and returns the ID of the player it was granted for.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_applications_verify_execute()` to send, or `games_applications_verify` for simplest API.

pub fn games_applications_verify_builder(
    client: &SimpleHttpClient,
    applicationId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/applications/{}/verify",
        applicationId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/applications/{applicationId}/verify
/// Verifies the auth token provided with this request is for the application with the specified ID, and returns the ID of the player it was granted for.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_applications_verify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_applications_verify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_verify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApplicationVerifyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApplicationVerifyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/applications/{applicationId}/verify
/// Verifies the auth token provided with this request is for the application with the specified ID, and returns the ID of the player it was granted for.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_applications_verify_builder()` + `games_applications_verify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_verify(
    client: &SimpleHttpClient,
    applicationId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApplicationVerifyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_applications_verify_builder(client, applicationId)?;
    games_applications_verify_execute(builder)
}

/// GET games/v1/events
/// Returns a list showing the current progress on events in this application for the currently authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_events_list_by_player_execute()` to send, or `games_events_list_by_player` for simplest API.

pub fn games_events_list_by_player_builder(
    client: &SimpleHttpClient,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/events",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/events
/// Returns a list showing the current progress on events in this application for the currently authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_events_list_by_player_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_events_list_by_player_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_events_list_by_player_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerEventListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlayerEventListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/events
/// Returns a list showing the current progress on events in this application for the currently authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_events_list_by_player_builder()` + `games_events_list_by_player_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_events_list_by_player(
    client: &SimpleHttpClient,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerEventListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_events_list_by_player_builder(client, language, maxResults, pageToken)?;
    games_events_list_by_player_execute(builder)
}

/// GET games/v1/eventDefinitions
/// Returns a list of the event definitions in this application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_events_list_definitions_execute()` to send, or `games_events_list_definitions` for simplest API.

pub fn games_events_list_definitions_builder(
    client: &SimpleHttpClient,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/eventDefinitions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/eventDefinitions
/// Returns a list of the event definitions in this application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_events_list_definitions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_events_list_definitions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_events_list_definitions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventDefinitionListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventDefinitionListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/eventDefinitions
/// Returns a list of the event definitions in this application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_events_list_definitions_builder()` + `games_events_list_definitions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_events_list_definitions(
    client: &SimpleHttpClient,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventDefinitionListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_events_list_definitions_builder(client, language, maxResults, pageToken)?;
    games_events_list_definitions_execute(builder)
}

/// GET games/v1/events
/// Records a batch of changes to the number of times events have occurred for the currently authenticated user of this application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_events_record_execute()` to send, or `games_events_record` for simplest API.

pub fn games_events_record_builder(
    client: &SimpleHttpClient,
    language: Option<&str>,
    body: &EventRecordRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/events",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/events
/// Records a batch of changes to the number of times events have occurred for the currently authenticated user of this application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_events_record_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_events_record_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_events_record_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventUpdateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/events
/// Records a batch of changes to the number of times events have occurred for the currently authenticated user of this application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_events_record_builder()` + `games_events_record_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_events_record(
    client: &SimpleHttpClient,
    language: Option<&str>,
    body: &EventRecordRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EventUpdateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_events_record_builder(client, language, body)?;
    games_events_record_execute(builder)
}

/// GET games/v1/leaderboards/{leaderboardId}
/// Retrieves the metadata of the leaderboard with the given ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_leaderboards_get_execute()` to send, or `games_leaderboards_get` for simplest API.

pub fn games_leaderboards_get_builder(
    client: &SimpleHttpClient,
    leaderboardId: &str,
    language: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/leaderboards/{}",
        leaderboardId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/leaderboards/{leaderboardId}
/// Retrieves the metadata of the leaderboard with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_leaderboards_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_leaderboards_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_leaderboards_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Leaderboard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Leaderboard = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/leaderboards/{leaderboardId}
/// Retrieves the metadata of the leaderboard with the given ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_leaderboards_get_builder()` + `games_leaderboards_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_leaderboards_get(
    client: &SimpleHttpClient,
    leaderboardId: &str,
    language: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Leaderboard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_leaderboards_get_builder(client, leaderboardId, language)?;
    games_leaderboards_get_execute(builder)
}

/// GET games/v1/leaderboards
/// Lists all the leaderboard metadata for your application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_leaderboards_list_execute()` to send, or `games_leaderboards_list` for simplest API.

pub fn games_leaderboards_list_builder(
    client: &SimpleHttpClient,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/leaderboards",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/leaderboards
/// Lists all the leaderboard metadata for your application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_leaderboards_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_leaderboards_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_leaderboards_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LeaderboardListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/leaderboards
/// Lists all the leaderboard metadata for your application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_leaderboards_list_builder()` + `games_leaderboards_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_leaderboards_list(
    client: &SimpleHttpClient,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_leaderboards_list_builder(client, language, maxResults, pageToken)?;
    games_leaderboards_list_execute(builder)
}

/// GET games/v1/metagameConfig
/// Return the metagame configuration data for the calling application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_metagame_get_metagame_config_execute()` to send, or `games_metagame_get_metagame_config` for simplest API.

pub fn games_metagame_get_metagame_config_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/metagameConfig",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/metagameConfig
/// Return the metagame configuration data for the calling application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_metagame_get_metagame_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_metagame_get_metagame_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_metagame_get_metagame_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetagameConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MetagameConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/metagameConfig
/// Return the metagame configuration data for the calling application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_metagame_get_metagame_config_builder()` + `games_metagame_get_metagame_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_metagame_get_metagame_config(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetagameConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_metagame_get_metagame_config_builder(client)?;
    games_metagame_get_metagame_config_execute(builder)
}

/// GET games/v1/players/{playerId}/categories/{collection}
/// List play data aggregated per category for the player corresponding to `playerId`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_metagame_list_categories_by_player_execute()` to send, or `games_metagame_list_categories_by_player` for simplest API.

pub fn games_metagame_list_categories_by_player_builder(
    client: &SimpleHttpClient,
    playerId: &str,
    collection: &str,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/players/{}/categories/{}",
        playerId, collection,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/{playerId}/categories/{collection}
/// List play data aggregated per category for the player corresponding to `playerId`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_metagame_list_categories_by_player_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_metagame_list_categories_by_player_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_metagame_list_categories_by_player_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CategoryListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CategoryListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/players/{playerId}/categories/{collection}
/// List play data aggregated per category for the player corresponding to `playerId`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_metagame_list_categories_by_player_builder()` + `games_metagame_list_categories_by_player_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_metagame_list_categories_by_player(
    client: &SimpleHttpClient,
    playerId: &str,
    collection: &str,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CategoryListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_metagame_list_categories_by_player_builder(
        client, playerId, collection, language, maxResults, pageToken,
    )?;
    games_metagame_list_categories_by_player_execute(builder)
}

/// GET games/v1/players/{playerId}
/// Retrieves the Player resource with the given ID. To retrieve the player for the currently authenticated user, set `playerId` to me.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_players_get_execute()` to send, or `games_players_get` for simplest API.

pub fn games_players_get_builder(
    client: &SimpleHttpClient,
    playerId: &str,
    language: Option<&str>,
    playerIdConsistencyToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/players/{}", playerId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = playerIdConsistencyToken {
        query_parts.push(format!("playerIdConsistencyToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/{playerId}
/// Retrieves the Player resource with the given ID. To retrieve the player for the currently authenticated user, set `playerId` to me.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_players_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_players_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Player>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Player = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/players/{playerId}
/// Retrieves the Player resource with the given ID. To retrieve the player for the currently authenticated user, set `playerId` to me.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_players_get_builder()` + `games_players_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_get(
    client: &SimpleHttpClient,
    playerId: &str,
    language: Option<&str>,
    playerIdConsistencyToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Player>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_players_get_builder(client, playerId, language, playerIdConsistencyToken)?;
    games_players_get_execute(builder)
}

/// GET games/v1/players/me/multipleApplicationPlayerIds
/// Get the application player ids for the currently authenticated player across all requested games by the same developer as the calling application. This will only return ids for players that actually have an id (scoped or otherwise) with that game.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_players_get_multiple_application_player_ids_execute()` to send, or `games_players_get_multiple_application_player_ids` for simplest API.

pub fn games_players_get_multiple_application_player_ids_builder(
    client: &SimpleHttpClient,
    applicationIds: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url =
        format!("https://games.googleapis.com/games/v1/players/me/multipleApplicationPlayerIds",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = applicationIds {
        query_parts.push(format!("applicationIds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/me/multipleApplicationPlayerIds
/// Get the application player ids for the currently authenticated player across all requested games by the same developer as the calling application. This will only return ids for players that actually have an id (scoped or otherwise) with that game.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_players_get_multiple_application_player_ids_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_players_get_multiple_application_player_ids_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_get_multiple_application_player_ids_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetMultipleApplicationPlayerIdsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetMultipleApplicationPlayerIdsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/players/me/multipleApplicationPlayerIds
/// Get the application player ids for the currently authenticated player across all requested games by the same developer as the calling application. This will only return ids for players that actually have an id (scoped or otherwise) with that game.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_players_get_multiple_application_player_ids_builder()` + `games_players_get_multiple_application_player_ids_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_get_multiple_application_player_ids(
    client: &SimpleHttpClient,
    applicationIds: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetMultipleApplicationPlayerIdsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        games_players_get_multiple_application_player_ids_builder(client, applicationIds)?;
    games_players_get_multiple_application_player_ids_execute(builder)
}

/// GET games/v1/players/me/scopedIds
/// Retrieves scoped player identifiers for currently authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_players_get_scoped_player_ids_execute()` to send, or `games_players_get_scoped_player_ids` for simplest API.

pub fn games_players_get_scoped_player_ids_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/players/me/scopedIds",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/me/scopedIds
/// Retrieves scoped player identifiers for currently authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_players_get_scoped_player_ids_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_players_get_scoped_player_ids_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_get_scoped_player_ids_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ScopedPlayerIds>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ScopedPlayerIds = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/players/me/scopedIds
/// Retrieves scoped player identifiers for currently authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_players_get_scoped_player_ids_builder()` + `games_players_get_scoped_player_ids_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_get_scoped_player_ids(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ScopedPlayerIds>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_players_get_scoped_player_ids_builder(client)?;
    games_players_get_scoped_player_ids_execute(builder)
}

/// GET games/v1/players/me/players/{collection}
/// Get the collection of players for the currently authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_players_list_execute()` to send, or `games_players_list` for simplest API.

pub fn games_players_list_builder(
    client: &SimpleHttpClient,
    collection: &str,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/players/me/players/{}",
        collection,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/me/players/{collection}
/// Get the collection of players for the currently authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_players_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_players_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlayerListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/players/me/players/{collection}
/// Get the collection of players for the currently authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_players_list_builder()` + `games_players_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_list(
    client: &SimpleHttpClient,
    collection: &str,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_players_list_builder(client, collection, language, maxResults, pageToken)?;
    games_players_list_execute(builder)
}

/// GET games/v1/recall/gamesPlayerTokens/{sessionId}
/// Retrieve the Recall tokens from all requested games that is associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have an active PGS Player profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_recall_games_player_tokens_execute()` to send, or `games_recall_games_player_tokens` for simplest API.

pub fn games_recall_games_player_tokens_builder(
    client: &SimpleHttpClient,
    sessionId: &str,
    applicationIds: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/recall/gamesPlayerTokens/{}",
        sessionId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = applicationIds {
        query_parts.push(format!("applicationIds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/recall/gamesPlayerTokens/{sessionId}
/// Retrieve the Recall tokens from all requested games that is associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have an active PGS Player profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_recall_games_player_tokens_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_recall_games_player_tokens_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_games_player_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetrieveGamesPlayerTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RetrieveGamesPlayerTokensResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/recall/gamesPlayerTokens/{sessionId}
/// Retrieve the Recall tokens from all requested games that is associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have an active PGS Player profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_recall_games_player_tokens_builder()` + `games_recall_games_player_tokens_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_games_player_tokens(
    client: &SimpleHttpClient,
    sessionId: &str,
    applicationIds: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetrieveGamesPlayerTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_recall_games_player_tokens_builder(client, sessionId, applicationIds)?;
    games_recall_games_player_tokens_execute(builder)
}

/// GET games/v1/recall/developerGamesLastPlayerToken/{sessionId}
/// Retrieve the last Recall token from all developer games that is associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have active PGS Player profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_recall_last_token_from_all_developer_games_execute()` to send, or `games_recall_last_token_from_all_developer_games` for simplest API.

pub fn games_recall_last_token_from_all_developer_games_builder(
    client: &SimpleHttpClient,
    sessionId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/recall/developerGamesLastPlayerToken/{}",
        sessionId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/recall/developerGamesLastPlayerToken/{sessionId}
/// Retrieve the last Recall token from all developer games that is associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have active PGS Player profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_recall_last_token_from_all_developer_games_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_recall_last_token_from_all_developer_games_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_last_token_from_all_developer_games_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetrieveDeveloperGamesLastPlayerTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RetrieveDeveloperGamesLastPlayerTokenResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/recall/developerGamesLastPlayerToken/{sessionId}
/// Retrieve the last Recall token from all developer games that is associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have active PGS Player profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_recall_last_token_from_all_developer_games_builder()` + `games_recall_last_token_from_all_developer_games_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_last_token_from_all_developer_games(
    client: &SimpleHttpClient,
    sessionId: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetrieveDeveloperGamesLastPlayerTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_recall_last_token_from_all_developer_games_builder(client, sessionId)?;
    games_recall_last_token_from_all_developer_games_execute(builder)
}

/// GET games/v1/recall:linkPersona
/// Associate the PGS Player principal encoded in the provided recall session id with an in-game account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_recall_link_persona_execute()` to send, or `games_recall_link_persona` for simplest API.

pub fn games_recall_link_persona_builder(
    client: &SimpleHttpClient,
    body: &LinkPersonaRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/recall:linkPersona",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/recall:linkPersona
/// Associate the PGS Player principal encoded in the provided recall session id with an in-game account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_recall_link_persona_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_recall_link_persona_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_link_persona_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LinkPersonaResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LinkPersonaResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/recall:linkPersona
/// Associate the PGS Player principal encoded in the provided recall session id with an in-game account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_recall_link_persona_builder()` + `games_recall_link_persona_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_link_persona(
    client: &SimpleHttpClient,
    body: &LinkPersonaRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LinkPersonaResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_recall_link_persona_builder(client, body)?;
    games_recall_link_persona_execute(builder)
}

/// GET games/v1/recall:resetPersona
/// Delete all Recall tokens linking the given persona to any player (with or without a profile).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_recall_reset_persona_execute()` to send, or `games_recall_reset_persona` for simplest API.

pub fn games_recall_reset_persona_builder(
    client: &SimpleHttpClient,
    body: &ResetPersonaRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/recall:resetPersona",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/recall:resetPersona
/// Delete all Recall tokens linking the given persona to any player (with or without a profile).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_recall_reset_persona_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_recall_reset_persona_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_reset_persona_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResetPersonaResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResetPersonaResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/recall:resetPersona
/// Delete all Recall tokens linking the given persona to any player (with or without a profile).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_recall_reset_persona_builder()` + `games_recall_reset_persona_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_reset_persona(
    client: &SimpleHttpClient,
    body: &ResetPersonaRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResetPersonaResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_recall_reset_persona_builder(client, body)?;
    games_recall_reset_persona_execute(builder)
}

/// GET games/v1/recall/tokens/{sessionId}
/// Retrieve all Recall tokens associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have active PGS Player profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_recall_retrieve_tokens_execute()` to send, or `games_recall_retrieve_tokens` for simplest API.

pub fn games_recall_retrieve_tokens_builder(
    client: &SimpleHttpClient,
    sessionId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/recall/tokens/{}",
        sessionId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/recall/tokens/{sessionId}
/// Retrieve all Recall tokens associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have active PGS Player profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_recall_retrieve_tokens_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_recall_retrieve_tokens_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_retrieve_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetrievePlayerTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RetrievePlayerTokensResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/recall/tokens/{sessionId}
/// Retrieve all Recall tokens associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have active PGS Player profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_recall_retrieve_tokens_builder()` + `games_recall_retrieve_tokens_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_retrieve_tokens(
    client: &SimpleHttpClient,
    sessionId: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetrievePlayerTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_recall_retrieve_tokens_builder(client, sessionId)?;
    games_recall_retrieve_tokens_execute(builder)
}

/// GET games/v1/recall:unlinkPersona
/// Delete a Recall token linking the PGS Player principal identified by the Recall session and an in-game account identified either by the 'persona' or by the token value.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_recall_unlink_persona_execute()` to send, or `games_recall_unlink_persona` for simplest API.

pub fn games_recall_unlink_persona_builder(
    client: &SimpleHttpClient,
    body: &UnlinkPersonaRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/recall:unlinkPersona",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/recall:unlinkPersona
/// Delete a Recall token linking the PGS Player principal identified by the Recall session and an in-game account identified either by the 'persona' or by the token value.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_recall_unlink_persona_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_recall_unlink_persona_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_unlink_persona_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnlinkPersonaResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UnlinkPersonaResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/recall:unlinkPersona
/// Delete a Recall token linking the PGS Player principal identified by the Recall session and an in-game account identified either by the 'persona' or by the token value.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_recall_unlink_persona_builder()` + `games_recall_unlink_persona_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_unlink_persona(
    client: &SimpleHttpClient,
    body: &UnlinkPersonaRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnlinkPersonaResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_recall_unlink_persona_builder(client, body)?;
    games_recall_unlink_persona_execute(builder)
}

/// GET games/v1/revisions/check
/// Checks whether the games client is out of date.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_revisions_check_execute()` to send, or `games_revisions_check` for simplest API.

pub fn games_revisions_check_builder(
    client: &SimpleHttpClient,
    clientRevision: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/revisions/check",
        clientRevision,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/revisions/check
/// Checks whether the games client is out of date.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_revisions_check_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_revisions_check_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_revisions_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RevisionCheckResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RevisionCheckResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/revisions/check
/// Checks whether the games client is out of date.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_revisions_check_builder()` + `games_revisions_check_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_revisions_check(
    client: &SimpleHttpClient,
    clientRevision: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RevisionCheckResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_revisions_check_builder(client, clientRevision)?;
    games_revisions_check_execute(builder)
}

/// GET games/v1/players/{playerId}/leaderboards/{leaderboardId}/scores/{timeSpan}
/// Get high scores, and optionally ranks, in leaderboards for the currently authenticated player. For a specific time span, `leaderboardId` can be set to ALL to retrieve data for all leaderboards in a given time span. NOTE: You cannot ask for 'ALL' leaderboards and 'ALL' `timeSpans` in the same request; only one parameter may be set to 'ALL'.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_scores_get_execute()` to send, or `games_scores_get` for simplest API.

pub fn games_scores_get_builder(
    client: &SimpleHttpClient,
    playerId: &str,
    leaderboardId: &str,
    timeSpan: &str,
    includeRankType: Option<&str>,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/players/{}/leaderboards/{}/scores/{}",
        playerId, leaderboardId, timeSpan,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeRankType {
        query_parts.push(format!("includeRankType={}", val));
    }
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/{playerId}/leaderboards/{leaderboardId}/scores/{timeSpan}
/// Get high scores, and optionally ranks, in leaderboards for the currently authenticated player. For a specific time span, `leaderboardId` can be set to ALL to retrieve data for all leaderboards in a given time span. NOTE: You cannot ask for 'ALL' leaderboards and 'ALL' `timeSpans` in the same request; only one parameter may be set to 'ALL'.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_scores_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_scores_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_scores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlayerLeaderboardScoreListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlayerLeaderboardScoreListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/players/{playerId}/leaderboards/{leaderboardId}/scores/{timeSpan}
/// Get high scores, and optionally ranks, in leaderboards for the currently authenticated player. For a specific time span, `leaderboardId` can be set to ALL to retrieve data for all leaderboards in a given time span. NOTE: You cannot ask for 'ALL' leaderboards and 'ALL' `timeSpans` in the same request; only one parameter may be set to 'ALL'.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_scores_get_builder()` + `games_scores_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_scores_get(
    client: &SimpleHttpClient,
    playerId: &str,
    leaderboardId: &str,
    timeSpan: &str,
    includeRankType: Option<&str>,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlayerLeaderboardScoreListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_scores_get_builder(
        client,
        playerId,
        leaderboardId,
        timeSpan,
        includeRankType,
        language,
        maxResults,
        pageToken,
    )?;
    games_scores_get_execute(builder)
}

/// GET games/v1/leaderboards/{leaderboardId}/scores/{collection}
/// Lists the scores in a leaderboard, starting from the top.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_scores_list_execute()` to send, or `games_scores_list` for simplest API.

pub fn games_scores_list_builder(
    client: &SimpleHttpClient,
    leaderboardId: &str,
    collection: &str,
    timeSpan: &str,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/leaderboards/{}/scores/{}",
        leaderboardId, collection, timeSpan,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/leaderboards/{leaderboardId}/scores/{collection}
/// Lists the scores in a leaderboard, starting from the top.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_scores_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_scores_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_scores_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardScores>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LeaderboardScores = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/leaderboards/{leaderboardId}/scores/{collection}
/// Lists the scores in a leaderboard, starting from the top.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_scores_list_builder()` + `games_scores_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_scores_list(
    client: &SimpleHttpClient,
    leaderboardId: &str,
    collection: &str,
    timeSpan: &str,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardScores>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_scores_list_builder(
        client,
        leaderboardId,
        collection,
        timeSpan,
        language,
        maxResults,
        pageToken,
    )?;
    games_scores_list_execute(builder)
}

/// GET games/v1/leaderboards/{leaderboardId}/window/{collection}
/// Lists the scores in a leaderboard around (and including) a player's score.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_scores_list_window_execute()` to send, or `games_scores_list_window` for simplest API.

pub fn games_scores_list_window_builder(
    client: &SimpleHttpClient,
    leaderboardId: &str,
    collection: &str,
    timeSpan: &str,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    resultsAbove: Option<i32>,
    returnTopIfAbsent: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/leaderboards/{}/window/{}",
        leaderboardId, collection, timeSpan,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resultsAbove {
        query_parts.push(format!("resultsAbove={}", val));
    }
    if let Some(val) = returnTopIfAbsent {
        query_parts.push(format!("returnTopIfAbsent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/leaderboards/{leaderboardId}/window/{collection}
/// Lists the scores in a leaderboard around (and including) a player's score.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_scores_list_window_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_scores_list_window_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_scores_list_window_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardScores>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LeaderboardScores = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/leaderboards/{leaderboardId}/window/{collection}
/// Lists the scores in a leaderboard around (and including) a player's score.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_scores_list_window_builder()` + `games_scores_list_window_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_scores_list_window(
    client: &SimpleHttpClient,
    leaderboardId: &str,
    collection: &str,
    timeSpan: &str,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    resultsAbove: Option<i32>,
    returnTopIfAbsent: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardScores>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_scores_list_window_builder(
        client,
        leaderboardId,
        collection,
        timeSpan,
        language,
        maxResults,
        pageToken,
        resultsAbove,
        returnTopIfAbsent,
    )?;
    games_scores_list_window_execute(builder)
}

/// GET games/v1/leaderboards/{leaderboardId}/scores
/// Submits a score to the specified leaderboard.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_scores_submit_execute()` to send, or `games_scores_submit` for simplest API.

pub fn games_scores_submit_builder(
    client: &SimpleHttpClient,
    leaderboardId: &str,
    score: &str,
    language: Option<&str>,
    scoreTag: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/leaderboards/{}/scores",
        leaderboardId, score,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = scoreTag {
        query_parts.push(format!("scoreTag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/leaderboards/{leaderboardId}/scores
/// Submits a score to the specified leaderboard.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_scores_submit_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_scores_submit_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_scores_submit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerScoreResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlayerScoreResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/leaderboards/{leaderboardId}/scores
/// Submits a score to the specified leaderboard.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_scores_submit_builder()` + `games_scores_submit_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_scores_submit(
    client: &SimpleHttpClient,
    leaderboardId: &str,
    score: &str,
    language: Option<&str>,
    scoreTag: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerScoreResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_scores_submit_builder(client, leaderboardId, score, language, scoreTag)?;
    games_scores_submit_execute(builder)
}

/// GET games/v1/leaderboards/scores
/// Submits multiple scores to leaderboards.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_scores_submit_multiple_execute()` to send, or `games_scores_submit_multiple` for simplest API.

pub fn games_scores_submit_multiple_builder(
    client: &SimpleHttpClient,
    language: Option<&str>,
    body: &PlayerScoreSubmissionList,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/leaderboards/scores",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/leaderboards/scores
/// Submits multiple scores to leaderboards.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_scores_submit_multiple_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_scores_submit_multiple_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_scores_submit_multiple_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerScoreListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlayerScoreListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/leaderboards/scores
/// Submits multiple scores to leaderboards.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_scores_submit_multiple_builder()` + `games_scores_submit_multiple_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_scores_submit_multiple(
    client: &SimpleHttpClient,
    language: Option<&str>,
    body: &PlayerScoreSubmissionList,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerScoreListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_scores_submit_multiple_builder(client, language, body)?;
    games_scores_submit_multiple_execute(builder)
}

/// GET games/v1/snapshots/{snapshotId}
/// Retrieves the metadata for a given snapshot ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_snapshots_get_execute()` to send, or `games_snapshots_get` for simplest API.

pub fn games_snapshots_get_builder(
    client: &SimpleHttpClient,
    snapshotId: &str,
    language: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/snapshots/{}",
        snapshotId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/snapshots/{snapshotId}
/// Retrieves the metadata for a given snapshot ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_snapshots_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_snapshots_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_snapshots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/snapshots/{snapshotId}
/// Retrieves the metadata for a given snapshot ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_snapshots_get_builder()` + `games_snapshots_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_snapshots_get(
    client: &SimpleHttpClient,
    snapshotId: &str,
    language: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_snapshots_get_builder(client, snapshotId, language)?;
    games_snapshots_get_execute(builder)
}

/// GET games/v1/players/{playerId}/snapshots
/// Retrieves a list of snapshots created by your application for the player corresponding to the player ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_snapshots_list_execute()` to send, or `games_snapshots_list` for simplest API.

pub fn games_snapshots_list_builder(
    client: &SimpleHttpClient,
    playerId: &str,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://games.googleapis.com/games/v1/players/{}/snapshots",
        playerId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/{playerId}/snapshots
/// Retrieves a list of snapshots created by your application for the player corresponding to the player ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_snapshots_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_snapshots_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SnapshotListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/players/{playerId}/snapshots
/// Retrieves a list of snapshots created by your application for the player corresponding to the player ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_snapshots_list_builder()` + `games_snapshots_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_snapshots_list(
    client: &SimpleHttpClient,
    playerId: &str,
    language: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_snapshots_list_builder(client, playerId, language, maxResults, pageToken)?;
    games_snapshots_list_execute(builder)
}

/// GET games/v1/stats
/// Returns engagement and spend statistics in this application for the currently authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_stats_get_execute()` to send, or `games_stats_get` for simplest API.

pub fn games_stats_get_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://games.googleapis.com/games/v1/stats",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/stats
/// Returns engagement and spend statistics in this application for the currently authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `games_stats_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from games_stats_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_stats_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StatsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/stats
/// Returns engagement and spend statistics in this application for the currently authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_stats_get_builder()` + `games_stats_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_stats_get(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StatsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_stats_get_builder(client)?;
    games_stats_get_execute(builder)
}
