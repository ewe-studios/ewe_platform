//! Auto-generated API clients for gcp/games.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET games/v1/accesstokens/generatePlayGroupingApiToken
/// Generates a Play Grouping API token for the PGS user identified by the attached credential.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_accesstokens_generate_play_grouping_api_token_execute()` to send, or `games_accesstokens_generate_play_grouping_api_token` for simplest API.

pub fn games_accesstokens_generate_play_grouping_api_token_builder(
    client: &SimpleHttpClient,
    packageName: Option<String>,
    persona: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://games.googleapis.com/games/v1/accesstokens/generatePlayGroupingApiToken",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = packageName {
        query_parts.push(format!("packageName={}", val));
    }
    if let Some(val) = persona {
        query_parts.push(format!("persona={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/accesstokens/generatePlayGroupingApiToken
/// Generates a Play Grouping API token for the PGS user identified by the attached credential.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_accesstokens_generate_play_grouping_api_token_execute()` or `games_accesstokens_generate_play_grouping_api_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_accesstokens_generate_play_grouping_api_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_accesstokens_generate_play_grouping_api_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GeneratePlayGroupingApiTokenResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GeneratePlayGroupingApiTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/accesstokens/generatePlayGroupingApiToken
/// Generates a Play Grouping API token for the PGS user identified by the attached credential.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_accesstokens_generate_play_grouping_api_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_accesstokens_generate_play_grouping_api_token_task()`.
/// For the simplest API, use `games_accesstokens_generate_play_grouping_api_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_accesstokens_generate_play_grouping_api_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_accesstokens_generate_play_grouping_api_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GeneratePlayGroupingApiTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = games_accesstokens_generate_play_grouping_api_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_accesstokens_generate_play_grouping_api_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesAccesstokensGeneratePlayGroupingApiTokenArgs {
    /// Query parameter: packageName
    pub packageName: Option<String>,
    /// Query parameter: persona
    pub persona: Option<String>,
}

/// GET games/v1/accesstokens/generatePlayGroupingApiToken
/// Generates a Play Grouping API token for the PGS user identified by the attached credential.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_accesstokens_generate_play_grouping_api_token_builder()` + `games_accesstokens_generate_play_grouping_api_token_execute()`.
/// For task-level control, use `games_accesstokens_generate_play_grouping_api_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_accesstokens_generate_play_grouping_api_token(
    client: &SimpleHttpClient,
    args: &GamesAccesstokensGeneratePlayGroupingApiTokenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GeneratePlayGroupingApiTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_accesstokens_generate_play_grouping_api_token_builder(
        client,
        args.packageName.clone(),
        args.persona.clone(),
    )?;
    games_accesstokens_generate_play_grouping_api_token_execute(builder)
}

/// GET games/v1/accesstokens/generateRecallPlayGroupingApiToken
/// Generates a Play Grouping API token for the PGS user identified by the Recall session ID provided in the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_accesstokens_generate_recall_play_grouping_api_token_execute()` to send, or `games_accesstokens_generate_recall_play_grouping_api_token` for simplest API.

pub fn games_accesstokens_generate_recall_play_grouping_api_token_builder(
    client: &SimpleHttpClient,
    packageName: Option<String>,
    persona: Option<String>,
    recallSessionId: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/accesstokens/generateRecallPlayGroupingApiToken",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = packageName {
        query_parts.push(format!("packageName={}", val));
    }
    if let Some(val) = persona {
        query_parts.push(format!("persona={}", val));
    }
    if let Some(val) = recallSessionId {
        query_parts.push(format!("recallSessionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/accesstokens/generateRecallPlayGroupingApiToken
/// Generates a Play Grouping API token for the PGS user identified by the Recall session ID provided in the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_accesstokens_generate_recall_play_grouping_api_token_execute()` or `games_accesstokens_generate_recall_play_grouping_api_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_accesstokens_generate_recall_play_grouping_api_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_accesstokens_generate_recall_play_grouping_api_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateRecallPlayGroupingApiTokenResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateRecallPlayGroupingApiTokenResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/accesstokens/generateRecallPlayGroupingApiToken
/// Generates a Play Grouping API token for the PGS user identified by the Recall session ID provided in the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_accesstokens_generate_recall_play_grouping_api_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_accesstokens_generate_recall_play_grouping_api_token_task()`.
/// For the simplest API, use `games_accesstokens_generate_recall_play_grouping_api_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_accesstokens_generate_recall_play_grouping_api_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_accesstokens_generate_recall_play_grouping_api_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateRecallPlayGroupingApiTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = games_accesstokens_generate_recall_play_grouping_api_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_accesstokens_generate_recall_play_grouping_api_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesAccesstokensGenerateRecallPlayGroupingApiTokenArgs {
    /// Query parameter: packageName
    pub packageName: Option<String>,
    /// Query parameter: persona
    pub persona: Option<String>,
    /// Query parameter: recallSessionId
    pub recallSessionId: Option<String>,
}

/// GET games/v1/accesstokens/generateRecallPlayGroupingApiToken
/// Generates a Play Grouping API token for the PGS user identified by the Recall session ID provided in the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_accesstokens_generate_recall_play_grouping_api_token_builder()` + `games_accesstokens_generate_recall_play_grouping_api_token_execute()`.
/// For task-level control, use `games_accesstokens_generate_recall_play_grouping_api_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_accesstokens_generate_recall_play_grouping_api_token(
    client: &SimpleHttpClient,
    args: &GamesAccesstokensGenerateRecallPlayGroupingApiTokenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateRecallPlayGroupingApiTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_accesstokens_generate_recall_play_grouping_api_token_builder(
        client,
        args.packageName.clone(),
        args.persona.clone(),
        args.recallSessionId.clone(),
    )?;
    games_accesstokens_generate_recall_play_grouping_api_token_execute(builder)
}

/// GET games/v1/achievements
/// Lists all the achievement definitions for your application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_achievement_definitions_list_execute()` to send, or `games_achievement_definitions_list` for simplest API.

pub fn games_achievement_definitions_list_builder(
    client: &SimpleHttpClient,
    language: Option<String>,
    maxResults: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://games.googleapis.com/games/v1/achievements",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/achievements
/// Lists all the achievement definitions for your application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_achievement_definitions_list_execute()` or `games_achievement_definitions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_achievement_definitions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievement_definitions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AchievementDefinitionsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementDefinitionsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/achievements
/// Lists all the achievement definitions for your application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_achievement_definitions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_achievement_definitions_list_task()`.
/// For the simplest API, use `games_achievement_definitions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_achievement_definitions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_achievement_definitions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementDefinitionsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = games_achievement_definitions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_achievement_definitions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesAchievementDefinitionsListArgs {
    /// Query parameter: language
    pub language: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET games/v1/achievements
/// Lists all the achievement definitions for your application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_achievement_definitions_list_builder()` + `games_achievement_definitions_list_execute()`.
/// For task-level control, use `games_achievement_definitions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievement_definitions_list(
    client: &SimpleHttpClient,
    args: &GamesAchievementDefinitionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementDefinitionsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_achievement_definitions_list_builder(
        client,
        args.language.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
    )?;
    games_achievement_definitions_list_execute(builder)
}

/// GET games/v1/players/{playerId}/achievements
/// Lists the progress for all your application's achievements for the currently authenticated player.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_achievements_list_execute()` to send, or `games_achievements_list` for simplest API.

pub fn games_achievements_list_builder(
    client: &SimpleHttpClient,
    playerId: String,
    language: Option<String>,
    maxResults: Option<i32>,
    pageToken: Option<String>,
    state: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/players/{}/achievements",
        playerId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = state {
        query_parts.push(format!("state={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/{playerId}/achievements
/// Lists the progress for all your application's achievements for the currently authenticated player.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_achievements_list_execute()` or `games_achievements_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_achievements_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlayerAchievementListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlayerAchievementListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/players/{playerId}/achievements
/// Lists the progress for all your application's achievements for the currently authenticated player.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_achievements_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_achievements_list_task()`.
/// For the simplest API, use `games_achievements_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_achievements_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_achievements_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlayerAchievementListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = games_achievements_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_achievements_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesAchievementsListArgs {
    /// Path parameter: playerId
    pub playerId: String,
    /// Query parameter: language
    pub language: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: state
    pub state: Option<String>,
}

/// GET games/v1/players/{playerId}/achievements
/// Lists the progress for all your application's achievements for the currently authenticated player.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_achievements_list_builder()` + `games_achievements_list_execute()`.
/// For task-level control, use `games_achievements_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_list(
    client: &SimpleHttpClient,
    args: &GamesAchievementsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlayerAchievementListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_achievements_list_builder(
        client,
        args.playerId.clone(),
        args.language.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
        args.state.clone(),
    )?;
    games_achievements_list_execute(builder)
}

/// GET games/v1/achievements/{achievementId}/reveal
/// Sets the state of the achievement with the given ID to REVEALED for the currently authenticated player.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_achievements_reveal_execute()` to send, or `games_achievements_reveal` for simplest API.

pub fn games_achievements_reveal_builder(
    client: &SimpleHttpClient,
    achievementId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/achievements/{}/reveal",
        achievementId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/achievements/{achievementId}/reveal
/// Sets the state of the achievement with the given ID to REVEALED for the currently authenticated player.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_achievements_reveal_execute()` or `games_achievements_reveal`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_achievements_reveal_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_reveal_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AchievementRevealResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementRevealResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/achievements/{achievementId}/reveal
/// Sets the state of the achievement with the given ID to REVEALED for the currently authenticated player.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_achievements_reveal_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_achievements_reveal_task()`.
/// For the simplest API, use `games_achievements_reveal()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_achievements_reveal_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_achievements_reveal_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementRevealResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_achievements_reveal_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_achievements_reveal`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesAchievementsRevealArgs {
    /// Path parameter: achievementId
    pub achievementId: String,
}

/// GET games/v1/achievements/{achievementId}/reveal
/// Sets the state of the achievement with the given ID to REVEALED for the currently authenticated player.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_achievements_reveal_builder()` + `games_achievements_reveal_execute()`.
/// For task-level control, use `games_achievements_reveal_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_reveal(
    client: &SimpleHttpClient,
    args: &GamesAchievementsRevealArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementRevealResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_achievements_reveal_builder(client, args.achievementId.clone())?;
    games_achievements_reveal_execute(builder)
}

/// GET games/v1/achievements/{achievementId}/unlock
/// Unlocks this achievement for the currently authenticated player.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_achievements_unlock_execute()` to send, or `games_achievements_unlock` for simplest API.

pub fn games_achievements_unlock_builder(
    client: &SimpleHttpClient,
    achievementId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/achievements/{}/unlock",
        achievementId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/achievements/{achievementId}/unlock
/// Unlocks this achievement for the currently authenticated player.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_achievements_unlock_execute()` or `games_achievements_unlock`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_achievements_unlock_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_unlock_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AchievementUnlockResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementUnlockResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/achievements/{achievementId}/unlock
/// Unlocks this achievement for the currently authenticated player.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_achievements_unlock_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_achievements_unlock_task()`.
/// For the simplest API, use `games_achievements_unlock()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_achievements_unlock_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_achievements_unlock_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementUnlockResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_achievements_unlock_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_achievements_unlock`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesAchievementsUnlockArgs {
    /// Path parameter: achievementId
    pub achievementId: String,
}

/// GET games/v1/achievements/{achievementId}/unlock
/// Unlocks this achievement for the currently authenticated player.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_achievements_unlock_builder()` + `games_achievements_unlock_execute()`.
/// For task-level control, use `games_achievements_unlock_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_unlock(
    client: &SimpleHttpClient,
    args: &GamesAchievementsUnlockArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementUnlockResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_achievements_unlock_builder(client, args.achievementId.clone())?;
    games_achievements_unlock_execute(builder)
}

/// GET games/v1/achievements/updateMultiple
/// Updates multiple achievements for the currently authenticated player.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_achievements_update_multiple_execute()` to send, or `games_achievements_update_multiple` for simplest API.

pub fn games_achievements_update_multiple_builder(
    client: &SimpleHttpClient,
    body: &AchievementUpdateMultipleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://games.googleapis.com/games/v1/achievements/updateMultiple",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/achievements/updateMultiple
/// Updates multiple achievements for the currently authenticated player.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_achievements_update_multiple_execute()` or `games_achievements_update_multiple`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_achievements_update_multiple_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_update_multiple_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AchievementUpdateMultipleResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementUpdateMultipleResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/achievements/updateMultiple
/// Updates multiple achievements for the currently authenticated player.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_achievements_update_multiple_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_achievements_update_multiple_task()`.
/// For the simplest API, use `games_achievements_update_multiple()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_achievements_update_multiple_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_achievements_update_multiple_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementUpdateMultipleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = games_achievements_update_multiple_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_achievements_update_multiple`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesAchievementsUpdateMultipleArgs {
    /// Request body.
    pub body: AchievementUpdateMultipleRequest,
}

/// GET games/v1/achievements/updateMultiple
/// Updates multiple achievements for the currently authenticated player.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_achievements_update_multiple_builder()` + `games_achievements_update_multiple_execute()`.
/// For task-level control, use `games_achievements_update_multiple_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_achievements_update_multiple(
    client: &SimpleHttpClient,
    args: &GamesAchievementsUpdateMultipleArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementUpdateMultipleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_achievements_update_multiple_builder(client, &args.body)?;
    games_achievements_update_multiple_execute(builder)
}

/// GET games/v1/applications/{applicationId}
/// Retrieves the metadata of the application with the given ID. If the requested application is not available for the specified `platformType`, the returned response will not include any instance data.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_applications_get_execute()` to send, or `games_applications_get` for simplest API.

pub fn games_applications_get_builder(
    client: &SimpleHttpClient,
    applicationId: String,
    language: Option<String>,
    platformType: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/applications/{}",
        applicationId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = platformType {
        query_parts.push(format!("platformType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/applications/{applicationId}
/// Retrieves the metadata of the application with the given ID. If the requested application is not available for the specified `platformType`, the returned response will not include any instance data.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_applications_get_execute()` or `games_applications_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_applications_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Application>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Application = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/applications/{applicationId}
/// Retrieves the metadata of the application with the given ID. If the requested application is not available for the specified `platformType`, the returned response will not include any instance data.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_applications_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_applications_get_task()`.
/// For the simplest API, use `games_applications_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_applications_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_applications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Application>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_applications_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_applications_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesApplicationsGetArgs {
    /// Path parameter: applicationId
    pub applicationId: String,
    /// Query parameter: language
    pub language: Option<String>,
    /// Query parameter: platformType
    pub platformType: Option<String>,
}

/// GET games/v1/applications/{applicationId}
/// Retrieves the metadata of the application with the given ID. If the requested application is not available for the specified `platformType`, the returned response will not include any instance data.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_applications_get_builder()` + `games_applications_get_execute()`.
/// For task-level control, use `games_applications_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_get(
    client: &SimpleHttpClient,
    args: &GamesApplicationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Application>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_applications_get_builder(
        client,
        args.applicationId.clone(),
        args.language.clone(),
        args.platformType.clone(),
    )?;
    games_applications_get_execute(builder)
}

/// GET games/v1/applications/getEndPoint
/// Returns a URL for the requested end point type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_applications_get_end_point_execute()` to send, or `games_applications_get_end_point` for simplest API.

pub fn games_applications_get_end_point_builder(
    client: &SimpleHttpClient,
    applicationId: Option<String>,
    endPointType: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://games.googleapis.com/games/v1/applications/getEndPoint",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = applicationId {
        query_parts.push(format!("applicationId={}", val));
    }
    if let Some(val) = endPointType {
        query_parts.push(format!("endPointType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/applications/getEndPoint
/// Returns a URL for the requested end point type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_applications_get_end_point_execute()` or `games_applications_get_end_point`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_applications_get_end_point_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_get_end_point_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EndPoint>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EndPoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/applications/getEndPoint
/// Returns a URL for the requested end point type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_applications_get_end_point_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_applications_get_end_point_task()`.
/// For the simplest API, use `games_applications_get_end_point()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_applications_get_end_point_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_applications_get_end_point_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EndPoint>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_applications_get_end_point_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_applications_get_end_point`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesApplicationsGetEndPointArgs {
    /// Query parameter: applicationId
    pub applicationId: Option<String>,
    /// Query parameter: endPointType
    pub endPointType: Option<String>,
}

/// GET games/v1/applications/getEndPoint
/// Returns a URL for the requested end point type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_applications_get_end_point_builder()` + `games_applications_get_end_point_execute()`.
/// For task-level control, use `games_applications_get_end_point_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_get_end_point(
    client: &SimpleHttpClient,
    args: &GamesApplicationsGetEndPointArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EndPoint>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_applications_get_end_point_builder(
        client,
        args.applicationId.clone(),
        args.endPointType.clone(),
    )?;
    games_applications_get_end_point_execute(builder)
}

/// GET games/v1/applications/played
/// Indicate that the currently authenticated user is playing your application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_applications_played_execute()` to send, or `games_applications_played` for simplest API.

pub fn games_applications_played_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://games.googleapis.com/games/v1/applications/played",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/applications/played
/// Indicate that the currently authenticated user is playing your application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_applications_played_execute()` or `games_applications_played`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_applications_played_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_played_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/applications/played
/// Indicate that the currently authenticated user is playing your application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_applications_played_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_applications_played_task()`.
/// For the simplest API, use `games_applications_played()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_applications_played_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_applications_played_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_applications_played_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/applications/played
/// Indicate that the currently authenticated user is playing your application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_applications_played_builder()` + `games_applications_played_execute()`.
/// For task-level control, use `games_applications_played_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_played(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_applications_played_builder(client)?;
    games_applications_played_execute(builder)
}

/// GET games/v1/applications/{applicationId}/verify
/// Verifies the auth token provided with this request is for the application with the specified ID, and returns the ID of the player it was granted for.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_applications_verify_execute()` to send, or `games_applications_verify` for simplest API.

pub fn games_applications_verify_builder(
    client: &SimpleHttpClient,
    applicationId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/applications/{}/verify",
        applicationId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/applications/{applicationId}/verify
/// Verifies the auth token provided with this request is for the application with the specified ID, and returns the ID of the player it was granted for.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_applications_verify_execute()` or `games_applications_verify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_applications_verify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_verify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApplicationVerifyResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApplicationVerifyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/applications/{applicationId}/verify
/// Verifies the auth token provided with this request is for the application with the specified ID, and returns the ID of the player it was granted for.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_applications_verify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_applications_verify_task()`.
/// For the simplest API, use `games_applications_verify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_applications_verify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_applications_verify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApplicationVerifyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_applications_verify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_applications_verify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesApplicationsVerifyArgs {
    /// Path parameter: applicationId
    pub applicationId: String,
}

/// GET games/v1/applications/{applicationId}/verify
/// Verifies the auth token provided with this request is for the application with the specified ID, and returns the ID of the player it was granted for.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_applications_verify_builder()` + `games_applications_verify_execute()`.
/// For task-level control, use `games_applications_verify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_applications_verify(
    client: &SimpleHttpClient,
    args: &GamesApplicationsVerifyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApplicationVerifyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_applications_verify_builder(client, args.applicationId.clone())?;
    games_applications_verify_execute(builder)
}

/// GET games/v1/events
/// Returns a list showing the current progress on events in this application for the currently authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_events_list_by_player_execute()` to send, or `games_events_list_by_player` for simplest API.

pub fn games_events_list_by_player_builder(
    client: &SimpleHttpClient,
    language: Option<String>,
    maxResults: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://games.googleapis.com/games/v1/events",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/events
/// Returns a list showing the current progress on events in this application for the currently authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_events_list_by_player_execute()` or `games_events_list_by_player`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_events_list_by_player_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_events_list_by_player_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlayerEventListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlayerEventListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/events
/// Returns a list showing the current progress on events in this application for the currently authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_events_list_by_player_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_events_list_by_player_task()`.
/// For the simplest API, use `games_events_list_by_player()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_events_list_by_player_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_events_list_by_player_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerEventListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_events_list_by_player_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_events_list_by_player`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesEventsListByPlayerArgs {
    /// Query parameter: language
    pub language: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET games/v1/events
/// Returns a list showing the current progress on events in this application for the currently authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_events_list_by_player_builder()` + `games_events_list_by_player_execute()`.
/// For task-level control, use `games_events_list_by_player_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_events_list_by_player(
    client: &SimpleHttpClient,
    args: &GamesEventsListByPlayerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerEventListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_events_list_by_player_builder(
        client,
        args.language.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
    )?;
    games_events_list_by_player_execute(builder)
}

/// GET games/v1/eventDefinitions
/// Returns a list of the event definitions in this application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_events_list_definitions_execute()` to send, or `games_events_list_definitions` for simplest API.

pub fn games_events_list_definitions_builder(
    client: &SimpleHttpClient,
    language: Option<String>,
    maxResults: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://games.googleapis.com/games/v1/eventDefinitions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/eventDefinitions
/// Returns a list of the event definitions in this application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_events_list_definitions_execute()` or `games_events_list_definitions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_events_list_definitions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_events_list_definitions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventDefinitionListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EventDefinitionListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/eventDefinitions
/// Returns a list of the event definitions in this application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_events_list_definitions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_events_list_definitions_task()`.
/// For the simplest API, use `games_events_list_definitions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_events_list_definitions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_events_list_definitions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventDefinitionListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = games_events_list_definitions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_events_list_definitions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesEventsListDefinitionsArgs {
    /// Query parameter: language
    pub language: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET games/v1/eventDefinitions
/// Returns a list of the event definitions in this application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_events_list_definitions_builder()` + `games_events_list_definitions_execute()`.
/// For task-level control, use `games_events_list_definitions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_events_list_definitions(
    client: &SimpleHttpClient,
    args: &GamesEventsListDefinitionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EventDefinitionListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_events_list_definitions_builder(
        client,
        args.language.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
    )?;
    games_events_list_definitions_execute(builder)
}

/// GET games/v1/leaderboards/{leaderboardId}
/// Retrieves the metadata of the leaderboard with the given ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_leaderboards_get_execute()` to send, or `games_leaderboards_get` for simplest API.

pub fn games_leaderboards_get_builder(
    client: &SimpleHttpClient,
    leaderboardId: String,
    language: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/leaderboards/{}",
        leaderboardId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/leaderboards/{leaderboardId}
/// Retrieves the metadata of the leaderboard with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_leaderboards_get_execute()` or `games_leaderboards_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_leaderboards_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_leaderboards_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Leaderboard>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Leaderboard = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/leaderboards/{leaderboardId}
/// Retrieves the metadata of the leaderboard with the given ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_leaderboards_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_leaderboards_get_task()`.
/// For the simplest API, use `games_leaderboards_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_leaderboards_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_leaderboards_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Leaderboard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_leaderboards_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_leaderboards_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesLeaderboardsGetArgs {
    /// Path parameter: leaderboardId
    pub leaderboardId: String,
    /// Query parameter: language
    pub language: Option<String>,
}

/// GET games/v1/leaderboards/{leaderboardId}
/// Retrieves the metadata of the leaderboard with the given ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_leaderboards_get_builder()` + `games_leaderboards_get_execute()`.
/// For task-level control, use `games_leaderboards_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_leaderboards_get(
    client: &SimpleHttpClient,
    args: &GamesLeaderboardsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Leaderboard>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        games_leaderboards_get_builder(client, args.leaderboardId.clone(), args.language.clone())?;
    games_leaderboards_get_execute(builder)
}

/// GET games/v1/leaderboards
/// Lists all the leaderboard metadata for your application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_leaderboards_list_execute()` to send, or `games_leaderboards_list` for simplest API.

pub fn games_leaderboards_list_builder(
    client: &SimpleHttpClient,
    language: Option<String>,
    maxResults: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://games.googleapis.com/games/v1/leaderboards",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/leaderboards
/// Lists all the leaderboard metadata for your application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_leaderboards_list_execute()` or `games_leaderboards_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_leaderboards_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_leaderboards_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LeaderboardListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LeaderboardListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/leaderboards
/// Lists all the leaderboard metadata for your application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_leaderboards_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_leaderboards_list_task()`.
/// For the simplest API, use `games_leaderboards_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_leaderboards_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_leaderboards_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_leaderboards_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_leaderboards_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesLeaderboardsListArgs {
    /// Query parameter: language
    pub language: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET games/v1/leaderboards
/// Lists all the leaderboard metadata for your application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_leaderboards_list_builder()` + `games_leaderboards_list_execute()`.
/// For task-level control, use `games_leaderboards_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_leaderboards_list(
    client: &SimpleHttpClient,
    args: &GamesLeaderboardsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaderboardListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_leaderboards_list_builder(
        client,
        args.language.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
    )?;
    games_leaderboards_list_execute(builder)
}

/// GET games/v1/metagameConfig
/// Return the metagame configuration data for the calling application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_metagame_get_metagame_config_execute()` to send, or `games_metagame_get_metagame_config` for simplest API.

pub fn games_metagame_get_metagame_config_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://games.googleapis.com/games/v1/metagameConfig",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/metagameConfig
/// Return the metagame configuration data for the calling application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_metagame_get_metagame_config_execute()` or `games_metagame_get_metagame_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_metagame_get_metagame_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_metagame_get_metagame_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MetagameConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MetagameConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/metagameConfig
/// Return the metagame configuration data for the calling application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_metagame_get_metagame_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_metagame_get_metagame_config_task()`.
/// For the simplest API, use `games_metagame_get_metagame_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_metagame_get_metagame_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_metagame_get_metagame_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetagameConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_metagame_get_metagame_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/metagameConfig
/// Return the metagame configuration data for the calling application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_metagame_get_metagame_config_builder()` + `games_metagame_get_metagame_config_execute()`.
/// For task-level control, use `games_metagame_get_metagame_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_metagame_get_metagame_config(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MetagameConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_metagame_get_metagame_config_builder(client)?;
    games_metagame_get_metagame_config_execute(builder)
}

/// GET games/v1/players/{playerId}/categories/{collection}
/// List play data aggregated per category for the player corresponding to `playerId`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_metagame_list_categories_by_player_execute()` to send, or `games_metagame_list_categories_by_player` for simplest API.

pub fn games_metagame_list_categories_by_player_builder(
    client: &SimpleHttpClient,
    playerId: String,
    collection: String,
    language: Option<String>,
    maxResults: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/players/{}/categories/{}",
        playerId.as_str(),
        collection.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/{playerId}/categories/{collection}
/// List play data aggregated per category for the player corresponding to `playerId`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_metagame_list_categories_by_player_execute()` or `games_metagame_list_categories_by_player`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_metagame_list_categories_by_player_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_metagame_list_categories_by_player_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CategoryListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CategoryListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/players/{playerId}/categories/{collection}
/// List play data aggregated per category for the player corresponding to `playerId`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_metagame_list_categories_by_player_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_metagame_list_categories_by_player_task()`.
/// For the simplest API, use `games_metagame_list_categories_by_player()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_metagame_list_categories_by_player_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_metagame_list_categories_by_player_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CategoryListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_metagame_list_categories_by_player_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_metagame_list_categories_by_player`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesMetagameListCategoriesByPlayerArgs {
    /// Path parameter: playerId
    pub playerId: String,
    /// Path parameter: collection
    pub collection: String,
    /// Query parameter: language
    pub language: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET games/v1/players/{playerId}/categories/{collection}
/// List play data aggregated per category for the player corresponding to `playerId`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_metagame_list_categories_by_player_builder()` + `games_metagame_list_categories_by_player_execute()`.
/// For task-level control, use `games_metagame_list_categories_by_player_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_metagame_list_categories_by_player(
    client: &SimpleHttpClient,
    args: &GamesMetagameListCategoriesByPlayerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CategoryListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_metagame_list_categories_by_player_builder(
        client,
        args.playerId.clone(),
        args.collection.clone(),
        args.language.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
    )?;
    games_metagame_list_categories_by_player_execute(builder)
}

/// GET games/v1/players/{playerId}
/// Retrieves the Player resource with the given ID. To retrieve the player for the currently authenticated user, set `playerId` to me.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_players_get_execute()` to send, or `games_players_get` for simplest API.

pub fn games_players_get_builder(
    client: &SimpleHttpClient,
    playerId: String,
    language: Option<String>,
    playerIdConsistencyToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/players/{}",
        playerId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = playerIdConsistencyToken {
        query_parts.push(format!("playerIdConsistencyToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/{playerId}
/// Retrieves the Player resource with the given ID. To retrieve the player for the currently authenticated user, set `playerId` to me.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_players_get_execute()` or `games_players_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_players_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Player>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Player = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/players/{playerId}
/// Retrieves the Player resource with the given ID. To retrieve the player for the currently authenticated user, set `playerId` to me.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_players_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_players_get_task()`.
/// For the simplest API, use `games_players_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_players_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_players_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Player>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_players_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_players_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesPlayersGetArgs {
    /// Path parameter: playerId
    pub playerId: String,
    /// Query parameter: language
    pub language: Option<String>,
    /// Query parameter: playerIdConsistencyToken
    pub playerIdConsistencyToken: Option<String>,
}

/// GET games/v1/players/{playerId}
/// Retrieves the Player resource with the given ID. To retrieve the player for the currently authenticated user, set `playerId` to me.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_players_get_builder()` + `games_players_get_execute()`.
/// For task-level control, use `games_players_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_get(
    client: &SimpleHttpClient,
    args: &GamesPlayersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Player>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_players_get_builder(
        client,
        args.playerId.clone(),
        args.language.clone(),
        args.playerIdConsistencyToken.clone(),
    )?;
    games_players_get_execute(builder)
}

/// GET games/v1/players/me/multipleApplicationPlayerIds
/// Get the application player ids for the currently authenticated player across all requested games by the same developer as the calling application. This will only return ids for players that actually have an id (scoped or otherwise) with that game.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_players_get_multiple_application_player_ids_execute()` to send, or `games_players_get_multiple_application_player_ids` for simplest API.

pub fn games_players_get_multiple_application_player_ids_builder(
    client: &SimpleHttpClient,
    applicationIds: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://games.googleapis.com/games/v1/players/me/multipleApplicationPlayerIds",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = applicationIds {
        query_parts.push(format!("applicationIds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/me/multipleApplicationPlayerIds
/// Get the application player ids for the currently authenticated player across all requested games by the same developer as the calling application. This will only return ids for players that actually have an id (scoped or otherwise) with that game.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_players_get_multiple_application_player_ids_execute()` or `games_players_get_multiple_application_player_ids`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_players_get_multiple_application_player_ids_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_get_multiple_application_player_ids_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetMultipleApplicationPlayerIdsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetMultipleApplicationPlayerIdsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/players/me/multipleApplicationPlayerIds
/// Get the application player ids for the currently authenticated player across all requested games by the same developer as the calling application. This will only return ids for players that actually have an id (scoped or otherwise) with that game.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_players_get_multiple_application_player_ids_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_players_get_multiple_application_player_ids_task()`.
/// For the simplest API, use `games_players_get_multiple_application_player_ids()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_players_get_multiple_application_player_ids_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_players_get_multiple_application_player_ids_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetMultipleApplicationPlayerIdsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = games_players_get_multiple_application_player_ids_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_players_get_multiple_application_player_ids`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesPlayersGetMultipleApplicationPlayerIdsArgs {
    /// Query parameter: applicationIds
    pub applicationIds: Option<String>,
}

/// GET games/v1/players/me/multipleApplicationPlayerIds
/// Get the application player ids for the currently authenticated player across all requested games by the same developer as the calling application. This will only return ids for players that actually have an id (scoped or otherwise) with that game.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_players_get_multiple_application_player_ids_builder()` + `games_players_get_multiple_application_player_ids_execute()`.
/// For task-level control, use `games_players_get_multiple_application_player_ids_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_get_multiple_application_player_ids(
    client: &SimpleHttpClient,
    args: &GamesPlayersGetMultipleApplicationPlayerIdsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetMultipleApplicationPlayerIdsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_players_get_multiple_application_player_ids_builder(
        client,
        args.applicationIds.clone(),
    )?;
    games_players_get_multiple_application_player_ids_execute(builder)
}

/// GET games/v1/players/me/scopedIds
/// Retrieves scoped player identifiers for currently authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_players_get_scoped_player_ids_execute()` to send, or `games_players_get_scoped_player_ids` for simplest API.

pub fn games_players_get_scoped_player_ids_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://games.googleapis.com/games/v1/players/me/scopedIds",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/me/scopedIds
/// Retrieves scoped player identifiers for currently authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_players_get_scoped_player_ids_execute()` or `games_players_get_scoped_player_ids`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_players_get_scoped_player_ids_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_get_scoped_player_ids_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ScopedPlayerIds>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ScopedPlayerIds = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/players/me/scopedIds
/// Retrieves scoped player identifiers for currently authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_players_get_scoped_player_ids_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_players_get_scoped_player_ids_task()`.
/// For the simplest API, use `games_players_get_scoped_player_ids()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_players_get_scoped_player_ids_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_players_get_scoped_player_ids_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ScopedPlayerIds>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_players_get_scoped_player_ids_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/players/me/scopedIds
/// Retrieves scoped player identifiers for currently authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_players_get_scoped_player_ids_builder()` + `games_players_get_scoped_player_ids_execute()`.
/// For task-level control, use `games_players_get_scoped_player_ids_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_get_scoped_player_ids(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ScopedPlayerIds>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_players_get_scoped_player_ids_builder(client)?;
    games_players_get_scoped_player_ids_execute(builder)
}

/// GET games/v1/players/me/players/{collection}
/// Get the collection of players for the currently authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_players_list_execute()` to send, or `games_players_list` for simplest API.

pub fn games_players_list_builder(
    client: &SimpleHttpClient,
    collection: String,
    language: Option<String>,
    maxResults: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/players/me/players/{}",
        collection.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/me/players/{collection}
/// Get the collection of players for the currently authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_players_list_execute()` or `games_players_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_players_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlayerListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlayerListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/players/me/players/{collection}
/// Get the collection of players for the currently authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_players_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_players_list_task()`.
/// For the simplest API, use `games_players_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_players_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_players_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_players_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_players_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesPlayersListArgs {
    /// Path parameter: collection
    pub collection: String,
    /// Query parameter: language
    pub language: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET games/v1/players/me/players/{collection}
/// Get the collection of players for the currently authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_players_list_builder()` + `games_players_list_execute()`.
/// For task-level control, use `games_players_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_players_list(
    client: &SimpleHttpClient,
    args: &GamesPlayersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_players_list_builder(
        client,
        args.collection.clone(),
        args.language.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
    )?;
    games_players_list_execute(builder)
}

/// GET games/v1/recall/gamesPlayerTokens/{sessionId}
/// Retrieve the Recall tokens from all requested games that is associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have an active PGS Player profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_recall_games_player_tokens_execute()` to send, or `games_recall_games_player_tokens` for simplest API.

pub fn games_recall_games_player_tokens_builder(
    client: &SimpleHttpClient,
    sessionId: String,
    applicationIds: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/recall/gamesPlayerTokens/{}",
        sessionId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = applicationIds {
        query_parts.push(format!("applicationIds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/recall/gamesPlayerTokens/{sessionId}
/// Retrieve the Recall tokens from all requested games that is associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have an active PGS Player profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_recall_games_player_tokens_execute()` or `games_recall_games_player_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_recall_games_player_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_games_player_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RetrieveGamesPlayerTokensResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RetrieveGamesPlayerTokensResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/recall/gamesPlayerTokens/{sessionId}
/// Retrieve the Recall tokens from all requested games that is associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have an active PGS Player profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_recall_games_player_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_recall_games_player_tokens_task()`.
/// For the simplest API, use `games_recall_games_player_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_recall_games_player_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_recall_games_player_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetrieveGamesPlayerTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = games_recall_games_player_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_recall_games_player_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesRecallGamesPlayerTokensArgs {
    /// Path parameter: sessionId
    pub sessionId: String,
    /// Query parameter: applicationIds
    pub applicationIds: Option<String>,
}

/// GET games/v1/recall/gamesPlayerTokens/{sessionId}
/// Retrieve the Recall tokens from all requested games that is associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have an active PGS Player profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_recall_games_player_tokens_builder()` + `games_recall_games_player_tokens_execute()`.
/// For task-level control, use `games_recall_games_player_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_games_player_tokens(
    client: &SimpleHttpClient,
    args: &GamesRecallGamesPlayerTokensArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetrieveGamesPlayerTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_recall_games_player_tokens_builder(
        client,
        args.sessionId.clone(),
        args.applicationIds.clone(),
    )?;
    games_recall_games_player_tokens_execute(builder)
}

/// GET games/v1/recall/developerGamesLastPlayerToken/{sessionId}
/// Retrieve the last Recall token from all developer games that is associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have active PGS Player profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_recall_last_token_from_all_developer_games_execute()` to send, or `games_recall_last_token_from_all_developer_games` for simplest API.

pub fn games_recall_last_token_from_all_developer_games_builder(
    client: &SimpleHttpClient,
    sessionId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/recall/developerGamesLastPlayerToken/{}",
        sessionId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/recall/developerGamesLastPlayerToken/{sessionId}
/// Retrieve the last Recall token from all developer games that is associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have active PGS Player profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_recall_last_token_from_all_developer_games_execute()` or `games_recall_last_token_from_all_developer_games`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_recall_last_token_from_all_developer_games_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_last_token_from_all_developer_games_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RetrieveDeveloperGamesLastPlayerTokenResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RetrieveDeveloperGamesLastPlayerTokenResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/recall/developerGamesLastPlayerToken/{sessionId}
/// Retrieve the last Recall token from all developer games that is associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have active PGS Player profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_recall_last_token_from_all_developer_games_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_recall_last_token_from_all_developer_games_task()`.
/// For the simplest API, use `games_recall_last_token_from_all_developer_games()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_recall_last_token_from_all_developer_games_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_recall_last_token_from_all_developer_games_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetrieveDeveloperGamesLastPlayerTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = games_recall_last_token_from_all_developer_games_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_recall_last_token_from_all_developer_games`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesRecallLastTokenFromAllDeveloperGamesArgs {
    /// Path parameter: sessionId
    pub sessionId: String,
}

/// GET games/v1/recall/developerGamesLastPlayerToken/{sessionId}
/// Retrieve the last Recall token from all developer games that is associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have active PGS Player profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_recall_last_token_from_all_developer_games_builder()` + `games_recall_last_token_from_all_developer_games_execute()`.
/// For task-level control, use `games_recall_last_token_from_all_developer_games_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_last_token_from_all_developer_games(
    client: &SimpleHttpClient,
    args: &GamesRecallLastTokenFromAllDeveloperGamesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetrieveDeveloperGamesLastPlayerTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        games_recall_last_token_from_all_developer_games_builder(client, args.sessionId.clone())?;
    games_recall_last_token_from_all_developer_games_execute(builder)
}

/// GET games/v1/recall:linkPersona
/// Associate the PGS Player principal encoded in the provided recall session id with an in-game account
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_recall_link_persona_execute()` to send, or `games_recall_link_persona` for simplest API.

pub fn games_recall_link_persona_builder(
    client: &SimpleHttpClient,
    body: &LinkPersonaRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://games.googleapis.com/games/v1/recall:linkPersona",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/recall:linkPersona
/// Associate the PGS Player principal encoded in the provided recall session id with an in-game account
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_recall_link_persona_execute()` or `games_recall_link_persona`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_recall_link_persona_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_link_persona_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LinkPersonaResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LinkPersonaResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/recall:linkPersona
/// Associate the PGS Player principal encoded in the provided recall session id with an in-game account
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_recall_link_persona_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_recall_link_persona_task()`.
/// For the simplest API, use `games_recall_link_persona()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_recall_link_persona_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_recall_link_persona_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LinkPersonaResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_recall_link_persona_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_recall_link_persona`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesRecallLinkPersonaArgs {
    /// Request body.
    pub body: LinkPersonaRequest,
}

/// GET games/v1/recall:linkPersona
/// Associate the PGS Player principal encoded in the provided recall session id with an in-game account
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_recall_link_persona_builder()` + `games_recall_link_persona_execute()`.
/// For task-level control, use `games_recall_link_persona_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_link_persona(
    client: &SimpleHttpClient,
    args: &GamesRecallLinkPersonaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LinkPersonaResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_recall_link_persona_builder(client, &args.body)?;
    games_recall_link_persona_execute(builder)
}

/// GET games/v1/recall:resetPersona
/// Delete all Recall tokens linking the given persona to any player (with or without a profile).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_recall_reset_persona_execute()` to send, or `games_recall_reset_persona` for simplest API.

pub fn games_recall_reset_persona_builder(
    client: &SimpleHttpClient,
    body: &ResetPersonaRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://games.googleapis.com/games/v1/recall:resetPersona",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/recall:resetPersona
/// Delete all Recall tokens linking the given persona to any player (with or without a profile).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_recall_reset_persona_execute()` or `games_recall_reset_persona`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_recall_reset_persona_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_reset_persona_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResetPersonaResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResetPersonaResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/recall:resetPersona
/// Delete all Recall tokens linking the given persona to any player (with or without a profile).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_recall_reset_persona_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_recall_reset_persona_task()`.
/// For the simplest API, use `games_recall_reset_persona()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_recall_reset_persona_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_recall_reset_persona_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResetPersonaResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_recall_reset_persona_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_recall_reset_persona`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesRecallResetPersonaArgs {
    /// Request body.
    pub body: ResetPersonaRequest,
}

/// GET games/v1/recall:resetPersona
/// Delete all Recall tokens linking the given persona to any player (with or without a profile).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_recall_reset_persona_builder()` + `games_recall_reset_persona_execute()`.
/// For task-level control, use `games_recall_reset_persona_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_reset_persona(
    client: &SimpleHttpClient,
    args: &GamesRecallResetPersonaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResetPersonaResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_recall_reset_persona_builder(client, &args.body)?;
    games_recall_reset_persona_execute(builder)
}

/// GET games/v1/recall/tokens/{sessionId}
/// Retrieve all Recall tokens associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have active PGS Player profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_recall_retrieve_tokens_execute()` to send, or `games_recall_retrieve_tokens` for simplest API.

pub fn games_recall_retrieve_tokens_builder(
    client: &SimpleHttpClient,
    sessionId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/recall/tokens/{}",
        sessionId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/recall/tokens/{sessionId}
/// Retrieve all Recall tokens associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have active PGS Player profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_recall_retrieve_tokens_execute()` or `games_recall_retrieve_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_recall_retrieve_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_retrieve_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RetrievePlayerTokensResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RetrievePlayerTokensResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/recall/tokens/{sessionId}
/// Retrieve all Recall tokens associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have active PGS Player profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_recall_retrieve_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_recall_retrieve_tokens_task()`.
/// For the simplest API, use `games_recall_retrieve_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_recall_retrieve_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_recall_retrieve_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetrievePlayerTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = games_recall_retrieve_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_recall_retrieve_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesRecallRetrieveTokensArgs {
    /// Path parameter: sessionId
    pub sessionId: String,
}

/// GET games/v1/recall/tokens/{sessionId}
/// Retrieve all Recall tokens associated with the PGS Player encoded in the provided recall session id. The API is only available for users that have active PGS Player profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_recall_retrieve_tokens_builder()` + `games_recall_retrieve_tokens_execute()`.
/// For task-level control, use `games_recall_retrieve_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_retrieve_tokens(
    client: &SimpleHttpClient,
    args: &GamesRecallRetrieveTokensArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetrievePlayerTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_recall_retrieve_tokens_builder(client, args.sessionId.clone())?;
    games_recall_retrieve_tokens_execute(builder)
}

/// GET games/v1/recall:unlinkPersona
/// Delete a Recall token linking the PGS Player principal identified by the Recall session and an in-game account identified either by the 'persona' or by the token value.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_recall_unlink_persona_execute()` to send, or `games_recall_unlink_persona` for simplest API.

pub fn games_recall_unlink_persona_builder(
    client: &SimpleHttpClient,
    body: &UnlinkPersonaRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://games.googleapis.com/games/v1/recall:unlinkPersona",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/recall:unlinkPersona
/// Delete a Recall token linking the PGS Player principal identified by the Recall session and an in-game account identified either by the 'persona' or by the token value.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_recall_unlink_persona_execute()` or `games_recall_unlink_persona`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_recall_unlink_persona_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_unlink_persona_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UnlinkPersonaResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UnlinkPersonaResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/recall:unlinkPersona
/// Delete a Recall token linking the PGS Player principal identified by the Recall session and an in-game account identified either by the 'persona' or by the token value.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_recall_unlink_persona_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_recall_unlink_persona_task()`.
/// For the simplest API, use `games_recall_unlink_persona()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_recall_unlink_persona_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_recall_unlink_persona_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnlinkPersonaResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_recall_unlink_persona_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_recall_unlink_persona`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesRecallUnlinkPersonaArgs {
    /// Request body.
    pub body: UnlinkPersonaRequest,
}

/// GET games/v1/recall:unlinkPersona
/// Delete a Recall token linking the PGS Player principal identified by the Recall session and an in-game account identified either by the 'persona' or by the token value.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_recall_unlink_persona_builder()` + `games_recall_unlink_persona_execute()`.
/// For task-level control, use `games_recall_unlink_persona_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_recall_unlink_persona(
    client: &SimpleHttpClient,
    args: &GamesRecallUnlinkPersonaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnlinkPersonaResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_recall_unlink_persona_builder(client, &args.body)?;
    games_recall_unlink_persona_execute(builder)
}

/// GET games/v1/players/{playerId}/leaderboards/{leaderboardId}/scores/{timeSpan}
/// Get high scores, and optionally ranks, in leaderboards for the currently authenticated player. For a specific time span, `leaderboardId` can be set to ALL to retrieve data for all leaderboards in a given time span. NOTE: You cannot ask for 'ALL' leaderboards and 'ALL' `timeSpans` in the same request; only one parameter may be set to 'ALL'.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_scores_get_execute()` to send, or `games_scores_get` for simplest API.

pub fn games_scores_get_builder(
    client: &SimpleHttpClient,
    playerId: String,
    leaderboardId: String,
    timeSpan: String,
    includeRankType: Option<String>,
    language: Option<String>,
    maxResults: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/players/{}/leaderboards/{}/scores/{}",
        playerId.as_str(),
        leaderboardId.as_str(),
        timeSpan.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = includeRankType {
        query_parts.push(format!("includeRankType={}", val));
    }
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/{playerId}/leaderboards/{leaderboardId}/scores/{timeSpan}
/// Get high scores, and optionally ranks, in leaderboards for the currently authenticated player. For a specific time span, `leaderboardId` can be set to ALL to retrieve data for all leaderboards in a given time span. NOTE: You cannot ask for 'ALL' leaderboards and 'ALL' `timeSpans` in the same request; only one parameter may be set to 'ALL'.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_scores_get_execute()` or `games_scores_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_scores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_scores_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlayerLeaderboardScoreListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlayerLeaderboardScoreListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/players/{playerId}/leaderboards/{leaderboardId}/scores/{timeSpan}
/// Get high scores, and optionally ranks, in leaderboards for the currently authenticated player. For a specific time span, `leaderboardId` can be set to ALL to retrieve data for all leaderboards in a given time span. NOTE: You cannot ask for 'ALL' leaderboards and 'ALL' `timeSpans` in the same request; only one parameter may be set to 'ALL'.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_scores_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_scores_get_task()`.
/// For the simplest API, use `games_scores_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_scores_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_scores_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlayerLeaderboardScoreListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = games_scores_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_scores_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesScoresGetArgs {
    /// Path parameter: playerId
    pub playerId: String,
    /// Path parameter: leaderboardId
    pub leaderboardId: String,
    /// Path parameter: timeSpan
    pub timeSpan: String,
    /// Query parameter: includeRankType
    pub includeRankType: Option<String>,
    /// Query parameter: language
    pub language: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET games/v1/players/{playerId}/leaderboards/{leaderboardId}/scores/{timeSpan}
/// Get high scores, and optionally ranks, in leaderboards for the currently authenticated player. For a specific time span, `leaderboardId` can be set to ALL to retrieve data for all leaderboards in a given time span. NOTE: You cannot ask for 'ALL' leaderboards and 'ALL' `timeSpans` in the same request; only one parameter may be set to 'ALL'.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_scores_get_builder()` + `games_scores_get_execute()`.
/// For task-level control, use `games_scores_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_scores_get(
    client: &SimpleHttpClient,
    args: &GamesScoresGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlayerLeaderboardScoreListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_scores_get_builder(
        client,
        args.playerId.clone(),
        args.leaderboardId.clone(),
        args.timeSpan.clone(),
        args.includeRankType.clone(),
        args.language.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
    )?;
    games_scores_get_execute(builder)
}

/// GET games/v1/leaderboards/scores
/// Submits multiple scores to leaderboards.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_scores_submit_multiple_execute()` to send, or `games_scores_submit_multiple` for simplest API.

pub fn games_scores_submit_multiple_builder(
    client: &SimpleHttpClient,
    language: Option<String>,
    body: &PlayerScoreSubmissionList,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://games.googleapis.com/games/v1/leaderboards/scores",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/leaderboards/scores
/// Submits multiple scores to leaderboards.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_scores_submit_multiple_execute()` or `games_scores_submit_multiple`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_scores_submit_multiple_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_scores_submit_multiple_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlayerScoreListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlayerScoreListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/leaderboards/scores
/// Submits multiple scores to leaderboards.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_scores_submit_multiple_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_scores_submit_multiple_task()`.
/// For the simplest API, use `games_scores_submit_multiple()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_scores_submit_multiple_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_scores_submit_multiple_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerScoreListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_scores_submit_multiple_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_scores_submit_multiple`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesScoresSubmitMultipleArgs {
    /// Query parameter: language
    pub language: Option<String>,
    /// Request body.
    pub body: PlayerScoreSubmissionList,
}

/// GET games/v1/leaderboards/scores
/// Submits multiple scores to leaderboards.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_scores_submit_multiple_builder()` + `games_scores_submit_multiple_execute()`.
/// For task-level control, use `games_scores_submit_multiple_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_scores_submit_multiple(
    client: &SimpleHttpClient,
    args: &GamesScoresSubmitMultipleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerScoreListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_scores_submit_multiple_builder(client, args.language.clone(), &args.body)?;
    games_scores_submit_multiple_execute(builder)
}

/// GET games/v1/snapshots/{snapshotId}
/// Retrieves the metadata for a given snapshot ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_snapshots_get_execute()` to send, or `games_snapshots_get` for simplest API.

pub fn games_snapshots_get_builder(
    client: &SimpleHttpClient,
    snapshotId: String,
    language: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/snapshots/{}",
        snapshotId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/snapshots/{snapshotId}
/// Retrieves the metadata for a given snapshot ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_snapshots_get_execute()` or `games_snapshots_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_snapshots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_snapshots_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snapshot>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/snapshots/{snapshotId}
/// Retrieves the metadata for a given snapshot ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_snapshots_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_snapshots_get_task()`.
/// For the simplest API, use `games_snapshots_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_snapshots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_snapshots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_snapshots_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_snapshots_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesSnapshotsGetArgs {
    /// Path parameter: snapshotId
    pub snapshotId: String,
    /// Query parameter: language
    pub language: Option<String>,
}

/// GET games/v1/snapshots/{snapshotId}
/// Retrieves the metadata for a given snapshot ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_snapshots_get_builder()` + `games_snapshots_get_execute()`.
/// For task-level control, use `games_snapshots_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_snapshots_get(
    client: &SimpleHttpClient,
    args: &GamesSnapshotsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        games_snapshots_get_builder(client, args.snapshotId.clone(), args.language.clone())?;
    games_snapshots_get_execute(builder)
}

/// GET games/v1/players/{playerId}/snapshots
/// Retrieves a list of snapshots created by your application for the player corresponding to the player ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_snapshots_list_execute()` to send, or `games_snapshots_list` for simplest API.

pub fn games_snapshots_list_builder(
    client: &SimpleHttpClient,
    playerId: String,
    language: Option<String>,
    maxResults: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/players/{}/snapshots",
        playerId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = language {
        query_parts.push(format!("language={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/players/{playerId}/snapshots
/// Retrieves a list of snapshots created by your application for the player corresponding to the player ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_snapshots_list_execute()` or `games_snapshots_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_snapshots_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SnapshotListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SnapshotListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/players/{playerId}/snapshots
/// Retrieves a list of snapshots created by your application for the player corresponding to the player ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_snapshots_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_snapshots_list_task()`.
/// For the simplest API, use `games_snapshots_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_snapshots_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_snapshots_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesSnapshotsListArgs {
    /// Path parameter: playerId
    pub playerId: String,
    /// Query parameter: language
    pub language: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET games/v1/players/{playerId}/snapshots
/// Retrieves a list of snapshots created by your application for the player corresponding to the player ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_snapshots_list_builder()` + `games_snapshots_list_execute()`.
/// For task-level control, use `games_snapshots_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_snapshots_list(
    client: &SimpleHttpClient,
    args: &GamesSnapshotsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SnapshotListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_snapshots_list_builder(
        client,
        args.playerId.clone(),
        args.language.clone(),
        args.maxResults.clone(),
        args.pageToken.clone(),
    )?;
    games_snapshots_list_execute(builder)
}

/// GET games/v1/stats
/// Returns engagement and spend statistics in this application for the currently authenticated user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_stats_get_execute()` to send, or `games_stats_get` for simplest API.

pub fn games_stats_get_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://games.googleapis.com/games/v1/stats",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1/stats
/// Returns engagement and spend statistics in this application for the currently authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_stats_get_execute()` or `games_stats_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_stats_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_stats_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StatsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1/stats
/// Returns engagement and spend statistics in this application for the currently authenticated user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_stats_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_stats_get_task()`.
/// For the simplest API, use `games_stats_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_stats_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_stats_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StatsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_stats_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1/stats
/// Returns engagement and spend statistics in this application for the currently authenticated user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_stats_get_builder()` + `games_stats_get_execute()`.
/// For task-level control, use `games_stats_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_stats_get(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StatsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_stats_get_builder(client)?;
    games_stats_get_execute(builder)
}
