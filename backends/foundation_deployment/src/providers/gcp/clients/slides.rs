//! Auto-generated API clients for gcp/slides.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/presentations/{presentationId}:batchUpdate
/// Applies one or more updates to the presentation. Each request is validated before being applied. If any request is not valid, then the entire request will fail and nothing will be applied. Some requests have replies to give you some information about how they are applied. Other requests do not need to return information; these each return an empty reply. The order of replies matches that of the requests. For example, suppose you call `batchUpdate` with four updates, and only the third one returns information. The response would have two empty replies: the reply to the third request, and another empty reply, in that order. Because other users may be editing the presentation, the presentation might not exactly reflect your changes: your changes may be altered with respect to collaborator changes. If there are no collaborators, the presentation should reflect your changes. In any case, the updates in your request are guaranteed to be applied together atomically.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `slides_presentations_batch_update_execute()` to send, or `slides_presentations_batch_update` for simplest API.

pub fn slides_presentations_batch_update_builder<R>(
    client: &SimpleHttpClient<R>,
    presentationId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://slides.googleapis.com/v1/presentations/{}:batchUpdate",
        presentationId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/presentations/{presentationId}:batchUpdate
/// Applies one or more updates to the presentation. Each request is validated before being applied. If any request is not valid, then the entire request will fail and nothing will be applied. Some requests have replies to give you some information about how they are applied. Other requests do not need to return information; these each return an empty reply. The order of replies matches that of the requests. For example, suppose you call `batchUpdate` with four updates, and only the third one returns information. The response would have two empty replies: the reply to the third request, and another empty reply, in that order. Because other users may be editing the presentation, the presentation might not exactly reflect your changes: your changes may be altered with respect to collaborator changes. If there are no collaborators, the presentation should reflect your changes. In any case, the updates in your request are guaranteed to be applied together atomically.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `slides_presentations_batch_update_execute()` or `slides_presentations_batch_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `slides_presentations_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_batch_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchUpdatePresentationResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdatePresentationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/presentations/{presentationId}:batchUpdate
/// Applies one or more updates to the presentation. Each request is validated before being applied. If any request is not valid, then the entire request will fail and nothing will be applied. Some requests have replies to give you some information about how they are applied. Other requests do not need to return information; these each return an empty reply. The order of replies matches that of the requests. For example, suppose you call `batchUpdate` with four updates, and only the third one returns information. The response would have two empty replies: the reply to the third request, and another empty reply, in that order. Because other users may be editing the presentation, the presentation might not exactly reflect your changes: your changes may be altered with respect to collaborator changes. If there are no collaborators, the presentation should reflect your changes. In any case, the updates in your request are guaranteed to be applied together atomically.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `slides_presentations_batch_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `slides_presentations_batch_update_task()`.
/// For the simplest API, use `slides_presentations_batch_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `slides_presentations_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn slides_presentations_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdatePresentationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = slides_presentations_batch_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`slides_presentations_batch_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SlidesPresentationsBatchUpdateArgs {
    /// Path parameter: presentationId
    pub presentationId: String,
}

/// POST v1/presentations/{presentationId}:batchUpdate
/// Applies one or more updates to the presentation. Each request is validated before being applied. If any request is not valid, then the entire request will fail and nothing will be applied. Some requests have replies to give you some information about how they are applied. Other requests do not need to return information; these each return an empty reply. The order of replies matches that of the requests. For example, suppose you call `batchUpdate` with four updates, and only the third one returns information. The response would have two empty replies: the reply to the third request, and another empty reply, in that order. Because other users may be editing the presentation, the presentation might not exactly reflect your changes: your changes may be altered with respect to collaborator changes. If there are no collaborators, the presentation should reflect your changes. In any case, the updates in your request are guaranteed to be applied together atomically.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `slides_presentations_batch_update_builder()` + `slides_presentations_batch_update_execute()`.
/// For task-level control, use `slides_presentations_batch_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_batch_update(
    client: &SimpleHttpClient,
    args: &SlidesPresentationsBatchUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdatePresentationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = slides_presentations_batch_update_builder(client, &args.presentationId)?;
    slides_presentations_batch_update_execute(builder)
}

/// POST v1/presentations
/// Creates a blank presentation using the title given in the request. If a `presentationId` is provided, it is used as the ID of the new presentation. Otherwise, a new ID is generated. Other fields in the request, including any provided content, are ignored. Returns the created presentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `slides_presentations_create_execute()` to send, or `slides_presentations_create` for simplest API.

pub fn slides_presentations_create_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://slides.googleapis.com/v1/presentations",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/presentations
/// Creates a blank presentation using the title given in the request. If a `presentationId` is provided, it is used as the ID of the new presentation. Otherwise, a new ID is generated. Other fields in the request, including any provided content, are ignored. Returns the created presentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `slides_presentations_create_execute()` or `slides_presentations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `slides_presentations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Presentation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Presentation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/presentations
/// Creates a blank presentation using the title given in the request. If a `presentationId` is provided, it is used as the ID of the new presentation. Otherwise, a new ID is generated. Other fields in the request, including any provided content, are ignored. Returns the created presentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `slides_presentations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `slides_presentations_create_task()`.
/// For the simplest API, use `slides_presentations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `slides_presentations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn slides_presentations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Presentation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = slides_presentations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/presentations
/// Creates a blank presentation using the title given in the request. If a `presentationId` is provided, it is used as the ID of the new presentation. Otherwise, a new ID is generated. Other fields in the request, including any provided content, are ignored. Returns the created presentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `slides_presentations_create_builder()` + `slides_presentations_create_execute()`.
/// For task-level control, use `slides_presentations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_create(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Presentation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = slides_presentations_create_builder(client)?;
    slides_presentations_create_execute(builder)
}

/// GET v1/presentations/{presentationsId}
/// Gets the latest version of the specified presentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `slides_presentations_get_execute()` to send, or `slides_presentations_get` for simplest API.

pub fn slides_presentations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    presentationId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://slides.googleapis.com/v1/presentations/{}",
        presentationId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/presentations/{presentationsId}
/// Gets the latest version of the specified presentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `slides_presentations_get_execute()` or `slides_presentations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `slides_presentations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Presentation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Presentation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/presentations/{presentationsId}
/// Gets the latest version of the specified presentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `slides_presentations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `slides_presentations_get_task()`.
/// For the simplest API, use `slides_presentations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `slides_presentations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn slides_presentations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Presentation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = slides_presentations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`slides_presentations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SlidesPresentationsGetArgs {
    /// Path parameter: presentationId
    pub presentationId: String,
}

/// GET v1/presentations/{presentationsId}
/// Gets the latest version of the specified presentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `slides_presentations_get_builder()` + `slides_presentations_get_execute()`.
/// For task-level control, use `slides_presentations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_get(
    client: &SimpleHttpClient,
    args: &SlidesPresentationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Presentation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = slides_presentations_get_builder(client, &args.presentationId)?;
    slides_presentations_get_execute(builder)
}

/// GET v1/presentations/{presentationId}/pages/{pageObjectId}
/// Gets the latest version of the specified page in the presentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `slides_presentations_pages_get_execute()` to send, or `slides_presentations_pages_get` for simplest API.

pub fn slides_presentations_pages_get_builder<R>(
    client: &SimpleHttpClient<R>,
    presentationId: &String,
    pageObjectId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://slides.googleapis.com/v1/presentations/{}/pages/{}",
        presentationId, pageObjectId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/presentations/{presentationId}/pages/{pageObjectId}
/// Gets the latest version of the specified page in the presentation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `slides_presentations_pages_get_execute()` or `slides_presentations_pages_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `slides_presentations_pages_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_pages_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Page>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/presentations/{presentationId}/pages/{pageObjectId}
/// Gets the latest version of the specified page in the presentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `slides_presentations_pages_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `slides_presentations_pages_get_task()`.
/// For the simplest API, use `slides_presentations_pages_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `slides_presentations_pages_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn slides_presentations_pages_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = slides_presentations_pages_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`slides_presentations_pages_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SlidesPresentationsPagesGetArgs {
    /// Path parameter: presentationId
    pub presentationId: String,
    /// Path parameter: pageObjectId
    pub pageObjectId: String,
}

/// GET v1/presentations/{presentationId}/pages/{pageObjectId}
/// Gets the latest version of the specified page in the presentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `slides_presentations_pages_get_builder()` + `slides_presentations_pages_get_execute()`.
/// For task-level control, use `slides_presentations_pages_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_pages_get(
    client: &SimpleHttpClient,
    args: &SlidesPresentationsPagesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        slides_presentations_pages_get_builder(client, &args.presentationId, &args.pageObjectId)?;
    slides_presentations_pages_get_execute(builder)
}

/// GET v1/presentations/{presentationId}/pages/{pageObjectId}/thumbnail
/// Generates a thumbnail of the latest version of the specified page in the presentation and returns a URL to the thumbnail image. This request counts as an [expensive read request](<https://developers.google.`com/workspace/slides/limits`>) for quota purposes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `slides_presentations_pages_get_thumbnail_execute()` to send, or `slides_presentations_pages_get_thumbnail` for simplest API.

pub fn slides_presentations_pages_get_thumbnail_builder<R>(
    client: &SimpleHttpClient<R>,
    presentationId: &String,
    pageObjectId: &String,
    thumbnailProperties_mimeType: &Option<Option<String>>,
    thumbnailProperties_thumbnailSize: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://slides.googleapis.com/v1/presentations/{}/pages/{}/thumbnail",
        presentationId, pageObjectId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = thumbnailProperties_mimeType.as_ref() {
        query_parts.push(format!("thumbnailProperties.mimeType={}", val));
    }
    if let Some(val) = thumbnailProperties_thumbnailSize.as_ref() {
        query_parts.push(format!("thumbnailProperties.thumbnailSize={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/presentations/{presentationId}/pages/{pageObjectId}/thumbnail
/// Generates a thumbnail of the latest version of the specified page in the presentation and returns a URL to the thumbnail image. This request counts as an [expensive read request](<https://developers.google.`com/workspace/slides/limits`>) for quota purposes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `slides_presentations_pages_get_thumbnail_execute()` or `slides_presentations_pages_get_thumbnail`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `slides_presentations_pages_get_thumbnail_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_pages_get_thumbnail_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Thumbnail>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Thumbnail = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/presentations/{presentationId}/pages/{pageObjectId}/thumbnail
/// Generates a thumbnail of the latest version of the specified page in the presentation and returns a URL to the thumbnail image. This request counts as an [expensive read request](<https://developers.google.`com/workspace/slides/limits`>) for quota purposes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `slides_presentations_pages_get_thumbnail_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `slides_presentations_pages_get_thumbnail_task()`.
/// For the simplest API, use `slides_presentations_pages_get_thumbnail()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `slides_presentations_pages_get_thumbnail_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn slides_presentations_pages_get_thumbnail_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Thumbnail>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = slides_presentations_pages_get_thumbnail_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`slides_presentations_pages_get_thumbnail`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SlidesPresentationsPagesGetThumbnailArgs {
    /// Path parameter: presentationId
    pub presentationId: String,
    /// Path parameter: pageObjectId
    pub pageObjectId: String,
    /// Query parameter: thumbnailProperties_mimeType
    pub thumbnailProperties_mimeType: Option<Option<String>>,
    /// Query parameter: thumbnailProperties_thumbnailSize
    pub thumbnailProperties_thumbnailSize: Option<Option<String>>,
}

/// GET v1/presentations/{presentationId}/pages/{pageObjectId}/thumbnail
/// Generates a thumbnail of the latest version of the specified page in the presentation and returns a URL to the thumbnail image. This request counts as an [expensive read request](<https://developers.google.`com/workspace/slides/limits`>) for quota purposes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `slides_presentations_pages_get_thumbnail_builder()` + `slides_presentations_pages_get_thumbnail_execute()`.
/// For task-level control, use `slides_presentations_pages_get_thumbnail_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_pages_get_thumbnail(
    client: &SimpleHttpClient,
    args: &SlidesPresentationsPagesGetThumbnailArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Thumbnail>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = slides_presentations_pages_get_thumbnail_builder(
        client,
        &args.presentationId,
        &args.pageObjectId,
        &args.thumbnailProperties_mimeType,
        &args.thumbnailProperties_thumbnailSize,
    )?;
    slides_presentations_pages_get_thumbnail_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for BatchUpdatePresentationResponse
// =============================================================================

/// ResourceIdentifier implementation for BatchUpdatePresentationResponse with SlidesPresentationsBatchUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SlidesPresentationsBatchUpdateArgs> for BatchUpdatePresentationResponse {
    fn generate_resource_id(&self, input: &SlidesPresentationsBatchUpdateArgs) -> String {
        format!(
            "gcp::slides::BatchUpdatePresentationResponse/{}",
            input.presentationId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::slides::BatchUpdatePresentationResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Presentation
// =============================================================================

/// ResourceIdentifier implementation for Presentation with SlidesPresentationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SlidesPresentationsCreateArgs> for Presentation {
    fn generate_resource_id(&self, input: &SlidesPresentationsCreateArgs) -> String {
        "gcp::slides::Presentation".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::slides::Presentation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Presentation
// =============================================================================

/// ResourceIdentifier implementation for Presentation with SlidesPresentationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SlidesPresentationsGetArgs> for Presentation {
    fn generate_resource_id(&self, input: &SlidesPresentationsGetArgs) -> String {
        format!("gcp::slides::Presentation/{}", input.presentationId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::slides::Presentation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Page
// =============================================================================

/// ResourceIdentifier implementation for Page with SlidesPresentationsPagesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SlidesPresentationsPagesGetArgs> for Page {
    fn generate_resource_id(&self, input: &SlidesPresentationsPagesGetArgs) -> String {
        format!(
            "gcp::slides::Page/{}/{}",
            input.presentationId, input.pageObjectId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::slides::Page"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Thumbnail
// =============================================================================

/// ResourceIdentifier implementation for Thumbnail with SlidesPresentationsPagesGetThumbnailArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SlidesPresentationsPagesGetThumbnailArgs> for Thumbnail {
    fn generate_resource_id(&self, input: &SlidesPresentationsPagesGetThumbnailArgs) -> String {
        format!(
            "gcp::slides::Thumbnail/{}/{}",
            input.presentationId, input.pageObjectId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::slides::Thumbnail"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
