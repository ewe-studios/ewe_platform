//! Auto-generated API clients for gcp/slides.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/slides`

#![cfg(feature = "gcp/slides")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/presentations/{presentationId}:batchUpdate
/// Applies one or more updates to the presentation. Each request is validated before being applied. If any request is not valid, then the entire request will fail and nothing will be applied. Some requests have replies to give you some information about how they are applied. Other requests do not need to return information; these each return an empty reply. The order of replies matches that of the requests. For example, suppose you call `batchUpdate` with four updates, and only the third one returns information. The response would have two empty replies: the reply to the third request, and another empty reply, in that order. Because other users may be editing the presentation, the presentation might not exactly reflect your changes: your changes may be altered with respect to collaborator changes. If there are no collaborators, the presentation should reflect your changes. In any case, the updates in your request are guaranteed to be applied together atomically.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `slides_presentations_batch_update_execute()` to send, or `slides_presentations_batch_update` for simplest API.

pub fn slides_presentations_batch_update_builder(
    client: &SimpleHttpClient,
    presentationId: &str,
    body: &BatchUpdatePresentationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://slides.googleapis.com/v1/presentations/{}:batchUpdate",
        presentationId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/presentations/{presentationId}:batchUpdate
/// Applies one or more updates to the presentation. Each request is validated before being applied. If any request is not valid, then the entire request will fail and nothing will be applied. Some requests have replies to give you some information about how they are applied. Other requests do not need to return information; these each return an empty reply. The order of replies matches that of the requests. For example, suppose you call `batchUpdate` with four updates, and only the third one returns information. The response would have two empty replies: the reply to the third request, and another empty reply, in that order. Because other users may be editing the presentation, the presentation might not exactly reflect your changes: your changes may be altered with respect to collaborator changes. If there are no collaborators, the presentation should reflect your changes. In any case, the updates in your request are guaranteed to be applied together atomically.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `slides_presentations_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from slides_presentations_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdatePresentationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdatePresentationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/presentations/{presentationId}:batchUpdate
/// Applies one or more updates to the presentation. Each request is validated before being applied. If any request is not valid, then the entire request will fail and nothing will be applied. Some requests have replies to give you some information about how they are applied. Other requests do not need to return information; these each return an empty reply. The order of replies matches that of the requests. For example, suppose you call `batchUpdate` with four updates, and only the third one returns information. The response would have two empty replies: the reply to the third request, and another empty reply, in that order. Because other users may be editing the presentation, the presentation might not exactly reflect your changes: your changes may be altered with respect to collaborator changes. If there are no collaborators, the presentation should reflect your changes. In any case, the updates in your request are guaranteed to be applied together atomically.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `slides_presentations_batch_update_builder()` + `slides_presentations_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_batch_update(
    client: &SimpleHttpClient,
    presentationId: &str,
    body: &BatchUpdatePresentationRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdatePresentationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = slides_presentations_batch_update_builder(client, presentationId, body)?;
    slides_presentations_batch_update_execute(builder)
}

/// GET v1/presentations
/// Creates a blank presentation using the title given in the request. If a `presentationId` is provided, it is used as the ID of the new presentation. Otherwise, a new ID is generated. Other fields in the request, including any provided content, are ignored. Returns the created presentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `slides_presentations_create_execute()` to send, or `slides_presentations_create` for simplest API.

pub fn slides_presentations_create_builder(
    client: &SimpleHttpClient,
    body: &Presentation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://slides.googleapis.com/v1/presentations",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/presentations
/// Creates a blank presentation using the title given in the request. If a `presentationId` is provided, it is used as the ID of the new presentation. Otherwise, a new ID is generated. Other fields in the request, including any provided content, are ignored. Returns the created presentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `slides_presentations_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from slides_presentations_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Presentation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Presentation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/presentations
/// Creates a blank presentation using the title given in the request. If a `presentationId` is provided, it is used as the ID of the new presentation. Otherwise, a new ID is generated. Other fields in the request, including any provided content, are ignored. Returns the created presentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `slides_presentations_create_builder()` + `slides_presentations_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_create(
    client: &SimpleHttpClient,
    body: &Presentation,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Presentation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = slides_presentations_create_builder(client, body)?;
    slides_presentations_create_execute(builder)
}

/// GET v1/presentations/{presentationsId}
/// Gets the latest version of the specified presentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `slides_presentations_get_execute()` to send, or `slides_presentations_get` for simplest API.

pub fn slides_presentations_get_builder(
    client: &SimpleHttpClient,
    presentationId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://slides.googleapis.com/v1/presentations/{}",
        presentationId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/presentations/{presentationsId}
/// Gets the latest version of the specified presentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `slides_presentations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from slides_presentations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Presentation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Presentation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/presentations/{presentationsId}
/// Gets the latest version of the specified presentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `slides_presentations_get_builder()` + `slides_presentations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_get(
    client: &SimpleHttpClient,
    presentationId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Presentation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = slides_presentations_get_builder(client, presentationId)?;
    slides_presentations_get_execute(builder)
}

/// GET v1/presentations/{presentationId}/pages/{pageObjectId}
/// Gets the latest version of the specified page in the presentation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `slides_presentations_pages_get_execute()` to send, or `slides_presentations_pages_get` for simplest API.

pub fn slides_presentations_pages_get_builder(
    client: &SimpleHttpClient,
    presentationId: &str,
    pageObjectId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://slides.googleapis.com/v1/presentations/{}/pages/{}",
        presentationId, pageObjectId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/presentations/{presentationId}/pages/{pageObjectId}
/// Gets the latest version of the specified page in the presentation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `slides_presentations_pages_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from slides_presentations_pages_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_pages_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/presentations/{presentationId}/pages/{pageObjectId}
/// Gets the latest version of the specified page in the presentation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `slides_presentations_pages_get_builder()` + `slides_presentations_pages_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_pages_get(
    client: &SimpleHttpClient,
    presentationId: &str,
    pageObjectId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Page>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = slides_presentations_pages_get_builder(client, presentationId, pageObjectId)?;
    slides_presentations_pages_get_execute(builder)
}

/// GET v1/presentations/{presentationId}/pages/{pageObjectId}/thumbnail
/// Generates a thumbnail of the latest version of the specified page in the presentation and returns a URL to the thumbnail image. This request counts as an [expensive read request](<https://developers.google.`com/workspace/slides/limits`>) for quota purposes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `slides_presentations_pages_get_thumbnail_execute()` to send, or `slides_presentations_pages_get_thumbnail` for simplest API.

pub fn slides_presentations_pages_get_thumbnail_builder(
    client: &SimpleHttpClient,
    presentationId: &str,
    pageObjectId: &str,
    thumbnailProperties_mimeType: Option<&str>,
    thumbnailProperties_thumbnailSize: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://slides.googleapis.com/v1/presentations/{}/pages/{}/thumbnail",
        presentationId, pageObjectId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = thumbnailProperties_mimeType {
        query_parts.push(format!("thumbnailProperties_mimeType={}", val));
    }
    if let Some(val) = thumbnailProperties_thumbnailSize {
        query_parts.push(format!("thumbnailProperties_thumbnailSize={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/presentations/{presentationId}/pages/{pageObjectId}/thumbnail
/// Generates a thumbnail of the latest version of the specified page in the presentation and returns a URL to the thumbnail image. This request counts as an [expensive read request](<https://developers.google.`com/workspace/slides/limits`>) for quota purposes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `slides_presentations_pages_get_thumbnail_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from slides_presentations_pages_get_thumbnail_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_pages_get_thumbnail_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Thumbnail>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Thumbnail = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/presentations/{presentationId}/pages/{pageObjectId}/thumbnail
/// Generates a thumbnail of the latest version of the specified page in the presentation and returns a URL to the thumbnail image. This request counts as an [expensive read request](<https://developers.google.`com/workspace/slides/limits`>) for quota purposes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `slides_presentations_pages_get_thumbnail_builder()` + `slides_presentations_pages_get_thumbnail_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn slides_presentations_pages_get_thumbnail(
    client: &SimpleHttpClient,
    presentationId: &str,
    pageObjectId: &str,
    thumbnailProperties_mimeType: Option<&str>,
    thumbnailProperties_thumbnailSize: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Thumbnail>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = slides_presentations_pages_get_thumbnail_builder(
        client,
        presentationId,
        pageObjectId,
        thumbnailProperties_mimeType,
        thumbnailProperties_thumbnailSize,
    )?;
    slides_presentations_pages_get_thumbnail_execute(builder)
}
