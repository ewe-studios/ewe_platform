//! Auto-generated API clients for gcp/drive.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET about
/// Gets information about the user, the user's Drive, and system capabilities. For more information, see [Return user info](<https://developers.google.`com/workspace/drive/api/guides/user-info`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_about_get_execute()` to send, or `drive_about_get` for simplest API.

pub fn drive_about_get_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comabout",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET about
/// Gets information about the user, the user's Drive, and system capabilities. For more information, see [Return user info](<https://developers.google.`com/workspace/drive/api/guides/user-info`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_about_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_about_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_about_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<About>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: About = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET about
/// Gets information about the user, the user's Drive, and system capabilities. For more information, see [Return user info](<https://developers.google.`com/workspace/drive/api/guides/user-info`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_about_get_builder()` + `drive_about_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_about_get(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<About>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_about_get_builder(client)?;
    drive_about_get_execute(builder)
}

/// GET files/{fileId}/accessproposals/{proposalId}
/// Retrieves an access proposal by ID. For more information, see [Manage pending access proposals](<https://developers.google.`com/workspace/drive/api/guides/pending-access`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_accessproposals_get_execute()` to send, or `drive_accessproposals_get` for simplest API.

pub fn drive_accessproposals_get_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    proposalId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/accessproposals/{}",
        fileId, proposalId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/accessproposals/{proposalId}
/// Retrieves an access proposal by ID. For more information, see [Manage pending access proposals](<https://developers.google.`com/workspace/drive/api/guides/pending-access`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_accessproposals_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_accessproposals_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_accessproposals_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccessProposal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AccessProposal = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/accessproposals/{proposalId}
/// Retrieves an access proposal by ID. For more information, see [Manage pending access proposals](<https://developers.google.`com/workspace/drive/api/guides/pending-access`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_accessproposals_get_builder()` + `drive_accessproposals_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_accessproposals_get(
    client: &SimpleHttpClient,
    fileId: &str,
    proposalId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AccessProposal>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = drive_accessproposals_get_builder(client, fileId, proposalId)?;
    drive_accessproposals_get_execute(builder)
}

/// GET files/{fileId}/accessproposals
/// List the access proposals on a file. For more information, see [Manage pending access proposals](<https://developers.google.`com/workspace/drive/api/guides/pending-access`>). Note: Only approvers are able to list access proposals on a file. If the user isn't an approver, a 403 error is returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_accessproposals_list_execute()` to send, or `drive_accessproposals_list` for simplest API.

pub fn drive_accessproposals_list_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}/accessproposals", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}/accessproposals
/// List the access proposals on a file. For more information, see [Manage pending access proposals](<https://developers.google.`com/workspace/drive/api/guides/pending-access`>). Note: Only approvers are able to list access proposals on a file. If the user isn't an approver, a 403 error is returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_accessproposals_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_accessproposals_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_accessproposals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAccessProposalsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAccessProposalsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/accessproposals
/// List the access proposals on a file. For more information, see [Manage pending access proposals](<https://developers.google.`com/workspace/drive/api/guides/pending-access`>). Note: Only approvers are able to list access proposals on a file. If the user isn't an approver, a 403 error is returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_accessproposals_list_builder()` + `drive_accessproposals_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_accessproposals_list(
    client: &SimpleHttpClient,
    fileId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAccessProposalsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drive_accessproposals_list_builder(client, fileId, pageSize, pageToken)?;
    drive_accessproposals_list_execute(builder)
}

/// GET files/{fileId}/accessproposals/{proposalId}:resolve
/// Approves or denies an access proposal. For more information, see [Manage pending access proposals](<https://developers.google.`com/workspace/drive/api/guides/pending-access`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_accessproposals_resolve_execute()` to send, or `drive_accessproposals_resolve` for simplest API.

pub fn drive_accessproposals_resolve_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    proposalId: &str,
    body: &ResolveAccessProposalRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/accessproposals/{}:resolve",
        fileId, proposalId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET files/{fileId}/accessproposals/{proposalId}:resolve
/// Approves or denies an access proposal. For more information, see [Manage pending access proposals](<https://developers.google.`com/workspace/drive/api/guides/pending-access`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_accessproposals_resolve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_accessproposals_resolve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_accessproposals_resolve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/accessproposals/{proposalId}:resolve
/// Approves or denies an access proposal. For more information, see [Manage pending access proposals](<https://developers.google.`com/workspace/drive/api/guides/pending-access`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_accessproposals_resolve_builder()` + `drive_accessproposals_resolve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_accessproposals_resolve(
    client: &SimpleHttpClient,
    fileId: &str,
    proposalId: &str,
    body: &ResolveAccessProposalRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_accessproposals_resolve_builder(client, fileId, proposalId, body)?;
    drive_accessproposals_resolve_execute(builder)
}

/// GET files/{fileId}/approvals/{approvalId}
/// Gets an Approval by ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_approvals_get_execute()` to send, or `drive_approvals_get` for simplest API.

pub fn drive_approvals_get_builder(
    client: &SimpleHttpClient,
    approvalId: &str,
    fileId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/approvals/{}",
        approvalId, fileId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/approvals/{approvalId}
/// Gets an Approval by ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_approvals_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_approvals_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_approvals_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Approval>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Approval = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/approvals/{approvalId}
/// Gets an Approval by ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_approvals_get_builder()` + `drive_approvals_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_approvals_get(
    client: &SimpleHttpClient,
    approvalId: &str,
    fileId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Approval>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_approvals_get_builder(client, approvalId, fileId)?;
    drive_approvals_get_execute(builder)
}

/// GET files/{fileId}/approvals
/// Lists the Approvals on a file.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_approvals_list_execute()` to send, or `drive_approvals_list` for simplest API.

pub fn drive_approvals_list_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}/approvals", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}/approvals
/// Lists the Approvals on a file.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_approvals_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_approvals_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_approvals_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApprovalList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApprovalList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/approvals
/// Lists the Approvals on a file.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_approvals_list_builder()` + `drive_approvals_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_approvals_list(
    client: &SimpleHttpClient,
    fileId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApprovalList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = drive_approvals_list_builder(client, fileId, pageSize, pageToken)?;
    drive_approvals_list_execute(builder)
}

/// GET apps/{appId}
/// Gets a specific app. For more information, see [Return user info](<https://developers.google.`com/workspace/drive/api/guides/user-info`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_apps_get_execute()` to send, or `drive_apps_get` for simplest API.

pub fn drive_apps_get_builder(
    client: &SimpleHttpClient,
    appId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comapps/{}", appId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apps/{appId}
/// Gets a specific app. For more information, see [Return user info](<https://developers.google.`com/workspace/drive/api/guides/user-info`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_apps_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_apps_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_apps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<App>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: App = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apps/{appId}
/// Gets a specific app. For more information, see [Return user info](<https://developers.google.`com/workspace/drive/api/guides/user-info`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_apps_get_builder()` + `drive_apps_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_apps_get(
    client: &SimpleHttpClient,
    appId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<App>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_apps_get_builder(client, appId)?;
    drive_apps_get_execute(builder)
}

/// GET apps
/// Lists a user's installed apps. For more information, see [Return user info](<https://developers.google.`com/workspace/drive/api/guides/user-info`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_apps_list_execute()` to send, or `drive_apps_list` for simplest API.

pub fn drive_apps_list_builder(
    client: &SimpleHttpClient,
    appFilterExtensions: Option<&str>,
    appFilterMimeTypes: Option<&str>,
    languageCode: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comapps",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "appFilterExtensions",
            appFilterExtensions
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "appFilterMimeTypes",
            appFilterMimeTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "languageCode",
            languageCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET apps
/// Lists a user's installed apps. For more information, see [Return user info](<https://developers.google.`com/workspace/drive/api/guides/user-info`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_apps_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_apps_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_apps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apps
/// Lists a user's installed apps. For more information, see [Return user info](<https://developers.google.`com/workspace/drive/api/guides/user-info`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_apps_list_builder()` + `drive_apps_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_apps_list(
    client: &SimpleHttpClient,
    appFilterExtensions: Option<&str>,
    appFilterMimeTypes: Option<&str>,
    languageCode: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_apps_list_builder(
        client,
        appFilterExtensions,
        appFilterMimeTypes,
        languageCode,
    )?;
    drive_apps_list_execute(builder)
}

/// GET changes/startPageToken
/// Gets the starting `pageToken` for listing future changes. For more information, see [Retrieve changes](<https://developers.google.`com/workspace/drive/api/guides/manage-changes`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_changes_get_start_page_token_execute()` to send, or `drive_changes_get_start_page_token` for simplest API.

pub fn drive_changes_get_start_page_token_builder(
    client: &SimpleHttpClient,
    driveId: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    teamDriveId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comchanges/startPageToken",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "driveId",
            driveId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "teamDriveId",
            teamDriveId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET changes/startPageToken
/// Gets the starting `pageToken` for listing future changes. For more information, see [Retrieve changes](<https://developers.google.`com/workspace/drive/api/guides/manage-changes`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_changes_get_start_page_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_changes_get_start_page_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_changes_get_start_page_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StartPageToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StartPageToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET changes/startPageToken
/// Gets the starting `pageToken` for listing future changes. For more information, see [Retrieve changes](<https://developers.google.`com/workspace/drive/api/guides/manage-changes`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_changes_get_start_page_token_builder()` + `drive_changes_get_start_page_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_changes_get_start_page_token(
    client: &SimpleHttpClient,
    driveId: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    teamDriveId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StartPageToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = drive_changes_get_start_page_token_builder(
        client,
        driveId,
        supportsAllDrives,
        supportsTeamDrives,
        teamDriveId,
    )?;
    drive_changes_get_start_page_token_execute(builder)
}

/// GET changes
/// Lists the changes for a user or shared drive. For more information, see [Retrieve changes](<https://developers.google.`com/workspace/drive/api/guides/manage-changes`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_changes_list_execute()` to send, or `drive_changes_list` for simplest API.

pub fn drive_changes_list_builder(
    client: &SimpleHttpClient,
    driveId: Option<&str>,
    includeCorpusRemovals: Option<bool>,
    includeItemsFromAllDrives: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    includeRemoved: Option<bool>,
    includeTeamDriveItems: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    restrictToMyDrive: Option<bool>,
    spaces: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    teamDriveId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comchanges",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "driveId",
            driveId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeCorpusRemovals",
            includeCorpusRemovals
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeItemsFromAllDrives",
            includeItemsFromAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeLabels",
            includeLabels
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includePermissionsForView",
            includePermissionsForView
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeRemoved",
            includeRemoved
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeTeamDriveItems",
            includeTeamDriveItems
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "restrictToMyDrive",
            restrictToMyDrive
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "spaces",
            spaces
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "teamDriveId",
            teamDriveId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET changes
/// Lists the changes for a user or shared drive. For more information, see [Retrieve changes](<https://developers.google.`com/workspace/drive/api/guides/manage-changes`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_changes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_changes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_changes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChangeList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ChangeList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET changes
/// Lists the changes for a user or shared drive. For more information, see [Retrieve changes](<https://developers.google.`com/workspace/drive/api/guides/manage-changes`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_changes_list_builder()` + `drive_changes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_changes_list(
    client: &SimpleHttpClient,
    driveId: Option<&str>,
    includeCorpusRemovals: Option<bool>,
    includeItemsFromAllDrives: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    includeRemoved: Option<bool>,
    includeTeamDriveItems: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    restrictToMyDrive: Option<bool>,
    spaces: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    teamDriveId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChangeList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_changes_list_builder(
        client,
        driveId,
        includeCorpusRemovals,
        includeItemsFromAllDrives,
        includeLabels,
        includePermissionsForView,
        includeRemoved,
        includeTeamDriveItems,
        pageSize,
        pageToken,
        restrictToMyDrive,
        spaces,
        supportsAllDrives,
        supportsTeamDrives,
        teamDriveId,
    )?;
    drive_changes_list_execute(builder)
}

/// GET changes/watch
/// Subscribes to changes for a user. For more information, see [Notifications for resource changes](<https://developers.google.`com/workspace/drive/api/guides/push`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_changes_watch_execute()` to send, or `drive_changes_watch` for simplest API.

pub fn drive_changes_watch_builder(
    client: &SimpleHttpClient,
    driveId: Option<&str>,
    includeCorpusRemovals: Option<bool>,
    includeItemsFromAllDrives: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    includeRemoved: Option<bool>,
    includeTeamDriveItems: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    restrictToMyDrive: Option<bool>,
    spaces: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    teamDriveId: Option<&str>,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comchanges/watch",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "driveId",
            driveId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeCorpusRemovals",
            includeCorpusRemovals
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeItemsFromAllDrives",
            includeItemsFromAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeLabels",
            includeLabels
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includePermissionsForView",
            includePermissionsForView
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeRemoved",
            includeRemoved
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeTeamDriveItems",
            includeTeamDriveItems
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "restrictToMyDrive",
            restrictToMyDrive
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "spaces",
            spaces
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "teamDriveId",
            teamDriveId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET changes/watch
/// Subscribes to changes for a user. For more information, see [Notifications for resource changes](<https://developers.google.`com/workspace/drive/api/guides/push`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_changes_watch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_changes_watch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_changes_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET changes/watch
/// Subscribes to changes for a user. For more information, see [Notifications for resource changes](<https://developers.google.`com/workspace/drive/api/guides/push`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_changes_watch_builder()` + `drive_changes_watch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_changes_watch(
    client: &SimpleHttpClient,
    driveId: Option<&str>,
    includeCorpusRemovals: Option<bool>,
    includeItemsFromAllDrives: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    includeRemoved: Option<bool>,
    includeTeamDriveItems: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    restrictToMyDrive: Option<bool>,
    spaces: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    teamDriveId: Option<&str>,
    body: &Channel,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_changes_watch_builder(
        client,
        driveId,
        includeCorpusRemovals,
        includeItemsFromAllDrives,
        includeLabels,
        includePermissionsForView,
        includeRemoved,
        includeTeamDriveItems,
        pageSize,
        pageToken,
        restrictToMyDrive,
        spaces,
        supportsAllDrives,
        supportsTeamDrives,
        teamDriveId,
        body,
    )?;
    drive_changes_watch_execute(builder)
}

/// GET channels/stop
/// Stops watching resources through this channel. For more information, see [Notifications for resource changes](<https://developers.google.`com/workspace/drive/api/guides/push`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_channels_stop_execute()` to send, or `drive_channels_stop` for simplest API.

pub fn drive_channels_stop_builder(
    client: &SimpleHttpClient,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comchannels/stop",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET channels/stop
/// Stops watching resources through this channel. For more information, see [Notifications for resource changes](<https://developers.google.`com/workspace/drive/api/guides/push`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_channels_stop_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_channels_stop_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_channels_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET channels/stop
/// Stops watching resources through this channel. For more information, see [Notifications for resource changes](<https://developers.google.`com/workspace/drive/api/guides/push`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_channels_stop_builder()` + `drive_channels_stop_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_channels_stop(
    client: &SimpleHttpClient,
    body: &Channel,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_channels_stop_builder(client, body)?;
    drive_channels_stop_execute(builder)
}

/// GET files/{fileId}/comments
/// Creates a comment on a file. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_comments_create_execute()` to send, or `drive_comments_create` for simplest API.

pub fn drive_comments_create_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    body: &Comment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}/comments", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET files/{fileId}/comments
/// Creates a comment on a file. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_comments_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_comments_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_comments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Comment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/comments
/// Creates a comment on a file. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_comments_create_builder()` + `drive_comments_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_comments_create(
    client: &SimpleHttpClient,
    fileId: &str,
    body: &Comment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_comments_create_builder(client, fileId, body)?;
    drive_comments_create_execute(builder)
}

/// GET files/{fileId}/comments/{commentId}
/// Deletes a comment. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_comments_delete_execute()` to send, or `drive_comments_delete` for simplest API.

pub fn drive_comments_delete_builder(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/comments/{}",
        commentId, fileId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/comments/{commentId}
/// Deletes a comment. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_comments_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_comments_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_comments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/comments/{commentId}
/// Deletes a comment. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_comments_delete_builder()` + `drive_comments_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_comments_delete(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_comments_delete_builder(client, commentId, fileId)?;
    drive_comments_delete_execute(builder)
}

/// GET files/{fileId}/comments/{commentId}
/// Gets a comment by ID. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_comments_get_execute()` to send, or `drive_comments_get` for simplest API.

pub fn drive_comments_get_builder(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
    includeDeleted: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/comments/{}",
        commentId, fileId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "includeDeleted",
            includeDeleted
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}/comments/{commentId}
/// Gets a comment by ID. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_comments_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_comments_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_comments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Comment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/comments/{commentId}
/// Gets a comment by ID. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_comments_get_builder()` + `drive_comments_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_comments_get(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
    includeDeleted: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_comments_get_builder(client, commentId, fileId, includeDeleted)?;
    drive_comments_get_execute(builder)
}

/// GET files/{fileId}/comments
/// Lists a file's comments. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_comments_list_execute()` to send, or `drive_comments_list` for simplest API.

pub fn drive_comments_list_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    includeDeleted: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    startModifiedTime: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}/comments", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "includeDeleted",
            includeDeleted
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startModifiedTime",
            startModifiedTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}/comments
/// Lists a file's comments. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_comments_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_comments_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_comments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CommentList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/comments
/// Lists a file's comments. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_comments_list_builder()` + `drive_comments_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_comments_list(
    client: &SimpleHttpClient,
    fileId: &str,
    includeDeleted: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    startModifiedTime: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CommentList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_comments_list_builder(
        client,
        fileId,
        includeDeleted,
        pageSize,
        pageToken,
        startModifiedTime,
    )?;
    drive_comments_list_execute(builder)
}

/// GET files/{fileId}/comments/{commentId}
/// Updates a comment with patch semantics. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_comments_update_execute()` to send, or `drive_comments_update` for simplest API.

pub fn drive_comments_update_builder(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
    body: &Comment,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/comments/{}",
        commentId, fileId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET files/{fileId}/comments/{commentId}
/// Updates a comment with patch semantics. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_comments_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_comments_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_comments_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Comment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/comments/{commentId}
/// Updates a comment with patch semantics. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>). Required: The fields parameter must be set. To return the exact fields you need, see [Return specific fields](<https://developers.google.`com/workspace/drive/api/guides/fields-parameter`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_comments_update_builder()` + `drive_comments_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_comments_update(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
    body: &Comment,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Comment>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_comments_update_builder(client, commentId, fileId, body)?;
    drive_comments_update_execute(builder)
}

/// GET drives
/// Creates a shared drive. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_drives_create_execute()` to send, or `drive_drives_create` for simplest API.

pub fn drive_drives_create_builder(
    client: &SimpleHttpClient,
    requestId: Option<&str>,
    body: &Drive,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdrives",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET drives
/// Creates a shared drive. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_drives_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_drives_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_drives_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Drive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Drive = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET drives
/// Creates a shared drive. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_drives_create_builder()` + `drive_drives_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_drives_create(
    client: &SimpleHttpClient,
    requestId: Option<&str>,
    body: &Drive,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Drive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_drives_create_builder(client, requestId, body)?;
    drive_drives_create_execute(builder)
}

/// GET drives/{driveId}
/// Permanently deletes a shared drive for which the user is an organizer. The shared drive cannot contain any untrashed items. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_drives_delete_execute()` to send, or `drive_drives_delete` for simplest API.

pub fn drive_drives_delete_builder(
    client: &SimpleHttpClient,
    driveId: &str,
    allowItemDeletion: Option<bool>,
    useDomainAdminAccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdrives/{}", driveId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "allowItemDeletion",
            allowItemDeletion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useDomainAdminAccess",
            useDomainAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET drives/{driveId}
/// Permanently deletes a shared drive for which the user is an organizer. The shared drive cannot contain any untrashed items. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_drives_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_drives_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_drives_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET drives/{driveId}
/// Permanently deletes a shared drive for which the user is an organizer. The shared drive cannot contain any untrashed items. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_drives_delete_builder()` + `drive_drives_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_drives_delete(
    client: &SimpleHttpClient,
    driveId: &str,
    allowItemDeletion: Option<bool>,
    useDomainAdminAccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        drive_drives_delete_builder(client, driveId, allowItemDeletion, useDomainAdminAccess)?;
    drive_drives_delete_execute(builder)
}

/// GET drives/{driveId}
/// Gets a shared drive's metadata by ID. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_drives_get_execute()` to send, or `drive_drives_get` for simplest API.

pub fn drive_drives_get_builder(
    client: &SimpleHttpClient,
    driveId: &str,
    useDomainAdminAccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdrives/{}", driveId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "useDomainAdminAccess",
            useDomainAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET drives/{driveId}
/// Gets a shared drive's metadata by ID. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_drives_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_drives_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_drives_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Drive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Drive = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET drives/{driveId}
/// Gets a shared drive's metadata by ID. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_drives_get_builder()` + `drive_drives_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_drives_get(
    client: &SimpleHttpClient,
    driveId: &str,
    useDomainAdminAccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Drive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_drives_get_builder(client, driveId, useDomainAdminAccess)?;
    drive_drives_get_execute(builder)
}

/// GET drives/{driveId}/hide
/// Hides a shared drive from the default view. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_drives_hide_execute()` to send, or `drive_drives_hide` for simplest API.

pub fn drive_drives_hide_builder(
    client: &SimpleHttpClient,
    driveId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdrives/{}/hide", driveId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET drives/{driveId}/hide
/// Hides a shared drive from the default view. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_drives_hide_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_drives_hide_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_drives_hide_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Drive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Drive = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET drives/{driveId}/hide
/// Hides a shared drive from the default view. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_drives_hide_builder()` + `drive_drives_hide_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_drives_hide(
    client: &SimpleHttpClient,
    driveId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Drive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_drives_hide_builder(client, driveId)?;
    drive_drives_hide_execute(builder)
}

/// GET drives
/// Lists the user's shared drives. This method accepts the q parameter, which is a search query combining one or more search terms. For more information, see the [Search for shared drives](<https://developers.google.`com/workspace/drive/api/guides/search-shareddrives`>) guide.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_drives_list_execute()` to send, or `drive_drives_list` for simplest API.

pub fn drive_drives_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    q: Option<&str>,
    useDomainAdminAccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdrives",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "q",
            q.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useDomainAdminAccess",
            useDomainAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET drives
/// Lists the user's shared drives. This method accepts the q parameter, which is a search query combining one or more search terms. For more information, see the [Search for shared drives](<https://developers.google.`com/workspace/drive/api/guides/search-shareddrives`>) guide.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_drives_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_drives_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_drives_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DriveList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DriveList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET drives
/// Lists the user's shared drives. This method accepts the q parameter, which is a search query combining one or more search terms. For more information, see the [Search for shared drives](<https://developers.google.`com/workspace/drive/api/guides/search-shareddrives`>) guide.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_drives_list_builder()` + `drive_drives_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_drives_list(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    q: Option<&str>,
    useDomainAdminAccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DriveList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_drives_list_builder(client, pageSize, pageToken, q, useDomainAdminAccess)?;
    drive_drives_list_execute(builder)
}

/// GET drives/{driveId}/unhide
/// Restores a shared drive to the default view. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_drives_unhide_execute()` to send, or `drive_drives_unhide` for simplest API.

pub fn drive_drives_unhide_builder(
    client: &SimpleHttpClient,
    driveId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdrives/{}/unhide", driveId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET drives/{driveId}/unhide
/// Restores a shared drive to the default view. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_drives_unhide_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_drives_unhide_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_drives_unhide_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Drive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Drive = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET drives/{driveId}/unhide
/// Restores a shared drive to the default view. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_drives_unhide_builder()` + `drive_drives_unhide_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_drives_unhide(
    client: &SimpleHttpClient,
    driveId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Drive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_drives_unhide_builder(client, driveId)?;
    drive_drives_unhide_execute(builder)
}

/// GET drives/{driveId}
/// Updates the metadata for a shared drive. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_drives_update_execute()` to send, or `drive_drives_update` for simplest API.

pub fn drive_drives_update_builder(
    client: &SimpleHttpClient,
    driveId: &str,
    useDomainAdminAccess: Option<bool>,
    body: &Drive,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdrives/{}", driveId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "useDomainAdminAccess",
            useDomainAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET drives/{driveId}
/// Updates the metadata for a shared drive. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_drives_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_drives_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_drives_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Drive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Drive = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET drives/{driveId}
/// Updates the metadata for a shared drive. For more information, see [Manage shared drives](<https://developers.google.`com/workspace/drive/api/guides/manage-shareddrives`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_drives_update_builder()` + `drive_drives_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_drives_update(
    client: &SimpleHttpClient,
    driveId: &str,
    useDomainAdminAccess: Option<bool>,
    body: &Drive,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Drive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_drives_update_builder(client, driveId, useDomainAdminAccess, body)?;
    drive_drives_update_execute(builder)
}

/// GET files/{fileId}/copy
/// Creates a copy of a file and applies any requested updates with patch semantics. For more information, see [Create and manage files](<https://developers.google.`com/workspace/drive/api/guides/create-file`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_files_copy_execute()` to send, or `drive_files_copy` for simplest API.

pub fn drive_files_copy_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    enforceSingleParent: Option<bool>,
    ignoreDefaultVisibility: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    keepRevisionForever: Option<bool>,
    ocrLanguage: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    body: &File,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}/copy", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "enforceSingleParent",
            enforceSingleParent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "ignoreDefaultVisibility",
            ignoreDefaultVisibility
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeLabels",
            includeLabels
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includePermissionsForView",
            includePermissionsForView
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "keepRevisionForever",
            keepRevisionForever
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "ocrLanguage",
            ocrLanguage
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET files/{fileId}/copy
/// Creates a copy of a file and applies any requested updates with patch semantics. For more information, see [Create and manage files](<https://developers.google.`com/workspace/drive/api/guides/create-file`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_files_copy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_files_copy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_copy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: File = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/copy
/// Creates a copy of a file and applies any requested updates with patch semantics. For more information, see [Create and manage files](<https://developers.google.`com/workspace/drive/api/guides/create-file`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_files_copy_builder()` + `drive_files_copy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_copy(
    client: &SimpleHttpClient,
    fileId: &str,
    enforceSingleParent: Option<bool>,
    ignoreDefaultVisibility: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    keepRevisionForever: Option<bool>,
    ocrLanguage: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    body: &File,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_files_copy_builder(
        client,
        fileId,
        enforceSingleParent,
        ignoreDefaultVisibility,
        includeLabels,
        includePermissionsForView,
        keepRevisionForever,
        ocrLanguage,
        supportsAllDrives,
        supportsTeamDrives,
        body,
    )?;
    drive_files_copy_execute(builder)
}

/// GET files
/// Creates a file. For more information, see [Create and manage files](<https://developers.google.`com/workspace/drive/api/guides/create-file`>). This method supports an */upload* URI and accepts uploaded media with the following characteristics: - *Maximum file size:* 5,120 GB - *Accepted Media MIME types:* */* (Specify a valid MIME type, rather than the literal */* value. The literal */* is only used to indicate that any valid MIME type can be uploaded. For more information, see [Google Workspace and Google Drive supported MIME types](<https://developers.google.`com/workspace/drive/api/guides/mime-types`>).) For more information on uploading files, see [Upload file data](<https://developers.google.`com/workspace/drive/api/guides/manage-uploads`>). Apps creating shortcuts with the create method must specify the MIME type `application/vnd`.google-apps.shortcut. Apps should specify a file extension in the name property when inserting files with the API. For example, an operation to insert a JPEG file should specify something like "name": "cat.jpg" in the metadata. Subsequent GET requests include the read-only `fileExtension` property populated with the extension originally specified in the name property. When a Google Drive user requests to download a file, or when the file is downloaded through the sync client, Drive builds a full filename (with extension) based on the name. In cases where the extension is missing, Drive attempts to determine the extension based on the file's MIME type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_files_create_execute()` to send, or `drive_files_create` for simplest API.

pub fn drive_files_create_builder(
    client: &SimpleHttpClient,
    enforceSingleParent: Option<bool>,
    ignoreDefaultVisibility: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    keepRevisionForever: Option<bool>,
    ocrLanguage: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    useContentAsIndexableText: Option<bool>,
    body: &File,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "enforceSingleParent",
            enforceSingleParent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "ignoreDefaultVisibility",
            ignoreDefaultVisibility
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeLabels",
            includeLabels
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includePermissionsForView",
            includePermissionsForView
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "keepRevisionForever",
            keepRevisionForever
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "ocrLanguage",
            ocrLanguage
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useContentAsIndexableText",
            useContentAsIndexableText
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET files
/// Creates a file. For more information, see [Create and manage files](<https://developers.google.`com/workspace/drive/api/guides/create-file`>). This method supports an */upload* URI and accepts uploaded media with the following characteristics: - *Maximum file size:* 5,120 GB - *Accepted Media MIME types:* */* (Specify a valid MIME type, rather than the literal */* value. The literal */* is only used to indicate that any valid MIME type can be uploaded. For more information, see [Google Workspace and Google Drive supported MIME types](<https://developers.google.`com/workspace/drive/api/guides/mime-types`>).) For more information on uploading files, see [Upload file data](<https://developers.google.`com/workspace/drive/api/guides/manage-uploads`>). Apps creating shortcuts with the create method must specify the MIME type `application/vnd`.google-apps.shortcut. Apps should specify a file extension in the name property when inserting files with the API. For example, an operation to insert a JPEG file should specify something like "name": "cat.jpg" in the metadata. Subsequent GET requests include the read-only `fileExtension` property populated with the extension originally specified in the name property. When a Google Drive user requests to download a file, or when the file is downloaded through the sync client, Drive builds a full filename (with extension) based on the name. In cases where the extension is missing, Drive attempts to determine the extension based on the file's MIME type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_files_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_files_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: File = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files
/// Creates a file. For more information, see [Create and manage files](<https://developers.google.`com/workspace/drive/api/guides/create-file`>). This method supports an */upload* URI and accepts uploaded media with the following characteristics: - *Maximum file size:* 5,120 GB - *Accepted Media MIME types:* */* (Specify a valid MIME type, rather than the literal */* value. The literal */* is only used to indicate that any valid MIME type can be uploaded. For more information, see [Google Workspace and Google Drive supported MIME types](<https://developers.google.`com/workspace/drive/api/guides/mime-types`>).) For more information on uploading files, see [Upload file data](<https://developers.google.`com/workspace/drive/api/guides/manage-uploads`>). Apps creating shortcuts with the create method must specify the MIME type `application/vnd`.google-apps.shortcut. Apps should specify a file extension in the name property when inserting files with the API. For example, an operation to insert a JPEG file should specify something like "name": "cat.jpg" in the metadata. Subsequent GET requests include the read-only `fileExtension` property populated with the extension originally specified in the name property. When a Google Drive user requests to download a file, or when the file is downloaded through the sync client, Drive builds a full filename (with extension) based on the name. In cases where the extension is missing, Drive attempts to determine the extension based on the file's MIME type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_files_create_builder()` + `drive_files_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_create(
    client: &SimpleHttpClient,
    enforceSingleParent: Option<bool>,
    ignoreDefaultVisibility: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    keepRevisionForever: Option<bool>,
    ocrLanguage: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    useContentAsIndexableText: Option<bool>,
    body: &File,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_files_create_builder(
        client,
        enforceSingleParent,
        ignoreDefaultVisibility,
        includeLabels,
        includePermissionsForView,
        keepRevisionForever,
        ocrLanguage,
        supportsAllDrives,
        supportsTeamDrives,
        useContentAsIndexableText,
        body,
    )?;
    drive_files_create_execute(builder)
}

/// GET files/{fileId}
/// Permanently deletes a file owned by the user without moving it to the trash. For more information, see [Trash or delete files and folders](<https://developers.google.`com/workspace/drive/api/guides/delete`>). If the file belongs to a shared drive, the user must be an organizer on the parent folder. If the target is a folder, all descendants owned by the user are also deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_files_delete_execute()` to send, or `drive_files_delete` for simplest API.

pub fn drive_files_delete_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    enforceSingleParent: Option<bool>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "enforceSingleParent",
            enforceSingleParent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}
/// Permanently deletes a file owned by the user without moving it to the trash. For more information, see [Trash or delete files and folders](<https://developers.google.`com/workspace/drive/api/guides/delete`>). If the file belongs to a shared drive, the user must be an organizer on the parent folder. If the target is a folder, all descendants owned by the user are also deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_files_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_files_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}
/// Permanently deletes a file owned by the user without moving it to the trash. For more information, see [Trash or delete files and folders](<https://developers.google.`com/workspace/drive/api/guides/delete`>). If the file belongs to a shared drive, the user must be an organizer on the parent folder. If the target is a folder, all descendants owned by the user are also deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_files_delete_builder()` + `drive_files_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_delete(
    client: &SimpleHttpClient,
    fileId: &str,
    enforceSingleParent: Option<bool>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_files_delete_builder(
        client,
        fileId,
        enforceSingleParent,
        supportsAllDrives,
        supportsTeamDrives,
    )?;
    drive_files_delete_execute(builder)
}

/// GET files/{fileId}/download
/// Downloads the content of a file. For more information, see [Download and export files](<https://developers.google.`com/workspace/drive/api/guides/manage-downloads`>). Operations are valid for 24 hours from the time of creation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_files_download_execute()` to send, or `drive_files_download` for simplest API.

pub fn drive_files_download_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    mimeType: Option<&str>,
    revisionId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}/download", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "mimeType",
            mimeType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "revisionId",
            revisionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}/download
/// Downloads the content of a file. For more information, see [Download and export files](<https://developers.google.`com/workspace/drive/api/guides/manage-downloads`>). Operations are valid for 24 hours from the time of creation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_files_download_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_files_download_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/download
/// Downloads the content of a file. For more information, see [Download and export files](<https://developers.google.`com/workspace/drive/api/guides/manage-downloads`>). Operations are valid for 24 hours from the time of creation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_files_download_builder()` + `drive_files_download_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_download(
    client: &SimpleHttpClient,
    fileId: &str,
    mimeType: Option<&str>,
    revisionId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_files_download_builder(client, fileId, mimeType, revisionId)?;
    drive_files_download_execute(builder)
}

/// GET files/trash
/// Permanently deletes all of the user's trashed files. For more information, see [Trash or delete files and folders](<https://developers.google.`com/workspace/drive/api/guides/delete`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_files_empty_trash_execute()` to send, or `drive_files_empty_trash` for simplest API.

pub fn drive_files_empty_trash_builder(
    client: &SimpleHttpClient,
    driveId: Option<&str>,
    enforceSingleParent: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/trash",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "driveId",
            driveId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "enforceSingleParent",
            enforceSingleParent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/trash
/// Permanently deletes all of the user's trashed files. For more information, see [Trash or delete files and folders](<https://developers.google.`com/workspace/drive/api/guides/delete`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_files_empty_trash_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_files_empty_trash_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_empty_trash_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/trash
/// Permanently deletes all of the user's trashed files. For more information, see [Trash or delete files and folders](<https://developers.google.`com/workspace/drive/api/guides/delete`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_files_empty_trash_builder()` + `drive_files_empty_trash_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_empty_trash(
    client: &SimpleHttpClient,
    driveId: Option<&str>,
    enforceSingleParent: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_files_empty_trash_builder(client, driveId, enforceSingleParent)?;
    drive_files_empty_trash_execute(builder)
}

/// GET files/{fileId}/export
/// Exports a Google Workspace document to the requested MIME type and returns exported byte content. For more information, see [Download and export files](<https://developers.google.`com/workspace/drive/api/guides/manage-downloads`>). Note that the exported content is limited to 10 MB.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_files_export_execute()` to send, or `drive_files_export` for simplest API.

pub fn drive_files_export_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    mimeType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}/export", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "mimeType",
            mimeType
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}/export
/// Exports a Google Workspace document to the requested MIME type and returns exported byte content. For more information, see [Download and export files](<https://developers.google.`com/workspace/drive/api/guides/manage-downloads`>). Note that the exported content is limited to 10 MB.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_files_export_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_files_export_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/export
/// Exports a Google Workspace document to the requested MIME type and returns exported byte content. For more information, see [Download and export files](<https://developers.google.`com/workspace/drive/api/guides/manage-downloads`>). Note that the exported content is limited to 10 MB.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_files_export_builder()` + `drive_files_export_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_export(
    client: &SimpleHttpClient,
    fileId: &str,
    mimeType: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_files_export_builder(client, fileId, mimeType)?;
    drive_files_export_execute(builder)
}

/// GET files/generateIds
/// Generates a set of file IDs which can be provided in create or copy requests. For more information, see [Create and manage files](<https://developers.google.`com/workspace/drive/api/guides/create-file`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_files_generate_ids_execute()` to send, or `drive_files_generate_ids` for simplest API.

pub fn drive_files_generate_ids_builder(
    client: &SimpleHttpClient,
    count: Option<i32>,
    space: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/generateIds",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "count",
            count
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "space",
            space
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/generateIds
/// Generates a set of file IDs which can be provided in create or copy requests. For more information, see [Create and manage files](<https://developers.google.`com/workspace/drive/api/guides/create-file`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_files_generate_ids_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_files_generate_ids_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_generate_ids_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GeneratedIds>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GeneratedIds = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/generateIds
/// Generates a set of file IDs which can be provided in create or copy requests. For more information, see [Create and manage files](<https://developers.google.`com/workspace/drive/api/guides/create-file`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_files_generate_ids_builder()` + `drive_files_generate_ids_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_generate_ids(
    client: &SimpleHttpClient,
    count: Option<i32>,
    space: Option<&str>,
    type_rs: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GeneratedIds>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = drive_files_generate_ids_builder(client, count, space, type_rs)?;
    drive_files_generate_ids_execute(builder)
}

/// GET files/{fileId}
/// Gets a file's metadata or content by ID. For more information, see [Search for files and folders](<https://developers.google.`com/workspace/drive/api/guides/search-files`>). If you provide the URL parameter alt=media, then the response includes the file contents in the response body. Downloading content with alt=media only works if the file is stored in Drive. To download Google Docs, Sheets, and Slides use [files.export](<https://developers.google.`com/workspace/drive/api/reference/rest/v3/files/export`>) instead. For more information, see [Download and export files](<https://developers.google.`com/workspace/drive/api/guides/manage-downloads`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_files_get_execute()` to send, or `drive_files_get` for simplest API.

pub fn drive_files_get_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    acknowledgeAbuse: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "acknowledgeAbuse",
            acknowledgeAbuse
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeLabels",
            includeLabels
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includePermissionsForView",
            includePermissionsForView
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}
/// Gets a file's metadata or content by ID. For more information, see [Search for files and folders](<https://developers.google.`com/workspace/drive/api/guides/search-files`>). If you provide the URL parameter alt=media, then the response includes the file contents in the response body. Downloading content with alt=media only works if the file is stored in Drive. To download Google Docs, Sheets, and Slides use [files.export](<https://developers.google.`com/workspace/drive/api/reference/rest/v3/files/export`>) instead. For more information, see [Download and export files](<https://developers.google.`com/workspace/drive/api/guides/manage-downloads`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_files_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_files_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: File = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}
/// Gets a file's metadata or content by ID. For more information, see [Search for files and folders](<https://developers.google.`com/workspace/drive/api/guides/search-files`>). If you provide the URL parameter alt=media, then the response includes the file contents in the response body. Downloading content with alt=media only works if the file is stored in Drive. To download Google Docs, Sheets, and Slides use [files.export](<https://developers.google.`com/workspace/drive/api/reference/rest/v3/files/export`>) instead. For more information, see [Download and export files](<https://developers.google.`com/workspace/drive/api/guides/manage-downloads`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_files_get_builder()` + `drive_files_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_get(
    client: &SimpleHttpClient,
    fileId: &str,
    acknowledgeAbuse: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_files_get_builder(
        client,
        fileId,
        acknowledgeAbuse,
        includeLabels,
        includePermissionsForView,
        supportsAllDrives,
        supportsTeamDrives,
    )?;
    drive_files_get_execute(builder)
}

/// GET files
/// Lists the user's files. For more information, see [Search for files and folders](<https://developers.google.`com/workspace/drive/api/guides/search-files`>). This method accepts the q parameter, which is a search query combining one or more search terms. This method returns *all* files by default, including trashed files. If you don't want trashed files to appear in the list, use the trashed=`false` query parameter to remove trashed files from the results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_files_list_execute()` to send, or `drive_files_list` for simplest API.

pub fn drive_files_list_builder(
    client: &SimpleHttpClient,
    corpora: Option<&str>,
    corpus: Option<&str>,
    driveId: Option<&str>,
    includeItemsFromAllDrives: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    includeTeamDriveItems: Option<bool>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    q: Option<&str>,
    spaces: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    teamDriveId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "corpora",
            corpora
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "corpus",
            corpus
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "driveId",
            driveId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeItemsFromAllDrives",
            includeItemsFromAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeLabels",
            includeLabels
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includePermissionsForView",
            includePermissionsForView
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeTeamDriveItems",
            includeTeamDriveItems
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "q",
            q.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "spaces",
            spaces
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "teamDriveId",
            teamDriveId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files
/// Lists the user's files. For more information, see [Search for files and folders](<https://developers.google.`com/workspace/drive/api/guides/search-files`>). This method accepts the q parameter, which is a search query combining one or more search terms. This method returns *all* files by default, including trashed files. If you don't want trashed files to appear in the list, use the trashed=`false` query parameter to remove trashed files from the results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_files_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_files_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FileList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FileList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files
/// Lists the user's files. For more information, see [Search for files and folders](<https://developers.google.`com/workspace/drive/api/guides/search-files`>). This method accepts the q parameter, which is a search query combining one or more search terms. This method returns *all* files by default, including trashed files. If you don't want trashed files to appear in the list, use the trashed=`false` query parameter to remove trashed files from the results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_files_list_builder()` + `drive_files_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_list(
    client: &SimpleHttpClient,
    corpora: Option<&str>,
    corpus: Option<&str>,
    driveId: Option<&str>,
    includeItemsFromAllDrives: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    includeTeamDriveItems: Option<bool>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    q: Option<&str>,
    spaces: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    teamDriveId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FileList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_files_list_builder(
        client,
        corpora,
        corpus,
        driveId,
        includeItemsFromAllDrives,
        includeLabels,
        includePermissionsForView,
        includeTeamDriveItems,
        orderBy,
        pageSize,
        pageToken,
        q,
        spaces,
        supportsAllDrives,
        supportsTeamDrives,
        teamDriveId,
    )?;
    drive_files_list_execute(builder)
}

/// GET files/{fileId}/listLabels
/// Lists the labels on a file. For more information, see [List labels on a file](<https://developers.google.`com/workspace/drive/api/guides/list-labels`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_files_list_labels_execute()` to send, or `drive_files_list_labels` for simplest API.

pub fn drive_files_list_labels_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}/listLabels", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}/listLabels
/// Lists the labels on a file. For more information, see [List labels on a file](<https://developers.google.`com/workspace/drive/api/guides/list-labels`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_files_list_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_files_list_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_list_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LabelList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LabelList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/listLabels
/// Lists the labels on a file. For more information, see [List labels on a file](<https://developers.google.`com/workspace/drive/api/guides/list-labels`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_files_list_labels_builder()` + `drive_files_list_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_list_labels(
    client: &SimpleHttpClient,
    fileId: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LabelList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_files_list_labels_builder(client, fileId, maxResults, pageToken)?;
    drive_files_list_labels_execute(builder)
}

/// GET files/{fileId}/modifyLabels
/// Modifies the set of labels applied to a file. For more information, see [Set a label field on a file](<https://developers.google.`com/workspace/drive/api/guides/set-label`>). Returns a list of the labels that were added or modified.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_files_modify_labels_execute()` to send, or `drive_files_modify_labels` for simplest API.

pub fn drive_files_modify_labels_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    body: &ModifyLabelsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}/modifyLabels", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET files/{fileId}/modifyLabels
/// Modifies the set of labels applied to a file. For more information, see [Set a label field on a file](<https://developers.google.`com/workspace/drive/api/guides/set-label`>). Returns a list of the labels that were added or modified.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_files_modify_labels_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_files_modify_labels_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_modify_labels_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ModifyLabelsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ModifyLabelsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/modifyLabels
/// Modifies the set of labels applied to a file. For more information, see [Set a label field on a file](<https://developers.google.`com/workspace/drive/api/guides/set-label`>). Returns a list of the labels that were added or modified.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_files_modify_labels_builder()` + `drive_files_modify_labels_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_modify_labels(
    client: &SimpleHttpClient,
    fileId: &str,
    body: &ModifyLabelsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ModifyLabelsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = drive_files_modify_labels_builder(client, fileId, body)?;
    drive_files_modify_labels_execute(builder)
}

/// GET files/{fileId}
/// Updates a file's metadata, content, or both. When calling this method, only populate fields in the request that you want to modify. When updating fields, some fields might be changed automatically, such as `modifiedDate`. This method supports patch semantics. This method supports an */upload* URI and accepts uploaded media with the following characteristics: - *Maximum file size:* 5,120 GB - *Accepted Media MIME types:* */* (Specify a valid MIME type, rather than the literal */* value. The literal */* is only used to indicate that any valid MIME type can be uploaded. For more information, see [Google Workspace and Google Drive supported MIME types](<https://developers.google.`com/workspace/drive/api/guides/mime-types`>).) For more information on uploading files, see [Upload file data](<https://developers.google.`com/workspace/drive/api/guides/manage-uploads`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_files_update_execute()` to send, or `drive_files_update` for simplest API.

pub fn drive_files_update_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    addParents: Option<&str>,
    enforceSingleParent: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    keepRevisionForever: Option<bool>,
    ocrLanguage: Option<&str>,
    removeParents: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    useContentAsIndexableText: Option<bool>,
    body: &File,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "addParents",
            addParents
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "enforceSingleParent",
            enforceSingleParent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeLabels",
            includeLabels
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includePermissionsForView",
            includePermissionsForView
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "keepRevisionForever",
            keepRevisionForever
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "ocrLanguage",
            ocrLanguage
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "removeParents",
            removeParents
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useContentAsIndexableText",
            useContentAsIndexableText
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET files/{fileId}
/// Updates a file's metadata, content, or both. When calling this method, only populate fields in the request that you want to modify. When updating fields, some fields might be changed automatically, such as `modifiedDate`. This method supports patch semantics. This method supports an */upload* URI and accepts uploaded media with the following characteristics: - *Maximum file size:* 5,120 GB - *Accepted Media MIME types:* */* (Specify a valid MIME type, rather than the literal */* value. The literal */* is only used to indicate that any valid MIME type can be uploaded. For more information, see [Google Workspace and Google Drive supported MIME types](<https://developers.google.`com/workspace/drive/api/guides/mime-types`>).) For more information on uploading files, see [Upload file data](<https://developers.google.`com/workspace/drive/api/guides/manage-uploads`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_files_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_files_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: File = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}
/// Updates a file's metadata, content, or both. When calling this method, only populate fields in the request that you want to modify. When updating fields, some fields might be changed automatically, such as `modifiedDate`. This method supports patch semantics. This method supports an */upload* URI and accepts uploaded media with the following characteristics: - *Maximum file size:* 5,120 GB - *Accepted Media MIME types:* */* (Specify a valid MIME type, rather than the literal */* value. The literal */* is only used to indicate that any valid MIME type can be uploaded. For more information, see [Google Workspace and Google Drive supported MIME types](<https://developers.google.`com/workspace/drive/api/guides/mime-types`>).) For more information on uploading files, see [Upload file data](<https://developers.google.`com/workspace/drive/api/guides/manage-uploads`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_files_update_builder()` + `drive_files_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_update(
    client: &SimpleHttpClient,
    fileId: &str,
    addParents: Option<&str>,
    enforceSingleParent: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    keepRevisionForever: Option<bool>,
    ocrLanguage: Option<&str>,
    removeParents: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    useContentAsIndexableText: Option<bool>,
    body: &File,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<File>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_files_update_builder(
        client,
        fileId,
        addParents,
        enforceSingleParent,
        includeLabels,
        includePermissionsForView,
        keepRevisionForever,
        ocrLanguage,
        removeParents,
        supportsAllDrives,
        supportsTeamDrives,
        useContentAsIndexableText,
        body,
    )?;
    drive_files_update_execute(builder)
}

/// GET files/{fileId}/watch
/// Subscribes to changes to a file. For more information, see [Notifications for resource changes](<https://developers.google.`com/workspace/drive/api/guides/push`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_files_watch_execute()` to send, or `drive_files_watch` for simplest API.

pub fn drive_files_watch_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    acknowledgeAbuse: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    body: &Channel,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}/watch", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "acknowledgeAbuse",
            acknowledgeAbuse
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeLabels",
            includeLabels
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includePermissionsForView",
            includePermissionsForView
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET files/{fileId}/watch
/// Subscribes to changes to a file. For more information, see [Notifications for resource changes](<https://developers.google.`com/workspace/drive/api/guides/push`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_files_watch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_files_watch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_watch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/watch
/// Subscribes to changes to a file. For more information, see [Notifications for resource changes](<https://developers.google.`com/workspace/drive/api/guides/push`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_files_watch_builder()` + `drive_files_watch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_files_watch(
    client: &SimpleHttpClient,
    fileId: &str,
    acknowledgeAbuse: Option<bool>,
    includeLabels: Option<&str>,
    includePermissionsForView: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    body: &Channel,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_files_watch_builder(
        client,
        fileId,
        acknowledgeAbuse,
        includeLabels,
        includePermissionsForView,
        supportsAllDrives,
        supportsTeamDrives,
        body,
    )?;
    drive_files_watch_execute(builder)
}

/// GET operations/{name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_operations_get_execute()` to send, or `drive_operations_get` for simplest API.

pub fn drive_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comoperations/{}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET operations/{name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET operations/{name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_operations_get_builder()` + `drive_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_operations_get_builder(client, name)?;
    drive_operations_get_execute(builder)
}

/// GET files/{fileId}/permissions
/// Creates a permission for a file or shared drive. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>). **Warning:** Concurrent permissions operations on the same file aren't supported; only the last update is applied.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_permissions_create_execute()` to send, or `drive_permissions_create` for simplest API.

pub fn drive_permissions_create_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    emailMessage: Option<&str>,
    enforceExpansiveAccess: Option<bool>,
    enforceSingleParent: Option<bool>,
    moveToNewOwnersRoot: Option<bool>,
    sendNotificationEmail: Option<bool>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    transferOwnership: Option<bool>,
    useDomainAdminAccess: Option<bool>,
    body: &Permission,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}/permissions", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "emailMessage",
            emailMessage
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "enforceExpansiveAccess",
            enforceExpansiveAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "enforceSingleParent",
            enforceSingleParent
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "moveToNewOwnersRoot",
            moveToNewOwnersRoot
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sendNotificationEmail",
            sendNotificationEmail
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "transferOwnership",
            transferOwnership
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useDomainAdminAccess",
            useDomainAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET files/{fileId}/permissions
/// Creates a permission for a file or shared drive. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>). **Warning:** Concurrent permissions operations on the same file aren't supported; only the last update is applied.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_permissions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_permissions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_permissions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Permission>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Permission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/permissions
/// Creates a permission for a file or shared drive. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>). **Warning:** Concurrent permissions operations on the same file aren't supported; only the last update is applied.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_permissions_create_builder()` + `drive_permissions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_permissions_create(
    client: &SimpleHttpClient,
    fileId: &str,
    emailMessage: Option<&str>,
    enforceExpansiveAccess: Option<bool>,
    enforceSingleParent: Option<bool>,
    moveToNewOwnersRoot: Option<bool>,
    sendNotificationEmail: Option<bool>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    transferOwnership: Option<bool>,
    useDomainAdminAccess: Option<bool>,
    body: &Permission,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Permission>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_permissions_create_builder(
        client,
        fileId,
        emailMessage,
        enforceExpansiveAccess,
        enforceSingleParent,
        moveToNewOwnersRoot,
        sendNotificationEmail,
        supportsAllDrives,
        supportsTeamDrives,
        transferOwnership,
        useDomainAdminAccess,
        body,
    )?;
    drive_permissions_create_execute(builder)
}

/// GET files/{fileId}/permissions/{permissionId}
/// Deletes a permission. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>). **Warning:** Concurrent permissions operations on the same file aren't supported; only the last update is applied.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_permissions_delete_execute()` to send, or `drive_permissions_delete` for simplest API.

pub fn drive_permissions_delete_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    permissionId: &str,
    enforceExpansiveAccess: Option<bool>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    useDomainAdminAccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/permissions/{}",
        fileId, permissionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "enforceExpansiveAccess",
            enforceExpansiveAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useDomainAdminAccess",
            useDomainAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}/permissions/{permissionId}
/// Deletes a permission. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>). **Warning:** Concurrent permissions operations on the same file aren't supported; only the last update is applied.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_permissions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_permissions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_permissions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/permissions/{permissionId}
/// Deletes a permission. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>). **Warning:** Concurrent permissions operations on the same file aren't supported; only the last update is applied.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_permissions_delete_builder()` + `drive_permissions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_permissions_delete(
    client: &SimpleHttpClient,
    fileId: &str,
    permissionId: &str,
    enforceExpansiveAccess: Option<bool>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    useDomainAdminAccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_permissions_delete_builder(
        client,
        fileId,
        permissionId,
        enforceExpansiveAccess,
        supportsAllDrives,
        supportsTeamDrives,
        useDomainAdminAccess,
    )?;
    drive_permissions_delete_execute(builder)
}

/// GET files/{fileId}/permissions/{permissionId}
/// Gets a permission by ID. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_permissions_get_execute()` to send, or `drive_permissions_get` for simplest API.

pub fn drive_permissions_get_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    permissionId: &str,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    useDomainAdminAccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/permissions/{}",
        fileId, permissionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useDomainAdminAccess",
            useDomainAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}/permissions/{permissionId}
/// Gets a permission by ID. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_permissions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_permissions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_permissions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Permission>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Permission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/permissions/{permissionId}
/// Gets a permission by ID. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_permissions_get_builder()` + `drive_permissions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_permissions_get(
    client: &SimpleHttpClient,
    fileId: &str,
    permissionId: &str,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    useDomainAdminAccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Permission>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_permissions_get_builder(
        client,
        fileId,
        permissionId,
        supportsAllDrives,
        supportsTeamDrives,
        useDomainAdminAccess,
    )?;
    drive_permissions_get_execute(builder)
}

/// GET files/{fileId}/permissions
/// Lists a file's or shared drive's permissions. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_permissions_list_execute()` to send, or `drive_permissions_list` for simplest API.

pub fn drive_permissions_list_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    includePermissionsForView: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    useDomainAdminAccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}/permissions", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "includePermissionsForView",
            includePermissionsForView
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useDomainAdminAccess",
            useDomainAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}/permissions
/// Lists a file's or shared drive's permissions. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_permissions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_permissions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_permissions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PermissionList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PermissionList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/permissions
/// Lists a file's or shared drive's permissions. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_permissions_list_builder()` + `drive_permissions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_permissions_list(
    client: &SimpleHttpClient,
    fileId: &str,
    includePermissionsForView: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    useDomainAdminAccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PermissionList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = drive_permissions_list_builder(
        client,
        fileId,
        includePermissionsForView,
        pageSize,
        pageToken,
        supportsAllDrives,
        supportsTeamDrives,
        useDomainAdminAccess,
    )?;
    drive_permissions_list_execute(builder)
}

/// GET files/{fileId}/permissions/{permissionId}
/// Updates a permission with patch semantics. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>). **Warning:** Concurrent permissions operations on the same file aren't supported; only the last update is applied.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_permissions_update_execute()` to send, or `drive_permissions_update` for simplest API.

pub fn drive_permissions_update_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    permissionId: &str,
    enforceExpansiveAccess: Option<bool>,
    removeExpiration: Option<bool>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    transferOwnership: Option<bool>,
    useDomainAdminAccess: Option<bool>,
    body: &Permission,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/permissions/{}",
        fileId, permissionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "enforceExpansiveAccess",
            enforceExpansiveAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "removeExpiration",
            removeExpiration
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsAllDrives",
            supportsAllDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "supportsTeamDrives",
            supportsTeamDrives
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "transferOwnership",
            transferOwnership
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useDomainAdminAccess",
            useDomainAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET files/{fileId}/permissions/{permissionId}
/// Updates a permission with patch semantics. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>). **Warning:** Concurrent permissions operations on the same file aren't supported; only the last update is applied.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_permissions_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_permissions_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_permissions_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Permission>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Permission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/permissions/{permissionId}
/// Updates a permission with patch semantics. For more information, see [Share files, folders, and drives](<https://developers.google.`com/workspace/drive/api/guides/manage-sharing`>). **Warning:** Concurrent permissions operations on the same file aren't supported; only the last update is applied.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_permissions_update_builder()` + `drive_permissions_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_permissions_update(
    client: &SimpleHttpClient,
    fileId: &str,
    permissionId: &str,
    enforceExpansiveAccess: Option<bool>,
    removeExpiration: Option<bool>,
    supportsAllDrives: Option<bool>,
    supportsTeamDrives: Option<bool>,
    transferOwnership: Option<bool>,
    useDomainAdminAccess: Option<bool>,
    body: &Permission,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Permission>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_permissions_update_builder(
        client,
        fileId,
        permissionId,
        enforceExpansiveAccess,
        removeExpiration,
        supportsAllDrives,
        supportsTeamDrives,
        transferOwnership,
        useDomainAdminAccess,
        body,
    )?;
    drive_permissions_update_execute(builder)
}

/// GET files/{fileId}/comments/{commentId}/replies
/// Creates a reply to a comment. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_replies_create_execute()` to send, or `drive_replies_create` for simplest API.

pub fn drive_replies_create_builder(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
    body: &Reply,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/comments/{}/replies",
        commentId, fileId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET files/{fileId}/comments/{commentId}/replies
/// Creates a reply to a comment. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_replies_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_replies_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_replies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reply>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Reply = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/comments/{commentId}/replies
/// Creates a reply to a comment. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_replies_create_builder()` + `drive_replies_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_replies_create(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
    body: &Reply,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reply>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_replies_create_builder(client, commentId, fileId, body)?;
    drive_replies_create_execute(builder)
}

/// GET files/{fileId}/comments/{commentId}/replies/{replyId}
/// Deletes a reply. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_replies_delete_execute()` to send, or `drive_replies_delete` for simplest API.

pub fn drive_replies_delete_builder(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
    replyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/comments/{}/replies/{}",
        commentId, fileId, replyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/comments/{commentId}/replies/{replyId}
/// Deletes a reply. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_replies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_replies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_replies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/comments/{commentId}/replies/{replyId}
/// Deletes a reply. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_replies_delete_builder()` + `drive_replies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_replies_delete(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
    replyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_replies_delete_builder(client, commentId, fileId, replyId)?;
    drive_replies_delete_execute(builder)
}

/// GET files/{fileId}/comments/{commentId}/replies/{replyId}
/// Gets a reply by ID. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_replies_get_execute()` to send, or `drive_replies_get` for simplest API.

pub fn drive_replies_get_builder(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
    replyId: &str,
    includeDeleted: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/comments/{}/replies/{}",
        commentId, fileId, replyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "includeDeleted",
            includeDeleted
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}/comments/{commentId}/replies/{replyId}
/// Gets a reply by ID. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_replies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_replies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_replies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reply>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Reply = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/comments/{commentId}/replies/{replyId}
/// Gets a reply by ID. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_replies_get_builder()` + `drive_replies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_replies_get(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
    replyId: &str,
    includeDeleted: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reply>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_replies_get_builder(client, commentId, fileId, replyId, includeDeleted)?;
    drive_replies_get_execute(builder)
}

/// GET files/{fileId}/comments/{commentId}/replies
/// Lists a comment's replies. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_replies_list_execute()` to send, or `drive_replies_list` for simplest API.

pub fn drive_replies_list_builder(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
    includeDeleted: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/comments/{}/replies",
        commentId, fileId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "includeDeleted",
            includeDeleted
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}/comments/{commentId}/replies
/// Lists a comment's replies. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_replies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_replies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_replies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReplyList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReplyList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/comments/{commentId}/replies
/// Lists a comment's replies. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_replies_list_builder()` + `drive_replies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_replies_list(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
    includeDeleted: Option<bool>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReplyList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_replies_list_builder(
        client,
        commentId,
        fileId,
        includeDeleted,
        pageSize,
        pageToken,
    )?;
    drive_replies_list_execute(builder)
}

/// GET files/{fileId}/comments/{commentId}/replies/{replyId}
/// Updates a reply with patch semantics. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_replies_update_execute()` to send, or `drive_replies_update` for simplest API.

pub fn drive_replies_update_builder(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
    replyId: &str,
    body: &Reply,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/comments/{}/replies/{}",
        commentId, fileId, replyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET files/{fileId}/comments/{commentId}/replies/{replyId}
/// Updates a reply with patch semantics. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_replies_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_replies_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_replies_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reply>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Reply = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/comments/{commentId}/replies/{replyId}
/// Updates a reply with patch semantics. For more information, see [Manage comments and replies](<https://developers.google.`com/workspace/drive/api/guides/manage-comments`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_replies_update_builder()` + `drive_replies_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_replies_update(
    client: &SimpleHttpClient,
    commentId: &str,
    fileId: &str,
    replyId: &str,
    body: &Reply,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Reply>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_replies_update_builder(client, commentId, fileId, replyId, body)?;
    drive_replies_update_execute(builder)
}

/// GET files/{fileId}/revisions/{revisionId}
/// Permanently deletes a file version. You can only delete revisions for files with binary content in Google Drive, like images or videos. Revisions for other files, like Google Docs or Sheets, and the last remaining file version can't be deleted. For more information, see [Manage file revisions](<https://developers.google.`com/drive/api/guides/manage-revisions`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_revisions_delete_execute()` to send, or `drive_revisions_delete` for simplest API.

pub fn drive_revisions_delete_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    revisionId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/revisions/{}",
        fileId, revisionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/revisions/{revisionId}
/// Permanently deletes a file version. You can only delete revisions for files with binary content in Google Drive, like images or videos. Revisions for other files, like Google Docs or Sheets, and the last remaining file version can't be deleted. For more information, see [Manage file revisions](<https://developers.google.`com/drive/api/guides/manage-revisions`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_revisions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_revisions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_revisions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/revisions/{revisionId}
/// Permanently deletes a file version. You can only delete revisions for files with binary content in Google Drive, like images or videos. Revisions for other files, like Google Docs or Sheets, and the last remaining file version can't be deleted. For more information, see [Manage file revisions](<https://developers.google.`com/drive/api/guides/manage-revisions`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_revisions_delete_builder()` + `drive_revisions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_revisions_delete(
    client: &SimpleHttpClient,
    fileId: &str,
    revisionId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_revisions_delete_builder(client, fileId, revisionId)?;
    drive_revisions_delete_execute(builder)
}

/// GET files/{fileId}/revisions/{revisionId}
/// Gets a revision's metadata or content by ID. For more information, see [Manage file revisions](<https://developers.google.`com/workspace/drive/api/guides/manage-revisions`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_revisions_get_execute()` to send, or `drive_revisions_get` for simplest API.

pub fn drive_revisions_get_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    revisionId: &str,
    acknowledgeAbuse: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/revisions/{}",
        fileId, revisionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "acknowledgeAbuse",
            acknowledgeAbuse
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}/revisions/{revisionId}
/// Gets a revision's metadata or content by ID. For more information, see [Manage file revisions](<https://developers.google.`com/workspace/drive/api/guides/manage-revisions`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_revisions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_revisions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_revisions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Revision>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Revision = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/revisions/{revisionId}
/// Gets a revision's metadata or content by ID. For more information, see [Manage file revisions](<https://developers.google.`com/workspace/drive/api/guides/manage-revisions`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_revisions_get_builder()` + `drive_revisions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_revisions_get(
    client: &SimpleHttpClient,
    fileId: &str,
    revisionId: &str,
    acknowledgeAbuse: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Revision>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_revisions_get_builder(client, fileId, revisionId, acknowledgeAbuse)?;
    drive_revisions_get_execute(builder)
}

/// GET files/{fileId}/revisions
/// Lists a file's revisions. For more information, see [Manage file revisions](<https://developers.google.`com/workspace/drive/api/guides/manage-revisions`>). **Important:** The list of revisions returned by this method might be incomplete for files with a large revision history, including frequently edited Google Docs, Sheets, and Slides. Older revisions might be omitted from the response, meaning the first revision returned may not be the oldest existing revision. The revision history visible in the Workspace editor user interface might be more complete than the list returned by the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_revisions_list_execute()` to send, or `drive_revisions_list` for simplest API.

pub fn drive_revisions_list_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comfiles/{}/revisions", fileId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET files/{fileId}/revisions
/// Lists a file's revisions. For more information, see [Manage file revisions](<https://developers.google.`com/workspace/drive/api/guides/manage-revisions`>). **Important:** The list of revisions returned by this method might be incomplete for files with a large revision history, including frequently edited Google Docs, Sheets, and Slides. Older revisions might be omitted from the response, meaning the first revision returned may not be the oldest existing revision. The revision history visible in the Workspace editor user interface might be more complete than the list returned by the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_revisions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_revisions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_revisions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RevisionList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RevisionList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/revisions
/// Lists a file's revisions. For more information, see [Manage file revisions](<https://developers.google.`com/workspace/drive/api/guides/manage-revisions`>). **Important:** The list of revisions returned by this method might be incomplete for files with a large revision history, including frequently edited Google Docs, Sheets, and Slides. Older revisions might be omitted from the response, meaning the first revision returned may not be the oldest existing revision. The revision history visible in the Workspace editor user interface might be more complete than the list returned by the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_revisions_list_builder()` + `drive_revisions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_revisions_list(
    client: &SimpleHttpClient,
    fileId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RevisionList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = drive_revisions_list_builder(client, fileId, pageSize, pageToken)?;
    drive_revisions_list_execute(builder)
}

/// GET files/{fileId}/revisions/{revisionId}
/// Updates a revision with patch semantics. For more information, see [Manage file revisions](<https://developers.google.`com/workspace/drive/api/guides/manage-revisions`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_revisions_update_execute()` to send, or `drive_revisions_update` for simplest API.

pub fn drive_revisions_update_builder(
    client: &SimpleHttpClient,
    fileId: &str,
    revisionId: &str,
    body: &Revision,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comfiles/{}/revisions/{}",
        fileId, revisionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET files/{fileId}/revisions/{revisionId}
/// Updates a revision with patch semantics. For more information, see [Manage file revisions](<https://developers.google.`com/workspace/drive/api/guides/manage-revisions`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_revisions_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_revisions_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_revisions_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Revision>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Revision = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET files/{fileId}/revisions/{revisionId}
/// Updates a revision with patch semantics. For more information, see [Manage file revisions](<https://developers.google.`com/workspace/drive/api/guides/manage-revisions`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_revisions_update_builder()` + `drive_revisions_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_revisions_update(
    client: &SimpleHttpClient,
    fileId: &str,
    revisionId: &str,
    body: &Revision,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Revision>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_revisions_update_builder(client, fileId, revisionId, body)?;
    drive_revisions_update_execute(builder)
}

/// GET teamdrives
/// Deprecated: Use drives.create instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_teamdrives_create_execute()` to send, or `drive_teamdrives_create` for simplest API.

pub fn drive_teamdrives_create_builder(
    client: &SimpleHttpClient,
    requestId: Option<&str>,
    body: &TeamDrive,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comteamdrives",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET teamdrives
/// Deprecated: Use drives.create instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_teamdrives_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_teamdrives_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_teamdrives_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TeamDrive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TeamDrive = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET teamdrives
/// Deprecated: Use drives.create instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_teamdrives_create_builder()` + `drive_teamdrives_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_teamdrives_create(
    client: &SimpleHttpClient,
    requestId: Option<&str>,
    body: &TeamDrive,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TeamDrive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_teamdrives_create_builder(client, requestId, body)?;
    drive_teamdrives_create_execute(builder)
}

/// GET teamdrives/{teamDriveId}
/// Deprecated: Use drives.delete instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_teamdrives_delete_execute()` to send, or `drive_teamdrives_delete` for simplest API.

pub fn drive_teamdrives_delete_builder(
    client: &SimpleHttpClient,
    teamDriveId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comteamdrives/{}", teamDriveId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET teamdrives/{teamDriveId}
/// Deprecated: Use drives.delete instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_teamdrives_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_teamdrives_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_teamdrives_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET teamdrives/{teamDriveId}
/// Deprecated: Use drives.delete instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_teamdrives_delete_builder()` + `drive_teamdrives_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_teamdrives_delete(
    client: &SimpleHttpClient,
    teamDriveId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_teamdrives_delete_builder(client, teamDriveId)?;
    drive_teamdrives_delete_execute(builder)
}

/// GET teamdrives/{teamDriveId}
/// Deprecated: Use drives.get instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_teamdrives_get_execute()` to send, or `drive_teamdrives_get` for simplest API.

pub fn drive_teamdrives_get_builder(
    client: &SimpleHttpClient,
    teamDriveId: &str,
    useDomainAdminAccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comteamdrives/{}", teamDriveId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "useDomainAdminAccess",
            useDomainAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET teamdrives/{teamDriveId}
/// Deprecated: Use drives.get instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_teamdrives_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_teamdrives_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_teamdrives_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TeamDrive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TeamDrive = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET teamdrives/{teamDriveId}
/// Deprecated: Use drives.get instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_teamdrives_get_builder()` + `drive_teamdrives_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_teamdrives_get(
    client: &SimpleHttpClient,
    teamDriveId: &str,
    useDomainAdminAccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TeamDrive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_teamdrives_get_builder(client, teamDriveId, useDomainAdminAccess)?;
    drive_teamdrives_get_execute(builder)
}

/// GET teamdrives
/// Deprecated: Use drives.list instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_teamdrives_list_execute()` to send, or `drive_teamdrives_list` for simplest API.

pub fn drive_teamdrives_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    q: Option<&str>,
    useDomainAdminAccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comteamdrives",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "q",
            q.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useDomainAdminAccess",
            useDomainAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET teamdrives
/// Deprecated: Use drives.list instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_teamdrives_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_teamdrives_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_teamdrives_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TeamDriveList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TeamDriveList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET teamdrives
/// Deprecated: Use drives.list instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_teamdrives_list_builder()` + `drive_teamdrives_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_teamdrives_list(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    q: Option<&str>,
    useDomainAdminAccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TeamDriveList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        drive_teamdrives_list_builder(client, pageSize, pageToken, q, useDomainAdminAccess)?;
    drive_teamdrives_list_execute(builder)
}

/// GET teamdrives/{teamDriveId}
/// Deprecated: Use drives.update instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drive_teamdrives_update_execute()` to send, or `drive_teamdrives_update` for simplest API.

pub fn drive_teamdrives_update_builder(
    client: &SimpleHttpClient,
    teamDriveId: &str,
    useDomainAdminAccess: Option<bool>,
    body: &TeamDrive,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comteamdrives/{}", teamDriveId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "useDomainAdminAccess",
            useDomainAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET teamdrives/{teamDriveId}
/// Deprecated: Use drives.update instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drive_teamdrives_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drive_teamdrives_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_teamdrives_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TeamDrive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TeamDrive = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET teamdrives/{teamDriveId}
/// Deprecated: Use drives.update instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drive_teamdrives_update_builder()` + `drive_teamdrives_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drive_teamdrives_update(
    client: &SimpleHttpClient,
    teamDriveId: &str,
    useDomainAdminAccess: Option<bool>,
    body: &TeamDrive,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TeamDrive>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = drive_teamdrives_update_builder(client, teamDriveId, useDomainAdminAccess, body)?;
    drive_teamdrives_update_execute(builder)
}
