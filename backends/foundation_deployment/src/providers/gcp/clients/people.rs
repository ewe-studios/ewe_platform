//! Auto-generated API clients for Google Cloud Platform - people.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/contactGroups:batchGet
/// Get a list of contact groups owned by the authenticated user by specifying a list of contact group resource names.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_contact_groups_batch_get_execute()` to send, or `people_contact_groups_batch_get` for simplest API.

pub fn people_contact_groups_batch_get_builder(
    client: &SimpleHttpClient,
    groupFields: Option<&str>,
    maxMembers: Option<i32>,
    resourceNames: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/contactGroups:batchGet",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "groupFields",
            groupFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxMembers",
            maxMembers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resourceNames",
            resourceNames
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/contactGroups:batchGet
/// Get a list of contact groups owned by the authenticated user by specifying a list of contact group resource names.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_contact_groups_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_contact_groups_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetContactGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetContactGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/contactGroups:batchGet
/// Get a list of contact groups owned by the authenticated user by specifying a list of contact group resource names.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_contact_groups_batch_get_builder()` + `people_contact_groups_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_batch_get(
    client: &SimpleHttpClient,
    groupFields: Option<&str>,
    maxMembers: Option<i32>,
    resourceNames: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetContactGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        people_contact_groups_batch_get_builder(client, groupFields, maxMembers, resourceNames)?;
    people_contact_groups_batch_get_execute(builder)
}

/// GET v1/contactGroups
/// Create a new contact group owned by the authenticated user. Created contact group names must be unique to the users contact groups. Attempting to create a group with a duplicate name will return a HTTP 409 error. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_contact_groups_create_execute()` to send, or `people_contact_groups_create` for simplest API.

pub fn people_contact_groups_create_builder(
    client: &SimpleHttpClient,
    body: &CreateContactGroupRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/contactGroups",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/contactGroups
/// Create a new contact group owned by the authenticated user. Created contact group names must be unique to the users contact groups. Attempting to create a group with a duplicate name will return a HTTP 409 error. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_contact_groups_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_contact_groups_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ContactGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/contactGroups
/// Create a new contact group owned by the authenticated user. Created contact group names must be unique to the users contact groups. Attempting to create a group with a duplicate name will return a HTTP 409 error. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_contact_groups_create_builder()` + `people_contact_groups_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_create(
    client: &SimpleHttpClient,
    body: &CreateContactGroupRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_contact_groups_create_builder(client, body)?;
    people_contact_groups_create_execute(builder)
}

/// GET v1/{+resourceName}
/// Delete an existing contact group owned by the authenticated user by specifying a contact group resource name. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_contact_groups_delete_execute()` to send, or `people_contact_groups_delete` for simplest API.

pub fn people_contact_groups_delete_builder(
    client: &SimpleHttpClient,
    resourceName: &str,
    deleteContacts: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+resourceName}", resourceName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "deleteContacts",
            deleteContacts
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+resourceName}
/// Delete an existing contact group owned by the authenticated user by specifying a contact group resource name. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_contact_groups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_contact_groups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resourceName}
/// Delete an existing contact group owned by the authenticated user by specifying a contact group resource name. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_contact_groups_delete_builder()` + `people_contact_groups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_delete(
    client: &SimpleHttpClient,
    resourceName: &str,
    deleteContacts: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = people_contact_groups_delete_builder(client, resourceName, deleteContacts)?;
    people_contact_groups_delete_execute(builder)
}

/// GET v1/{+resourceName}
/// Get a specific contact group owned by the authenticated user by specifying a contact group resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_contact_groups_get_execute()` to send, or `people_contact_groups_get` for simplest API.

pub fn people_contact_groups_get_builder(
    client: &SimpleHttpClient,
    resourceName: &str,
    groupFields: Option<&str>,
    maxMembers: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+resourceName}", resourceName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "groupFields",
            groupFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxMembers",
            maxMembers
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+resourceName}
/// Get a specific contact group owned by the authenticated user by specifying a contact group resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_contact_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_contact_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ContactGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resourceName}
/// Get a specific contact group owned by the authenticated user by specifying a contact group resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_contact_groups_get_builder()` + `people_contact_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_get(
    client: &SimpleHttpClient,
    resourceName: &str,
    groupFields: Option<&str>,
    maxMembers: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_contact_groups_get_builder(client, resourceName, groupFields, maxMembers)?;
    people_contact_groups_get_execute(builder)
}

/// GET v1/contactGroups
/// List all contact groups owned by the authenticated user. Members of the contact groups are not populated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_contact_groups_list_execute()` to send, or `people_contact_groups_list` for simplest API.

pub fn people_contact_groups_list_builder(
    client: &SimpleHttpClient,
    groupFields: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    syncToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/contactGroups",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "groupFields",
            groupFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "syncToken",
            syncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/contactGroups
/// List all contact groups owned by the authenticated user. Members of the contact groups are not populated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_contact_groups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_contact_groups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListContactGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListContactGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/contactGroups
/// List all contact groups owned by the authenticated user. Members of the contact groups are not populated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_contact_groups_list_builder()` + `people_contact_groups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_list(
    client: &SimpleHttpClient,
    groupFields: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    syncToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListContactGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        people_contact_groups_list_builder(client, groupFields, pageSize, pageToken, syncToken)?;
    people_contact_groups_list_execute(builder)
}

/// GET v1/{+resourceName}
/// Update the name of an existing contact group owned by the authenticated user. Updated contact group names must be unique to the users contact groups. Attempting to create a group with a duplicate name will return a HTTP 409 error. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_contact_groups_update_execute()` to send, or `people_contact_groups_update` for simplest API.

pub fn people_contact_groups_update_builder(
    client: &SimpleHttpClient,
    resourceName: &str,
    body: &UpdateContactGroupRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+resourceName}", resourceName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resourceName}
/// Update the name of an existing contact group owned by the authenticated user. Updated contact group names must be unique to the users contact groups. Attempting to create a group with a duplicate name will return a HTTP 409 error. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_contact_groups_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_contact_groups_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ContactGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resourceName}
/// Update the name of an existing contact group owned by the authenticated user. Updated contact group names must be unique to the users contact groups. Attempting to create a group with a duplicate name will return a HTTP 409 error. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_contact_groups_update_builder()` + `people_contact_groups_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_update(
    client: &SimpleHttpClient,
    resourceName: &str,
    body: &UpdateContactGroupRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_contact_groups_update_builder(client, resourceName, body)?;
    people_contact_groups_update_execute(builder)
}

/// GET v1/{+resourceName}/members:modify
/// Modify the members of a contact group owned by the authenticated user. The only system contact groups that can have members added are contactG`roups/`myContacts`` and contactG`roups/starred`. Other system contact groups are deprecated and can only have contacts removed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_contact_groups_members_modify_execute()` to send, or `people_contact_groups_members_modify` for simplest API.

pub fn people_contact_groups_members_modify_builder(
    client: &SimpleHttpClient,
    resourceName: &str,
    body: &ModifyContactGroupMembersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resourceName}/members:modify",
        resourceName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resourceName}/members:modify
/// Modify the members of a contact group owned by the authenticated user. The only system contact groups that can have members added are contactG`roups/`myContacts`` and contactG`roups/starred`. Other system contact groups are deprecated and can only have contacts removed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_contact_groups_members_modify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_contact_groups_members_modify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_members_modify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ModifyContactGroupMembersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ModifyContactGroupMembersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resourceName}/members:modify
/// Modify the members of a contact group owned by the authenticated user. The only system contact groups that can have members added are contactG`roups/`myContacts`` and contactG`roups/starred`. Other system contact groups are deprecated and can only have contacts removed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_contact_groups_members_modify_builder()` + `people_contact_groups_members_modify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_members_modify(
    client: &SimpleHttpClient,
    resourceName: &str,
    body: &ModifyContactGroupMembersRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ModifyContactGroupMembersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = people_contact_groups_members_modify_builder(client, resourceName, body)?;
    people_contact_groups_members_modify_execute(builder)
}

/// GET v1/{+resourceName}:copyOtherContactToMyContactsGroup
/// Copies an "Other contact" to a new contact in the user's "`myContacts`" group Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_other_contacts_copy_other_contact_to_my_contacts_group_execute()` to send, or `people_other_contacts_copy_other_contact_to_my_contacts_group` for simplest API.

pub fn people_other_contacts_copy_other_contact_to_my_contacts_group_builder(
    client: &SimpleHttpClient,
    resourceName: &str,
    body: &CopyOtherContactToMyContactsGroupRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resourceName}:copyOtherContactToMyContactsGroup",
        resourceName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resourceName}:copyOtherContactToMyContactsGroup
/// Copies an "Other contact" to a new contact in the user's "`myContacts`" group Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_other_contacts_copy_other_contact_to_my_contacts_group_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_other_contacts_copy_other_contact_to_my_contacts_group_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_other_contacts_copy_other_contact_to_my_contacts_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resourceName}:copyOtherContactToMyContactsGroup
/// Copies an "Other contact" to a new contact in the user's "`myContacts`" group Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_other_contacts_copy_other_contact_to_my_contacts_group_builder()` + `people_other_contacts_copy_other_contact_to_my_contacts_group_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_other_contacts_copy_other_contact_to_my_contacts_group(
    client: &SimpleHttpClient,
    resourceName: &str,
    body: &CopyOtherContactToMyContactsGroupRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = people_other_contacts_copy_other_contact_to_my_contacts_group_builder(
        client,
        resourceName,
        body,
    )?;
    people_other_contacts_copy_other_contact_to_my_contacts_group_execute(builder)
}

/// GET v1/otherContacts
/// List all "Other contacts", that is contacts that are not in a contact group. "Other contacts" are typically auto created contacts from interactions. Sync tokens expire 7 days after the full sync. A request with an expired sync token will get an error with an [google.rpc.ErrorInfo](<https://cloud.google.`com/apis/design/errors`#error_info>) with reason "EXPIRED_SYNC_TOKEN". In the case of such an error clients should make a full sync request without a sync_token. The first page of a full sync request has an additional quota. If the quota is exceeded, a 429 error will be returned. This quota is fixed and can not be increased. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the user's other contacts that have changed](/`people/v1/other-contacts`#list_the_users_other_contacts_that_have_changed).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_other_contacts_list_execute()` to send, or `people_other_contacts_list` for simplest API.

pub fn people_other_contacts_list_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
    requestSyncToken: Option<bool>,
    sources: Option<&str>,
    syncToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/otherContacts",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestSyncToken",
            requestSyncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sources",
            sources
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "syncToken",
            syncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/otherContacts
/// List all "Other contacts", that is contacts that are not in a contact group. "Other contacts" are typically auto created contacts from interactions. Sync tokens expire 7 days after the full sync. A request with an expired sync token will get an error with an [google.rpc.ErrorInfo](<https://cloud.google.`com/apis/design/errors`#error_info>) with reason "EXPIRED_SYNC_TOKEN". In the case of such an error clients should make a full sync request without a sync_token. The first page of a full sync request has an additional quota. If the quota is exceeded, a 429 error will be returned. This quota is fixed and can not be increased. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the user's other contacts that have changed](/`people/v1/other-contacts`#list_the_users_other_contacts_that_have_changed).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_other_contacts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_other_contacts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_other_contacts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOtherContactsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOtherContactsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/otherContacts
/// List all "Other contacts", that is contacts that are not in a contact group. "Other contacts" are typically auto created contacts from interactions. Sync tokens expire 7 days after the full sync. A request with an expired sync token will get an error with an [google.rpc.ErrorInfo](<https://cloud.google.`com/apis/design/errors`#error_info>) with reason "EXPIRED_SYNC_TOKEN". In the case of such an error clients should make a full sync request without a sync_token. The first page of a full sync request has an additional quota. If the quota is exceeded, a 429 error will be returned. This quota is fixed and can not be increased. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the user's other contacts that have changed](/`people/v1/other-contacts`#list_the_users_other_contacts_that_have_changed).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_other_contacts_list_builder()` + `people_other_contacts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_other_contacts_list(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
    requestSyncToken: Option<bool>,
    sources: Option<&str>,
    syncToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOtherContactsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_other_contacts_list_builder(
        client,
        pageSize,
        pageToken,
        readMask,
        requestSyncToken,
        sources,
        syncToken,
    )?;
    people_other_contacts_list_execute(builder)
}

/// GET v1/otherContacts:search
/// Provides a list of contacts in the authenticated user's other contacts that matches the search query. The query matches on a contact's names, `emailAddresses`, and `phoneNumbers` fields that are from the OTHER_CONTACT source. **IMPORTANT**: Before searching, clients should send a warmup request with an empty query to update the cache. See <https://developers.google.`com/people/v1/other-contacts`#search_the_users_other_contacts>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_other_contacts_search_execute()` to send, or `people_other_contacts_search` for simplest API.

pub fn people_other_contacts_search_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    query: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/otherContacts:search",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "query",
            query
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/otherContacts:search
/// Provides a list of contacts in the authenticated user's other contacts that matches the search query. The query matches on a contact's names, `emailAddresses`, and `phoneNumbers` fields that are from the OTHER_CONTACT source. **IMPORTANT**: Before searching, clients should send a warmup request with an empty query to update the cache. See <https://developers.google.`com/people/v1/other-contacts`#search_the_users_other_contacts>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_other_contacts_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_other_contacts_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_other_contacts_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/otherContacts:search
/// Provides a list of contacts in the authenticated user's other contacts that matches the search query. The query matches on a contact's names, `emailAddresses`, and `phoneNumbers` fields that are from the OTHER_CONTACT source. **IMPORTANT**: Before searching, clients should send a warmup request with an empty query to update the cache. See <https://developers.google.`com/people/v1/other-contacts`#search_the_users_other_contacts>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_other_contacts_search_builder()` + `people_other_contacts_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_other_contacts_search(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    query: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_other_contacts_search_builder(client, pageSize, query, readMask)?;
    people_other_contacts_search_execute(builder)
}

/// GET v1/people:batchCreateContacts
/// Create a batch of new contacts and return the PersonResponses for the newly Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_batch_create_contacts_execute()` to send, or `people_people_batch_create_contacts` for simplest API.

pub fn people_people_batch_create_contacts_builder(
    client: &SimpleHttpClient,
    body: &BatchCreateContactsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/people:batchCreateContacts",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/people:batchCreateContacts
/// Create a batch of new contacts and return the PersonResponses for the newly Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_people_batch_create_contacts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_people_batch_create_contacts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_batch_create_contacts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchCreateContactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchCreateContactsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/people:batchCreateContacts
/// Create a batch of new contacts and return the PersonResponses for the newly Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_batch_create_contacts_builder()` + `people_people_batch_create_contacts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_batch_create_contacts(
    client: &SimpleHttpClient,
    body: &BatchCreateContactsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchCreateContactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_batch_create_contacts_builder(client, body)?;
    people_people_batch_create_contacts_execute(builder)
}

/// GET v1/people:batchDeleteContacts
/// Delete a batch of contacts. Any non-contact data will not be deleted. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_batch_delete_contacts_execute()` to send, or `people_people_batch_delete_contacts` for simplest API.

pub fn people_people_batch_delete_contacts_builder(
    client: &SimpleHttpClient,
    body: &BatchDeleteContactsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/people:batchDeleteContacts",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/people:batchDeleteContacts
/// Delete a batch of contacts. Any non-contact data will not be deleted. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_people_batch_delete_contacts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_people_batch_delete_contacts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_batch_delete_contacts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/people:batchDeleteContacts
/// Delete a batch of contacts. Any non-contact data will not be deleted. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_batch_delete_contacts_builder()` + `people_people_batch_delete_contacts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_batch_delete_contacts(
    client: &SimpleHttpClient,
    body: &BatchDeleteContactsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = people_people_batch_delete_contacts_builder(client, body)?;
    people_people_batch_delete_contacts_execute(builder)
}

/// GET v1/people:batchUpdateContacts
/// Update a batch of contacts and return a map of resource names to PersonResponses for the updated contacts. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_batch_update_contacts_execute()` to send, or `people_people_batch_update_contacts` for simplest API.

pub fn people_people_batch_update_contacts_builder(
    client: &SimpleHttpClient,
    body: &BatchUpdateContactsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/people:batchUpdateContacts",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/people:batchUpdateContacts
/// Update a batch of contacts and return a map of resource names to PersonResponses for the updated contacts. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_people_batch_update_contacts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_people_batch_update_contacts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_batch_update_contacts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateContactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateContactsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/people:batchUpdateContacts
/// Update a batch of contacts and return a map of resource names to PersonResponses for the updated contacts. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_batch_update_contacts_builder()` + `people_people_batch_update_contacts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_batch_update_contacts(
    client: &SimpleHttpClient,
    body: &BatchUpdateContactsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateContactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_batch_update_contacts_builder(client, body)?;
    people_people_batch_update_contacts_execute(builder)
}

/// GET v1/people:createContact
/// Create a new contact and return the person resource for that contact. The request returns a 400 error if more than one field is specified on a field that is a singleton for contact sources: * biographies * birthdays * genders * names Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_create_contact_execute()` to send, or `people_people_create_contact` for simplest API.

pub fn people_people_create_contact_builder(
    client: &SimpleHttpClient,
    personFields: Option<&str>,
    sources: Option<&str>,
    body: &Person,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/people:createContact",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "personFields",
            personFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sources",
            sources
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/people:createContact
/// Create a new contact and return the person resource for that contact. The request returns a 400 error if more than one field is specified on a field that is a singleton for contact sources: * biographies * birthdays * genders * names Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_people_create_contact_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_people_create_contact_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_create_contact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/people:createContact
/// Create a new contact and return the person resource for that contact. The request returns a 400 error if more than one field is specified on a field that is a singleton for contact sources: * biographies * birthdays * genders * names Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_create_contact_builder()` + `people_people_create_contact_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_create_contact(
    client: &SimpleHttpClient,
    personFields: Option<&str>,
    sources: Option<&str>,
    body: &Person,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = people_people_create_contact_builder(client, personFields, sources, body)?;
    people_people_create_contact_execute(builder)
}

/// GET v1/{+resourceName}:deleteContact
/// Delete a contact person. Any non-contact data will not be deleted. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_delete_contact_execute()` to send, or `people_people_delete_contact` for simplest API.

pub fn people_people_delete_contact_builder(
    client: &SimpleHttpClient,
    resourceName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resourceName}:deleteContact",
        resourceName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resourceName}:deleteContact
/// Delete a contact person. Any non-contact data will not be deleted. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_people_delete_contact_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_people_delete_contact_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_delete_contact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resourceName}:deleteContact
/// Delete a contact person. Any non-contact data will not be deleted. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_delete_contact_builder()` + `people_people_delete_contact_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_delete_contact(
    client: &SimpleHttpClient,
    resourceName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = people_people_delete_contact_builder(client, resourceName)?;
    people_people_delete_contact_execute(builder)
}

/// GET v1/{+resourceName}:deleteContactPhoto
/// Delete a contact's photo. Mutate requests for the same user should be done sequentially to avoid // lock contention.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_delete_contact_photo_execute()` to send, or `people_people_delete_contact_photo` for simplest API.

pub fn people_people_delete_contact_photo_builder(
    client: &SimpleHttpClient,
    resourceName: &str,
    personFields: Option<&str>,
    sources: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resourceName}:deleteContactPhoto",
        resourceName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "personFields",
            personFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sources",
            sources
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+resourceName}:deleteContactPhoto
/// Delete a contact's photo. Mutate requests for the same user should be done sequentially to avoid // lock contention.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_people_delete_contact_photo_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_people_delete_contact_photo_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_delete_contact_photo_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeleteContactPhotoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteContactPhotoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resourceName}:deleteContactPhoto
/// Delete a contact's photo. Mutate requests for the same user should be done sequentially to avoid // lock contention.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_delete_contact_photo_builder()` + `people_people_delete_contact_photo_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_delete_contact_photo(
    client: &SimpleHttpClient,
    resourceName: &str,
    personFields: Option<&str>,
    sources: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeleteContactPhotoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        people_people_delete_contact_photo_builder(client, resourceName, personFields, sources)?;
    people_people_delete_contact_photo_execute(builder)
}

/// GET v1/{+resourceName}
/// Provides information about a person by specifying a resource name. Use `people/me` to indicate the authenticated user. The request returns a 400 error if '`personFields`' is not specified.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_get_execute()` to send, or `people_people_get` for simplest API.

pub fn people_people_get_builder(
    client: &SimpleHttpClient,
    resourceName: &str,
    personFields: Option<&str>,
    requestMask_includeField: Option<&str>,
    sources: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+resourceName}", resourceName,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "personFields",
            personFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestMask_includeField",
            requestMask_includeField
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sources",
            sources
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+resourceName}
/// Provides information about a person by specifying a resource name. Use `people/me` to indicate the authenticated user. The request returns a 400 error if '`personFields`' is not specified.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_people_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_people_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resourceName}
/// Provides information about a person by specifying a resource name. Use `people/me` to indicate the authenticated user. The request returns a 400 error if '`personFields`' is not specified.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_get_builder()` + `people_people_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_get(
    client: &SimpleHttpClient,
    resourceName: &str,
    personFields: Option<&str>,
    requestMask_includeField: Option<&str>,
    sources: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = people_people_get_builder(
        client,
        resourceName,
        personFields,
        requestMask_includeField,
        sources,
    )?;
    people_people_get_execute(builder)
}

/// GET v1/people:batchGet
/// Provides information about a list of specific people by specifying a list of requested resource names. Use `people/me` to indicate the authenticated user. The request returns a 400 error if '`personFields`' is not specified.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_get_batch_get_execute()` to send, or `people_people_get_batch_get` for simplest API.

pub fn people_people_get_batch_get_builder(
    client: &SimpleHttpClient,
    personFields: Option<&str>,
    requestMask_includeField: Option<&str>,
    resourceNames: Option<&str>,
    sources: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/people:batchGet",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "personFields",
            personFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestMask_includeField",
            requestMask_includeField
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "resourceNames",
            resourceNames
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sources",
            sources
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/people:batchGet
/// Provides information about a list of specific people by specifying a list of requested resource names. Use `people/me` to indicate the authenticated user. The request returns a 400 error if '`personFields`' is not specified.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_people_get_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_people_get_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_get_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetPeopleResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetPeopleResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/people:batchGet
/// Provides information about a list of specific people by specifying a list of requested resource names. Use `people/me` to indicate the authenticated user. The request returns a 400 error if '`personFields`' is not specified.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_get_batch_get_builder()` + `people_people_get_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_get_batch_get(
    client: &SimpleHttpClient,
    personFields: Option<&str>,
    requestMask_includeField: Option<&str>,
    resourceNames: Option<&str>,
    sources: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetPeopleResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_get_batch_get_builder(
        client,
        personFields,
        requestMask_includeField,
        resourceNames,
        sources,
    )?;
    people_people_get_batch_get_execute(builder)
}

/// GET v1/people:listDirectoryPeople
/// Provides a list of domain profiles and domain contacts in the authenticated user's domain directory. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the directory people that have changed](/`people/v1/directory`#list_the_directory_people_that_have_changed).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_list_directory_people_execute()` to send, or `people_people_list_directory_people` for simplest API.

pub fn people_people_list_directory_people_builder(
    client: &SimpleHttpClient,
    mergeSources: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
    requestSyncToken: Option<bool>,
    sources: Option<&str>,
    syncToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/people:listDirectoryPeople",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "mergeSources",
            mergeSources
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestSyncToken",
            requestSyncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sources",
            sources
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "syncToken",
            syncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/people:listDirectoryPeople
/// Provides a list of domain profiles and domain contacts in the authenticated user's domain directory. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the directory people that have changed](/`people/v1/directory`#list_the_directory_people_that_have_changed).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_people_list_directory_people_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_people_list_directory_people_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_list_directory_people_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDirectoryPeopleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDirectoryPeopleResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/people:listDirectoryPeople
/// Provides a list of domain profiles and domain contacts in the authenticated user's domain directory. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the directory people that have changed](/`people/v1/directory`#list_the_directory_people_that_have_changed).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_list_directory_people_builder()` + `people_people_list_directory_people_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_list_directory_people(
    client: &SimpleHttpClient,
    mergeSources: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
    requestSyncToken: Option<bool>,
    sources: Option<&str>,
    syncToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDirectoryPeopleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_list_directory_people_builder(
        client,
        mergeSources,
        pageSize,
        pageToken,
        readMask,
        requestSyncToken,
        sources,
        syncToken,
    )?;
    people_people_list_directory_people_execute(builder)
}

/// GET v1/people:searchContacts
/// Provides a list of contacts in the authenticated user's grouped contacts that matches the search query. The query matches on a contact's names, `nickNames`, `emailAddresses`, `phoneNumbers`, and organizations fields that are from the CONTACT source. **IMPORTANT**: Before searching, clients should send a warmup request with an empty query to update the cache. See <https://developers.google.`com/people/v1/contacts`#search_the_users_contacts>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_search_contacts_execute()` to send, or `people_people_search_contacts` for simplest API.

pub fn people_people_search_contacts_builder(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    query: Option<&str>,
    readMask: Option<&str>,
    sources: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/people:searchContacts",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "query",
            query
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sources",
            sources
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/people:searchContacts
/// Provides a list of contacts in the authenticated user's grouped contacts that matches the search query. The query matches on a contact's names, `nickNames`, `emailAddresses`, `phoneNumbers`, and organizations fields that are from the CONTACT source. **IMPORTANT**: Before searching, clients should send a warmup request with an empty query to update the cache. See <https://developers.google.`com/people/v1/contacts`#search_the_users_contacts>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_people_search_contacts_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_people_search_contacts_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_search_contacts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/people:searchContacts
/// Provides a list of contacts in the authenticated user's grouped contacts that matches the search query. The query matches on a contact's names, `nickNames`, `emailAddresses`, `phoneNumbers`, and organizations fields that are from the CONTACT source. **IMPORTANT**: Before searching, clients should send a warmup request with an empty query to update the cache. See <https://developers.google.`com/people/v1/contacts`#search_the_users_contacts>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_search_contacts_builder()` + `people_people_search_contacts_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_search_contacts(
    client: &SimpleHttpClient,
    pageSize: Option<i32>,
    query: Option<&str>,
    readMask: Option<&str>,
    sources: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        people_people_search_contacts_builder(client, pageSize, query, readMask, sources)?;
    people_people_search_contacts_execute(builder)
}

/// GET v1/people:searchDirectoryPeople
/// Provides a list of domain profiles and domain contacts in the authenticated user's domain directory that match the search query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_search_directory_people_execute()` to send, or `people_people_search_directory_people` for simplest API.

pub fn people_people_search_directory_people_builder(
    client: &SimpleHttpClient,
    mergeSources: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
    readMask: Option<&str>,
    sources: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/people:searchDirectoryPeople",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "mergeSources",
            mergeSources
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "query",
            query
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "readMask",
            readMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sources",
            sources
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/people:searchDirectoryPeople
/// Provides a list of domain profiles and domain contacts in the authenticated user's domain directory that match the search query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_people_search_directory_people_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_people_search_directory_people_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_search_directory_people_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchDirectoryPeopleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchDirectoryPeopleResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/people:searchDirectoryPeople
/// Provides a list of domain profiles and domain contacts in the authenticated user's domain directory that match the search query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_search_directory_people_builder()` + `people_people_search_directory_people_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_search_directory_people(
    client: &SimpleHttpClient,
    mergeSources: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
    readMask: Option<&str>,
    sources: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchDirectoryPeopleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_search_directory_people_builder(
        client,
        mergeSources,
        pageSize,
        pageToken,
        query,
        readMask,
        sources,
    )?;
    people_people_search_directory_people_execute(builder)
}

/// GET v1/{+resourceName}:updateContact
/// Update contact data for an existing contact person. Any non-contact data will not be modified. Any non-contact data in the person to update will be ignored. All fields specified in the update_mask will be replaced. The server returns a 400 error if person.metadata.sources is not specified for the contact to be updated or if there is no contact source. The server returns a 400 error with reason "`failedPrecondition`" if person.metadata.sources.etag is different than the contact's etag, which indicates the contact has changed since its data was read. Clients should get the latest person and merge their updates into the latest person. If making sequential updates to the same person, the etag from the `updateContact` response should be used to avoid failures. The server returns a 400 error if memberships are being updated and there are no contact group memberships specified on the person. The server returns a 400 error if more than one field is specified on a field that is a singleton for contact sources: * biographies * birthdays * genders * names Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_update_contact_execute()` to send, or `people_people_update_contact` for simplest API.

pub fn people_people_update_contact_builder(
    client: &SimpleHttpClient,
    resourceName: &str,
    personFields: Option<&str>,
    sources: Option<&str>,
    updatePersonFields: Option<&str>,
    body: &Person,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resourceName}:updateContact",
        resourceName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "personFields",
            personFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sources",
            sources
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updatePersonFields",
            updatePersonFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resourceName}:updateContact
/// Update contact data for an existing contact person. Any non-contact data will not be modified. Any non-contact data in the person to update will be ignored. All fields specified in the update_mask will be replaced. The server returns a 400 error if person.metadata.sources is not specified for the contact to be updated or if there is no contact source. The server returns a 400 error with reason "`failedPrecondition`" if person.metadata.sources.etag is different than the contact's etag, which indicates the contact has changed since its data was read. Clients should get the latest person and merge their updates into the latest person. If making sequential updates to the same person, the etag from the `updateContact` response should be used to avoid failures. The server returns a 400 error if memberships are being updated and there are no contact group memberships specified on the person. The server returns a 400 error if more than one field is specified on a field that is a singleton for contact sources: * biographies * birthdays * genders * names Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_people_update_contact_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_people_update_contact_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_update_contact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resourceName}:updateContact
/// Update contact data for an existing contact person. Any non-contact data will not be modified. Any non-contact data in the person to update will be ignored. All fields specified in the update_mask will be replaced. The server returns a 400 error if person.metadata.sources is not specified for the contact to be updated or if there is no contact source. The server returns a 400 error with reason "`failedPrecondition`" if person.metadata.sources.etag is different than the contact's etag, which indicates the contact has changed since its data was read. Clients should get the latest person and merge their updates into the latest person. If making sequential updates to the same person, the etag from the `updateContact` response should be used to avoid failures. The server returns a 400 error if memberships are being updated and there are no contact group memberships specified on the person. The server returns a 400 error if more than one field is specified on a field that is a singleton for contact sources: * biographies * birthdays * genders * names Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_update_contact_builder()` + `people_people_update_contact_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_update_contact(
    client: &SimpleHttpClient,
    resourceName: &str,
    personFields: Option<&str>,
    sources: Option<&str>,
    updatePersonFields: Option<&str>,
    body: &Person,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = people_people_update_contact_builder(
        client,
        resourceName,
        personFields,
        sources,
        updatePersonFields,
        body,
    )?;
    people_people_update_contact_execute(builder)
}

/// GET v1/{+resourceName}:updateContactPhoto
/// Update a contact's photo. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_update_contact_photo_execute()` to send, or `people_people_update_contact_photo` for simplest API.

pub fn people_people_update_contact_photo_builder(
    client: &SimpleHttpClient,
    resourceName: &str,
    body: &UpdateContactPhotoRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resourceName}:updateContactPhoto",
        resourceName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+resourceName}:updateContactPhoto
/// Update a contact's photo. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_people_update_contact_photo_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_people_update_contact_photo_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_update_contact_photo_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateContactPhotoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateContactPhotoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resourceName}:updateContactPhoto
/// Update a contact's photo. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_update_contact_photo_builder()` + `people_people_update_contact_photo_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_update_contact_photo(
    client: &SimpleHttpClient,
    resourceName: &str,
    body: &UpdateContactPhotoRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateContactPhotoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_update_contact_photo_builder(client, resourceName, body)?;
    people_people_update_contact_photo_execute(builder)
}

/// GET v1/{+resourceName}/connections
/// Provides a list of the authenticated user's contacts. Sync tokens expire 7 days after the full sync. A request with an expired sync token will get an error with an [google.rpc.ErrorInfo](<https://cloud.google.`com/apis/design/errors`#error_info>) with reason "EXPIRED_SYNC_TOKEN". In the case of such an error clients should make a full sync request without a sync_token. The first page of a full sync request has an additional quota. If the quota is exceeded, a 429 error will be returned. This quota is fixed and can not be increased. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the user's contacts that have changed](/`people/v1/contacts`#list_the_users_contacts_that_have_changed).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_connections_list_execute()` to send, or `people_people_connections_list` for simplest API.

pub fn people_people_connections_list_builder(
    client: &SimpleHttpClient,
    resourceName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    personFields: Option<&str>,
    requestMask_includeField: Option<&str>,
    requestSyncToken: Option<bool>,
    sortOrder: Option<&str>,
    sources: Option<&str>,
    syncToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+resourceName}/connections",
        resourceName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "personFields",
            personFields
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestMask_includeField",
            requestMask_includeField
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestSyncToken",
            requestSyncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sortOrder",
            sortOrder
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sources",
            sources
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "syncToken",
            syncToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+resourceName}/connections
/// Provides a list of the authenticated user's contacts. Sync tokens expire 7 days after the full sync. A request with an expired sync token will get an error with an [google.rpc.ErrorInfo](<https://cloud.google.`com/apis/design/errors`#error_info>) with reason "EXPIRED_SYNC_TOKEN". In the case of such an error clients should make a full sync request without a sync_token. The first page of a full sync request has an additional quota. If the quota is exceeded, a 429 error will be returned. This quota is fixed and can not be increased. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the user's contacts that have changed](/`people/v1/contacts`#list_the_users_contacts_that_have_changed).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `people_people_connections_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from people_people_connections_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_connections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConnectionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConnectionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+resourceName}/connections
/// Provides a list of the authenticated user's contacts. Sync tokens expire 7 days after the full sync. A request with an expired sync token will get an error with an [google.rpc.ErrorInfo](<https://cloud.google.`com/apis/design/errors`#error_info>) with reason "EXPIRED_SYNC_TOKEN". In the case of such an error clients should make a full sync request without a sync_token. The first page of a full sync request has an additional quota. If the quota is exceeded, a 429 error will be returned. This quota is fixed and can not be increased. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the user's contacts that have changed](/`people/v1/contacts`#list_the_users_contacts_that_have_changed).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_connections_list_builder()` + `people_people_connections_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_connections_list(
    client: &SimpleHttpClient,
    resourceName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    personFields: Option<&str>,
    requestMask_includeField: Option<&str>,
    requestSyncToken: Option<bool>,
    sortOrder: Option<&str>,
    sources: Option<&str>,
    syncToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConnectionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_connections_list_builder(
        client,
        resourceName,
        pageSize,
        pageToken,
        personFields,
        requestMask_includeField,
        requestSyncToken,
        sortOrder,
        sources,
        syncToken,
    )?;
    people_people_connections_list_execute(builder)
}
