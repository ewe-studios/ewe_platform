//! Auto-generated API clients for gcp/people.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/contactGroups:batchGet
/// Get a list of contact groups owned by the authenticated user by specifying a list of contact group resource names.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_contact_groups_batch_get_execute()` to send, or `people_contact_groups_batch_get` for simplest API.

pub fn people_contact_groups_batch_get_builder<R>(
    client: &SimpleHttpClient<R>,
    groupFields: &Option<Option<String>>,
    maxMembers: &Option<Option<String>>,
    resourceNames: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://people.googleapis.com/v1/contactGroups:batchGet",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = groupFields.as_ref() {
        query_parts.push(format!("groupFields={}", val));
    }
    if let Some(val) = maxMembers.as_ref() {
        query_parts.push(format!("maxMembers={}", val));
    }
    if let Some(val) = resourceNames.as_ref() {
        query_parts.push(format!("resourceNames={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/contactGroups:batchGet
/// Get a list of contact groups owned by the authenticated user by specifying a list of contact group resource names.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_contact_groups_batch_get_execute()` or `people_contact_groups_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_contact_groups_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchGetContactGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetContactGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/contactGroups:batchGet
/// Get a list of contact groups owned by the authenticated user by specifying a list of contact group resource names.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_contact_groups_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_contact_groups_batch_get_task()`.
/// For the simplest API, use `people_contact_groups_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_contact_groups_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_contact_groups_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetContactGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = people_contact_groups_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_contact_groups_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeopleContactGroupsBatchGetArgs {
    /// Query parameter: groupFields
    pub groupFields: Option<Option<String>>,
    /// Query parameter: maxMembers
    pub maxMembers: Option<Option<String>>,
    /// Query parameter: resourceNames
    pub resourceNames: Option<Option<String>>,
}

/// GET v1/contactGroups:batchGet
/// Get a list of contact groups owned by the authenticated user by specifying a list of contact group resource names.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_contact_groups_batch_get_builder()` + `people_contact_groups_batch_get_execute()`.
/// For task-level control, use `people_contact_groups_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_batch_get(
    client: &SimpleHttpClient,
    args: &PeopleContactGroupsBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetContactGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = people_contact_groups_batch_get_builder(
        client,
        &args.groupFields,
        &args.maxMembers,
        &args.resourceNames,
    )?;
    people_contact_groups_batch_get_execute(builder)
}

/// POST v1/contactGroups
/// Create a new contact group owned by the authenticated user. Created contact group names must be unique to the users contact groups. Attempting to create a group with a duplicate name will return a HTTP 409 error. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_contact_groups_create_execute()` to send, or `people_contact_groups_create` for simplest API.

pub fn people_contact_groups_create_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://people.googleapis.com/v1/contactGroups",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/contactGroups
/// Create a new contact group owned by the authenticated user. Created contact group names must be unique to the users contact groups. Attempting to create a group with a duplicate name will return a HTTP 409 error. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_contact_groups_create_execute()` or `people_contact_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_contact_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ContactGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ContactGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/contactGroups
/// Create a new contact group owned by the authenticated user. Created contact group names must be unique to the users contact groups. Attempting to create a group with a duplicate name will return a HTTP 409 error. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_contact_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_contact_groups_create_task()`.
/// For the simplest API, use `people_contact_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_contact_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_contact_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = people_contact_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/contactGroups
/// Create a new contact group owned by the authenticated user. Created contact group names must be unique to the users contact groups. Attempting to create a group with a duplicate name will return a HTTP 409 error. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_contact_groups_create_builder()` + `people_contact_groups_create_execute()`.
/// For task-level control, use `people_contact_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_create(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_contact_groups_create_builder(client)?;
    people_contact_groups_create_execute(builder)
}

/// DELETE v1/contactGroups/{contactGroupsId}
/// Delete an existing contact group owned by the authenticated user by specifying a contact group resource name. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_contact_groups_delete_execute()` to send, or `people_contact_groups_delete` for simplest API.

pub fn people_contact_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
    deleteContacts: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://people.googleapis.com/v1/contactGroups/{}",
        resourceName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = deleteContacts.as_ref() {
        query_parts.push(format!("deleteContacts={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/contactGroups/{contactGroupsId}
/// Delete an existing contact group owned by the authenticated user by specifying a contact group resource name. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_contact_groups_delete_execute()` or `people_contact_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_contact_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/contactGroups/{contactGroupsId}
/// Delete an existing contact group owned by the authenticated user by specifying a contact group resource name. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_contact_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_contact_groups_delete_task()`.
/// For the simplest API, use `people_contact_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_contact_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_contact_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = people_contact_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_contact_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeopleContactGroupsDeleteArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
    /// Query parameter: deleteContacts
    pub deleteContacts: Option<Option<String>>,
}

/// DELETE v1/contactGroups/{contactGroupsId}
/// Delete an existing contact group owned by the authenticated user by specifying a contact group resource name. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_contact_groups_delete_builder()` + `people_contact_groups_delete_execute()`.
/// For task-level control, use `people_contact_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_delete(
    client: &SimpleHttpClient,
    args: &PeopleContactGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        people_contact_groups_delete_builder(client, &args.resourceName, &args.deleteContacts)?;
    people_contact_groups_delete_execute(builder)
}

/// GET v1/contactGroups/{contactGroupsId}
/// Get a specific contact group owned by the authenticated user by specifying a contact group resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_contact_groups_get_execute()` to send, or `people_contact_groups_get` for simplest API.

pub fn people_contact_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
    groupFields: &Option<Option<String>>,
    maxMembers: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://people.googleapis.com/v1/contactGroups/{}",
        resourceName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = groupFields.as_ref() {
        query_parts.push(format!("groupFields={}", val));
    }
    if let Some(val) = maxMembers.as_ref() {
        query_parts.push(format!("maxMembers={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/contactGroups/{contactGroupsId}
/// Get a specific contact group owned by the authenticated user by specifying a contact group resource name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_contact_groups_get_execute()` or `people_contact_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_contact_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ContactGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ContactGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/contactGroups/{contactGroupsId}
/// Get a specific contact group owned by the authenticated user by specifying a contact group resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_contact_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_contact_groups_get_task()`.
/// For the simplest API, use `people_contact_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_contact_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_contact_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = people_contact_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_contact_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeopleContactGroupsGetArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
    /// Query parameter: groupFields
    pub groupFields: Option<Option<String>>,
    /// Query parameter: maxMembers
    pub maxMembers: Option<Option<String>>,
}

/// GET v1/contactGroups/{contactGroupsId}
/// Get a specific contact group owned by the authenticated user by specifying a contact group resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_contact_groups_get_builder()` + `people_contact_groups_get_execute()`.
/// For task-level control, use `people_contact_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_get(
    client: &SimpleHttpClient,
    args: &PeopleContactGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_contact_groups_get_builder(
        client,
        &args.resourceName,
        &args.groupFields,
        &args.maxMembers,
    )?;
    people_contact_groups_get_execute(builder)
}

/// GET v1/contactGroups
/// List all contact groups owned by the authenticated user. Members of the contact groups are not populated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_contact_groups_list_execute()` to send, or `people_contact_groups_list` for simplest API.

pub fn people_contact_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    groupFields: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    syncToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://people.googleapis.com/v1/contactGroups",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = groupFields.as_ref() {
        query_parts.push(format!("groupFields={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = syncToken.as_ref() {
        query_parts.push(format!("syncToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/contactGroups
/// List all contact groups owned by the authenticated user. Members of the contact groups are not populated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_contact_groups_list_execute()` or `people_contact_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_contact_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListContactGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListContactGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/contactGroups
/// List all contact groups owned by the authenticated user. Members of the contact groups are not populated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_contact_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_contact_groups_list_task()`.
/// For the simplest API, use `people_contact_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_contact_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_contact_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListContactGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = people_contact_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_contact_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeopleContactGroupsListArgs {
    /// Query parameter: groupFields
    pub groupFields: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: syncToken
    pub syncToken: Option<Option<String>>,
}

/// GET v1/contactGroups
/// List all contact groups owned by the authenticated user. Members of the contact groups are not populated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_contact_groups_list_builder()` + `people_contact_groups_list_execute()`.
/// For task-level control, use `people_contact_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_list(
    client: &SimpleHttpClient,
    args: &PeopleContactGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListContactGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_contact_groups_list_builder(
        client,
        &args.groupFields,
        &args.pageSize,
        &args.pageToken,
        &args.syncToken,
    )?;
    people_contact_groups_list_execute(builder)
}

/// PUT v1/contactGroups/{contactGroupsId}
/// Update the name of an existing contact group owned by the authenticated user. Updated contact group names must be unique to the users contact groups. Attempting to create a group with a duplicate name will return a HTTP 409 error. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_contact_groups_update_execute()` to send, or `people_contact_groups_update` for simplest API.

pub fn people_contact_groups_update_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://people.googleapis.com/v1/contactGroups/{}",
        resourceName,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/contactGroups/{contactGroupsId}
/// Update the name of an existing contact group owned by the authenticated user. Updated contact group names must be unique to the users contact groups. Attempting to create a group with a duplicate name will return a HTTP 409 error. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_contact_groups_update_execute()` or `people_contact_groups_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_contact_groups_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ContactGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ContactGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/contactGroups/{contactGroupsId}
/// Update the name of an existing contact group owned by the authenticated user. Updated contact group names must be unique to the users contact groups. Attempting to create a group with a duplicate name will return a HTTP 409 error. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_contact_groups_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_contact_groups_update_task()`.
/// For the simplest API, use `people_contact_groups_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_contact_groups_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_contact_groups_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = people_contact_groups_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_contact_groups_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeopleContactGroupsUpdateArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
}

/// PUT v1/contactGroups/{contactGroupsId}
/// Update the name of an existing contact group owned by the authenticated user. Updated contact group names must be unique to the users contact groups. Attempting to create a group with a duplicate name will return a HTTP 409 error. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_contact_groups_update_builder()` + `people_contact_groups_update_execute()`.
/// For task-level control, use `people_contact_groups_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_update(
    client: &SimpleHttpClient,
    args: &PeopleContactGroupsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ContactGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_contact_groups_update_builder(client, &args.resourceName)?;
    people_contact_groups_update_execute(builder)
}

/// POST v1/contactGroups/{contactGroupsId}/members:modify
/// Modify the members of a contact group owned by the authenticated user. The only system contact groups that can have members added are contactG`roups/`myContacts`` and contactG`roups/starred`. Other system contact groups are deprecated and can only have contacts removed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_contact_groups_members_modify_execute()` to send, or `people_contact_groups_members_modify` for simplest API.

pub fn people_contact_groups_members_modify_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://people.googleapis.com/v1/contactGroups/{}/members:modify",
        resourceName,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/contactGroups/{contactGroupsId}/members:modify
/// Modify the members of a contact group owned by the authenticated user. The only system contact groups that can have members added are contactG`roups/`myContacts`` and contactG`roups/starred`. Other system contact groups are deprecated and can only have contacts removed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_contact_groups_members_modify_execute()` or `people_contact_groups_members_modify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_contact_groups_members_modify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_members_modify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ModifyContactGroupMembersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ModifyContactGroupMembersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/contactGroups/{contactGroupsId}/members:modify
/// Modify the members of a contact group owned by the authenticated user. The only system contact groups that can have members added are contactG`roups/`myContacts`` and contactG`roups/starred`. Other system contact groups are deprecated and can only have contacts removed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_contact_groups_members_modify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_contact_groups_members_modify_task()`.
/// For the simplest API, use `people_contact_groups_members_modify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_contact_groups_members_modify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_contact_groups_members_modify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ModifyContactGroupMembersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = people_contact_groups_members_modify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_contact_groups_members_modify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeopleContactGroupsMembersModifyArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
}

/// POST v1/contactGroups/{contactGroupsId}/members:modify
/// Modify the members of a contact group owned by the authenticated user. The only system contact groups that can have members added are contactG`roups/`myContacts`` and contactG`roups/starred`. Other system contact groups are deprecated and can only have contacts removed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_contact_groups_members_modify_builder()` + `people_contact_groups_members_modify_execute()`.
/// For task-level control, use `people_contact_groups_members_modify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_contact_groups_members_modify(
    client: &SimpleHttpClient,
    args: &PeopleContactGroupsMembersModifyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ModifyContactGroupMembersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = people_contact_groups_members_modify_builder(client, &args.resourceName)?;
    people_contact_groups_members_modify_execute(builder)
}

/// POST v1/otherContacts/{otherContactsId}:copyOtherContactToMyContactsGroup
/// Copies an "Other contact" to a new contact in the user''s "`myContacts`" group Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_other_contacts_copy_other_contact_to_my_contacts_group_execute()` to send, or `people_other_contacts_copy_other_contact_to_my_contacts_group` for simplest API.

pub fn people_other_contacts_copy_other_contact_to_my_contacts_group_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://people.googleapis.com/v1/otherContacts/{}:copyOtherContactToMyContactsGroup",
        resourceName,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/otherContacts/{otherContactsId}:copyOtherContactToMyContactsGroup
/// Copies an "Other contact" to a new contact in the user''s "`myContacts`" group Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_other_contacts_copy_other_contact_to_my_contacts_group_execute()` or `people_other_contacts_copy_other_contact_to_my_contacts_group`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_other_contacts_copy_other_contact_to_my_contacts_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_other_contacts_copy_other_contact_to_my_contacts_group_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Person>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/otherContacts/{otherContactsId}:copyOtherContactToMyContactsGroup
/// Copies an "Other contact" to a new contact in the user''s "`myContacts`" group Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_other_contacts_copy_other_contact_to_my_contacts_group_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_other_contacts_copy_other_contact_to_my_contacts_group_task()`.
/// For the simplest API, use `people_other_contacts_copy_other_contact_to_my_contacts_group()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_other_contacts_copy_other_contact_to_my_contacts_group_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_other_contacts_copy_other_contact_to_my_contacts_group_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = people_other_contacts_copy_other_contact_to_my_contacts_group_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_other_contacts_copy_other_contact_to_my_contacts_group`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeopleOtherContactsCopyOtherContactToMyContactsGroupArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
}

/// POST v1/otherContacts/{otherContactsId}:copyOtherContactToMyContactsGroup
/// Copies an "Other contact" to a new contact in the user''s "`myContacts`" group Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_other_contacts_copy_other_contact_to_my_contacts_group_builder()` + `people_other_contacts_copy_other_contact_to_my_contacts_group_execute()`.
/// For task-level control, use `people_other_contacts_copy_other_contact_to_my_contacts_group_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_other_contacts_copy_other_contact_to_my_contacts_group(
    client: &SimpleHttpClient,
    args: &PeopleOtherContactsCopyOtherContactToMyContactsGroupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = people_other_contacts_copy_other_contact_to_my_contacts_group_builder(
        client,
        &args.resourceName,
    )?;
    people_other_contacts_copy_other_contact_to_my_contacts_group_execute(builder)
}

/// GET v1/otherContacts
/// List all "Other contacts", that is contacts that are not in a contact group. "Other contacts" are typically auto created contacts from interactions. Sync tokens expire 7 days after the full sync. A request with an expired sync token will get an error with an [google.rpc.ErrorInfo](<https://cloud.google.`com/apis/design/errors`#error_info>) with reason "EXPIRED_SYNC_TOKEN". In the case of such an error clients should make a full sync request without a sync_token. The first page of a full sync request has an additional quota. If the quota is exceeded, a 429 error will be returned. This quota is fixed and can not be increased. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the user''s other contacts that have changed](/`people/v1/other-contacts`#list_the_users_other_contacts_that_have_changed).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_other_contacts_list_execute()` to send, or `people_other_contacts_list` for simplest API.

pub fn people_other_contacts_list_builder<R>(
    client: &SimpleHttpClient<R>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
    requestSyncToken: &Option<Option<String>>,
    sources: &Option<Option<String>>,
    syncToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://people.googleapis.com/v1/otherContacts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }
    if let Some(val) = requestSyncToken.as_ref() {
        query_parts.push(format!("requestSyncToken={}", val));
    }
    if let Some(val) = sources.as_ref() {
        query_parts.push(format!("sources={}", val));
    }
    if let Some(val) = syncToken.as_ref() {
        query_parts.push(format!("syncToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/otherContacts
/// List all "Other contacts", that is contacts that are not in a contact group. "Other contacts" are typically auto created contacts from interactions. Sync tokens expire 7 days after the full sync. A request with an expired sync token will get an error with an [google.rpc.ErrorInfo](<https://cloud.google.`com/apis/design/errors`#error_info>) with reason "EXPIRED_SYNC_TOKEN". In the case of such an error clients should make a full sync request without a sync_token. The first page of a full sync request has an additional quota. If the quota is exceeded, a 429 error will be returned. This quota is fixed and can not be increased. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the user''s other contacts that have changed](/`people/v1/other-contacts`#list_the_users_other_contacts_that_have_changed).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_other_contacts_list_execute()` or `people_other_contacts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_other_contacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_other_contacts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOtherContactsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOtherContactsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/otherContacts
/// List all "Other contacts", that is contacts that are not in a contact group. "Other contacts" are typically auto created contacts from interactions. Sync tokens expire 7 days after the full sync. A request with an expired sync token will get an error with an [google.rpc.ErrorInfo](<https://cloud.google.`com/apis/design/errors`#error_info>) with reason "EXPIRED_SYNC_TOKEN". In the case of such an error clients should make a full sync request without a sync_token. The first page of a full sync request has an additional quota. If the quota is exceeded, a 429 error will be returned. This quota is fixed and can not be increased. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the user''s other contacts that have changed](/`people/v1/other-contacts`#list_the_users_other_contacts_that_have_changed).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_other_contacts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_other_contacts_list_task()`.
/// For the simplest API, use `people_other_contacts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_other_contacts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_other_contacts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOtherContactsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = people_other_contacts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_other_contacts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeopleOtherContactsListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
    /// Query parameter: requestSyncToken
    pub requestSyncToken: Option<Option<String>>,
    /// Query parameter: sources
    pub sources: Option<Option<String>>,
    /// Query parameter: syncToken
    pub syncToken: Option<Option<String>>,
}

/// GET v1/otherContacts
/// List all "Other contacts", that is contacts that are not in a contact group. "Other contacts" are typically auto created contacts from interactions. Sync tokens expire 7 days after the full sync. A request with an expired sync token will get an error with an [google.rpc.ErrorInfo](<https://cloud.google.`com/apis/design/errors`#error_info>) with reason "EXPIRED_SYNC_TOKEN". In the case of such an error clients should make a full sync request without a sync_token. The first page of a full sync request has an additional quota. If the quota is exceeded, a 429 error will be returned. This quota is fixed and can not be increased. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the user''s other contacts that have changed](/`people/v1/other-contacts`#list_the_users_other_contacts_that_have_changed).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_other_contacts_list_builder()` + `people_other_contacts_list_execute()`.
/// For task-level control, use `people_other_contacts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_other_contacts_list(
    client: &SimpleHttpClient,
    args: &PeopleOtherContactsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOtherContactsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_other_contacts_list_builder(
        client,
        &args.pageSize,
        &args.pageToken,
        &args.readMask,
        &args.requestSyncToken,
        &args.sources,
        &args.syncToken,
    )?;
    people_other_contacts_list_execute(builder)
}

/// GET v1/otherContacts:search
/// Provides a list of contacts in the authenticated user''s other contacts that matches the search query. The query matches on a contact''s names, `emailAddresses`, and `phoneNumbers` fields that are from the OTHER_CONTACT source. **IMPORTANT**: Before searching, clients should send a warmup request with an empty query to update the cache. See <https://developers.google.`com/people/v1/other-contacts`#search_the_users_other_contacts>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_other_contacts_search_execute()` to send, or `people_other_contacts_search` for simplest API.

pub fn people_other_contacts_search_builder<R>(
    client: &SimpleHttpClient<R>,
    pageSize: &Option<Option<String>>,
    query: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://people.googleapis.com/v1/otherContacts:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/otherContacts:search
/// Provides a list of contacts in the authenticated user''s other contacts that matches the search query. The query matches on a contact''s names, `emailAddresses`, and `phoneNumbers` fields that are from the OTHER_CONTACT source. **IMPORTANT**: Before searching, clients should send a warmup request with an empty query to update the cache. See <https://developers.google.`com/people/v1/other-contacts`#search_the_users_other_contacts>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_other_contacts_search_execute()` or `people_other_contacts_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_other_contacts_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_other_contacts_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/otherContacts:search
/// Provides a list of contacts in the authenticated user''s other contacts that matches the search query. The query matches on a contact''s names, `emailAddresses`, and `phoneNumbers` fields that are from the OTHER_CONTACT source. **IMPORTANT**: Before searching, clients should send a warmup request with an empty query to update the cache. See <https://developers.google.`com/people/v1/other-contacts`#search_the_users_other_contacts>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_other_contacts_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_other_contacts_search_task()`.
/// For the simplest API, use `people_other_contacts_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_other_contacts_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_other_contacts_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = people_other_contacts_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_other_contacts_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeopleOtherContactsSearchArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: query
    pub query: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
}

/// GET v1/otherContacts:search
/// Provides a list of contacts in the authenticated user''s other contacts that matches the search query. The query matches on a contact''s names, `emailAddresses`, and `phoneNumbers` fields that are from the OTHER_CONTACT source. **IMPORTANT**: Before searching, clients should send a warmup request with an empty query to update the cache. See <https://developers.google.`com/people/v1/other-contacts`#search_the_users_other_contacts>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_other_contacts_search_builder()` + `people_other_contacts_search_execute()`.
/// For task-level control, use `people_other_contacts_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_other_contacts_search(
    client: &SimpleHttpClient,
    args: &PeopleOtherContactsSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        people_other_contacts_search_builder(client, &args.pageSize, &args.query, &args.readMask)?;
    people_other_contacts_search_execute(builder)
}

/// POST v1/people:batchCreateContacts
/// Create a batch of new contacts and return the PersonResponses for the newly Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_batch_create_contacts_execute()` to send, or `people_people_batch_create_contacts` for simplest API.

pub fn people_people_batch_create_contacts_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://people.googleapis.com/v1/people:batchCreateContacts",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/people:batchCreateContacts
/// Create a batch of new contacts and return the PersonResponses for the newly Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_people_batch_create_contacts_execute()` or `people_people_batch_create_contacts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_batch_create_contacts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_batch_create_contacts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchCreateContactsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchCreateContactsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/people:batchCreateContacts
/// Create a batch of new contacts and return the PersonResponses for the newly Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_people_batch_create_contacts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_people_batch_create_contacts_task()`.
/// For the simplest API, use `people_people_batch_create_contacts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_batch_create_contacts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_people_batch_create_contacts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchCreateContactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = people_people_batch_create_contacts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/people:batchCreateContacts
/// Create a batch of new contacts and return the PersonResponses for the newly Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_batch_create_contacts_builder()` + `people_people_batch_create_contacts_execute()`.
/// For task-level control, use `people_people_batch_create_contacts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_batch_create_contacts(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchCreateContactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_batch_create_contacts_builder(client)?;
    people_people_batch_create_contacts_execute(builder)
}

/// POST v1/people:batchDeleteContacts
/// Delete a batch of contacts. Any non-contact data will not be deleted. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_batch_delete_contacts_execute()` to send, or `people_people_batch_delete_contacts` for simplest API.

pub fn people_people_batch_delete_contacts_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://people.googleapis.com/v1/people:batchDeleteContacts",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/people:batchDeleteContacts
/// Delete a batch of contacts. Any non-contact data will not be deleted. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_people_batch_delete_contacts_execute()` or `people_people_batch_delete_contacts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_batch_delete_contacts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_batch_delete_contacts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/people:batchDeleteContacts
/// Delete a batch of contacts. Any non-contact data will not be deleted. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_people_batch_delete_contacts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_people_batch_delete_contacts_task()`.
/// For the simplest API, use `people_people_batch_delete_contacts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_batch_delete_contacts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_people_batch_delete_contacts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = people_people_batch_delete_contacts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/people:batchDeleteContacts
/// Delete a batch of contacts. Any non-contact data will not be deleted. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_batch_delete_contacts_builder()` + `people_people_batch_delete_contacts_execute()`.
/// For task-level control, use `people_people_batch_delete_contacts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_batch_delete_contacts(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = people_people_batch_delete_contacts_builder(client)?;
    people_people_batch_delete_contacts_execute(builder)
}

/// POST v1/people:batchUpdateContacts
/// Update a batch of contacts and return a map of resource names to PersonResponses for the updated contacts. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_batch_update_contacts_execute()` to send, or `people_people_batch_update_contacts` for simplest API.

pub fn people_people_batch_update_contacts_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://people.googleapis.com/v1/people:batchUpdateContacts",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/people:batchUpdateContacts
/// Update a batch of contacts and return a map of resource names to PersonResponses for the updated contacts. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_people_batch_update_contacts_execute()` or `people_people_batch_update_contacts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_batch_update_contacts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_batch_update_contacts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchUpdateContactsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateContactsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/people:batchUpdateContacts
/// Update a batch of contacts and return a map of resource names to PersonResponses for the updated contacts. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_people_batch_update_contacts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_people_batch_update_contacts_task()`.
/// For the simplest API, use `people_people_batch_update_contacts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_batch_update_contacts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_people_batch_update_contacts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateContactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = people_people_batch_update_contacts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/people:batchUpdateContacts
/// Update a batch of contacts and return a map of resource names to PersonResponses for the updated contacts. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_batch_update_contacts_builder()` + `people_people_batch_update_contacts_execute()`.
/// For task-level control, use `people_people_batch_update_contacts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_batch_update_contacts(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateContactsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_batch_update_contacts_builder(client)?;
    people_people_batch_update_contacts_execute(builder)
}

/// POST v1/people:createContact
/// Create a new contact and return the person resource for that contact. The request returns a 400 error if more than one field is specified on a field that is a singleton for contact sources: * biographies * birthdays * genders * names Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_create_contact_execute()` to send, or `people_people_create_contact` for simplest API.

pub fn people_people_create_contact_builder<R>(
    client: &SimpleHttpClient<R>,
    personFields: &Option<Option<String>>,
    sources: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://people.googleapis.com/v1/people:createContact",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = personFields.as_ref() {
        query_parts.push(format!("personFields={}", val));
    }
    if let Some(val) = sources.as_ref() {
        query_parts.push(format!("sources={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/people:createContact
/// Create a new contact and return the person resource for that contact. The request returns a 400 error if more than one field is specified on a field that is a singleton for contact sources: * biographies * birthdays * genders * names Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_people_create_contact_execute()` or `people_people_create_contact`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_create_contact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_create_contact_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Person>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/people:createContact
/// Create a new contact and return the person resource for that contact. The request returns a 400 error if more than one field is specified on a field that is a singleton for contact sources: * biographies * birthdays * genders * names Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_people_create_contact_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_people_create_contact_task()`.
/// For the simplest API, use `people_people_create_contact()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_create_contact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_people_create_contact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = people_people_create_contact_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_people_create_contact`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeoplePeopleCreateContactArgs {
    /// Query parameter: personFields
    pub personFields: Option<Option<String>>,
    /// Query parameter: sources
    pub sources: Option<Option<String>>,
}

/// POST v1/people:createContact
/// Create a new contact and return the person resource for that contact. The request returns a 400 error if more than one field is specified on a field that is a singleton for contact sources: * biographies * birthdays * genders * names Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_create_contact_builder()` + `people_people_create_contact_execute()`.
/// For task-level control, use `people_people_create_contact_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_create_contact(
    client: &SimpleHttpClient,
    args: &PeoplePeopleCreateContactArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = people_people_create_contact_builder(client, &args.personFields, &args.sources)?;
    people_people_create_contact_execute(builder)
}

/// DELETE v1/people/{peopleId}:deleteContact
/// Delete a contact person. Any non-contact data will not be deleted. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_delete_contact_execute()` to send, or `people_people_delete_contact` for simplest API.

pub fn people_people_delete_contact_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://people.googleapis.com/v1/people/{}:deleteContact",
        resourceName,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/people/{peopleId}:deleteContact
/// Delete a contact person. Any non-contact data will not be deleted. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_people_delete_contact_execute()` or `people_people_delete_contact`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_delete_contact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_delete_contact_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/people/{peopleId}:deleteContact
/// Delete a contact person. Any non-contact data will not be deleted. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_people_delete_contact_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_people_delete_contact_task()`.
/// For the simplest API, use `people_people_delete_contact()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_delete_contact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_people_delete_contact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = people_people_delete_contact_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_people_delete_contact`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeoplePeopleDeleteContactArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
}

/// DELETE v1/people/{peopleId}:deleteContact
/// Delete a contact person. Any non-contact data will not be deleted. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_delete_contact_builder()` + `people_people_delete_contact_execute()`.
/// For task-level control, use `people_people_delete_contact_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_delete_contact(
    client: &SimpleHttpClient,
    args: &PeoplePeopleDeleteContactArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = people_people_delete_contact_builder(client, &args.resourceName)?;
    people_people_delete_contact_execute(builder)
}

/// DELETE v1/people/{peopleId}:deleteContactPhoto
/// Delete a contact''s photo. Mutate requests for the same user should be done sequentially to avoid // lock contention.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_delete_contact_photo_execute()` to send, or `people_people_delete_contact_photo` for simplest API.

pub fn people_people_delete_contact_photo_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
    personFields: &Option<Option<String>>,
    sources: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://people.googleapis.com/v1/people/{}:deleteContactPhoto",
        resourceName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = personFields.as_ref() {
        query_parts.push(format!("personFields={}", val));
    }
    if let Some(val) = sources.as_ref() {
        query_parts.push(format!("sources={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/people/{peopleId}:deleteContactPhoto
/// Delete a contact''s photo. Mutate requests for the same user should be done sequentially to avoid // lock contention.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_people_delete_contact_photo_execute()` or `people_people_delete_contact_photo`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_delete_contact_photo_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_delete_contact_photo_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeleteContactPhotoResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteContactPhotoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/people/{peopleId}:deleteContactPhoto
/// Delete a contact''s photo. Mutate requests for the same user should be done sequentially to avoid // lock contention.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_people_delete_contact_photo_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_people_delete_contact_photo_task()`.
/// For the simplest API, use `people_people_delete_contact_photo()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_delete_contact_photo_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_people_delete_contact_photo_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeleteContactPhotoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = people_people_delete_contact_photo_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_people_delete_contact_photo`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeoplePeopleDeleteContactPhotoArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
    /// Query parameter: personFields
    pub personFields: Option<Option<String>>,
    /// Query parameter: sources
    pub sources: Option<Option<String>>,
}

/// DELETE v1/people/{peopleId}:deleteContactPhoto
/// Delete a contact''s photo. Mutate requests for the same user should be done sequentially to avoid // lock contention.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_delete_contact_photo_builder()` + `people_people_delete_contact_photo_execute()`.
/// For task-level control, use `people_people_delete_contact_photo_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_delete_contact_photo(
    client: &SimpleHttpClient,
    args: &PeoplePeopleDeleteContactPhotoArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeleteContactPhotoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_delete_contact_photo_builder(
        client,
        &args.resourceName,
        &args.personFields,
        &args.sources,
    )?;
    people_people_delete_contact_photo_execute(builder)
}

/// GET v1/people/{peopleId}
/// Provides information about a person by specifying a resource name. Use `people/me` to indicate the authenticated user. The request returns a 400 error if ''`personFields`'' is not specified.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_get_execute()` to send, or `people_people_get` for simplest API.

pub fn people_people_get_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
    personFields: &Option<Option<String>>,
    requestMask_includeField: &Option<Option<String>>,
    sources: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://people.googleapis.com/v1/people/{}", resourceName,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = personFields.as_ref() {
        query_parts.push(format!("personFields={}", val));
    }
    if let Some(val) = requestMask_includeField.as_ref() {
        query_parts.push(format!("requestMask.includeField={}", val));
    }
    if let Some(val) = sources.as_ref() {
        query_parts.push(format!("sources={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/people/{peopleId}
/// Provides information about a person by specifying a resource name. Use `people/me` to indicate the authenticated user. The request returns a 400 error if ''`personFields`'' is not specified.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_people_get_execute()` or `people_people_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Person>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/people/{peopleId}
/// Provides information about a person by specifying a resource name. Use `people/me` to indicate the authenticated user. The request returns a 400 error if ''`personFields`'' is not specified.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_people_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_people_get_task()`.
/// For the simplest API, use `people_people_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_people_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = people_people_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_people_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeoplePeopleGetArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
    /// Query parameter: personFields
    pub personFields: Option<Option<String>>,
    /// Query parameter: requestMask_includeField
    pub requestMask_includeField: Option<Option<String>>,
    /// Query parameter: sources
    pub sources: Option<Option<String>>,
}

/// GET v1/people/{peopleId}
/// Provides information about a person by specifying a resource name. Use `people/me` to indicate the authenticated user. The request returns a 400 error if ''`personFields`'' is not specified.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_get_builder()` + `people_people_get_execute()`.
/// For task-level control, use `people_people_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_get(
    client: &SimpleHttpClient,
    args: &PeoplePeopleGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = people_people_get_builder(
        client,
        &args.resourceName,
        &args.personFields,
        &args.requestMask_includeField,
        &args.sources,
    )?;
    people_people_get_execute(builder)
}

/// GET v1/people:batchGet
/// Provides information about a list of specific people by specifying a list of requested resource names. Use `people/me` to indicate the authenticated user. The request returns a 400 error if ''`personFields`'' is not specified.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_get_batch_get_execute()` to send, or `people_people_get_batch_get` for simplest API.

pub fn people_people_get_batch_get_builder<R>(
    client: &SimpleHttpClient<R>,
    personFields: &Option<Option<String>>,
    requestMask_includeField: &Option<Option<String>>,
    resourceNames: &Option<Option<String>>,
    sources: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://people.googleapis.com/v1/people:batchGet",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = personFields.as_ref() {
        query_parts.push(format!("personFields={}", val));
    }
    if let Some(val) = requestMask_includeField.as_ref() {
        query_parts.push(format!("requestMask.includeField={}", val));
    }
    if let Some(val) = resourceNames.as_ref() {
        query_parts.push(format!("resourceNames={}", val));
    }
    if let Some(val) = sources.as_ref() {
        query_parts.push(format!("sources={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/people:batchGet
/// Provides information about a list of specific people by specifying a list of requested resource names. Use `people/me` to indicate the authenticated user. The request returns a 400 error if ''`personFields`'' is not specified.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_people_get_batch_get_execute()` or `people_people_get_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_get_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_get_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetPeopleResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetPeopleResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/people:batchGet
/// Provides information about a list of specific people by specifying a list of requested resource names. Use `people/me` to indicate the authenticated user. The request returns a 400 error if ''`personFields`'' is not specified.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_people_get_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_people_get_batch_get_task()`.
/// For the simplest API, use `people_people_get_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_get_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_people_get_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetPeopleResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = people_people_get_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_people_get_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeoplePeopleGetBatchGetArgs {
    /// Query parameter: personFields
    pub personFields: Option<Option<String>>,
    /// Query parameter: requestMask_includeField
    pub requestMask_includeField: Option<Option<String>>,
    /// Query parameter: resourceNames
    pub resourceNames: Option<Option<String>>,
    /// Query parameter: sources
    pub sources: Option<Option<String>>,
}

/// GET v1/people:batchGet
/// Provides information about a list of specific people by specifying a list of requested resource names. Use `people/me` to indicate the authenticated user. The request returns a 400 error if ''`personFields`'' is not specified.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_get_batch_get_builder()` + `people_people_get_batch_get_execute()`.
/// For task-level control, use `people_people_get_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_get_batch_get(
    client: &SimpleHttpClient,
    args: &PeoplePeopleGetBatchGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetPeopleResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_get_batch_get_builder(
        client,
        &args.personFields,
        &args.requestMask_includeField,
        &args.resourceNames,
        &args.sources,
    )?;
    people_people_get_batch_get_execute(builder)
}

/// GET v1/people:listDirectoryPeople
/// Provides a list of domain profiles and domain contacts in the authenticated user''s domain directory. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the directory people that have changed](/`people/v1/directory`#list_the_directory_people_that_have_changed).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_list_directory_people_execute()` to send, or `people_people_list_directory_people` for simplest API.

pub fn people_people_list_directory_people_builder<R>(
    client: &SimpleHttpClient<R>,
    mergeSources: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
    requestSyncToken: &Option<Option<String>>,
    sources: &Option<Option<String>>,
    syncToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://people.googleapis.com/v1/people:listDirectoryPeople",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = mergeSources.as_ref() {
        query_parts.push(format!("mergeSources={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }
    if let Some(val) = requestSyncToken.as_ref() {
        query_parts.push(format!("requestSyncToken={}", val));
    }
    if let Some(val) = sources.as_ref() {
        query_parts.push(format!("sources={}", val));
    }
    if let Some(val) = syncToken.as_ref() {
        query_parts.push(format!("syncToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/people:listDirectoryPeople
/// Provides a list of domain profiles and domain contacts in the authenticated user''s domain directory. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the directory people that have changed](/`people/v1/directory`#list_the_directory_people_that_have_changed).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_people_list_directory_people_execute()` or `people_people_list_directory_people`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_list_directory_people_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_list_directory_people_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDirectoryPeopleResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDirectoryPeopleResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/people:listDirectoryPeople
/// Provides a list of domain profiles and domain contacts in the authenticated user''s domain directory. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the directory people that have changed](/`people/v1/directory`#list_the_directory_people_that_have_changed).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_people_list_directory_people_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_people_list_directory_people_task()`.
/// For the simplest API, use `people_people_list_directory_people()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_list_directory_people_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_people_list_directory_people_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDirectoryPeopleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = people_people_list_directory_people_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_people_list_directory_people`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeoplePeopleListDirectoryPeopleArgs {
    /// Query parameter: mergeSources
    pub mergeSources: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
    /// Query parameter: requestSyncToken
    pub requestSyncToken: Option<Option<String>>,
    /// Query parameter: sources
    pub sources: Option<Option<String>>,
    /// Query parameter: syncToken
    pub syncToken: Option<Option<String>>,
}

/// GET v1/people:listDirectoryPeople
/// Provides a list of domain profiles and domain contacts in the authenticated user''s domain directory. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the directory people that have changed](/`people/v1/directory`#list_the_directory_people_that_have_changed).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_list_directory_people_builder()` + `people_people_list_directory_people_execute()`.
/// For task-level control, use `people_people_list_directory_people_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_list_directory_people(
    client: &SimpleHttpClient,
    args: &PeoplePeopleListDirectoryPeopleArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDirectoryPeopleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_list_directory_people_builder(
        client,
        &args.mergeSources,
        &args.pageSize,
        &args.pageToken,
        &args.readMask,
        &args.requestSyncToken,
        &args.sources,
        &args.syncToken,
    )?;
    people_people_list_directory_people_execute(builder)
}

/// GET v1/people:searchContacts
/// Provides a list of contacts in the authenticated user''s grouped contacts that matches the search query. The query matches on a contact''s names, `nickNames`, `emailAddresses`, `phoneNumbers`, and organizations fields that are from the CONTACT source. **IMPORTANT**: Before searching, clients should send a warmup request with an empty query to update the cache. See <https://developers.google.`com/people/v1/contacts`#search_the_users_contacts>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_search_contacts_execute()` to send, or `people_people_search_contacts` for simplest API.

pub fn people_people_search_contacts_builder<R>(
    client: &SimpleHttpClient<R>,
    pageSize: &Option<Option<String>>,
    query: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
    sources: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://people.googleapis.com/v1/people:searchContacts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }
    if let Some(val) = sources.as_ref() {
        query_parts.push(format!("sources={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/people:searchContacts
/// Provides a list of contacts in the authenticated user''s grouped contacts that matches the search query. The query matches on a contact''s names, `nickNames`, `emailAddresses`, `phoneNumbers`, and organizations fields that are from the CONTACT source. **IMPORTANT**: Before searching, clients should send a warmup request with an empty query to update the cache. See <https://developers.google.`com/people/v1/contacts`#search_the_users_contacts>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_people_search_contacts_execute()` or `people_people_search_contacts`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_search_contacts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_search_contacts_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/people:searchContacts
/// Provides a list of contacts in the authenticated user''s grouped contacts that matches the search query. The query matches on a contact''s names, `nickNames`, `emailAddresses`, `phoneNumbers`, and organizations fields that are from the CONTACT source. **IMPORTANT**: Before searching, clients should send a warmup request with an empty query to update the cache. See <https://developers.google.`com/people/v1/contacts`#search_the_users_contacts>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_people_search_contacts_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_people_search_contacts_task()`.
/// For the simplest API, use `people_people_search_contacts()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_search_contacts_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_people_search_contacts_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = people_people_search_contacts_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_people_search_contacts`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeoplePeopleSearchContactsArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: query
    pub query: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
    /// Query parameter: sources
    pub sources: Option<Option<String>>,
}

/// GET v1/people:searchContacts
/// Provides a list of contacts in the authenticated user''s grouped contacts that matches the search query. The query matches on a contact''s names, `nickNames`, `emailAddresses`, `phoneNumbers`, and organizations fields that are from the CONTACT source. **IMPORTANT**: Before searching, clients should send a warmup request with an empty query to update the cache. See <https://developers.google.`com/people/v1/contacts`#search_the_users_contacts>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_search_contacts_builder()` + `people_people_search_contacts_execute()`.
/// For task-level control, use `people_people_search_contacts_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_search_contacts(
    client: &SimpleHttpClient,
    args: &PeoplePeopleSearchContactsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_search_contacts_builder(
        client,
        &args.pageSize,
        &args.query,
        &args.readMask,
        &args.sources,
    )?;
    people_people_search_contacts_execute(builder)
}

/// GET v1/people:searchDirectoryPeople
/// Provides a list of domain profiles and domain contacts in the authenticated user''s domain directory that match the search query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_search_directory_people_execute()` to send, or `people_people_search_directory_people` for simplest API.

pub fn people_people_search_directory_people_builder<R>(
    client: &SimpleHttpClient<R>,
    mergeSources: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    query: &Option<Option<String>>,
    readMask: &Option<Option<String>>,
    sources: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://people.googleapis.com/v1/people:searchDirectoryPeople",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = mergeSources.as_ref() {
        query_parts.push(format!("mergeSources={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }
    if let Some(val) = sources.as_ref() {
        query_parts.push(format!("sources={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/people:searchDirectoryPeople
/// Provides a list of domain profiles and domain contacts in the authenticated user''s domain directory that match the search query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_people_search_directory_people_execute()` or `people_people_search_directory_people`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_search_directory_people_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_search_directory_people_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchDirectoryPeopleResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchDirectoryPeopleResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/people:searchDirectoryPeople
/// Provides a list of domain profiles and domain contacts in the authenticated user''s domain directory that match the search query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_people_search_directory_people_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_people_search_directory_people_task()`.
/// For the simplest API, use `people_people_search_directory_people()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_search_directory_people_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_people_search_directory_people_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchDirectoryPeopleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = people_people_search_directory_people_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_people_search_directory_people`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeoplePeopleSearchDirectoryPeopleArgs {
    /// Query parameter: mergeSources
    pub mergeSources: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: query
    pub query: Option<Option<String>>,
    /// Query parameter: readMask
    pub readMask: Option<Option<String>>,
    /// Query parameter: sources
    pub sources: Option<Option<String>>,
}

/// GET v1/people:searchDirectoryPeople
/// Provides a list of domain profiles and domain contacts in the authenticated user''s domain directory that match the search query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_search_directory_people_builder()` + `people_people_search_directory_people_execute()`.
/// For task-level control, use `people_people_search_directory_people_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_search_directory_people(
    client: &SimpleHttpClient,
    args: &PeoplePeopleSearchDirectoryPeopleArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchDirectoryPeopleResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_search_directory_people_builder(
        client,
        &args.mergeSources,
        &args.pageSize,
        &args.pageToken,
        &args.query,
        &args.readMask,
        &args.sources,
    )?;
    people_people_search_directory_people_execute(builder)
}

/// PATCH v1/people/{peopleId}:updateContact
/// Update contact data for an existing contact person. Any non-contact data will not be modified. Any non-contact data in the person to update will be ignored. All fields specified in the update_mask will be replaced. The server returns a 400 error if person.metadata.sources is not specified for the contact to be updated or if there is no contact source. The server returns a 400 error with reason "`failedPrecondition`" if person.metadata.sources.etag is different than the contact''s etag, which indicates the contact has changed since its data was read. Clients should get the latest person and merge their updates into the latest person. If making sequential updates to the same person, the etag from the `updateContact` response should be used to avoid failures. The server returns a 400 error if memberships are being updated and there are no contact group memberships specified on the person. The server returns a 400 error if more than one field is specified on a field that is a singleton for contact sources: * biographies * birthdays * genders * names Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_update_contact_execute()` to send, or `people_people_update_contact` for simplest API.

pub fn people_people_update_contact_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
    personFields: &Option<Option<String>>,
    sources: &Option<Option<String>>,
    updatePersonFields: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://people.googleapis.com/v1/people/{}:updateContact",
        resourceName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = personFields.as_ref() {
        query_parts.push(format!("personFields={}", val));
    }
    if let Some(val) = sources.as_ref() {
        query_parts.push(format!("sources={}", val));
    }
    if let Some(val) = updatePersonFields.as_ref() {
        query_parts.push(format!("updatePersonFields={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/people/{peopleId}:updateContact
/// Update contact data for an existing contact person. Any non-contact data will not be modified. Any non-contact data in the person to update will be ignored. All fields specified in the update_mask will be replaced. The server returns a 400 error if person.metadata.sources is not specified for the contact to be updated or if there is no contact source. The server returns a 400 error with reason "`failedPrecondition`" if person.metadata.sources.etag is different than the contact''s etag, which indicates the contact has changed since its data was read. Clients should get the latest person and merge their updates into the latest person. If making sequential updates to the same person, the etag from the `updateContact` response should be used to avoid failures. The server returns a 400 error if memberships are being updated and there are no contact group memberships specified on the person. The server returns a 400 error if more than one field is specified on a field that is a singleton for contact sources: * biographies * birthdays * genders * names Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_people_update_contact_execute()` or `people_people_update_contact`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_update_contact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_update_contact_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Person>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Person = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/people/{peopleId}:updateContact
/// Update contact data for an existing contact person. Any non-contact data will not be modified. Any non-contact data in the person to update will be ignored. All fields specified in the update_mask will be replaced. The server returns a 400 error if person.metadata.sources is not specified for the contact to be updated or if there is no contact source. The server returns a 400 error with reason "`failedPrecondition`" if person.metadata.sources.etag is different than the contact''s etag, which indicates the contact has changed since its data was read. Clients should get the latest person and merge their updates into the latest person. If making sequential updates to the same person, the etag from the `updateContact` response should be used to avoid failures. The server returns a 400 error if memberships are being updated and there are no contact group memberships specified on the person. The server returns a 400 error if more than one field is specified on a field that is a singleton for contact sources: * biographies * birthdays * genders * names Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_people_update_contact_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_people_update_contact_task()`.
/// For the simplest API, use `people_people_update_contact()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_update_contact_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_people_update_contact_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = people_people_update_contact_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_people_update_contact`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeoplePeopleUpdateContactArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
    /// Query parameter: personFields
    pub personFields: Option<Option<String>>,
    /// Query parameter: sources
    pub sources: Option<Option<String>>,
    /// Query parameter: updatePersonFields
    pub updatePersonFields: Option<Option<String>>,
}

/// PATCH v1/people/{peopleId}:updateContact
/// Update contact data for an existing contact person. Any non-contact data will not be modified. Any non-contact data in the person to update will be ignored. All fields specified in the update_mask will be replaced. The server returns a 400 error if person.metadata.sources is not specified for the contact to be updated or if there is no contact source. The server returns a 400 error with reason "`failedPrecondition`" if person.metadata.sources.etag is different than the contact''s etag, which indicates the contact has changed since its data was read. Clients should get the latest person and merge their updates into the latest person. If making sequential updates to the same person, the etag from the `updateContact` response should be used to avoid failures. The server returns a 400 error if memberships are being updated and there are no contact group memberships specified on the person. The server returns a 400 error if more than one field is specified on a field that is a singleton for contact sources: * biographies * birthdays * genders * names Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_update_contact_builder()` + `people_people_update_contact_execute()`.
/// For task-level control, use `people_people_update_contact_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_update_contact(
    client: &SimpleHttpClient,
    args: &PeoplePeopleUpdateContactArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Person>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = people_people_update_contact_builder(
        client,
        &args.resourceName,
        &args.personFields,
        &args.sources,
        &args.updatePersonFields,
    )?;
    people_people_update_contact_execute(builder)
}

/// PATCH v1/people/{peopleId}:updateContactPhoto
/// Update a contact''s photo. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_update_contact_photo_execute()` to send, or `people_people_update_contact_photo` for simplest API.

pub fn people_people_update_contact_photo_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://people.googleapis.com/v1/people/{}:updateContactPhoto",
        resourceName,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/people/{peopleId}:updateContactPhoto
/// Update a contact''s photo. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_people_update_contact_photo_execute()` or `people_people_update_contact_photo`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_update_contact_photo_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_update_contact_photo_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UpdateContactPhotoResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateContactPhotoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/people/{peopleId}:updateContactPhoto
/// Update a contact''s photo. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_people_update_contact_photo_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_people_update_contact_photo_task()`.
/// For the simplest API, use `people_people_update_contact_photo()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_update_contact_photo_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_people_update_contact_photo_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateContactPhotoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = people_people_update_contact_photo_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_people_update_contact_photo`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeoplePeopleUpdateContactPhotoArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
}

/// PATCH v1/people/{peopleId}:updateContactPhoto
/// Update a contact''s photo. Mutate requests for the same user should be sent sequentially to avoid increased latency and failures.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_update_contact_photo_builder()` + `people_people_update_contact_photo_execute()`.
/// For task-level control, use `people_people_update_contact_photo_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_update_contact_photo(
    client: &SimpleHttpClient,
    args: &PeoplePeopleUpdateContactPhotoArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateContactPhotoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_update_contact_photo_builder(client, &args.resourceName)?;
    people_people_update_contact_photo_execute(builder)
}

/// GET v1/people/{peopleId}/connections
/// Provides a list of the authenticated user''s contacts. Sync tokens expire 7 days after the full sync. A request with an expired sync token will get an error with an [google.rpc.ErrorInfo](<https://cloud.google.`com/apis/design/errors`#error_info>) with reason "EXPIRED_SYNC_TOKEN". In the case of such an error clients should make a full sync request without a sync_token. The first page of a full sync request has an additional quota. If the quota is exceeded, a 429 error will be returned. This quota is fixed and can not be increased. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the user''s contacts that have changed](/`people/v1/contacts`#list_the_users_contacts_that_have_changed).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `people_people_connections_list_execute()` to send, or `people_people_connections_list` for simplest API.

pub fn people_people_connections_list_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    personFields: &Option<Option<String>>,
    requestMask_includeField: &Option<Option<String>>,
    requestSyncToken: &Option<Option<String>>,
    sortOrder: &Option<Option<String>>,
    sources: &Option<Option<String>>,
    syncToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://people.googleapis.com/v1/people/{}/connections",
        resourceName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = personFields.as_ref() {
        query_parts.push(format!("personFields={}", val));
    }
    if let Some(val) = requestMask_includeField.as_ref() {
        query_parts.push(format!("requestMask.includeField={}", val));
    }
    if let Some(val) = requestSyncToken.as_ref() {
        query_parts.push(format!("requestSyncToken={}", val));
    }
    if let Some(val) = sortOrder.as_ref() {
        query_parts.push(format!("sortOrder={}", val));
    }
    if let Some(val) = sources.as_ref() {
        query_parts.push(format!("sources={}", val));
    }
    if let Some(val) = syncToken.as_ref() {
        query_parts.push(format!("syncToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/people/{peopleId}/connections
/// Provides a list of the authenticated user''s contacts. Sync tokens expire 7 days after the full sync. A request with an expired sync token will get an error with an [google.rpc.ErrorInfo](<https://cloud.google.`com/apis/design/errors`#error_info>) with reason "EXPIRED_SYNC_TOKEN". In the case of such an error clients should make a full sync request without a sync_token. The first page of a full sync request has an additional quota. If the quota is exceeded, a 429 error will be returned. This quota is fixed and can not be increased. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the user''s contacts that have changed](/`people/v1/contacts`#list_the_users_contacts_that_have_changed).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `people_people_connections_list_execute()` or `people_people_connections_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_connections_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_connections_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListConnectionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConnectionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/people/{peopleId}/connections
/// Provides a list of the authenticated user''s contacts. Sync tokens expire 7 days after the full sync. A request with an expired sync token will get an error with an [google.rpc.ErrorInfo](<https://cloud.google.`com/apis/design/errors`#error_info>) with reason "EXPIRED_SYNC_TOKEN". In the case of such an error clients should make a full sync request without a sync_token. The first page of a full sync request has an additional quota. If the quota is exceeded, a 429 error will be returned. This quota is fixed and can not be increased. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the user''s contacts that have changed](/`people/v1/contacts`#list_the_users_contacts_that_have_changed).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `people_people_connections_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `people_people_connections_list_task()`.
/// For the simplest API, use `people_people_connections_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `people_people_connections_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn people_people_connections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConnectionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = people_people_connections_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`people_people_connections_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PeoplePeopleConnectionsListArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: personFields
    pub personFields: Option<Option<String>>,
    /// Query parameter: requestMask_includeField
    pub requestMask_includeField: Option<Option<String>>,
    /// Query parameter: requestSyncToken
    pub requestSyncToken: Option<Option<String>>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<Option<String>>,
    /// Query parameter: sources
    pub sources: Option<Option<String>>,
    /// Query parameter: syncToken
    pub syncToken: Option<Option<String>>,
}

/// GET v1/people/{peopleId}/connections
/// Provides a list of the authenticated user''s contacts. Sync tokens expire 7 days after the full sync. A request with an expired sync token will get an error with an [google.rpc.ErrorInfo](<https://cloud.google.`com/apis/design/errors`#error_info>) with reason "EXPIRED_SYNC_TOKEN". In the case of such an error clients should make a full sync request without a sync_token. The first page of a full sync request has an additional quota. If the quota is exceeded, a 429 error will be returned. This quota is fixed and can not be increased. When the sync_token is specified, resources deleted since the last sync will be returned as a person with PersonMetadata.deleted set to `true`. When the page_token or sync_token is specified, all other request parameters must match the first call. Writes may have a propagation delay of several minutes for sync requests. Incremental syncs are not intended for read-after-write use cases. See example usage at [List the user''s contacts that have changed](/`people/v1/contacts`#list_the_users_contacts_that_have_changed).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `people_people_connections_list_builder()` + `people_people_connections_list_execute()`.
/// For task-level control, use `people_people_connections_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn people_people_connections_list(
    client: &SimpleHttpClient,
    args: &PeoplePeopleConnectionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConnectionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = people_people_connections_list_builder(
        client,
        &args.resourceName,
        &args.pageSize,
        &args.pageToken,
        &args.personFields,
        &args.requestMask_includeField,
        &args.requestSyncToken,
        &args.sortOrder,
        &args.sources,
        &args.syncToken,
    )?;
    people_people_connections_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for BatchGetContactGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for BatchGetContactGroupsResponse with PeopleContactGroupsBatchGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeopleContactGroupsBatchGetArgs> for BatchGetContactGroupsResponse {
    fn generate_resource_id(&self, input: &PeopleContactGroupsBatchGetArgs) -> String {
        "gcp::people::BatchGetContactGroupsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::BatchGetContactGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ContactGroup
// =============================================================================

/// ResourceIdentifier implementation for ContactGroup with PeopleContactGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeopleContactGroupsCreateArgs> for ContactGroup {
    fn generate_resource_id(&self, input: &PeopleContactGroupsCreateArgs) -> String {
        "gcp::people::ContactGroup".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::ContactGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with PeopleContactGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeopleContactGroupsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &PeopleContactGroupsDeleteArgs) -> String {
        format!("gcp::people::Empty/{}", input.resourceName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ContactGroup
// =============================================================================

/// ResourceIdentifier implementation for ContactGroup with PeopleContactGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeopleContactGroupsGetArgs> for ContactGroup {
    fn generate_resource_id(&self, input: &PeopleContactGroupsGetArgs) -> String {
        format!("gcp::people::ContactGroup/{}", input.resourceName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::ContactGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListContactGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListContactGroupsResponse with PeopleContactGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeopleContactGroupsListArgs> for ListContactGroupsResponse {
    fn generate_resource_id(&self, input: &PeopleContactGroupsListArgs) -> String {
        "gcp::people::ListContactGroupsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::ListContactGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ContactGroup
// =============================================================================

/// ResourceIdentifier implementation for ContactGroup with PeopleContactGroupsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeopleContactGroupsUpdateArgs> for ContactGroup {
    fn generate_resource_id(&self, input: &PeopleContactGroupsUpdateArgs) -> String {
        format!("gcp::people::ContactGroup/{}", input.resourceName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::ContactGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ModifyContactGroupMembersResponse
// =============================================================================

/// ResourceIdentifier implementation for ModifyContactGroupMembersResponse with PeopleContactGroupsMembersModifyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeopleContactGroupsMembersModifyArgs>
    for ModifyContactGroupMembersResponse
{
    fn generate_resource_id(&self, input: &PeopleContactGroupsMembersModifyArgs) -> String {
        format!(
            "gcp::people::ModifyContactGroupMembersResponse/{}",
            input.resourceName
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::ModifyContactGroupMembersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Person
// =============================================================================

/// ResourceIdentifier implementation for Person with PeopleOtherContactsCopyOtherContactToMyContactsGroupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeopleOtherContactsCopyOtherContactToMyContactsGroupArgs> for Person {
    fn generate_resource_id(
        &self,
        input: &PeopleOtherContactsCopyOtherContactToMyContactsGroupArgs,
    ) -> String {
        format!("gcp::people::Person/{}", input.resourceName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::Person"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOtherContactsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOtherContactsResponse with PeopleOtherContactsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeopleOtherContactsListArgs> for ListOtherContactsResponse {
    fn generate_resource_id(&self, input: &PeopleOtherContactsListArgs) -> String {
        "gcp::people::ListOtherContactsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::ListOtherContactsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchResponse
// =============================================================================

/// ResourceIdentifier implementation for SearchResponse with PeopleOtherContactsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeopleOtherContactsSearchArgs> for SearchResponse {
    fn generate_resource_id(&self, input: &PeopleOtherContactsSearchArgs) -> String {
        "gcp::people::SearchResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::SearchResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BatchCreateContactsResponse
// =============================================================================

/// ResourceIdentifier implementation for BatchCreateContactsResponse with PeoplePeopleBatchCreateContactsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeoplePeopleBatchCreateContactsArgs> for BatchCreateContactsResponse {
    fn generate_resource_id(&self, input: &PeoplePeopleBatchCreateContactsArgs) -> String {
        "gcp::people::BatchCreateContactsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::BatchCreateContactsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with PeoplePeopleBatchDeleteContactsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeoplePeopleBatchDeleteContactsArgs> for Empty {
    fn generate_resource_id(&self, input: &PeoplePeopleBatchDeleteContactsArgs) -> String {
        "gcp::people::Empty".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BatchUpdateContactsResponse
// =============================================================================

/// ResourceIdentifier implementation for BatchUpdateContactsResponse with PeoplePeopleBatchUpdateContactsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeoplePeopleBatchUpdateContactsArgs> for BatchUpdateContactsResponse {
    fn generate_resource_id(&self, input: &PeoplePeopleBatchUpdateContactsArgs) -> String {
        "gcp::people::BatchUpdateContactsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::BatchUpdateContactsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Person
// =============================================================================

/// ResourceIdentifier implementation for Person with PeoplePeopleCreateContactArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeoplePeopleCreateContactArgs> for Person {
    fn generate_resource_id(&self, input: &PeoplePeopleCreateContactArgs) -> String {
        "gcp::people::Person".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::Person"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with PeoplePeopleDeleteContactArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeoplePeopleDeleteContactArgs> for Empty {
    fn generate_resource_id(&self, input: &PeoplePeopleDeleteContactArgs) -> String {
        format!("gcp::people::Empty/{}", input.resourceName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DeleteContactPhotoResponse
// =============================================================================

/// ResourceIdentifier implementation for DeleteContactPhotoResponse with PeoplePeopleDeleteContactPhotoArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeoplePeopleDeleteContactPhotoArgs> for DeleteContactPhotoResponse {
    fn generate_resource_id(&self, input: &PeoplePeopleDeleteContactPhotoArgs) -> String {
        format!(
            "gcp::people::DeleteContactPhotoResponse/{}",
            input.resourceName
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::DeleteContactPhotoResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Person
// =============================================================================

/// ResourceIdentifier implementation for Person with PeoplePeopleGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeoplePeopleGetArgs> for Person {
    fn generate_resource_id(&self, input: &PeoplePeopleGetArgs) -> String {
        format!("gcp::people::Person/{}", input.resourceName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::Person"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GetPeopleResponse
// =============================================================================

/// ResourceIdentifier implementation for GetPeopleResponse with PeoplePeopleGetBatchGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeoplePeopleGetBatchGetArgs> for GetPeopleResponse {
    fn generate_resource_id(&self, input: &PeoplePeopleGetBatchGetArgs) -> String {
        "gcp::people::GetPeopleResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::GetPeopleResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDirectoryPeopleResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDirectoryPeopleResponse with PeoplePeopleListDirectoryPeopleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeoplePeopleListDirectoryPeopleArgs> for ListDirectoryPeopleResponse {
    fn generate_resource_id(&self, input: &PeoplePeopleListDirectoryPeopleArgs) -> String {
        "gcp::people::ListDirectoryPeopleResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::ListDirectoryPeopleResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchResponse
// =============================================================================

/// ResourceIdentifier implementation for SearchResponse with PeoplePeopleSearchContactsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeoplePeopleSearchContactsArgs> for SearchResponse {
    fn generate_resource_id(&self, input: &PeoplePeopleSearchContactsArgs) -> String {
        "gcp::people::SearchResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::SearchResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchDirectoryPeopleResponse
// =============================================================================

/// ResourceIdentifier implementation for SearchDirectoryPeopleResponse with PeoplePeopleSearchDirectoryPeopleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeoplePeopleSearchDirectoryPeopleArgs> for SearchDirectoryPeopleResponse {
    fn generate_resource_id(&self, input: &PeoplePeopleSearchDirectoryPeopleArgs) -> String {
        "gcp::people::SearchDirectoryPeopleResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::SearchDirectoryPeopleResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Person
// =============================================================================

/// ResourceIdentifier implementation for Person with PeoplePeopleUpdateContactArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeoplePeopleUpdateContactArgs> for Person {
    fn generate_resource_id(&self, input: &PeoplePeopleUpdateContactArgs) -> String {
        format!("gcp::people::Person/{}", input.resourceName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::Person"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UpdateContactPhotoResponse
// =============================================================================

/// ResourceIdentifier implementation for UpdateContactPhotoResponse with PeoplePeopleUpdateContactPhotoArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeoplePeopleUpdateContactPhotoArgs> for UpdateContactPhotoResponse {
    fn generate_resource_id(&self, input: &PeoplePeopleUpdateContactPhotoArgs) -> String {
        format!(
            "gcp::people::UpdateContactPhotoResponse/{}",
            input.resourceName
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::UpdateContactPhotoResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListConnectionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListConnectionsResponse with PeoplePeopleConnectionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PeoplePeopleConnectionsListArgs> for ListConnectionsResponse {
    fn generate_resource_id(&self, input: &PeoplePeopleConnectionsListArgs) -> String {
        format!(
            "gcp::people::ListConnectionsResponse/{}",
            input.resourceName
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::people::ListConnectionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
