//! Auto-generated API clients for gcp/iamcredentials.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/locations/{locationsId}/workforcePools/{workforcePoolsId}/allowedLocations
/// Returns the trust boundary info for a given workforce pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iamcredentials_locations_workforce_pools_get_allowed_locations_execute()` to send, or `iamcredentials_locations_workforce_pools_get_allowed_locations` for simplest API.

pub fn iamcredentials_locations_workforce_pools_get_allowed_locations_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://iamcredentials.googleapis.com/v1/locations/{}/workforcePools/{workforcePoolsId}/allowedLocations",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/workforcePools/{workforcePoolsId}/allowedLocations
/// Returns the trust boundary info for a given workforce pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iamcredentials_locations_workforce_pools_get_allowed_locations_execute()` or `iamcredentials_locations_workforce_pools_get_allowed_locations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iamcredentials_locations_workforce_pools_get_allowed_locations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iamcredentials_locations_workforce_pools_get_allowed_locations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkforcePoolAllowedLocations>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkforcePoolAllowedLocations = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/locations/{locationsId}/workforcePools/{workforcePoolsId}/allowedLocations
/// Returns the trust boundary info for a given workforce pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iamcredentials_locations_workforce_pools_get_allowed_locations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iamcredentials_locations_workforce_pools_get_allowed_locations_task()`.
/// For the simplest API, use `iamcredentials_locations_workforce_pools_get_allowed_locations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iamcredentials_locations_workforce_pools_get_allowed_locations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iamcredentials_locations_workforce_pools_get_allowed_locations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WorkforcePoolAllowedLocations>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = iamcredentials_locations_workforce_pools_get_allowed_locations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iamcredentials_locations_workforce_pools_get_allowed_locations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IamcredentialsLocationsWorkforcePoolsGetAllowedLocationsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/locations/{locationsId}/workforcePools/{workforcePoolsId}/allowedLocations
/// Returns the trust boundary info for a given workforce pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iamcredentials_locations_workforce_pools_get_allowed_locations_builder()` + `iamcredentials_locations_workforce_pools_get_allowed_locations_execute()`.
/// For task-level control, use `iamcredentials_locations_workforce_pools_get_allowed_locations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iamcredentials_locations_workforce_pools_get_allowed_locations(
    client: &SimpleHttpClient,
    args: &IamcredentialsLocationsWorkforcePoolsGetAllowedLocationsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WorkforcePoolAllowedLocations>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        iamcredentials_locations_workforce_pools_get_allowed_locations_builder(client, &args.name)?;
    iamcredentials_locations_workforce_pools_get_allowed_locations_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workloadIdentityPools/{workloadIdentityPoolsId}/allowedLocations
/// Returns the trust boundary info for a given workload identity pool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_execute()` to send, or `iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations` for simplest API.

pub fn iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://iamcredentials.googleapis.com/v1/projects/{}/locations/{locationsId}/workloadIdentityPools/{workloadIdentityPoolsId}/allowedLocations",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workloadIdentityPools/{workloadIdentityPoolsId}/allowedLocations
/// Returns the trust boundary info for a given workload identity pool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_execute()` or `iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkloadIdentityPoolAllowedLocations>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WorkloadIdentityPoolAllowedLocations = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workloadIdentityPools/{workloadIdentityPoolsId}/allowedLocations
/// Returns the trust boundary info for a given workload identity pool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_task()`.
/// For the simplest API, use `iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WorkloadIdentityPoolAllowedLocations>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IamcredentialsProjectsLocationsWorkloadIdentityPoolsGetAllowedLocationsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/workloadIdentityPools/{workloadIdentityPoolsId}/allowedLocations
/// Returns the trust boundary info for a given workload identity pool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_builder()` + `iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_execute()`.
/// For task-level control, use `iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations(
    client: &SimpleHttpClient,
    args: &IamcredentialsProjectsLocationsWorkloadIdentityPoolsGetAllowedLocationsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<WorkloadIdentityPoolAllowedLocations>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_builder(
            client, &args.name,
        )?;
    iamcredentials_projects_locations_workload_identity_pools_get_allowed_locations_execute(builder)
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:generateAccessToken
/// Generates an OAuth 2.0 access token for a service account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iamcredentials_projects_service_accounts_generate_access_token_execute()` to send, or `iamcredentials_projects_service_accounts_generate_access_token` for simplest API.

pub fn iamcredentials_projects_service_accounts_generate_access_token_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://iamcredentials.googleapis.com/v1/projects/{}/serviceAccounts/{serviceAccountsId}:generateAccessToken",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:generateAccessToken
/// Generates an OAuth 2.0 access token for a service account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iamcredentials_projects_service_accounts_generate_access_token_execute()` or `iamcredentials_projects_service_accounts_generate_access_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iamcredentials_projects_service_accounts_generate_access_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iamcredentials_projects_service_accounts_generate_access_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateAccessTokenResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateAccessTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:generateAccessToken
/// Generates an OAuth 2.0 access token for a service account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iamcredentials_projects_service_accounts_generate_access_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iamcredentials_projects_service_accounts_generate_access_token_task()`.
/// For the simplest API, use `iamcredentials_projects_service_accounts_generate_access_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iamcredentials_projects_service_accounts_generate_access_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iamcredentials_projects_service_accounts_generate_access_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateAccessTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = iamcredentials_projects_service_accounts_generate_access_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iamcredentials_projects_service_accounts_generate_access_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IamcredentialsProjectsServiceAccountsGenerateAccessTokenArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:generateAccessToken
/// Generates an OAuth 2.0 access token for a service account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iamcredentials_projects_service_accounts_generate_access_token_builder()` + `iamcredentials_projects_service_accounts_generate_access_token_execute()`.
/// For task-level control, use `iamcredentials_projects_service_accounts_generate_access_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iamcredentials_projects_service_accounts_generate_access_token(
    client: &SimpleHttpClient,
    args: &IamcredentialsProjectsServiceAccountsGenerateAccessTokenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateAccessTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        iamcredentials_projects_service_accounts_generate_access_token_builder(client, &args.name)?;
    iamcredentials_projects_service_accounts_generate_access_token_execute(builder)
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:generateIdToken
/// Generates an OpenID Connect ID token for a service account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iamcredentials_projects_service_accounts_generate_id_token_execute()` to send, or `iamcredentials_projects_service_accounts_generate_id_token` for simplest API.

pub fn iamcredentials_projects_service_accounts_generate_id_token_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://iamcredentials.googleapis.com/v1/projects/{}/serviceAccounts/{serviceAccountsId}:generateIdToken",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:generateIdToken
/// Generates an OpenID Connect ID token for a service account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iamcredentials_projects_service_accounts_generate_id_token_execute()` or `iamcredentials_projects_service_accounts_generate_id_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iamcredentials_projects_service_accounts_generate_id_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iamcredentials_projects_service_accounts_generate_id_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateIdTokenResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateIdTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:generateIdToken
/// Generates an OpenID Connect ID token for a service account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iamcredentials_projects_service_accounts_generate_id_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iamcredentials_projects_service_accounts_generate_id_token_task()`.
/// For the simplest API, use `iamcredentials_projects_service_accounts_generate_id_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iamcredentials_projects_service_accounts_generate_id_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iamcredentials_projects_service_accounts_generate_id_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenerateIdTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = iamcredentials_projects_service_accounts_generate_id_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iamcredentials_projects_service_accounts_generate_id_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IamcredentialsProjectsServiceAccountsGenerateIdTokenArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:generateIdToken
/// Generates an OpenID Connect ID token for a service account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iamcredentials_projects_service_accounts_generate_id_token_builder()` + `iamcredentials_projects_service_accounts_generate_id_token_execute()`.
/// For task-level control, use `iamcredentials_projects_service_accounts_generate_id_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iamcredentials_projects_service_accounts_generate_id_token(
    client: &SimpleHttpClient,
    args: &IamcredentialsProjectsServiceAccountsGenerateIdTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GenerateIdTokenResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        iamcredentials_projects_service_accounts_generate_id_token_builder(client, &args.name)?;
    iamcredentials_projects_service_accounts_generate_id_token_execute(builder)
}

/// GET v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}/allowedLocations
/// Returns the trust boundary info for a given service account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iamcredentials_projects_service_accounts_get_allowed_locations_execute()` to send, or `iamcredentials_projects_service_accounts_get_allowed_locations` for simplest API.

pub fn iamcredentials_projects_service_accounts_get_allowed_locations_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://iamcredentials.googleapis.com/v1/projects/{}/serviceAccounts/{serviceAccountsId}/allowedLocations",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}/allowedLocations
/// Returns the trust boundary info for a given service account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iamcredentials_projects_service_accounts_get_allowed_locations_execute()` or `iamcredentials_projects_service_accounts_get_allowed_locations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iamcredentials_projects_service_accounts_get_allowed_locations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iamcredentials_projects_service_accounts_get_allowed_locations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceAccountAllowedLocations>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceAccountAllowedLocations = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}/allowedLocations
/// Returns the trust boundary info for a given service account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iamcredentials_projects_service_accounts_get_allowed_locations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iamcredentials_projects_service_accounts_get_allowed_locations_task()`.
/// For the simplest API, use `iamcredentials_projects_service_accounts_get_allowed_locations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iamcredentials_projects_service_accounts_get_allowed_locations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iamcredentials_projects_service_accounts_get_allowed_locations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ServiceAccountAllowedLocations>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = iamcredentials_projects_service_accounts_get_allowed_locations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iamcredentials_projects_service_accounts_get_allowed_locations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IamcredentialsProjectsServiceAccountsGetAllowedLocationsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}/allowedLocations
/// Returns the trust boundary info for a given service account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iamcredentials_projects_service_accounts_get_allowed_locations_builder()` + `iamcredentials_projects_service_accounts_get_allowed_locations_execute()`.
/// For task-level control, use `iamcredentials_projects_service_accounts_get_allowed_locations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iamcredentials_projects_service_accounts_get_allowed_locations(
    client: &SimpleHttpClient,
    args: &IamcredentialsProjectsServiceAccountsGetAllowedLocationsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ServiceAccountAllowedLocations>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        iamcredentials_projects_service_accounts_get_allowed_locations_builder(client, &args.name)?;
    iamcredentials_projects_service_accounts_get_allowed_locations_execute(builder)
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:signBlob
/// Signs a blob using a service account''s system-managed private key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iamcredentials_projects_service_accounts_sign_blob_execute()` to send, or `iamcredentials_projects_service_accounts_sign_blob` for simplest API.

pub fn iamcredentials_projects_service_accounts_sign_blob_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://iamcredentials.googleapis.com/v1/projects/{}/serviceAccounts/{serviceAccountsId}:signBlob",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:signBlob
/// Signs a blob using a service account''s system-managed private key.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iamcredentials_projects_service_accounts_sign_blob_execute()` or `iamcredentials_projects_service_accounts_sign_blob`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iamcredentials_projects_service_accounts_sign_blob_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iamcredentials_projects_service_accounts_sign_blob_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SignBlobResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SignBlobResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:signBlob
/// Signs a blob using a service account''s system-managed private key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iamcredentials_projects_service_accounts_sign_blob_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iamcredentials_projects_service_accounts_sign_blob_task()`.
/// For the simplest API, use `iamcredentials_projects_service_accounts_sign_blob()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iamcredentials_projects_service_accounts_sign_blob_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iamcredentials_projects_service_accounts_sign_blob_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignBlobResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = iamcredentials_projects_service_accounts_sign_blob_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iamcredentials_projects_service_accounts_sign_blob`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IamcredentialsProjectsServiceAccountsSignBlobArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:signBlob
/// Signs a blob using a service account''s system-managed private key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iamcredentials_projects_service_accounts_sign_blob_builder()` + `iamcredentials_projects_service_accounts_sign_blob_execute()`.
/// For task-level control, use `iamcredentials_projects_service_accounts_sign_blob_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iamcredentials_projects_service_accounts_sign_blob(
    client: &SimpleHttpClient,
    args: &IamcredentialsProjectsServiceAccountsSignBlobArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignBlobResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = iamcredentials_projects_service_accounts_sign_blob_builder(client, &args.name)?;
    iamcredentials_projects_service_accounts_sign_blob_execute(builder)
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:signJwt
/// Signs a JWT using a service account''s system-managed private key.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iamcredentials_projects_service_accounts_sign_jwt_execute()` to send, or `iamcredentials_projects_service_accounts_sign_jwt` for simplest API.

pub fn iamcredentials_projects_service_accounts_sign_jwt_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://iamcredentials.googleapis.com/v1/projects/{}/serviceAccounts/{serviceAccountsId}:signJwt",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:signJwt
/// Signs a JWT using a service account''s system-managed private key.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iamcredentials_projects_service_accounts_sign_jwt_execute()` or `iamcredentials_projects_service_accounts_sign_jwt`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iamcredentials_projects_service_accounts_sign_jwt_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iamcredentials_projects_service_accounts_sign_jwt_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SignJwtResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SignJwtResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:signJwt
/// Signs a JWT using a service account''s system-managed private key.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iamcredentials_projects_service_accounts_sign_jwt_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iamcredentials_projects_service_accounts_sign_jwt_task()`.
/// For the simplest API, use `iamcredentials_projects_service_accounts_sign_jwt()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iamcredentials_projects_service_accounts_sign_jwt_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iamcredentials_projects_service_accounts_sign_jwt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignJwtResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = iamcredentials_projects_service_accounts_sign_jwt_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iamcredentials_projects_service_accounts_sign_jwt`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IamcredentialsProjectsServiceAccountsSignJwtArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/serviceAccounts/{serviceAccountsId}:signJwt
/// Signs a JWT using a service account''s system-managed private key.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iamcredentials_projects_service_accounts_sign_jwt_builder()` + `iamcredentials_projects_service_accounts_sign_jwt_execute()`.
/// For task-level control, use `iamcredentials_projects_service_accounts_sign_jwt_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iamcredentials_projects_service_accounts_sign_jwt(
    client: &SimpleHttpClient,
    args: &IamcredentialsProjectsServiceAccountsSignJwtArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SignJwtResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = iamcredentials_projects_service_accounts_sign_jwt_builder(client, &args.name)?;
    iamcredentials_projects_service_accounts_sign_jwt_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for WorkforcePoolAllowedLocations
// =============================================================================

/// ResourceIdentifier implementation for WorkforcePoolAllowedLocations with IamcredentialsLocationsWorkforcePoolsGetAllowedLocationsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IamcredentialsLocationsWorkforcePoolsGetAllowedLocationsArgs>
    for WorkforcePoolAllowedLocations
{
    fn generate_resource_id(
        &self,
        input: &IamcredentialsLocationsWorkforcePoolsGetAllowedLocationsArgs,
    ) -> String {
        format!(
            "gcp::iamcredentials::WorkforcePoolAllowedLocations/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::iamcredentials::WorkforcePoolAllowedLocations"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for WorkloadIdentityPoolAllowedLocations
// =============================================================================

/// ResourceIdentifier implementation for WorkloadIdentityPoolAllowedLocations with IamcredentialsProjectsLocationsWorkloadIdentityPoolsGetAllowedLocationsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IamcredentialsProjectsLocationsWorkloadIdentityPoolsGetAllowedLocationsArgs>
    for WorkloadIdentityPoolAllowedLocations
{
    fn generate_resource_id(
        &self,
        input: &IamcredentialsProjectsLocationsWorkloadIdentityPoolsGetAllowedLocationsArgs,
    ) -> String {
        format!(
            "gcp::iamcredentials::WorkloadIdentityPoolAllowedLocations/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::iamcredentials::WorkloadIdentityPoolAllowedLocations"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GenerateAccessTokenResponse
// =============================================================================

/// ResourceIdentifier implementation for GenerateAccessTokenResponse with IamcredentialsProjectsServiceAccountsGenerateAccessTokenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IamcredentialsProjectsServiceAccountsGenerateAccessTokenArgs>
    for GenerateAccessTokenResponse
{
    fn generate_resource_id(
        &self,
        input: &IamcredentialsProjectsServiceAccountsGenerateAccessTokenArgs,
    ) -> String {
        format!(
            "gcp::iamcredentials::GenerateAccessTokenResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::iamcredentials::GenerateAccessTokenResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GenerateIdTokenResponse
// =============================================================================

/// ResourceIdentifier implementation for GenerateIdTokenResponse with IamcredentialsProjectsServiceAccountsGenerateIdTokenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IamcredentialsProjectsServiceAccountsGenerateIdTokenArgs>
    for GenerateIdTokenResponse
{
    fn generate_resource_id(
        &self,
        input: &IamcredentialsProjectsServiceAccountsGenerateIdTokenArgs,
    ) -> String {
        format!(
            "gcp::iamcredentials::GenerateIdTokenResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::iamcredentials::GenerateIdTokenResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServiceAccountAllowedLocations
// =============================================================================

/// ResourceIdentifier implementation for ServiceAccountAllowedLocations with IamcredentialsProjectsServiceAccountsGetAllowedLocationsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IamcredentialsProjectsServiceAccountsGetAllowedLocationsArgs>
    for ServiceAccountAllowedLocations
{
    fn generate_resource_id(
        &self,
        input: &IamcredentialsProjectsServiceAccountsGetAllowedLocationsArgs,
    ) -> String {
        format!(
            "gcp::iamcredentials::ServiceAccountAllowedLocations/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::iamcredentials::ServiceAccountAllowedLocations"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SignBlobResponse
// =============================================================================

/// ResourceIdentifier implementation for SignBlobResponse with IamcredentialsProjectsServiceAccountsSignBlobArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IamcredentialsProjectsServiceAccountsSignBlobArgs> for SignBlobResponse {
    fn generate_resource_id(
        &self,
        input: &IamcredentialsProjectsServiceAccountsSignBlobArgs,
    ) -> String {
        format!("gcp::iamcredentials::SignBlobResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::iamcredentials::SignBlobResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SignJwtResponse
// =============================================================================

/// ResourceIdentifier implementation for SignJwtResponse with IamcredentialsProjectsServiceAccountsSignJwtArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<IamcredentialsProjectsServiceAccountsSignJwtArgs> for SignJwtResponse {
    fn generate_resource_id(
        &self,
        input: &IamcredentialsProjectsServiceAccountsSignJwtArgs,
    ) -> String {
        format!("gcp::iamcredentials::SignJwtResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::iamcredentials::SignJwtResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
