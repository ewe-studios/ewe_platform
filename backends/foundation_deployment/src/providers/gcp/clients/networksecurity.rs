//! Auto-generated API clients for gcp/networksecurity.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:addItems
/// Adds items to an address group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_address_groups_add_items_execute()` to send, or `networksecurity_organizations_locations_address_groups_add_items` for simplest API.

pub fn networksecurity_organizations_locations_address_groups_add_items_builder<R>(
    client: &SimpleHttpClient<R>,
    addressGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/addressGroups/{addressGroupsId}:addItems",
        addressGroup,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:addItems
/// Adds items to an address group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_address_groups_add_items_execute()` or `networksecurity_organizations_locations_address_groups_add_items`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_add_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_add_items_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:addItems
/// Adds items to an address group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_address_groups_add_items_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_add_items_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_address_groups_add_items()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_add_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_address_groups_add_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_address_groups_add_items_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_address_groups_add_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsAddressGroupsAddItemsArgs {
    /// Path parameter: addressGroup
    pub addressGroup: String,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:addItems
/// Adds items to an address group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_address_groups_add_items_builder()` + `networksecurity_organizations_locations_address_groups_add_items_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_add_items_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_add_items(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsAddressGroupsAddItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_address_groups_add_items_builder(
        client,
        &args.addressGroup,
    )?;
    networksecurity_organizations_locations_address_groups_add_items_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:cloneItems
/// Clones items from one address group to another.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_address_groups_clone_items_execute()` to send, or `networksecurity_organizations_locations_address_groups_clone_items` for simplest API.

pub fn networksecurity_organizations_locations_address_groups_clone_items_builder<R>(
    client: &SimpleHttpClient<R>,
    addressGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/addressGroups/{addressGroupsId}:cloneItems",
        addressGroup,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:cloneItems
/// Clones items from one address group to another.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_address_groups_clone_items_execute()` or `networksecurity_organizations_locations_address_groups_clone_items`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_clone_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_clone_items_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:cloneItems
/// Clones items from one address group to another.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_address_groups_clone_items_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_clone_items_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_address_groups_clone_items()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_clone_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_address_groups_clone_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_address_groups_clone_items_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_address_groups_clone_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsAddressGroupsCloneItemsArgs {
    /// Path parameter: addressGroup
    pub addressGroup: String,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:cloneItems
/// Clones items from one address group to another.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_address_groups_clone_items_builder()` + `networksecurity_organizations_locations_address_groups_clone_items_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_clone_items_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_clone_items(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsAddressGroupsCloneItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_address_groups_clone_items_builder(
        client,
        &args.addressGroup,
    )?;
    networksecurity_organizations_locations_address_groups_clone_items_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups
/// Creates a new address group in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_address_groups_create_execute()` to send, or `networksecurity_organizations_locations_address_groups_create` for simplest API.

pub fn networksecurity_organizations_locations_address_groups_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    addressGroupId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/addressGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = addressGroupId.as_ref() {
        query_parts.push(format!("addressGroupId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups
/// Creates a new address group in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_address_groups_create_execute()` or `networksecurity_organizations_locations_address_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups
/// Creates a new address group in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_address_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_create_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_address_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_address_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_address_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_address_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsAddressGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: addressGroupId
    pub addressGroupId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups
/// Creates a new address group in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_address_groups_create_builder()` + `networksecurity_organizations_locations_address_groups_create_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsAddressGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_address_groups_create_builder(
        client,
        &args.parent,
        &args.addressGroupId,
        &args.requestId,
    )?;
    networksecurity_organizations_locations_address_groups_create_execute(builder)
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Deletes an address group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_address_groups_delete_execute()` to send, or `networksecurity_organizations_locations_address_groups_delete` for simplest API.

pub fn networksecurity_organizations_locations_address_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/addressGroups/{addressGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Deletes an address group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_address_groups_delete_execute()` or `networksecurity_organizations_locations_address_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Deletes an address group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_address_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_delete_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_address_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_address_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_address_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_address_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsAddressGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Deletes an address group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_address_groups_delete_builder()` + `networksecurity_organizations_locations_address_groups_delete_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsAddressGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_address_groups_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networksecurity_organizations_locations_address_groups_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Gets details of a single address group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_address_groups_get_execute()` to send, or `networksecurity_organizations_locations_address_groups_get` for simplest API.

pub fn networksecurity_organizations_locations_address_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/addressGroups/{addressGroupsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Gets details of a single address group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_address_groups_get_execute()` or `networksecurity_organizations_locations_address_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddressGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddressGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Gets details of a single address group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_address_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_get_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_address_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_address_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_address_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_address_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsAddressGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Gets details of a single address group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_address_groups_get_builder()` + `networksecurity_organizations_locations_address_groups_get_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsAddressGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_organizations_locations_address_groups_get_builder(client, &args.name)?;
    networksecurity_organizations_locations_address_groups_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups
/// Lists address groups in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_address_groups_list_execute()` to send, or `networksecurity_organizations_locations_address_groups_list` for simplest API.

pub fn networksecurity_organizations_locations_address_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/addressGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups
/// Lists address groups in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_address_groups_list_execute()` or `networksecurity_organizations_locations_address_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAddressGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAddressGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups
/// Lists address groups in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_address_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_list_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_address_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_address_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAddressGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_address_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_address_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsAddressGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups
/// Lists address groups in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_address_groups_list_builder()` + `networksecurity_organizations_locations_address_groups_list_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsAddressGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAddressGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_address_groups_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    networksecurity_organizations_locations_address_groups_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:listReferences
/// Lists references of an address group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_address_groups_list_references_execute()` to send, or `networksecurity_organizations_locations_address_groups_list_references` for simplest API.

pub fn networksecurity_organizations_locations_address_groups_list_references_builder<R>(
    client: &SimpleHttpClient<R>,
    addressGroup: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/addressGroups/{addressGroupsId}:listReferences",
        addressGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:listReferences
/// Lists references of an address group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_address_groups_list_references_execute()` or `networksecurity_organizations_locations_address_groups_list_references`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_list_references_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_list_references_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAddressGroupReferencesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAddressGroupReferencesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:listReferences
/// Lists references of an address group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_address_groups_list_references_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_list_references_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_address_groups_list_references()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_list_references_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_address_groups_list_references_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAddressGroupReferencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networksecurity_organizations_locations_address_groups_list_references_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_address_groups_list_references`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsAddressGroupsListReferencesArgs {
    /// Path parameter: addressGroup
    pub addressGroup: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:listReferences
/// Lists references of an address group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_address_groups_list_references_builder()` + `networksecurity_organizations_locations_address_groups_list_references_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_list_references_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_list_references(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsAddressGroupsListReferencesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAddressGroupReferencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_address_groups_list_references_builder(
        client,
        &args.addressGroup,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_organizations_locations_address_groups_list_references_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Updates parameters of an address group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_address_groups_patch_execute()` to send, or `networksecurity_organizations_locations_address_groups_patch` for simplest API.

pub fn networksecurity_organizations_locations_address_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/addressGroups/{addressGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Updates parameters of an address group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_address_groups_patch_execute()` or `networksecurity_organizations_locations_address_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Updates parameters of an address group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_address_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_patch_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_address_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_address_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_address_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_address_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsAddressGroupsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Updates parameters of an address group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_address_groups_patch_builder()` + `networksecurity_organizations_locations_address_groups_patch_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsAddressGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_address_groups_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networksecurity_organizations_locations_address_groups_patch_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:removeItems
/// Removes items from an address group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_address_groups_remove_items_execute()` to send, or `networksecurity_organizations_locations_address_groups_remove_items` for simplest API.

pub fn networksecurity_organizations_locations_address_groups_remove_items_builder<R>(
    client: &SimpleHttpClient<R>,
    addressGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/addressGroups/{addressGroupsId}:removeItems",
        addressGroup,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:removeItems
/// Removes items from an address group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_address_groups_remove_items_execute()` or `networksecurity_organizations_locations_address_groups_remove_items`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_remove_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_remove_items_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:removeItems
/// Removes items from an address group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_address_groups_remove_items_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_remove_items_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_address_groups_remove_items()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_address_groups_remove_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_address_groups_remove_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_address_groups_remove_items_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_address_groups_remove_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsAddressGroupsRemoveItemsArgs {
    /// Path parameter: addressGroup
    pub addressGroup: String,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:removeItems
/// Removes items from an address group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_address_groups_remove_items_builder()` + `networksecurity_organizations_locations_address_groups_remove_items_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_address_groups_remove_items_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_address_groups_remove_items(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsAddressGroupsRemoveItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_address_groups_remove_items_builder(
        client,
        &args.addressGroup,
    )?;
    networksecurity_organizations_locations_address_groups_remove_items_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints
/// Creates a new FirewallEndpoint in a given organization and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_firewall_endpoints_create_execute()` to send, or `networksecurity_organizations_locations_firewall_endpoints_create` for simplest API.

pub fn networksecurity_organizations_locations_firewall_endpoints_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    firewallEndpointId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/firewallEndpoints",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = firewallEndpointId.as_ref() {
        query_parts.push(format!("firewallEndpointId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints
/// Creates a new FirewallEndpoint in a given organization and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_firewall_endpoints_create_execute()` or `networksecurity_organizations_locations_firewall_endpoints_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_firewall_endpoints_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_firewall_endpoints_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints
/// Creates a new FirewallEndpoint in a given organization and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_firewall_endpoints_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_firewall_endpoints_create_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_firewall_endpoints_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_firewall_endpoints_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_firewall_endpoints_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_firewall_endpoints_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_firewall_endpoints_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsFirewallEndpointsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: firewallEndpointId
    pub firewallEndpointId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints
/// Creates a new FirewallEndpoint in a given organization and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_firewall_endpoints_create_builder()` + `networksecurity_organizations_locations_firewall_endpoints_create_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_firewall_endpoints_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_firewall_endpoints_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsFirewallEndpointsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_firewall_endpoints_create_builder(
        client,
        &args.parent,
        &args.firewallEndpointId,
        &args.requestId,
    )?;
    networksecurity_organizations_locations_firewall_endpoints_create_execute(builder)
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}
/// Deletes a single org Endpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_firewall_endpoints_delete_execute()` to send, or `networksecurity_organizations_locations_firewall_endpoints_delete` for simplest API.

pub fn networksecurity_organizations_locations_firewall_endpoints_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}
/// Deletes a single org Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_firewall_endpoints_delete_execute()` or `networksecurity_organizations_locations_firewall_endpoints_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_firewall_endpoints_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_firewall_endpoints_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}
/// Deletes a single org Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_firewall_endpoints_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_firewall_endpoints_delete_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_firewall_endpoints_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_firewall_endpoints_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_firewall_endpoints_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_firewall_endpoints_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_firewall_endpoints_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsFirewallEndpointsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}
/// Deletes a single org Endpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_firewall_endpoints_delete_builder()` + `networksecurity_organizations_locations_firewall_endpoints_delete_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_firewall_endpoints_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_firewall_endpoints_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsFirewallEndpointsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_firewall_endpoints_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networksecurity_organizations_locations_firewall_endpoints_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}
/// Gets details of a single org Endpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_firewall_endpoints_get_execute()` to send, or `networksecurity_organizations_locations_firewall_endpoints_get` for simplest API.

pub fn networksecurity_organizations_locations_firewall_endpoints_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}
/// Gets details of a single org Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_firewall_endpoints_get_execute()` or `networksecurity_organizations_locations_firewall_endpoints_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_firewall_endpoints_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_firewall_endpoints_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallEndpoint>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallEndpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}
/// Gets details of a single org Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_firewall_endpoints_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_firewall_endpoints_get_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_firewall_endpoints_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_firewall_endpoints_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_firewall_endpoints_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallEndpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_firewall_endpoints_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_firewall_endpoints_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsFirewallEndpointsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}
/// Gets details of a single org Endpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_firewall_endpoints_get_builder()` + `networksecurity_organizations_locations_firewall_endpoints_get_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_firewall_endpoints_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_firewall_endpoints_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsFirewallEndpointsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FirewallEndpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_organizations_locations_firewall_endpoints_get_builder(client, &args.name)?;
    networksecurity_organizations_locations_firewall_endpoints_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints
/// Lists FirewallEndpoints in a given organization and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_firewall_endpoints_list_execute()` to send, or `networksecurity_organizations_locations_firewall_endpoints_list` for simplest API.

pub fn networksecurity_organizations_locations_firewall_endpoints_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/firewallEndpoints",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints
/// Lists FirewallEndpoints in a given organization and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_firewall_endpoints_list_execute()` or `networksecurity_organizations_locations_firewall_endpoints_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_firewall_endpoints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_firewall_endpoints_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFirewallEndpointsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFirewallEndpointsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints
/// Lists FirewallEndpoints in a given organization and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_firewall_endpoints_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_firewall_endpoints_list_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_firewall_endpoints_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_firewall_endpoints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_firewall_endpoints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFirewallEndpointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_firewall_endpoints_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_firewall_endpoints_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsFirewallEndpointsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints
/// Lists FirewallEndpoints in a given organization and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_firewall_endpoints_list_builder()` + `networksecurity_organizations_locations_firewall_endpoints_list_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_firewall_endpoints_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_firewall_endpoints_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsFirewallEndpointsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFirewallEndpointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_firewall_endpoints_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_organizations_locations_firewall_endpoints_list_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}
/// Update a single org Endpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_firewall_endpoints_patch_execute()` to send, or `networksecurity_organizations_locations_firewall_endpoints_patch` for simplest API.

pub fn networksecurity_organizations_locations_firewall_endpoints_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}
/// Update a single org Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_firewall_endpoints_patch_execute()` or `networksecurity_organizations_locations_firewall_endpoints_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_firewall_endpoints_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_firewall_endpoints_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}
/// Update a single org Endpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_firewall_endpoints_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_firewall_endpoints_patch_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_firewall_endpoints_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_firewall_endpoints_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_firewall_endpoints_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_firewall_endpoints_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_firewall_endpoints_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsFirewallEndpointsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/firewallEndpoints/{firewallEndpointsId}
/// Update a single org Endpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_firewall_endpoints_patch_builder()` + `networksecurity_organizations_locations_firewall_endpoints_patch_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_firewall_endpoints_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_firewall_endpoints_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsFirewallEndpointsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_firewall_endpoints_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networksecurity_organizations_locations_firewall_endpoints_patch_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_operations_cancel_execute()` to send, or `networksecurity_organizations_locations_operations_cancel` for simplest API.

pub fn networksecurity_organizations_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_operations_cancel_execute()` or `networksecurity_organizations_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_operations_cancel_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_operations_cancel_builder()` + `networksecurity_organizations_locations_operations_cancel_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_organizations_locations_operations_cancel_builder(client, &args.name)?;
    networksecurity_organizations_locations_operations_cancel_execute(builder)
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_operations_delete_execute()` to send, or `networksecurity_organizations_locations_operations_delete` for simplest API.

pub fn networksecurity_organizations_locations_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_operations_delete_execute()` or `networksecurity_organizations_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_operations_delete_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_operations_delete_builder()` + `networksecurity_organizations_locations_operations_delete_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_organizations_locations_operations_delete_builder(client, &args.name)?;
    networksecurity_organizations_locations_operations_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_operations_get_execute()` to send, or `networksecurity_organizations_locations_operations_get` for simplest API.

pub fn networksecurity_organizations_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_operations_get_execute()` or `networksecurity_organizations_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_operations_get_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_operations_get_builder()` + `networksecurity_organizations_locations_operations_get_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_operations_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_organizations_locations_operations_get_builder(client, &args.name)?;
    networksecurity_organizations_locations_operations_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_operations_list_execute()` to send, or `networksecurity_organizations_locations_operations_list` for simplest API.

pub fn networksecurity_organizations_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_operations_list_execute()` or `networksecurity_organizations_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_operations_list_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_operations_list_builder()` + `networksecurity_organizations_locations_operations_list_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_operations_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    networksecurity_organizations_locations_operations_list_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups
/// Creates a new SecurityProfileGroup in a given organization and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_security_profile_groups_create_execute()` to send, or `networksecurity_organizations_locations_security_profile_groups_create` for simplest API.

pub fn networksecurity_organizations_locations_security_profile_groups_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    securityProfileGroupId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/securityProfileGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = securityProfileGroupId.as_ref() {
        query_parts.push(format!("securityProfileGroupId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups
/// Creates a new SecurityProfileGroup in a given organization and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_security_profile_groups_create_execute()` or `networksecurity_organizations_locations_security_profile_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profile_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profile_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups
/// Creates a new SecurityProfileGroup in a given organization and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_security_profile_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_security_profile_groups_create_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_security_profile_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profile_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_security_profile_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_organizations_locations_security_profile_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_security_profile_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsSecurityProfileGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: securityProfileGroupId
    pub securityProfileGroupId: Option<Option<String>>,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups
/// Creates a new SecurityProfileGroup in a given organization and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_security_profile_groups_create_builder()` + `networksecurity_organizations_locations_security_profile_groups_create_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_security_profile_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profile_groups_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_security_profile_groups_create_builder(
        client,
        &args.parent,
        &args.securityProfileGroupId,
    )?;
    networksecurity_organizations_locations_security_profile_groups_create_execute(builder)
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}
/// Deletes a single SecurityProfileGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_security_profile_groups_delete_execute()` to send, or `networksecurity_organizations_locations_security_profile_groups_delete` for simplest API.

pub fn networksecurity_organizations_locations_security_profile_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    etag: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}
/// Deletes a single SecurityProfileGroup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_security_profile_groups_delete_execute()` or `networksecurity_organizations_locations_security_profile_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profile_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profile_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}
/// Deletes a single SecurityProfileGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_security_profile_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_security_profile_groups_delete_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_security_profile_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profile_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_security_profile_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_organizations_locations_security_profile_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_security_profile_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsSecurityProfileGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<Option<String>>,
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}
/// Deletes a single SecurityProfileGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_security_profile_groups_delete_builder()` + `networksecurity_organizations_locations_security_profile_groups_delete_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_security_profile_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profile_groups_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_security_profile_groups_delete_builder(
        client, &args.name, &args.etag,
    )?;
    networksecurity_organizations_locations_security_profile_groups_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}
/// Gets details of a single SecurityProfileGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_security_profile_groups_get_execute()` to send, or `networksecurity_organizations_locations_security_profile_groups_get` for simplest API.

pub fn networksecurity_organizations_locations_security_profile_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}
/// Gets details of a single SecurityProfileGroup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_security_profile_groups_get_execute()` or `networksecurity_organizations_locations_security_profile_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profile_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profile_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityProfileGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityProfileGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}
/// Gets details of a single SecurityProfileGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_security_profile_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_security_profile_groups_get_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_security_profile_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profile_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_security_profile_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityProfileGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_security_profile_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_security_profile_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsSecurityProfileGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}
/// Gets details of a single SecurityProfileGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_security_profile_groups_get_builder()` + `networksecurity_organizations_locations_security_profile_groups_get_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_security_profile_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profile_groups_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityProfileGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_security_profile_groups_get_builder(
        client, &args.name,
    )?;
    networksecurity_organizations_locations_security_profile_groups_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups
/// Lists SecurityProfileGroups in a given organization and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_security_profile_groups_list_execute()` to send, or `networksecurity_organizations_locations_security_profile_groups_list` for simplest API.

pub fn networksecurity_organizations_locations_security_profile_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/securityProfileGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups
/// Lists SecurityProfileGroups in a given organization and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_security_profile_groups_list_execute()` or `networksecurity_organizations_locations_security_profile_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profile_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profile_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSecurityProfileGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSecurityProfileGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups
/// Lists SecurityProfileGroups in a given organization and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_security_profile_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_security_profile_groups_list_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_security_profile_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profile_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_security_profile_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityProfileGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_security_profile_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_security_profile_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsSecurityProfileGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups
/// Lists SecurityProfileGroups in a given organization and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_security_profile_groups_list_builder()` + `networksecurity_organizations_locations_security_profile_groups_list_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_security_profile_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profile_groups_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityProfileGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_security_profile_groups_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_organizations_locations_security_profile_groups_list_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}
/// Updates the parameters of a single SecurityProfileGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_security_profile_groups_patch_execute()` to send, or `networksecurity_organizations_locations_security_profile_groups_patch` for simplest API.

pub fn networksecurity_organizations_locations_security_profile_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}
/// Updates the parameters of a single SecurityProfileGroup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_security_profile_groups_patch_execute()` or `networksecurity_organizations_locations_security_profile_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profile_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profile_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}
/// Updates the parameters of a single SecurityProfileGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_security_profile_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_security_profile_groups_patch_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_security_profile_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profile_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_security_profile_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_security_profile_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_security_profile_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsSecurityProfileGroupsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/securityProfileGroups/{securityProfileGroupsId}
/// Updates the parameters of a single SecurityProfileGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_security_profile_groups_patch_builder()` + `networksecurity_organizations_locations_security_profile_groups_patch_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_security_profile_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profile_groups_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_security_profile_groups_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    networksecurity_organizations_locations_security_profile_groups_patch_execute(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles
/// Creates a new SecurityProfile in a given organization and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_security_profiles_create_execute()` to send, or `networksecurity_organizations_locations_security_profiles_create` for simplest API.

pub fn networksecurity_organizations_locations_security_profiles_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    securityProfileId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/securityProfiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = securityProfileId.as_ref() {
        query_parts.push(format!("securityProfileId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles
/// Creates a new SecurityProfile in a given organization and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_security_profiles_create_execute()` or `networksecurity_organizations_locations_security_profiles_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profiles_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profiles_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles
/// Creates a new SecurityProfile in a given organization and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_security_profiles_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_security_profiles_create_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_security_profiles_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profiles_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_security_profiles_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_security_profiles_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_security_profiles_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsSecurityProfilesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: securityProfileId
    pub securityProfileId: Option<Option<String>>,
}

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles
/// Creates a new SecurityProfile in a given organization and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_security_profiles_create_builder()` + `networksecurity_organizations_locations_security_profiles_create_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_security_profiles_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profiles_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsSecurityProfilesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_security_profiles_create_builder(
        client,
        &args.parent,
        &args.securityProfileId,
    )?;
    networksecurity_organizations_locations_security_profiles_create_execute(builder)
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles/{securityProfilesId}
/// Deletes a single SecurityProfile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_security_profiles_delete_execute()` to send, or `networksecurity_organizations_locations_security_profiles_delete` for simplest API.

pub fn networksecurity_organizations_locations_security_profiles_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    etag: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/securityProfiles/{securityProfilesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles/{securityProfilesId}
/// Deletes a single SecurityProfile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_security_profiles_delete_execute()` or `networksecurity_organizations_locations_security_profiles_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profiles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profiles_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles/{securityProfilesId}
/// Deletes a single SecurityProfile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_security_profiles_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_security_profiles_delete_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_security_profiles_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profiles_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_security_profiles_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_security_profiles_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_security_profiles_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsSecurityProfilesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<Option<String>>,
}

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles/{securityProfilesId}
/// Deletes a single SecurityProfile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_security_profiles_delete_builder()` + `networksecurity_organizations_locations_security_profiles_delete_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_security_profiles_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profiles_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsSecurityProfilesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_security_profiles_delete_builder(
        client, &args.name, &args.etag,
    )?;
    networksecurity_organizations_locations_security_profiles_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles/{securityProfilesId}
/// Gets details of a single SecurityProfile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_security_profiles_get_execute()` to send, or `networksecurity_organizations_locations_security_profiles_get` for simplest API.

pub fn networksecurity_organizations_locations_security_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/securityProfiles/{securityProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles/{securityProfilesId}
/// Gets details of a single SecurityProfile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_security_profiles_get_execute()` or `networksecurity_organizations_locations_security_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SecurityProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles/{securityProfilesId}
/// Gets details of a single SecurityProfile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_security_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_security_profiles_get_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_security_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_security_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_security_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_security_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsSecurityProfilesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles/{securityProfilesId}
/// Gets details of a single SecurityProfile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_security_profiles_get_builder()` + `networksecurity_organizations_locations_security_profiles_get_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_security_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profiles_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsSecurityProfilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SecurityProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_organizations_locations_security_profiles_get_builder(client, &args.name)?;
    networksecurity_organizations_locations_security_profiles_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles
/// Lists SecurityProfiles in a given organization and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_security_profiles_list_execute()` to send, or `networksecurity_organizations_locations_security_profiles_list` for simplest API.

pub fn networksecurity_organizations_locations_security_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/securityProfiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles
/// Lists SecurityProfiles in a given organization and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_security_profiles_list_execute()` or `networksecurity_organizations_locations_security_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSecurityProfilesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSecurityProfilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles
/// Lists SecurityProfiles in a given organization and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_security_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_security_profiles_list_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_security_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_security_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_security_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_security_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsSecurityProfilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles
/// Lists SecurityProfiles in a given organization and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_security_profiles_list_builder()` + `networksecurity_organizations_locations_security_profiles_list_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_security_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profiles_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsSecurityProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSecurityProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_security_profiles_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_organizations_locations_security_profiles_list_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles/{securityProfilesId}
/// Updates the parameters of a single SecurityProfile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_organizations_locations_security_profiles_patch_execute()` to send, or `networksecurity_organizations_locations_security_profiles_patch` for simplest API.

pub fn networksecurity_organizations_locations_security_profiles_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/organizations/{}/locations/{locationsId}/securityProfiles/{securityProfilesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles/{securityProfilesId}
/// Updates the parameters of a single SecurityProfile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_organizations_locations_security_profiles_patch_execute()` or `networksecurity_organizations_locations_security_profiles_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profiles_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profiles_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles/{securityProfilesId}
/// Updates the parameters of a single SecurityProfile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_organizations_locations_security_profiles_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_organizations_locations_security_profiles_patch_task()`.
/// For the simplest API, use `networksecurity_organizations_locations_security_profiles_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_organizations_locations_security_profiles_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_organizations_locations_security_profiles_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_organizations_locations_security_profiles_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_organizations_locations_security_profiles_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityOrganizationsLocationsSecurityProfilesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/securityProfiles/{securityProfilesId}
/// Updates the parameters of a single SecurityProfile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_organizations_locations_security_profiles_patch_builder()` + `networksecurity_organizations_locations_security_profiles_patch_execute()`.
/// For task-level control, use `networksecurity_organizations_locations_security_profiles_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_organizations_locations_security_profiles_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityOrganizationsLocationsSecurityProfilesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_organizations_locations_security_profiles_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    networksecurity_organizations_locations_security_profiles_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_get_execute()` to send, or `networksecurity_projects_locations_get` for simplest API.

pub fn networksecurity_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_get_execute()` or `networksecurity_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_get_builder()` + `networksecurity_projects_locations_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_get_builder(client, &args.name)?;
    networksecurity_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_list_execute()` to send, or `networksecurity_projects_locations_list` for simplest API.

pub fn networksecurity_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_list_execute()` or `networksecurity_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_list_builder()` + `networksecurity_projects_locations_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:addItems
/// Adds items to an address group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_address_groups_add_items_execute()` to send, or `networksecurity_projects_locations_address_groups_add_items` for simplest API.

pub fn networksecurity_projects_locations_address_groups_add_items_builder<R>(
    client: &SimpleHttpClient<R>,
    addressGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/addressGroups/{addressGroupsId}:addItems",
        addressGroup,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:addItems
/// Adds items to an address group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_address_groups_add_items_execute()` or `networksecurity_projects_locations_address_groups_add_items`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_add_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_add_items_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:addItems
/// Adds items to an address group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_address_groups_add_items_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_address_groups_add_items_task()`.
/// For the simplest API, use `networksecurity_projects_locations_address_groups_add_items()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_add_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_address_groups_add_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_address_groups_add_items_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_address_groups_add_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAddressGroupsAddItemsArgs {
    /// Path parameter: addressGroup
    pub addressGroup: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:addItems
/// Adds items to an address group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_address_groups_add_items_builder()` + `networksecurity_projects_locations_address_groups_add_items_execute()`.
/// For task-level control, use `networksecurity_projects_locations_address_groups_add_items_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_add_items(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAddressGroupsAddItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_address_groups_add_items_builder(
        client,
        &args.addressGroup,
    )?;
    networksecurity_projects_locations_address_groups_add_items_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:cloneItems
/// Clones items from one address group to another.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_address_groups_clone_items_execute()` to send, or `networksecurity_projects_locations_address_groups_clone_items` for simplest API.

pub fn networksecurity_projects_locations_address_groups_clone_items_builder<R>(
    client: &SimpleHttpClient<R>,
    addressGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/addressGroups/{addressGroupsId}:cloneItems",
        addressGroup,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:cloneItems
/// Clones items from one address group to another.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_address_groups_clone_items_execute()` or `networksecurity_projects_locations_address_groups_clone_items`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_clone_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_clone_items_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:cloneItems
/// Clones items from one address group to another.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_address_groups_clone_items_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_address_groups_clone_items_task()`.
/// For the simplest API, use `networksecurity_projects_locations_address_groups_clone_items()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_clone_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_address_groups_clone_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_address_groups_clone_items_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_address_groups_clone_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAddressGroupsCloneItemsArgs {
    /// Path parameter: addressGroup
    pub addressGroup: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:cloneItems
/// Clones items from one address group to another.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_address_groups_clone_items_builder()` + `networksecurity_projects_locations_address_groups_clone_items_execute()`.
/// For task-level control, use `networksecurity_projects_locations_address_groups_clone_items_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_clone_items(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAddressGroupsCloneItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_address_groups_clone_items_builder(
        client,
        &args.addressGroup,
    )?;
    networksecurity_projects_locations_address_groups_clone_items_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups
/// Creates a new address group in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_address_groups_create_execute()` to send, or `networksecurity_projects_locations_address_groups_create` for simplest API.

pub fn networksecurity_projects_locations_address_groups_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    addressGroupId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/addressGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = addressGroupId.as_ref() {
        query_parts.push(format!("addressGroupId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups
/// Creates a new address group in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_address_groups_create_execute()` or `networksecurity_projects_locations_address_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups
/// Creates a new address group in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_address_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_address_groups_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_address_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_address_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_address_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_address_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAddressGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: addressGroupId
    pub addressGroupId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups
/// Creates a new address group in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_address_groups_create_builder()` + `networksecurity_projects_locations_address_groups_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_address_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAddressGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_address_groups_create_builder(
        client,
        &args.parent,
        &args.addressGroupId,
        &args.requestId,
    )?;
    networksecurity_projects_locations_address_groups_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Deletes a single address group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_address_groups_delete_execute()` to send, or `networksecurity_projects_locations_address_groups_delete` for simplest API.

pub fn networksecurity_projects_locations_address_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/addressGroups/{addressGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Deletes a single address group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_address_groups_delete_execute()` or `networksecurity_projects_locations_address_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Deletes a single address group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_address_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_address_groups_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_address_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_address_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_address_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_address_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAddressGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Deletes a single address group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_address_groups_delete_builder()` + `networksecurity_projects_locations_address_groups_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_address_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAddressGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_address_groups_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networksecurity_projects_locations_address_groups_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Gets details of a single address group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_address_groups_get_execute()` to send, or `networksecurity_projects_locations_address_groups_get` for simplest API.

pub fn networksecurity_projects_locations_address_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/addressGroups/{addressGroupsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Gets details of a single address group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_address_groups_get_execute()` or `networksecurity_projects_locations_address_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddressGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddressGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Gets details of a single address group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_address_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_address_groups_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_address_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_address_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_address_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_address_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAddressGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Gets details of a single address group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_address_groups_get_builder()` + `networksecurity_projects_locations_address_groups_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_address_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAddressGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddressGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_address_groups_get_builder(client, &args.name)?;
    networksecurity_projects_locations_address_groups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_address_groups_get_iam_policy_execute()` to send, or `networksecurity_projects_locations_address_groups_get_iam_policy` for simplest API.

pub fn networksecurity_projects_locations_address_groups_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/addressGroups/{addressGroupsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_address_groups_get_iam_policy_execute()` or `networksecurity_projects_locations_address_groups_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_address_groups_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_address_groups_get_iam_policy_task()`.
/// For the simplest API, use `networksecurity_projects_locations_address_groups_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_address_groups_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_address_groups_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_address_groups_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAddressGroupsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_address_groups_get_iam_policy_builder()` + `networksecurity_projects_locations_address_groups_get_iam_policy_execute()`.
/// For task-level control, use `networksecurity_projects_locations_address_groups_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_get_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAddressGroupsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_address_groups_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    networksecurity_projects_locations_address_groups_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups
/// Lists address groups in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_address_groups_list_execute()` to send, or `networksecurity_projects_locations_address_groups_list` for simplest API.

pub fn networksecurity_projects_locations_address_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/addressGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups
/// Lists address groups in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_address_groups_list_execute()` or `networksecurity_projects_locations_address_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAddressGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAddressGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups
/// Lists address groups in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_address_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_address_groups_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_address_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_address_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAddressGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_address_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_address_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAddressGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups
/// Lists address groups in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_address_groups_list_builder()` + `networksecurity_projects_locations_address_groups_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_address_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAddressGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAddressGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_address_groups_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    networksecurity_projects_locations_address_groups_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:listReferences
/// Lists references of an address group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_address_groups_list_references_execute()` to send, or `networksecurity_projects_locations_address_groups_list_references` for simplest API.

pub fn networksecurity_projects_locations_address_groups_list_references_builder<R>(
    client: &SimpleHttpClient<R>,
    addressGroup: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/addressGroups/{addressGroupsId}:listReferences",
        addressGroup,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:listReferences
/// Lists references of an address group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_address_groups_list_references_execute()` or `networksecurity_projects_locations_address_groups_list_references`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_list_references_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_list_references_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAddressGroupReferencesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAddressGroupReferencesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:listReferences
/// Lists references of an address group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_address_groups_list_references_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_address_groups_list_references_task()`.
/// For the simplest API, use `networksecurity_projects_locations_address_groups_list_references()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_list_references_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_address_groups_list_references_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAddressGroupReferencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_address_groups_list_references_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_address_groups_list_references`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAddressGroupsListReferencesArgs {
    /// Path parameter: addressGroup
    pub addressGroup: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:listReferences
/// Lists references of an address group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_address_groups_list_references_builder()` + `networksecurity_projects_locations_address_groups_list_references_execute()`.
/// For task-level control, use `networksecurity_projects_locations_address_groups_list_references_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_list_references(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAddressGroupsListReferencesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAddressGroupReferencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_address_groups_list_references_builder(
        client,
        &args.addressGroup,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_address_groups_list_references_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Updates the parameters of a single address group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_address_groups_patch_execute()` to send, or `networksecurity_projects_locations_address_groups_patch` for simplest API.

pub fn networksecurity_projects_locations_address_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/addressGroups/{addressGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Updates the parameters of a single address group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_address_groups_patch_execute()` or `networksecurity_projects_locations_address_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Updates the parameters of a single address group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_address_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_address_groups_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_address_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_address_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_address_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_address_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAddressGroupsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}
/// Updates the parameters of a single address group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_address_groups_patch_builder()` + `networksecurity_projects_locations_address_groups_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_address_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAddressGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_address_groups_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_address_groups_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:removeItems
/// Removes items from an address group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_address_groups_remove_items_execute()` to send, or `networksecurity_projects_locations_address_groups_remove_items` for simplest API.

pub fn networksecurity_projects_locations_address_groups_remove_items_builder<R>(
    client: &SimpleHttpClient<R>,
    addressGroup: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/addressGroups/{addressGroupsId}:removeItems",
        addressGroup,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:removeItems
/// Removes items from an address group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_address_groups_remove_items_execute()` or `networksecurity_projects_locations_address_groups_remove_items`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_remove_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_remove_items_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:removeItems
/// Removes items from an address group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_address_groups_remove_items_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_address_groups_remove_items_task()`.
/// For the simplest API, use `networksecurity_projects_locations_address_groups_remove_items()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_remove_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_address_groups_remove_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_address_groups_remove_items_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_address_groups_remove_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAddressGroupsRemoveItemsArgs {
    /// Path parameter: addressGroup
    pub addressGroup: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:removeItems
/// Removes items from an address group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_address_groups_remove_items_builder()` + `networksecurity_projects_locations_address_groups_remove_items_execute()`.
/// For task-level control, use `networksecurity_projects_locations_address_groups_remove_items_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_remove_items(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAddressGroupsRemoveItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_address_groups_remove_items_builder(
        client,
        &args.addressGroup,
    )?;
    networksecurity_projects_locations_address_groups_remove_items_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_address_groups_set_iam_policy_execute()` to send, or `networksecurity_projects_locations_address_groups_set_iam_policy` for simplest API.

pub fn networksecurity_projects_locations_address_groups_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/addressGroups/{addressGroupsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_address_groups_set_iam_policy_execute()` or `networksecurity_projects_locations_address_groups_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_address_groups_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_address_groups_set_iam_policy_task()`.
/// For the simplest API, use `networksecurity_projects_locations_address_groups_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_address_groups_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_address_groups_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_address_groups_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAddressGroupsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_address_groups_set_iam_policy_builder()` + `networksecurity_projects_locations_address_groups_set_iam_policy_execute()`.
/// For task-level control, use `networksecurity_projects_locations_address_groups_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_set_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAddressGroupsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_address_groups_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    networksecurity_projects_locations_address_groups_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_address_groups_test_iam_permissions_execute()` to send, or `networksecurity_projects_locations_address_groups_test_iam_permissions` for simplest API.

pub fn networksecurity_projects_locations_address_groups_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/addressGroups/{addressGroupsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_address_groups_test_iam_permissions_execute()` or `networksecurity_projects_locations_address_groups_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_address_groups_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_address_groups_test_iam_permissions_task()`.
/// For the simplest API, use `networksecurity_projects_locations_address_groups_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_address_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_address_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_address_groups_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_address_groups_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAddressGroupsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/addressGroups/{addressGroupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_address_groups_test_iam_permissions_builder()` + `networksecurity_projects_locations_address_groups_test_iam_permissions_execute()`.
/// For task-level control, use `networksecurity_projects_locations_address_groups_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_address_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAddressGroupsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_address_groups_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    networksecurity_projects_locations_address_groups_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies
/// Creates a new AuthorizationPolicy in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authorization_policies_create_execute()` to send, or `networksecurity_projects_locations_authorization_policies_create` for simplest API.

pub fn networksecurity_projects_locations_authorization_policies_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    authorizationPolicyId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authorizationPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = authorizationPolicyId.as_ref() {
        query_parts.push(format!("authorizationPolicyId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies
/// Creates a new AuthorizationPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authorization_policies_create_execute()` or `networksecurity_projects_locations_authorization_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies
/// Creates a new AuthorizationPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authorization_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authorization_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authorization_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_authorization_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authorization_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthorizationPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: authorizationPolicyId
    pub authorizationPolicyId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies
/// Creates a new AuthorizationPolicy in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authorization_policies_create_builder()` + `networksecurity_projects_locations_authorization_policies_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthorizationPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_authorization_policies_create_builder(
        client,
        &args.parent,
        &args.authorizationPolicyId,
    )?;
    networksecurity_projects_locations_authorization_policies_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}
/// Deletes a single AuthorizationPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authorization_policies_delete_execute()` to send, or `networksecurity_projects_locations_authorization_policies_delete` for simplest API.

pub fn networksecurity_projects_locations_authorization_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}
/// Deletes a single AuthorizationPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authorization_policies_delete_execute()` or `networksecurity_projects_locations_authorization_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}
/// Deletes a single AuthorizationPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authorization_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authorization_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authorization_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_authorization_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authorization_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthorizationPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}
/// Deletes a single AuthorizationPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authorization_policies_delete_builder()` + `networksecurity_projects_locations_authorization_policies_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthorizationPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_authorization_policies_delete_builder(
        client, &args.name,
    )?;
    networksecurity_projects_locations_authorization_policies_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}
/// Gets details of a single AuthorizationPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authorization_policies_get_execute()` to send, or `networksecurity_projects_locations_authorization_policies_get` for simplest API.

pub fn networksecurity_projects_locations_authorization_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}
/// Gets details of a single AuthorizationPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authorization_policies_get_execute()` or `networksecurity_projects_locations_authorization_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AuthorizationPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthorizationPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}
/// Gets details of a single AuthorizationPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authorization_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authorization_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authorization_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthorizationPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_authorization_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authorization_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthorizationPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}
/// Gets details of a single AuthorizationPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authorization_policies_get_builder()` + `networksecurity_projects_locations_authorization_policies_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthorizationPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthorizationPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_authorization_policies_get_builder(client, &args.name)?;
    networksecurity_projects_locations_authorization_policies_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authorization_policies_get_iam_policy_execute()` to send, or `networksecurity_projects_locations_authorization_policies_get_iam_policy` for simplest API.

pub fn networksecurity_projects_locations_authorization_policies_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authorization_policies_get_iam_policy_execute()` or `networksecurity_projects_locations_authorization_policies_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authorization_policies_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_get_iam_policy_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authorization_policies_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authorization_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_authorization_policies_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authorization_policies_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthorizationPoliciesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authorization_policies_get_iam_policy_builder()` + `networksecurity_projects_locations_authorization_policies_get_iam_policy_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_get_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthorizationPoliciesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_authorization_policies_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    networksecurity_projects_locations_authorization_policies_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies
/// Lists AuthorizationPolicies in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authorization_policies_list_execute()` to send, or `networksecurity_projects_locations_authorization_policies_list` for simplest API.

pub fn networksecurity_projects_locations_authorization_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authorizationPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies
/// Lists AuthorizationPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authorization_policies_list_execute()` or `networksecurity_projects_locations_authorization_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAuthorizationPoliciesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAuthorizationPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies
/// Lists AuthorizationPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authorization_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authorization_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authorization_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuthorizationPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_authorization_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authorization_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthorizationPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies
/// Lists AuthorizationPolicies in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authorization_policies_list_builder()` + `networksecurity_projects_locations_authorization_policies_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthorizationPoliciesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAuthorizationPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_authorization_policies_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_authorization_policies_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}
/// Updates the parameters of a single AuthorizationPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authorization_policies_patch_execute()` to send, or `networksecurity_projects_locations_authorization_policies_patch` for simplest API.

pub fn networksecurity_projects_locations_authorization_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}
/// Updates the parameters of a single AuthorizationPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authorization_policies_patch_execute()` or `networksecurity_projects_locations_authorization_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}
/// Updates the parameters of a single AuthorizationPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authorization_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authorization_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authorization_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_authorization_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authorization_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthorizationPoliciesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}
/// Updates the parameters of a single AuthorizationPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authorization_policies_patch_builder()` + `networksecurity_projects_locations_authorization_policies_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthorizationPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_authorization_policies_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_authorization_policies_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authorization_policies_set_iam_policy_execute()` to send, or `networksecurity_projects_locations_authorization_policies_set_iam_policy` for simplest API.

pub fn networksecurity_projects_locations_authorization_policies_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authorization_policies_set_iam_policy_execute()` or `networksecurity_projects_locations_authorization_policies_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authorization_policies_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_set_iam_policy_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authorization_policies_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authorization_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_authorization_policies_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authorization_policies_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthorizationPoliciesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authorization_policies_set_iam_policy_builder()` + `networksecurity_projects_locations_authorization_policies_set_iam_policy_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_set_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthorizationPoliciesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_authorization_policies_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    networksecurity_projects_locations_authorization_policies_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authorization_policies_test_iam_permissions_execute()` to send, or `networksecurity_projects_locations_authorization_policies_test_iam_permissions` for simplest API.

pub fn networksecurity_projects_locations_authorization_policies_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authorization_policies_test_iam_permissions_execute()` or `networksecurity_projects_locations_authorization_policies_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authorization_policies_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_test_iam_permissions_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authorization_policies_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authorization_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authorization_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_authorization_policies_test_iam_permissions_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authorization_policies_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthorizationPoliciesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authorizationPolicies/{authorizationPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authorization_policies_test_iam_permissions_builder()` + `networksecurity_projects_locations_authorization_policies_test_iam_permissions_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authorization_policies_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authorization_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthorizationPoliciesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_authorization_policies_test_iam_permissions_builder(
            client,
            &args.resource,
        )?;
    networksecurity_projects_locations_authorization_policies_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authzPolicies
/// Creates a new AuthzPolicy in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authz_policies_create_execute()` to send, or `networksecurity_projects_locations_authz_policies_create` for simplest API.

pub fn networksecurity_projects_locations_authz_policies_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    authzPolicyId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authzPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = authzPolicyId.as_ref() {
        query_parts.push(format!("authzPolicyId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authzPolicies
/// Creates a new AuthzPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authz_policies_create_execute()` or `networksecurity_projects_locations_authz_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authzPolicies
/// Creates a new AuthzPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authz_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authz_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authz_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_authz_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authz_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthzPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: authzPolicyId
    pub authzPolicyId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authzPolicies
/// Creates a new AuthzPolicy in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authz_policies_create_builder()` + `networksecurity_projects_locations_authz_policies_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthzPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_authz_policies_create_builder(
        client,
        &args.parent,
        &args.authzPolicyId,
        &args.requestId,
    )?;
    networksecurity_projects_locations_authz_policies_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}
/// Deletes a single AuthzPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authz_policies_delete_execute()` to send, or `networksecurity_projects_locations_authz_policies_delete` for simplest API.

pub fn networksecurity_projects_locations_authz_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authzPolicies/{authzPoliciesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}
/// Deletes a single AuthzPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authz_policies_delete_execute()` or `networksecurity_projects_locations_authz_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}
/// Deletes a single AuthzPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authz_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authz_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authz_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_authz_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authz_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthzPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}
/// Deletes a single AuthzPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authz_policies_delete_builder()` + `networksecurity_projects_locations_authz_policies_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthzPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_authz_policies_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networksecurity_projects_locations_authz_policies_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}
/// Gets details of a single AuthzPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authz_policies_get_execute()` to send, or `networksecurity_projects_locations_authz_policies_get` for simplest API.

pub fn networksecurity_projects_locations_authz_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authzPolicies/{authzPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}
/// Gets details of a single AuthzPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authz_policies_get_execute()` or `networksecurity_projects_locations_authz_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AuthzPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuthzPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}
/// Gets details of a single AuthzPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authz_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authz_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authz_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthzPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_authz_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authz_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthzPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}
/// Gets details of a single AuthzPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authz_policies_get_builder()` + `networksecurity_projects_locations_authz_policies_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthzPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuthzPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_authz_policies_get_builder(client, &args.name)?;
    networksecurity_projects_locations_authz_policies_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authz_policies_get_iam_policy_execute()` to send, or `networksecurity_projects_locations_authz_policies_get_iam_policy` for simplest API.

pub fn networksecurity_projects_locations_authz_policies_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authz_policies_get_iam_policy_execute()` or `networksecurity_projects_locations_authz_policies_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authz_policies_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_get_iam_policy_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authz_policies_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authz_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_authz_policies_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authz_policies_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthzPoliciesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authz_policies_get_iam_policy_builder()` + `networksecurity_projects_locations_authz_policies_get_iam_policy_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_get_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthzPoliciesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_authz_policies_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    networksecurity_projects_locations_authz_policies_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authzPolicies
/// Lists AuthzPolicies in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authz_policies_list_execute()` to send, or `networksecurity_projects_locations_authz_policies_list` for simplest API.

pub fn networksecurity_projects_locations_authz_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authzPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authzPolicies
/// Lists AuthzPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authz_policies_list_execute()` or `networksecurity_projects_locations_authz_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAuthzPoliciesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAuthzPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authzPolicies
/// Lists AuthzPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authz_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authz_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authz_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAuthzPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_authz_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authz_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthzPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/authzPolicies
/// Lists AuthzPolicies in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authz_policies_list_builder()` + `networksecurity_projects_locations_authz_policies_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthzPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAuthzPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_authz_policies_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_authz_policies_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}
/// Updates the parameters of a single AuthzPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authz_policies_patch_execute()` to send, or `networksecurity_projects_locations_authz_policies_patch` for simplest API.

pub fn networksecurity_projects_locations_authz_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authzPolicies/{authzPoliciesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}
/// Updates the parameters of a single AuthzPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authz_policies_patch_execute()` or `networksecurity_projects_locations_authz_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}
/// Updates the parameters of a single AuthzPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authz_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authz_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authz_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_authz_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authz_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthzPoliciesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}
/// Updates the parameters of a single AuthzPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authz_policies_patch_builder()` + `networksecurity_projects_locations_authz_policies_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthzPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_authz_policies_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_authz_policies_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authz_policies_set_iam_policy_execute()` to send, or `networksecurity_projects_locations_authz_policies_set_iam_policy` for simplest API.

pub fn networksecurity_projects_locations_authz_policies_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authz_policies_set_iam_policy_execute()` or `networksecurity_projects_locations_authz_policies_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authz_policies_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_set_iam_policy_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authz_policies_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authz_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_authz_policies_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authz_policies_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthzPoliciesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authz_policies_set_iam_policy_builder()` + `networksecurity_projects_locations_authz_policies_set_iam_policy_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_set_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthzPoliciesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_authz_policies_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    networksecurity_projects_locations_authz_policies_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_authz_policies_test_iam_permissions_execute()` to send, or `networksecurity_projects_locations_authz_policies_test_iam_permissions` for simplest API.

pub fn networksecurity_projects_locations_authz_policies_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_authz_policies_test_iam_permissions_execute()` or `networksecurity_projects_locations_authz_policies_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_authz_policies_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_test_iam_permissions_task()`.
/// For the simplest API, use `networksecurity_projects_locations_authz_policies_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_authz_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_authz_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_authz_policies_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_authz_policies_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsAuthzPoliciesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/authzPolicies/{authzPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_authz_policies_test_iam_permissions_builder()` + `networksecurity_projects_locations_authz_policies_test_iam_permissions_execute()`.
/// For task-level control, use `networksecurity_projects_locations_authz_policies_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_authz_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsAuthzPoliciesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_authz_policies_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    networksecurity_projects_locations_authz_policies_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs
/// Creates a new BackendAuthenticationConfig in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_backend_authentication_configs_create_execute()` to send, or `networksecurity_projects_locations_backend_authentication_configs_create` for simplest API.

pub fn networksecurity_projects_locations_backend_authentication_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    backendAuthenticationConfigId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/backendAuthenticationConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = backendAuthenticationConfigId.as_ref() {
        query_parts.push(format!("backendAuthenticationConfigId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs
/// Creates a new BackendAuthenticationConfig in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_backend_authentication_configs_create_execute()` or `networksecurity_projects_locations_backend_authentication_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_backend_authentication_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_backend_authentication_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs
/// Creates a new BackendAuthenticationConfig in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_backend_authentication_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_backend_authentication_configs_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_backend_authentication_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_backend_authentication_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_backend_authentication_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_backend_authentication_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_backend_authentication_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsBackendAuthenticationConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: backendAuthenticationConfigId
    pub backendAuthenticationConfigId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs
/// Creates a new BackendAuthenticationConfig in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_backend_authentication_configs_create_builder()` + `networksecurity_projects_locations_backend_authentication_configs_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_backend_authentication_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_backend_authentication_configs_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_backend_authentication_configs_create_builder(
        client,
        &args.parent,
        &args.backendAuthenticationConfigId,
    )?;
    networksecurity_projects_locations_backend_authentication_configs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}
/// Deletes a single BackendAuthenticationConfig to BackendAuthenticationConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_backend_authentication_configs_delete_execute()` to send, or `networksecurity_projects_locations_backend_authentication_configs_delete` for simplest API.

pub fn networksecurity_projects_locations_backend_authentication_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    etag: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}
/// Deletes a single BackendAuthenticationConfig to BackendAuthenticationConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_backend_authentication_configs_delete_execute()` or `networksecurity_projects_locations_backend_authentication_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_backend_authentication_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_backend_authentication_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}
/// Deletes a single BackendAuthenticationConfig to BackendAuthenticationConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_backend_authentication_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_backend_authentication_configs_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_backend_authentication_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_backend_authentication_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_backend_authentication_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_backend_authentication_configs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_backend_authentication_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsBackendAuthenticationConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}
/// Deletes a single BackendAuthenticationConfig to BackendAuthenticationConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_backend_authentication_configs_delete_builder()` + `networksecurity_projects_locations_backend_authentication_configs_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_backend_authentication_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_backend_authentication_configs_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_backend_authentication_configs_delete_builder(
        client, &args.name, &args.etag,
    )?;
    networksecurity_projects_locations_backend_authentication_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}
/// Gets details of a single BackendAuthenticationConfig to BackendAuthenticationConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_backend_authentication_configs_get_execute()` to send, or `networksecurity_projects_locations_backend_authentication_configs_get` for simplest API.

pub fn networksecurity_projects_locations_backend_authentication_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}
/// Gets details of a single BackendAuthenticationConfig to BackendAuthenticationConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_backend_authentication_configs_get_execute()` or `networksecurity_projects_locations_backend_authentication_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_backend_authentication_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_backend_authentication_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendAuthenticationConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackendAuthenticationConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}
/// Gets details of a single BackendAuthenticationConfig to BackendAuthenticationConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_backend_authentication_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_backend_authentication_configs_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_backend_authentication_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_backend_authentication_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_backend_authentication_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BackendAuthenticationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_backend_authentication_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_backend_authentication_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsBackendAuthenticationConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}
/// Gets details of a single BackendAuthenticationConfig to BackendAuthenticationConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_backend_authentication_configs_get_builder()` + `networksecurity_projects_locations_backend_authentication_configs_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_backend_authentication_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_backend_authentication_configs_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BackendAuthenticationConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_backend_authentication_configs_get_builder(
        client, &args.name,
    )?;
    networksecurity_projects_locations_backend_authentication_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs
/// Lists BackendAuthenticationConfigs in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_backend_authentication_configs_list_execute()` to send, or `networksecurity_projects_locations_backend_authentication_configs_list` for simplest API.

pub fn networksecurity_projects_locations_backend_authentication_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/backendAuthenticationConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs
/// Lists BackendAuthenticationConfigs in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_backend_authentication_configs_list_execute()` or `networksecurity_projects_locations_backend_authentication_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_backend_authentication_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_backend_authentication_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBackendAuthenticationConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBackendAuthenticationConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs
/// Lists BackendAuthenticationConfigs in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_backend_authentication_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_backend_authentication_configs_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_backend_authentication_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_backend_authentication_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_backend_authentication_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBackendAuthenticationConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_backend_authentication_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_backend_authentication_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsBackendAuthenticationConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs
/// Lists BackendAuthenticationConfigs in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_backend_authentication_configs_list_builder()` + `networksecurity_projects_locations_backend_authentication_configs_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_backend_authentication_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_backend_authentication_configs_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListBackendAuthenticationConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_backend_authentication_configs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_backend_authentication_configs_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}
/// Updates the parameters of a single BackendAuthenticationConfig to BackendAuthenticationConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_backend_authentication_configs_patch_execute()` to send, or `networksecurity_projects_locations_backend_authentication_configs_patch` for simplest API.

pub fn networksecurity_projects_locations_backend_authentication_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}
/// Updates the parameters of a single BackendAuthenticationConfig to BackendAuthenticationConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_backend_authentication_configs_patch_execute()` or `networksecurity_projects_locations_backend_authentication_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_backend_authentication_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_backend_authentication_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}
/// Updates the parameters of a single BackendAuthenticationConfig to BackendAuthenticationConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_backend_authentication_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_backend_authentication_configs_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_backend_authentication_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_backend_authentication_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_backend_authentication_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_backend_authentication_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_backend_authentication_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsBackendAuthenticationConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/backendAuthenticationConfigs/{backendAuthenticationConfigsId}
/// Updates the parameters of a single BackendAuthenticationConfig to BackendAuthenticationConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_backend_authentication_configs_patch_builder()` + `networksecurity_projects_locations_backend_authentication_configs_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_backend_authentication_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_backend_authentication_configs_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_backend_authentication_configs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_backend_authentication_configs_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies
/// Creates a new ClientTlsPolicy in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_client_tls_policies_create_execute()` to send, or `networksecurity_projects_locations_client_tls_policies_create` for simplest API.

pub fn networksecurity_projects_locations_client_tls_policies_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    clientTlsPolicyId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/clientTlsPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientTlsPolicyId.as_ref() {
        query_parts.push(format!("clientTlsPolicyId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies
/// Creates a new ClientTlsPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_client_tls_policies_create_execute()` or `networksecurity_projects_locations_client_tls_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies
/// Creates a new ClientTlsPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_client_tls_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_client_tls_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_client_tls_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_client_tls_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_client_tls_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsClientTlsPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: clientTlsPolicyId
    pub clientTlsPolicyId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies
/// Creates a new ClientTlsPolicy in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_client_tls_policies_create_builder()` + `networksecurity_projects_locations_client_tls_policies_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsClientTlsPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_client_tls_policies_create_builder(
        client,
        &args.parent,
        &args.clientTlsPolicyId,
    )?;
    networksecurity_projects_locations_client_tls_policies_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}
/// Deletes a single ClientTlsPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_client_tls_policies_delete_execute()` to send, or `networksecurity_projects_locations_client_tls_policies_delete` for simplest API.

pub fn networksecurity_projects_locations_client_tls_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}
/// Deletes a single ClientTlsPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_client_tls_policies_delete_execute()` or `networksecurity_projects_locations_client_tls_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}
/// Deletes a single ClientTlsPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_client_tls_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_client_tls_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_client_tls_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_client_tls_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_client_tls_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsClientTlsPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}
/// Deletes a single ClientTlsPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_client_tls_policies_delete_builder()` + `networksecurity_projects_locations_client_tls_policies_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsClientTlsPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_client_tls_policies_delete_builder(client, &args.name)?;
    networksecurity_projects_locations_client_tls_policies_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}
/// Gets details of a single ClientTlsPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_client_tls_policies_get_execute()` to send, or `networksecurity_projects_locations_client_tls_policies_get` for simplest API.

pub fn networksecurity_projects_locations_client_tls_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}
/// Gets details of a single ClientTlsPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_client_tls_policies_get_execute()` or `networksecurity_projects_locations_client_tls_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClientTlsPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientTlsPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}
/// Gets details of a single ClientTlsPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_client_tls_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_client_tls_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_client_tls_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientTlsPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_client_tls_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_client_tls_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsClientTlsPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}
/// Gets details of a single ClientTlsPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_client_tls_policies_get_builder()` + `networksecurity_projects_locations_client_tls_policies_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsClientTlsPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientTlsPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_client_tls_policies_get_builder(client, &args.name)?;
    networksecurity_projects_locations_client_tls_policies_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_client_tls_policies_get_iam_policy_execute()` to send, or `networksecurity_projects_locations_client_tls_policies_get_iam_policy` for simplest API.

pub fn networksecurity_projects_locations_client_tls_policies_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_client_tls_policies_get_iam_policy_execute()` or `networksecurity_projects_locations_client_tls_policies_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_client_tls_policies_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_get_iam_policy_task()`.
/// For the simplest API, use `networksecurity_projects_locations_client_tls_policies_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_client_tls_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_client_tls_policies_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_client_tls_policies_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsClientTlsPoliciesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_client_tls_policies_get_iam_policy_builder()` + `networksecurity_projects_locations_client_tls_policies_get_iam_policy_execute()`.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_get_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsClientTlsPoliciesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_client_tls_policies_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    networksecurity_projects_locations_client_tls_policies_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies
/// Lists ClientTlsPolicies in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_client_tls_policies_list_execute()` to send, or `networksecurity_projects_locations_client_tls_policies_list` for simplest API.

pub fn networksecurity_projects_locations_client_tls_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/clientTlsPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies
/// Lists ClientTlsPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_client_tls_policies_list_execute()` or `networksecurity_projects_locations_client_tls_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListClientTlsPoliciesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClientTlsPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies
/// Lists ClientTlsPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_client_tls_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_client_tls_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_client_tls_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListClientTlsPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_client_tls_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_client_tls_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsClientTlsPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies
/// Lists ClientTlsPolicies in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_client_tls_policies_list_builder()` + `networksecurity_projects_locations_client_tls_policies_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsClientTlsPoliciesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListClientTlsPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_client_tls_policies_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_client_tls_policies_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}
/// Updates the parameters of a single ClientTlsPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_client_tls_policies_patch_execute()` to send, or `networksecurity_projects_locations_client_tls_policies_patch` for simplest API.

pub fn networksecurity_projects_locations_client_tls_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}
/// Updates the parameters of a single ClientTlsPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_client_tls_policies_patch_execute()` or `networksecurity_projects_locations_client_tls_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}
/// Updates the parameters of a single ClientTlsPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_client_tls_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_client_tls_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_client_tls_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_client_tls_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_client_tls_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsClientTlsPoliciesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}
/// Updates the parameters of a single ClientTlsPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_client_tls_policies_patch_builder()` + `networksecurity_projects_locations_client_tls_policies_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsClientTlsPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_client_tls_policies_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_client_tls_policies_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_client_tls_policies_set_iam_policy_execute()` to send, or `networksecurity_projects_locations_client_tls_policies_set_iam_policy` for simplest API.

pub fn networksecurity_projects_locations_client_tls_policies_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_client_tls_policies_set_iam_policy_execute()` or `networksecurity_projects_locations_client_tls_policies_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_client_tls_policies_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_set_iam_policy_task()`.
/// For the simplest API, use `networksecurity_projects_locations_client_tls_policies_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_client_tls_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_client_tls_policies_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_client_tls_policies_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsClientTlsPoliciesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_client_tls_policies_set_iam_policy_builder()` + `networksecurity_projects_locations_client_tls_policies_set_iam_policy_execute()`.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_set_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsClientTlsPoliciesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_client_tls_policies_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    networksecurity_projects_locations_client_tls_policies_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_client_tls_policies_test_iam_permissions_execute()` to send, or `networksecurity_projects_locations_client_tls_policies_test_iam_permissions` for simplest API.

pub fn networksecurity_projects_locations_client_tls_policies_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_client_tls_policies_test_iam_permissions_execute()` or `networksecurity_projects_locations_client_tls_policies_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_client_tls_policies_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_test_iam_permissions_task()`.
/// For the simplest API, use `networksecurity_projects_locations_client_tls_policies_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_client_tls_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_client_tls_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_client_tls_policies_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_client_tls_policies_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsClientTlsPoliciesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clientTlsPolicies/{clientTlsPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_client_tls_policies_test_iam_permissions_builder()` + `networksecurity_projects_locations_client_tls_policies_test_iam_permissions_execute()`.
/// For task-level control, use `networksecurity_projects_locations_client_tls_policies_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_client_tls_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsClientTlsPoliciesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_client_tls_policies_test_iam_permissions_builder(
            client,
            &args.resource,
        )?;
    networksecurity_projects_locations_client_tls_policies_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors
/// Creates a new DnsThreatDetector in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_dns_threat_detectors_create_execute()` to send, or `networksecurity_projects_locations_dns_threat_detectors_create` for simplest API.

pub fn networksecurity_projects_locations_dns_threat_detectors_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    dnsThreatDetectorId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/dnsThreatDetectors",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dnsThreatDetectorId.as_ref() {
        query_parts.push(format!("dnsThreatDetectorId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors
/// Creates a new DnsThreatDetector in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_dns_threat_detectors_create_execute()` or `networksecurity_projects_locations_dns_threat_detectors_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_dns_threat_detectors_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_dns_threat_detectors_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DnsThreatDetector>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DnsThreatDetector = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors
/// Creates a new DnsThreatDetector in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_dns_threat_detectors_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_dns_threat_detectors_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_dns_threat_detectors_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_dns_threat_detectors_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_dns_threat_detectors_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsThreatDetector>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_dns_threat_detectors_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_dns_threat_detectors_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsDnsThreatDetectorsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: dnsThreatDetectorId
    pub dnsThreatDetectorId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors
/// Creates a new DnsThreatDetector in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_dns_threat_detectors_create_builder()` + `networksecurity_projects_locations_dns_threat_detectors_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_dns_threat_detectors_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_dns_threat_detectors_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsDnsThreatDetectorsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsThreatDetector>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_dns_threat_detectors_create_builder(
        client,
        &args.parent,
        &args.dnsThreatDetectorId,
    )?;
    networksecurity_projects_locations_dns_threat_detectors_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}
/// Deletes a single DnsThreatDetector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_dns_threat_detectors_delete_execute()` to send, or `networksecurity_projects_locations_dns_threat_detectors_delete` for simplest API.

pub fn networksecurity_projects_locations_dns_threat_detectors_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}
/// Deletes a single DnsThreatDetector.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_dns_threat_detectors_delete_execute()` or `networksecurity_projects_locations_dns_threat_detectors_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_dns_threat_detectors_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_dns_threat_detectors_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}
/// Deletes a single DnsThreatDetector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_dns_threat_detectors_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_dns_threat_detectors_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_dns_threat_detectors_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_dns_threat_detectors_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_dns_threat_detectors_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_dns_threat_detectors_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_dns_threat_detectors_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsDnsThreatDetectorsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}
/// Deletes a single DnsThreatDetector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_dns_threat_detectors_delete_builder()` + `networksecurity_projects_locations_dns_threat_detectors_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_dns_threat_detectors_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_dns_threat_detectors_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsDnsThreatDetectorsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_dns_threat_detectors_delete_builder(client, &args.name)?;
    networksecurity_projects_locations_dns_threat_detectors_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}
/// Gets the details of a single DnsThreatDetector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_dns_threat_detectors_get_execute()` to send, or `networksecurity_projects_locations_dns_threat_detectors_get` for simplest API.

pub fn networksecurity_projects_locations_dns_threat_detectors_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}
/// Gets the details of a single DnsThreatDetector.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_dns_threat_detectors_get_execute()` or `networksecurity_projects_locations_dns_threat_detectors_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_dns_threat_detectors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_dns_threat_detectors_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DnsThreatDetector>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DnsThreatDetector = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}
/// Gets the details of a single DnsThreatDetector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_dns_threat_detectors_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_dns_threat_detectors_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_dns_threat_detectors_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_dns_threat_detectors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_dns_threat_detectors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsThreatDetector>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_dns_threat_detectors_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_dns_threat_detectors_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsDnsThreatDetectorsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}
/// Gets the details of a single DnsThreatDetector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_dns_threat_detectors_get_builder()` + `networksecurity_projects_locations_dns_threat_detectors_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_dns_threat_detectors_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_dns_threat_detectors_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsDnsThreatDetectorsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsThreatDetector>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_dns_threat_detectors_get_builder(client, &args.name)?;
    networksecurity_projects_locations_dns_threat_detectors_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors
/// Lists DnsThreatDetectors in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_dns_threat_detectors_list_execute()` to send, or `networksecurity_projects_locations_dns_threat_detectors_list` for simplest API.

pub fn networksecurity_projects_locations_dns_threat_detectors_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/dnsThreatDetectors",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors
/// Lists DnsThreatDetectors in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_dns_threat_detectors_list_execute()` or `networksecurity_projects_locations_dns_threat_detectors_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_dns_threat_detectors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_dns_threat_detectors_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDnsThreatDetectorsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDnsThreatDetectorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors
/// Lists DnsThreatDetectors in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_dns_threat_detectors_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_dns_threat_detectors_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_dns_threat_detectors_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_dns_threat_detectors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_dns_threat_detectors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDnsThreatDetectorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_dns_threat_detectors_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_dns_threat_detectors_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsDnsThreatDetectorsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors
/// Lists DnsThreatDetectors in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_dns_threat_detectors_list_builder()` + `networksecurity_projects_locations_dns_threat_detectors_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_dns_threat_detectors_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_dns_threat_detectors_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsDnsThreatDetectorsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDnsThreatDetectorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_dns_threat_detectors_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_dns_threat_detectors_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}
/// Updates a single DnsThreatDetector.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_dns_threat_detectors_patch_execute()` to send, or `networksecurity_projects_locations_dns_threat_detectors_patch` for simplest API.

pub fn networksecurity_projects_locations_dns_threat_detectors_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}
/// Updates a single DnsThreatDetector.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_dns_threat_detectors_patch_execute()` or `networksecurity_projects_locations_dns_threat_detectors_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_dns_threat_detectors_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_dns_threat_detectors_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DnsThreatDetector>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DnsThreatDetector = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}
/// Updates a single DnsThreatDetector.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_dns_threat_detectors_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_dns_threat_detectors_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_dns_threat_detectors_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_dns_threat_detectors_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_dns_threat_detectors_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsThreatDetector>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_dns_threat_detectors_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_dns_threat_detectors_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsDnsThreatDetectorsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/dnsThreatDetectors/{dnsThreatDetectorsId}
/// Updates a single DnsThreatDetector.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_dns_threat_detectors_patch_builder()` + `networksecurity_projects_locations_dns_threat_detectors_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_dns_threat_detectors_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_dns_threat_detectors_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsDnsThreatDetectorsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsThreatDetector>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_dns_threat_detectors_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_dns_threat_detectors_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations
/// Creates a new FirewallEndpointAssociation in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_firewall_endpoint_associations_create_execute()` to send, or `networksecurity_projects_locations_firewall_endpoint_associations_create` for simplest API.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    firewallEndpointAssociationId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/firewallEndpointAssociations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = firewallEndpointAssociationId.as_ref() {
        query_parts.push(format!("firewallEndpointAssociationId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations
/// Creates a new FirewallEndpointAssociation in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_firewall_endpoint_associations_create_execute()` or `networksecurity_projects_locations_firewall_endpoint_associations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_firewall_endpoint_associations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations
/// Creates a new FirewallEndpointAssociation in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_firewall_endpoint_associations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_firewall_endpoint_associations_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_firewall_endpoint_associations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_firewall_endpoint_associations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_firewall_endpoint_associations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_firewall_endpoint_associations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsFirewallEndpointAssociationsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: firewallEndpointAssociationId
    pub firewallEndpointAssociationId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations
/// Creates a new FirewallEndpointAssociation in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_firewall_endpoint_associations_create_builder()` + `networksecurity_projects_locations_firewall_endpoint_associations_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_firewall_endpoint_associations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_firewall_endpoint_associations_create_builder(
        client,
        &args.parent,
        &args.firewallEndpointAssociationId,
        &args.requestId,
    )?;
    networksecurity_projects_locations_firewall_endpoint_associations_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}
/// Deletes a single FirewallEndpointAssociation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_firewall_endpoint_associations_delete_execute()` to send, or `networksecurity_projects_locations_firewall_endpoint_associations_delete` for simplest API.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}
/// Deletes a single FirewallEndpointAssociation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_firewall_endpoint_associations_delete_execute()` or `networksecurity_projects_locations_firewall_endpoint_associations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_firewall_endpoint_associations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}
/// Deletes a single FirewallEndpointAssociation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_firewall_endpoint_associations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_firewall_endpoint_associations_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_firewall_endpoint_associations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_firewall_endpoint_associations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_firewall_endpoint_associations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_firewall_endpoint_associations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsFirewallEndpointAssociationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}
/// Deletes a single FirewallEndpointAssociation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_firewall_endpoint_associations_delete_builder()` + `networksecurity_projects_locations_firewall_endpoint_associations_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_firewall_endpoint_associations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_firewall_endpoint_associations_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networksecurity_projects_locations_firewall_endpoint_associations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}
/// Gets details of a single FirewallEndpointAssociation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_firewall_endpoint_associations_get_execute()` to send, or `networksecurity_projects_locations_firewall_endpoint_associations_get` for simplest API.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}
/// Gets details of a single FirewallEndpointAssociation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_firewall_endpoint_associations_get_execute()` or `networksecurity_projects_locations_firewall_endpoint_associations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_firewall_endpoint_associations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallEndpointAssociation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirewallEndpointAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}
/// Gets details of a single FirewallEndpointAssociation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_firewall_endpoint_associations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_firewall_endpoint_associations_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_firewall_endpoint_associations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_firewall_endpoint_associations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FirewallEndpointAssociation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_firewall_endpoint_associations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_firewall_endpoint_associations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsFirewallEndpointAssociationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}
/// Gets details of a single FirewallEndpointAssociation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_firewall_endpoint_associations_get_builder()` + `networksecurity_projects_locations_firewall_endpoint_associations_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_firewall_endpoint_associations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FirewallEndpointAssociation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_firewall_endpoint_associations_get_builder(
        client, &args.name,
    )?;
    networksecurity_projects_locations_firewall_endpoint_associations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations
/// Lists Associations in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_firewall_endpoint_associations_list_execute()` to send, or `networksecurity_projects_locations_firewall_endpoint_associations_list` for simplest API.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/firewallEndpointAssociations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations
/// Lists Associations in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_firewall_endpoint_associations_list_execute()` or `networksecurity_projects_locations_firewall_endpoint_associations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_firewall_endpoint_associations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFirewallEndpointAssociationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFirewallEndpointAssociationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations
/// Lists Associations in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_firewall_endpoint_associations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_firewall_endpoint_associations_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_firewall_endpoint_associations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_firewall_endpoint_associations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFirewallEndpointAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_firewall_endpoint_associations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_firewall_endpoint_associations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsFirewallEndpointAssociationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations
/// Lists Associations in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_firewall_endpoint_associations_list_builder()` + `networksecurity_projects_locations_firewall_endpoint_associations_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_firewall_endpoint_associations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFirewallEndpointAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_firewall_endpoint_associations_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_firewall_endpoint_associations_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}
/// Update a single FirewallEndpointAssociation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_firewall_endpoint_associations_patch_execute()` to send, or `networksecurity_projects_locations_firewall_endpoint_associations_patch` for simplest API.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}
/// Update a single FirewallEndpointAssociation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_firewall_endpoint_associations_patch_execute()` or `networksecurity_projects_locations_firewall_endpoint_associations_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_firewall_endpoint_associations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}
/// Update a single FirewallEndpointAssociation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_firewall_endpoint_associations_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_firewall_endpoint_associations_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_firewall_endpoint_associations_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_firewall_endpoint_associations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_firewall_endpoint_associations_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_firewall_endpoint_associations_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsFirewallEndpointAssociationsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/firewallEndpointAssociations/{firewallEndpointAssociationsId}
/// Update a single FirewallEndpointAssociation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_firewall_endpoint_associations_patch_builder()` + `networksecurity_projects_locations_firewall_endpoint_associations_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_firewall_endpoint_associations_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_firewall_endpoint_associations_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_firewall_endpoint_associations_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_firewall_endpoint_associations_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies
/// Creates a new GatewaySecurityPolicy in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_gateway_security_policies_create_execute()` to send, or `networksecurity_projects_locations_gateway_security_policies_create` for simplest API.

pub fn networksecurity_projects_locations_gateway_security_policies_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    gatewaySecurityPolicyId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/gatewaySecurityPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = gatewaySecurityPolicyId.as_ref() {
        query_parts.push(format!("gatewaySecurityPolicyId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies
/// Creates a new GatewaySecurityPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_gateway_security_policies_create_execute()` or `networksecurity_projects_locations_gateway_security_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies
/// Creates a new GatewaySecurityPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_gateway_security_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_gateway_security_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_gateway_security_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_gateway_security_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_gateway_security_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsGatewaySecurityPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: gatewaySecurityPolicyId
    pub gatewaySecurityPolicyId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies
/// Creates a new GatewaySecurityPolicy in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_gateway_security_policies_create_builder()` + `networksecurity_projects_locations_gateway_security_policies_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_gateway_security_policies_create_builder(
        client,
        &args.parent,
        &args.gatewaySecurityPolicyId,
    )?;
    networksecurity_projects_locations_gateway_security_policies_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}
/// Deletes a single GatewaySecurityPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_gateway_security_policies_delete_execute()` to send, or `networksecurity_projects_locations_gateway_security_policies_delete` for simplest API.

pub fn networksecurity_projects_locations_gateway_security_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}
/// Deletes a single GatewaySecurityPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_gateway_security_policies_delete_execute()` or `networksecurity_projects_locations_gateway_security_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}
/// Deletes a single GatewaySecurityPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_gateway_security_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_gateway_security_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_gateway_security_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_gateway_security_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_gateway_security_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsGatewaySecurityPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}
/// Deletes a single GatewaySecurityPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_gateway_security_policies_delete_builder()` + `networksecurity_projects_locations_gateway_security_policies_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_gateway_security_policies_delete_builder(
        client, &args.name,
    )?;
    networksecurity_projects_locations_gateway_security_policies_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}
/// Gets details of a single GatewaySecurityPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_gateway_security_policies_get_execute()` to send, or `networksecurity_projects_locations_gateway_security_policies_get` for simplest API.

pub fn networksecurity_projects_locations_gateway_security_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}
/// Gets details of a single GatewaySecurityPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_gateway_security_policies_get_execute()` or `networksecurity_projects_locations_gateway_security_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GatewaySecurityPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GatewaySecurityPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}
/// Gets details of a single GatewaySecurityPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_gateway_security_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_gateway_security_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_gateway_security_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GatewaySecurityPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_gateway_security_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_gateway_security_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsGatewaySecurityPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}
/// Gets details of a single GatewaySecurityPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_gateway_security_policies_get_builder()` + `networksecurity_projects_locations_gateway_security_policies_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GatewaySecurityPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_gateway_security_policies_get_builder(
        client, &args.name,
    )?;
    networksecurity_projects_locations_gateway_security_policies_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies
/// Lists GatewaySecurityPolicies in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_gateway_security_policies_list_execute()` to send, or `networksecurity_projects_locations_gateway_security_policies_list` for simplest API.

pub fn networksecurity_projects_locations_gateway_security_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/gatewaySecurityPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies
/// Lists GatewaySecurityPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_gateway_security_policies_list_execute()` or `networksecurity_projects_locations_gateway_security_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListGatewaySecurityPoliciesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGatewaySecurityPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies
/// Lists GatewaySecurityPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_gateway_security_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_gateway_security_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_gateway_security_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListGatewaySecurityPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_gateway_security_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_gateway_security_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsGatewaySecurityPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies
/// Lists GatewaySecurityPolicies in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_gateway_security_policies_list_builder()` + `networksecurity_projects_locations_gateway_security_policies_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListGatewaySecurityPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_gateway_security_policies_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_gateway_security_policies_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}
/// Updates the parameters of a single GatewaySecurityPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_gateway_security_policies_patch_execute()` to send, or `networksecurity_projects_locations_gateway_security_policies_patch` for simplest API.

pub fn networksecurity_projects_locations_gateway_security_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}
/// Updates the parameters of a single GatewaySecurityPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_gateway_security_policies_patch_execute()` or `networksecurity_projects_locations_gateway_security_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}
/// Updates the parameters of a single GatewaySecurityPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_gateway_security_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_gateway_security_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_gateway_security_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_gateway_security_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_gateway_security_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsGatewaySecurityPoliciesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}
/// Updates the parameters of a single GatewaySecurityPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_gateway_security_policies_patch_builder()` + `networksecurity_projects_locations_gateway_security_policies_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_gateway_security_policies_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_gateway_security_policies_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules
/// Creates a new GatewaySecurityPolicy in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_gateway_security_policies_rules_create_execute()` to send, or `networksecurity_projects_locations_gateway_security_policies_rules_create` for simplest API.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    gatewaySecurityPolicyRuleId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = gatewaySecurityPolicyRuleId.as_ref() {
        query_parts.push(format!("gatewaySecurityPolicyRuleId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules
/// Creates a new GatewaySecurityPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_gateway_security_policies_rules_create_execute()` or `networksecurity_projects_locations_gateway_security_policies_rules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_rules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules
/// Creates a new GatewaySecurityPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_gateway_security_policies_rules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_rules_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_gateway_security_policies_rules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_rules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_gateway_security_policies_rules_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_gateway_security_policies_rules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: gatewaySecurityPolicyRuleId
    pub gatewaySecurityPolicyRuleId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules
/// Creates a new GatewaySecurityPolicy in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_gateway_security_policies_rules_create_builder()` + `networksecurity_projects_locations_gateway_security_policies_rules_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_rules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_gateway_security_policies_rules_create_builder(
            client,
            &args.parent,
            &args.gatewaySecurityPolicyRuleId,
        )?;
    networksecurity_projects_locations_gateway_security_policies_rules_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}
/// Deletes a single GatewaySecurityPolicyRule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_gateway_security_policies_rules_delete_execute()` to send, or `networksecurity_projects_locations_gateway_security_policies_rules_delete` for simplest API.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}
/// Deletes a single GatewaySecurityPolicyRule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_gateway_security_policies_rules_delete_execute()` or `networksecurity_projects_locations_gateway_security_policies_rules_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_rules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}
/// Deletes a single GatewaySecurityPolicyRule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_gateway_security_policies_rules_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_rules_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_gateway_security_policies_rules_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_rules_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_gateway_security_policies_rules_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_gateway_security_policies_rules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}
/// Deletes a single GatewaySecurityPolicyRule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_gateway_security_policies_rules_delete_builder()` + `networksecurity_projects_locations_gateway_security_policies_rules_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_rules_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_gateway_security_policies_rules_delete_builder(
            client, &args.name,
        )?;
    networksecurity_projects_locations_gateway_security_policies_rules_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}
/// Gets details of a single GatewaySecurityPolicyRule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_gateway_security_policies_rules_get_execute()` to send, or `networksecurity_projects_locations_gateway_security_policies_rules_get` for simplest API.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}
/// Gets details of a single GatewaySecurityPolicyRule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_gateway_security_policies_rules_get_execute()` or `networksecurity_projects_locations_gateway_security_policies_rules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_rules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GatewaySecurityPolicyRule>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GatewaySecurityPolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}
/// Gets details of a single GatewaySecurityPolicyRule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_gateway_security_policies_rules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_rules_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_gateway_security_policies_rules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_rules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GatewaySecurityPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_gateway_security_policies_rules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_gateway_security_policies_rules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}
/// Gets details of a single GatewaySecurityPolicyRule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_gateway_security_policies_rules_get_builder()` + `networksecurity_projects_locations_gateway_security_policies_rules_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_rules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GatewaySecurityPolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_gateway_security_policies_rules_get_builder(
        client, &args.name,
    )?;
    networksecurity_projects_locations_gateway_security_policies_rules_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules
/// Lists GatewaySecurityPolicyRules in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_gateway_security_policies_rules_list_execute()` to send, or `networksecurity_projects_locations_gateway_security_policies_rules_list` for simplest API.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules
/// Lists GatewaySecurityPolicyRules in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_gateway_security_policies_rules_list_execute()` or `networksecurity_projects_locations_gateway_security_policies_rules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_rules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListGatewaySecurityPolicyRulesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGatewaySecurityPolicyRulesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules
/// Lists GatewaySecurityPolicyRules in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_gateway_security_policies_rules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_rules_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_gateway_security_policies_rules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_rules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListGatewaySecurityPolicyRulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_gateway_security_policies_rules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_gateway_security_policies_rules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules
/// Lists GatewaySecurityPolicyRules in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_gateway_security_policies_rules_list_builder()` + `networksecurity_projects_locations_gateway_security_policies_rules_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_rules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListGatewaySecurityPolicyRulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_gateway_security_policies_rules_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_gateway_security_policies_rules_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}
/// Updates the parameters of a single GatewaySecurityPolicyRule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_gateway_security_policies_rules_patch_execute()` to send, or `networksecurity_projects_locations_gateway_security_policies_rules_patch` for simplest API.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}
/// Updates the parameters of a single GatewaySecurityPolicyRule.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_gateway_security_policies_rules_patch_execute()` or `networksecurity_projects_locations_gateway_security_policies_rules_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_rules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}
/// Updates the parameters of a single GatewaySecurityPolicyRule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_gateway_security_policies_rules_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_rules_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_gateway_security_policies_rules_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_gateway_security_policies_rules_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_gateway_security_policies_rules_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_gateway_security_policies_rules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/gatewaySecurityPolicies/{gatewaySecurityPoliciesId}/rules/{rulesId}
/// Updates the parameters of a single GatewaySecurityPolicyRule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_gateway_security_policies_rules_patch_builder()` + `networksecurity_projects_locations_gateway_security_policies_rules_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_gateway_security_policies_rules_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_gateway_security_policies_rules_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_gateway_security_policies_rules_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_gateway_security_policies_rules_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups
/// Creates a deployment group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_deployment_groups_create_execute()` to send, or `networksecurity_projects_locations_intercept_deployment_groups_create` for simplest API.

pub fn networksecurity_projects_locations_intercept_deployment_groups_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    interceptDeploymentGroupId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptDeploymentGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = interceptDeploymentGroupId.as_ref() {
        query_parts.push(format!("interceptDeploymentGroupId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups
/// Creates a deployment group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_deployment_groups_create_execute()` or `networksecurity_projects_locations_intercept_deployment_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployment_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployment_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups
/// Creates a deployment group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_deployment_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployment_groups_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_deployment_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployment_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_deployment_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_deployment_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_deployment_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptDeploymentGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: interceptDeploymentGroupId
    pub interceptDeploymentGroupId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups
/// Creates a deployment group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_deployment_groups_create_builder()` + `networksecurity_projects_locations_intercept_deployment_groups_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployment_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployment_groups_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_intercept_deployment_groups_create_builder(
        client,
        &args.parent,
        &args.interceptDeploymentGroupId,
        &args.requestId,
    )?;
    networksecurity_projects_locations_intercept_deployment_groups_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}
/// Deletes a deployment group. See <https://google.aip.`dev/135`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_deployment_groups_delete_execute()` to send, or `networksecurity_projects_locations_intercept_deployment_groups_delete` for simplest API.

pub fn networksecurity_projects_locations_intercept_deployment_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}
/// Deletes a deployment group. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_deployment_groups_delete_execute()` or `networksecurity_projects_locations_intercept_deployment_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployment_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployment_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}
/// Deletes a deployment group. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_deployment_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployment_groups_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_deployment_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployment_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_deployment_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_deployment_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_deployment_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptDeploymentGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}
/// Deletes a deployment group. See <https://google.aip.`dev/135`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_deployment_groups_delete_builder()` + `networksecurity_projects_locations_intercept_deployment_groups_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployment_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployment_groups_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_intercept_deployment_groups_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networksecurity_projects_locations_intercept_deployment_groups_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}
/// Gets a specific deployment group. See <https://google.aip.`dev/131`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_deployment_groups_get_execute()` to send, or `networksecurity_projects_locations_intercept_deployment_groups_get` for simplest API.

pub fn networksecurity_projects_locations_intercept_deployment_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}
/// Gets a specific deployment group. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_deployment_groups_get_execute()` or `networksecurity_projects_locations_intercept_deployment_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployment_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployment_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterceptDeploymentGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterceptDeploymentGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}
/// Gets a specific deployment group. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_deployment_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployment_groups_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_deployment_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployment_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_deployment_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterceptDeploymentGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_deployment_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_deployment_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptDeploymentGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}
/// Gets a specific deployment group. See <https://google.aip.`dev/131`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_deployment_groups_get_builder()` + `networksecurity_projects_locations_intercept_deployment_groups_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployment_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployment_groups_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterceptDeploymentGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_intercept_deployment_groups_get_builder(
        client, &args.name,
    )?;
    networksecurity_projects_locations_intercept_deployment_groups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups
/// Lists deployment groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_deployment_groups_list_execute()` to send, or `networksecurity_projects_locations_intercept_deployment_groups_list` for simplest API.

pub fn networksecurity_projects_locations_intercept_deployment_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptDeploymentGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups
/// Lists deployment groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_deployment_groups_list_execute()` or `networksecurity_projects_locations_intercept_deployment_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployment_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployment_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInterceptDeploymentGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInterceptDeploymentGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups
/// Lists deployment groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_deployment_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployment_groups_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_deployment_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployment_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_deployment_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInterceptDeploymentGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_deployment_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_deployment_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptDeploymentGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups
/// Lists deployment groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_deployment_groups_list_builder()` + `networksecurity_projects_locations_intercept_deployment_groups_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployment_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployment_groups_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInterceptDeploymentGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_intercept_deployment_groups_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_intercept_deployment_groups_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}
/// Updates a deployment group. See <https://google.aip.`dev/134`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_deployment_groups_patch_execute()` to send, or `networksecurity_projects_locations_intercept_deployment_groups_patch` for simplest API.

pub fn networksecurity_projects_locations_intercept_deployment_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}
/// Updates a deployment group. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_deployment_groups_patch_execute()` or `networksecurity_projects_locations_intercept_deployment_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployment_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployment_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}
/// Updates a deployment group. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_deployment_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployment_groups_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_deployment_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployment_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_deployment_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_deployment_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_deployment_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptDeploymentGroupsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptDeploymentGroups/{interceptDeploymentGroupsId}
/// Updates a deployment group. See <https://google.aip.`dev/134`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_deployment_groups_patch_builder()` + `networksecurity_projects_locations_intercept_deployment_groups_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployment_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployment_groups_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_intercept_deployment_groups_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_intercept_deployment_groups_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments
/// Creates a deployment in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_deployments_create_execute()` to send, or `networksecurity_projects_locations_intercept_deployments_create` for simplest API.

pub fn networksecurity_projects_locations_intercept_deployments_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    interceptDeploymentId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptDeployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = interceptDeploymentId.as_ref() {
        query_parts.push(format!("interceptDeploymentId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments
/// Creates a deployment in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_deployments_create_execute()` or `networksecurity_projects_locations_intercept_deployments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments
/// Creates a deployment in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_deployments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployments_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_deployments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_deployments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_deployments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_deployments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptDeploymentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: interceptDeploymentId
    pub interceptDeploymentId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments
/// Creates a deployment in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_deployments_create_builder()` + `networksecurity_projects_locations_intercept_deployments_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployments_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptDeploymentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_intercept_deployments_create_builder(
        client,
        &args.parent,
        &args.interceptDeploymentId,
        &args.requestId,
    )?;
    networksecurity_projects_locations_intercept_deployments_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}
/// Deletes a deployment. See <https://google.aip.`dev/135`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_deployments_delete_execute()` to send, or `networksecurity_projects_locations_intercept_deployments_delete` for simplest API.

pub fn networksecurity_projects_locations_intercept_deployments_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}
/// Deletes a deployment. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_deployments_delete_execute()` or `networksecurity_projects_locations_intercept_deployments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}
/// Deletes a deployment. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_deployments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployments_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_deployments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_deployments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_deployments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_deployments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptDeploymentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}
/// Deletes a deployment. See <https://google.aip.`dev/135`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_deployments_delete_builder()` + `networksecurity_projects_locations_intercept_deployments_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployments_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptDeploymentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_intercept_deployments_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networksecurity_projects_locations_intercept_deployments_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}
/// Gets a specific deployment. See <https://google.aip.`dev/131`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_deployments_get_execute()` to send, or `networksecurity_projects_locations_intercept_deployments_get` for simplest API.

pub fn networksecurity_projects_locations_intercept_deployments_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}
/// Gets a specific deployment. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_deployments_get_execute()` or `networksecurity_projects_locations_intercept_deployments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterceptDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterceptDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}
/// Gets a specific deployment. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_deployments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployments_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_deployments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterceptDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_deployments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_deployments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptDeploymentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}
/// Gets a specific deployment. See <https://google.aip.`dev/131`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_deployments_get_builder()` + `networksecurity_projects_locations_intercept_deployments_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployments_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptDeploymentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterceptDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_intercept_deployments_get_builder(client, &args.name)?;
    networksecurity_projects_locations_intercept_deployments_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments
/// Lists deployments in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_deployments_list_execute()` to send, or `networksecurity_projects_locations_intercept_deployments_list` for simplest API.

pub fn networksecurity_projects_locations_intercept_deployments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptDeployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments
/// Lists deployments in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_deployments_list_execute()` or `networksecurity_projects_locations_intercept_deployments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInterceptDeploymentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInterceptDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments
/// Lists deployments in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_deployments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployments_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_deployments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInterceptDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_deployments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_deployments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptDeploymentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments
/// Lists deployments in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_deployments_list_builder()` + `networksecurity_projects_locations_intercept_deployments_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployments_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptDeploymentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInterceptDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_intercept_deployments_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_intercept_deployments_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}
/// Updates a deployment. See <https://google.aip.`dev/134`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_deployments_patch_execute()` to send, or `networksecurity_projects_locations_intercept_deployments_patch` for simplest API.

pub fn networksecurity_projects_locations_intercept_deployments_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}
/// Updates a deployment. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_deployments_patch_execute()` or `networksecurity_projects_locations_intercept_deployments_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployments_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}
/// Updates a deployment. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_deployments_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployments_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_deployments_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_deployments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_deployments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_deployments_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_deployments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptDeploymentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptDeployments/{interceptDeploymentsId}
/// Updates a deployment. See <https://google.aip.`dev/134`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_deployments_patch_builder()` + `networksecurity_projects_locations_intercept_deployments_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_deployments_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_deployments_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptDeploymentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_intercept_deployments_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_intercept_deployments_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations
/// Creates an association in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_endpoint_group_associations_create_execute()` to send, or `networksecurity_projects_locations_intercept_endpoint_group_associations_create` for simplest API.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    interceptEndpointGroupAssociationId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptEndpointGroupAssociations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = interceptEndpointGroupAssociationId.as_ref() {
        query_parts.push(format!("interceptEndpointGroupAssociationId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations
/// Creates an association in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_endpoint_group_associations_create_execute()` or `networksecurity_projects_locations_intercept_endpoint_group_associations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_group_associations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations
/// Creates an association in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_endpoint_group_associations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_group_associations_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_endpoint_group_associations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_group_associations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_intercept_endpoint_group_associations_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_endpoint_group_associations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: interceptEndpointGroupAssociationId
    pub interceptEndpointGroupAssociationId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations
/// Creates an association in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_endpoint_group_associations_create_builder()` + `networksecurity_projects_locations_intercept_endpoint_group_associations_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_group_associations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_intercept_endpoint_group_associations_create_builder(
            client,
            &args.parent,
            &args.interceptEndpointGroupAssociationId,
            &args.requestId,
        )?;
    networksecurity_projects_locations_intercept_endpoint_group_associations_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}
/// Deletes an association. See <https://google.aip.`dev/135`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_endpoint_group_associations_delete_execute()` to send, or `networksecurity_projects_locations_intercept_endpoint_group_associations_delete` for simplest API.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}
/// Deletes an association. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_endpoint_group_associations_delete_execute()` or `networksecurity_projects_locations_intercept_endpoint_group_associations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_group_associations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}
/// Deletes an association. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_endpoint_group_associations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_group_associations_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_endpoint_group_associations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_group_associations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_intercept_endpoint_group_associations_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_endpoint_group_associations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}
/// Deletes an association. See <https://google.aip.`dev/135`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_endpoint_group_associations_delete_builder()` + `networksecurity_projects_locations_intercept_endpoint_group_associations_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_group_associations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_intercept_endpoint_group_associations_delete_builder(
            client,
            &args.name,
            &args.requestId,
        )?;
    networksecurity_projects_locations_intercept_endpoint_group_associations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}
/// Gets a specific association. See <https://google.aip.`dev/131`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_endpoint_group_associations_get_execute()` to send, or `networksecurity_projects_locations_intercept_endpoint_group_associations_get` for simplest API.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}
/// Gets a specific association. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_endpoint_group_associations_get_execute()` or `networksecurity_projects_locations_intercept_endpoint_group_associations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_group_associations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterceptEndpointGroupAssociation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterceptEndpointGroupAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}
/// Gets a specific association. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_endpoint_group_associations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_group_associations_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_endpoint_group_associations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_group_associations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterceptEndpointGroupAssociation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_intercept_endpoint_group_associations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_endpoint_group_associations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}
/// Gets a specific association. See <https://google.aip.`dev/131`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_endpoint_group_associations_get_builder()` + `networksecurity_projects_locations_intercept_endpoint_group_associations_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_group_associations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InterceptEndpointGroupAssociation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_intercept_endpoint_group_associations_get_builder(
            client, &args.name,
        )?;
    networksecurity_projects_locations_intercept_endpoint_group_associations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations
/// Lists associations in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_endpoint_group_associations_list_execute()` to send, or `networksecurity_projects_locations_intercept_endpoint_group_associations_list` for simplest API.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptEndpointGroupAssociations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations
/// Lists associations in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_endpoint_group_associations_list_execute()` or `networksecurity_projects_locations_intercept_endpoint_group_associations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_group_associations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInterceptEndpointGroupAssociationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInterceptEndpointGroupAssociationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations
/// Lists associations in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_endpoint_group_associations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_group_associations_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_endpoint_group_associations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_group_associations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInterceptEndpointGroupAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_endpoint_group_associations_list_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_endpoint_group_associations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations
/// Lists associations in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_endpoint_group_associations_list_builder()` + `networksecurity_projects_locations_intercept_endpoint_group_associations_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_group_associations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInterceptEndpointGroupAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_intercept_endpoint_group_associations_list_builder(
            client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )?;
    networksecurity_projects_locations_intercept_endpoint_group_associations_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}
/// Updates an association. See <https://google.aip.`dev/134`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_endpoint_group_associations_patch_execute()` to send, or `networksecurity_projects_locations_intercept_endpoint_group_associations_patch` for simplest API.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}
/// Updates an association. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_endpoint_group_associations_patch_execute()` or `networksecurity_projects_locations_intercept_endpoint_group_associations_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_group_associations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}
/// Updates an association. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_endpoint_group_associations_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_group_associations_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_endpoint_group_associations_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_group_associations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_endpoint_group_associations_patch_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_endpoint_group_associations_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroupAssociations/{interceptEndpointGroupAssociationsId}
/// Updates an association. See <https://google.aip.`dev/134`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_endpoint_group_associations_patch_builder()` + `networksecurity_projects_locations_intercept_endpoint_group_associations_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_group_associations_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_intercept_endpoint_group_associations_patch_builder(
            client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )?;
    networksecurity_projects_locations_intercept_endpoint_group_associations_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups
/// Creates an endpoint group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_endpoint_groups_create_execute()` to send, or `networksecurity_projects_locations_intercept_endpoint_groups_create` for simplest API.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    interceptEndpointGroupId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptEndpointGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = interceptEndpointGroupId.as_ref() {
        query_parts.push(format!("interceptEndpointGroupId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups
/// Creates an endpoint group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_endpoint_groups_create_execute()` or `networksecurity_projects_locations_intercept_endpoint_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups
/// Creates an endpoint group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_endpoint_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_groups_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_endpoint_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_endpoint_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_endpoint_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptEndpointGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: interceptEndpointGroupId
    pub interceptEndpointGroupId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups
/// Creates an endpoint group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_endpoint_groups_create_builder()` + `networksecurity_projects_locations_intercept_endpoint_groups_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptEndpointGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_intercept_endpoint_groups_create_builder(
        client,
        &args.parent,
        &args.interceptEndpointGroupId,
        &args.requestId,
    )?;
    networksecurity_projects_locations_intercept_endpoint_groups_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}
/// Deletes an endpoint group. See <https://google.aip.`dev/135`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_endpoint_groups_delete_execute()` to send, or `networksecurity_projects_locations_intercept_endpoint_groups_delete` for simplest API.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}
/// Deletes an endpoint group. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_endpoint_groups_delete_execute()` or `networksecurity_projects_locations_intercept_endpoint_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}
/// Deletes an endpoint group. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_endpoint_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_groups_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_endpoint_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_endpoint_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_endpoint_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptEndpointGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}
/// Deletes an endpoint group. See <https://google.aip.`dev/135`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_endpoint_groups_delete_builder()` + `networksecurity_projects_locations_intercept_endpoint_groups_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptEndpointGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_intercept_endpoint_groups_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networksecurity_projects_locations_intercept_endpoint_groups_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}
/// Gets a specific endpoint group. See <https://google.aip.`dev/131`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_endpoint_groups_get_execute()` to send, or `networksecurity_projects_locations_intercept_endpoint_groups_get` for simplest API.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}
/// Gets a specific endpoint group. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_endpoint_groups_get_execute()` or `networksecurity_projects_locations_intercept_endpoint_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterceptEndpointGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InterceptEndpointGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}
/// Gets a specific endpoint group. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_endpoint_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_groups_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_endpoint_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterceptEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_endpoint_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_endpoint_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptEndpointGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}
/// Gets a specific endpoint group. See <https://google.aip.`dev/131`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_endpoint_groups_get_builder()` + `networksecurity_projects_locations_intercept_endpoint_groups_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptEndpointGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InterceptEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_intercept_endpoint_groups_get_builder(
        client, &args.name,
    )?;
    networksecurity_projects_locations_intercept_endpoint_groups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups
/// Lists endpoint groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_endpoint_groups_list_execute()` to send, or `networksecurity_projects_locations_intercept_endpoint_groups_list` for simplest API.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptEndpointGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups
/// Lists endpoint groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_endpoint_groups_list_execute()` or `networksecurity_projects_locations_intercept_endpoint_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInterceptEndpointGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInterceptEndpointGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups
/// Lists endpoint groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_endpoint_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_groups_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_endpoint_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInterceptEndpointGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_endpoint_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_endpoint_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptEndpointGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups
/// Lists endpoint groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_endpoint_groups_list_builder()` + `networksecurity_projects_locations_intercept_endpoint_groups_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptEndpointGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInterceptEndpointGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_intercept_endpoint_groups_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_intercept_endpoint_groups_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}
/// Updates an endpoint group. See <https://google.aip.`dev/134`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_intercept_endpoint_groups_patch_execute()` to send, or `networksecurity_projects_locations_intercept_endpoint_groups_patch` for simplest API.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}
/// Updates an endpoint group. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_intercept_endpoint_groups_patch_execute()` or `networksecurity_projects_locations_intercept_endpoint_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}
/// Updates an endpoint group. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_intercept_endpoint_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_groups_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_intercept_endpoint_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_intercept_endpoint_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_intercept_endpoint_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_intercept_endpoint_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsInterceptEndpointGroupsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/interceptEndpointGroups/{interceptEndpointGroupsId}
/// Updates an endpoint group. See <https://google.aip.`dev/134`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_intercept_endpoint_groups_patch_builder()` + `networksecurity_projects_locations_intercept_endpoint_groups_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_intercept_endpoint_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_intercept_endpoint_groups_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsInterceptEndpointGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_intercept_endpoint_groups_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_intercept_endpoint_groups_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups
/// Creates a deployment group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_deployment_groups_create_execute()` to send, or `networksecurity_projects_locations_mirroring_deployment_groups_create` for simplest API.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    mirroringDeploymentGroupId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringDeploymentGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = mirroringDeploymentGroupId.as_ref() {
        query_parts.push(format!("mirroringDeploymentGroupId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups
/// Creates a deployment group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_deployment_groups_create_execute()` or `networksecurity_projects_locations_mirroring_deployment_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployment_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups
/// Creates a deployment group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_deployment_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployment_groups_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_deployment_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployment_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_deployment_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_deployment_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringDeploymentGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: mirroringDeploymentGroupId
    pub mirroringDeploymentGroupId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups
/// Creates a deployment group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_deployment_groups_create_builder()` + `networksecurity_projects_locations_mirroring_deployment_groups_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployment_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_mirroring_deployment_groups_create_builder(
        client,
        &args.parent,
        &args.mirroringDeploymentGroupId,
        &args.requestId,
    )?;
    networksecurity_projects_locations_mirroring_deployment_groups_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}
/// Deletes a deployment group. See <https://google.aip.`dev/135`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_deployment_groups_delete_execute()` to send, or `networksecurity_projects_locations_mirroring_deployment_groups_delete` for simplest API.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}
/// Deletes a deployment group. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_deployment_groups_delete_execute()` or `networksecurity_projects_locations_mirroring_deployment_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployment_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}
/// Deletes a deployment group. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_deployment_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployment_groups_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_deployment_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployment_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_deployment_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_deployment_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringDeploymentGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}
/// Deletes a deployment group. See <https://google.aip.`dev/135`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_deployment_groups_delete_builder()` + `networksecurity_projects_locations_mirroring_deployment_groups_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployment_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_mirroring_deployment_groups_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networksecurity_projects_locations_mirroring_deployment_groups_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}
/// Gets a specific deployment group. See <https://google.aip.`dev/131`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_deployment_groups_get_execute()` to send, or `networksecurity_projects_locations_mirroring_deployment_groups_get` for simplest API.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}
/// Gets a specific deployment group. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_deployment_groups_get_execute()` or `networksecurity_projects_locations_mirroring_deployment_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployment_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MirroringDeploymentGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MirroringDeploymentGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}
/// Gets a specific deployment group. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_deployment_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployment_groups_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_deployment_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployment_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MirroringDeploymentGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_deployment_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_deployment_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringDeploymentGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}
/// Gets a specific deployment group. See <https://google.aip.`dev/131`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_deployment_groups_get_builder()` + `networksecurity_projects_locations_mirroring_deployment_groups_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployment_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MirroringDeploymentGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_mirroring_deployment_groups_get_builder(
        client, &args.name,
    )?;
    networksecurity_projects_locations_mirroring_deployment_groups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups
/// Lists deployment groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_deployment_groups_list_execute()` to send, or `networksecurity_projects_locations_mirroring_deployment_groups_list` for simplest API.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringDeploymentGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups
/// Lists deployment groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_deployment_groups_list_execute()` or `networksecurity_projects_locations_mirroring_deployment_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployment_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMirroringDeploymentGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMirroringDeploymentGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups
/// Lists deployment groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_deployment_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployment_groups_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_deployment_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployment_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMirroringDeploymentGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_deployment_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_deployment_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringDeploymentGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups
/// Lists deployment groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_deployment_groups_list_builder()` + `networksecurity_projects_locations_mirroring_deployment_groups_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployment_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMirroringDeploymentGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_mirroring_deployment_groups_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_mirroring_deployment_groups_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}
/// Updates a deployment group. See <https://google.aip.`dev/134`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_deployment_groups_patch_execute()` to send, or `networksecurity_projects_locations_mirroring_deployment_groups_patch` for simplest API.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}
/// Updates a deployment group. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_deployment_groups_patch_execute()` or `networksecurity_projects_locations_mirroring_deployment_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployment_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}
/// Updates a deployment group. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_deployment_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployment_groups_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_deployment_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployment_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_deployment_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_deployment_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringDeploymentGroupsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringDeploymentGroups/{mirroringDeploymentGroupsId}
/// Updates a deployment group. See <https://google.aip.`dev/134`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_deployment_groups_patch_builder()` + `networksecurity_projects_locations_mirroring_deployment_groups_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployment_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployment_groups_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_mirroring_deployment_groups_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_mirroring_deployment_groups_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments
/// Creates a deployment in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_deployments_create_execute()` to send, or `networksecurity_projects_locations_mirroring_deployments_create` for simplest API.

pub fn networksecurity_projects_locations_mirroring_deployments_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    mirroringDeploymentId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringDeployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = mirroringDeploymentId.as_ref() {
        query_parts.push(format!("mirroringDeploymentId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments
/// Creates a deployment in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_deployments_create_execute()` or `networksecurity_projects_locations_mirroring_deployments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments
/// Creates a deployment in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_deployments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployments_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_deployments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_deployments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_deployments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_deployments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringDeploymentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: mirroringDeploymentId
    pub mirroringDeploymentId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments
/// Creates a deployment in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_deployments_create_builder()` + `networksecurity_projects_locations_mirroring_deployments_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployments_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringDeploymentsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_mirroring_deployments_create_builder(
        client,
        &args.parent,
        &args.mirroringDeploymentId,
        &args.requestId,
    )?;
    networksecurity_projects_locations_mirroring_deployments_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}
/// Deletes a deployment. See <https://google.aip.`dev/135`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_deployments_delete_execute()` to send, or `networksecurity_projects_locations_mirroring_deployments_delete` for simplest API.

pub fn networksecurity_projects_locations_mirroring_deployments_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}
/// Deletes a deployment. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_deployments_delete_execute()` or `networksecurity_projects_locations_mirroring_deployments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}
/// Deletes a deployment. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_deployments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployments_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_deployments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_deployments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_deployments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_deployments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringDeploymentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}
/// Deletes a deployment. See <https://google.aip.`dev/135`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_deployments_delete_builder()` + `networksecurity_projects_locations_mirroring_deployments_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployments_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringDeploymentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_mirroring_deployments_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networksecurity_projects_locations_mirroring_deployments_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}
/// Gets a specific deployment. See <https://google.aip.`dev/131`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_deployments_get_execute()` to send, or `networksecurity_projects_locations_mirroring_deployments_get` for simplest API.

pub fn networksecurity_projects_locations_mirroring_deployments_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}
/// Gets a specific deployment. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_deployments_get_execute()` or `networksecurity_projects_locations_mirroring_deployments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MirroringDeployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MirroringDeployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}
/// Gets a specific deployment. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_deployments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployments_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_deployments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MirroringDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_deployments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_deployments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringDeploymentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}
/// Gets a specific deployment. See <https://google.aip.`dev/131`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_deployments_get_builder()` + `networksecurity_projects_locations_mirroring_deployments_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployments_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringDeploymentsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MirroringDeployment>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_mirroring_deployments_get_builder(client, &args.name)?;
    networksecurity_projects_locations_mirroring_deployments_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments
/// Lists deployments in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_deployments_list_execute()` to send, or `networksecurity_projects_locations_mirroring_deployments_list` for simplest API.

pub fn networksecurity_projects_locations_mirroring_deployments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringDeployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments
/// Lists deployments in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_deployments_list_execute()` or `networksecurity_projects_locations_mirroring_deployments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMirroringDeploymentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMirroringDeploymentsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments
/// Lists deployments in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_deployments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployments_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_deployments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMirroringDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_deployments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_deployments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringDeploymentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments
/// Lists deployments in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_deployments_list_builder()` + `networksecurity_projects_locations_mirroring_deployments_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployments_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringDeploymentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMirroringDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_mirroring_deployments_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_mirroring_deployments_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}
/// Updates a deployment. See <https://google.aip.`dev/134`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_deployments_patch_execute()` to send, or `networksecurity_projects_locations_mirroring_deployments_patch` for simplest API.

pub fn networksecurity_projects_locations_mirroring_deployments_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}
/// Updates a deployment. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_deployments_patch_execute()` or `networksecurity_projects_locations_mirroring_deployments_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployments_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}
/// Updates a deployment. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_deployments_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployments_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_deployments_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_deployments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_deployments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_deployments_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_deployments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringDeploymentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringDeployments/{mirroringDeploymentsId}
/// Updates a deployment. See <https://google.aip.`dev/134`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_deployments_patch_builder()` + `networksecurity_projects_locations_mirroring_deployments_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_deployments_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_deployments_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringDeploymentsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_mirroring_deployments_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_mirroring_deployments_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations
/// Creates an association in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_endpoint_group_associations_create_execute()` to send, or `networksecurity_projects_locations_mirroring_endpoint_group_associations_create` for simplest API.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    mirroringEndpointGroupAssociationId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringEndpointGroupAssociations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = mirroringEndpointGroupAssociationId.as_ref() {
        query_parts.push(format!("mirroringEndpointGroupAssociationId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations
/// Creates an association in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_create_execute()` or `networksecurity_projects_locations_mirroring_endpoint_group_associations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_group_associations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations
/// Creates an association in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_group_associations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_mirroring_endpoint_group_associations_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_endpoint_group_associations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: mirroringEndpointGroupAssociationId
    pub mirroringEndpointGroupAssociationId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations
/// Creates an association in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_create_builder()` + `networksecurity_projects_locations_mirroring_endpoint_group_associations_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_mirroring_endpoint_group_associations_create_builder(
            client,
            &args.parent,
            &args.mirroringEndpointGroupAssociationId,
            &args.requestId,
        )?;
    networksecurity_projects_locations_mirroring_endpoint_group_associations_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}
/// Deletes an association. See <https://google.aip.`dev/135`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_execute()` to send, or `networksecurity_projects_locations_mirroring_endpoint_group_associations_delete` for simplest API.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}
/// Deletes an association. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_execute()` or `networksecurity_projects_locations_mirroring_endpoint_group_associations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}
/// Deletes an association. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_endpoint_group_associations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}
/// Deletes an association. See <https://google.aip.`dev/135`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_builder()` + `networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_builder(
            client,
            &args.name,
            &args.requestId,
        )?;
    networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}
/// Gets a specific association. See <https://google.aip.`dev/131`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_endpoint_group_associations_get_execute()` to send, or `networksecurity_projects_locations_mirroring_endpoint_group_associations_get` for simplest API.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}
/// Gets a specific association. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_get_execute()` or `networksecurity_projects_locations_mirroring_endpoint_group_associations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_group_associations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MirroringEndpointGroupAssociation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MirroringEndpointGroupAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}
/// Gets a specific association. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_group_associations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MirroringEndpointGroupAssociation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_mirroring_endpoint_group_associations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_endpoint_group_associations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}
/// Gets a specific association. See <https://google.aip.`dev/131`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_get_builder()` + `networksecurity_projects_locations_mirroring_endpoint_group_associations_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MirroringEndpointGroupAssociation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_mirroring_endpoint_group_associations_get_builder(
            client, &args.name,
        )?;
    networksecurity_projects_locations_mirroring_endpoint_group_associations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations
/// Lists associations in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_endpoint_group_associations_list_execute()` to send, or `networksecurity_projects_locations_mirroring_endpoint_group_associations_list` for simplest API.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringEndpointGroupAssociations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations
/// Lists associations in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_list_execute()` or `networksecurity_projects_locations_mirroring_endpoint_group_associations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_group_associations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMirroringEndpointGroupAssociationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMirroringEndpointGroupAssociationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations
/// Lists associations in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_group_associations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMirroringEndpointGroupAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_endpoint_group_associations_list_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_endpoint_group_associations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations
/// Lists associations in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_list_builder()` + `networksecurity_projects_locations_mirroring_endpoint_group_associations_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMirroringEndpointGroupAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_mirroring_endpoint_group_associations_list_builder(
            client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )?;
    networksecurity_projects_locations_mirroring_endpoint_group_associations_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}
/// Updates an association. See <https://google.aip.`dev/134`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_execute()` to send, or `networksecurity_projects_locations_mirroring_endpoint_group_associations_patch` for simplest API.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}
/// Updates an association. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_execute()` or `networksecurity_projects_locations_mirroring_endpoint_group_associations_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}
/// Updates an association. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_endpoint_group_associations_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroupAssociations/{mirroringEndpointGroupAssociationsId}
/// Updates an association. See <https://google.aip.`dev/134`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_builder()` + `networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_builder(
            client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )?;
    networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups
/// Creates an endpoint group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_endpoint_groups_create_execute()` to send, or `networksecurity_projects_locations_mirroring_endpoint_groups_create` for simplest API.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    mirroringEndpointGroupId: &Option<Option<String>>,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringEndpointGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = mirroringEndpointGroupId.as_ref() {
        query_parts.push(format!("mirroringEndpointGroupId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups
/// Creates an endpoint group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_endpoint_groups_create_execute()` or `networksecurity_projects_locations_mirroring_endpoint_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups
/// Creates an endpoint group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_endpoint_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_groups_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_endpoint_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_endpoint_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_endpoint_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringEndpointGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: mirroringEndpointGroupId
    pub mirroringEndpointGroupId: Option<Option<String>>,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups
/// Creates an endpoint group in a given project and location. See <https://google.aip.`dev/133`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_endpoint_groups_create_builder()` + `networksecurity_projects_locations_mirroring_endpoint_groups_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringEndpointGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_mirroring_endpoint_groups_create_builder(
        client,
        &args.parent,
        &args.mirroringEndpointGroupId,
        &args.requestId,
    )?;
    networksecurity_projects_locations_mirroring_endpoint_groups_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}
/// Deletes an endpoint group. See <https://google.aip.`dev/135`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_endpoint_groups_delete_execute()` to send, or `networksecurity_projects_locations_mirroring_endpoint_groups_delete` for simplest API.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}
/// Deletes an endpoint group. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_endpoint_groups_delete_execute()` or `networksecurity_projects_locations_mirroring_endpoint_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}
/// Deletes an endpoint group. See <https://google.aip.`dev/135`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_endpoint_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_groups_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_endpoint_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_endpoint_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_endpoint_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringEndpointGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}
/// Deletes an endpoint group. See <https://google.aip.`dev/135`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_endpoint_groups_delete_builder()` + `networksecurity_projects_locations_mirroring_endpoint_groups_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringEndpointGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_mirroring_endpoint_groups_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networksecurity_projects_locations_mirroring_endpoint_groups_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}
/// Gets a specific endpoint group. See <https://google.aip.`dev/131`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_endpoint_groups_get_execute()` to send, or `networksecurity_projects_locations_mirroring_endpoint_groups_get` for simplest API.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}
/// Gets a specific endpoint group. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_endpoint_groups_get_execute()` or `networksecurity_projects_locations_mirroring_endpoint_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MirroringEndpointGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MirroringEndpointGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}
/// Gets a specific endpoint group. See <https://google.aip.`dev/131`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_endpoint_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_groups_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_endpoint_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MirroringEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_endpoint_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_endpoint_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringEndpointGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}
/// Gets a specific endpoint group. See <https://google.aip.`dev/131`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_endpoint_groups_get_builder()` + `networksecurity_projects_locations_mirroring_endpoint_groups_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringEndpointGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<MirroringEndpointGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_mirroring_endpoint_groups_get_builder(
        client, &args.name,
    )?;
    networksecurity_projects_locations_mirroring_endpoint_groups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups
/// Lists endpoint groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_endpoint_groups_list_execute()` to send, or `networksecurity_projects_locations_mirroring_endpoint_groups_list` for simplest API.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    orderBy: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringEndpointGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups
/// Lists endpoint groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_endpoint_groups_list_execute()` or `networksecurity_projects_locations_mirroring_endpoint_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMirroringEndpointGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMirroringEndpointGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups
/// Lists endpoint groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_endpoint_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_groups_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_endpoint_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMirroringEndpointGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_endpoint_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_endpoint_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringEndpointGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: orderBy
    pub orderBy: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups
/// Lists endpoint groups in a given project and location. See <https://google.aip.`dev/132`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_endpoint_groups_list_builder()` + `networksecurity_projects_locations_mirroring_endpoint_groups_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringEndpointGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMirroringEndpointGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_mirroring_endpoint_groups_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_mirroring_endpoint_groups_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}
/// Updates an endpoint group. See <https://google.aip.`dev/134`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_mirroring_endpoint_groups_patch_execute()` to send, or `networksecurity_projects_locations_mirroring_endpoint_groups_patch` for simplest API.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}
/// Updates an endpoint group. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_mirroring_endpoint_groups_patch_execute()` or `networksecurity_projects_locations_mirroring_endpoint_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}
/// Updates an endpoint group. See <https://google.aip.`dev/134`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_mirroring_endpoint_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_groups_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_mirroring_endpoint_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_mirroring_endpoint_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_mirroring_endpoint_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_mirroring_endpoint_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsMirroringEndpointGroupsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/mirroringEndpointGroups/{mirroringEndpointGroupsId}
/// Updates an endpoint group. See <https://google.aip.`dev/134`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_mirroring_endpoint_groups_patch_builder()` + `networksecurity_projects_locations_mirroring_endpoint_groups_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_mirroring_endpoint_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_mirroring_endpoint_groups_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsMirroringEndpointGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_mirroring_endpoint_groups_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_mirroring_endpoint_groups_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_operations_cancel_execute()` to send, or `networksecurity_projects_locations_operations_cancel` for simplest API.

pub fn networksecurity_projects_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_operations_cancel_execute()` or `networksecurity_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `networksecurity_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_operations_cancel_builder()` + `networksecurity_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `networksecurity_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_operations_cancel_builder(client, &args.name)?;
    networksecurity_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_operations_delete_execute()` to send, or `networksecurity_projects_locations_operations_delete` for simplest API.

pub fn networksecurity_projects_locations_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_operations_delete_execute()` or `networksecurity_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_operations_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_operations_delete_builder()` + `networksecurity_projects_locations_operations_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_operations_delete_builder(client, &args.name)?;
    networksecurity_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_operations_get_execute()` to send, or `networksecurity_projects_locations_operations_get` for simplest API.

pub fn networksecurity_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_operations_get_execute()` or `networksecurity_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_operations_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_operations_get_builder()` + `networksecurity_projects_locations_operations_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_operations_get_builder(client, &args.name)?;
    networksecurity_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_operations_list_execute()` to send, or `networksecurity_projects_locations_operations_list` for simplest API.

pub fn networksecurity_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_operations_list_execute()` or `networksecurity_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_operations_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_operations_list_builder()` + `networksecurity_projects_locations_operations_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    networksecurity_projects_locations_operations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies
/// Creates a new ServerTlsPolicy in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_server_tls_policies_create_execute()` to send, or `networksecurity_projects_locations_server_tls_policies_create` for simplest API.

pub fn networksecurity_projects_locations_server_tls_policies_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    serverTlsPolicyId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/serverTlsPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = serverTlsPolicyId.as_ref() {
        query_parts.push(format!("serverTlsPolicyId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies
/// Creates a new ServerTlsPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_server_tls_policies_create_execute()` or `networksecurity_projects_locations_server_tls_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies
/// Creates a new ServerTlsPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_server_tls_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_server_tls_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_server_tls_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_server_tls_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_server_tls_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsServerTlsPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: serverTlsPolicyId
    pub serverTlsPolicyId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies
/// Creates a new ServerTlsPolicy in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_server_tls_policies_create_builder()` + `networksecurity_projects_locations_server_tls_policies_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsServerTlsPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_server_tls_policies_create_builder(
        client,
        &args.parent,
        &args.serverTlsPolicyId,
    )?;
    networksecurity_projects_locations_server_tls_policies_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}
/// Deletes a single ServerTlsPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_server_tls_policies_delete_execute()` to send, or `networksecurity_projects_locations_server_tls_policies_delete` for simplest API.

pub fn networksecurity_projects_locations_server_tls_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}
/// Deletes a single ServerTlsPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_server_tls_policies_delete_execute()` or `networksecurity_projects_locations_server_tls_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}
/// Deletes a single ServerTlsPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_server_tls_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_server_tls_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_server_tls_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_server_tls_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_server_tls_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsServerTlsPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}
/// Deletes a single ServerTlsPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_server_tls_policies_delete_builder()` + `networksecurity_projects_locations_server_tls_policies_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsServerTlsPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_server_tls_policies_delete_builder(client, &args.name)?;
    networksecurity_projects_locations_server_tls_policies_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}
/// Gets details of a single ServerTlsPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_server_tls_policies_get_execute()` to send, or `networksecurity_projects_locations_server_tls_policies_get` for simplest API.

pub fn networksecurity_projects_locations_server_tls_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}
/// Gets details of a single ServerTlsPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_server_tls_policies_get_execute()` or `networksecurity_projects_locations_server_tls_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServerTlsPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServerTlsPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}
/// Gets details of a single ServerTlsPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_server_tls_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_server_tls_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_server_tls_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServerTlsPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_server_tls_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_server_tls_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsServerTlsPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}
/// Gets details of a single ServerTlsPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_server_tls_policies_get_builder()` + `networksecurity_projects_locations_server_tls_policies_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsServerTlsPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServerTlsPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_server_tls_policies_get_builder(client, &args.name)?;
    networksecurity_projects_locations_server_tls_policies_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_server_tls_policies_get_iam_policy_execute()` to send, or `networksecurity_projects_locations_server_tls_policies_get_iam_policy` for simplest API.

pub fn networksecurity_projects_locations_server_tls_policies_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_server_tls_policies_get_iam_policy_execute()` or `networksecurity_projects_locations_server_tls_policies_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_server_tls_policies_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_get_iam_policy_task()`.
/// For the simplest API, use `networksecurity_projects_locations_server_tls_policies_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_server_tls_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_server_tls_policies_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_server_tls_policies_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsServerTlsPoliciesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_server_tls_policies_get_iam_policy_builder()` + `networksecurity_projects_locations_server_tls_policies_get_iam_policy_execute()`.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_get_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsServerTlsPoliciesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_server_tls_policies_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    networksecurity_projects_locations_server_tls_policies_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies
/// Lists ServerTlsPolicies in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_server_tls_policies_list_execute()` to send, or `networksecurity_projects_locations_server_tls_policies_list` for simplest API.

pub fn networksecurity_projects_locations_server_tls_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    returnPartialSuccess: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/serverTlsPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies
/// Lists ServerTlsPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_server_tls_policies_list_execute()` or `networksecurity_projects_locations_server_tls_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListServerTlsPoliciesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListServerTlsPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies
/// Lists ServerTlsPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_server_tls_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_server_tls_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_server_tls_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListServerTlsPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_server_tls_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_server_tls_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsServerTlsPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies
/// Lists ServerTlsPolicies in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_server_tls_policies_list_builder()` + `networksecurity_projects_locations_server_tls_policies_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsServerTlsPoliciesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListServerTlsPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_server_tls_policies_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    networksecurity_projects_locations_server_tls_policies_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}
/// Updates the parameters of a single ServerTlsPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_server_tls_policies_patch_execute()` to send, or `networksecurity_projects_locations_server_tls_policies_patch` for simplest API.

pub fn networksecurity_projects_locations_server_tls_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}
/// Updates the parameters of a single ServerTlsPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_server_tls_policies_patch_execute()` or `networksecurity_projects_locations_server_tls_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}
/// Updates the parameters of a single ServerTlsPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_server_tls_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_server_tls_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_server_tls_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_server_tls_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_server_tls_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsServerTlsPoliciesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}
/// Updates the parameters of a single ServerTlsPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_server_tls_policies_patch_builder()` + `networksecurity_projects_locations_server_tls_policies_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsServerTlsPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_server_tls_policies_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_server_tls_policies_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_server_tls_policies_set_iam_policy_execute()` to send, or `networksecurity_projects_locations_server_tls_policies_set_iam_policy` for simplest API.

pub fn networksecurity_projects_locations_server_tls_policies_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_server_tls_policies_set_iam_policy_execute()` or `networksecurity_projects_locations_server_tls_policies_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_server_tls_policies_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_set_iam_policy_task()`.
/// For the simplest API, use `networksecurity_projects_locations_server_tls_policies_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_server_tls_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_server_tls_policies_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_server_tls_policies_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsServerTlsPoliciesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_server_tls_policies_set_iam_policy_builder()` + `networksecurity_projects_locations_server_tls_policies_set_iam_policy_execute()`.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_set_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsServerTlsPoliciesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_server_tls_policies_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    networksecurity_projects_locations_server_tls_policies_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_server_tls_policies_test_iam_permissions_execute()` to send, or `networksecurity_projects_locations_server_tls_policies_test_iam_permissions` for simplest API.

pub fn networksecurity_projects_locations_server_tls_policies_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_server_tls_policies_test_iam_permissions_execute()` or `networksecurity_projects_locations_server_tls_policies_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_server_tls_policies_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_test_iam_permissions_task()`.
/// For the simplest API, use `networksecurity_projects_locations_server_tls_policies_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_server_tls_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_server_tls_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networksecurity_projects_locations_server_tls_policies_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_server_tls_policies_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsServerTlsPoliciesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serverTlsPolicies/{serverTlsPoliciesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_server_tls_policies_test_iam_permissions_builder()` + `networksecurity_projects_locations_server_tls_policies_test_iam_permissions_execute()`.
/// For task-level control, use `networksecurity_projects_locations_server_tls_policies_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_server_tls_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsServerTlsPoliciesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_server_tls_policies_test_iam_permissions_builder(
            client,
            &args.resource,
        )?;
    networksecurity_projects_locations_server_tls_policies_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies
/// Creates a new TlsInspectionPolicy in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_tls_inspection_policies_create_execute()` to send, or `networksecurity_projects_locations_tls_inspection_policies_create` for simplest API.

pub fn networksecurity_projects_locations_tls_inspection_policies_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    tlsInspectionPolicyId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/tlsInspectionPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = tlsInspectionPolicyId.as_ref() {
        query_parts.push(format!("tlsInspectionPolicyId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies
/// Creates a new TlsInspectionPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_tls_inspection_policies_create_execute()` or `networksecurity_projects_locations_tls_inspection_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_tls_inspection_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_tls_inspection_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies
/// Creates a new TlsInspectionPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_tls_inspection_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_tls_inspection_policies_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_tls_inspection_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_tls_inspection_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_tls_inspection_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_tls_inspection_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_tls_inspection_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsTlsInspectionPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: tlsInspectionPolicyId
    pub tlsInspectionPolicyId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies
/// Creates a new TlsInspectionPolicy in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_tls_inspection_policies_create_builder()` + `networksecurity_projects_locations_tls_inspection_policies_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_tls_inspection_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_tls_inspection_policies_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsTlsInspectionPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_tls_inspection_policies_create_builder(
        client,
        &args.parent,
        &args.tlsInspectionPolicyId,
    )?;
    networksecurity_projects_locations_tls_inspection_policies_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}
/// Deletes a single TlsInspectionPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_tls_inspection_policies_delete_execute()` to send, or `networksecurity_projects_locations_tls_inspection_policies_delete` for simplest API.

pub fn networksecurity_projects_locations_tls_inspection_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    force: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}
/// Deletes a single TlsInspectionPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_tls_inspection_policies_delete_execute()` or `networksecurity_projects_locations_tls_inspection_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_tls_inspection_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_tls_inspection_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}
/// Deletes a single TlsInspectionPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_tls_inspection_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_tls_inspection_policies_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_tls_inspection_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_tls_inspection_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_tls_inspection_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_tls_inspection_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_tls_inspection_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsTlsInspectionPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<Option<String>>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}
/// Deletes a single TlsInspectionPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_tls_inspection_policies_delete_builder()` + `networksecurity_projects_locations_tls_inspection_policies_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_tls_inspection_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_tls_inspection_policies_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsTlsInspectionPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_tls_inspection_policies_delete_builder(
        client,
        &args.name,
        &args.force,
    )?;
    networksecurity_projects_locations_tls_inspection_policies_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}
/// Gets details of a single TlsInspectionPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_tls_inspection_policies_get_execute()` to send, or `networksecurity_projects_locations_tls_inspection_policies_get` for simplest API.

pub fn networksecurity_projects_locations_tls_inspection_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}
/// Gets details of a single TlsInspectionPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_tls_inspection_policies_get_execute()` or `networksecurity_projects_locations_tls_inspection_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_tls_inspection_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_tls_inspection_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TlsInspectionPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TlsInspectionPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}
/// Gets details of a single TlsInspectionPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_tls_inspection_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_tls_inspection_policies_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_tls_inspection_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_tls_inspection_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_tls_inspection_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TlsInspectionPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_tls_inspection_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_tls_inspection_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsTlsInspectionPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}
/// Gets details of a single TlsInspectionPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_tls_inspection_policies_get_builder()` + `networksecurity_projects_locations_tls_inspection_policies_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_tls_inspection_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_tls_inspection_policies_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsTlsInspectionPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TlsInspectionPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networksecurity_projects_locations_tls_inspection_policies_get_builder(client, &args.name)?;
    networksecurity_projects_locations_tls_inspection_policies_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies
/// Lists TlsInspectionPolicies in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_tls_inspection_policies_list_execute()` to send, or `networksecurity_projects_locations_tls_inspection_policies_list` for simplest API.

pub fn networksecurity_projects_locations_tls_inspection_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/tlsInspectionPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies
/// Lists TlsInspectionPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_tls_inspection_policies_list_execute()` or `networksecurity_projects_locations_tls_inspection_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_tls_inspection_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_tls_inspection_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTlsInspectionPoliciesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTlsInspectionPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies
/// Lists TlsInspectionPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_tls_inspection_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_tls_inspection_policies_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_tls_inspection_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_tls_inspection_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_tls_inspection_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTlsInspectionPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_tls_inspection_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_tls_inspection_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsTlsInspectionPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies
/// Lists TlsInspectionPolicies in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_tls_inspection_policies_list_builder()` + `networksecurity_projects_locations_tls_inspection_policies_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_tls_inspection_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_tls_inspection_policies_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsTlsInspectionPoliciesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTlsInspectionPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_tls_inspection_policies_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_tls_inspection_policies_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}
/// Updates the parameters of a single TlsInspectionPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_tls_inspection_policies_patch_execute()` to send, or `networksecurity_projects_locations_tls_inspection_policies_patch` for simplest API.

pub fn networksecurity_projects_locations_tls_inspection_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}
/// Updates the parameters of a single TlsInspectionPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_tls_inspection_policies_patch_execute()` or `networksecurity_projects_locations_tls_inspection_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_tls_inspection_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_tls_inspection_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}
/// Updates the parameters of a single TlsInspectionPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_tls_inspection_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_tls_inspection_policies_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_tls_inspection_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_tls_inspection_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_tls_inspection_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_tls_inspection_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_tls_inspection_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsTlsInspectionPoliciesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/tlsInspectionPolicies/{tlsInspectionPoliciesId}
/// Updates the parameters of a single TlsInspectionPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_tls_inspection_policies_patch_builder()` + `networksecurity_projects_locations_tls_inspection_policies_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_tls_inspection_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_tls_inspection_policies_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsTlsInspectionPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_tls_inspection_policies_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_tls_inspection_policies_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/urlLists
/// Creates a new UrlList in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_url_lists_create_execute()` to send, or `networksecurity_projects_locations_url_lists_create` for simplest API.

pub fn networksecurity_projects_locations_url_lists_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    urlListId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/urlLists",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = urlListId.as_ref() {
        query_parts.push(format!("urlListId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/urlLists
/// Creates a new UrlList in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_url_lists_create_execute()` or `networksecurity_projects_locations_url_lists_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_url_lists_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_url_lists_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/urlLists
/// Creates a new UrlList in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_url_lists_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_url_lists_create_task()`.
/// For the simplest API, use `networksecurity_projects_locations_url_lists_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_url_lists_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_url_lists_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_url_lists_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_url_lists_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsUrlListsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: urlListId
    pub urlListId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/urlLists
/// Creates a new UrlList in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_url_lists_create_builder()` + `networksecurity_projects_locations_url_lists_create_execute()`.
/// For task-level control, use `networksecurity_projects_locations_url_lists_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_url_lists_create(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsUrlListsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_url_lists_create_builder(
        client,
        &args.parent,
        &args.urlListId,
    )?;
    networksecurity_projects_locations_url_lists_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/urlLists/{urlListsId}
/// Deletes a single UrlList.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_url_lists_delete_execute()` to send, or `networksecurity_projects_locations_url_lists_delete` for simplest API.

pub fn networksecurity_projects_locations_url_lists_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/urlLists/{urlListsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/urlLists/{urlListsId}
/// Deletes a single UrlList.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_url_lists_delete_execute()` or `networksecurity_projects_locations_url_lists_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_url_lists_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_url_lists_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/urlLists/{urlListsId}
/// Deletes a single UrlList.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_url_lists_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_url_lists_delete_task()`.
/// For the simplest API, use `networksecurity_projects_locations_url_lists_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_url_lists_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_url_lists_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_url_lists_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_url_lists_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsUrlListsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/urlLists/{urlListsId}
/// Deletes a single UrlList.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_url_lists_delete_builder()` + `networksecurity_projects_locations_url_lists_delete_execute()`.
/// For task-level control, use `networksecurity_projects_locations_url_lists_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_url_lists_delete(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsUrlListsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_url_lists_delete_builder(client, &args.name)?;
    networksecurity_projects_locations_url_lists_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/urlLists/{urlListsId}
/// Gets details of a single UrlList.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_url_lists_get_execute()` to send, or `networksecurity_projects_locations_url_lists_get` for simplest API.

pub fn networksecurity_projects_locations_url_lists_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/urlLists/{urlListsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/urlLists/{urlListsId}
/// Gets details of a single UrlList.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_url_lists_get_execute()` or `networksecurity_projects_locations_url_lists_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_url_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_url_lists_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UrlList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/urlLists/{urlListsId}
/// Gets details of a single UrlList.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_url_lists_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_url_lists_get_task()`.
/// For the simplest API, use `networksecurity_projects_locations_url_lists_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_url_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_url_lists_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_url_lists_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_url_lists_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsUrlListsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/urlLists/{urlListsId}
/// Gets details of a single UrlList.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_url_lists_get_builder()` + `networksecurity_projects_locations_url_lists_get_execute()`.
/// For task-level control, use `networksecurity_projects_locations_url_lists_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_url_lists_get(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsUrlListsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UrlList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_url_lists_get_builder(client, &args.name)?;
    networksecurity_projects_locations_url_lists_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/urlLists
/// Lists UrlLists in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_url_lists_list_execute()` to send, or `networksecurity_projects_locations_url_lists_list` for simplest API.

pub fn networksecurity_projects_locations_url_lists_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/urlLists",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/urlLists
/// Lists UrlLists in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_url_lists_list_execute()` or `networksecurity_projects_locations_url_lists_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_url_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_url_lists_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUrlListsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUrlListsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/urlLists
/// Lists UrlLists in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_url_lists_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_url_lists_list_task()`.
/// For the simplest API, use `networksecurity_projects_locations_url_lists_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_url_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_url_lists_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUrlListsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_url_lists_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_url_lists_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsUrlListsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/urlLists
/// Lists UrlLists in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_url_lists_list_builder()` + `networksecurity_projects_locations_url_lists_list_execute()`.
/// For task-level control, use `networksecurity_projects_locations_url_lists_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_url_lists_list(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsUrlListsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUrlListsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_url_lists_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    networksecurity_projects_locations_url_lists_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/urlLists/{urlListsId}
/// Updates the parameters of a single UrlList.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networksecurity_projects_locations_url_lists_patch_execute()` to send, or `networksecurity_projects_locations_url_lists_patch` for simplest API.

pub fn networksecurity_projects_locations_url_lists_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networksecurity.googleapis.com/v1/projects/{}/locations/{locationsId}/urlLists/{urlListsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/urlLists/{urlListsId}
/// Updates the parameters of a single UrlList.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networksecurity_projects_locations_url_lists_patch_execute()` or `networksecurity_projects_locations_url_lists_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_url_lists_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_url_lists_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/urlLists/{urlListsId}
/// Updates the parameters of a single UrlList.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networksecurity_projects_locations_url_lists_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networksecurity_projects_locations_url_lists_patch_task()`.
/// For the simplest API, use `networksecurity_projects_locations_url_lists_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networksecurity_projects_locations_url_lists_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networksecurity_projects_locations_url_lists_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networksecurity_projects_locations_url_lists_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networksecurity_projects_locations_url_lists_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworksecurityProjectsLocationsUrlListsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/urlLists/{urlListsId}
/// Updates the parameters of a single UrlList.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networksecurity_projects_locations_url_lists_patch_builder()` + `networksecurity_projects_locations_url_lists_patch_execute()`.
/// For task-level control, use `networksecurity_projects_locations_url_lists_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networksecurity_projects_locations_url_lists_patch(
    client: &SimpleHttpClient,
    args: &NetworksecurityProjectsLocationsUrlListsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networksecurity_projects_locations_url_lists_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    networksecurity_projects_locations_url_lists_patch_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsAddressGroupsAddItemsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsAddressGroupsAddItemsArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsAddressGroupsAddItemsArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.addressGroup)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsAddressGroupsCloneItemsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsAddressGroupsCloneItemsArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsAddressGroupsCloneItemsArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.addressGroup)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsAddressGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsAddressGroupsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsAddressGroupsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsAddressGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsAddressGroupsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsAddressGroupsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AddressGroup
// =============================================================================

/// ResourceIdentifier implementation for AddressGroup with NetworksecurityOrganizationsLocationsAddressGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsAddressGroupsGetArgs>
    for AddressGroup
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsAddressGroupsGetArgs,
    ) -> String {
        format!("gcp::networksecurity::AddressGroup/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::AddressGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAddressGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAddressGroupsResponse with NetworksecurityOrganizationsLocationsAddressGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsAddressGroupsListArgs>
    for ListAddressGroupsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsAddressGroupsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListAddressGroupsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListAddressGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAddressGroupReferencesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAddressGroupReferencesResponse with NetworksecurityOrganizationsLocationsAddressGroupsListReferencesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsAddressGroupsListReferencesArgs>
    for ListAddressGroupReferencesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsAddressGroupsListReferencesArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListAddressGroupReferencesResponse/{}",
            input.addressGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListAddressGroupReferencesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsAddressGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsAddressGroupsPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsAddressGroupsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsAddressGroupsRemoveItemsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsAddressGroupsRemoveItemsArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsAddressGroupsRemoveItemsArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.addressGroup)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsFirewallEndpointsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsFirewallEndpointsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsFirewallEndpointsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsFirewallEndpointsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsFirewallEndpointsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsFirewallEndpointsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallEndpoint
// =============================================================================

/// ResourceIdentifier implementation for FirewallEndpoint with NetworksecurityOrganizationsLocationsFirewallEndpointsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsFirewallEndpointsGetArgs>
    for FirewallEndpoint
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsFirewallEndpointsGetArgs,
    ) -> String {
        format!("gcp::networksecurity::FirewallEndpoint/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::FirewallEndpoint"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListFirewallEndpointsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListFirewallEndpointsResponse with NetworksecurityOrganizationsLocationsFirewallEndpointsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsFirewallEndpointsListArgs>
    for ListFirewallEndpointsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsFirewallEndpointsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListFirewallEndpointsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListFirewallEndpointsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsFirewallEndpointsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsFirewallEndpointsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsFirewallEndpointsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with NetworksecurityOrganizationsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::networksecurity::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with NetworksecurityOrganizationsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsOperationsGetArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with NetworksecurityOrganizationsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsOperationsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsSecurityProfileGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsSecurityProfileGroupsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsSecurityProfileGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsSecurityProfileGroupsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityProfileGroup
// =============================================================================

/// ResourceIdentifier implementation for SecurityProfileGroup with NetworksecurityOrganizationsLocationsSecurityProfileGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsSecurityProfileGroupsGetArgs>
    for SecurityProfileGroup
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsGetArgs,
    ) -> String {
        format!("gcp::networksecurity::SecurityProfileGroup/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::SecurityProfileGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSecurityProfileGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSecurityProfileGroupsResponse with NetworksecurityOrganizationsLocationsSecurityProfileGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsSecurityProfileGroupsListArgs>
    for ListSecurityProfileGroupsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListSecurityProfileGroupsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListSecurityProfileGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsSecurityProfileGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsSecurityProfileGroupsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsSecurityProfilesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsSecurityProfilesCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsSecurityProfilesCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsSecurityProfilesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsSecurityProfilesDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsSecurityProfilesDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SecurityProfile
// =============================================================================

/// ResourceIdentifier implementation for SecurityProfile with NetworksecurityOrganizationsLocationsSecurityProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsSecurityProfilesGetArgs>
    for SecurityProfile
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsSecurityProfilesGetArgs,
    ) -> String {
        format!("gcp::networksecurity::SecurityProfile/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::SecurityProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSecurityProfilesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSecurityProfilesResponse with NetworksecurityOrganizationsLocationsSecurityProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsSecurityProfilesListArgs>
    for ListSecurityProfilesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsSecurityProfilesListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListSecurityProfilesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListSecurityProfilesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityOrganizationsLocationsSecurityProfilesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityOrganizationsLocationsSecurityProfilesPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityOrganizationsLocationsSecurityProfilesPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with NetworksecurityProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &NetworksecurityProjectsLocationsGetArgs) -> String {
        format!("gcp::networksecurity::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with NetworksecurityProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &NetworksecurityProjectsLocationsListArgs) -> String {
        format!("gcp::networksecurity::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsAddressGroupsAddItemsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAddressGroupsAddItemsArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAddressGroupsAddItemsArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.addressGroup)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsAddressGroupsCloneItemsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAddressGroupsCloneItemsArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAddressGroupsCloneItemsArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.addressGroup)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsAddressGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAddressGroupsCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAddressGroupsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsAddressGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAddressGroupsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAddressGroupsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AddressGroup
// =============================================================================

/// ResourceIdentifier implementation for AddressGroup with NetworksecurityProjectsLocationsAddressGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAddressGroupsGetArgs> for AddressGroup {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAddressGroupsGetArgs,
    ) -> String {
        format!("gcp::networksecurity::AddressGroup/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::AddressGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1Policy
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1Policy with NetworksecurityProjectsLocationsAddressGroupsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAddressGroupsGetIamPolicyArgs>
    for GoogleIamV1Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAddressGroupsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::networksecurity::GoogleIamV1Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAddressGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAddressGroupsResponse with NetworksecurityProjectsLocationsAddressGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAddressGroupsListArgs>
    for ListAddressGroupsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAddressGroupsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListAddressGroupsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListAddressGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAddressGroupReferencesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAddressGroupReferencesResponse with NetworksecurityProjectsLocationsAddressGroupsListReferencesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAddressGroupsListReferencesArgs>
    for ListAddressGroupReferencesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAddressGroupsListReferencesArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListAddressGroupReferencesResponse/{}",
            input.addressGroup
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListAddressGroupReferencesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsAddressGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAddressGroupsPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAddressGroupsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsAddressGroupsRemoveItemsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAddressGroupsRemoveItemsArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAddressGroupsRemoveItemsArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.addressGroup)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1Policy
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1Policy with NetworksecurityProjectsLocationsAddressGroupsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAddressGroupsSetIamPolicyArgs>
    for GoogleIamV1Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAddressGroupsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::networksecurity::GoogleIamV1Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1TestIamPermissionsResponse with NetworksecurityProjectsLocationsAddressGroupsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAddressGroupsTestIamPermissionsArgs>
    for GoogleIamV1TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAddressGroupsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::GoogleIamV1TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsAuthorizationPoliciesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthorizationPoliciesCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthorizationPoliciesCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsAuthorizationPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthorizationPoliciesDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthorizationPoliciesDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AuthorizationPolicy
// =============================================================================

/// ResourceIdentifier implementation for AuthorizationPolicy with NetworksecurityProjectsLocationsAuthorizationPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthorizationPoliciesGetArgs>
    for AuthorizationPolicy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthorizationPoliciesGetArgs,
    ) -> String {
        format!("gcp::networksecurity::AuthorizationPolicy/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::AuthorizationPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1Policy
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1Policy with NetworksecurityProjectsLocationsAuthorizationPoliciesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthorizationPoliciesGetIamPolicyArgs>
    for GoogleIamV1Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthorizationPoliciesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::networksecurity::GoogleIamV1Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAuthorizationPoliciesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAuthorizationPoliciesResponse with NetworksecurityProjectsLocationsAuthorizationPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthorizationPoliciesListArgs>
    for ListAuthorizationPoliciesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthorizationPoliciesListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListAuthorizationPoliciesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListAuthorizationPoliciesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsAuthorizationPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthorizationPoliciesPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthorizationPoliciesPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1Policy
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1Policy with NetworksecurityProjectsLocationsAuthorizationPoliciesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthorizationPoliciesSetIamPolicyArgs>
    for GoogleIamV1Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthorizationPoliciesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::networksecurity::GoogleIamV1Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1TestIamPermissionsResponse with NetworksecurityProjectsLocationsAuthorizationPoliciesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthorizationPoliciesTestIamPermissionsArgs>
    for GoogleIamV1TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthorizationPoliciesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::GoogleIamV1TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsAuthzPoliciesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthzPoliciesCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthzPoliciesCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsAuthzPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthzPoliciesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthzPoliciesDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AuthzPolicy
// =============================================================================

/// ResourceIdentifier implementation for AuthzPolicy with NetworksecurityProjectsLocationsAuthzPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthzPoliciesGetArgs> for AuthzPolicy {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthzPoliciesGetArgs,
    ) -> String {
        format!("gcp::networksecurity::AuthzPolicy/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::AuthzPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1Policy
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1Policy with NetworksecurityProjectsLocationsAuthzPoliciesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthzPoliciesGetIamPolicyArgs>
    for GoogleIamV1Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthzPoliciesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::networksecurity::GoogleIamV1Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAuthzPoliciesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAuthzPoliciesResponse with NetworksecurityProjectsLocationsAuthzPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthzPoliciesListArgs>
    for ListAuthzPoliciesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthzPoliciesListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListAuthzPoliciesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListAuthzPoliciesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsAuthzPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthzPoliciesPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthzPoliciesPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1Policy
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1Policy with NetworksecurityProjectsLocationsAuthzPoliciesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthzPoliciesSetIamPolicyArgs>
    for GoogleIamV1Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthzPoliciesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::networksecurity::GoogleIamV1Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1TestIamPermissionsResponse with NetworksecurityProjectsLocationsAuthzPoliciesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsAuthzPoliciesTestIamPermissionsArgs>
    for GoogleIamV1TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsAuthzPoliciesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::GoogleIamV1TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsBackendAuthenticationConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsBackendAuthenticationConfigsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsBackendAuthenticationConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsBackendAuthenticationConfigsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackendAuthenticationConfig
// =============================================================================

/// ResourceIdentifier implementation for BackendAuthenticationConfig with NetworksecurityProjectsLocationsBackendAuthenticationConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsBackendAuthenticationConfigsGetArgs>
    for BackendAuthenticationConfig
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsGetArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::BackendAuthenticationConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::BackendAuthenticationConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBackendAuthenticationConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBackendAuthenticationConfigsResponse with NetworksecurityProjectsLocationsBackendAuthenticationConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsBackendAuthenticationConfigsListArgs>
    for ListBackendAuthenticationConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListBackendAuthenticationConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListBackendAuthenticationConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsBackendAuthenticationConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsBackendAuthenticationConfigsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsClientTlsPoliciesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsClientTlsPoliciesCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsClientTlsPoliciesCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsClientTlsPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsClientTlsPoliciesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsClientTlsPoliciesDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ClientTlsPolicy
// =============================================================================

/// ResourceIdentifier implementation for ClientTlsPolicy with NetworksecurityProjectsLocationsClientTlsPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsClientTlsPoliciesGetArgs>
    for ClientTlsPolicy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsClientTlsPoliciesGetArgs,
    ) -> String {
        format!("gcp::networksecurity::ClientTlsPolicy/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ClientTlsPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1Policy
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1Policy with NetworksecurityProjectsLocationsClientTlsPoliciesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsClientTlsPoliciesGetIamPolicyArgs>
    for GoogleIamV1Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsClientTlsPoliciesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::networksecurity::GoogleIamV1Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListClientTlsPoliciesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListClientTlsPoliciesResponse with NetworksecurityProjectsLocationsClientTlsPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsClientTlsPoliciesListArgs>
    for ListClientTlsPoliciesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsClientTlsPoliciesListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListClientTlsPoliciesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListClientTlsPoliciesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsClientTlsPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsClientTlsPoliciesPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsClientTlsPoliciesPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1Policy
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1Policy with NetworksecurityProjectsLocationsClientTlsPoliciesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsClientTlsPoliciesSetIamPolicyArgs>
    for GoogleIamV1Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsClientTlsPoliciesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::networksecurity::GoogleIamV1Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1TestIamPermissionsResponse with NetworksecurityProjectsLocationsClientTlsPoliciesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsClientTlsPoliciesTestIamPermissionsArgs>
    for GoogleIamV1TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsClientTlsPoliciesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::GoogleIamV1TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DnsThreatDetector
// =============================================================================

/// ResourceIdentifier implementation for DnsThreatDetector with NetworksecurityProjectsLocationsDnsThreatDetectorsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsDnsThreatDetectorsCreateArgs>
    for DnsThreatDetector
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsDnsThreatDetectorsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::DnsThreatDetector/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::DnsThreatDetector"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with NetworksecurityProjectsLocationsDnsThreatDetectorsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsDnsThreatDetectorsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsDnsThreatDetectorsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DnsThreatDetector
// =============================================================================

/// ResourceIdentifier implementation for DnsThreatDetector with NetworksecurityProjectsLocationsDnsThreatDetectorsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsDnsThreatDetectorsGetArgs>
    for DnsThreatDetector
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsDnsThreatDetectorsGetArgs,
    ) -> String {
        format!("gcp::networksecurity::DnsThreatDetector/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::DnsThreatDetector"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDnsThreatDetectorsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDnsThreatDetectorsResponse with NetworksecurityProjectsLocationsDnsThreatDetectorsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsDnsThreatDetectorsListArgs>
    for ListDnsThreatDetectorsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsDnsThreatDetectorsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListDnsThreatDetectorsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListDnsThreatDetectorsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DnsThreatDetector
// =============================================================================

/// ResourceIdentifier implementation for DnsThreatDetector with NetworksecurityProjectsLocationsDnsThreatDetectorsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsDnsThreatDetectorsPatchArgs>
    for DnsThreatDetector
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsDnsThreatDetectorsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::DnsThreatDetector/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::DnsThreatDetector"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsFirewallEndpointAssociationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsFirewallEndpointAssociationsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsFirewallEndpointAssociationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsFirewallEndpointAssociationsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirewallEndpointAssociation
// =============================================================================

/// ResourceIdentifier implementation for FirewallEndpointAssociation with NetworksecurityProjectsLocationsFirewallEndpointAssociationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsFirewallEndpointAssociationsGetArgs>
    for FirewallEndpointAssociation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsGetArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::FirewallEndpointAssociation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::FirewallEndpointAssociation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListFirewallEndpointAssociationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListFirewallEndpointAssociationsResponse with NetworksecurityProjectsLocationsFirewallEndpointAssociationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsFirewallEndpointAssociationsListArgs>
    for ListFirewallEndpointAssociationsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListFirewallEndpointAssociationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListFirewallEndpointAssociationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsFirewallEndpointAssociationsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsFirewallEndpointAssociationsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsGatewaySecurityPoliciesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsGatewaySecurityPoliciesCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsGatewaySecurityPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsGatewaySecurityPoliciesDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GatewaySecurityPolicy
// =============================================================================

/// ResourceIdentifier implementation for GatewaySecurityPolicy with NetworksecurityProjectsLocationsGatewaySecurityPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsGatewaySecurityPoliciesGetArgs>
    for GatewaySecurityPolicy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesGetArgs,
    ) -> String {
        format!("gcp::networksecurity::GatewaySecurityPolicy/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GatewaySecurityPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListGatewaySecurityPoliciesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListGatewaySecurityPoliciesResponse with NetworksecurityProjectsLocationsGatewaySecurityPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsGatewaySecurityPoliciesListArgs>
    for ListGatewaySecurityPoliciesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListGatewaySecurityPoliciesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListGatewaySecurityPoliciesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsGatewaySecurityPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsGatewaySecurityPoliciesPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GatewaySecurityPolicyRule
// =============================================================================

/// ResourceIdentifier implementation for GatewaySecurityPolicyRule with NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesGetArgs>
    for GatewaySecurityPolicyRule
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesGetArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::GatewaySecurityPolicyRule/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GatewaySecurityPolicyRule"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListGatewaySecurityPolicyRulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListGatewaySecurityPolicyRulesResponse with NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesListArgs>
    for ListGatewaySecurityPolicyRulesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListGatewaySecurityPolicyRulesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListGatewaySecurityPolicyRulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsInterceptDeploymentGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptDeploymentGroupsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsInterceptDeploymentGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptDeploymentGroupsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterceptDeploymentGroup
// =============================================================================

/// ResourceIdentifier implementation for InterceptDeploymentGroup with NetworksecurityProjectsLocationsInterceptDeploymentGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptDeploymentGroupsGetArgs>
    for InterceptDeploymentGroup
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsGetArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::InterceptDeploymentGroup/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::InterceptDeploymentGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListInterceptDeploymentGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListInterceptDeploymentGroupsResponse with NetworksecurityProjectsLocationsInterceptDeploymentGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptDeploymentGroupsListArgs>
    for ListInterceptDeploymentGroupsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListInterceptDeploymentGroupsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListInterceptDeploymentGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsInterceptDeploymentGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptDeploymentGroupsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsInterceptDeploymentsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptDeploymentsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptDeploymentsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsInterceptDeploymentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptDeploymentsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptDeploymentsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterceptDeployment
// =============================================================================

/// ResourceIdentifier implementation for InterceptDeployment with NetworksecurityProjectsLocationsInterceptDeploymentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptDeploymentsGetArgs>
    for InterceptDeployment
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptDeploymentsGetArgs,
    ) -> String {
        format!("gcp::networksecurity::InterceptDeployment/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::InterceptDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListInterceptDeploymentsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListInterceptDeploymentsResponse with NetworksecurityProjectsLocationsInterceptDeploymentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptDeploymentsListArgs>
    for ListInterceptDeploymentsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptDeploymentsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListInterceptDeploymentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListInterceptDeploymentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsInterceptDeploymentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptDeploymentsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptDeploymentsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterceptEndpointGroupAssociation
// =============================================================================

/// ResourceIdentifier implementation for InterceptEndpointGroupAssociation with NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsGetArgs>
    for InterceptEndpointGroupAssociation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsGetArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::InterceptEndpointGroupAssociation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::InterceptEndpointGroupAssociation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListInterceptEndpointGroupAssociationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListInterceptEndpointGroupAssociationsResponse with NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsListArgs>
    for ListInterceptEndpointGroupAssociationsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListInterceptEndpointGroupAssociationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListInterceptEndpointGroupAssociationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsInterceptEndpointGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptEndpointGroupsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptEndpointGroupsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsInterceptEndpointGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptEndpointGroupsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptEndpointGroupsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InterceptEndpointGroup
// =============================================================================

/// ResourceIdentifier implementation for InterceptEndpointGroup with NetworksecurityProjectsLocationsInterceptEndpointGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptEndpointGroupsGetArgs>
    for InterceptEndpointGroup
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptEndpointGroupsGetArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::InterceptEndpointGroup/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::InterceptEndpointGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListInterceptEndpointGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListInterceptEndpointGroupsResponse with NetworksecurityProjectsLocationsInterceptEndpointGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptEndpointGroupsListArgs>
    for ListInterceptEndpointGroupsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptEndpointGroupsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListInterceptEndpointGroupsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListInterceptEndpointGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsInterceptEndpointGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsInterceptEndpointGroupsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsInterceptEndpointGroupsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsMirroringDeploymentGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringDeploymentGroupsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsMirroringDeploymentGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringDeploymentGroupsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MirroringDeploymentGroup
// =============================================================================

/// ResourceIdentifier implementation for MirroringDeploymentGroup with NetworksecurityProjectsLocationsMirroringDeploymentGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringDeploymentGroupsGetArgs>
    for MirroringDeploymentGroup
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsGetArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::MirroringDeploymentGroup/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::MirroringDeploymentGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMirroringDeploymentGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMirroringDeploymentGroupsResponse with NetworksecurityProjectsLocationsMirroringDeploymentGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringDeploymentGroupsListArgs>
    for ListMirroringDeploymentGroupsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListMirroringDeploymentGroupsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListMirroringDeploymentGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsMirroringDeploymentGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringDeploymentGroupsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsMirroringDeploymentsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringDeploymentsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringDeploymentsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsMirroringDeploymentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringDeploymentsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringDeploymentsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MirroringDeployment
// =============================================================================

/// ResourceIdentifier implementation for MirroringDeployment with NetworksecurityProjectsLocationsMirroringDeploymentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringDeploymentsGetArgs>
    for MirroringDeployment
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringDeploymentsGetArgs,
    ) -> String {
        format!("gcp::networksecurity::MirroringDeployment/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::MirroringDeployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMirroringDeploymentsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMirroringDeploymentsResponse with NetworksecurityProjectsLocationsMirroringDeploymentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringDeploymentsListArgs>
    for ListMirroringDeploymentsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringDeploymentsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListMirroringDeploymentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListMirroringDeploymentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsMirroringDeploymentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringDeploymentsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringDeploymentsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MirroringEndpointGroupAssociation
// =============================================================================

/// ResourceIdentifier implementation for MirroringEndpointGroupAssociation with NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsGetArgs>
    for MirroringEndpointGroupAssociation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsGetArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::MirroringEndpointGroupAssociation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::MirroringEndpointGroupAssociation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMirroringEndpointGroupAssociationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMirroringEndpointGroupAssociationsResponse with NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsListArgs>
    for ListMirroringEndpointGroupAssociationsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListMirroringEndpointGroupAssociationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListMirroringEndpointGroupAssociationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsMirroringEndpointGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringEndpointGroupsCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringEndpointGroupsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsMirroringEndpointGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringEndpointGroupsDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringEndpointGroupsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MirroringEndpointGroup
// =============================================================================

/// ResourceIdentifier implementation for MirroringEndpointGroup with NetworksecurityProjectsLocationsMirroringEndpointGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringEndpointGroupsGetArgs>
    for MirroringEndpointGroup
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringEndpointGroupsGetArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::MirroringEndpointGroup/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::MirroringEndpointGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMirroringEndpointGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMirroringEndpointGroupsResponse with NetworksecurityProjectsLocationsMirroringEndpointGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringEndpointGroupsListArgs>
    for ListMirroringEndpointGroupsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringEndpointGroupsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListMirroringEndpointGroupsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListMirroringEndpointGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsMirroringEndpointGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsMirroringEndpointGroupsPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsMirroringEndpointGroupsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with NetworksecurityProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::networksecurity::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with NetworksecurityProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsOperationsGetArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with NetworksecurityProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsOperationsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsServerTlsPoliciesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsServerTlsPoliciesCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsServerTlsPoliciesCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsServerTlsPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsServerTlsPoliciesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsServerTlsPoliciesDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServerTlsPolicy
// =============================================================================

/// ResourceIdentifier implementation for ServerTlsPolicy with NetworksecurityProjectsLocationsServerTlsPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsServerTlsPoliciesGetArgs>
    for ServerTlsPolicy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsServerTlsPoliciesGetArgs,
    ) -> String {
        format!("gcp::networksecurity::ServerTlsPolicy/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ServerTlsPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1Policy
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1Policy with NetworksecurityProjectsLocationsServerTlsPoliciesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsServerTlsPoliciesGetIamPolicyArgs>
    for GoogleIamV1Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsServerTlsPoliciesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::networksecurity::GoogleIamV1Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListServerTlsPoliciesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListServerTlsPoliciesResponse with NetworksecurityProjectsLocationsServerTlsPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsServerTlsPoliciesListArgs>
    for ListServerTlsPoliciesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsServerTlsPoliciesListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListServerTlsPoliciesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListServerTlsPoliciesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsServerTlsPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsServerTlsPoliciesPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsServerTlsPoliciesPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1Policy
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1Policy with NetworksecurityProjectsLocationsServerTlsPoliciesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsServerTlsPoliciesSetIamPolicyArgs>
    for GoogleIamV1Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsServerTlsPoliciesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::networksecurity::GoogleIamV1Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleIamV1TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleIamV1TestIamPermissionsResponse with NetworksecurityProjectsLocationsServerTlsPoliciesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsServerTlsPoliciesTestIamPermissionsArgs>
    for GoogleIamV1TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsServerTlsPoliciesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::GoogleIamV1TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::GoogleIamV1TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsTlsInspectionPoliciesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsTlsInspectionPoliciesCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsTlsInspectionPoliciesCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsTlsInspectionPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsTlsInspectionPoliciesDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsTlsInspectionPoliciesDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TlsInspectionPolicy
// =============================================================================

/// ResourceIdentifier implementation for TlsInspectionPolicy with NetworksecurityProjectsLocationsTlsInspectionPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsTlsInspectionPoliciesGetArgs>
    for TlsInspectionPolicy
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsTlsInspectionPoliciesGetArgs,
    ) -> String {
        format!("gcp::networksecurity::TlsInspectionPolicy/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::TlsInspectionPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListTlsInspectionPoliciesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListTlsInspectionPoliciesResponse with NetworksecurityProjectsLocationsTlsInspectionPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsTlsInspectionPoliciesListArgs>
    for ListTlsInspectionPoliciesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsTlsInspectionPoliciesListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListTlsInspectionPoliciesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListTlsInspectionPoliciesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsTlsInspectionPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsTlsInspectionPoliciesPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsTlsInspectionPoliciesPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsUrlListsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsUrlListsCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsUrlListsCreateArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsUrlListsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsUrlListsDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsUrlListsDeleteArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UrlList
// =============================================================================

/// ResourceIdentifier implementation for UrlList with NetworksecurityProjectsLocationsUrlListsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsUrlListsGetArgs> for UrlList {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsUrlListsGetArgs,
    ) -> String {
        format!("gcp::networksecurity::UrlList/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::UrlList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListUrlListsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListUrlListsResponse with NetworksecurityProjectsLocationsUrlListsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsUrlListsListArgs> for ListUrlListsResponse {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsUrlListsListArgs,
    ) -> String {
        format!(
            "gcp::networksecurity::ListUrlListsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::ListUrlListsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with NetworksecurityProjectsLocationsUrlListsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworksecurityProjectsLocationsUrlListsPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &NetworksecurityProjectsLocationsUrlListsPatchArgs,
    ) -> String {
        format!("gcp::networksecurity::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networksecurity::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
