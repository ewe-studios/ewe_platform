//! Auto-generated API clients for gcp/vision.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/files:annotate
/// Service that performs image detection and annotation for a batch of files. Now only "`application/pdf`", "`image/tiff`" and "`image/gif`" are supported. This service will extract at most 5 (customers can specify which 5 in AnnotateFileRequest.pages) frames (gif) or pages (pdf or tiff) from each file provided and perform detection and annotation for each image extracted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vision_files_annotate_execute()` to send, or `vision_files_annotate` for simplest API.

pub fn vision_files_annotate_builder(
    client: &SimpleHttpClient,
    body: &BatchAnnotateFilesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://vision.googleapis.com/v1/files:annotate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/files:annotate
/// Service that performs image detection and annotation for a batch of files. Now only "`application/pdf`", "`image/tiff`" and "`image/gif`" are supported. This service will extract at most 5 (customers can specify which 5 in AnnotateFileRequest.pages) frames (gif) or pages (pdf or tiff) from each file provided and perform detection and annotation for each image extracted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vision_files_annotate_execute()` or `vision_files_annotate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_files_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_files_annotate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchAnnotateFilesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchAnnotateFilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/files:annotate
/// Service that performs image detection and annotation for a batch of files. Now only "`application/pdf`", "`image/tiff`" and "`image/gif`" are supported. This service will extract at most 5 (customers can specify which 5 in AnnotateFileRequest.pages) frames (gif) or pages (pdf or tiff) from each file provided and perform detection and annotation for each image extracted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vision_files_annotate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vision_files_annotate_task()`.
/// For the simplest API, use `vision_files_annotate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_files_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vision_files_annotate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchAnnotateFilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = vision_files_annotate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vision_files_annotate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VisionFilesAnnotateArgs {
    /// Request body.
    pub body: BatchAnnotateFilesRequest,
}

/// GET v1/files:annotate
/// Service that performs image detection and annotation for a batch of files. Now only "`application/pdf`", "`image/tiff`" and "`image/gif`" are supported. This service will extract at most 5 (customers can specify which 5 in AnnotateFileRequest.pages) frames (gif) or pages (pdf or tiff) from each file provided and perform detection and annotation for each image extracted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vision_files_annotate_builder()` + `vision_files_annotate_execute()`.
/// For task-level control, use `vision_files_annotate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_files_annotate(
    client: &SimpleHttpClient,
    args: &VisionFilesAnnotateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchAnnotateFilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vision_files_annotate_builder(client, &args.body)?;
    vision_files_annotate_execute(builder)
}

/// GET v1/files:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of generic files, such as PDF files, which may contain multiple pages and multiple images per page. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateFilesResponse (results).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vision_files_async_batch_annotate_execute()` to send, or `vision_files_async_batch_annotate` for simplest API.

pub fn vision_files_async_batch_annotate_builder(
    client: &SimpleHttpClient,
    body: &AsyncBatchAnnotateFilesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://vision.googleapis.com/v1/files:asyncBatchAnnotate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/files:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of generic files, such as PDF files, which may contain multiple pages and multiple images per page. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateFilesResponse (results).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vision_files_async_batch_annotate_execute()` or `vision_files_async_batch_annotate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_files_async_batch_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_files_async_batch_annotate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/files:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of generic files, such as PDF files, which may contain multiple pages and multiple images per page. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateFilesResponse (results).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vision_files_async_batch_annotate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vision_files_async_batch_annotate_task()`.
/// For the simplest API, use `vision_files_async_batch_annotate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_files_async_batch_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vision_files_async_batch_annotate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vision_files_async_batch_annotate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vision_files_async_batch_annotate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VisionFilesAsyncBatchAnnotateArgs {
    /// Request body.
    pub body: AsyncBatchAnnotateFilesRequest,
}

/// GET v1/files:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of generic files, such as PDF files, which may contain multiple pages and multiple images per page. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateFilesResponse (results).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vision_files_async_batch_annotate_builder()` + `vision_files_async_batch_annotate_execute()`.
/// For task-level control, use `vision_files_async_batch_annotate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_files_async_batch_annotate(
    client: &SimpleHttpClient,
    args: &VisionFilesAsyncBatchAnnotateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vision_files_async_batch_annotate_builder(client, &args.body)?;
    vision_files_async_batch_annotate_execute(builder)
}

/// GET v1/images:annotate
/// Run image detection and annotation for a batch of images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vision_images_annotate_execute()` to send, or `vision_images_annotate` for simplest API.

pub fn vision_images_annotate_builder(
    client: &SimpleHttpClient,
    body: &BatchAnnotateImagesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://vision.googleapis.com/v1/images:annotate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/images:annotate
/// Run image detection and annotation for a batch of images.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vision_images_annotate_execute()` or `vision_images_annotate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_images_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_images_annotate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchAnnotateImagesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchAnnotateImagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/images:annotate
/// Run image detection and annotation for a batch of images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vision_images_annotate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vision_images_annotate_task()`.
/// For the simplest API, use `vision_images_annotate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_images_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vision_images_annotate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchAnnotateImagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = vision_images_annotate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vision_images_annotate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VisionImagesAnnotateArgs {
    /// Request body.
    pub body: BatchAnnotateImagesRequest,
}

/// GET v1/images:annotate
/// Run image detection and annotation for a batch of images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vision_images_annotate_builder()` + `vision_images_annotate_execute()`.
/// For task-level control, use `vision_images_annotate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_images_annotate(
    client: &SimpleHttpClient,
    args: &VisionImagesAnnotateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchAnnotateImagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vision_images_annotate_builder(client, &args.body)?;
    vision_images_annotate_execute(builder)
}

/// GET v1/images:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of images. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateImagesResponse (results). This service will write image annotation outputs to json files in customer GCS bucket, each json file containing BatchAnnotateImagesResponse proto.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vision_images_async_batch_annotate_execute()` to send, or `vision_images_async_batch_annotate` for simplest API.

pub fn vision_images_async_batch_annotate_builder(
    client: &SimpleHttpClient,
    body: &AsyncBatchAnnotateImagesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://vision.googleapis.com/v1/images:asyncBatchAnnotate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/images:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of images. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateImagesResponse (results). This service will write image annotation outputs to json files in customer GCS bucket, each json file containing BatchAnnotateImagesResponse proto.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vision_images_async_batch_annotate_execute()` or `vision_images_async_batch_annotate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_images_async_batch_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_images_async_batch_annotate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/images:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of images. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateImagesResponse (results). This service will write image annotation outputs to json files in customer GCS bucket, each json file containing BatchAnnotateImagesResponse proto.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vision_images_async_batch_annotate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vision_images_async_batch_annotate_task()`.
/// For the simplest API, use `vision_images_async_batch_annotate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_images_async_batch_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vision_images_async_batch_annotate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vision_images_async_batch_annotate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vision_images_async_batch_annotate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VisionImagesAsyncBatchAnnotateArgs {
    /// Request body.
    pub body: AsyncBatchAnnotateImagesRequest,
}

/// GET v1/images:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of images. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateImagesResponse (results). This service will write image annotation outputs to json files in customer GCS bucket, each json file containing BatchAnnotateImagesResponse proto.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vision_images_async_batch_annotate_builder()` + `vision_images_async_batch_annotate_execute()`.
/// For task-level control, use `vision_images_async_batch_annotate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_images_async_batch_annotate(
    client: &SimpleHttpClient,
    args: &VisionImagesAsyncBatchAnnotateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vision_images_async_batch_annotate_builder(client, &args.body)?;
    vision_images_async_batch_annotate_execute(builder)
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vision_operations_cancel_execute()` to send, or `vision_operations_cancel` for simplest API.

pub fn vision_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: String,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://vision.googleapis.com/v1/operations/{}:cancel",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vision_operations_cancel_execute()` or `vision_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vision_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vision_operations_cancel_task()`.
/// For the simplest API, use `vision_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vision_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vision_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vision_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VisionOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: CancelOperationRequest,
}

/// GET v1/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vision_operations_cancel_builder()` + `vision_operations_cancel_execute()`.
/// For task-level control, use `vision_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_operations_cancel(
    client: &SimpleHttpClient,
    args: &VisionOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vision_operations_cancel_builder(client, args.name.clone(), &args.body)?;
    vision_operations_cancel_execute(builder)
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vision_operations_delete_execute()` to send, or `vision_operations_delete` for simplest API.

pub fn vision_operations_delete_builder(
    client: &SimpleHttpClient,
    name: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://vision.googleapis.com/v1/operations/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vision_operations_delete_execute()` or `vision_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vision_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vision_operations_delete_task()`.
/// For the simplest API, use `vision_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vision_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vision_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vision_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VisionOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vision_operations_delete_builder()` + `vision_operations_delete_execute()`.
/// For task-level control, use `vision_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_operations_delete(
    client: &SimpleHttpClient,
    args: &VisionOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vision_operations_delete_builder(client, args.name.clone())?;
    vision_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/files:annotate
/// Service that performs image detection and annotation for a batch of files. Now only "`application/pdf`", "`image/tiff`" and "`image/gif`" are supported. This service will extract at most 5 (customers can specify which 5 in AnnotateFileRequest.pages) frames (gif) or pages (pdf or tiff) from each file provided and perform detection and annotation for each image extracted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vision_projects_files_annotate_execute()` to send, or `vision_projects_files_annotate` for simplest API.

pub fn vision_projects_files_annotate_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &BatchAnnotateFilesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://vision.googleapis.com/v1/projects/{}/files:annotate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/files:annotate
/// Service that performs image detection and annotation for a batch of files. Now only "`application/pdf`", "`image/tiff`" and "`image/gif`" are supported. This service will extract at most 5 (customers can specify which 5 in AnnotateFileRequest.pages) frames (gif) or pages (pdf or tiff) from each file provided and perform detection and annotation for each image extracted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vision_projects_files_annotate_execute()` or `vision_projects_files_annotate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_projects_files_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_projects_files_annotate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchAnnotateFilesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchAnnotateFilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/files:annotate
/// Service that performs image detection and annotation for a batch of files. Now only "`application/pdf`", "`image/tiff`" and "`image/gif`" are supported. This service will extract at most 5 (customers can specify which 5 in AnnotateFileRequest.pages) frames (gif) or pages (pdf or tiff) from each file provided and perform detection and annotation for each image extracted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vision_projects_files_annotate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vision_projects_files_annotate_task()`.
/// For the simplest API, use `vision_projects_files_annotate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_projects_files_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vision_projects_files_annotate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchAnnotateFilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = vision_projects_files_annotate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vision_projects_files_annotate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VisionProjectsFilesAnnotateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: BatchAnnotateFilesRequest,
}

/// GET v1/projects/{projectsId}/files:annotate
/// Service that performs image detection and annotation for a batch of files. Now only "`application/pdf`", "`image/tiff`" and "`image/gif`" are supported. This service will extract at most 5 (customers can specify which 5 in AnnotateFileRequest.pages) frames (gif) or pages (pdf or tiff) from each file provided and perform detection and annotation for each image extracted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vision_projects_files_annotate_builder()` + `vision_projects_files_annotate_execute()`.
/// For task-level control, use `vision_projects_files_annotate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_projects_files_annotate(
    client: &SimpleHttpClient,
    args: &VisionProjectsFilesAnnotateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchAnnotateFilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vision_projects_files_annotate_builder(client, args.parent.clone(), &args.body)?;
    vision_projects_files_annotate_execute(builder)
}

/// GET v1/projects/{projectsId}/files:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of generic files, such as PDF files, which may contain multiple pages and multiple images per page. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateFilesResponse (results).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vision_projects_files_async_batch_annotate_execute()` to send, or `vision_projects_files_async_batch_annotate` for simplest API.

pub fn vision_projects_files_async_batch_annotate_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &AsyncBatchAnnotateFilesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://vision.googleapis.com/v1/projects/{}/files:asyncBatchAnnotate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/files:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of generic files, such as PDF files, which may contain multiple pages and multiple images per page. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateFilesResponse (results).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vision_projects_files_async_batch_annotate_execute()` or `vision_projects_files_async_batch_annotate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_projects_files_async_batch_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_projects_files_async_batch_annotate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/files:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of generic files, such as PDF files, which may contain multiple pages and multiple images per page. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateFilesResponse (results).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vision_projects_files_async_batch_annotate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vision_projects_files_async_batch_annotate_task()`.
/// For the simplest API, use `vision_projects_files_async_batch_annotate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_projects_files_async_batch_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vision_projects_files_async_batch_annotate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vision_projects_files_async_batch_annotate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vision_projects_files_async_batch_annotate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VisionProjectsFilesAsyncBatchAnnotateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: AsyncBatchAnnotateFilesRequest,
}

/// GET v1/projects/{projectsId}/files:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of generic files, such as PDF files, which may contain multiple pages and multiple images per page. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateFilesResponse (results).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vision_projects_files_async_batch_annotate_builder()` + `vision_projects_files_async_batch_annotate_execute()`.
/// For task-level control, use `vision_projects_files_async_batch_annotate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_projects_files_async_batch_annotate(
    client: &SimpleHttpClient,
    args: &VisionProjectsFilesAsyncBatchAnnotateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vision_projects_files_async_batch_annotate_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    vision_projects_files_async_batch_annotate_execute(builder)
}

/// GET v1/projects/{projectsId}/images:annotate
/// Run image detection and annotation for a batch of images.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vision_projects_images_annotate_execute()` to send, or `vision_projects_images_annotate` for simplest API.

pub fn vision_projects_images_annotate_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &BatchAnnotateImagesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://vision.googleapis.com/v1/projects/{}/images:annotate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/images:annotate
/// Run image detection and annotation for a batch of images.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vision_projects_images_annotate_execute()` or `vision_projects_images_annotate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_projects_images_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_projects_images_annotate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchAnnotateImagesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchAnnotateImagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/images:annotate
/// Run image detection and annotation for a batch of images.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vision_projects_images_annotate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vision_projects_images_annotate_task()`.
/// For the simplest API, use `vision_projects_images_annotate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_projects_images_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vision_projects_images_annotate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchAnnotateImagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = vision_projects_images_annotate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vision_projects_images_annotate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VisionProjectsImagesAnnotateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: BatchAnnotateImagesRequest,
}

/// GET v1/projects/{projectsId}/images:annotate
/// Run image detection and annotation for a batch of images.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vision_projects_images_annotate_builder()` + `vision_projects_images_annotate_execute()`.
/// For task-level control, use `vision_projects_images_annotate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_projects_images_annotate(
    client: &SimpleHttpClient,
    args: &VisionProjectsImagesAnnotateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchAnnotateImagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = vision_projects_images_annotate_builder(client, args.parent.clone(), &args.body)?;
    vision_projects_images_annotate_execute(builder)
}

/// GET v1/projects/{projectsId}/images:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of images. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateImagesResponse (results). This service will write image annotation outputs to json files in customer GCS bucket, each json file containing BatchAnnotateImagesResponse proto.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `vision_projects_images_async_batch_annotate_execute()` to send, or `vision_projects_images_async_batch_annotate` for simplest API.

pub fn vision_projects_images_async_batch_annotate_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &AsyncBatchAnnotateImagesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://vision.googleapis.com/v1/projects/{}/images:asyncBatchAnnotate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/images:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of images. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateImagesResponse (results). This service will write image annotation outputs to json files in customer GCS bucket, each json file containing BatchAnnotateImagesResponse proto.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `vision_projects_images_async_batch_annotate_execute()` or `vision_projects_images_async_batch_annotate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_projects_images_async_batch_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_projects_images_async_batch_annotate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/images:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of images. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateImagesResponse (results). This service will write image annotation outputs to json files in customer GCS bucket, each json file containing BatchAnnotateImagesResponse proto.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `vision_projects_images_async_batch_annotate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `vision_projects_images_async_batch_annotate_task()`.
/// For the simplest API, use `vision_projects_images_async_batch_annotate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `vision_projects_images_async_batch_annotate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn vision_projects_images_async_batch_annotate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = vision_projects_images_async_batch_annotate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`vision_projects_images_async_batch_annotate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VisionProjectsImagesAsyncBatchAnnotateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: AsyncBatchAnnotateImagesRequest,
}

/// GET v1/projects/{projectsId}/images:asyncBatchAnnotate
/// Run asynchronous image detection and annotation for a list of images. Progress and results can be retrieved through the google.longrunning.Operations interface. Operation.metadata contains OperationMetadata (metadata). Operation.response contains AsyncBatchAnnotateImagesResponse (results). This service will write image annotation outputs to json files in customer GCS bucket, each json file containing BatchAnnotateImagesResponse proto.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `vision_projects_images_async_batch_annotate_builder()` + `vision_projects_images_async_batch_annotate_execute()`.
/// For task-level control, use `vision_projects_images_async_batch_annotate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn vision_projects_images_async_batch_annotate(
    client: &SimpleHttpClient,
    args: &VisionProjectsImagesAsyncBatchAnnotateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = vision_projects_images_async_batch_annotate_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    vision_projects_images_async_batch_annotate_execute(builder)
}
