//! Auto-generated API clients for gcp/paymentsresellersubscription.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+parent}/products
/// Currently, it doesn't support **YouTube** products. Retrieves the products that can be resold by the partner. It should be authenticated with a service account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `paymentsresellersubscription_partners_products_list_execute()` to send, or `paymentsresellersubscription_partners_products_list` for simplest API.

pub fn paymentsresellersubscription_partners_products_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/products", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/products
/// Currently, it doesn't support **YouTube** products. Retrieves the products that can be resold by the partner. It should be authenticated with a service account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `paymentsresellersubscription_partners_products_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from paymentsresellersubscription_partners_products_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_products_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProductsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProductsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/products
/// Currently, it doesn't support **YouTube** products. Retrieves the products that can be resold by the partner. It should be authenticated with a service account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `paymentsresellersubscription_partners_products_list_builder()` + `paymentsresellersubscription_partners_products_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_products_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListProductsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = paymentsresellersubscription_partners_products_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    paymentsresellersubscription_partners_products_list_execute(builder)
}

/// GET v1/{+parent}/promotions:findEligible
/// Currently, it is only enabled for **YouTube**. Finds eligible promotions for the current user. The API requires user authorization via OAuth. The bare minimum oauth scope openid is sufficient, which will skip the consent screen.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `paymentsresellersubscription_partners_promotions_find_eligible_execute()` to send, or `paymentsresellersubscription_partners_promotions_find_eligible` for simplest API.

pub fn paymentsresellersubscription_partners_promotions_find_eligible_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &FindEligiblePromotionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/promotions:findEligible",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/promotions:findEligible
/// Currently, it is only enabled for **YouTube**. Finds eligible promotions for the current user. The API requires user authorization via OAuth. The bare minimum oauth scope openid is sufficient, which will skip the consent screen.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `paymentsresellersubscription_partners_promotions_find_eligible_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from paymentsresellersubscription_partners_promotions_find_eligible_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_promotions_find_eligible_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FindEligiblePromotionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FindEligiblePromotionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/promotions:findEligible
/// Currently, it is only enabled for **YouTube**. Finds eligible promotions for the current user. The API requires user authorization via OAuth. The bare minimum oauth scope openid is sufficient, which will skip the consent screen.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `paymentsresellersubscription_partners_promotions_find_eligible_builder()` + `paymentsresellersubscription_partners_promotions_find_eligible_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_promotions_find_eligible(
    client: &SimpleHttpClient,
    parent: &str,
    body: &FindEligiblePromotionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FindEligiblePromotionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = paymentsresellersubscription_partners_promotions_find_eligible_builder(
        client, parent, body,
    )?;
    paymentsresellersubscription_partners_promotions_find_eligible_execute(builder)
}

/// GET v1/{+parent}/promotions
/// Currently, it doesn't support **YouTube** promotions. Retrieves the promotions, such as free trial, that can be used by the partner. It should be authenticated with a service account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `paymentsresellersubscription_partners_promotions_list_execute()` to send, or `paymentsresellersubscription_partners_promotions_list` for simplest API.

pub fn paymentsresellersubscription_partners_promotions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/promotions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/promotions
/// Currently, it doesn't support **YouTube** promotions. Retrieves the promotions, such as free trial, that can be used by the partner. It should be authenticated with a service account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `paymentsresellersubscription_partners_promotions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from paymentsresellersubscription_partners_promotions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_promotions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPromotionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPromotionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/promotions
/// Currently, it doesn't support **YouTube** promotions. Retrieves the promotions, such as free trial, that can be used by the partner. It should be authenticated with a service account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `paymentsresellersubscription_partners_promotions_list_builder()` + `paymentsresellersubscription_partners_promotions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_promotions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPromotionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = paymentsresellersubscription_partners_promotions_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    paymentsresellersubscription_partners_promotions_list_execute(builder)
}

/// GET v1/{+name}:cancel
/// Cancels a subscription service either immediately or by the end of the current billing cycle for their customers. It should be called directly by the partner using service accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `paymentsresellersubscription_partners_subscriptions_cancel_execute()` to send, or `paymentsresellersubscription_partners_subscriptions_cancel` for simplest API.

pub fn paymentsresellersubscription_partners_subscriptions_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelSubscriptionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:cancel
/// Cancels a subscription service either immediately or by the end of the current billing cycle for their customers. It should be called directly by the partner using service accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from paymentsresellersubscription_partners_subscriptions_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CancelSubscriptionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CancelSubscriptionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:cancel
/// Cancels a subscription service either immediately or by the end of the current billing cycle for their customers. It should be called directly by the partner using service accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_cancel_builder()` + `paymentsresellersubscription_partners_subscriptions_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelSubscriptionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CancelSubscriptionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        paymentsresellersubscription_partners_subscriptions_cancel_builder(client, name, body)?;
    paymentsresellersubscription_partners_subscriptions_cancel_execute(builder)
}

/// GET v1/{+parent}/subscriptions
/// Used by partners to create a subscription for their customers. The created subscription is associated with the end user inferred from the end user credentials. This API must be authorized by the end user using OAuth.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `paymentsresellersubscription_partners_subscriptions_create_execute()` to send, or `paymentsresellersubscription_partners_subscriptions_create` for simplest API.

pub fn paymentsresellersubscription_partners_subscriptions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    subscriptionId: Option<&str>,
    body: &Subscription,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/subscriptions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "subscriptionId",
            subscriptionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/subscriptions
/// Used by partners to create a subscription for their customers. The created subscription is associated with the end user inferred from the end user credentials. This API must be authorized by the end user using OAuth.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from paymentsresellersubscription_partners_subscriptions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/subscriptions
/// Used by partners to create a subscription for their customers. The created subscription is associated with the end user inferred from the end user credentials. This API must be authorized by the end user using OAuth.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_create_builder()` + `paymentsresellersubscription_partners_subscriptions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_create(
    client: &SimpleHttpClient,
    parent: &str,
    subscriptionId: Option<&str>,
    body: &Subscription,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = paymentsresellersubscription_partners_subscriptions_create_builder(
        client,
        parent,
        subscriptionId,
        body,
    )?;
    paymentsresellersubscription_partners_subscriptions_create_execute(builder)
}

/// GET v1/{+name}:entitle
/// Entitles a previously provisioned subscription to the current end user. The end user identity is inferred from the authorized credential of the request. This API must be authorized by the end user using OAuth.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `paymentsresellersubscription_partners_subscriptions_entitle_execute()` to send, or `paymentsresellersubscription_partners_subscriptions_entitle` for simplest API.

pub fn paymentsresellersubscription_partners_subscriptions_entitle_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &EntitleSubscriptionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:entitle", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:entitle
/// Entitles a previously provisioned subscription to the current end user. The end user identity is inferred from the authorized credential of the request. This API must be authorized by the end user using OAuth.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_entitle_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from paymentsresellersubscription_partners_subscriptions_entitle_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_entitle_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EntitleSubscriptionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EntitleSubscriptionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:entitle
/// Entitles a previously provisioned subscription to the current end user. The end user identity is inferred from the authorized credential of the request. This API must be authorized by the end user using OAuth.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_entitle_builder()` + `paymentsresellersubscription_partners_subscriptions_entitle_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_entitle(
    client: &SimpleHttpClient,
    name: &str,
    body: &EntitleSubscriptionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EntitleSubscriptionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        paymentsresellersubscription_partners_subscriptions_entitle_builder(client, name, body)?;
    paymentsresellersubscription_partners_subscriptions_entitle_execute(builder)
}

/// GET v1/{+name}:extend
/// [Opt-in only] Most partners should be on auto-extend by default. Extends a subscription service for their customers on an ongoing basis for the subscription to remain active and renewable. It should be called directly by the partner using service accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `paymentsresellersubscription_partners_subscriptions_extend_execute()` to send, or `paymentsresellersubscription_partners_subscriptions_extend` for simplest API.

pub fn paymentsresellersubscription_partners_subscriptions_extend_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExtendSubscriptionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:extend", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:extend
/// [Opt-in only] Most partners should be on auto-extend by default. Extends a subscription service for their customers on an ongoing basis for the subscription to remain active and renewable. It should be called directly by the partner using service accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_extend_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from paymentsresellersubscription_partners_subscriptions_extend_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_extend_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ExtendSubscriptionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExtendSubscriptionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:extend
/// [Opt-in only] Most partners should be on auto-extend by default. Extends a subscription service for their customers on an ongoing basis for the subscription to remain active and renewable. It should be called directly by the partner using service accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_extend_builder()` + `paymentsresellersubscription_partners_subscriptions_extend_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_extend(
    client: &SimpleHttpClient,
    name: &str,
    body: &ExtendSubscriptionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ExtendSubscriptionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        paymentsresellersubscription_partners_subscriptions_extend_builder(client, name, body)?;
    paymentsresellersubscription_partners_subscriptions_extend_execute(builder)
}

/// GET v1/{+name}
/// Gets a subscription by id. It should be called directly by the partner using service accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `paymentsresellersubscription_partners_subscriptions_get_execute()` to send, or `paymentsresellersubscription_partners_subscriptions_get` for simplest API.

pub fn paymentsresellersubscription_partners_subscriptions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a subscription by id. It should be called directly by the partner using service accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from paymentsresellersubscription_partners_subscriptions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a subscription by id. It should be called directly by the partner using service accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_get_builder()` + `paymentsresellersubscription_partners_subscriptions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = paymentsresellersubscription_partners_subscriptions_get_builder(client, name)?;
    paymentsresellersubscription_partners_subscriptions_get_execute(builder)
}

/// GET v1/{+parent}/subscriptions:provision
/// Used by partners to provision a subscription for their customers. This creates a subscription without associating it with the end user account. EntitleSubscription must be called separately using OAuth in order for the end user account to be associated with the subscription. It should be called directly by the partner using service accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `paymentsresellersubscription_partners_subscriptions_provision_execute()` to send, or `paymentsresellersubscription_partners_subscriptions_provision` for simplest API.

pub fn paymentsresellersubscription_partners_subscriptions_provision_builder(
    client: &SimpleHttpClient,
    parent: &str,
    cycleOptions_initialCycleDuration_count: Option<i32>,
    cycleOptions_initialCycleDuration_unit: Option<&str>,
    subscriptionId: Option<&str>,
    body: &Subscription,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/subscriptions:provision",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "cycleOptions_initialCycleDuration_count",
            cycleOptions_initialCycleDuration_count
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "cycleOptions_initialCycleDuration_unit",
            cycleOptions_initialCycleDuration_unit
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "subscriptionId",
            subscriptionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/subscriptions:provision
/// Used by partners to provision a subscription for their customers. This creates a subscription without associating it with the end user account. EntitleSubscription must be called separately using OAuth in order for the end user account to be associated with the subscription. It should be called directly by the partner using service accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_provision_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from paymentsresellersubscription_partners_subscriptions_provision_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_provision_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/subscriptions:provision
/// Used by partners to provision a subscription for their customers. This creates a subscription without associating it with the end user account. EntitleSubscription must be called separately using OAuth in order for the end user account to be associated with the subscription. It should be called directly by the partner using service accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_provision_builder()` + `paymentsresellersubscription_partners_subscriptions_provision_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_provision(
    client: &SimpleHttpClient,
    parent: &str,
    cycleOptions_initialCycleDuration_count: Option<i32>,
    cycleOptions_initialCycleDuration_unit: Option<&str>,
    subscriptionId: Option<&str>,
    body: &Subscription,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = paymentsresellersubscription_partners_subscriptions_provision_builder(
        client,
        parent,
        cycleOptions_initialCycleDuration_count,
        cycleOptions_initialCycleDuration_unit,
        subscriptionId,
        body,
    )?;
    paymentsresellersubscription_partners_subscriptions_provision_execute(builder)
}

/// GET v1/{+name}:resume
/// Resumes a suspended subscription. The new billing cycle will start at the time of the request. It should be called directly by the partner using service accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `paymentsresellersubscription_partners_subscriptions_resume_execute()` to send, or `paymentsresellersubscription_partners_subscriptions_resume` for simplest API.

pub fn paymentsresellersubscription_partners_subscriptions_resume_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResumeSubscriptionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:resume", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:resume
/// Resumes a suspended subscription. The new billing cycle will start at the time of the request. It should be called directly by the partner using service accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_resume_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from paymentsresellersubscription_partners_subscriptions_resume_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_resume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResumeSubscriptionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResumeSubscriptionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:resume
/// Resumes a suspended subscription. The new billing cycle will start at the time of the request. It should be called directly by the partner using service accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_resume_builder()` + `paymentsresellersubscription_partners_subscriptions_resume_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_resume(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResumeSubscriptionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResumeSubscriptionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        paymentsresellersubscription_partners_subscriptions_resume_builder(client, name, body)?;
    paymentsresellersubscription_partners_subscriptions_resume_execute(builder)
}

/// GET v1/{+name}:suspend
/// Suspends a subscription. Contract terms may dictate if a prorated refund will be issued upon suspension. It should be called directly by the partner using service accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `paymentsresellersubscription_partners_subscriptions_suspend_execute()` to send, or `paymentsresellersubscription_partners_subscriptions_suspend` for simplest API.

pub fn paymentsresellersubscription_partners_subscriptions_suspend_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SuspendSubscriptionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:suspend", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:suspend
/// Suspends a subscription. Contract terms may dictate if a prorated refund will be issued upon suspension. It should be called directly by the partner using service accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_suspend_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from paymentsresellersubscription_partners_subscriptions_suspend_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_suspend_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SuspendSubscriptionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SuspendSubscriptionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:suspend
/// Suspends a subscription. Contract terms may dictate if a prorated refund will be issued upon suspension. It should be called directly by the partner using service accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_suspend_builder()` + `paymentsresellersubscription_partners_subscriptions_suspend_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_suspend(
    client: &SimpleHttpClient,
    name: &str,
    body: &SuspendSubscriptionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SuspendSubscriptionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        paymentsresellersubscription_partners_subscriptions_suspend_builder(client, name, body)?;
    paymentsresellersubscription_partners_subscriptions_suspend_execute(builder)
}

/// GET v1/{+name}:undoCancel
/// Currently, it is used by **Google One, Play Pass** partners. Revokes the pending cancellation of a subscription, which is currently in STATE_CANCEL_AT_END_OF_CYCLE state. If the subscription is already cancelled, the request will fail. It should be called directly by the partner using service accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `paymentsresellersubscription_partners_subscriptions_undo_cancel_execute()` to send, or `paymentsresellersubscription_partners_subscriptions_undo_cancel` for simplest API.

pub fn paymentsresellersubscription_partners_subscriptions_undo_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndoCancelSubscriptionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:undoCancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:undoCancel
/// Currently, it is used by **Google One, Play Pass** partners. Revokes the pending cancellation of a subscription, which is currently in STATE_CANCEL_AT_END_OF_CYCLE state. If the subscription is already cancelled, the request will fail. It should be called directly by the partner using service accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_undo_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from paymentsresellersubscription_partners_subscriptions_undo_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_undo_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UndoCancelSubscriptionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UndoCancelSubscriptionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:undoCancel
/// Currently, it is used by **Google One, Play Pass** partners. Revokes the pending cancellation of a subscription, which is currently in STATE_CANCEL_AT_END_OF_CYCLE state. If the subscription is already cancelled, the request will fail. It should be called directly by the partner using service accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_undo_cancel_builder()` + `paymentsresellersubscription_partners_subscriptions_undo_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_undo_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &UndoCancelSubscriptionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UndoCancelSubscriptionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = paymentsresellersubscription_partners_subscriptions_undo_cancel_builder(
        client, name, body,
    )?;
    paymentsresellersubscription_partners_subscriptions_undo_cancel_execute(builder)
}

/// GET v1/{+name}
/// Updates a line item of a subscription. It should be authenticated with a service account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `paymentsresellersubscription_partners_subscriptions_line_items_patch_execute()` to send, or `paymentsresellersubscription_partners_subscriptions_line_items_patch` for simplest API.

pub fn paymentsresellersubscription_partners_subscriptions_line_items_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SubscriptionLineItem,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates a line item of a subscription. It should be authenticated with a service account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_line_items_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from paymentsresellersubscription_partners_subscriptions_line_items_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_line_items_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionLineItem>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionLineItem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates a line item of a subscription. It should be authenticated with a service account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `paymentsresellersubscription_partners_subscriptions_line_items_patch_builder()` + `paymentsresellersubscription_partners_subscriptions_line_items_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_subscriptions_line_items_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SubscriptionLineItem,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionLineItem>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = paymentsresellersubscription_partners_subscriptions_line_items_patch_builder(
        client, name, updateMask, body,
    )?;
    paymentsresellersubscription_partners_subscriptions_line_items_patch_execute(builder)
}

/// GET v1/{+parent}/userSessions:generate
/// This API replaces user authorized OAuth consent based APIs (Create, Entitle). Issues a timed session token for the given user intent. You can use the session token to redirect the user to Google to finish the signup flow. You can re-generate new session token repeatedly for the same request if necessary, regardless of the previous tokens being expired or not. By default, the session token is valid for 1 hour.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `paymentsresellersubscription_partners_user_sessions_generate_execute()` to send, or `paymentsresellersubscription_partners_user_sessions_generate` for simplest API.

pub fn paymentsresellersubscription_partners_user_sessions_generate_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GenerateUserSessionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/userSessions:generate",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/userSessions:generate
/// This API replaces user authorized OAuth consent based APIs (Create, Entitle). Issues a timed session token for the given user intent. You can use the session token to redirect the user to Google to finish the signup flow. You can re-generate new session token repeatedly for the same request if necessary, regardless of the previous tokens being expired or not. By default, the session token is valid for 1 hour.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `paymentsresellersubscription_partners_user_sessions_generate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from paymentsresellersubscription_partners_user_sessions_generate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_user_sessions_generate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateUserSessionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateUserSessionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/userSessions:generate
/// This API replaces user authorized OAuth consent based APIs (Create, Entitle). Issues a timed session token for the given user intent. You can use the session token to redirect the user to Google to finish the signup flow. You can re-generate new session token repeatedly for the same request if necessary, regardless of the previous tokens being expired or not. By default, the session token is valid for 1 hour.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `paymentsresellersubscription_partners_user_sessions_generate_builder()` + `paymentsresellersubscription_partners_user_sessions_generate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn paymentsresellersubscription_partners_user_sessions_generate(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GenerateUserSessionRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateUserSessionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        paymentsresellersubscription_partners_user_sessions_generate_builder(client, parent, body)?;
    paymentsresellersubscription_partners_user_sessions_generate_execute(builder)
}
