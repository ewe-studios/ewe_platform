//! Auto-generated API clients for gcp/threatintelligence.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1beta/projects/{projectsId}:generateOrgProfile
/// Triggers the generation of a Customer Profile for a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_generate_org_profile_execute()` to send, or `threatintelligence_projects_generate_org_profile` for simplest API.

pub fn threatintelligence_projects_generate_org_profile_builder(
    client: &SimpleHttpClient,
    name: &String,
    body: &GenerateOrgProfileConfigurationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://threatintelligence.googleapis.com/v1beta/projects/{}:generateOrgProfile",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}:generateOrgProfile
/// Triggers the generation of a Customer Profile for a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `threatintelligence_projects_generate_org_profile_execute()` or `threatintelligence_projects_generate_org_profile`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `threatintelligence_projects_generate_org_profile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_generate_org_profile_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta/projects/{projectsId}:generateOrgProfile
/// Triggers the generation of a Customer Profile for a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `threatintelligence_projects_generate_org_profile_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `threatintelligence_projects_generate_org_profile_task()`.
/// For the simplest API, use `threatintelligence_projects_generate_org_profile()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `threatintelligence_projects_generate_org_profile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn threatintelligence_projects_generate_org_profile_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = threatintelligence_projects_generate_org_profile_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`threatintelligence_projects_generate_org_profile`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ThreatintelligenceProjectsGenerateOrgProfileArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GenerateOrgProfileConfigurationRequest,
}

/// GET v1beta/projects/{projectsId}:generateOrgProfile
/// Triggers the generation of a Customer Profile for a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_generate_org_profile_builder()` + `threatintelligence_projects_generate_org_profile_execute()`.
/// For task-level control, use `threatintelligence_projects_generate_org_profile_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_generate_org_profile(
    client: &SimpleHttpClient,
    args: &ThreatintelligenceProjectsGenerateOrgProfileArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        threatintelligence_projects_generate_org_profile_builder(client, &args.name, &args.body)?;
    threatintelligence_projects_generate_org_profile_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts:enumerateFacets
/// EnumerateAlertFacets returns the facets and the number of alerts that meet the filter criteria and have that value for each facet.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_enumerate_facets_execute()` to send, or `threatintelligence_projects_alerts_enumerate_facets` for simplest API.

pub fn threatintelligence_projects_alerts_enumerate_facets_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts:enumerateFacets",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/alerts:enumerateFacets
/// EnumerateAlertFacets returns the facets and the number of alerts that meet the filter criteria and have that value for each facet.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `threatintelligence_projects_alerts_enumerate_facets_execute()` or `threatintelligence_projects_alerts_enumerate_facets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `threatintelligence_projects_alerts_enumerate_facets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_enumerate_facets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EnumerateAlertFacetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EnumerateAlertFacetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta/projects/{projectsId}/alerts:enumerateFacets
/// EnumerateAlertFacets returns the facets and the number of alerts that meet the filter criteria and have that value for each facet.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `threatintelligence_projects_alerts_enumerate_facets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `threatintelligence_projects_alerts_enumerate_facets_task()`.
/// For the simplest API, use `threatintelligence_projects_alerts_enumerate_facets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `threatintelligence_projects_alerts_enumerate_facets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn threatintelligence_projects_alerts_enumerate_facets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EnumerateAlertFacetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = threatintelligence_projects_alerts_enumerate_facets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`threatintelligence_projects_alerts_enumerate_facets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ThreatintelligenceProjectsAlertsEnumerateFacetsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
}

/// GET v1beta/projects/{projectsId}/alerts:enumerateFacets
/// EnumerateAlertFacets returns the facets and the number of alerts that meet the filter criteria and have that value for each facet.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_enumerate_facets_builder()` + `threatintelligence_projects_alerts_enumerate_facets_execute()`.
/// For task-level control, use `threatintelligence_projects_alerts_enumerate_facets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_enumerate_facets(
    client: &SimpleHttpClient,
    args: &ThreatintelligenceProjectsAlertsEnumerateFacetsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EnumerateAlertFacetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_alerts_enumerate_facets_builder(
        client,
        &args.parent,
        &args.filter,
    )?;
    threatintelligence_projects_alerts_enumerate_facets_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts
/// Get a list of alerts that meet the filter criteria.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_list_execute()` to send, or `threatintelligence_projects_alerts_list` for simplest API.

pub fn threatintelligence_projects_alerts_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/alerts
/// Get a list of alerts that meet the filter criteria.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `threatintelligence_projects_alerts_list_execute()` or `threatintelligence_projects_alerts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `threatintelligence_projects_alerts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAlertsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAlertsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta/projects/{projectsId}/alerts
/// Get a list of alerts that meet the filter criteria.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `threatintelligence_projects_alerts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `threatintelligence_projects_alerts_list_task()`.
/// For the simplest API, use `threatintelligence_projects_alerts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `threatintelligence_projects_alerts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn threatintelligence_projects_alerts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = threatintelligence_projects_alerts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`threatintelligence_projects_alerts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ThreatintelligenceProjectsAlertsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta/projects/{projectsId}/alerts
/// Get a list of alerts that meet the filter criteria.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_list_builder()` + `threatintelligence_projects_alerts_list_execute()`.
/// For task-level control, use `threatintelligence_projects_alerts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_list(
    client: &SimpleHttpClient,
    args: &ThreatintelligenceProjectsAlertsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_alerts_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    threatintelligence_projects_alerts_list_execute(builder)
}

/// GET v1beta/projects/{projectsId}/configurations
/// Get a list of configurations that meet the filter criteria.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_configurations_list_execute()` to send, or `threatintelligence_projects_configurations_list` for simplest API.

pub fn threatintelligence_projects_configurations_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://threatintelligence.googleapis.com/v1beta/projects/{}/configurations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/configurations
/// Get a list of configurations that meet the filter criteria.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `threatintelligence_projects_configurations_list_execute()` or `threatintelligence_projects_configurations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `threatintelligence_projects_configurations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_configurations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListConfigurationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConfigurationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta/projects/{projectsId}/configurations
/// Get a list of configurations that meet the filter criteria.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `threatintelligence_projects_configurations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `threatintelligence_projects_configurations_list_task()`.
/// For the simplest API, use `threatintelligence_projects_configurations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `threatintelligence_projects_configurations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn threatintelligence_projects_configurations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConfigurationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = threatintelligence_projects_configurations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`threatintelligence_projects_configurations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ThreatintelligenceProjectsConfigurationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta/projects/{projectsId}/configurations
/// Get a list of configurations that meet the filter criteria.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_configurations_list_builder()` + `threatintelligence_projects_configurations_list_execute()`.
/// For task-level control, use `threatintelligence_projects_configurations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_configurations_list(
    client: &SimpleHttpClient,
    args: &ThreatintelligenceProjectsConfigurationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConfigurationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_configurations_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    threatintelligence_projects_configurations_list_execute(builder)
}

/// GET v1beta/projects/{projectsId}/configurations:upsert
/// Creates or updates a configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_configurations_upsert_execute()` to send, or `threatintelligence_projects_configurations_upsert` for simplest API.

pub fn threatintelligence_projects_configurations_upsert_builder(
    client: &SimpleHttpClient,
    parent: &String,
    publishTime: &Option<String>,
    body: &Configuration,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/configurations:upsert",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = publishTime.as_ref() {
        query_parts.push(format!("publishTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/configurations:upsert
/// Creates or updates a configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `threatintelligence_projects_configurations_upsert_execute()` or `threatintelligence_projects_configurations_upsert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `threatintelligence_projects_configurations_upsert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_configurations_upsert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UpsertConfigurationResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpsertConfigurationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta/projects/{projectsId}/configurations:upsert
/// Creates or updates a configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `threatintelligence_projects_configurations_upsert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `threatintelligence_projects_configurations_upsert_task()`.
/// For the simplest API, use `threatintelligence_projects_configurations_upsert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `threatintelligence_projects_configurations_upsert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn threatintelligence_projects_configurations_upsert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpsertConfigurationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = threatintelligence_projects_configurations_upsert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`threatintelligence_projects_configurations_upsert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ThreatintelligenceProjectsConfigurationsUpsertArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: publishTime
    pub publishTime: Option<String>,
    /// Request body.
    pub body: Configuration,
}

/// GET v1beta/projects/{projectsId}/configurations:upsert
/// Creates or updates a configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_configurations_upsert_builder()` + `threatintelligence_projects_configurations_upsert_execute()`.
/// For task-level control, use `threatintelligence_projects_configurations_upsert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_configurations_upsert(
    client: &SimpleHttpClient,
    args: &ThreatintelligenceProjectsConfigurationsUpsertArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpsertConfigurationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_configurations_upsert_builder(
        client,
        &args.parent,
        &args.publishTime,
        &args.body,
    )?;
    threatintelligence_projects_configurations_upsert_execute(builder)
}

/// GET v1beta/projects/{projectsId}/findings
/// Get a list of findings that meet the filter criteria. The parent field in ListFindingsRequest should have the format: `projects/{project}`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_findings_list_execute()` to send, or `threatintelligence_projects_findings_list` for simplest API.

pub fn threatintelligence_projects_findings_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://threatintelligence.googleapis.com/v1beta/projects/{}/findings",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/findings
/// Get a list of findings that meet the filter criteria. The parent field in ListFindingsRequest should have the format: `projects/{project}`
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `threatintelligence_projects_findings_list_execute()` or `threatintelligence_projects_findings_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `threatintelligence_projects_findings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_findings_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFindingsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta/projects/{projectsId}/findings
/// Get a list of findings that meet the filter criteria. The parent field in ListFindingsRequest should have the format: `projects/{project}`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `threatintelligence_projects_findings_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `threatintelligence_projects_findings_list_task()`.
/// For the simplest API, use `threatintelligence_projects_findings_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `threatintelligence_projects_findings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn threatintelligence_projects_findings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = threatintelligence_projects_findings_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`threatintelligence_projects_findings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ThreatintelligenceProjectsFindingsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta/projects/{projectsId}/findings
/// Get a list of findings that meet the filter criteria. The parent field in ListFindingsRequest should have the format: `projects/{project}`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_findings_list_builder()` + `threatintelligence_projects_findings_list_execute()`.
/// For task-level control, use `threatintelligence_projects_findings_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_findings_list(
    client: &SimpleHttpClient,
    args: &ThreatintelligenceProjectsFindingsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_findings_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    threatintelligence_projects_findings_list_execute(builder)
}

/// GET v1beta/projects/{projectsId}/findings:search
/// SearchFindings is a more powerful version of ListFindings that supports complex queries like "findings for alerts" using functions such as has_alert in the query string. The parent field in SearchFindingsRequest should have the format: `projects/{project}` Example to search for findings for a specific issue: has_alert("name=\"`projects/gti-12345/alerts/alert-12345`\"")
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_findings_search_execute()` to send, or `threatintelligence_projects_findings_search` for simplest API.

pub fn threatintelligence_projects_findings_search_builder(
    client: &SimpleHttpClient,
    parent: &String,
    orderBy: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    query: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://threatintelligence.googleapis.com/v1beta/projects/{}/findings:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/findings:search
/// SearchFindings is a more powerful version of ListFindings that supports complex queries like "findings for alerts" using functions such as has_alert in the query string. The parent field in SearchFindingsRequest should have the format: `projects/{project}` Example to search for findings for a specific issue: has_alert("name=\"`projects/gti-12345/alerts/alert-12345`\"")
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `threatintelligence_projects_findings_search_execute()` or `threatintelligence_projects_findings_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `threatintelligence_projects_findings_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_findings_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchFindingsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta/projects/{projectsId}/findings:search
/// SearchFindings is a more powerful version of ListFindings that supports complex queries like "findings for alerts" using functions such as has_alert in the query string. The parent field in SearchFindingsRequest should have the format: `projects/{project}` Example to search for findings for a specific issue: has_alert("name=\"`projects/gti-12345/alerts/alert-12345`\"")
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `threatintelligence_projects_findings_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `threatintelligence_projects_findings_search_task()`.
/// For the simplest API, use `threatintelligence_projects_findings_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `threatintelligence_projects_findings_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn threatintelligence_projects_findings_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = threatintelligence_projects_findings_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`threatintelligence_projects_findings_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ThreatintelligenceProjectsFindingsSearchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v1beta/projects/{projectsId}/findings:search
/// SearchFindings is a more powerful version of ListFindings that supports complex queries like "findings for alerts" using functions such as has_alert in the query string. The parent field in SearchFindingsRequest should have the format: `projects/{project}` Example to search for findings for a specific issue: has_alert("name=\"`projects/gti-12345/alerts/alert-12345`\"")
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_findings_search_builder()` + `threatintelligence_projects_findings_search_execute()`.
/// For task-level control, use `threatintelligence_projects_findings_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_findings_search(
    client: &SimpleHttpClient,
    args: &ThreatintelligenceProjectsFindingsSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_findings_search_builder(
        client,
        &args.parent,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.query,
    )?;
    threatintelligence_projects_findings_search_execute(builder)
}
