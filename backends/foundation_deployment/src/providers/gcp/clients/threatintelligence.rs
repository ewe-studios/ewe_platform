//! Auto-generated API clients for gcp/threatintelligence.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/threatintelligence`

#![cfg(feature = "gcp/threatintelligence")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1beta/projects/{projectsId}:generateOrgProfile
/// Triggers the generation of a Customer Profile for a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_generate_org_profile_execute()` to send, or `threatintelligence_projects_generate_org_profile` for simplest API.

pub fn threatintelligence_projects_generate_org_profile_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GenerateOrgProfileConfigurationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}:generateOrgProfile",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}:generateOrgProfile
/// Triggers the generation of a Customer Profile for a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_generate_org_profile_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_generate_org_profile_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_generate_org_profile_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}:generateOrgProfile
/// Triggers the generation of a Customer Profile for a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_generate_org_profile_builder()` + `threatintelligence_projects_generate_org_profile_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_generate_org_profile(
    client: &SimpleHttpClient,
    name: &str,
    body: &GenerateOrgProfileConfigurationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_generate_org_profile_builder(client, name, body)?;
    threatintelligence_projects_generate_org_profile_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:benign
/// Marks an alert as benign - BENIGN.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_benign_execute()` to send, or `threatintelligence_projects_alerts_benign` for simplest API.

pub fn threatintelligence_projects_alerts_benign_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsBenignRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts/{}:benign",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:benign
/// Marks an alert as benign - BENIGN.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_alerts_benign_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_alerts_benign_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_benign_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Alert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:benign
/// Marks an alert as benign - BENIGN.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_benign_builder()` + `threatintelligence_projects_alerts_benign_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_benign(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsBenignRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_alerts_benign_builder(client, name, body)?;
    threatintelligence_projects_alerts_benign_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:duplicate
/// Marks an alert as a duplicate of another alert. - DUPLICATE.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_duplicate_execute()` to send, or `threatintelligence_projects_alerts_duplicate` for simplest API.

pub fn threatintelligence_projects_alerts_duplicate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsDuplicateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts/{}:duplicate",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:duplicate
/// Marks an alert as a duplicate of another alert. - DUPLICATE.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_alerts_duplicate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_alerts_duplicate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_duplicate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Alert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:duplicate
/// Marks an alert as a duplicate of another alert. - DUPLICATE.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_duplicate_builder()` + `threatintelligence_projects_alerts_duplicate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_duplicate(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsDuplicateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_alerts_duplicate_builder(client, name, body)?;
    threatintelligence_projects_alerts_duplicate_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts:enumerateFacets
/// EnumerateAlertFacets returns the facets and the number of alerts that meet the filter criteria and have that value for each facet.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_enumerate_facets_execute()` to send, or `threatintelligence_projects_alerts_enumerate_facets` for simplest API.

pub fn threatintelligence_projects_alerts_enumerate_facets_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts:enumerateFacets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/alerts:enumerateFacets
/// EnumerateAlertFacets returns the facets and the number of alerts that meet the filter criteria and have that value for each facet.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_alerts_enumerate_facets_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_alerts_enumerate_facets_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_enumerate_facets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EnumerateAlertFacetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EnumerateAlertFacetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts:enumerateFacets
/// EnumerateAlertFacets returns the facets and the number of alerts that meet the filter criteria and have that value for each facet.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_enumerate_facets_builder()` + `threatintelligence_projects_alerts_enumerate_facets_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_enumerate_facets(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EnumerateAlertFacetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        threatintelligence_projects_alerts_enumerate_facets_builder(client, parent, filter)?;
    threatintelligence_projects_alerts_enumerate_facets_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:escalate
/// Marks an alert as escalated - ESCALATED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_escalate_execute()` to send, or `threatintelligence_projects_alerts_escalate` for simplest API.

pub fn threatintelligence_projects_alerts_escalate_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsEscalatedRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts/{}:escalate",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:escalate
/// Marks an alert as escalated - ESCALATED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_alerts_escalate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_alerts_escalate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_escalate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Alert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:escalate
/// Marks an alert as escalated - ESCALATED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_escalate_builder()` + `threatintelligence_projects_alerts_escalate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_escalate(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsEscalatedRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_alerts_escalate_builder(client, name, body)?;
    threatintelligence_projects_alerts_escalate_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:falsePositive
/// Marks an alert as a `false` positive - FALSE_POSITIVE.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_false_positive_execute()` to send, or `threatintelligence_projects_alerts_false_positive` for simplest API.

pub fn threatintelligence_projects_alerts_false_positive_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsFalsePositiveRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts/{}:falsePositive",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:falsePositive
/// Marks an alert as a `false` positive - FALSE_POSITIVE.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_alerts_false_positive_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_alerts_false_positive_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_false_positive_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Alert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:falsePositive
/// Marks an alert as a `false` positive - FALSE_POSITIVE.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_false_positive_builder()` + `threatintelligence_projects_alerts_false_positive_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_false_positive(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsFalsePositiveRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_alerts_false_positive_builder(client, name, body)?;
    threatintelligence_projects_alerts_false_positive_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}
/// Get an alert by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_get_execute()` to send, or `threatintelligence_projects_alerts_get` for simplest API.

pub fn threatintelligence_projects_alerts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}
/// Get an alert by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_alerts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_alerts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Alert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}
/// Get an alert by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_get_builder()` + `threatintelligence_projects_alerts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_alerts_get_builder(client, name)?;
    threatintelligence_projects_alerts_get_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts
/// Get a list of alerts that meet the filter criteria.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_list_execute()` to send, or `threatintelligence_projects_alerts_list` for simplest API.

pub fn threatintelligence_projects_alerts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/alerts
/// Get a list of alerts that meet the filter criteria.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_alerts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_alerts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAlertsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts
/// Get a list of alerts that meet the filter criteria.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_list_builder()` + `threatintelligence_projects_alerts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAlertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_alerts_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    threatintelligence_projects_alerts_list_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:notActionable
/// Marks an alert as not actionable - NOT_ACTIONABLE.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_not_actionable_execute()` to send, or `threatintelligence_projects_alerts_not_actionable` for simplest API.

pub fn threatintelligence_projects_alerts_not_actionable_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsNotActionableRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts/{}:notActionable",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:notActionable
/// Marks an alert as not actionable - NOT_ACTIONABLE.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_alerts_not_actionable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_alerts_not_actionable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_not_actionable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Alert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:notActionable
/// Marks an alert as not actionable - NOT_ACTIONABLE.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_not_actionable_builder()` + `threatintelligence_projects_alerts_not_actionable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_not_actionable(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsNotActionableRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_alerts_not_actionable_builder(client, name, body)?;
    threatintelligence_projects_alerts_not_actionable_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:read
/// Marks an alert as read - READ.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_read_execute()` to send, or `threatintelligence_projects_alerts_read` for simplest API.

pub fn threatintelligence_projects_alerts_read_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsReadRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts/{}:read",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:read
/// Marks an alert as read - READ.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_alerts_read_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_alerts_read_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_read_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Alert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:read
/// Marks an alert as read - READ.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_read_builder()` + `threatintelligence_projects_alerts_read_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_read(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsReadRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_alerts_read_builder(client, name, body)?;
    threatintelligence_projects_alerts_read_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:resolve
/// Marks an alert to closed state - RESOLVED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_resolve_execute()` to send, or `threatintelligence_projects_alerts_resolve` for simplest API.

pub fn threatintelligence_projects_alerts_resolve_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsResolvedRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts/{}:resolve",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:resolve
/// Marks an alert to closed state - RESOLVED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_alerts_resolve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_alerts_resolve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_resolve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Alert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:resolve
/// Marks an alert to closed state - RESOLVED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_resolve_builder()` + `threatintelligence_projects_alerts_resolve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_resolve(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsResolvedRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_alerts_resolve_builder(client, name, body)?;
    threatintelligence_projects_alerts_resolve_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:trackExternally
/// Marks an alert as tracked externally - TRACKED_EXTERNALLY.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_track_externally_execute()` to send, or `threatintelligence_projects_alerts_track_externally` for simplest API.

pub fn threatintelligence_projects_alerts_track_externally_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsTrackedExternallyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts/{}:trackExternally",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:trackExternally
/// Marks an alert as tracked externally - TRACKED_EXTERNALLY.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_alerts_track_externally_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_alerts_track_externally_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_track_externally_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Alert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:trackExternally
/// Marks an alert as tracked externally - TRACKED_EXTERNALLY.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_track_externally_builder()` + `threatintelligence_projects_alerts_track_externally_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_track_externally(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsTrackedExternallyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_alerts_track_externally_builder(client, name, body)?;
    threatintelligence_projects_alerts_track_externally_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:triage
/// Marks an alert as triaged - TRIAGED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_triage_execute()` to send, or `threatintelligence_projects_alerts_triage` for simplest API.

pub fn threatintelligence_projects_alerts_triage_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsTriagedRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts/{}:triage",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:triage
/// Marks an alert as triaged - TRIAGED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_alerts_triage_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_alerts_triage_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_triage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Alert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}:triage
/// Marks an alert as triaged - TRIAGED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_triage_builder()` + `threatintelligence_projects_alerts_triage_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_triage(
    client: &SimpleHttpClient,
    name: &str,
    body: &MarkAlertAsTriagedRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Alert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_alerts_triage_builder(client, name, body)?;
    threatintelligence_projects_alerts_triage_execute(builder)
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}/documents/{documentsId}
/// Gets a specific document associated with an alert.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_alerts_documents_get_execute()` to send, or `threatintelligence_projects_alerts_documents_get` for simplest API.

pub fn threatintelligence_projects_alerts_documents_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/alerts/{}/documents/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}/documents/{documentsId}
/// Gets a specific document associated with an alert.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_alerts_documents_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_alerts_documents_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_documents_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertDocument>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AlertDocument = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/alerts/{alertsId}/documents/{documentsId}
/// Gets a specific document associated with an alert.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_alerts_documents_get_builder()` + `threatintelligence_projects_alerts_documents_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_alerts_documents_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AlertDocument>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_alerts_documents_get_builder(client, name)?;
    threatintelligence_projects_alerts_documents_get_execute(builder)
}

/// GET v1beta/projects/{projectsId}/configurations/{configurationsId}
/// Get a configuration by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_configurations_get_execute()` to send, or `threatintelligence_projects_configurations_get` for simplest API.

pub fn threatintelligence_projects_configurations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/configurations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/configurations/{configurationsId}
/// Get a configuration by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_configurations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_configurations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_configurations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Configuration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Configuration = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/configurations/{configurationsId}
/// Get a configuration by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_configurations_get_builder()` + `threatintelligence_projects_configurations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_configurations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Configuration>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_configurations_get_builder(client, name)?;
    threatintelligence_projects_configurations_get_execute(builder)
}

/// GET v1beta/projects/{projectsId}/configurations
/// Get a list of configurations that meet the filter criteria.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_configurations_list_execute()` to send, or `threatintelligence_projects_configurations_list` for simplest API.

pub fn threatintelligence_projects_configurations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/configurations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/configurations
/// Get a list of configurations that meet the filter criteria.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_configurations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_configurations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_configurations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConfigurationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConfigurationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/configurations
/// Get a list of configurations that meet the filter criteria.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_configurations_list_builder()` + `threatintelligence_projects_configurations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_configurations_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConfigurationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_configurations_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    threatintelligence_projects_configurations_list_execute(builder)
}

/// GET v1beta/projects/{projectsId}/configurations:upsert
/// Creates or updates a configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_configurations_upsert_execute()` to send, or `threatintelligence_projects_configurations_upsert` for simplest API.

pub fn threatintelligence_projects_configurations_upsert_builder(
    client: &SimpleHttpClient,
    parent: &str,
    publishTime: Option<&str>,
    body: &Configuration,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/configurations:upsert",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = publishTime {
        query_parts.push(format!("publishTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/configurations:upsert
/// Creates or updates a configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_configurations_upsert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_configurations_upsert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_configurations_upsert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpsertConfigurationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpsertConfigurationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/configurations:upsert
/// Creates or updates a configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_configurations_upsert_builder()` + `threatintelligence_projects_configurations_upsert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_configurations_upsert(
    client: &SimpleHttpClient,
    parent: &str,
    publishTime: Option<&str>,
    body: &Configuration,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpsertConfigurationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_configurations_upsert_builder(
        client,
        parent,
        publishTime,
        body,
    )?;
    threatintelligence_projects_configurations_upsert_execute(builder)
}

/// GET v1beta/projects/{projectsId}/configurations/{configurationsId}/revisions
/// List configuration revisions that meet the filter criteria.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_configurations_revisions_list_execute()` to send, or `threatintelligence_projects_configurations_revisions_list` for simplest API.

pub fn threatintelligence_projects_configurations_revisions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/configurations/{}/revisions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/configurations/{configurationsId}/revisions
/// List configuration revisions that meet the filter criteria.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_configurations_revisions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_configurations_revisions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_configurations_revisions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConfigurationRevisionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConfigurationRevisionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/configurations/{configurationsId}/revisions
/// List configuration revisions that meet the filter criteria.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_configurations_revisions_list_builder()` + `threatintelligence_projects_configurations_revisions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_configurations_revisions_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListConfigurationRevisionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_configurations_revisions_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    threatintelligence_projects_configurations_revisions_list_execute(builder)
}

/// GET v1beta/projects/{projectsId}/findings/{findingsId}
/// Get a finding by name. The name field should have the format: `projects/{project}/findings/{finding}`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_findings_get_execute()` to send, or `threatintelligence_projects_findings_get` for simplest API.

pub fn threatintelligence_projects_findings_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/findings/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/findings/{findingsId}
/// Get a finding by name. The name field should have the format: `projects/{project}/findings/{finding}`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_findings_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_findings_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_findings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Finding = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/findings/{findingsId}
/// Get a finding by name. The name field should have the format: `projects/{project}/findings/{finding}`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_findings_get_builder()` + `threatintelligence_projects_findings_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_findings_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Finding>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_findings_get_builder(client, name)?;
    threatintelligence_projects_findings_get_execute(builder)
}

/// GET v1beta/projects/{projectsId}/findings
/// Get a list of findings that meet the filter criteria. The parent field in ListFindingsRequest should have the format: `projects/{project}`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_findings_list_execute()` to send, or `threatintelligence_projects_findings_list` for simplest API.

pub fn threatintelligence_projects_findings_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/findings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/findings
/// Get a list of findings that meet the filter criteria. The parent field in ListFindingsRequest should have the format: `projects/{project}`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_findings_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_findings_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_findings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/findings
/// Get a list of findings that meet the filter criteria. The parent field in ListFindingsRequest should have the format: `projects/{project}`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_findings_list_builder()` + `threatintelligence_projects_findings_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_findings_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_findings_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    threatintelligence_projects_findings_list_execute(builder)
}

/// GET v1beta/projects/{projectsId}/findings:search
/// SearchFindings is a more powerful version of ListFindings that supports complex queries like "findings for alerts" using functions such as has_alert in the query string. The parent field in SearchFindingsRequest should have the format: `projects/{project}` Example to search for findings for a specific issue: has_alert("name=\"`projects/gti-12345/alerts/alert-12345`\"")
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `threatintelligence_projects_findings_search_execute()` to send, or `threatintelligence_projects_findings_search` for simplest API.

pub fn threatintelligence_projects_findings_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{}/findings:search",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/findings:search
/// SearchFindings is a more powerful version of ListFindings that supports complex queries like "findings for alerts" using functions such as has_alert in the query string. The parent field in SearchFindingsRequest should have the format: `projects/{project}` Example to search for findings for a specific issue: has_alert("name=\"`projects/gti-12345/alerts/alert-12345`\"")
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `threatintelligence_projects_findings_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from threatintelligence_projects_findings_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_findings_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchFindingsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta/projects/{projectsId}/findings:search
/// SearchFindings is a more powerful version of ListFindings that supports complex queries like "findings for alerts" using functions such as has_alert in the query string. The parent field in SearchFindingsRequest should have the format: `projects/{project}` Example to search for findings for a specific issue: has_alert("name=\"`projects/gti-12345/alerts/alert-12345`\"")
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `threatintelligence_projects_findings_search_builder()` + `threatintelligence_projects_findings_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn threatintelligence_projects_findings_search(
    client: &SimpleHttpClient,
    parent: &str,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchFindingsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = threatintelligence_projects_findings_search_builder(
        client, parent, orderBy, pageSize, pageToken, query,
    )?;
    threatintelligence_projects_findings_search_execute(builder)
}
