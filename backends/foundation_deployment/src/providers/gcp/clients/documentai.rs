//! Auto-generated API clients for gcp/documentai.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// DELETE v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_operations_delete_execute()` to send, or `documentai_operations_delete` for simplest API.

pub fn documentai_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://documentai.googleapis.com/v1/operations/{}", name,);

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_operations_delete_execute()` or `documentai_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_operations_delete_task()`.
/// For the simplest API, use `documentai_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = documentai_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_operations_delete_builder()` + `documentai_operations_delete_execute()`.
/// For task-level control, use `documentai_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_operations_delete(
    client: &SimpleHttpClient,
    args: &DocumentaiOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_operations_delete_builder(client, &args.name)?;
    documentai_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:fetchProcessorTypes
/// Fetches processor types. Note that we don''t use ListProcessorTypes here, because it isn''t paginated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_fetch_processor_types_execute()` to send, or `documentai_projects_locations_fetch_processor_types` for simplest API.

pub fn documentai_projects_locations_fetch_processor_types_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}:fetchProcessorTypes",
        parent,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:fetchProcessorTypes
/// Fetches processor types. Note that we don''t use ListProcessorTypes here, because it isn''t paginated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_fetch_processor_types_execute()` or `documentai_projects_locations_fetch_processor_types`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_fetch_processor_types_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_fetch_processor_types_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDocumentaiV1FetchProcessorTypesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1FetchProcessorTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:fetchProcessorTypes
/// Fetches processor types. Note that we don''t use ListProcessorTypes here, because it isn''t paginated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_fetch_processor_types_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_fetch_processor_types_task()`.
/// For the simplest API, use `documentai_projects_locations_fetch_processor_types()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_fetch_processor_types_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_fetch_processor_types_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1FetchProcessorTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_fetch_processor_types_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_fetch_processor_types`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsFetchProcessorTypesArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:fetchProcessorTypes
/// Fetches processor types. Note that we don''t use ListProcessorTypes here, because it isn''t paginated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_fetch_processor_types_builder()` + `documentai_projects_locations_fetch_processor_types_execute()`.
/// For task-level control, use `documentai_projects_locations_fetch_processor_types_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_fetch_processor_types(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsFetchProcessorTypesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1FetchProcessorTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_fetch_processor_types_builder(client, &args.parent)?;
    documentai_projects_locations_fetch_processor_types_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_get_execute()` to send, or `documentai_projects_locations_get` for simplest API.

pub fn documentai_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_get_execute()` or `documentai_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudLocationLocation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudLocationLocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_get_task()`.
/// For the simplest API, use `documentai_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationLocation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_get_builder()` + `documentai_projects_locations_get_execute()`.
/// For task-level control, use `documentai_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationLocation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_get_builder(client, &args.name)?;
    documentai_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_list_execute()` to send, or `documentai_projects_locations_list` for simplest API.

pub fn documentai_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_list_execute()` or `documentai_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudLocationListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudLocationListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_list_task()`.
/// For the simplest API, use `documentai_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationListLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_list_builder()` + `documentai_projects_locations_list_execute()`.
/// For task-level control, use `documentai_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationListLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    documentai_projects_locations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_operations_cancel_execute()` to send, or `documentai_projects_locations_operations_cancel` for simplest API.

pub fn documentai_projects_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_operations_cancel_execute()` or `documentai_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `documentai_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_operations_cancel_builder()` + `documentai_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `documentai_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_operations_cancel_builder(client, &args.name)?;
    documentai_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_operations_get_execute()` to send, or `documentai_projects_locations_operations_get` for simplest API.

pub fn documentai_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_operations_get_execute()` or `documentai_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_operations_get_task()`.
/// For the simplest API, use `documentai_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_operations_get_builder()` + `documentai_projects_locations_operations_get_execute()`.
/// For task-level control, use `documentai_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_operations_get_builder(client, &args.name)?;
    documentai_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_operations_list_execute()` to send, or `documentai_projects_locations_operations_list` for simplest API.

pub fn documentai_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_operations_list_execute()` or `documentai_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_operations_list_task()`.
/// For the simplest API, use `documentai_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_operations_list_builder()` + `documentai_projects_locations_operations_list_execute()`.
/// For task-level control, use `documentai_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    documentai_projects_locations_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processorTypes/{processorTypesId}
/// Gets a processor type detail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processor_types_get_execute()` to send, or `documentai_projects_locations_processor_types_get` for simplest API.

pub fn documentai_projects_locations_processor_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processorTypes/{processorTypesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processorTypes/{processorTypesId}
/// Gets a processor type detail.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processor_types_get_execute()` or `documentai_projects_locations_processor_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processor_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processor_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessorType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ProcessorType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processorTypes/{processorTypesId}
/// Gets a processor type detail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processor_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processor_types_get_task()`.
/// For the simplest API, use `documentai_projects_locations_processor_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processor_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processor_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessorType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processor_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processor_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorTypesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processorTypes/{processorTypesId}
/// Gets a processor type detail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processor_types_get_builder()` + `documentai_projects_locations_processor_types_get_execute()`.
/// For task-level control, use `documentai_projects_locations_processor_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processor_types_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorTypesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessorType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processor_types_get_builder(client, &args.name)?;
    documentai_projects_locations_processor_types_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processorTypes
/// Lists the processor types that exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processor_types_list_execute()` to send, or `documentai_projects_locations_processor_types_list` for simplest API.

pub fn documentai_projects_locations_processor_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processorTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processorTypes
/// Lists the processor types that exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processor_types_list_execute()` or `documentai_projects_locations_processor_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processor_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processor_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDocumentaiV1ListProcessorTypesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ListProcessorTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processorTypes
/// Lists the processor types that exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processor_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processor_types_list_task()`.
/// For the simplest API, use `documentai_projects_locations_processor_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processor_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processor_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListProcessorTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processor_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processor_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processorTypes
/// Lists the processor types that exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processor_types_list_builder()` + `documentai_projects_locations_processor_types_list_execute()`.
/// For task-level control, use `documentai_projects_locations_processor_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processor_types_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListProcessorTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processor_types_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    documentai_projects_locations_processor_types_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:batchProcess
/// LRO endpoint to batch process many documents. The output is written to Cloud Storage as JSON in the [Document] format.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_batch_process_execute()` to send, or `documentai_projects_locations_processors_batch_process` for simplest API.

pub fn documentai_projects_locations_processors_batch_process_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}:batchProcess",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:batchProcess
/// LRO endpoint to batch process many documents. The output is written to Cloud Storage as JSON in the [Document] format.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_batch_process_execute()` or `documentai_projects_locations_processors_batch_process`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_batch_process_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_batch_process_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:batchProcess
/// LRO endpoint to batch process many documents. The output is written to Cloud Storage as JSON in the [Document] format.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_batch_process_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_batch_process_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_batch_process()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_batch_process_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_batch_process_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_batch_process_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_batch_process`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsBatchProcessArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:batchProcess
/// LRO endpoint to batch process many documents. The output is written to Cloud Storage as JSON in the [Document] format.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_batch_process_builder()` + `documentai_projects_locations_processors_batch_process_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_batch_process_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_batch_process(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsBatchProcessArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_processors_batch_process_builder(client, &args.name)?;
    documentai_projects_locations_processors_batch_process_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors
/// Creates a processor from the ProcessorType provided. The processor will be at ENABLED state by default after its creation. Note that this method requires the documentai.processors.create permission on the project, which is highly privileged. A user or service account with this permission can create new processors that can interact with any gcs bucket in your project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_create_execute()` to send, or `documentai_projects_locations_processors_create` for simplest API.

pub fn documentai_projects_locations_processors_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors
/// Creates a processor from the ProcessorType provided. The processor will be at ENABLED state by default after its creation. Note that this method requires the documentai.processors.create permission on the project, which is highly privileged. A user or service account with this permission can create new processors that can interact with any gcs bucket in your project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_create_execute()` or `documentai_projects_locations_processors_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1Processor>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1Processor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors
/// Creates a processor from the ProcessorType provided. The processor will be at ENABLED state by default after its creation. Note that this method requires the documentai.processors.create permission on the project, which is highly privileged. A user or service account with this permission can create new processors that can interact with any gcs bucket in your project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_create_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1Processor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors
/// Creates a processor from the ProcessorType provided. The processor will be at ENABLED state by default after its creation. Note that this method requires the documentai.processors.create permission on the project, which is highly privileged. A user or service account with this permission can create new processors that can interact with any gcs bucket in your project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_create_builder()` + `documentai_projects_locations_processors_create_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_create(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1Processor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_create_builder(client, &args.parent)?;
    documentai_projects_locations_processors_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}
/// Deletes the processor, unloads all deployed model artifacts if it was enabled and then deletes all artifacts associated with this processor.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_delete_execute()` to send, or `documentai_projects_locations_processors_delete` for simplest API.

pub fn documentai_projects_locations_processors_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}
/// Deletes the processor, unloads all deployed model artifacts if it was enabled and then deletes all artifacts associated with this processor.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_delete_execute()` or `documentai_projects_locations_processors_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}
/// Deletes the processor, unloads all deployed model artifacts if it was enabled and then deletes all artifacts associated with this processor.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_delete_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}
/// Deletes the processor, unloads all deployed model artifacts if it was enabled and then deletes all artifacts associated with this processor.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_delete_builder()` + `documentai_projects_locations_processors_delete_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_delete(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_delete_builder(client, &args.name)?;
    documentai_projects_locations_processors_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:disable
/// Disables a processor
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_disable_execute()` to send, or `documentai_projects_locations_processors_disable` for simplest API.

pub fn documentai_projects_locations_processors_disable_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}:disable",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:disable
/// Disables a processor
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_disable_execute()` or `documentai_projects_locations_processors_disable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_disable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_disable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:disable
/// Disables a processor
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_disable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_disable_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_disable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_disable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_disable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_disable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_disable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsDisableArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:disable
/// Disables a processor
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_disable_builder()` + `documentai_projects_locations_processors_disable_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_disable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_disable(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsDisableArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_disable_builder(client, &args.name)?;
    documentai_projects_locations_processors_disable_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:enable
/// Enables a processor
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_enable_execute()` to send, or `documentai_projects_locations_processors_enable` for simplest API.

pub fn documentai_projects_locations_processors_enable_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}:enable",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:enable
/// Enables a processor
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_enable_execute()` or `documentai_projects_locations_processors_enable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_enable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_enable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:enable
/// Enables a processor
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_enable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_enable_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_enable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_enable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_enable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_enable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_enable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsEnableArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:enable
/// Enables a processor
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_enable_builder()` + `documentai_projects_locations_processors_enable_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_enable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_enable(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsEnableArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_enable_builder(client, &args.name)?;
    documentai_projects_locations_processors_enable_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}
/// Gets a processor detail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_get_execute()` to send, or `documentai_projects_locations_processors_get` for simplest API.

pub fn documentai_projects_locations_processors_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}
/// Gets a processor detail.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_get_execute()` or `documentai_projects_locations_processors_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1Processor>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1Processor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}
/// Gets a processor detail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_get_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1Processor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}
/// Gets a processor detail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_get_builder()` + `documentai_projects_locations_processors_get_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1Processor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_get_builder(client, &args.name)?;
    documentai_projects_locations_processors_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors
/// Lists all processors which belong to this project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_list_execute()` to send, or `documentai_projects_locations_processors_list` for simplest API.

pub fn documentai_projects_locations_processors_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors
/// Lists all processors which belong to this project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_list_execute()` or `documentai_projects_locations_processors_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1ListProcessorsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ListProcessorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors
/// Lists all processors which belong to this project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_list_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListProcessorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors
/// Lists all processors which belong to this project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_list_builder()` + `documentai_projects_locations_processors_list_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListProcessorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    documentai_projects_locations_processors_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:process
/// Processes a single document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_process_execute()` to send, or `documentai_projects_locations_processors_process` for simplest API.

pub fn documentai_projects_locations_processors_process_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}:process",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:process
/// Processes a single document.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_process_execute()` or `documentai_projects_locations_processors_process`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_process_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_process_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ProcessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:process
/// Processes a single document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_process_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_process_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_process()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_process_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_process_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_process_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_process`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:process
/// Processes a single document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_process_builder()` + `documentai_projects_locations_processors_process_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_process_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_process(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_process_builder(client, &args.name)?;
    documentai_projects_locations_processors_process_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:setDefaultProcessorVersion
/// Set the default (active) version of a Processor that will be used in ProcessDocument and BatchProcessDocuments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_set_default_processor_version_execute()` to send, or `documentai_projects_locations_processors_set_default_processor_version` for simplest API.

pub fn documentai_projects_locations_processors_set_default_processor_version_builder<R>(
    client: &SimpleHttpClient<R>,
    processor: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}:setDefaultProcessorVersion",
        processor,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:setDefaultProcessorVersion
/// Set the default (active) version of a Processor that will be used in ProcessDocument and BatchProcessDocuments.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_set_default_processor_version_execute()` or `documentai_projects_locations_processors_set_default_processor_version`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_set_default_processor_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_set_default_processor_version_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:setDefaultProcessorVersion
/// Set the default (active) version of a Processor that will be used in ProcessDocument and BatchProcessDocuments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_set_default_processor_version_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_set_default_processor_version_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_set_default_processor_version()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_set_default_processor_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_set_default_processor_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        documentai_projects_locations_processors_set_default_processor_version_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_set_default_processor_version`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsSetDefaultProcessorVersionArgs {
    /// Path parameter: processor
    pub processor: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:setDefaultProcessorVersion
/// Set the default (active) version of a Processor that will be used in ProcessDocument and BatchProcessDocuments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_set_default_processor_version_builder()` + `documentai_projects_locations_processors_set_default_processor_version_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_set_default_processor_version_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_set_default_processor_version(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsSetDefaultProcessorVersionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_set_default_processor_version_builder(
        client,
        &args.processor,
    )?;
    documentai_projects_locations_processors_set_default_processor_version_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/humanReviewConfig:reviewDocument
/// Send a document for Human Review. The input document should be processed by the specified processor.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_human_review_config_review_document_execute()` to send, or `documentai_projects_locations_processors_human_review_config_review_document` for simplest API.

pub fn documentai_projects_locations_processors_human_review_config_review_document_builder<R>(
    client: &SimpleHttpClient<R>,
    humanReviewConfig: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}/humanReviewConfig:reviewDocument",
        humanReviewConfig,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/humanReviewConfig:reviewDocument
/// Send a document for Human Review. The input document should be processed by the specified processor.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_human_review_config_review_document_execute()` or `documentai_projects_locations_processors_human_review_config_review_document`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_human_review_config_review_document_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_human_review_config_review_document_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/humanReviewConfig:reviewDocument
/// Send a document for Human Review. The input document should be processed by the specified processor.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_human_review_config_review_document_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_human_review_config_review_document_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_human_review_config_review_document()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_human_review_config_review_document_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_human_review_config_review_document_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        documentai_projects_locations_processors_human_review_config_review_document_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_human_review_config_review_document`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsHumanReviewConfigReviewDocumentArgs {
    /// Path parameter: humanReviewConfig
    pub humanReviewConfig: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/humanReviewConfig:reviewDocument
/// Send a document for Human Review. The input document should be processed by the specified processor.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_human_review_config_review_document_builder()` + `documentai_projects_locations_processors_human_review_config_review_document_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_human_review_config_review_document_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_human_review_config_review_document(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsHumanReviewConfigReviewDocumentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_processors_human_review_config_review_document_builder(
            client,
            &args.humanReviewConfig,
        )?;
    documentai_projects_locations_processors_human_review_config_review_document_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:batchProcess
/// LRO endpoint to batch process many documents. The output is written to Cloud Storage as JSON in the [Document] format.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_batch_process_execute()` to send, or `documentai_projects_locations_processors_processor_versions_batch_process` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_batch_process_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:batchProcess",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:batchProcess
/// LRO endpoint to batch process many documents. The output is written to Cloud Storage as JSON in the [Document] format.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_processor_versions_batch_process_execute()` or `documentai_projects_locations_processors_processor_versions_batch_process`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_batch_process_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_batch_process_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:batchProcess
/// LRO endpoint to batch process many documents. The output is written to Cloud Storage as JSON in the [Document] format.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_processor_versions_batch_process_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_batch_process_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_processor_versions_batch_process()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_batch_process_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_processor_versions_batch_process_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        documentai_projects_locations_processors_processor_versions_batch_process_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_batch_process`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsBatchProcessArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:batchProcess
/// LRO endpoint to batch process many documents. The output is written to Cloud Storage as JSON in the [Document] format.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_batch_process_builder()` + `documentai_projects_locations_processors_processor_versions_batch_process_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_batch_process_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_batch_process(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsBatchProcessArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_processors_processor_versions_batch_process_builder(
            client, &args.name,
        )?;
    documentai_projects_locations_processors_processor_versions_batch_process_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}
/// Deletes the processor version, all artifacts under the processor version will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_delete_execute()` to send, or `documentai_projects_locations_processors_processor_versions_delete` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}
/// Deletes the processor version, all artifacts under the processor version will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_processor_versions_delete_execute()` or `documentai_projects_locations_processors_processor_versions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}
/// Deletes the processor version, all artifacts under the processor version will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_processor_versions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_delete_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_processor_versions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_processor_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_processor_versions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}
/// Deletes the processor version, all artifacts under the processor version will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_delete_builder()` + `documentai_projects_locations_processors_processor_versions_delete_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_delete(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_delete_builder(
        client, &args.name,
    )?;
    documentai_projects_locations_processors_processor_versions_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:deploy
/// Deploys the processor version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_deploy_execute()` to send, or `documentai_projects_locations_processors_processor_versions_deploy` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_deploy_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:deploy",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:deploy
/// Deploys the processor version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_processor_versions_deploy_execute()` or `documentai_projects_locations_processors_processor_versions_deploy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_deploy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:deploy
/// Deploys the processor version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_processor_versions_deploy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_deploy_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_processor_versions_deploy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_processor_versions_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_processor_versions_deploy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_deploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsDeployArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:deploy
/// Deploys the processor version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_deploy_builder()` + `documentai_projects_locations_processors_processor_versions_deploy_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_deploy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_deploy(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsDeployArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_deploy_builder(
        client, &args.name,
    )?;
    documentai_projects_locations_processors_processor_versions_deploy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:evaluateProcessorVersion
/// Evaluates a ProcessorVersion against annotated documents, producing an Evaluation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_evaluate_processor_version_execute()` to send, or `documentai_projects_locations_processors_processor_versions_evaluate_processor_version` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_evaluate_processor_version_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    processorVersion: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:evaluateProcessorVersion",
        processorVersion,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:evaluateProcessorVersion
/// Evaluates a ProcessorVersion against annotated documents, producing an Evaluation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_processor_versions_evaluate_processor_version_execute()` or `documentai_projects_locations_processors_processor_versions_evaluate_processor_version`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_evaluate_processor_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_evaluate_processor_version_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:evaluateProcessorVersion
/// Evaluates a ProcessorVersion against annotated documents, producing an Evaluation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_processor_versions_evaluate_processor_version_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_evaluate_processor_version_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_processor_versions_evaluate_processor_version()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_evaluate_processor_version_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_processor_versions_evaluate_processor_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_processor_versions_evaluate_processor_version_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_evaluate_processor_version`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluateProcessorVersionArgs {
    /// Path parameter: processorVersion
    pub processorVersion: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:evaluateProcessorVersion
/// Evaluates a ProcessorVersion against annotated documents, producing an Evaluation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_evaluate_processor_version_builder()` + `documentai_projects_locations_processors_processor_versions_evaluate_processor_version_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_evaluate_processor_version_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_evaluate_processor_version(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluateProcessorVersionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_evaluate_processor_version_builder(client, &args.processorVersion)?;
    documentai_projects_locations_processors_processor_versions_evaluate_processor_version_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}
/// Gets a processor version detail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_get_execute()` to send, or `documentai_projects_locations_processors_processor_versions_get` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}
/// Gets a processor version detail.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_processor_versions_get_execute()` or `documentai_projects_locations_processors_processor_versions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessorVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ProcessorVersion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}
/// Gets a processor version detail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_processor_versions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_get_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_processor_versions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_processor_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessorVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_processor_versions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}
/// Gets a processor version detail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_get_builder()` + `documentai_projects_locations_processors_processor_versions_get_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessorVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_get_builder(
        client, &args.name,
    )?;
    documentai_projects_locations_processors_processor_versions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions
/// Lists all versions of a processor.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_list_execute()` to send, or `documentai_projects_locations_processors_processor_versions_list` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}/processorVersions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions
/// Lists all versions of a processor.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_processor_versions_list_execute()` or `documentai_projects_locations_processors_processor_versions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDocumentaiV1ListProcessorVersionsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ListProcessorVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions
/// Lists all versions of a processor.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_processor_versions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_list_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_processor_versions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_processor_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListProcessorVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_processor_versions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions
/// Lists all versions of a processor.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_list_builder()` + `documentai_projects_locations_processors_processor_versions_list_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListProcessorVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    documentai_projects_locations_processors_processor_versions_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:process
/// Processes a single document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_process_execute()` to send, or `documentai_projects_locations_processors_processor_versions_process` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_process_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:process",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:process
/// Processes a single document.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_processor_versions_process_execute()` or `documentai_projects_locations_processors_processor_versions_process`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_process_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_process_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ProcessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:process
/// Processes a single document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_processor_versions_process_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_process_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_processor_versions_process()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_process_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_processor_versions_process_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_processor_versions_process_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_process`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsProcessArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:process
/// Processes a single document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_process_builder()` + `documentai_projects_locations_processors_processor_versions_process_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_process_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_process(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsProcessArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_process_builder(
        client, &args.name,
    )?;
    documentai_projects_locations_processors_processor_versions_process_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions:train
/// Trains a new processor version. Operation metadata is returned as TrainProcessorVersionMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_train_execute()` to send, or `documentai_projects_locations_processors_processor_versions_train` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_train_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}/processorVersions:train",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions:train
/// Trains a new processor version. Operation metadata is returned as TrainProcessorVersionMetadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_processor_versions_train_execute()` or `documentai_projects_locations_processors_processor_versions_train`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_train_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_train_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions:train
/// Trains a new processor version. Operation metadata is returned as TrainProcessorVersionMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_processor_versions_train_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_train_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_processor_versions_train()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_train_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_processor_versions_train_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_processor_versions_train_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_train`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsTrainArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions:train
/// Trains a new processor version. Operation metadata is returned as TrainProcessorVersionMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_train_builder()` + `documentai_projects_locations_processors_processor_versions_train_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_train_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_train(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsTrainArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_train_builder(
        client,
        &args.parent,
    )?;
    documentai_projects_locations_processors_processor_versions_train_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:undeploy
/// Undeploys the processor version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_undeploy_execute()` to send, or `documentai_projects_locations_processors_processor_versions_undeploy` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_undeploy_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:undeploy",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:undeploy
/// Undeploys the processor version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_processor_versions_undeploy_execute()` or `documentai_projects_locations_processors_processor_versions_undeploy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_undeploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_undeploy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:undeploy
/// Undeploys the processor version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_processor_versions_undeploy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_undeploy_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_processor_versions_undeploy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_undeploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_processor_versions_undeploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_processors_processor_versions_undeploy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_undeploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsUndeployArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:undeploy
/// Undeploys the processor version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_undeploy_builder()` + `documentai_projects_locations_processors_processor_versions_undeploy_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_undeploy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_undeploy(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsUndeployArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_undeploy_builder(
        client, &args.name,
    )?;
    documentai_projects_locations_processors_processor_versions_undeploy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations/{evaluationsId}
/// Retrieves a specific evaluation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_evaluations_get_execute()` to send, or `documentai_projects_locations_processors_processor_versions_evaluations_get` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_evaluations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations/{evaluationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations/{evaluationsId}
/// Retrieves a specific evaluation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_processor_versions_evaluations_get_execute()` or `documentai_projects_locations_processors_processor_versions_evaluations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_evaluations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_evaluations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1Evaluation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1Evaluation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations/{evaluationsId}
/// Retrieves a specific evaluation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_processor_versions_evaluations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_evaluations_get_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_processor_versions_evaluations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_evaluations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_processor_versions_evaluations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1Evaluation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        documentai_projects_locations_processors_processor_versions_evaluations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_evaluations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations/{evaluationsId}
/// Retrieves a specific evaluation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_evaluations_get_builder()` + `documentai_projects_locations_processors_processor_versions_evaluations_get_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_evaluations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_evaluations_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1Evaluation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_processors_processor_versions_evaluations_get_builder(
            client, &args.name,
        )?;
    documentai_projects_locations_processors_processor_versions_evaluations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations
/// Retrieves a set of evaluations for a given processor version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_evaluations_list_execute()` to send, or `documentai_projects_locations_processors_processor_versions_evaluations_list` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_evaluations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations
/// Retrieves a set of evaluations for a given processor version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_processors_processor_versions_evaluations_list_execute()` or `documentai_projects_locations_processors_processor_versions_evaluations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_evaluations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_evaluations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1ListEvaluationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ListEvaluationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations
/// Retrieves a set of evaluations for a given processor version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_processors_processor_versions_evaluations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_evaluations_list_task()`.
/// For the simplest API, use `documentai_projects_locations_processors_processor_versions_evaluations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_processors_processor_versions_evaluations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_processors_processor_versions_evaluations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListEvaluationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        documentai_projects_locations_processors_processor_versions_evaluations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_evaluations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations
/// Retrieves a set of evaluations for a given processor version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_evaluations_list_builder()` + `documentai_projects_locations_processors_processor_versions_evaluations_list_execute()`.
/// For task-level control, use `documentai_projects_locations_processors_processor_versions_evaluations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_evaluations_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListEvaluationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_processors_processor_versions_evaluations_list_builder(
            client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )?;
    documentai_projects_locations_processors_processor_versions_evaluations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/schemas
/// Creates a schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_create_execute()` to send, or `documentai_projects_locations_schemas_create` for simplest API.

pub fn documentai_projects_locations_schemas_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/schemas",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/schemas
/// Creates a schema.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_schemas_create_execute()` or `documentai_projects_locations_schemas_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1NextSchema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/schemas
/// Creates a schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_schemas_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_schemas_create_task()`.
/// For the simplest API, use `documentai_projects_locations_schemas_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_schemas_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_schemas_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/schemas
/// Creates a schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_create_builder()` + `documentai_projects_locations_schemas_create_execute()`.
/// For task-level control, use `documentai_projects_locations_schemas_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_create(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_schemas_create_builder(client, &args.parent)?;
    documentai_projects_locations_schemas_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Deletes a schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_delete_execute()` to send, or `documentai_projects_locations_schemas_delete` for simplest API.

pub fn documentai_projects_locations_schemas_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    force: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/schemas/{schemasId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Deletes a schema.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_schemas_delete_execute()` or `documentai_projects_locations_schemas_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Deletes a schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_schemas_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_schemas_delete_task()`.
/// For the simplest API, use `documentai_projects_locations_schemas_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_schemas_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_schemas_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Deletes a schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_delete_builder()` + `documentai_projects_locations_schemas_delete_execute()`.
/// For task-level control, use `documentai_projects_locations_schemas_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_delete(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_schemas_delete_builder(client, &args.name, &args.force)?;
    documentai_projects_locations_schemas_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Gets a schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_get_execute()` to send, or `documentai_projects_locations_schemas_get` for simplest API.

pub fn documentai_projects_locations_schemas_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/schemas/{schemasId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Gets a schema.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_schemas_get_execute()` or `documentai_projects_locations_schemas_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1NextSchema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Gets a schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_schemas_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_schemas_get_task()`.
/// For the simplest API, use `documentai_projects_locations_schemas_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_schemas_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_schemas_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Gets a schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_get_builder()` + `documentai_projects_locations_schemas_get_execute()`.
/// For task-level control, use `documentai_projects_locations_schemas_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_schemas_get_builder(client, &args.name)?;
    documentai_projects_locations_schemas_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas
/// Lists Schemas.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_list_execute()` to send, or `documentai_projects_locations_schemas_list` for simplest API.

pub fn documentai_projects_locations_schemas_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/schemas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas
/// Lists Schemas.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_schemas_list_execute()` or `documentai_projects_locations_schemas_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1ListSchemasResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ListSchemasResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas
/// Lists Schemas.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_schemas_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_schemas_list_task()`.
/// For the simplest API, use `documentai_projects_locations_schemas_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_schemas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListSchemasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_schemas_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas
/// Lists Schemas.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_list_builder()` + `documentai_projects_locations_schemas_list_execute()`.
/// For task-level control, use `documentai_projects_locations_schemas_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListSchemasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_schemas_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    documentai_projects_locations_schemas_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Updates a schema. Editable fields are: - display_name - labels
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_patch_execute()` to send, or `documentai_projects_locations_schemas_patch` for simplest API.

pub fn documentai_projects_locations_schemas_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/schemas/{schemasId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Updates a schema. Editable fields are: - display_name - labels
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_schemas_patch_execute()` or `documentai_projects_locations_schemas_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1NextSchema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Updates a schema. Editable fields are: - display_name - labels
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_schemas_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_schemas_patch_task()`.
/// For the simplest API, use `documentai_projects_locations_schemas_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_schemas_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_schemas_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Updates a schema. Editable fields are: - display_name - labels
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_patch_builder()` + `documentai_projects_locations_schemas_patch_execute()`.
/// For task-level control, use `documentai_projects_locations_schemas_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_patch(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_schemas_patch_builder(client, &args.name, &args.updateMask)?;
    documentai_projects_locations_schemas_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions
/// Creates a schema version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_schema_versions_create_execute()` to send, or `documentai_projects_locations_schemas_schema_versions_create` for simplest API.

pub fn documentai_projects_locations_schemas_schema_versions_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/schemas/{schemasId}/schemaVersions",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions
/// Creates a schema version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_schemas_schema_versions_create_execute()` or `documentai_projects_locations_schemas_schema_versions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_schema_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1SchemaVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions
/// Creates a schema version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_schemas_schema_versions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_schemas_schema_versions_create_task()`.
/// For the simplest API, use `documentai_projects_locations_schemas_schema_versions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_schema_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_schemas_schema_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_schemas_schema_versions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_schema_versions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasSchemaVersionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions
/// Creates a schema version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_create_builder()` + `documentai_projects_locations_schemas_schema_versions_create_execute()`.
/// For task-level control, use `documentai_projects_locations_schemas_schema_versions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_create(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasSchemaVersionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_schemas_schema_versions_create_builder(client, &args.parent)?;
    documentai_projects_locations_schemas_schema_versions_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Deletes a schema version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_schema_versions_delete_execute()` to send, or `documentai_projects_locations_schemas_schema_versions_delete` for simplest API.

pub fn documentai_projects_locations_schemas_schema_versions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Deletes a schema version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_schemas_schema_versions_delete_execute()` or `documentai_projects_locations_schemas_schema_versions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_schema_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Deletes a schema version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_schemas_schema_versions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_schemas_schema_versions_delete_task()`.
/// For the simplest API, use `documentai_projects_locations_schemas_schema_versions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_schema_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_schemas_schema_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_schemas_schema_versions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_schema_versions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasSchemaVersionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Deletes a schema version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_delete_builder()` + `documentai_projects_locations_schemas_schema_versions_delete_execute()`.
/// For task-level control, use `documentai_projects_locations_schemas_schema_versions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_delete(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasSchemaVersionsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_schemas_schema_versions_delete_builder(client, &args.name)?;
    documentai_projects_locations_schemas_schema_versions_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions:generate
/// Generates a schema version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_schema_versions_generate_execute()` to send, or `documentai_projects_locations_schemas_schema_versions_generate` for simplest API.

pub fn documentai_projects_locations_schemas_schema_versions_generate_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/schemas/{schemasId}/schemaVersions:generate",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions:generate
/// Generates a schema version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_schemas_schema_versions_generate_execute()` or `documentai_projects_locations_schemas_schema_versions_generate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_schema_versions_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_generate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDocumentaiV1GenerateSchemaVersionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1GenerateSchemaVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions:generate
/// Generates a schema version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_schemas_schema_versions_generate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_schemas_schema_versions_generate_task()`.
/// For the simplest API, use `documentai_projects_locations_schemas_schema_versions_generate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_schema_versions_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_schemas_schema_versions_generate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1GenerateSchemaVersionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_schemas_schema_versions_generate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_schema_versions_generate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasSchemaVersionsGenerateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions:generate
/// Generates a schema version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_generate_builder()` + `documentai_projects_locations_schemas_schema_versions_generate_execute()`.
/// For task-level control, use `documentai_projects_locations_schemas_schema_versions_generate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_generate(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasSchemaVersionsGenerateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1GenerateSchemaVersionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_schemas_schema_versions_generate_builder(
        client,
        &args.parent,
    )?;
    documentai_projects_locations_schemas_schema_versions_generate_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Gets a schema version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_schema_versions_get_execute()` to send, or `documentai_projects_locations_schemas_schema_versions_get` for simplest API.

pub fn documentai_projects_locations_schemas_schema_versions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Gets a schema version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_schemas_schema_versions_get_execute()` or `documentai_projects_locations_schemas_schema_versions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_schema_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1SchemaVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Gets a schema version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_schemas_schema_versions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_schemas_schema_versions_get_task()`.
/// For the simplest API, use `documentai_projects_locations_schemas_schema_versions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_schema_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_schemas_schema_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_schemas_schema_versions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_schema_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasSchemaVersionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Gets a schema version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_get_builder()` + `documentai_projects_locations_schemas_schema_versions_get_execute()`.
/// For task-level control, use `documentai_projects_locations_schemas_schema_versions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasSchemaVersionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_schemas_schema_versions_get_builder(client, &args.name)?;
    documentai_projects_locations_schemas_schema_versions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions
/// Lists SchemaVersions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_schema_versions_list_execute()` to send, or `documentai_projects_locations_schemas_schema_versions_list` for simplest API.

pub fn documentai_projects_locations_schemas_schema_versions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/schemas/{schemasId}/schemaVersions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions
/// Lists SchemaVersions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_schemas_schema_versions_list_execute()` or `documentai_projects_locations_schemas_schema_versions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_schema_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDocumentaiV1ListSchemaVersionsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ListSchemaVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions
/// Lists SchemaVersions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_schemas_schema_versions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_schemas_schema_versions_list_task()`.
/// For the simplest API, use `documentai_projects_locations_schemas_schema_versions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_schema_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_schemas_schema_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListSchemaVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_schemas_schema_versions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_schema_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasSchemaVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions
/// Lists SchemaVersions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_list_builder()` + `documentai_projects_locations_schemas_schema_versions_list_execute()`.
/// For task-level control, use `documentai_projects_locations_schemas_schema_versions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasSchemaVersionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListSchemaVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_schemas_schema_versions_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    documentai_projects_locations_schemas_schema_versions_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Updates a schema version. Editable fields are: - display_name - labels
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_schema_versions_patch_execute()` to send, or `documentai_projects_locations_schemas_schema_versions_patch` for simplest API.

pub fn documentai_projects_locations_schemas_schema_versions_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Updates a schema version. Editable fields are: - display_name - labels
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_locations_schemas_schema_versions_patch_execute()` or `documentai_projects_locations_schemas_schema_versions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_schema_versions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1SchemaVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Updates a schema version. Editable fields are: - display_name - labels
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_locations_schemas_schema_versions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_locations_schemas_schema_versions_patch_task()`.
/// For the simplest API, use `documentai_projects_locations_schemas_schema_versions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_locations_schemas_schema_versions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_locations_schemas_schema_versions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_locations_schemas_schema_versions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_schema_versions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasSchemaVersionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Updates a schema version. Editable fields are: - display_name - labels
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_patch_builder()` + `documentai_projects_locations_schemas_schema_versions_patch_execute()`.
/// For task-level control, use `documentai_projects_locations_schemas_schema_versions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_patch(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasSchemaVersionsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_schemas_schema_versions_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    documentai_projects_locations_schemas_schema_versions_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_operations_get_execute()` to send, or `documentai_projects_operations_get` for simplest API.

pub fn documentai_projects_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `documentai_projects_operations_get_execute()` or `documentai_projects_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `documentai_projects_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `documentai_projects_operations_get_task()`.
/// For the simplest API, use `documentai_projects_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `documentai_projects_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn documentai_projects_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = documentai_projects_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_operations_get_builder()` + `documentai_projects_operations_get_execute()`.
/// For task-level control, use `documentai_projects_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_operations_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_operations_get_builder(client, &args.name)?;
    documentai_projects_operations_get_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DocumentaiOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiOperationsDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DocumentaiOperationsDeleteArgs) -> String {
        format!("gcp::documentai::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1FetchProcessorTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1FetchProcessorTypesResponse with DocumentaiProjectsLocationsFetchProcessorTypesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsFetchProcessorTypesArgs>
    for GoogleCloudDocumentaiV1FetchProcessorTypesResponse
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsFetchProcessorTypesArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1FetchProcessorTypesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1FetchProcessorTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudLocationLocation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudLocationLocation with DocumentaiProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsGetArgs> for GoogleCloudLocationLocation {
    fn generate_resource_id(&self, input: &DocumentaiProjectsLocationsGetArgs) -> String {
        format!(
            "gcp::documentai::GoogleCloudLocationLocation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudLocationLocation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudLocationListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudLocationListLocationsResponse with DocumentaiProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsListArgs>
    for GoogleCloudLocationListLocationsResponse
{
    fn generate_resource_id(&self, input: &DocumentaiProjectsLocationsListArgs) -> String {
        format!(
            "gcp::documentai::GoogleCloudLocationListLocationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudLocationListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DocumentaiProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsOperationsCancelArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::documentai::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsOperationsGetArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(&self, input: &DocumentaiProjectsLocationsOperationsGetArgs) -> String {
        format!("gcp::documentai::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse with DocumentaiProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsOperationsListArgs>
    for GoogleLongrunningListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsOperationsListArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleLongrunningListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ProcessorType
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ProcessorType with DocumentaiProjectsLocationsProcessorTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorTypesGetArgs>
    for GoogleCloudDocumentaiV1ProcessorType
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorTypesGetArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1ProcessorType/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1ProcessorType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ListProcessorTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ListProcessorTypesResponse with DocumentaiProjectsLocationsProcessorTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorTypesListArgs>
    for GoogleCloudDocumentaiV1ListProcessorTypesResponse
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorTypesListArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1ListProcessorTypesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1ListProcessorTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsProcessorsBatchProcessArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsBatchProcessArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsBatchProcessArgs,
    ) -> String {
        format!("gcp::documentai::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1Processor
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1Processor with DocumentaiProjectsLocationsProcessorsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsCreateArgs>
    for GoogleCloudDocumentaiV1Processor
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsCreateArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1Processor/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1Processor"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsProcessorsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsDeleteArgs,
    ) -> String {
        format!("gcp::documentai::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsProcessorsDisableArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsDisableArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsDisableArgs,
    ) -> String {
        format!("gcp::documentai::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsProcessorsEnableArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsEnableArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsEnableArgs,
    ) -> String {
        format!("gcp::documentai::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1Processor
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1Processor with DocumentaiProjectsLocationsProcessorsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsGetArgs>
    for GoogleCloudDocumentaiV1Processor
{
    fn generate_resource_id(&self, input: &DocumentaiProjectsLocationsProcessorsGetArgs) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1Processor/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1Processor"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ListProcessorsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ListProcessorsResponse with DocumentaiProjectsLocationsProcessorsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsListArgs>
    for GoogleCloudDocumentaiV1ListProcessorsResponse
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsListArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1ListProcessorsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1ListProcessorsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ProcessResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ProcessResponse with DocumentaiProjectsLocationsProcessorsProcessArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsProcessArgs>
    for GoogleCloudDocumentaiV1ProcessResponse
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsProcessArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1ProcessResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1ProcessResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsProcessorsSetDefaultProcessorVersionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsSetDefaultProcessorVersionArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsSetDefaultProcessorVersionArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleLongrunningOperation/{}",
            input.processor
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsProcessorsHumanReviewConfigReviewDocumentArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsHumanReviewConfigReviewDocumentArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsHumanReviewConfigReviewDocumentArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleLongrunningOperation/{}",
            input.humanReviewConfig
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsProcessorsProcessorVersionsBatchProcessArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsProcessorVersionsBatchProcessArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsProcessorVersionsBatchProcessArgs,
    ) -> String {
        format!("gcp::documentai::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsProcessorsProcessorVersionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsProcessorVersionsDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsProcessorVersionsDeleteArgs,
    ) -> String {
        format!("gcp::documentai::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsProcessorsProcessorVersionsDeployArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsProcessorVersionsDeployArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsProcessorVersionsDeployArgs,
    ) -> String {
        format!("gcp::documentai::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluateProcessorVersionArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluateProcessorVersionArgs,
    > for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluateProcessorVersionArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleLongrunningOperation/{}",
            input.processorVersion
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ProcessorVersion
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ProcessorVersion with DocumentaiProjectsLocationsProcessorsProcessorVersionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsProcessorVersionsGetArgs>
    for GoogleCloudDocumentaiV1ProcessorVersion
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsProcessorVersionsGetArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1ProcessorVersion/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1ProcessorVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ListProcessorVersionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ListProcessorVersionsResponse with DocumentaiProjectsLocationsProcessorsProcessorVersionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsProcessorVersionsListArgs>
    for GoogleCloudDocumentaiV1ListProcessorVersionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsProcessorVersionsListArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1ListProcessorVersionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1ListProcessorVersionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ProcessResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ProcessResponse with DocumentaiProjectsLocationsProcessorsProcessorVersionsProcessArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsProcessorVersionsProcessArgs>
    for GoogleCloudDocumentaiV1ProcessResponse
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsProcessorVersionsProcessArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1ProcessResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1ProcessResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsProcessorsProcessorVersionsTrainArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsProcessorVersionsTrainArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsProcessorVersionsTrainArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsProcessorsProcessorVersionsUndeployArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsProcessorVersionsUndeployArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsProcessorVersionsUndeployArgs,
    ) -> String {
        format!("gcp::documentai::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1Evaluation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1Evaluation with DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluationsGetArgs>
    for GoogleCloudDocumentaiV1Evaluation
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluationsGetArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1Evaluation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1Evaluation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ListEvaluationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ListEvaluationsResponse with DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluationsListArgs>
    for GoogleCloudDocumentaiV1ListEvaluationsResponse
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluationsListArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1ListEvaluationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1ListEvaluationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1NextSchema
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1NextSchema with DocumentaiProjectsLocationsSchemasCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsSchemasCreateArgs>
    for GoogleCloudDocumentaiV1NextSchema
{
    fn generate_resource_id(&self, input: &DocumentaiProjectsLocationsSchemasCreateArgs) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1NextSchema/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1NextSchema"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsSchemasDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsSchemasDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(&self, input: &DocumentaiProjectsLocationsSchemasDeleteArgs) -> String {
        format!("gcp::documentai::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1NextSchema
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1NextSchema with DocumentaiProjectsLocationsSchemasGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsSchemasGetArgs>
    for GoogleCloudDocumentaiV1NextSchema
{
    fn generate_resource_id(&self, input: &DocumentaiProjectsLocationsSchemasGetArgs) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1NextSchema/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1NextSchema"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ListSchemasResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ListSchemasResponse with DocumentaiProjectsLocationsSchemasListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsSchemasListArgs>
    for GoogleCloudDocumentaiV1ListSchemasResponse
{
    fn generate_resource_id(&self, input: &DocumentaiProjectsLocationsSchemasListArgs) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1ListSchemasResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1ListSchemasResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1NextSchema
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1NextSchema with DocumentaiProjectsLocationsSchemasPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsSchemasPatchArgs>
    for GoogleCloudDocumentaiV1NextSchema
{
    fn generate_resource_id(&self, input: &DocumentaiProjectsLocationsSchemasPatchArgs) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1NextSchema/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1NextSchema"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1SchemaVersion
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1SchemaVersion with DocumentaiProjectsLocationsSchemasSchemaVersionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsSchemasSchemaVersionsCreateArgs>
    for GoogleCloudDocumentaiV1SchemaVersion
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsSchemasSchemaVersionsCreateArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1SchemaVersion/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1SchemaVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsLocationsSchemasSchemaVersionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsSchemasSchemaVersionsDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsSchemasSchemaVersionsDeleteArgs,
    ) -> String {
        format!("gcp::documentai::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1GenerateSchemaVersionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1GenerateSchemaVersionResponse with DocumentaiProjectsLocationsSchemasSchemaVersionsGenerateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsSchemasSchemaVersionsGenerateArgs>
    for GoogleCloudDocumentaiV1GenerateSchemaVersionResponse
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsSchemasSchemaVersionsGenerateArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1GenerateSchemaVersionResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1GenerateSchemaVersionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1SchemaVersion
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1SchemaVersion with DocumentaiProjectsLocationsSchemasSchemaVersionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsSchemasSchemaVersionsGetArgs>
    for GoogleCloudDocumentaiV1SchemaVersion
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsSchemasSchemaVersionsGetArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1SchemaVersion/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1SchemaVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ListSchemaVersionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1ListSchemaVersionsResponse with DocumentaiProjectsLocationsSchemasSchemaVersionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsSchemasSchemaVersionsListArgs>
    for GoogleCloudDocumentaiV1ListSchemaVersionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsSchemasSchemaVersionsListArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1ListSchemaVersionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1ListSchemaVersionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDocumentaiV1SchemaVersion
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDocumentaiV1SchemaVersion with DocumentaiProjectsLocationsSchemasSchemaVersionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsLocationsSchemasSchemaVersionsPatchArgs>
    for GoogleCloudDocumentaiV1SchemaVersion
{
    fn generate_resource_id(
        &self,
        input: &DocumentaiProjectsLocationsSchemasSchemaVersionsPatchArgs,
    ) -> String {
        format!(
            "gcp::documentai::GoogleCloudDocumentaiV1SchemaVersion/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleCloudDocumentaiV1SchemaVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DocumentaiProjectsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DocumentaiProjectsOperationsGetArgs> for GoogleLongrunningOperation {
    fn generate_resource_id(&self, input: &DocumentaiProjectsOperationsGetArgs) -> String {
        format!("gcp::documentai::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::documentai::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
