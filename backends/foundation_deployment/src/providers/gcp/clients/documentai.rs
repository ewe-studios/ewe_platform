//! Auto-generated API clients for gcp/documentai.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/documentai`

#![cfg(feature = "gcp/documentai")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_operations_delete_execute()` to send, or `documentai_operations_delete` for simplest API.

pub fn documentai_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://documentai.googleapis.com/v1/operations/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_operations_delete_builder()` + `documentai_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_operations_delete(
    client: &SimpleHttpClient,
    args: &DocumentaiOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_operations_delete_builder(client, &args.name)?;
    documentai_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:fetchProcessorTypes
/// Fetches processor types. Note that we don't use ListProcessorTypes here, because it isn't paginated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_fetch_processor_types_execute()` to send, or `documentai_projects_locations_fetch_processor_types` for simplest API.

pub fn documentai_projects_locations_fetch_processor_types_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}:fetchProcessorTypes",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:fetchProcessorTypes
/// Fetches processor types. Note that we don't use ListProcessorTypes here, because it isn't paginated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_fetch_processor_types_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_fetch_processor_types_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_fetch_processor_types_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1FetchProcessorTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1FetchProcessorTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_fetch_processor_types`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsFetchProcessorTypesArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}:fetchProcessorTypes
/// Fetches processor types. Note that we don't use ListProcessorTypes here, because it isn't paginated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_fetch_processor_types_builder()` + `documentai_projects_locations_fetch_processor_types_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_fetch_processor_types(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsFetchProcessorTypesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1FetchProcessorTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_fetch_processor_types_builder(client, &args.parent)?;
    documentai_projects_locations_fetch_processor_types_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_get_execute()` to send, or `documentai_projects_locations_get` for simplest API.

pub fn documentai_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationLocation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudLocationLocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_get_builder()` + `documentai_projects_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationLocation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_get_builder(client, &args.name)?;
    documentai_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_list_execute()` to send, or `documentai_projects_locations_list` for simplest API.

pub fn documentai_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationListLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudLocationListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_list_builder()` + `documentai_projects_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationListLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_list_builder(
        client,
        &args.name,
        args.extraLocationTypes.as_deref(),
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    documentai_projects_locations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_operations_cancel_execute()` to send, or `documentai_projects_locations_operations_cancel` for simplest API.

pub fn documentai_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_operations_cancel_builder()` + `documentai_projects_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_operations_cancel_builder(client, &args.name)?;
    documentai_projects_locations_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_operations_get_execute()` to send, or `documentai_projects_locations_operations_get` for simplest API.

pub fn documentai_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_operations_get_builder()` + `documentai_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_operations_get_builder(client, &args.name)?;
    documentai_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_operations_list_execute()` to send, or `documentai_projects_locations_operations_list` for simplest API.

pub fn documentai_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_operations_list_builder()` + `documentai_projects_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    documentai_projects_locations_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processorTypes/{processorTypesId}
/// Gets a processor type detail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processor_types_get_execute()` to send, or `documentai_projects_locations_processor_types_get` for simplest API.

pub fn documentai_projects_locations_processor_types_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processorTypes/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processorTypes/{processorTypesId}
/// Gets a processor type detail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processor_types_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processor_types_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processor_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessorType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ProcessorType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processor_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorTypesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processorTypes/{processorTypesId}
/// Gets a processor type detail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processor_types_get_builder()` + `documentai_projects_locations_processor_types_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processor_types_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorTypesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessorType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processor_types_get_builder(client, &args.name)?;
    documentai_projects_locations_processor_types_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processorTypes
/// Lists the processor types that exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processor_types_list_execute()` to send, or `documentai_projects_locations_processor_types_list` for simplest API.

pub fn documentai_projects_locations_processor_types_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processorTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processorTypes
/// Lists the processor types that exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processor_types_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processor_types_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processor_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListProcessorTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ListProcessorTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processor_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processorTypes
/// Lists the processor types that exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processor_types_list_builder()` + `documentai_projects_locations_processor_types_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processor_types_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListProcessorTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processor_types_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    documentai_projects_locations_processor_types_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:batchProcess
/// LRO endpoint to batch process many documents. The output is written to Cloud Storage as JSON in the [Document] format.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_batch_process_execute()` to send, or `documentai_projects_locations_processors_batch_process` for simplest API.

pub fn documentai_projects_locations_processors_batch_process_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDocumentaiV1BatchProcessRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}:batchProcess",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:batchProcess
/// LRO endpoint to batch process many documents. The output is written to Cloud Storage as JSON in the [Document] format.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_batch_process_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_batch_process_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_batch_process_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_batch_process`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsBatchProcessArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1BatchProcessRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:batchProcess
/// LRO endpoint to batch process many documents. The output is written to Cloud Storage as JSON in the [Document] format.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_batch_process_builder()` + `documentai_projects_locations_processors_batch_process_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_batch_process(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsBatchProcessArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_batch_process_builder(
        client, &args.name, &args.body,
    )?;
    documentai_projects_locations_processors_batch_process_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors
/// Creates a processor from the ProcessorType provided. The processor will be at ENABLED state by default after its creation. Note that this method requires the documentai.processors.create permission on the project, which is highly privileged. A user or service account with this permission can create new processors that can interact with any gcs bucket in your project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_create_execute()` to send, or `documentai_projects_locations_processors_create` for simplest API.

pub fn documentai_projects_locations_processors_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDocumentaiV1Processor,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors
/// Creates a processor from the ProcessorType provided. The processor will be at ENABLED state by default after its creation. Note that this method requires the documentai.processors.create permission on the project, which is highly privileged. A user or service account with this permission can create new processors that can interact with any gcs bucket in your project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1Processor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1Processor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1Processor,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors
/// Creates a processor from the ProcessorType provided. The processor will be at ENABLED state by default after its creation. Note that this method requires the documentai.processors.create permission on the project, which is highly privileged. A user or service account with this permission can create new processors that can interact with any gcs bucket in your project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_create_builder()` + `documentai_projects_locations_processors_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_create(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1Processor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_processors_create_builder(client, &args.parent, &args.body)?;
    documentai_projects_locations_processors_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}
/// Deletes the processor, unloads all deployed model artifacts if it was enabled and then deletes all artifacts associated with this processor.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_delete_execute()` to send, or `documentai_projects_locations_processors_delete` for simplest API.

pub fn documentai_projects_locations_processors_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}
/// Deletes the processor, unloads all deployed model artifacts if it was enabled and then deletes all artifacts associated with this processor.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}
/// Deletes the processor, unloads all deployed model artifacts if it was enabled and then deletes all artifacts associated with this processor.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_delete_builder()` + `documentai_projects_locations_processors_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_delete(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_delete_builder(client, &args.name)?;
    documentai_projects_locations_processors_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:disable
/// Disables a processor
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_disable_execute()` to send, or `documentai_projects_locations_processors_disable` for simplest API.

pub fn documentai_projects_locations_processors_disable_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDocumentaiV1DisableProcessorRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}:disable",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:disable
/// Disables a processor
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_disable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_disable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_disable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_disable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsDisableArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1DisableProcessorRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:disable
/// Disables a processor
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_disable_builder()` + `documentai_projects_locations_processors_disable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_disable(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsDisableArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_processors_disable_builder(client, &args.name, &args.body)?;
    documentai_projects_locations_processors_disable_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:enable
/// Enables a processor
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_enable_execute()` to send, or `documentai_projects_locations_processors_enable` for simplest API.

pub fn documentai_projects_locations_processors_enable_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDocumentaiV1EnableProcessorRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}:enable",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:enable
/// Enables a processor
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_enable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_enable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_enable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_enable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsEnableArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1EnableProcessorRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:enable
/// Enables a processor
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_enable_builder()` + `documentai_projects_locations_processors_enable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_enable(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsEnableArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_processors_enable_builder(client, &args.name, &args.body)?;
    documentai_projects_locations_processors_enable_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}
/// Gets a processor detail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_get_execute()` to send, or `documentai_projects_locations_processors_get` for simplest API.

pub fn documentai_projects_locations_processors_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}
/// Gets a processor detail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1Processor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1Processor = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}
/// Gets a processor detail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_get_builder()` + `documentai_projects_locations_processors_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1Processor>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_get_builder(client, &args.name)?;
    documentai_projects_locations_processors_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors
/// Lists all processors which belong to this project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_list_execute()` to send, or `documentai_projects_locations_processors_list` for simplest API.

pub fn documentai_projects_locations_processors_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors
/// Lists all processors which belong to this project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListProcessorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ListProcessorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors
/// Lists all processors which belong to this project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_list_builder()` + `documentai_projects_locations_processors_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListProcessorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    documentai_projects_locations_processors_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:process
/// Processes a single document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_process_execute()` to send, or `documentai_projects_locations_processors_process` for simplest API.

pub fn documentai_projects_locations_processors_process_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDocumentaiV1ProcessRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}:process",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:process
/// Processes a single document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_process_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_process_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_process_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ProcessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_process`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1ProcessRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:process
/// Processes a single document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_process_builder()` + `documentai_projects_locations_processors_process_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_process(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_processors_process_builder(client, &args.name, &args.body)?;
    documentai_projects_locations_processors_process_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:setDefaultProcessorVersion
/// Set the default (active) version of a Processor that will be used in ProcessDocument and BatchProcessDocuments.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_set_default_processor_version_execute()` to send, or `documentai_projects_locations_processors_set_default_processor_version` for simplest API.

pub fn documentai_projects_locations_processors_set_default_processor_version_builder(
    client: &SimpleHttpClient,
    processor: &str,
    body: &GoogleCloudDocumentaiV1SetDefaultProcessorVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}:setDefaultProcessorVersion",
        processor,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:setDefaultProcessorVersion
/// Set the default (active) version of a Processor that will be used in ProcessDocument and BatchProcessDocuments.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_set_default_processor_version_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_set_default_processor_version_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_set_default_processor_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_set_default_processor_version`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsSetDefaultProcessorVersionArgs {
    /// Path parameter: processor
    pub processor: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1SetDefaultProcessorVersionRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}:setDefaultProcessorVersion
/// Set the default (active) version of a Processor that will be used in ProcessDocument and BatchProcessDocuments.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_set_default_processor_version_builder()` + `documentai_projects_locations_processors_set_default_processor_version_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_set_default_processor_version(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsSetDefaultProcessorVersionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_set_default_processor_version_builder(
        client,
        &args.processor,
        &args.body,
    )?;
    documentai_projects_locations_processors_set_default_processor_version_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/humanReviewConfig:reviewDocument
/// Send a document for Human Review. The input document should be processed by the specified processor.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_human_review_config_review_document_execute()` to send, or `documentai_projects_locations_processors_human_review_config_review_document` for simplest API.

pub fn documentai_projects_locations_processors_human_review_config_review_document_builder(
    client: &SimpleHttpClient,
    humanReviewConfig: &str,
    body: &GoogleCloudDocumentaiV1ReviewDocumentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}/humanReviewConfig:reviewDocument",
        humanReviewConfig,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/humanReviewConfig:reviewDocument
/// Send a document for Human Review. The input document should be processed by the specified processor.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_human_review_config_review_document_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_human_review_config_review_document_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_human_review_config_review_document_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_human_review_config_review_document`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsHumanReviewConfigReviewDocumentArgs {
    /// Path parameter: humanReviewConfig
    pub humanReviewConfig: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1ReviewDocumentRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/humanReviewConfig:reviewDocument
/// Send a document for Human Review. The input document should be processed by the specified processor.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_human_review_config_review_document_builder()` + `documentai_projects_locations_processors_human_review_config_review_document_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_human_review_config_review_document(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsHumanReviewConfigReviewDocumentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_processors_human_review_config_review_document_builder(
            client,
            &args.humanReviewConfig,
            &args.body,
        )?;
    documentai_projects_locations_processors_human_review_config_review_document_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:batchProcess
/// LRO endpoint to batch process many documents. The output is written to Cloud Storage as JSON in the [Document] format.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_batch_process_execute()` to send, or `documentai_projects_locations_processors_processor_versions_batch_process` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_batch_process_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDocumentaiV1BatchProcessRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}/processorVersions/{}:batchProcess",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:batchProcess
/// LRO endpoint to batch process many documents. The output is written to Cloud Storage as JSON in the [Document] format.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_processor_versions_batch_process_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_processor_versions_batch_process_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_batch_process_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_batch_process`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsBatchProcessArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1BatchProcessRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:batchProcess
/// LRO endpoint to batch process many documents. The output is written to Cloud Storage as JSON in the [Document] format.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_batch_process_builder()` + `documentai_projects_locations_processors_processor_versions_batch_process_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_batch_process(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsBatchProcessArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_processors_processor_versions_batch_process_builder(
            client, &args.name, &args.body,
        )?;
    documentai_projects_locations_processors_processor_versions_batch_process_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}
/// Deletes the processor version, all artifacts under the processor version will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_delete_execute()` to send, or `documentai_projects_locations_processors_processor_versions_delete` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}/processorVersions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}
/// Deletes the processor version, all artifacts under the processor version will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_processor_versions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_processor_versions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}
/// Deletes the processor version, all artifacts under the processor version will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_delete_builder()` + `documentai_projects_locations_processors_processor_versions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_delete(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_delete_builder(
        client, &args.name,
    )?;
    documentai_projects_locations_processors_processor_versions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:deploy
/// Deploys the processor version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_deploy_execute()` to send, or `documentai_projects_locations_processors_processor_versions_deploy` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_deploy_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDocumentaiV1DeployProcessorVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}/processorVersions/{}:deploy",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:deploy
/// Deploys the processor version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_processor_versions_deploy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_processor_versions_deploy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_deploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsDeployArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1DeployProcessorVersionRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:deploy
/// Deploys the processor version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_deploy_builder()` + `documentai_projects_locations_processors_processor_versions_deploy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_deploy(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsDeployArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_deploy_builder(
        client, &args.name, &args.body,
    )?;
    documentai_projects_locations_processors_processor_versions_deploy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:evaluateProcessorVersion
/// Evaluates a ProcessorVersion against annotated documents, producing an Evaluation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_evaluate_processor_version_execute()` to send, or `documentai_projects_locations_processors_processor_versions_evaluate_processor_version` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_evaluate_processor_version_builder(
    client: &SimpleHttpClient,
    processorVersion: &str,
    body: &GoogleCloudDocumentaiV1EvaluateProcessorVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}/processorVersions/{}:evaluateProcessorVersion",
        processorVersion,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:evaluateProcessorVersion
/// Evaluates a ProcessorVersion against annotated documents, producing an Evaluation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_processor_versions_evaluate_processor_version_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_processor_versions_evaluate_processor_version_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_evaluate_processor_version_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_evaluate_processor_version`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluateProcessorVersionArgs {
    /// Path parameter: processorVersion
    pub processorVersion: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1EvaluateProcessorVersionRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:evaluateProcessorVersion
/// Evaluates a ProcessorVersion against annotated documents, producing an Evaluation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_evaluate_processor_version_builder()` + `documentai_projects_locations_processors_processor_versions_evaluate_processor_version_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_evaluate_processor_version(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluateProcessorVersionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_evaluate_processor_version_builder(client, &args.processorVersion, &args.body)?;
    documentai_projects_locations_processors_processor_versions_evaluate_processor_version_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}
/// Gets a processor version detail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_get_execute()` to send, or `documentai_projects_locations_processors_processor_versions_get` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}/processorVersions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}
/// Gets a processor version detail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_processor_versions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_processor_versions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessorVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ProcessorVersion =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}
/// Gets a processor version detail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_get_builder()` + `documentai_projects_locations_processors_processor_versions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessorVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_get_builder(
        client, &args.name,
    )?;
    documentai_projects_locations_processors_processor_versions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions
/// Lists all versions of a processor.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_list_execute()` to send, or `documentai_projects_locations_processors_processor_versions_list` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}/processorVersions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions
/// Lists all versions of a processor.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_processor_versions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_processor_versions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListProcessorVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ListProcessorVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions
/// Lists all versions of a processor.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_list_builder()` + `documentai_projects_locations_processors_processor_versions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListProcessorVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    documentai_projects_locations_processors_processor_versions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:process
/// Processes a single document.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_process_execute()` to send, or `documentai_projects_locations_processors_processor_versions_process` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_process_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDocumentaiV1ProcessRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}/processorVersions/{}:process",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:process
/// Processes a single document.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_processor_versions_process_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_processor_versions_process_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_process_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ProcessResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_process`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsProcessArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1ProcessRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:process
/// Processes a single document.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_process_builder()` + `documentai_projects_locations_processors_processor_versions_process_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_process(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsProcessArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ProcessResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_process_builder(
        client, &args.name, &args.body,
    )?;
    documentai_projects_locations_processors_processor_versions_process_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions:train
/// Trains a new processor version. Operation metadata is returned as TrainProcessorVersionMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_train_execute()` to send, or `documentai_projects_locations_processors_processor_versions_train` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_train_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDocumentaiV1TrainProcessorVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}/processorVersions:train",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions:train
/// Trains a new processor version. Operation metadata is returned as TrainProcessorVersionMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_processor_versions_train_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_processor_versions_train_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_train_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_train`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsTrainArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1TrainProcessorVersionRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions:train
/// Trains a new processor version. Operation metadata is returned as TrainProcessorVersionMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_train_builder()` + `documentai_projects_locations_processors_processor_versions_train_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_train(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsTrainArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_train_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    documentai_projects_locations_processors_processor_versions_train_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:undeploy
/// Undeploys the processor version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_undeploy_execute()` to send, or `documentai_projects_locations_processors_processor_versions_undeploy` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_undeploy_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudDocumentaiV1UndeployProcessorVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}/processorVersions/{}:undeploy",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:undeploy
/// Undeploys the processor version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_processor_versions_undeploy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_processor_versions_undeploy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_undeploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_undeploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsUndeployArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1UndeployProcessorVersionRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}:undeploy
/// Undeploys the processor version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_undeploy_builder()` + `documentai_projects_locations_processors_processor_versions_undeploy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_undeploy(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsUndeployArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_processors_processor_versions_undeploy_builder(
        client, &args.name, &args.body,
    )?;
    documentai_projects_locations_processors_processor_versions_undeploy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations/{evaluationsId}
/// Retrieves a specific evaluation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_evaluations_get_execute()` to send, or `documentai_projects_locations_processors_processor_versions_evaluations_get` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_evaluations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}/processorVersions/{}/evaluations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations/{evaluationsId}
/// Retrieves a specific evaluation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_processor_versions_evaluations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_processor_versions_evaluations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_evaluations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1Evaluation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1Evaluation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_evaluations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations/{evaluationsId}
/// Retrieves a specific evaluation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_evaluations_get_builder()` + `documentai_projects_locations_processors_processor_versions_evaluations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_evaluations_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1Evaluation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_processors_processor_versions_evaluations_get_builder(
            client, &args.name,
        )?;
    documentai_projects_locations_processors_processor_versions_evaluations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations
/// Retrieves a set of evaluations for a given processor version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_processors_processor_versions_evaluations_list_execute()` to send, or `documentai_projects_locations_processors_processor_versions_evaluations_list` for simplest API.

pub fn documentai_projects_locations_processors_processor_versions_evaluations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/processors/{}/processorVersions/{}/evaluations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations
/// Retrieves a set of evaluations for a given processor version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_processors_processor_versions_evaluations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_processors_processor_versions_evaluations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_evaluations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListEvaluationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ListEvaluationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_processors_processor_versions_evaluations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/processors/{processorsId}/processorVersions/{processorVersionsId}/evaluations
/// Retrieves a set of evaluations for a given processor version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_processors_processor_versions_evaluations_list_builder()` + `documentai_projects_locations_processors_processor_versions_evaluations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_processors_processor_versions_evaluations_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsProcessorsProcessorVersionsEvaluationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListEvaluationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_processors_processor_versions_evaluations_list_builder(
            client,
            &args.parent,
            args.pageSize,
            args.pageToken.as_deref(),
        )?;
    documentai_projects_locations_processors_processor_versions_evaluations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas
/// Creates a schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_create_execute()` to send, or `documentai_projects_locations_schemas_create` for simplest API.

pub fn documentai_projects_locations_schemas_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDocumentaiV1NextSchema,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/schemas",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas
/// Creates a schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_schemas_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_schemas_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1NextSchema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1NextSchema,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas
/// Creates a schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_create_builder()` + `documentai_projects_locations_schemas_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_create(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_schemas_create_builder(client, &args.parent, &args.body)?;
    documentai_projects_locations_schemas_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Deletes a schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_delete_execute()` to send, or `documentai_projects_locations_schemas_delete` for simplest API.

pub fn documentai_projects_locations_schemas_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    force: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/schemas/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Deletes a schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_schemas_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_schemas_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<bool>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Deletes a schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_delete_builder()` + `documentai_projects_locations_schemas_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_delete(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_schemas_delete_builder(client, &args.name, args.force)?;
    documentai_projects_locations_schemas_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Gets a schema.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_get_execute()` to send, or `documentai_projects_locations_schemas_get` for simplest API.

pub fn documentai_projects_locations_schemas_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/schemas/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Gets a schema.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_schemas_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_schemas_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1NextSchema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Gets a schema.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_get_builder()` + `documentai_projects_locations_schemas_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_schemas_get_builder(client, &args.name)?;
    documentai_projects_locations_schemas_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas
/// Lists Schemas.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_list_execute()` to send, or `documentai_projects_locations_schemas_list` for simplest API.

pub fn documentai_projects_locations_schemas_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/schemas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas
/// Lists Schemas.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_schemas_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_schemas_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListSchemasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ListSchemasResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas
/// Lists Schemas.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_list_builder()` + `documentai_projects_locations_schemas_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListSchemasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_schemas_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    documentai_projects_locations_schemas_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Updates a schema. Editable fields are: - display_name - labels
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_patch_execute()` to send, or `documentai_projects_locations_schemas_patch` for simplest API.

pub fn documentai_projects_locations_schemas_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDocumentaiV1NextSchema,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/schemas/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Updates a schema. Editable fields are: - display_name - labels
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_schemas_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_schemas_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1NextSchema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1NextSchema,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}
/// Updates a schema. Editable fields are: - display_name - labels
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_patch_builder()` + `documentai_projects_locations_schemas_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_patch(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1NextSchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_schemas_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    documentai_projects_locations_schemas_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions
/// Creates a schema version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_schema_versions_create_execute()` to send, or `documentai_projects_locations_schemas_schema_versions_create` for simplest API.

pub fn documentai_projects_locations_schemas_schema_versions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDocumentaiV1SchemaVersion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/schemas/{}/schemaVersions",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions
/// Creates a schema version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_schemas_schema_versions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1SchemaVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_schema_versions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasSchemaVersionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1SchemaVersion,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions
/// Creates a schema version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_create_builder()` + `documentai_projects_locations_schemas_schema_versions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_create(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasSchemaVersionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_schemas_schema_versions_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    documentai_projects_locations_schemas_schema_versions_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Deletes a schema version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_schema_versions_delete_execute()` to send, or `documentai_projects_locations_schemas_schema_versions_delete` for simplest API.

pub fn documentai_projects_locations_schemas_schema_versions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/schemas/{}/schemaVersions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Deletes a schema version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_schemas_schema_versions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_schema_versions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasSchemaVersionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Deletes a schema version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_delete_builder()` + `documentai_projects_locations_schemas_schema_versions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_delete(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasSchemaVersionsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_schemas_schema_versions_delete_builder(client, &args.name)?;
    documentai_projects_locations_schemas_schema_versions_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions:generate
/// Generates a schema version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_schema_versions_generate_execute()` to send, or `documentai_projects_locations_schemas_schema_versions_generate` for simplest API.

pub fn documentai_projects_locations_schemas_schema_versions_generate_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudDocumentaiV1GenerateSchemaVersionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/schemas/{}/schemaVersions:generate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions:generate
/// Generates a schema version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_generate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_schemas_schema_versions_generate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_generate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1GenerateSchemaVersionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1GenerateSchemaVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_schema_versions_generate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasSchemaVersionsGenerateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1GenerateSchemaVersionRequest,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions:generate
/// Generates a schema version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_generate_builder()` + `documentai_projects_locations_schemas_schema_versions_generate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_generate(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasSchemaVersionsGenerateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1GenerateSchemaVersionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_schemas_schema_versions_generate_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    documentai_projects_locations_schemas_schema_versions_generate_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Gets a schema version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_schema_versions_get_execute()` to send, or `documentai_projects_locations_schemas_schema_versions_get` for simplest API.

pub fn documentai_projects_locations_schemas_schema_versions_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/schemas/{}/schemaVersions/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Gets a schema version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_schemas_schema_versions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1SchemaVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_schema_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasSchemaVersionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Gets a schema version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_get_builder()` + `documentai_projects_locations_schemas_schema_versions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasSchemaVersionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        documentai_projects_locations_schemas_schema_versions_get_builder(client, &args.name)?;
    documentai_projects_locations_schemas_schema_versions_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions
/// Lists SchemaVersions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_schema_versions_list_execute()` to send, or `documentai_projects_locations_schemas_schema_versions_list` for simplest API.

pub fn documentai_projects_locations_schemas_schema_versions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/schemas/{}/schemaVersions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions
/// Lists SchemaVersions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_schemas_schema_versions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListSchemaVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1ListSchemaVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_schema_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasSchemaVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions
/// Lists SchemaVersions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_list_builder()` + `documentai_projects_locations_schemas_schema_versions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_list(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasSchemaVersionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1ListSchemaVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_schemas_schema_versions_list_builder(
        client,
        &args.parent,
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    documentai_projects_locations_schemas_schema_versions_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Updates a schema version. Editable fields are: - display_name - labels
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_locations_schemas_schema_versions_patch_execute()` to send, or `documentai_projects_locations_schemas_schema_versions_patch` for simplest API.

pub fn documentai_projects_locations_schemas_schema_versions_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudDocumentaiV1SchemaVersion,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/locations/{}/schemas/{}/schemaVersions/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Updates a schema version. Editable fields are: - display_name - labels
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_locations_schemas_schema_versions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDocumentaiV1SchemaVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_locations_schemas_schema_versions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsLocationsSchemasSchemaVersionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDocumentaiV1SchemaVersion,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/schemas/{schemasId}/schemaVersions/{schemaVersionsId}
/// Updates a schema version. Editable fields are: - display_name - labels
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_locations_schemas_schema_versions_patch_builder()` + `documentai_projects_locations_schemas_schema_versions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_locations_schemas_schema_versions_patch(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsLocationsSchemasSchemaVersionsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDocumentaiV1SchemaVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_locations_schemas_schema_versions_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    documentai_projects_locations_schemas_schema_versions_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `documentai_projects_operations_get_execute()` to send, or `documentai_projects_operations_get` for simplest API.

pub fn documentai_projects_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://documentai.googleapis.com/v1/projects/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `documentai_projects_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from documentai_projects_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`documentai_projects_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DocumentaiProjectsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `documentai_projects_operations_get_builder()` + `documentai_projects_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn documentai_projects_operations_get(
    client: &SimpleHttpClient,
    args: &DocumentaiProjectsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = documentai_projects_operations_get_builder(client, &args.name)?;
    documentai_projects_operations_get_execute(builder)
}
