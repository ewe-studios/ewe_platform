//! Auto-generated API clients for gcp/mybusinessbusinessinformation.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/accounts/{accountsId}/locations
/// Creates a new Location that will be owned by the logged in user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_accounts_locations_create_execute()` to send, or `mybusinessbusinessinformation_accounts_locations_create` for simplest API.

pub fn mybusinessbusinessinformation_accounts_locations_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    requestId: &Option<String>,
    validateOnly: &Option<bool>,
    body: &Location,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://mybusinessbusinessinformation.googleapis.com/v1/accounts/{}/locations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}/locations
/// Creates a new Location that will be owned by the logged in user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessbusinessinformation_accounts_locations_create_execute()` or `mybusinessbusinessinformation_accounts_locations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_accounts_locations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_accounts_locations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/accounts/{accountsId}/locations
/// Creates a new Location that will be owned by the logged in user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessbusinessinformation_accounts_locations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessbusinessinformation_accounts_locations_create_task()`.
/// For the simplest API, use `mybusinessbusinessinformation_accounts_locations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_accounts_locations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessbusinessinformation_accounts_locations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = mybusinessbusinessinformation_accounts_locations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessbusinessinformation_accounts_locations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessbusinessinformationAccountsLocationsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: Location,
}

/// GET v1/accounts/{accountsId}/locations
/// Creates a new Location that will be owned by the logged in user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_accounts_locations_create_builder()` + `mybusinessbusinessinformation_accounts_locations_create_execute()`.
/// For task-level control, use `mybusinessbusinessinformation_accounts_locations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_accounts_locations_create(
    client: &SimpleHttpClient,
    args: &MybusinessbusinessinformationAccountsLocationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_accounts_locations_create_builder(
        client,
        &args.parent,
        &args.requestId,
        &args.validateOnly,
        &args.body,
    )?;
    mybusinessbusinessinformation_accounts_locations_create_execute(builder)
}

/// GET v1/attributes
/// Returns the list of attributes that would be available for a location with the given primary category and country.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_attributes_list_execute()` to send, or `mybusinessbusinessinformation_attributes_list` for simplest API.

pub fn mybusinessbusinessinformation_attributes_list_builder(
    client: &SimpleHttpClient,
    categoryName: &Option<String>,
    languageCode: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    parent: &Option<String>,
    regionCode: &Option<String>,
    showAll: &Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://mybusinessbusinessinformation.googleapis.com/v1/attributes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = categoryName.as_ref() {
        query_parts.push(format!("categoryName={}", val));
    }
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent.as_ref() {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = regionCode.as_ref() {
        query_parts.push(format!("regionCode={}", val));
    }
    if let Some(val) = showAll.as_ref() {
        query_parts.push(format!("showAll={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/attributes
/// Returns the list of attributes that would be available for a location with the given primary category and country.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessbusinessinformation_attributes_list_execute()` or `mybusinessbusinessinformation_attributes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_attributes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_attributes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAttributeMetadataResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAttributeMetadataResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/attributes
/// Returns the list of attributes that would be available for a location with the given primary category and country.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessbusinessinformation_attributes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessbusinessinformation_attributes_list_task()`.
/// For the simplest API, use `mybusinessbusinessinformation_attributes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_attributes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessbusinessinformation_attributes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAttributeMetadataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessbusinessinformation_attributes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessbusinessinformation_attributes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessbusinessinformationAttributesListArgs {
    /// Query parameter: categoryName
    pub categoryName: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parent
    pub parent: Option<String>,
    /// Query parameter: regionCode
    pub regionCode: Option<String>,
    /// Query parameter: showAll
    pub showAll: Option<bool>,
}

/// GET v1/attributes
/// Returns the list of attributes that would be available for a location with the given primary category and country.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_attributes_list_builder()` + `mybusinessbusinessinformation_attributes_list_execute()`.
/// For task-level control, use `mybusinessbusinessinformation_attributes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_attributes_list(
    client: &SimpleHttpClient,
    args: &MybusinessbusinessinformationAttributesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAttributeMetadataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_attributes_list_builder(
        client,
        &args.categoryName,
        &args.languageCode,
        &args.pageSize,
        &args.pageToken,
        &args.parent,
        &args.regionCode,
        &args.showAll,
    )?;
    mybusinessbusinessinformation_attributes_list_execute(builder)
}

/// GET v1/categories:batchGet
/// Returns a list of business categories for the provided language and GConcept ids.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_categories_batch_get_execute()` to send, or `mybusinessbusinessinformation_categories_batch_get` for simplest API.

pub fn mybusinessbusinessinformation_categories_batch_get_builder(
    client: &SimpleHttpClient,
    languageCode: &Option<String>,
    names: &Option<String>,
    regionCode: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://mybusinessbusinessinformation.googleapis.com/v1/categories:batchGet",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = names.as_ref() {
        query_parts.push(format!("names={}", val));
    }
    if let Some(val) = regionCode.as_ref() {
        query_parts.push(format!("regionCode={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/categories:batchGet
/// Returns a list of business categories for the provided language and GConcept ids.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessbusinessinformation_categories_batch_get_execute()` or `mybusinessbusinessinformation_categories_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_categories_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_categories_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchGetCategoriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetCategoriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/categories:batchGet
/// Returns a list of business categories for the provided language and GConcept ids.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessbusinessinformation_categories_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessbusinessinformation_categories_batch_get_task()`.
/// For the simplest API, use `mybusinessbusinessinformation_categories_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_categories_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessbusinessinformation_categories_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetCategoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessbusinessinformation_categories_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessbusinessinformation_categories_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessbusinessinformationCategoriesBatchGetArgs {
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: names
    pub names: Option<String>,
    /// Query parameter: regionCode
    pub regionCode: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/categories:batchGet
/// Returns a list of business categories for the provided language and GConcept ids.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_categories_batch_get_builder()` + `mybusinessbusinessinformation_categories_batch_get_execute()`.
/// For task-level control, use `mybusinessbusinessinformation_categories_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_categories_batch_get(
    client: &SimpleHttpClient,
    args: &MybusinessbusinessinformationCategoriesBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetCategoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_categories_batch_get_builder(
        client,
        &args.languageCode,
        &args.names,
        &args.regionCode,
        &args.view,
    )?;
    mybusinessbusinessinformation_categories_batch_get_execute(builder)
}

/// GET v1/categories
/// Returns a list of business categories. Search will match the category name but not the category ID. Search only matches the front of a category name (that is, 'food' may return 'Food Court' but not 'Fast Food Restaurant').
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_categories_list_execute()` to send, or `mybusinessbusinessinformation_categories_list` for simplest API.

pub fn mybusinessbusinessinformation_categories_list_builder(
    client: &SimpleHttpClient,
    filter: &Option<String>,
    languageCode: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
    regionCode: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://mybusinessbusinessinformation.googleapis.com/v1/categories",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = regionCode.as_ref() {
        query_parts.push(format!("regionCode={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/categories
/// Returns a list of business categories. Search will match the category name but not the category ID. Search only matches the front of a category name (that is, 'food' may return 'Food Court' but not 'Fast Food Restaurant').
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessbusinessinformation_categories_list_execute()` or `mybusinessbusinessinformation_categories_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_categories_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_categories_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCategoriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCategoriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/categories
/// Returns a list of business categories. Search will match the category name but not the category ID. Search only matches the front of a category name (that is, 'food' may return 'Food Court' but not 'Fast Food Restaurant').
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessbusinessinformation_categories_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessbusinessinformation_categories_list_task()`.
/// For the simplest API, use `mybusinessbusinessinformation_categories_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_categories_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessbusinessinformation_categories_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCategoriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessbusinessinformation_categories_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessbusinessinformation_categories_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessbusinessinformationCategoriesListArgs {
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: regionCode
    pub regionCode: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/categories
/// Returns a list of business categories. Search will match the category name but not the category ID. Search only matches the front of a category name (that is, 'food' may return 'Food Court' but not 'Fast Food Restaurant').
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_categories_list_builder()` + `mybusinessbusinessinformation_categories_list_execute()`.
/// For task-level control, use `mybusinessbusinessinformation_categories_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_categories_list(
    client: &SimpleHttpClient,
    args: &MybusinessbusinessinformationCategoriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCategoriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_categories_list_builder(
        client,
        &args.filter,
        &args.languageCode,
        &args.pageSize,
        &args.pageToken,
        &args.regionCode,
        &args.view,
    )?;
    mybusinessbusinessinformation_categories_list_execute(builder)
}

/// GET v1/chains/{chainsId}
/// Gets the specified chain. Returns NOT_FOUND if the chain does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_chains_get_execute()` to send, or `mybusinessbusinessinformation_chains_get` for simplest API.

pub fn mybusinessbusinessinformation_chains_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://mybusinessbusinessinformation.googleapis.com/v1/chains/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/chains/{chainsId}
/// Gets the specified chain. Returns NOT_FOUND if the chain does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessbusinessinformation_chains_get_execute()` or `mybusinessbusinessinformation_chains_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_chains_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_chains_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Chain>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Chain = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/chains/{chainsId}
/// Gets the specified chain. Returns NOT_FOUND if the chain does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessbusinessinformation_chains_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessbusinessinformation_chains_get_task()`.
/// For the simplest API, use `mybusinessbusinessinformation_chains_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_chains_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessbusinessinformation_chains_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Chain>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = mybusinessbusinessinformation_chains_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessbusinessinformation_chains_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessbusinessinformationChainsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/chains/{chainsId}
/// Gets the specified chain. Returns NOT_FOUND if the chain does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_chains_get_builder()` + `mybusinessbusinessinformation_chains_get_execute()`.
/// For task-level control, use `mybusinessbusinessinformation_chains_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_chains_get(
    client: &SimpleHttpClient,
    args: &MybusinessbusinessinformationChainsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Chain>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_chains_get_builder(client, &args.name)?;
    mybusinessbusinessinformation_chains_get_execute(builder)
}

/// GET v1/chains:search
/// Searches the chain based on chain name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_chains_search_execute()` to send, or `mybusinessbusinessinformation_chains_search` for simplest API.

pub fn mybusinessbusinessinformation_chains_search_builder(
    client: &SimpleHttpClient,
    chainName: &Option<String>,
    pageSize: &Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://mybusinessbusinessinformation.googleapis.com/v1/chains:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = chainName.as_ref() {
        query_parts.push(format!("chainName={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/chains:search
/// Searches the chain based on chain name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessbusinessinformation_chains_search_execute()` or `mybusinessbusinessinformation_chains_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_chains_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_chains_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchChainsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchChainsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/chains:search
/// Searches the chain based on chain name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessbusinessinformation_chains_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessbusinessinformation_chains_search_task()`.
/// For the simplest API, use `mybusinessbusinessinformation_chains_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_chains_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessbusinessinformation_chains_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchChainsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessbusinessinformation_chains_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessbusinessinformation_chains_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessbusinessinformationChainsSearchArgs {
    /// Query parameter: chainName
    pub chainName: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
}

/// GET v1/chains:search
/// Searches the chain based on chain name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_chains_search_builder()` + `mybusinessbusinessinformation_chains_search_execute()`.
/// For task-level control, use `mybusinessbusinessinformation_chains_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_chains_search(
    client: &SimpleHttpClient,
    args: &MybusinessbusinessinformationChainsSearchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchChainsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_chains_search_builder(
        client,
        &args.chainName,
        &args.pageSize,
    )?;
    mybusinessbusinessinformation_chains_search_execute(builder)
}

/// GET v1/googleLocations:search
/// Search all of the possible locations that are a match to the specified request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_google_locations_search_execute()` to send, or `mybusinessbusinessinformation_google_locations_search` for simplest API.

pub fn mybusinessbusinessinformation_google_locations_search_builder(
    client: &SimpleHttpClient,
    body: &SearchGoogleLocationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://mybusinessbusinessinformation.googleapis.com/v1/googleLocations:search",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/googleLocations:search
/// Search all of the possible locations that are a match to the specified request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessbusinessinformation_google_locations_search_execute()` or `mybusinessbusinessinformation_google_locations_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_google_locations_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_google_locations_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchGoogleLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchGoogleLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/googleLocations:search
/// Search all of the possible locations that are a match to the specified request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessbusinessinformation_google_locations_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessbusinessinformation_google_locations_search_task()`.
/// For the simplest API, use `mybusinessbusinessinformation_google_locations_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_google_locations_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessbusinessinformation_google_locations_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchGoogleLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessbusinessinformation_google_locations_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessbusinessinformation_google_locations_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessbusinessinformationGoogleLocationsSearchArgs {
    /// Request body.
    pub body: SearchGoogleLocationsRequest,
}

/// GET v1/googleLocations:search
/// Search all of the possible locations that are a match to the specified request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_google_locations_search_builder()` + `mybusinessbusinessinformation_google_locations_search_execute()`.
/// For task-level control, use `mybusinessbusinessinformation_google_locations_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_google_locations_search(
    client: &SimpleHttpClient,
    args: &MybusinessbusinessinformationGoogleLocationsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchGoogleLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        mybusinessbusinessinformation_google_locations_search_builder(client, &args.body)?;
    mybusinessbusinessinformation_google_locations_search_execute(builder)
}

/// GET v1/locations/{locationsId}
/// Deletes a location. If this location cannot be deleted using the API and it is marked so in the google.mybusiness.businessinformation.v1.LocationState, use the [Google Business Profile](<https://business.google.`com/manage/`>) website.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_locations_delete_execute()` to send, or `mybusinessbusinessinformation_locations_delete` for simplest API.

pub fn mybusinessbusinessinformation_locations_delete_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://mybusinessbusinessinformation.googleapis.com/v1/locations/{}",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}
/// Deletes a location. If this location cannot be deleted using the API and it is marked so in the google.mybusiness.businessinformation.v1.LocationState, use the [Google Business Profile](<https://business.google.`com/manage/`>) website.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessbusinessinformation_locations_delete_execute()` or `mybusinessbusinessinformation_locations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_locations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/locations/{locationsId}
/// Deletes a location. If this location cannot be deleted using the API and it is marked so in the google.mybusiness.businessinformation.v1.LocationState, use the [Google Business Profile](<https://business.google.`com/manage/`>) website.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessbusinessinformation_locations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessbusinessinformation_locations_delete_task()`.
/// For the simplest API, use `mybusinessbusinessinformation_locations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_locations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessbusinessinformation_locations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = mybusinessbusinessinformation_locations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessbusinessinformation_locations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessbusinessinformationLocationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/locations/{locationsId}
/// Deletes a location. If this location cannot be deleted using the API and it is marked so in the google.mybusiness.businessinformation.v1.LocationState, use the [Google Business Profile](<https://business.google.`com/manage/`>) website.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_locations_delete_builder()` + `mybusinessbusinessinformation_locations_delete_execute()`.
/// For task-level control, use `mybusinessbusinessinformation_locations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_delete(
    client: &SimpleHttpClient,
    args: &MybusinessbusinessinformationLocationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_locations_delete_builder(client, &args.name)?;
    mybusinessbusinessinformation_locations_delete_execute(builder)
}

/// GET v1/locations/{locationsId}/attributes
/// Looks up all the attributes set for a given location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_locations_get_attributes_execute()` to send, or `mybusinessbusinessinformation_locations_get_attributes` for simplest API.

pub fn mybusinessbusinessinformation_locations_get_attributes_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://mybusinessbusinessinformation.googleapis.com/v1/locations/{}/attributes",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/attributes
/// Looks up all the attributes set for a given location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessbusinessinformation_locations_get_attributes_execute()` or `mybusinessbusinessinformation_locations_get_attributes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_locations_get_attributes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_get_attributes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Attributes>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Attributes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/locations/{locationsId}/attributes
/// Looks up all the attributes set for a given location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessbusinessinformation_locations_get_attributes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessbusinessinformation_locations_get_attributes_task()`.
/// For the simplest API, use `mybusinessbusinessinformation_locations_get_attributes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_locations_get_attributes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessbusinessinformation_locations_get_attributes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attributes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = mybusinessbusinessinformation_locations_get_attributes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessbusinessinformation_locations_get_attributes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessbusinessinformationLocationsGetAttributesArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/locations/{locationsId}/attributes
/// Looks up all the attributes set for a given location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_locations_get_attributes_builder()` + `mybusinessbusinessinformation_locations_get_attributes_execute()`.
/// For task-level control, use `mybusinessbusinessinformation_locations_get_attributes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_get_attributes(
    client: &SimpleHttpClient,
    args: &MybusinessbusinessinformationLocationsGetAttributesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attributes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        mybusinessbusinessinformation_locations_get_attributes_builder(client, &args.name)?;
    mybusinessbusinessinformation_locations_get_attributes_execute(builder)
}

/// GET v1/locations/{locationsId}:getGoogleUpdated
/// Gets the Google-updated version of the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_locations_get_google_updated_execute()` to send, or `mybusinessbusinessinformation_locations_get_google_updated` for simplest API.

pub fn mybusinessbusinessinformation_locations_get_google_updated_builder(
    client: &SimpleHttpClient,
    name: &String,
    readMask: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://mybusinessbusinessinformation.googleapis.com/v1/locations/{}:getGoogleUpdated",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}:getGoogleUpdated
/// Gets the Google-updated version of the specified location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessbusinessinformation_locations_get_google_updated_execute()` or `mybusinessbusinessinformation_locations_get_google_updated`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_locations_get_google_updated_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_get_google_updated_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleUpdatedLocation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleUpdatedLocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/locations/{locationsId}:getGoogleUpdated
/// Gets the Google-updated version of the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessbusinessinformation_locations_get_google_updated_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessbusinessinformation_locations_get_google_updated_task()`.
/// For the simplest API, use `mybusinessbusinessinformation_locations_get_google_updated()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_locations_get_google_updated_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessbusinessinformation_locations_get_google_updated_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleUpdatedLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = mybusinessbusinessinformation_locations_get_google_updated_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessbusinessinformation_locations_get_google_updated`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessbusinessinformationLocationsGetGoogleUpdatedArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/locations/{locationsId}:getGoogleUpdated
/// Gets the Google-updated version of the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_locations_get_google_updated_builder()` + `mybusinessbusinessinformation_locations_get_google_updated_execute()`.
/// For task-level control, use `mybusinessbusinessinformation_locations_get_google_updated_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_get_google_updated(
    client: &SimpleHttpClient,
    args: &MybusinessbusinessinformationLocationsGetGoogleUpdatedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleUpdatedLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_locations_get_google_updated_builder(
        client,
        &args.name,
        &args.readMask,
    )?;
    mybusinessbusinessinformation_locations_get_google_updated_execute(builder)
}

/// GET v1/locations/{locationsId}/attributes:getGoogleUpdated
/// Gets the Google-updated version of the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_locations_attributes_get_google_updated_execute()` to send, or `mybusinessbusinessinformation_locations_attributes_get_google_updated` for simplest API.

pub fn mybusinessbusinessinformation_locations_attributes_get_google_updated_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://mybusinessbusinessinformation.googleapis.com/v1/locations/{}/attributes:getGoogleUpdated",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/attributes:getGoogleUpdated
/// Gets the Google-updated version of the specified location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `mybusinessbusinessinformation_locations_attributes_get_google_updated_execute()` or `mybusinessbusinessinformation_locations_attributes_get_google_updated`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_locations_attributes_get_google_updated_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_attributes_get_google_updated_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Attributes>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Attributes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/locations/{locationsId}/attributes:getGoogleUpdated
/// Gets the Google-updated version of the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `mybusinessbusinessinformation_locations_attributes_get_google_updated_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `mybusinessbusinessinformation_locations_attributes_get_google_updated_task()`.
/// For the simplest API, use `mybusinessbusinessinformation_locations_attributes_get_google_updated()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `mybusinessbusinessinformation_locations_attributes_get_google_updated_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn mybusinessbusinessinformation_locations_attributes_get_google_updated_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attributes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = mybusinessbusinessinformation_locations_attributes_get_google_updated_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessbusinessinformation_locations_attributes_get_google_updated`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessbusinessinformationLocationsAttributesGetGoogleUpdatedArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/locations/{locationsId}/attributes:getGoogleUpdated
/// Gets the Google-updated version of the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_locations_attributes_get_google_updated_builder()` + `mybusinessbusinessinformation_locations_attributes_get_google_updated_execute()`.
/// For task-level control, use `mybusinessbusinessinformation_locations_attributes_get_google_updated_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_attributes_get_google_updated(
    client: &SimpleHttpClient,
    args: &MybusinessbusinessinformationLocationsAttributesGetGoogleUpdatedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attributes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_locations_attributes_get_google_updated_builder(
        client, &args.name,
    )?;
    mybusinessbusinessinformation_locations_attributes_get_google_updated_execute(builder)
}
