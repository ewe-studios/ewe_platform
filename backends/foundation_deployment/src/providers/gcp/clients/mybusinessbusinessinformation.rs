//! Auto-generated API clients for gcp/mybusinessbusinessinformation.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/mybusinessbusinessinformation`

#![cfg(feature = "gcp/mybusinessbusinessinformation")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/accounts/{accountsId}/locations
/// Creates a new Location that will be owned by the logged in user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_accounts_locations_create_execute()` to send, or `mybusinessbusinessinformation_accounts_locations_create` for simplest API.

pub fn mybusinessbusinessinformation_accounts_locations_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Location,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessbusinessinformation.googleapis.com/v1/accounts/{}/locations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}/locations
/// Creates a new Location that will be owned by the logged in user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_accounts_locations_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_accounts_locations_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_accounts_locations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}/locations
/// Creates a new Location that will be owned by the logged in user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_accounts_locations_create_builder()` + `mybusinessbusinessinformation_accounts_locations_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_accounts_locations_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Location,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_accounts_locations_create_builder(
        client,
        parent,
        requestId,
        validateOnly,
        body,
    )?;
    mybusinessbusinessinformation_accounts_locations_create_execute(builder)
}

/// GET v1/accounts/{accountsId}/locations
/// Lists the locations for the specified account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_accounts_locations_list_execute()` to send, or `mybusinessbusinessinformation_accounts_locations_list` for simplest API.

pub fn mybusinessbusinessinformation_accounts_locations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessbusinessinformation.googleapis.com/v1/accounts/{}/locations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}/locations
/// Lists the locations for the specified account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_accounts_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_accounts_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_accounts_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}/locations
/// Lists the locations for the specified account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_accounts_locations_list_builder()` + `mybusinessbusinessinformation_accounts_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_accounts_locations_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_accounts_locations_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken, readMask,
    )?;
    mybusinessbusinessinformation_accounts_locations_list_execute(builder)
}

/// GET v1/attributes
/// Returns the list of attributes that would be available for a location with the given primary category and country.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_attributes_list_execute()` to send, or `mybusinessbusinessinformation_attributes_list` for simplest API.

pub fn mybusinessbusinessinformation_attributes_list_builder(
    client: &SimpleHttpClient,
    categoryName: Option<&str>,
    languageCode: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    regionCode: Option<&str>,
    showAll: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://mybusinessbusinessinformation.googleapis.com/v1/attributes",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = categoryName {
        query_parts.push(format!("categoryName={}", val));
    }
    if let Some(val) = languageCode {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parent {
        query_parts.push(format!("parent={}", val));
    }
    if let Some(val) = regionCode {
        query_parts.push(format!("regionCode={}", val));
    }
    if let Some(val) = showAll {
        query_parts.push(format!("showAll={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/attributes
/// Returns the list of attributes that would be available for a location with the given primary category and country.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_attributes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_attributes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_attributes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAttributeMetadataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAttributeMetadataResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/attributes
/// Returns the list of attributes that would be available for a location with the given primary category and country.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_attributes_list_builder()` + `mybusinessbusinessinformation_attributes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_attributes_list(
    client: &SimpleHttpClient,
    categoryName: Option<&str>,
    languageCode: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parent: Option<&str>,
    regionCode: Option<&str>,
    showAll: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAttributeMetadataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_attributes_list_builder(
        client,
        categoryName,
        languageCode,
        pageSize,
        pageToken,
        parent,
        regionCode,
        showAll,
    )?;
    mybusinessbusinessinformation_attributes_list_execute(builder)
}

/// GET v1/categories:batchGet
/// Returns a list of business categories for the provided language and GConcept ids.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_categories_batch_get_execute()` to send, or `mybusinessbusinessinformation_categories_batch_get` for simplest API.

pub fn mybusinessbusinessinformation_categories_batch_get_builder(
    client: &SimpleHttpClient,
    languageCode: Option<&str>,
    names: Option<&str>,
    regionCode: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url =
        format!("https://mybusinessbusinessinformation.googleapis.com/v1/categories:batchGet",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = names {
        query_parts.push(format!("names={}", val));
    }
    if let Some(val) = regionCode {
        query_parts.push(format!("regionCode={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/categories:batchGet
/// Returns a list of business categories for the provided language and GConcept ids.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_categories_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_categories_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_categories_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetCategoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetCategoriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/categories:batchGet
/// Returns a list of business categories for the provided language and GConcept ids.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_categories_batch_get_builder()` + `mybusinessbusinessinformation_categories_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_categories_batch_get(
    client: &SimpleHttpClient,
    languageCode: Option<&str>,
    names: Option<&str>,
    regionCode: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetCategoriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_categories_batch_get_builder(
        client,
        languageCode,
        names,
        regionCode,
        view,
    )?;
    mybusinessbusinessinformation_categories_batch_get_execute(builder)
}

/// GET v1/categories
/// Returns a list of business categories. Search will match the category name but not the category ID. Search only matches the front of a category name (that is, 'food' may return 'Food Court' but not 'Fast Food Restaurant').
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_categories_list_execute()` to send, or `mybusinessbusinessinformation_categories_list` for simplest API.

pub fn mybusinessbusinessinformation_categories_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    languageCode: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    regionCode: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://mybusinessbusinessinformation.googleapis.com/v1/categories",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = languageCode {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = regionCode {
        query_parts.push(format!("regionCode={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/categories
/// Returns a list of business categories. Search will match the category name but not the category ID. Search only matches the front of a category name (that is, 'food' may return 'Food Court' but not 'Fast Food Restaurant').
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_categories_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_categories_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_categories_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCategoriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCategoriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/categories
/// Returns a list of business categories. Search will match the category name but not the category ID. Search only matches the front of a category name (that is, 'food' may return 'Food Court' but not 'Fast Food Restaurant').
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_categories_list_builder()` + `mybusinessbusinessinformation_categories_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_categories_list(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    languageCode: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    regionCode: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCategoriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_categories_list_builder(
        client,
        filter,
        languageCode,
        pageSize,
        pageToken,
        regionCode,
        view,
    )?;
    mybusinessbusinessinformation_categories_list_execute(builder)
}

/// GET v1/chains/{chainsId}
/// Gets the specified chain. Returns NOT_FOUND if the chain does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_chains_get_execute()` to send, or `mybusinessbusinessinformation_chains_get` for simplest API.

pub fn mybusinessbusinessinformation_chains_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessbusinessinformation.googleapis.com/v1/chains/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/chains/{chainsId}
/// Gets the specified chain. Returns NOT_FOUND if the chain does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_chains_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_chains_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_chains_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Chain>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Chain = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/chains/{chainsId}
/// Gets the specified chain. Returns NOT_FOUND if the chain does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_chains_get_builder()` + `mybusinessbusinessinformation_chains_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_chains_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Chain>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_chains_get_builder(client, name)?;
    mybusinessbusinessinformation_chains_get_execute(builder)
}

/// GET v1/chains:search
/// Searches the chain based on chain name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_chains_search_execute()` to send, or `mybusinessbusinessinformation_chains_search` for simplest API.

pub fn mybusinessbusinessinformation_chains_search_builder(
    client: &SimpleHttpClient,
    chainName: Option<&str>,
    pageSize: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://mybusinessbusinessinformation.googleapis.com/v1/chains:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = chainName {
        query_parts.push(format!("chainName={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/chains:search
/// Searches the chain based on chain name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_chains_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_chains_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_chains_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchChainsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchChainsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/chains:search
/// Searches the chain based on chain name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_chains_search_builder()` + `mybusinessbusinessinformation_chains_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_chains_search(
    client: &SimpleHttpClient,
    chainName: Option<&str>,
    pageSize: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchChainsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_chains_search_builder(client, chainName, pageSize)?;
    mybusinessbusinessinformation_chains_search_execute(builder)
}

/// GET v1/googleLocations:search
/// Search all of the possible locations that are a match to the specified request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_google_locations_search_execute()` to send, or `mybusinessbusinessinformation_google_locations_search` for simplest API.

pub fn mybusinessbusinessinformation_google_locations_search_builder(
    client: &SimpleHttpClient,
    body: &SearchGoogleLocationsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url =
        format!("https://mybusinessbusinessinformation.googleapis.com/v1/googleLocations:search",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/googleLocations:search
/// Search all of the possible locations that are a match to the specified request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_google_locations_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_google_locations_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_google_locations_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchGoogleLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchGoogleLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/googleLocations:search
/// Search all of the possible locations that are a match to the specified request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_google_locations_search_builder()` + `mybusinessbusinessinformation_google_locations_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_google_locations_search(
    client: &SimpleHttpClient,
    body: &SearchGoogleLocationsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchGoogleLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_google_locations_search_builder(client, body)?;
    mybusinessbusinessinformation_google_locations_search_execute(builder)
}

/// GET v1/locations/{locationsId}
/// Deletes a location. If this location cannot be deleted using the API and it is marked so in the google.mybusiness.businessinformation.v1.LocationState, use the [Google Business Profile](<https://business.google.`com/manage/`>) website.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_locations_delete_execute()` to send, or `mybusinessbusinessinformation_locations_delete` for simplest API.

pub fn mybusinessbusinessinformation_locations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessbusinessinformation.googleapis.com/v1/locations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}
/// Deletes a location. If this location cannot be deleted using the API and it is marked so in the google.mybusiness.businessinformation.v1.LocationState, use the [Google Business Profile](<https://business.google.`com/manage/`>) website.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_locations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_locations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}
/// Deletes a location. If this location cannot be deleted using the API and it is marked so in the google.mybusiness.businessinformation.v1.LocationState, use the [Google Business Profile](<https://business.google.`com/manage/`>) website.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_locations_delete_builder()` + `mybusinessbusinessinformation_locations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_locations_delete_builder(client, name)?;
    mybusinessbusinessinformation_locations_delete_execute(builder)
}

/// GET v1/locations/{locationsId}
/// Returns the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_locations_get_execute()` to send, or `mybusinessbusinessinformation_locations_get` for simplest API.

pub fn mybusinessbusinessinformation_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessbusinessinformation.googleapis.com/v1/locations/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}
/// Returns the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}
/// Returns the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_locations_get_builder()` + `mybusinessbusinessinformation_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_get(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_locations_get_builder(client, name, readMask)?;
    mybusinessbusinessinformation_locations_get_execute(builder)
}

/// GET v1/locations/{locationsId}/attributes
/// Looks up all the attributes set for a given location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_locations_get_attributes_execute()` to send, or `mybusinessbusinessinformation_locations_get_attributes` for simplest API.

pub fn mybusinessbusinessinformation_locations_get_attributes_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessbusinessinformation.googleapis.com/v1/locations/{}/attributes",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/attributes
/// Looks up all the attributes set for a given location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_locations_get_attributes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_locations_get_attributes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_get_attributes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attributes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Attributes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}/attributes
/// Looks up all the attributes set for a given location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_locations_get_attributes_builder()` + `mybusinessbusinessinformation_locations_get_attributes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_get_attributes(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attributes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_locations_get_attributes_builder(client, name)?;
    mybusinessbusinessinformation_locations_get_attributes_execute(builder)
}

/// GET v1/locations/{locationsId}:getGoogleUpdated
/// Gets the Google-updated version of the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_locations_get_google_updated_execute()` to send, or `mybusinessbusinessinformation_locations_get_google_updated` for simplest API.

pub fn mybusinessbusinessinformation_locations_get_google_updated_builder(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessbusinessinformation.googleapis.com/v1/locations/{}:getGoogleUpdated",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}:getGoogleUpdated
/// Gets the Google-updated version of the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_locations_get_google_updated_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_locations_get_google_updated_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_get_google_updated_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleUpdatedLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleUpdatedLocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}:getGoogleUpdated
/// Gets the Google-updated version of the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_locations_get_google_updated_builder()` + `mybusinessbusinessinformation_locations_get_google_updated_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_get_google_updated(
    client: &SimpleHttpClient,
    name: &str,
    readMask: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleUpdatedLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        mybusinessbusinessinformation_locations_get_google_updated_builder(client, name, readMask)?;
    mybusinessbusinessinformation_locations_get_google_updated_execute(builder)
}

/// GET v1/locations/{locationsId}
/// Updates the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_locations_patch_execute()` to send, or `mybusinessbusinessinformation_locations_patch` for simplest API.

pub fn mybusinessbusinessinformation_locations_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Location,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessbusinessinformation.googleapis.com/v1/locations/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}
/// Updates the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_locations_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_locations_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}
/// Updates the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_locations_patch_builder()` + `mybusinessbusinessinformation_locations_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Location,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_locations_patch_builder(
        client,
        name,
        updateMask,
        validateOnly,
        body,
    )?;
    mybusinessbusinessinformation_locations_patch_execute(builder)
}

/// GET v1/locations/{locationsId}/attributes
/// Update attributes for a given location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_locations_update_attributes_execute()` to send, or `mybusinessbusinessinformation_locations_update_attributes` for simplest API.

pub fn mybusinessbusinessinformation_locations_update_attributes_builder(
    client: &SimpleHttpClient,
    name: &str,
    attributeMask: Option<&str>,
    body: &Attributes,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessbusinessinformation.googleapis.com/v1/locations/{}/attributes",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = attributeMask {
        query_parts.push(format!("attributeMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}/attributes
/// Update attributes for a given location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_locations_update_attributes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_locations_update_attributes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_update_attributes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attributes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Attributes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}/attributes
/// Update attributes for a given location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_locations_update_attributes_builder()` + `mybusinessbusinessinformation_locations_update_attributes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_update_attributes(
    client: &SimpleHttpClient,
    name: &str,
    attributeMask: Option<&str>,
    body: &Attributes,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attributes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_locations_update_attributes_builder(
        client,
        name,
        attributeMask,
        body,
    )?;
    mybusinessbusinessinformation_locations_update_attributes_execute(builder)
}

/// GET v1/locations/{locationsId}/attributes:getGoogleUpdated
/// Gets the Google-updated version of the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessbusinessinformation_locations_attributes_get_google_updated_execute()` to send, or `mybusinessbusinessinformation_locations_attributes_get_google_updated` for simplest API.

pub fn mybusinessbusinessinformation_locations_attributes_get_google_updated_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessbusinessinformation.googleapis.com/v1/locations/{}/attributes:getGoogleUpdated",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/attributes:getGoogleUpdated
/// Gets the Google-updated version of the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessbusinessinformation_locations_attributes_get_google_updated_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessbusinessinformation_locations_attributes_get_google_updated_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_attributes_get_google_updated_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attributes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Attributes = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}/attributes:getGoogleUpdated
/// Gets the Google-updated version of the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessbusinessinformation_locations_attributes_get_google_updated_builder()` + `mybusinessbusinessinformation_locations_attributes_get_google_updated_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessbusinessinformation_locations_attributes_get_google_updated(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Attributes>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessbusinessinformation_locations_attributes_get_google_updated_builder(
        client, name,
    )?;
    mybusinessbusinessinformation_locations_attributes_get_google_updated_execute(builder)
}
