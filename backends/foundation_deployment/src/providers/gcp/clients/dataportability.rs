//! Auto-generated API clients for gcp/dataportability.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/dataportability`

#![cfg(feature = "gcp/dataportability")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/accessType:check
/// Gets the access type of the token.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataportability_access_type_check_execute()` to send, or `dataportability_access_type_check` for simplest API.

pub fn dataportability_access_type_check_builder(
    client: &SimpleHttpClient,
    body: &CheckAccessTypeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://dataportability.googleapis.com/v1/accessType:check",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accessType:check
/// Gets the access type of the token.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataportability_access_type_check_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataportability_access_type_check_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_access_type_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckAccessTypeResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckAccessTypeResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataportability_access_type_check`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataportabilityAccessTypeCheckArgs {
    /// Request body.
    pub body: CheckAccessTypeRequest,
}

/// GET v1/accessType:check
/// Gets the access type of the token.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataportability_access_type_check_builder()` + `dataportability_access_type_check_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_access_type_check(
    client: &SimpleHttpClient,
    args: &DataportabilityAccessTypeCheckArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckAccessTypeResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataportability_access_type_check_builder(client, &args.body)?;
    dataportability_access_type_check_execute(builder)
}

/// GET v1/archiveJobs/{archiveJobsId}:cancel
/// Cancels a Portability Archive job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataportability_archive_jobs_cancel_execute()` to send, or `dataportability_archive_jobs_cancel` for simplest API.

pub fn dataportability_archive_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelPortabilityArchiveRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataportability.googleapis.com/v1/archiveJobs/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/archiveJobs/{archiveJobsId}:cancel
/// Cancels a Portability Archive job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataportability_archive_jobs_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataportability_archive_jobs_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_archive_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CancelPortabilityArchiveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CancelPortabilityArchiveResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataportability_archive_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataportabilityArchiveJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: CancelPortabilityArchiveRequest,
}

/// GET v1/archiveJobs/{archiveJobsId}:cancel
/// Cancels a Portability Archive job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataportability_archive_jobs_cancel_builder()` + `dataportability_archive_jobs_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_archive_jobs_cancel(
    client: &SimpleHttpClient,
    args: &DataportabilityArchiveJobsCancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CancelPortabilityArchiveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataportability_archive_jobs_cancel_builder(client, &args.name, &args.body)?;
    dataportability_archive_jobs_cancel_execute(builder)
}

/// GET v1/archiveJobs/{archiveJobsId}/portabilityArchiveState
/// Retrieves the state of an Archive job for the Portability API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataportability_archive_jobs_get_portability_archive_state_execute()` to send, or `dataportability_archive_jobs_get_portability_archive_state` for simplest API.

pub fn dataportability_archive_jobs_get_portability_archive_state_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataportability.googleapis.com/v1/archiveJobs/{}/portabilityArchiveState",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/archiveJobs/{archiveJobsId}/portabilityArchiveState
/// Retrieves the state of an Archive job for the Portability API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataportability_archive_jobs_get_portability_archive_state_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataportability_archive_jobs_get_portability_archive_state_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_archive_jobs_get_portability_archive_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PortabilityArchiveState>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PortabilityArchiveState = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataportability_archive_jobs_get_portability_archive_state`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataportabilityArchiveJobsGetPortabilityArchiveStateArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/archiveJobs/{archiveJobsId}/portabilityArchiveState
/// Retrieves the state of an Archive job for the Portability API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataportability_archive_jobs_get_portability_archive_state_builder()` + `dataportability_archive_jobs_get_portability_archive_state_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_archive_jobs_get_portability_archive_state(
    client: &SimpleHttpClient,
    args: &DataportabilityArchiveJobsGetPortabilityArchiveStateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PortabilityArchiveState>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataportability_archive_jobs_get_portability_archive_state_builder(client, &args.name)?;
    dataportability_archive_jobs_get_portability_archive_state_execute(builder)
}

/// GET v1/archiveJobs/{archiveJobsId}:retry
/// Retries a failed Portability Archive job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataportability_archive_jobs_retry_execute()` to send, or `dataportability_archive_jobs_retry` for simplest API.

pub fn dataportability_archive_jobs_retry_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RetryPortabilityArchiveRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataportability.googleapis.com/v1/archiveJobs/{}:retry",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/archiveJobs/{archiveJobsId}:retry
/// Retries a failed Portability Archive job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataportability_archive_jobs_retry_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataportability_archive_jobs_retry_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_archive_jobs_retry_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetryPortabilityArchiveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RetryPortabilityArchiveResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataportability_archive_jobs_retry`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataportabilityArchiveJobsRetryArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: RetryPortabilityArchiveRequest,
}

/// GET v1/archiveJobs/{archiveJobsId}:retry
/// Retries a failed Portability Archive job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataportability_archive_jobs_retry_builder()` + `dataportability_archive_jobs_retry_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_archive_jobs_retry(
    client: &SimpleHttpClient,
    args: &DataportabilityArchiveJobsRetryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetryPortabilityArchiveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataportability_archive_jobs_retry_builder(client, &args.name, &args.body)?;
    dataportability_archive_jobs_retry_execute(builder)
}

/// GET v1/authorization:reset
/// Revokes OAuth tokens and resets exhausted scopes for a `user/project` pair. This method allows you to initiate a request after a new consent is granted. This method also indicates that previous archives can be garbage collected. You should call this method when all jobs are complete and all archives are downloaded. Do not call it only when you start a new job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataportability_authorization_reset_execute()` to send, or `dataportability_authorization_reset` for simplest API.

pub fn dataportability_authorization_reset_builder(
    client: &SimpleHttpClient,
    body: &ResetAuthorizationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://dataportability.googleapis.com/v1/authorization:reset",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/authorization:reset
/// Revokes OAuth tokens and resets exhausted scopes for a `user/project` pair. This method allows you to initiate a request after a new consent is granted. This method also indicates that previous archives can be garbage collected. You should call this method when all jobs are complete and all archives are downloaded. Do not call it only when you start a new job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataportability_authorization_reset_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataportability_authorization_reset_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_authorization_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataportability_authorization_reset`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataportabilityAuthorizationResetArgs {
    /// Request body.
    pub body: ResetAuthorizationRequest,
}

/// GET v1/authorization:reset
/// Revokes OAuth tokens and resets exhausted scopes for a `user/project` pair. This method allows you to initiate a request after a new consent is granted. This method also indicates that previous archives can be garbage collected. You should call this method when all jobs are complete and all archives are downloaded. Do not call it only when you start a new job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataportability_authorization_reset_builder()` + `dataportability_authorization_reset_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_authorization_reset(
    client: &SimpleHttpClient,
    args: &DataportabilityAuthorizationResetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataportability_authorization_reset_builder(client, &args.body)?;
    dataportability_authorization_reset_execute(builder)
}

/// GET v1/portabilityArchive:initiate
/// Initiates a new Archive job for the Portability API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataportability_portability_archive_initiate_execute()` to send, or `dataportability_portability_archive_initiate` for simplest API.

pub fn dataportability_portability_archive_initiate_builder(
    client: &SimpleHttpClient,
    body: &InitiatePortabilityArchiveRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://dataportability.googleapis.com/v1/portabilityArchive:initiate",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/portabilityArchive:initiate
/// Initiates a new Archive job for the Portability API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataportability_portability_archive_initiate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataportability_portability_archive_initiate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_portability_archive_initiate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InitiatePortabilityArchiveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InitiatePortabilityArchiveResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataportability_portability_archive_initiate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataportabilityPortabilityArchiveInitiateArgs {
    /// Request body.
    pub body: InitiatePortabilityArchiveRequest,
}

/// GET v1/portabilityArchive:initiate
/// Initiates a new Archive job for the Portability API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataportability_portability_archive_initiate_builder()` + `dataportability_portability_archive_initiate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_portability_archive_initiate(
    client: &SimpleHttpClient,
    args: &DataportabilityPortabilityArchiveInitiateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InitiatePortabilityArchiveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataportability_portability_archive_initiate_builder(client, &args.body)?;
    dataportability_portability_archive_initiate_execute(builder)
}
