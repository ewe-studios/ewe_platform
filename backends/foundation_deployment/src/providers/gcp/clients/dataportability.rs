//! Auto-generated API clients for gcp/dataportability.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/accessType:check
/// Gets the access type of the token.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataportability_access_type_check_execute()` to send, or `dataportability_access_type_check` for simplest API.

pub fn dataportability_access_type_check_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://dataportability.googleapis.com/v1/accessType:check",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/accessType:check
/// Gets the access type of the token.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataportability_access_type_check_execute()` or `dataportability_access_type_check`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataportability_access_type_check_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_access_type_check_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CheckAccessTypeResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckAccessTypeResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/accessType:check
/// Gets the access type of the token.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataportability_access_type_check_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataportability_access_type_check_task()`.
/// For the simplest API, use `dataportability_access_type_check()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataportability_access_type_check_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataportability_access_type_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckAccessTypeResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataportability_access_type_check_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/accessType:check
/// Gets the access type of the token.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataportability_access_type_check_builder()` + `dataportability_access_type_check_execute()`.
/// For task-level control, use `dataportability_access_type_check_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_access_type_check(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CheckAccessTypeResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataportability_access_type_check_builder(client)?;
    dataportability_access_type_check_execute(builder)
}

/// POST v1/archiveJobs/{archiveJobsId}:cancel
/// Cancels a Portability Archive job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataportability_archive_jobs_cancel_execute()` to send, or `dataportability_archive_jobs_cancel` for simplest API.

pub fn dataportability_archive_jobs_cancel_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataportability.googleapis.com/v1/archiveJobs/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/archiveJobs/{archiveJobsId}:cancel
/// Cancels a Portability Archive job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataportability_archive_jobs_cancel_execute()` or `dataportability_archive_jobs_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataportability_archive_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_archive_jobs_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CancelPortabilityArchiveResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CancelPortabilityArchiveResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/archiveJobs/{archiveJobsId}:cancel
/// Cancels a Portability Archive job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataportability_archive_jobs_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataportability_archive_jobs_cancel_task()`.
/// For the simplest API, use `dataportability_archive_jobs_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataportability_archive_jobs_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataportability_archive_jobs_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CancelPortabilityArchiveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataportability_archive_jobs_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataportability_archive_jobs_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataportabilityArchiveJobsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/archiveJobs/{archiveJobsId}:cancel
/// Cancels a Portability Archive job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataportability_archive_jobs_cancel_builder()` + `dataportability_archive_jobs_cancel_execute()`.
/// For task-level control, use `dataportability_archive_jobs_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_archive_jobs_cancel(
    client: &SimpleHttpClient,
    args: &DataportabilityArchiveJobsCancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CancelPortabilityArchiveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataportability_archive_jobs_cancel_builder(client, &args.name)?;
    dataportability_archive_jobs_cancel_execute(builder)
}

/// GET v1/archiveJobs/{archiveJobsId}/portabilityArchiveState
/// Retrieves the state of an Archive job for the Portability API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataportability_archive_jobs_get_portability_archive_state_execute()` to send, or `dataportability_archive_jobs_get_portability_archive_state` for simplest API.

pub fn dataportability_archive_jobs_get_portability_archive_state_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataportability.googleapis.com/v1/archiveJobs/{}/portabilityArchiveState",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/archiveJobs/{archiveJobsId}/portabilityArchiveState
/// Retrieves the state of an Archive job for the Portability API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataportability_archive_jobs_get_portability_archive_state_execute()` or `dataportability_archive_jobs_get_portability_archive_state`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataportability_archive_jobs_get_portability_archive_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_archive_jobs_get_portability_archive_state_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PortabilityArchiveState>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PortabilityArchiveState = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/archiveJobs/{archiveJobsId}/portabilityArchiveState
/// Retrieves the state of an Archive job for the Portability API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataportability_archive_jobs_get_portability_archive_state_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataportability_archive_jobs_get_portability_archive_state_task()`.
/// For the simplest API, use `dataportability_archive_jobs_get_portability_archive_state()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataportability_archive_jobs_get_portability_archive_state_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataportability_archive_jobs_get_portability_archive_state_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PortabilityArchiveState>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataportability_archive_jobs_get_portability_archive_state_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataportability_archive_jobs_get_portability_archive_state`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataportabilityArchiveJobsGetPortabilityArchiveStateArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/archiveJobs/{archiveJobsId}/portabilityArchiveState
/// Retrieves the state of an Archive job for the Portability API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataportability_archive_jobs_get_portability_archive_state_builder()` + `dataportability_archive_jobs_get_portability_archive_state_execute()`.
/// For task-level control, use `dataportability_archive_jobs_get_portability_archive_state_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_archive_jobs_get_portability_archive_state(
    client: &SimpleHttpClient,
    args: &DataportabilityArchiveJobsGetPortabilityArchiveStateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PortabilityArchiveState>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataportability_archive_jobs_get_portability_archive_state_builder(client, &args.name)?;
    dataportability_archive_jobs_get_portability_archive_state_execute(builder)
}

/// POST v1/archiveJobs/{archiveJobsId}:retry
/// Retries a failed Portability Archive job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataportability_archive_jobs_retry_execute()` to send, or `dataportability_archive_jobs_retry` for simplest API.

pub fn dataportability_archive_jobs_retry_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataportability.googleapis.com/v1/archiveJobs/{}:retry",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/archiveJobs/{archiveJobsId}:retry
/// Retries a failed Portability Archive job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataportability_archive_jobs_retry_execute()` or `dataportability_archive_jobs_retry`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataportability_archive_jobs_retry_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_archive_jobs_retry_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RetryPortabilityArchiveResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RetryPortabilityArchiveResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/archiveJobs/{archiveJobsId}:retry
/// Retries a failed Portability Archive job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataportability_archive_jobs_retry_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataportability_archive_jobs_retry_task()`.
/// For the simplest API, use `dataportability_archive_jobs_retry()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataportability_archive_jobs_retry_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataportability_archive_jobs_retry_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetryPortabilityArchiveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataportability_archive_jobs_retry_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataportability_archive_jobs_retry`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataportabilityArchiveJobsRetryArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/archiveJobs/{archiveJobsId}:retry
/// Retries a failed Portability Archive job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataportability_archive_jobs_retry_builder()` + `dataportability_archive_jobs_retry_execute()`.
/// For task-level control, use `dataportability_archive_jobs_retry_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_archive_jobs_retry(
    client: &SimpleHttpClient,
    args: &DataportabilityArchiveJobsRetryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RetryPortabilityArchiveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataportability_archive_jobs_retry_builder(client, &args.name)?;
    dataportability_archive_jobs_retry_execute(builder)
}

/// POST v1/authorization:reset
/// Revokes OAuth tokens and resets exhausted scopes for a `user/project` pair. This method allows you to initiate a request after a new consent is granted. This method also indicates that previous archives can be garbage collected. You should call this method when all jobs are complete and all archives are downloaded. Do not call it only when you start a new job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataportability_authorization_reset_execute()` to send, or `dataportability_authorization_reset` for simplest API.

pub fn dataportability_authorization_reset_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://dataportability.googleapis.com/v1/authorization:reset",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/authorization:reset
/// Revokes OAuth tokens and resets exhausted scopes for a `user/project` pair. This method allows you to initiate a request after a new consent is granted. This method also indicates that previous archives can be garbage collected. You should call this method when all jobs are complete and all archives are downloaded. Do not call it only when you start a new job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataportability_authorization_reset_execute()` or `dataportability_authorization_reset`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataportability_authorization_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_authorization_reset_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/authorization:reset
/// Revokes OAuth tokens and resets exhausted scopes for a `user/project` pair. This method allows you to initiate a request after a new consent is granted. This method also indicates that previous archives can be garbage collected. You should call this method when all jobs are complete and all archives are downloaded. Do not call it only when you start a new job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataportability_authorization_reset_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataportability_authorization_reset_task()`.
/// For the simplest API, use `dataportability_authorization_reset()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataportability_authorization_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataportability_authorization_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataportability_authorization_reset_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/authorization:reset
/// Revokes OAuth tokens and resets exhausted scopes for a `user/project` pair. This method allows you to initiate a request after a new consent is granted. This method also indicates that previous archives can be garbage collected. You should call this method when all jobs are complete and all archives are downloaded. Do not call it only when you start a new job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataportability_authorization_reset_builder()` + `dataportability_authorization_reset_execute()`.
/// For task-level control, use `dataportability_authorization_reset_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_authorization_reset(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataportability_authorization_reset_builder(client)?;
    dataportability_authorization_reset_execute(builder)
}

/// POST v1/portabilityArchive:initiate
/// Initiates a new Archive job for the Portability API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataportability_portability_archive_initiate_execute()` to send, or `dataportability_portability_archive_initiate` for simplest API.

pub fn dataportability_portability_archive_initiate_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://dataportability.googleapis.com/v1/portabilityArchive:initiate",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/portabilityArchive:initiate
/// Initiates a new Archive job for the Portability API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataportability_portability_archive_initiate_execute()` or `dataportability_portability_archive_initiate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataportability_portability_archive_initiate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_portability_archive_initiate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InitiatePortabilityArchiveResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InitiatePortabilityArchiveResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/portabilityArchive:initiate
/// Initiates a new Archive job for the Portability API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataportability_portability_archive_initiate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataportability_portability_archive_initiate_task()`.
/// For the simplest API, use `dataportability_portability_archive_initiate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataportability_portability_archive_initiate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataportability_portability_archive_initiate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InitiatePortabilityArchiveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataportability_portability_archive_initiate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/portabilityArchive:initiate
/// Initiates a new Archive job for the Portability API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataportability_portability_archive_initiate_builder()` + `dataportability_portability_archive_initiate_execute()`.
/// For task-level control, use `dataportability_portability_archive_initiate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataportability_portability_archive_initiate(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InitiatePortabilityArchiveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataportability_portability_archive_initiate_builder(client)?;
    dataportability_portability_archive_initiate_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for CheckAccessTypeResponse
// =============================================================================

/// ResourceIdentifier implementation for CheckAccessTypeResponse with DataportabilityAccessTypeCheckArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataportabilityAccessTypeCheckArgs> for CheckAccessTypeResponse {
    fn generate_resource_id(&self, input: &DataportabilityAccessTypeCheckArgs) -> String {
        "gcp::dataportability::CheckAccessTypeResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataportability::CheckAccessTypeResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CancelPortabilityArchiveResponse
// =============================================================================

/// ResourceIdentifier implementation for CancelPortabilityArchiveResponse with DataportabilityArchiveJobsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataportabilityArchiveJobsCancelArgs> for CancelPortabilityArchiveResponse {
    fn generate_resource_id(&self, input: &DataportabilityArchiveJobsCancelArgs) -> String {
        format!(
            "gcp::dataportability::CancelPortabilityArchiveResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataportability::CancelPortabilityArchiveResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PortabilityArchiveState
// =============================================================================

/// ResourceIdentifier implementation for PortabilityArchiveState with DataportabilityArchiveJobsGetPortabilityArchiveStateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataportabilityArchiveJobsGetPortabilityArchiveStateArgs>
    for PortabilityArchiveState
{
    fn generate_resource_id(
        &self,
        input: &DataportabilityArchiveJobsGetPortabilityArchiveStateArgs,
    ) -> String {
        format!(
            "gcp::dataportability::PortabilityArchiveState/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataportability::PortabilityArchiveState"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RetryPortabilityArchiveResponse
// =============================================================================

/// ResourceIdentifier implementation for RetryPortabilityArchiveResponse with DataportabilityArchiveJobsRetryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataportabilityArchiveJobsRetryArgs> for RetryPortabilityArchiveResponse {
    fn generate_resource_id(&self, input: &DataportabilityArchiveJobsRetryArgs) -> String {
        format!(
            "gcp::dataportability::RetryPortabilityArchiveResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataportability::RetryPortabilityArchiveResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DataportabilityAuthorizationResetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataportabilityAuthorizationResetArgs> for Empty {
    fn generate_resource_id(&self, input: &DataportabilityAuthorizationResetArgs) -> String {
        "gcp::dataportability::Empty".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataportability::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InitiatePortabilityArchiveResponse
// =============================================================================

/// ResourceIdentifier implementation for InitiatePortabilityArchiveResponse with DataportabilityPortabilityArchiveInitiateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DataportabilityPortabilityArchiveInitiateArgs>
    for InitiatePortabilityArchiveResponse
{
    fn generate_resource_id(
        &self,
        input: &DataportabilityPortabilityArchiveInitiateArgs,
    ) -> String {
        "gcp::dataportability::InitiatePortabilityArchiveResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dataportability::InitiatePortabilityArchiveResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
