//! Auto-generated API clients for gcp/firebaserules.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/firebaserules`

#![cfg(feature = "gcp/firebaserules")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/projects/{projectsId}:test
/// Test Source for syntactic and semantic correctness. Issues present, if any, will be returned to the caller with a description, severity, and source location. The test method may be executed with Source or a Ruleset name. Passing Source is useful for unit testing new rules. Passing a Ruleset name is useful for regression testing an existing rule. The following is an example of Source that permits users to upload images to a bucket bearing their user id and matching the correct metadata: _*Example*_ // Users are allowed to subscribe and unsubscribe to the blog. service firebase.storage { match /`users/{`userId`}/images/{`imageName`}` { allow write: if `userId` == request.auth.uid && (`imageName`.matches('*.png$') || `imageName`.matches('*.jpg$')) && resource.`mimeType`.matches('^image/') } }
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaserules_projects_test_execute()` to send, or `firebaserules_projects_test` for simplest API.

pub fn firebaserules_projects_test_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &TestRulesetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebaserules.googleapis.com/v1/projects/{}:test",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:test
/// Test Source for syntactic and semantic correctness. Issues present, if any, will be returned to the caller with a description, severity, and source location. The test method may be executed with Source or a Ruleset name. Passing Source is useful for unit testing new rules. Passing a Ruleset name is useful for regression testing an existing rule. The following is an example of Source that permits users to upload images to a bucket bearing their user id and matching the correct metadata: _*Example*_ // Users are allowed to subscribe and unsubscribe to the blog. service firebase.storage { match /`users/{`userId`}/images/{`imageName`}` { allow write: if `userId` == request.auth.uid && (`imageName`.matches('*.png$') || `imageName`.matches('*.jpg$')) && resource.`mimeType`.matches('^image/') } }
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaserules_projects_test_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaserules_projects_test_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestRulesetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestRulesetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:test
/// Test Source for syntactic and semantic correctness. Issues present, if any, will be returned to the caller with a description, severity, and source location. The test method may be executed with Source or a Ruleset name. Passing Source is useful for unit testing new rules. Passing a Ruleset name is useful for regression testing an existing rule. The following is an example of Source that permits users to upload images to a bucket bearing their user id and matching the correct metadata: _*Example*_ // Users are allowed to subscribe and unsubscribe to the blog. service firebase.storage { match /`users/{`userId`}/images/{`imageName`}` { allow write: if `userId` == request.auth.uid && (`imageName`.matches('*.png$') || `imageName`.matches('*.jpg$')) && resource.`mimeType`.matches('^image/') } }
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaserules_projects_test_builder()` + `firebaserules_projects_test_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_test(
    client: &SimpleHttpClient,
    name: &str,
    body: &TestRulesetRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestRulesetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebaserules_projects_test_builder(client, name, body)?;
    firebaserules_projects_test_execute(builder)
}

/// GET v1/projects/{projectsId}/releases
/// Create a Release. Release names should reflect the developer's deployment practices. For example, the release name may include the environment name, application name, application version, or any other name meaningful to the developer. Once a Release refers to a Ruleset, the rules can be enforced by Firebase Rules-enabled services. More than one Release may be 'live' concurrently. Consider the following three Release names for `projects/foo` and the Ruleset to which they refer. Release Name -&gt; Ruleset Name * `projects/foo/releases/prod` -&gt; `projects/foo/rulesets/uuid123` * `projects/foo/releases/prod/beta` -&gt; `projects/foo/rulesets/uuid123` * `projects/foo/releases/prod/v23` -&gt; `projects/foo/rulesets/uuid456` The relationships reflect a Ruleset rollout in progress. The prod and `prod/beta` releases refer to the same Ruleset. However, `prod/v23` refers to a new Ruleset. The Ruleset reference for a Release may be updated using the UpdateRelease method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaserules_projects_releases_create_execute()` to send, or `firebaserules_projects_releases_create` for simplest API.

pub fn firebaserules_projects_releases_create_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &Release,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebaserules.googleapis.com/v1/projects/{}/releases",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/releases
/// Create a Release. Release names should reflect the developer's deployment practices. For example, the release name may include the environment name, application name, application version, or any other name meaningful to the developer. Once a Release refers to a Ruleset, the rules can be enforced by Firebase Rules-enabled services. More than one Release may be 'live' concurrently. Consider the following three Release names for `projects/foo` and the Ruleset to which they refer. Release Name -&gt; Ruleset Name * `projects/foo/releases/prod` -&gt; `projects/foo/rulesets/uuid123` * `projects/foo/releases/prod/beta` -&gt; `projects/foo/rulesets/uuid123` * `projects/foo/releases/prod/v23` -&gt; `projects/foo/rulesets/uuid456` The relationships reflect a Ruleset rollout in progress. The prod and `prod/beta` releases refer to the same Ruleset. However, `prod/v23` refers to a new Ruleset. The Ruleset reference for a Release may be updated using the UpdateRelease method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaserules_projects_releases_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaserules_projects_releases_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_releases_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Release = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/releases
/// Create a Release. Release names should reflect the developer's deployment practices. For example, the release name may include the environment name, application name, application version, or any other name meaningful to the developer. Once a Release refers to a Ruleset, the rules can be enforced by Firebase Rules-enabled services. More than one Release may be 'live' concurrently. Consider the following three Release names for `projects/foo` and the Ruleset to which they refer. Release Name -&gt; Ruleset Name * `projects/foo/releases/prod` -&gt; `projects/foo/rulesets/uuid123` * `projects/foo/releases/prod/beta` -&gt; `projects/foo/rulesets/uuid123` * `projects/foo/releases/prod/v23` -&gt; `projects/foo/rulesets/uuid456` The relationships reflect a Ruleset rollout in progress. The prod and `prod/beta` releases refer to the same Ruleset. However, `prod/v23` refers to a new Ruleset. The Ruleset reference for a Release may be updated using the UpdateRelease method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaserules_projects_releases_create_builder()` + `firebaserules_projects_releases_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_releases_create(
    client: &SimpleHttpClient,
    name: &str,
    body: &Release,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebaserules_projects_releases_create_builder(client, name, body)?;
    firebaserules_projects_releases_create_execute(builder)
}

/// GET v1/projects/{projectsId}/releases/{releasesId}
/// Delete a Release by resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaserules_projects_releases_delete_execute()` to send, or `firebaserules_projects_releases_delete` for simplest API.

pub fn firebaserules_projects_releases_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebaserules.googleapis.com/v1/projects/{}/releases/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/releases/{releasesId}
/// Delete a Release by resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaserules_projects_releases_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaserules_projects_releases_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_releases_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/releases/{releasesId}
/// Delete a Release by resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaserules_projects_releases_delete_builder()` + `firebaserules_projects_releases_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_releases_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebaserules_projects_releases_delete_builder(client, name)?;
    firebaserules_projects_releases_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/releases/{releasesId}
/// Get a Release by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaserules_projects_releases_get_execute()` to send, or `firebaserules_projects_releases_get` for simplest API.

pub fn firebaserules_projects_releases_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebaserules.googleapis.com/v1/projects/{}/releases/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/releases/{releasesId}
/// Get a Release by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaserules_projects_releases_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaserules_projects_releases_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_releases_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Release = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/releases/{releasesId}
/// Get a Release by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaserules_projects_releases_get_builder()` + `firebaserules_projects_releases_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_releases_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebaserules_projects_releases_get_builder(client, name)?;
    firebaserules_projects_releases_get_execute(builder)
}

/// GET v1/projects/{projectsId}/releases/{releasesId}:getExecutable
/// Get the Release executable to use when enforcing rules.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaserules_projects_releases_get_executable_execute()` to send, or `firebaserules_projects_releases_get_executable` for simplest API.

pub fn firebaserules_projects_releases_get_executable_builder(
    client: &SimpleHttpClient,
    name: &str,
    executableVersion: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebaserules.googleapis.com/v1/projects/{}/releases/{}:getExecutable",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = executableVersion {
        query_parts.push(format!("executableVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/releases/{releasesId}:getExecutable
/// Get the Release executable to use when enforcing rules.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaserules_projects_releases_get_executable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaserules_projects_releases_get_executable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_releases_get_executable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetReleaseExecutableResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetReleaseExecutableResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/releases/{releasesId}:getExecutable
/// Get the Release executable to use when enforcing rules.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaserules_projects_releases_get_executable_builder()` + `firebaserules_projects_releases_get_executable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_releases_get_executable(
    client: &SimpleHttpClient,
    name: &str,
    executableVersion: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetReleaseExecutableResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaserules_projects_releases_get_executable_builder(client, name, executableVersion)?;
    firebaserules_projects_releases_get_executable_execute(builder)
}

/// GET v1/projects/{projectsId}/releases
/// List the Release values for a project. This list may optionally be filtered by Release name, Ruleset name, TestSuite name, or any combination thereof.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaserules_projects_releases_list_execute()` to send, or `firebaserules_projects_releases_list` for simplest API.

pub fn firebaserules_projects_releases_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebaserules.googleapis.com/v1/projects/{}/releases",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/releases
/// List the Release values for a project. This list may optionally be filtered by Release name, Ruleset name, TestSuite name, or any combination thereof.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaserules_projects_releases_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaserules_projects_releases_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_releases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReleasesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReleasesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/releases
/// List the Release values for a project. This list may optionally be filtered by Release name, Ruleset name, TestSuite name, or any combination thereof.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaserules_projects_releases_list_builder()` + `firebaserules_projects_releases_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_releases_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReleasesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaserules_projects_releases_list_builder(client, name, filter, pageSize, pageToken)?;
    firebaserules_projects_releases_list_execute(builder)
}

/// GET v1/projects/{projectsId}/releases/{releasesId}
/// Update a Release via PATCH. Only updates to ruleset_name will be honored. Release rename is not supported. To create a Release use the CreateRelease method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaserules_projects_releases_patch_execute()` to send, or `firebaserules_projects_releases_patch` for simplest API.

pub fn firebaserules_projects_releases_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpdateReleaseRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebaserules.googleapis.com/v1/projects/{}/releases/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/releases/{releasesId}
/// Update a Release via PATCH. Only updates to ruleset_name will be honored. Release rename is not supported. To create a Release use the CreateRelease method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaserules_projects_releases_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaserules_projects_releases_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_releases_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Release = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/releases/{releasesId}
/// Update a Release via PATCH. Only updates to ruleset_name will be honored. Release rename is not supported. To create a Release use the CreateRelease method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaserules_projects_releases_patch_builder()` + `firebaserules_projects_releases_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_releases_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpdateReleaseRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebaserules_projects_releases_patch_builder(client, name, body)?;
    firebaserules_projects_releases_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/rulesets
/// Create a Ruleset from Source. The Ruleset is given a unique generated name which is returned to the caller. Source containing syntactic or semantics errors will result in an error response indicating the first error encountered. For a detailed view of Source issues, use TestRuleset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaserules_projects_rulesets_create_execute()` to send, or `firebaserules_projects_rulesets_create` for simplest API.

pub fn firebaserules_projects_rulesets_create_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &Ruleset,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebaserules.googleapis.com/v1/projects/{}/rulesets",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/rulesets
/// Create a Ruleset from Source. The Ruleset is given a unique generated name which is returned to the caller. Source containing syntactic or semantics errors will result in an error response indicating the first error encountered. For a detailed view of Source issues, use TestRuleset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaserules_projects_rulesets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaserules_projects_rulesets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_rulesets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ruleset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Ruleset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/rulesets
/// Create a Ruleset from Source. The Ruleset is given a unique generated name which is returned to the caller. Source containing syntactic or semantics errors will result in an error response indicating the first error encountered. For a detailed view of Source issues, use TestRuleset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaserules_projects_rulesets_create_builder()` + `firebaserules_projects_rulesets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_rulesets_create(
    client: &SimpleHttpClient,
    name: &str,
    body: &Ruleset,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ruleset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebaserules_projects_rulesets_create_builder(client, name, body)?;
    firebaserules_projects_rulesets_create_execute(builder)
}

/// GET v1/projects/{projectsId}/rulesets/{rulesetsId}
/// Delete a Ruleset by resource name. If the Ruleset is referenced by a Release the operation will fail.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaserules_projects_rulesets_delete_execute()` to send, or `firebaserules_projects_rulesets_delete` for simplest API.

pub fn firebaserules_projects_rulesets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebaserules.googleapis.com/v1/projects/{}/rulesets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/rulesets/{rulesetsId}
/// Delete a Ruleset by resource name. If the Ruleset is referenced by a Release the operation will fail.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaserules_projects_rulesets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaserules_projects_rulesets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_rulesets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/rulesets/{rulesetsId}
/// Delete a Ruleset by resource name. If the Ruleset is referenced by a Release the operation will fail.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaserules_projects_rulesets_delete_builder()` + `firebaserules_projects_rulesets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_rulesets_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebaserules_projects_rulesets_delete_builder(client, name)?;
    firebaserules_projects_rulesets_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/rulesets/{rulesetsId}
/// Get a Ruleset by name including the full Source contents.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaserules_projects_rulesets_get_execute()` to send, or `firebaserules_projects_rulesets_get` for simplest API.

pub fn firebaserules_projects_rulesets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebaserules.googleapis.com/v1/projects/{}/rulesets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/rulesets/{rulesetsId}
/// Get a Ruleset by name including the full Source contents.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaserules_projects_rulesets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaserules_projects_rulesets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_rulesets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ruleset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Ruleset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/rulesets/{rulesetsId}
/// Get a Ruleset by name including the full Source contents.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaserules_projects_rulesets_get_builder()` + `firebaserules_projects_rulesets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_rulesets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ruleset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebaserules_projects_rulesets_get_builder(client, name)?;
    firebaserules_projects_rulesets_get_execute(builder)
}

/// GET v1/projects/{projectsId}/rulesets
/// List Ruleset metadata only and optionally filter the results by Ruleset name. The full Source contents of a Ruleset may be retrieved with GetRuleset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaserules_projects_rulesets_list_execute()` to send, or `firebaserules_projects_rulesets_list` for simplest API.

pub fn firebaserules_projects_rulesets_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://firebaserules.googleapis.com/v1/projects/{}/rulesets",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/rulesets
/// List Ruleset metadata only and optionally filter the results by Ruleset name. The full Source contents of a Ruleset may be retrieved with GetRuleset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `firebaserules_projects_rulesets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from firebaserules_projects_rulesets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_rulesets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRulesetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRulesetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/rulesets
/// List Ruleset metadata only and optionally filter the results by Ruleset name. The full Source contents of a Ruleset may be retrieved with GetRuleset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaserules_projects_rulesets_list_builder()` + `firebaserules_projects_rulesets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_rulesets_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRulesetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        firebaserules_projects_rulesets_list_builder(client, name, filter, pageSize, pageToken)?;
    firebaserules_projects_rulesets_list_execute(builder)
}
