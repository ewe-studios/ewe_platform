//! Auto-generated API clients for gcp/firebaserules.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}:test
/// Test Source for syntactic and semantic correctness. Issues present, if any, will be returned to the caller with a description, severity, and source location. The test method may be executed with Source or a Ruleset name. Passing Source is useful for unit testing new rules. Passing a Ruleset name is useful for regression testing an existing rule. The following is an example of Source that permits users to upload images to a bucket bearing their user id and matching the correct metadata: _*Example*_ // Users are allowed to subscribe and unsubscribe to the blog. service firebase.storage { match /`users/{`userId`}/images/{`imageName`}` { allow write: if `userId` == request.auth.uid && (`imageName`.matches('*.png$') || `imageName`.matches('*.jpg$')) && resource.`mimeType`.matches('^image/') } }
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaserules_projects_test_execute()` to send, or `firebaserules_projects_test` for simplest API.

pub fn firebaserules_projects_test_builder(
    client: &SimpleHttpClient,
    name: &String,
    body: &TestRulesetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://firebaserules.googleapis.com/v1/projects/{}:test",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:test
/// Test Source for syntactic and semantic correctness. Issues present, if any, will be returned to the caller with a description, severity, and source location. The test method may be executed with Source or a Ruleset name. Passing Source is useful for unit testing new rules. Passing a Ruleset name is useful for regression testing an existing rule. The following is an example of Source that permits users to upload images to a bucket bearing their user id and matching the correct metadata: _*Example*_ // Users are allowed to subscribe and unsubscribe to the blog. service firebase.storage { match /`users/{`userId`}/images/{`imageName`}` { allow write: if `userId` == request.auth.uid && (`imageName`.matches('*.png$') || `imageName`.matches('*.jpg$')) && resource.`mimeType`.matches('^image/') } }
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaserules_projects_test_execute()` or `firebaserules_projects_test`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaserules_projects_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_test_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestRulesetResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestRulesetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}:test
/// Test Source for syntactic and semantic correctness. Issues present, if any, will be returned to the caller with a description, severity, and source location. The test method may be executed with Source or a Ruleset name. Passing Source is useful for unit testing new rules. Passing a Ruleset name is useful for regression testing an existing rule. The following is an example of Source that permits users to upload images to a bucket bearing their user id and matching the correct metadata: _*Example*_ // Users are allowed to subscribe and unsubscribe to the blog. service firebase.storage { match /`users/{`userId`}/images/{`imageName`}` { allow write: if `userId` == request.auth.uid && (`imageName`.matches('*.png$') || `imageName`.matches('*.jpg$')) && resource.`mimeType`.matches('^image/') } }
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaserules_projects_test_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaserules_projects_test_task()`.
/// For the simplest API, use `firebaserules_projects_test()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaserules_projects_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaserules_projects_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestRulesetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebaserules_projects_test_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaserules_projects_test`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaserulesProjectsTestArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: TestRulesetRequest,
}

/// GET v1/projects/{projectsId}:test
/// Test Source for syntactic and semantic correctness. Issues present, if any, will be returned to the caller with a description, severity, and source location. The test method may be executed with Source or a Ruleset name. Passing Source is useful for unit testing new rules. Passing a Ruleset name is useful for regression testing an existing rule. The following is an example of Source that permits users to upload images to a bucket bearing their user id and matching the correct metadata: _*Example*_ // Users are allowed to subscribe and unsubscribe to the blog. service firebase.storage { match /`users/{`userId`}/images/{`imageName`}` { allow write: if `userId` == request.auth.uid && (`imageName`.matches('*.png$') || `imageName`.matches('*.jpg$')) && resource.`mimeType`.matches('^image/') } }
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaserules_projects_test_builder()` + `firebaserules_projects_test_execute()`.
/// For task-level control, use `firebaserules_projects_test_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_test(
    client: &SimpleHttpClient,
    args: &FirebaserulesProjectsTestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TestRulesetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebaserules_projects_test_builder(client, &args.name, &args.body)?;
    firebaserules_projects_test_execute(builder)
}

/// GET v1/projects/{projectsId}/releases
/// Create a Release. Release names should reflect the developer's deployment practices. For example, the release name may include the environment name, application name, application version, or any other name meaningful to the developer. Once a Release refers to a Ruleset, the rules can be enforced by Firebase Rules-enabled services. More than one Release may be 'live' concurrently. Consider the following three Release names for `projects/foo` and the Ruleset to which they refer. Release Name -&gt; Ruleset Name * `projects/foo/releases/prod` -&gt; `projects/foo/rulesets/uuid123` * `projects/foo/releases/prod/beta` -&gt; `projects/foo/rulesets/uuid123` * `projects/foo/releases/prod/v23` -&gt; `projects/foo/rulesets/uuid456` The relationships reflect a Ruleset rollout in progress. The prod and `prod/beta` releases refer to the same Ruleset. However, `prod/v23` refers to a new Ruleset. The Ruleset reference for a Release may be updated using the UpdateRelease method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaserules_projects_releases_create_execute()` to send, or `firebaserules_projects_releases_create` for simplest API.

pub fn firebaserules_projects_releases_create_builder(
    client: &SimpleHttpClient,
    name: &String,
    body: &Release,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://firebaserules.googleapis.com/v1/projects/{}/releases",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/releases
/// Create a Release. Release names should reflect the developer's deployment practices. For example, the release name may include the environment name, application name, application version, or any other name meaningful to the developer. Once a Release refers to a Ruleset, the rules can be enforced by Firebase Rules-enabled services. More than one Release may be 'live' concurrently. Consider the following three Release names for `projects/foo` and the Ruleset to which they refer. Release Name -&gt; Ruleset Name * `projects/foo/releases/prod` -&gt; `projects/foo/rulesets/uuid123` * `projects/foo/releases/prod/beta` -&gt; `projects/foo/rulesets/uuid123` * `projects/foo/releases/prod/v23` -&gt; `projects/foo/rulesets/uuid456` The relationships reflect a Ruleset rollout in progress. The prod and `prod/beta` releases refer to the same Ruleset. However, `prod/v23` refers to a new Ruleset. The Ruleset reference for a Release may be updated using the UpdateRelease method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaserules_projects_releases_create_execute()` or `firebaserules_projects_releases_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaserules_projects_releases_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_releases_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Release>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Release = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/releases
/// Create a Release. Release names should reflect the developer's deployment practices. For example, the release name may include the environment name, application name, application version, or any other name meaningful to the developer. Once a Release refers to a Ruleset, the rules can be enforced by Firebase Rules-enabled services. More than one Release may be 'live' concurrently. Consider the following three Release names for `projects/foo` and the Ruleset to which they refer. Release Name -&gt; Ruleset Name * `projects/foo/releases/prod` -&gt; `projects/foo/rulesets/uuid123` * `projects/foo/releases/prod/beta` -&gt; `projects/foo/rulesets/uuid123` * `projects/foo/releases/prod/v23` -&gt; `projects/foo/rulesets/uuid456` The relationships reflect a Ruleset rollout in progress. The prod and `prod/beta` releases refer to the same Ruleset. However, `prod/v23` refers to a new Ruleset. The Ruleset reference for a Release may be updated using the UpdateRelease method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaserules_projects_releases_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaserules_projects_releases_create_task()`.
/// For the simplest API, use `firebaserules_projects_releases_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaserules_projects_releases_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaserules_projects_releases_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebaserules_projects_releases_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaserules_projects_releases_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaserulesProjectsReleasesCreateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: Release,
}

/// GET v1/projects/{projectsId}/releases
/// Create a Release. Release names should reflect the developer's deployment practices. For example, the release name may include the environment name, application name, application version, or any other name meaningful to the developer. Once a Release refers to a Ruleset, the rules can be enforced by Firebase Rules-enabled services. More than one Release may be 'live' concurrently. Consider the following three Release names for `projects/foo` and the Ruleset to which they refer. Release Name -&gt; Ruleset Name * `projects/foo/releases/prod` -&gt; `projects/foo/rulesets/uuid123` * `projects/foo/releases/prod/beta` -&gt; `projects/foo/rulesets/uuid123` * `projects/foo/releases/prod/v23` -&gt; `projects/foo/rulesets/uuid456` The relationships reflect a Ruleset rollout in progress. The prod and `prod/beta` releases refer to the same Ruleset. However, `prod/v23` refers to a new Ruleset. The Ruleset reference for a Release may be updated using the UpdateRelease method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaserules_projects_releases_create_builder()` + `firebaserules_projects_releases_create_execute()`.
/// For task-level control, use `firebaserules_projects_releases_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_releases_create(
    client: &SimpleHttpClient,
    args: &FirebaserulesProjectsReleasesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebaserules_projects_releases_create_builder(client, &args.name, &args.body)?;
    firebaserules_projects_releases_create_execute(builder)
}

/// GET v1/projects/{projectsId}/rulesets
/// Create a Ruleset from Source. The Ruleset is given a unique generated name which is returned to the caller. Source containing syntactic or semantics errors will result in an error response indicating the first error encountered. For a detailed view of Source issues, use TestRuleset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebaserules_projects_rulesets_create_execute()` to send, or `firebaserules_projects_rulesets_create` for simplest API.

pub fn firebaserules_projects_rulesets_create_builder(
    client: &SimpleHttpClient,
    name: &String,
    body: &Ruleset,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://firebaserules.googleapis.com/v1/projects/{}/rulesets",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/rulesets
/// Create a Ruleset from Source. The Ruleset is given a unique generated name which is returned to the caller. Source containing syntactic or semantics errors will result in an error response indicating the first error encountered. For a detailed view of Source issues, use TestRuleset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebaserules_projects_rulesets_create_execute()` or `firebaserules_projects_rulesets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaserules_projects_rulesets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_rulesets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Ruleset>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Ruleset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/rulesets
/// Create a Ruleset from Source. The Ruleset is given a unique generated name which is returned to the caller. Source containing syntactic or semantics errors will result in an error response indicating the first error encountered. For a detailed view of Source issues, use TestRuleset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebaserules_projects_rulesets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebaserules_projects_rulesets_create_task()`.
/// For the simplest API, use `firebaserules_projects_rulesets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebaserules_projects_rulesets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebaserules_projects_rulesets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ruleset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebaserules_projects_rulesets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebaserules_projects_rulesets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaserulesProjectsRulesetsCreateArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: Ruleset,
}

/// GET v1/projects/{projectsId}/rulesets
/// Create a Ruleset from Source. The Ruleset is given a unique generated name which is returned to the caller. Source containing syntactic or semantics errors will result in an error response indicating the first error encountered. For a detailed view of Source issues, use TestRuleset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebaserules_projects_rulesets_create_builder()` + `firebaserules_projects_rulesets_create_execute()`.
/// For task-level control, use `firebaserules_projects_rulesets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebaserules_projects_rulesets_create(
    client: &SimpleHttpClient,
    args: &FirebaserulesProjectsRulesetsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Ruleset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebaserules_projects_rulesets_create_builder(client, &args.name, &args.body)?;
    firebaserules_projects_rulesets_create_execute(builder)
}
