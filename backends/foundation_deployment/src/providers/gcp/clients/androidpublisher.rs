//! Auto-generated API clients for Google Cloud Platform - androidpublisher.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET androidpublisher/v3/applications/{packageName}/dataSafety
/// Writes the Safety Labels declaration of an app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_applications_data_safety_execute()` to send, or `androidpublisher_applications_data_safety` for simplest API.

pub fn androidpublisher_applications_data_safety_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &SafetyLabelsUpdateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/dataSafety",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/dataSafety
/// Writes the Safety Labels declaration of an app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_applications_data_safety_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_applications_data_safety_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_data_safety_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SafetyLabelsUpdateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SafetyLabelsUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/dataSafety
/// Writes the Safety Labels declaration of an app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_applications_data_safety_builder()` + `androidpublisher_applications_data_safety_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_data_safety(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &SafetyLabelsUpdateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SafetyLabelsUpdateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_applications_data_safety_builder(client, packageName, body)?;
    androidpublisher_applications_data_safety_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs
/// Creates a new device tier config for an app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_applications_device_tier_configs_create_execute()` to send, or `androidpublisher_applications_device_tier_configs_create` for simplest API.

pub fn androidpublisher_applications_device_tier_configs_create_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    allowUnknownDevices: Option<bool>,
    body: &DeviceTierConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/deviceTierConfigs",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "allowUnknownDevices",
            allowUnknownDevices
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs
/// Creates a new device tier config for an app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_applications_device_tier_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_applications_device_tier_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_device_tier_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceTierConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeviceTierConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs
/// Creates a new device tier config for an app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_applications_device_tier_configs_create_builder()` + `androidpublisher_applications_device_tier_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_device_tier_configs_create(
    client: &SimpleHttpClient,
    packageName: &str,
    allowUnknownDevices: Option<bool>,
    body: &DeviceTierConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceTierConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_applications_device_tier_configs_create_builder(
        client,
        packageName,
        allowUnknownDevices,
        body,
    )?;
    androidpublisher_applications_device_tier_configs_create_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs/{deviceTierConfigId}
/// Returns a particular device tier config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_applications_device_tier_configs_get_execute()` to send, or `androidpublisher_applications_device_tier_configs_get` for simplest API.

pub fn androidpublisher_applications_device_tier_configs_get_builder(
    client: &SimpleHttpClient,
    deviceTierConfigId: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/deviceTierConfigs/{}",
        deviceTierConfigId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs/{deviceTierConfigId}
/// Returns a particular device tier config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_applications_device_tier_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_applications_device_tier_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_device_tier_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceTierConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeviceTierConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs/{deviceTierConfigId}
/// Returns a particular device tier config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_applications_device_tier_configs_get_builder()` + `androidpublisher_applications_device_tier_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_device_tier_configs_get(
    client: &SimpleHttpClient,
    deviceTierConfigId: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceTierConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_applications_device_tier_configs_get_builder(
        client,
        deviceTierConfigId,
        packageName,
    )?;
    androidpublisher_applications_device_tier_configs_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs
/// Returns created device tier configs, ordered by descending creation time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_applications_device_tier_configs_list_execute()` to send, or `androidpublisher_applications_device_tier_configs_list` for simplest API.

pub fn androidpublisher_applications_device_tier_configs_list_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/deviceTierConfigs",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs
/// Returns created device tier configs, ordered by descending creation time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_applications_device_tier_configs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_applications_device_tier_configs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_device_tier_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDeviceTierConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDeviceTierConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs
/// Returns created device tier configs, ordered by descending creation time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_applications_device_tier_configs_list_builder()` + `androidpublisher_applications_device_tier_configs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_device_tier_configs_list(
    client: &SimpleHttpClient,
    packageName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDeviceTierConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_applications_device_tier_configs_list_builder(
        client,
        packageName,
        pageSize,
        pageToken,
    )?;
    androidpublisher_applications_device_tier_configs_list_execute(builder)
}

/// GET androidpublisher/v3/{+parent}/releases
/// Returns the list of all releases for a given track. This excludes any releases that are obsolete.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_applications_tracks_releases_list_execute()` to send, or `androidpublisher_applications_tracks_releases_list` for simplest API.

pub fn androidpublisher_applications_tracks_releases_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/{+parent}/releases",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+parent}/releases
/// Returns the list of all releases for a given track. This excludes any releases that are obsolete.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_applications_tracks_releases_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_applications_tracks_releases_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_tracks_releases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListReleaseSummariesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReleaseSummariesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+parent}/releases
/// Returns the list of all releases for a given track. This excludes any releases that are obsolete.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_applications_tracks_releases_list_builder()` + `androidpublisher_applications_tracks_releases_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_tracks_releases_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListReleaseSummariesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_applications_tracks_releases_list_builder(client, parent)?;
    androidpublisher_applications_tracks_releases_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:addTargeting
/// Incrementally update targeting for a recovery action. Note that only the criteria selected during the creation of recovery action can be expanded.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_apprecovery_add_targeting_execute()` to send, or `androidpublisher_apprecovery_add_targeting` for simplest API.

pub fn androidpublisher_apprecovery_add_targeting_builder(
    client: &SimpleHttpClient,
    appRecoveryId: &str,
    packageName: &str,
    body: &AddTargetingRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/appRecoveries/{}:addTargeting",
        appRecoveryId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:addTargeting
/// Incrementally update targeting for a recovery action. Note that only the criteria selected during the creation of recovery action can be expanded.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_apprecovery_add_targeting_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_apprecovery_add_targeting_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_add_targeting_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddTargetingResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddTargetingResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:addTargeting
/// Incrementally update targeting for a recovery action. Note that only the criteria selected during the creation of recovery action can be expanded.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_apprecovery_add_targeting_builder()` + `androidpublisher_apprecovery_add_targeting_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_add_targeting(
    client: &SimpleHttpClient,
    appRecoveryId: &str,
    packageName: &str,
    body: &AddTargetingRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddTargetingResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_apprecovery_add_targeting_builder(
        client,
        appRecoveryId,
        packageName,
        body,
    )?;
    androidpublisher_apprecovery_add_targeting_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:cancel
/// Cancel an already executing app recovery action. Note that this action changes status of the recovery action to CANCELED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_apprecovery_cancel_execute()` to send, or `androidpublisher_apprecovery_cancel` for simplest API.

pub fn androidpublisher_apprecovery_cancel_builder(
    client: &SimpleHttpClient,
    appRecoveryId: &str,
    packageName: &str,
    body: &CancelAppRecoveryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/appRecoveries/{}:cancel",
        appRecoveryId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:cancel
/// Cancel an already executing app recovery action. Note that this action changes status of the recovery action to CANCELED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_apprecovery_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_apprecovery_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CancelAppRecoveryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CancelAppRecoveryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:cancel
/// Cancel an already executing app recovery action. Note that this action changes status of the recovery action to CANCELED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_apprecovery_cancel_builder()` + `androidpublisher_apprecovery_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_cancel(
    client: &SimpleHttpClient,
    appRecoveryId: &str,
    packageName: &str,
    body: &CancelAppRecoveryRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CancelAppRecoveryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_apprecovery_cancel_builder(client, appRecoveryId, packageName, body)?;
    androidpublisher_apprecovery_cancel_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries
/// Create an app recovery action with recovery status as DRAFT. Note that this action does not execute the recovery action.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_apprecovery_create_execute()` to send, or `androidpublisher_apprecovery_create` for simplest API.

pub fn androidpublisher_apprecovery_create_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &CreateDraftAppRecoveryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/appRecoveries",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries
/// Create an app recovery action with recovery status as DRAFT. Note that this action does not execute the recovery action.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_apprecovery_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_apprecovery_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppRecoveryAction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppRecoveryAction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries
/// Create an app recovery action with recovery status as DRAFT. Note that this action does not execute the recovery action.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_apprecovery_create_builder()` + `androidpublisher_apprecovery_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_create(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &CreateDraftAppRecoveryRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppRecoveryAction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_apprecovery_create_builder(client, packageName, body)?;
    androidpublisher_apprecovery_create_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:deploy
/// Deploy an already created app recovery action with recovery status DRAFT. Note that this action activates the recovery action for all targeted users and changes its status to `ACTIVE`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_apprecovery_deploy_execute()` to send, or `androidpublisher_apprecovery_deploy` for simplest API.

pub fn androidpublisher_apprecovery_deploy_builder(
    client: &SimpleHttpClient,
    appRecoveryId: &str,
    packageName: &str,
    body: &DeployAppRecoveryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/appRecoveries/{}:deploy",
        appRecoveryId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:deploy
/// Deploy an already created app recovery action with recovery status DRAFT. Note that this action activates the recovery action for all targeted users and changes its status to `ACTIVE`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_apprecovery_deploy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_apprecovery_deploy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeployAppRecoveryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeployAppRecoveryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:deploy
/// Deploy an already created app recovery action with recovery status DRAFT. Note that this action activates the recovery action for all targeted users and changes its status to `ACTIVE`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_apprecovery_deploy_builder()` + `androidpublisher_apprecovery_deploy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_deploy(
    client: &SimpleHttpClient,
    appRecoveryId: &str,
    packageName: &str,
    body: &DeployAppRecoveryRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeployAppRecoveryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_apprecovery_deploy_builder(client, appRecoveryId, packageName, body)?;
    androidpublisher_apprecovery_deploy_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries
/// List all app recovery action resources associated with a particular package name and app version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_apprecovery_list_execute()` to send, or `androidpublisher_apprecovery_list` for simplest API.

pub fn androidpublisher_apprecovery_list_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    versionCode: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/appRecoveries",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "versionCode",
            versionCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries
/// List all app recovery action resources associated with a particular package name and app version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_apprecovery_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_apprecovery_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAppRecoveriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAppRecoveriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries
/// List all app recovery action resources associated with a particular package name and app version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_apprecovery_list_builder()` + `androidpublisher_apprecovery_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_list(
    client: &SimpleHttpClient,
    packageName: &str,
    versionCode: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAppRecoveriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_apprecovery_list_builder(client, packageName, versionCode)?;
    androidpublisher_apprecovery_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}:commit
/// Commits an app edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_commit_execute()` to send, or `androidpublisher_edits_commit` for simplest API.

pub fn androidpublisher_edits_commit_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    changesInReviewBehavior: Option<&str>,
    changesNotSentForReview: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}:commit",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "changesInReviewBehavior",
            changesInReviewBehavior
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "changesNotSentForReview",
            changesNotSentForReview
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}:commit
/// Commits an app edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_commit_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_commit_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_commit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppEdit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppEdit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}:commit
/// Commits an app edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_commit_builder()` + `androidpublisher_edits_commit_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_commit(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    changesInReviewBehavior: Option<&str>,
    changesNotSentForReview: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppEdit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_commit_builder(
        client,
        editId,
        packageName,
        changesInReviewBehavior,
        changesNotSentForReview,
    )?;
    androidpublisher_edits_commit_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}
/// Deletes an app edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_delete_execute()` to send, or `androidpublisher_edits_delete` for simplest API.

pub fn androidpublisher_edits_delete_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}
/// Deletes an app edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}
/// Deletes an app edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_delete_builder()` + `androidpublisher_edits_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_delete(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_delete_builder(client, editId, packageName)?;
    androidpublisher_edits_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}
/// Gets an app edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_get_execute()` to send, or `androidpublisher_edits_get` for simplest API.

pub fn androidpublisher_edits_get_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}
/// Gets an app edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppEdit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppEdit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}
/// Gets an app edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_get_builder()` + `androidpublisher_edits_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_get(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppEdit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_get_builder(client, editId, packageName)?;
    androidpublisher_edits_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits
/// Creates a new edit for an app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_insert_execute()` to send, or `androidpublisher_edits_insert` for simplest API.

pub fn androidpublisher_edits_insert_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &AppEdit,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/edits
/// Creates a new edit for an app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppEdit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppEdit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits
/// Creates a new edit for an app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_insert_builder()` + `androidpublisher_edits_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_insert(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &AppEdit,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppEdit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_insert_builder(client, packageName, body)?;
    androidpublisher_edits_insert_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}:validate
/// Validates an app edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_validate_execute()` to send, or `androidpublisher_edits_validate` for simplest API.

pub fn androidpublisher_edits_validate_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}:validate",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}:validate
/// Validates an app edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_validate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_validate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppEdit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppEdit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}:validate
/// Validates an app edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_validate_builder()` + `androidpublisher_edits_validate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_validate(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppEdit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_validate_builder(client, editId, packageName)?;
    androidpublisher_edits_validate_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/externallyHosted
/// Creates a new APK without uploading the APK itself to Google Play, instead hosting the APK at a specified URL. This function is only available to organizations using Managed Play whose application is configured to restrict distribution to the organizations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_apks_addexternallyhosted_execute()` to send, or `androidpublisher_edits_apks_addexternallyhosted` for simplest API.

pub fn androidpublisher_edits_apks_addexternallyhosted_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    body: &ApksAddExternallyHostedRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/apks/externallyHosted",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/externallyHosted
/// Creates a new APK without uploading the APK itself to Google Play, instead hosting the APK at a specified URL. This function is only available to organizations using Managed Play whose application is configured to restrict distribution to the organizations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_apks_addexternallyhosted_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_apks_addexternallyhosted_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_apks_addexternallyhosted_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ApksAddExternallyHostedResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApksAddExternallyHostedResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/externallyHosted
/// Creates a new APK without uploading the APK itself to Google Play, instead hosting the APK at a specified URL. This function is only available to organizations using Managed Play whose application is configured to restrict distribution to the organizations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_apks_addexternallyhosted_builder()` + `androidpublisher_edits_apks_addexternallyhosted_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_apks_addexternallyhosted(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    body: &ApksAddExternallyHostedRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ApksAddExternallyHostedResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_edits_apks_addexternallyhosted_builder(client, editId, packageName, body)?;
    androidpublisher_edits_apks_addexternallyhosted_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks
/// Lists all current APKs of the app and edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_apks_list_execute()` to send, or `androidpublisher_edits_apks_list` for simplest API.

pub fn androidpublisher_edits_apks_list_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/apks",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks
/// Lists all current APKs of the app and edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_apks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_apks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_apks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApksListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApksListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks
/// Lists all current APKs of the app and edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_apks_list_builder()` + `androidpublisher_edits_apks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_apks_list(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApksListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_apks_list_builder(client, editId, packageName)?;
    androidpublisher_edits_apks_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks
/// Uploads an APK and adds to the current edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_apks_upload_execute()` to send, or `androidpublisher_edits_apks_upload` for simplest API.

pub fn androidpublisher_edits_apks_upload_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/apks",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks
/// Uploads an APK and adds to the current edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_apks_upload_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_apks_upload_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_apks_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Apk>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Apk = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks
/// Uploads an APK and adds to the current edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_apks_upload_builder()` + `androidpublisher_edits_apks_upload_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_apks_upload(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Apk>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_apks_upload_builder(client, editId, packageName)?;
    androidpublisher_edits_apks_upload_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/bundles
/// Lists all current Android App Bundles of the app and edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_bundles_list_execute()` to send, or `androidpublisher_edits_bundles_list` for simplest API.

pub fn androidpublisher_edits_bundles_list_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/bundles",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/bundles
/// Lists all current Android App Bundles of the app and edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_bundles_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_bundles_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_bundles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BundlesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BundlesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/bundles
/// Lists all current Android App Bundles of the app and edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_bundles_list_builder()` + `androidpublisher_edits_bundles_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_bundles_list(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BundlesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_bundles_list_builder(client, editId, packageName)?;
    androidpublisher_edits_bundles_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/bundles
/// Uploads a new Android App Bundle to this edit. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_bundles_upload_execute()` to send, or `androidpublisher_edits_bundles_upload` for simplest API.

pub fn androidpublisher_edits_bundles_upload_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    ackBundleInstallationWarning: Option<bool>,
    deviceTierConfigId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/bundles",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "ackBundleInstallationWarning",
            ackBundleInstallationWarning
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "deviceTierConfigId",
            deviceTierConfigId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/bundles
/// Uploads a new Android App Bundle to this edit. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_bundles_upload_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_bundles_upload_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_bundles_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bundle>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Bundle = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/bundles
/// Uploads a new Android App Bundle to this edit. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_bundles_upload_builder()` + `androidpublisher_edits_bundles_upload_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_bundles_upload(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    ackBundleInstallationWarning: Option<bool>,
    deviceTierConfigId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Bundle>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_bundles_upload_builder(
        client,
        editId,
        packageName,
        ackBundleInstallationWarning,
        deviceTierConfigId,
    )?;
    androidpublisher_edits_bundles_upload_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/countryAvailability/{track}
/// Gets country availability.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_countryavailability_get_execute()` to send, or `androidpublisher_edits_countryavailability_get` for simplest API.

pub fn androidpublisher_edits_countryavailability_get_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    track: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/countryAvailability/{}",
        editId,
        packageName,
        track,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/countryAvailability/{track}
/// Gets country availability.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_countryavailability_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_countryavailability_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_countryavailability_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TrackCountryAvailability>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TrackCountryAvailability = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/countryAvailability/{track}
/// Gets country availability.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_countryavailability_get_builder()` + `androidpublisher_edits_countryavailability_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_countryavailability_get(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    track: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TrackCountryAvailability>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_edits_countryavailability_get_builder(client, editId, packageName, track)?;
    androidpublisher_edits_countryavailability_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/deobfuscationFiles/{deobfuscationFileType}
/// Uploads a new deobfuscation file and attaches to the specified APK.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_deobfuscationfiles_upload_execute()` to send, or `androidpublisher_edits_deobfuscationfiles_upload` for simplest API.

pub fn androidpublisher_edits_deobfuscationfiles_upload_builder(
    client: &SimpleHttpClient,
    apkVersionCode: &str,
    deobfuscationFileType: &str,
    editId: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/apks/{}/deobfuscationFiles/{}",
        apkVersionCode,
        deobfuscationFileType,
        editId,
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/deobfuscationFiles/{deobfuscationFileType}
/// Uploads a new deobfuscation file and attaches to the specified APK.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_deobfuscationfiles_upload_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_deobfuscationfiles_upload_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_deobfuscationfiles_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeobfuscationFilesUploadResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeobfuscationFilesUploadResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/deobfuscationFiles/{deobfuscationFileType}
/// Uploads a new deobfuscation file and attaches to the specified APK.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_deobfuscationfiles_upload_builder()` + `androidpublisher_edits_deobfuscationfiles_upload_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_deobfuscationfiles_upload(
    client: &SimpleHttpClient,
    apkVersionCode: &str,
    deobfuscationFileType: &str,
    editId: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeobfuscationFilesUploadResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_deobfuscationfiles_upload_builder(
        client,
        apkVersionCode,
        deobfuscationFileType,
        editId,
        packageName,
    )?;
    androidpublisher_edits_deobfuscationfiles_upload_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/details
/// Gets details of an app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_details_get_execute()` to send, or `androidpublisher_edits_details_get` for simplest API.

pub fn androidpublisher_edits_details_get_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/details",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/details
/// Gets details of an app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_details_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_details_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_details_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppDetails>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppDetails = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/details
/// Gets details of an app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_details_get_builder()` + `androidpublisher_edits_details_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_details_get(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppDetails>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_details_get_builder(client, editId, packageName)?;
    androidpublisher_edits_details_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/details
/// Patches details of an app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_details_patch_execute()` to send, or `androidpublisher_edits_details_patch` for simplest API.

pub fn androidpublisher_edits_details_patch_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    body: &AppDetails,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/details",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/details
/// Patches details of an app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_details_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_details_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_details_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppDetails>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppDetails = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/details
/// Patches details of an app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_details_patch_builder()` + `androidpublisher_edits_details_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_details_patch(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    body: &AppDetails,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppDetails>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_details_patch_builder(client, editId, packageName, body)?;
    androidpublisher_edits_details_patch_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/details
/// Updates details of an app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_details_update_execute()` to send, or `androidpublisher_edits_details_update` for simplest API.

pub fn androidpublisher_edits_details_update_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    body: &AppDetails,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/details",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/details
/// Updates details of an app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_details_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_details_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_details_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppDetails>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppDetails = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/details
/// Updates details of an app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_details_update_builder()` + `androidpublisher_edits_details_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_details_update(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    body: &AppDetails,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppDetails>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_details_update_builder(client, editId, packageName, body)?;
    androidpublisher_edits_details_update_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Fetches the expansion file configuration for the specified APK.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_expansionfiles_get_execute()` to send, or `androidpublisher_edits_expansionfiles_get` for simplest API.

pub fn androidpublisher_edits_expansionfiles_get_builder(
    client: &SimpleHttpClient,
    apkVersionCode: &str,
    editId: &str,
    expansionFileType: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/apks/{}/expansionFiles/{}",
        apkVersionCode,
        editId,
        expansionFileType,
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Fetches the expansion file configuration for the specified APK.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_expansionfiles_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_expansionfiles_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_expansionfiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExpansionFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExpansionFile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Fetches the expansion file configuration for the specified APK.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_expansionfiles_get_builder()` + `androidpublisher_edits_expansionfiles_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_expansionfiles_get(
    client: &SimpleHttpClient,
    apkVersionCode: &str,
    editId: &str,
    expansionFileType: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExpansionFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_expansionfiles_get_builder(
        client,
        apkVersionCode,
        editId,
        expansionFileType,
        packageName,
    )?;
    androidpublisher_edits_expansionfiles_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Patches the APK's expansion file configuration to reference another APK's expansion file. To add a new expansion file use the Upload method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_expansionfiles_patch_execute()` to send, or `androidpublisher_edits_expansionfiles_patch` for simplest API.

pub fn androidpublisher_edits_expansionfiles_patch_builder(
    client: &SimpleHttpClient,
    apkVersionCode: &str,
    editId: &str,
    expansionFileType: &str,
    packageName: &str,
    body: &ExpansionFile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/apks/{}/expansionFiles/{}",
        apkVersionCode,
        editId,
        expansionFileType,
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Patches the APK's expansion file configuration to reference another APK's expansion file. To add a new expansion file use the Upload method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_expansionfiles_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_expansionfiles_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_expansionfiles_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExpansionFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExpansionFile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Patches the APK's expansion file configuration to reference another APK's expansion file. To add a new expansion file use the Upload method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_expansionfiles_patch_builder()` + `androidpublisher_edits_expansionfiles_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_expansionfiles_patch(
    client: &SimpleHttpClient,
    apkVersionCode: &str,
    editId: &str,
    expansionFileType: &str,
    packageName: &str,
    body: &ExpansionFile,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExpansionFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_expansionfiles_patch_builder(
        client,
        apkVersionCode,
        editId,
        expansionFileType,
        packageName,
        body,
    )?;
    androidpublisher_edits_expansionfiles_patch_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Updates the APK's expansion file configuration to reference another APK's expansion file. To add a new expansion file use the Upload method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_expansionfiles_update_execute()` to send, or `androidpublisher_edits_expansionfiles_update` for simplest API.

pub fn androidpublisher_edits_expansionfiles_update_builder(
    client: &SimpleHttpClient,
    apkVersionCode: &str,
    editId: &str,
    expansionFileType: &str,
    packageName: &str,
    body: &ExpansionFile,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/apks/{}/expansionFiles/{}",
        apkVersionCode,
        editId,
        expansionFileType,
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Updates the APK's expansion file configuration to reference another APK's expansion file. To add a new expansion file use the Upload method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_expansionfiles_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_expansionfiles_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_expansionfiles_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExpansionFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExpansionFile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Updates the APK's expansion file configuration to reference another APK's expansion file. To add a new expansion file use the Upload method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_expansionfiles_update_builder()` + `androidpublisher_edits_expansionfiles_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_expansionfiles_update(
    client: &SimpleHttpClient,
    apkVersionCode: &str,
    editId: &str,
    expansionFileType: &str,
    packageName: &str,
    body: &ExpansionFile,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExpansionFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_expansionfiles_update_builder(
        client,
        apkVersionCode,
        editId,
        expansionFileType,
        packageName,
        body,
    )?;
    androidpublisher_edits_expansionfiles_update_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Uploads a new expansion file and attaches to the specified APK.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_expansionfiles_upload_execute()` to send, or `androidpublisher_edits_expansionfiles_upload` for simplest API.

pub fn androidpublisher_edits_expansionfiles_upload_builder(
    client: &SimpleHttpClient,
    apkVersionCode: &str,
    editId: &str,
    expansionFileType: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/apks/{}/expansionFiles/{}",
        apkVersionCode,
        editId,
        expansionFileType,
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Uploads a new expansion file and attaches to the specified APK.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_expansionfiles_upload_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_expansionfiles_upload_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_expansionfiles_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ExpansionFilesUploadResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExpansionFilesUploadResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Uploads a new expansion file and attaches to the specified APK.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_expansionfiles_upload_builder()` + `androidpublisher_edits_expansionfiles_upload_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_expansionfiles_upload(
    client: &SimpleHttpClient,
    apkVersionCode: &str,
    editId: &str,
    expansionFileType: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ExpansionFilesUploadResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_expansionfiles_upload_builder(
        client,
        apkVersionCode,
        editId,
        expansionFileType,
        packageName,
    )?;
    androidpublisher_edits_expansionfiles_upload_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}/{imageId}
/// Deletes the image (specified by id) from the edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_images_delete_execute()` to send, or `androidpublisher_edits_images_delete` for simplest API.

pub fn androidpublisher_edits_images_delete_builder(
    client: &SimpleHttpClient,
    editId: &str,
    imageId: &str,
    imageType: &str,
    language: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/listings/{}/{}/{}",
        editId, imageId, imageType, language, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}/{imageId}
/// Deletes the image (specified by id) from the edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_images_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_images_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_images_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}/{imageId}
/// Deletes the image (specified by id) from the edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_images_delete_builder()` + `androidpublisher_edits_images_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_images_delete(
    client: &SimpleHttpClient,
    editId: &str,
    imageId: &str,
    imageType: &str,
    language: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_images_delete_builder(
        client,
        editId,
        imageId,
        imageType,
        language,
        packageName,
    )?;
    androidpublisher_edits_images_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}
/// Deletes all images for the specified language and image type. Returns an empty response if no images are found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_images_deleteall_execute()` to send, or `androidpublisher_edits_images_deleteall` for simplest API.

pub fn androidpublisher_edits_images_deleteall_builder(
    client: &SimpleHttpClient,
    editId: &str,
    imageType: &str,
    language: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/listings/{}/{}",
        editId, imageType, language, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}
/// Deletes all images for the specified language and image type. Returns an empty response if no images are found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_images_deleteall_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_images_deleteall_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_images_deleteall_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImagesDeleteAllResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImagesDeleteAllResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}
/// Deletes all images for the specified language and image type. Returns an empty response if no images are found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_images_deleteall_builder()` + `androidpublisher_edits_images_deleteall_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_images_deleteall(
    client: &SimpleHttpClient,
    editId: &str,
    imageType: &str,
    language: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImagesDeleteAllResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_images_deleteall_builder(
        client,
        editId,
        imageType,
        language,
        packageName,
    )?;
    androidpublisher_edits_images_deleteall_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}
/// Lists all images. The response may be empty.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_images_list_execute()` to send, or `androidpublisher_edits_images_list` for simplest API.

pub fn androidpublisher_edits_images_list_builder(
    client: &SimpleHttpClient,
    editId: &str,
    imageType: &str,
    language: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/listings/{}/{}",
        editId, imageType, language, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}
/// Lists all images. The response may be empty.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_images_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_images_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_images_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImagesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImagesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}
/// Lists all images. The response may be empty.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_images_list_builder()` + `androidpublisher_edits_images_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_images_list(
    client: &SimpleHttpClient,
    editId: &str,
    imageType: &str,
    language: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImagesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_images_list_builder(
        client,
        editId,
        imageType,
        language,
        packageName,
    )?;
    androidpublisher_edits_images_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}
/// Uploads an image of the specified language and image type, and adds to the edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_images_upload_execute()` to send, or `androidpublisher_edits_images_upload` for simplest API.

pub fn androidpublisher_edits_images_upload_builder(
    client: &SimpleHttpClient,
    editId: &str,
    imageType: &str,
    language: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/listings/{}/{}",
        editId, imageType, language, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}
/// Uploads an image of the specified language and image type, and adds to the edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_images_upload_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_images_upload_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_images_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImagesUploadResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImagesUploadResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}
/// Uploads an image of the specified language and image type, and adds to the edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_images_upload_builder()` + `androidpublisher_edits_images_upload_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_images_upload(
    client: &SimpleHttpClient,
    editId: &str,
    imageType: &str,
    language: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImagesUploadResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_images_upload_builder(
        client,
        editId,
        imageType,
        language,
        packageName,
    )?;
    androidpublisher_edits_images_upload_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Deletes a localized store listing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_listings_delete_execute()` to send, or `androidpublisher_edits_listings_delete` for simplest API.

pub fn androidpublisher_edits_listings_delete_builder(
    client: &SimpleHttpClient,
    editId: &str,
    language: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/listings/{}",
        editId, language, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Deletes a localized store listing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_listings_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_listings_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Deletes a localized store listing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_listings_delete_builder()` + `androidpublisher_edits_listings_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_delete(
    client: &SimpleHttpClient,
    editId: &str,
    language: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_edits_listings_delete_builder(client, editId, language, packageName)?;
    androidpublisher_edits_listings_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings
/// Deletes all store listings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_listings_deleteall_execute()` to send, or `androidpublisher_edits_listings_deleteall` for simplest API.

pub fn androidpublisher_edits_listings_deleteall_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/listings",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings
/// Deletes all store listings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_listings_deleteall_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_listings_deleteall_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_deleteall_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings
/// Deletes all store listings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_listings_deleteall_builder()` + `androidpublisher_edits_listings_deleteall_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_deleteall(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_listings_deleteall_builder(client, editId, packageName)?;
    androidpublisher_edits_listings_deleteall_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Gets a localized store listing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_listings_get_execute()` to send, or `androidpublisher_edits_listings_get` for simplest API.

pub fn androidpublisher_edits_listings_get_builder(
    client: &SimpleHttpClient,
    editId: &str,
    language: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/listings/{}",
        editId, language, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Gets a localized store listing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_listings_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_listings_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Listing>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Listing = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Gets a localized store listing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_listings_get_builder()` + `androidpublisher_edits_listings_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_get(
    client: &SimpleHttpClient,
    editId: &str,
    language: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Listing>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_edits_listings_get_builder(client, editId, language, packageName)?;
    androidpublisher_edits_listings_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings
/// Lists all localized store listings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_listings_list_execute()` to send, or `androidpublisher_edits_listings_list` for simplest API.

pub fn androidpublisher_edits_listings_list_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/listings",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings
/// Lists all localized store listings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_listings_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_listings_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListingsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListingsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings
/// Lists all localized store listings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_listings_list_builder()` + `androidpublisher_edits_listings_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_list(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListingsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_listings_list_builder(client, editId, packageName)?;
    androidpublisher_edits_listings_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Patches a localized store listing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_listings_patch_execute()` to send, or `androidpublisher_edits_listings_patch` for simplest API.

pub fn androidpublisher_edits_listings_patch_builder(
    client: &SimpleHttpClient,
    editId: &str,
    language: &str,
    packageName: &str,
    body: &Listing,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/listings/{}",
        editId, language, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Patches a localized store listing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_listings_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_listings_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Listing>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Listing = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Patches a localized store listing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_listings_patch_builder()` + `androidpublisher_edits_listings_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_patch(
    client: &SimpleHttpClient,
    editId: &str,
    language: &str,
    packageName: &str,
    body: &Listing,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Listing>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_edits_listings_patch_builder(client, editId, language, packageName, body)?;
    androidpublisher_edits_listings_patch_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Creates or updates a localized store listing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_listings_update_execute()` to send, or `androidpublisher_edits_listings_update` for simplest API.

pub fn androidpublisher_edits_listings_update_builder(
    client: &SimpleHttpClient,
    editId: &str,
    language: &str,
    packageName: &str,
    body: &Listing,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/listings/{}",
        editId, language, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Creates or updates a localized store listing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_listings_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_listings_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Listing>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Listing = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Creates or updates a localized store listing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_listings_update_builder()` + `androidpublisher_edits_listings_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_update(
    client: &SimpleHttpClient,
    editId: &str,
    language: &str,
    packageName: &str,
    body: &Listing,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Listing>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_listings_update_builder(
        client,
        editId,
        language,
        packageName,
        body,
    )?;
    androidpublisher_edits_listings_update_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/testers/{track}
/// Gets testers. Note: Testers resource does not support email lists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_testers_get_execute()` to send, or `androidpublisher_edits_testers_get` for simplest API.

pub fn androidpublisher_edits_testers_get_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    track: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/testers/{}",
        editId, packageName, track,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/testers/{track}
/// Gets testers. Note: Testers resource does not support email lists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_testers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_testers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_testers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Testers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Testers = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/testers/{track}
/// Gets testers. Note: Testers resource does not support email lists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_testers_get_builder()` + `androidpublisher_edits_testers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_testers_get(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    track: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Testers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_testers_get_builder(client, editId, packageName, track)?;
    androidpublisher_edits_testers_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/testers/{track}
/// Patches testers. Note: Testers resource does not support email lists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_testers_patch_execute()` to send, or `androidpublisher_edits_testers_patch` for simplest API.

pub fn androidpublisher_edits_testers_patch_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    track: &str,
    body: &Testers,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/testers/{}",
        editId, packageName, track,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/testers/{track}
/// Patches testers. Note: Testers resource does not support email lists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_testers_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_testers_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_testers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Testers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Testers = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/testers/{track}
/// Patches testers. Note: Testers resource does not support email lists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_testers_patch_builder()` + `androidpublisher_edits_testers_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_testers_patch(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    track: &str,
    body: &Testers,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Testers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_edits_testers_patch_builder(client, editId, packageName, track, body)?;
    androidpublisher_edits_testers_patch_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/testers/{track}
/// Updates testers. Note: Testers resource does not support email lists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_testers_update_execute()` to send, or `androidpublisher_edits_testers_update` for simplest API.

pub fn androidpublisher_edits_testers_update_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    track: &str,
    body: &Testers,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/testers/{}",
        editId, packageName, track,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/testers/{track}
/// Updates testers. Note: Testers resource does not support email lists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_testers_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_testers_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_testers_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Testers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Testers = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/testers/{track}
/// Updates testers. Note: Testers resource does not support email lists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_testers_update_builder()` + `androidpublisher_edits_testers_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_testers_update(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    track: &str,
    body: &Testers,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Testers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_edits_testers_update_builder(client, editId, packageName, track, body)?;
    androidpublisher_edits_testers_update_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks
/// Creates a new track.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_tracks_create_execute()` to send, or `androidpublisher_edits_tracks_create` for simplest API.

pub fn androidpublisher_edits_tracks_create_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    body: &TrackConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/tracks",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks
/// Creates a new track.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_tracks_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_tracks_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_tracks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Track>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Track = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks
/// Creates a new track.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_tracks_create_builder()` + `androidpublisher_edits_tracks_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_tracks_create(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    body: &TrackConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Track>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_tracks_create_builder(client, editId, packageName, body)?;
    androidpublisher_edits_tracks_create_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks/{track}
/// Gets a track.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_tracks_get_execute()` to send, or `androidpublisher_edits_tracks_get` for simplest API.

pub fn androidpublisher_edits_tracks_get_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    track: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/tracks/{}",
        editId, packageName, track,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks/{track}
/// Gets a track.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_tracks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_tracks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_tracks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Track>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Track = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks/{track}
/// Gets a track.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_tracks_get_builder()` + `androidpublisher_edits_tracks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_tracks_get(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    track: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Track>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_tracks_get_builder(client, editId, packageName, track)?;
    androidpublisher_edits_tracks_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks
/// Lists all tracks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_tracks_list_execute()` to send, or `androidpublisher_edits_tracks_list` for simplest API.

pub fn androidpublisher_edits_tracks_list_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/tracks",
        editId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks
/// Lists all tracks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_tracks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_tracks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_tracks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TracksListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TracksListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks
/// Lists all tracks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_tracks_list_builder()` + `androidpublisher_edits_tracks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_tracks_list(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TracksListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_tracks_list_builder(client, editId, packageName)?;
    androidpublisher_edits_tracks_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks/{track}
/// Patches a track.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_tracks_patch_execute()` to send, or `androidpublisher_edits_tracks_patch` for simplest API.

pub fn androidpublisher_edits_tracks_patch_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    track: &str,
    body: &Track,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/tracks/{}",
        editId, packageName, track,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks/{track}
/// Patches a track.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_tracks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_tracks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_tracks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Track>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Track = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks/{track}
/// Patches a track.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_tracks_patch_builder()` + `androidpublisher_edits_tracks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_tracks_patch(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    track: &str,
    body: &Track,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Track>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_edits_tracks_patch_builder(client, editId, packageName, track, body)?;
    androidpublisher_edits_tracks_patch_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks/{track}
/// Updates a track.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_tracks_update_execute()` to send, or `androidpublisher_edits_tracks_update` for simplest API.

pub fn androidpublisher_edits_tracks_update_builder(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    track: &str,
    body: &Track,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/edits/{}/tracks/{}",
        editId, packageName, track,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks/{track}
/// Updates a track.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_edits_tracks_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_edits_tracks_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_tracks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Track>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Track = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks/{track}
/// Updates a track.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_tracks_update_builder()` + `androidpublisher_edits_tracks_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_tracks_update(
    client: &SimpleHttpClient,
    editId: &str,
    packageName: &str,
    track: &str,
    body: &Track,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Track>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_edits_tracks_update_builder(client, editId, packageName, track, body)?;
    androidpublisher_edits_tracks_update_execute(builder)
}

/// GET androidpublisher/v3/{+parent}/externalTransactions
/// Creates a new external transaction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_externaltransactions_createexternaltransaction_execute()` to send, or `androidpublisher_externaltransactions_createexternaltransaction` for simplest API.

pub fn androidpublisher_externaltransactions_createexternaltransaction_builder(
    client: &SimpleHttpClient,
    parent: &str,
    externalTransactionId: Option<&str>,
    body: &ExternalTransaction,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/{+parent}/externalTransactions",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "externalTransactionId",
            externalTransactionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/{+parent}/externalTransactions
/// Creates a new external transaction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_externaltransactions_createexternaltransaction_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_externaltransactions_createexternaltransaction_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_externaltransactions_createexternaltransaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExternalTransaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+parent}/externalTransactions
/// Creates a new external transaction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_externaltransactions_createexternaltransaction_builder()` + `androidpublisher_externaltransactions_createexternaltransaction_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_externaltransactions_createexternaltransaction(
    client: &SimpleHttpClient,
    parent: &str,
    externalTransactionId: Option<&str>,
    body: &ExternalTransaction,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_externaltransactions_createexternaltransaction_builder(
        client,
        parent,
        externalTransactionId,
        body,
    )?;
    androidpublisher_externaltransactions_createexternaltransaction_execute(builder)
}

/// GET androidpublisher/v3/{+name}
/// Gets an existing external transaction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_externaltransactions_getexternaltransaction_execute()` to send, or `androidpublisher_externaltransactions_getexternaltransaction` for simplest API.

pub fn androidpublisher_externaltransactions_getexternaltransaction_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comandroidpublisher/v3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+name}
/// Gets an existing external transaction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_externaltransactions_getexternaltransaction_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_externaltransactions_getexternaltransaction_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_externaltransactions_getexternaltransaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExternalTransaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+name}
/// Gets an existing external transaction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_externaltransactions_getexternaltransaction_builder()` + `androidpublisher_externaltransactions_getexternaltransaction_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_externaltransactions_getexternaltransaction(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_externaltransactions_getexternaltransaction_builder(client, name)?;
    androidpublisher_externaltransactions_getexternaltransaction_execute(builder)
}

/// GET androidpublisher/v3/{+name}:refund
/// Refunds or partially refunds an existing external transaction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_externaltransactions_refundexternaltransaction_execute()` to send, or `androidpublisher_externaltransactions_refundexternaltransaction` for simplest API.

pub fn androidpublisher_externaltransactions_refundexternaltransaction_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RefundExternalTransactionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/{+name}:refund",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/{+name}:refund
/// Refunds or partially refunds an existing external transaction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_externaltransactions_refundexternaltransaction_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_externaltransactions_refundexternaltransaction_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_externaltransactions_refundexternaltransaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExternalTransaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+name}:refund
/// Refunds or partially refunds an existing external transaction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_externaltransactions_refundexternaltransaction_builder()` + `androidpublisher_externaltransactions_refundexternaltransaction_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_externaltransactions_refundexternaltransaction(
    client: &SimpleHttpClient,
    name: &str,
    body: &RefundExternalTransactionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_externaltransactions_refundexternaltransaction_builder(
        client, name, body,
    )?;
    androidpublisher_externaltransactions_refundexternaltransaction_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/generatedApks/{versionCode}/downloads/{downloadId}:download
/// Downloads a single signed APK generated from an app bundle.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_generatedapks_download_execute()` to send, or `androidpublisher_generatedapks_download` for simplest API.

pub fn androidpublisher_generatedapks_download_builder(
    client: &SimpleHttpClient,
    downloadId: &str,
    packageName: &str,
    versionCode: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/generatedApks/{}/downloads/{}:download",
        downloadId,
        packageName,
        versionCode,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/generatedApks/{versionCode}/downloads/{downloadId}:download
/// Downloads a single signed APK generated from an app bundle.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_generatedapks_download_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_generatedapks_download_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_generatedapks_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/generatedApks/{versionCode}/downloads/{downloadId}:download
/// Downloads a single signed APK generated from an app bundle.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_generatedapks_download_builder()` + `androidpublisher_generatedapks_download_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_generatedapks_download(
    client: &SimpleHttpClient,
    downloadId: &str,
    packageName: &str,
    versionCode: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_generatedapks_download_builder(
        client,
        downloadId,
        packageName,
        versionCode,
    )?;
    androidpublisher_generatedapks_download_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/generatedApks/{versionCode}
/// Returns download metadata for all APKs that were generated from a given app bundle.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_generatedapks_list_execute()` to send, or `androidpublisher_generatedapks_list` for simplest API.

pub fn androidpublisher_generatedapks_list_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    versionCode: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/generatedApks/{}",
        packageName, versionCode,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/generatedApks/{versionCode}
/// Returns download metadata for all APKs that were generated from a given app bundle.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_generatedapks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_generatedapks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_generatedapks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GeneratedApksListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GeneratedApksListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/generatedApks/{versionCode}
/// Returns download metadata for all APKs that were generated from a given app bundle.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_generatedapks_list_builder()` + `androidpublisher_generatedapks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_generatedapks_list(
    client: &SimpleHttpClient,
    packageName: &str,
    versionCode: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GeneratedApksListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_generatedapks_list_builder(client, packageName, versionCode)?;
    androidpublisher_generatedapks_list_execute(builder)
}

/// GET androidpublisher/v3/{+parent}/grants
/// Grant access for a user to the given package.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_grants_create_execute()` to send, or `androidpublisher_grants_create` for simplest API.

pub fn androidpublisher_grants_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Grant,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/{+parent}/grants",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/{+parent}/grants
/// Grant access for a user to the given package.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_grants_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_grants_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_grants_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Grant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Grant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+parent}/grants
/// Grant access for a user to the given package.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_grants_create_builder()` + `androidpublisher_grants_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_grants_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Grant,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Grant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_grants_create_builder(client, parent, body)?;
    androidpublisher_grants_create_execute(builder)
}

/// GET androidpublisher/v3/{+name}
/// Removes all access for the user to the given package or developer account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_grants_delete_execute()` to send, or `androidpublisher_grants_delete` for simplest API.

pub fn androidpublisher_grants_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comandroidpublisher/v3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+name}
/// Removes all access for the user to the given package or developer account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_grants_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_grants_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_grants_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+name}
/// Removes all access for the user to the given package or developer account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_grants_delete_builder()` + `androidpublisher_grants_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_grants_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_grants_delete_builder(client, name)?;
    androidpublisher_grants_delete_execute(builder)
}

/// GET androidpublisher/v3/{+name}
/// Updates access for the user to the given package.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_grants_patch_execute()` to send, or `androidpublisher_grants_patch` for simplest API.

pub fn androidpublisher_grants_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Grant,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comandroidpublisher/v3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/{+name}
/// Updates access for the user to the given package.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_grants_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_grants_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_grants_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Grant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Grant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+name}
/// Updates access for the user to the given package.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_grants_patch_builder()` + `androidpublisher_grants_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_grants_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Grant,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Grant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_grants_patch_builder(client, name, updateMask, body)?;
    androidpublisher_grants_patch_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchDelete
/// Deletes in-app products (managed products or subscriptions). Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput. This method should not be used to delete subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_inappproducts_batch_delete_execute()` to send, or `androidpublisher_inappproducts_batch_delete` for simplest API.

pub fn androidpublisher_inappproducts_batch_delete_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &InappproductsBatchDeleteRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/inappproducts:batchDelete",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchDelete
/// Deletes in-app products (managed products or subscriptions). Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput. This method should not be used to delete subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_inappproducts_batch_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_inappproducts_batch_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchDelete
/// Deletes in-app products (managed products or subscriptions). Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput. This method should not be used to delete subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_inappproducts_batch_delete_builder()` + `androidpublisher_inappproducts_batch_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_batch_delete(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &InappproductsBatchDeleteRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_inappproducts_batch_delete_builder(client, packageName, body)?;
    androidpublisher_inappproducts_batch_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchGet
/// Reads multiple in-app products, which can be managed products or subscriptions. This method should not be used to retrieve subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_inappproducts_batch_get_execute()` to send, or `androidpublisher_inappproducts_batch_get` for simplest API.

pub fn androidpublisher_inappproducts_batch_get_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    sku: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/inappproducts:batchGet",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "sku",
            sku.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchGet
/// Reads multiple in-app products, which can be managed products or subscriptions. This method should not be used to retrieve subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_inappproducts_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_inappproducts_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InappproductsBatchGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InappproductsBatchGetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchGet
/// Reads multiple in-app products, which can be managed products or subscriptions. This method should not be used to retrieve subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_inappproducts_batch_get_builder()` + `androidpublisher_inappproducts_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_batch_get(
    client: &SimpleHttpClient,
    packageName: &str,
    sku: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InappproductsBatchGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_inappproducts_batch_get_builder(client, packageName, sku)?;
    androidpublisher_inappproducts_batch_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchUpdate
/// Updates or inserts one or more in-app products (managed products or subscriptions). Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput. This method should no longer be used to update subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_inappproducts_batch_update_execute()` to send, or `androidpublisher_inappproducts_batch_update` for simplest API.

pub fn androidpublisher_inappproducts_batch_update_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &InappproductsBatchUpdateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/inappproducts:batchUpdate",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchUpdate
/// Updates or inserts one or more in-app products (managed products or subscriptions). Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput. This method should no longer be used to update subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_inappproducts_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_inappproducts_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InappproductsBatchUpdateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InappproductsBatchUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchUpdate
/// Updates or inserts one or more in-app products (managed products or subscriptions). Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput. This method should no longer be used to update subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_inappproducts_batch_update_builder()` + `androidpublisher_inappproducts_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_batch_update(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &InappproductsBatchUpdateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InappproductsBatchUpdateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_inappproducts_batch_update_builder(client, packageName, body)?;
    androidpublisher_inappproducts_batch_update_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Deletes an in-app product (a managed product or a subscription). This method should no longer be used to delete subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_inappproducts_delete_execute()` to send, or `androidpublisher_inappproducts_delete` for simplest API.

pub fn androidpublisher_inappproducts_delete_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    sku: &str,
    latencyTolerance: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/inappproducts/{}",
        packageName, sku,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "latencyTolerance",
            latencyTolerance
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Deletes an in-app product (a managed product or a subscription). This method should no longer be used to delete subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_inappproducts_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_inappproducts_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Deletes an in-app product (a managed product or a subscription). This method should no longer be used to delete subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_inappproducts_delete_builder()` + `androidpublisher_inappproducts_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_delete(
    client: &SimpleHttpClient,
    packageName: &str,
    sku: &str,
    latencyTolerance: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_inappproducts_delete_builder(client, packageName, sku, latencyTolerance)?;
    androidpublisher_inappproducts_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Gets an in-app product, which can be a managed product or a subscription. This method should no longer be used to retrieve subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_inappproducts_get_execute()` to send, or `androidpublisher_inappproducts_get` for simplest API.

pub fn androidpublisher_inappproducts_get_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    sku: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/inappproducts/{}",
        packageName, sku,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Gets an in-app product, which can be a managed product or a subscription. This method should no longer be used to retrieve subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_inappproducts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_inappproducts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InAppProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InAppProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Gets an in-app product, which can be a managed product or a subscription. This method should no longer be used to retrieve subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_inappproducts_get_builder()` + `androidpublisher_inappproducts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_get(
    client: &SimpleHttpClient,
    packageName: &str,
    sku: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InAppProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_inappproducts_get_builder(client, packageName, sku)?;
    androidpublisher_inappproducts_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts
/// Creates an in-app product (a managed product or a subscription). This method should no longer be used to create subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_inappproducts_insert_execute()` to send, or `androidpublisher_inappproducts_insert` for simplest API.

pub fn androidpublisher_inappproducts_insert_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    autoConvertMissingPrices: Option<bool>,
    body: &InAppProduct,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/inappproducts",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "autoConvertMissingPrices",
            autoConvertMissingPrices
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts
/// Creates an in-app product (a managed product or a subscription). This method should no longer be used to create subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_inappproducts_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_inappproducts_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InAppProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InAppProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts
/// Creates an in-app product (a managed product or a subscription). This method should no longer be used to create subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_inappproducts_insert_builder()` + `androidpublisher_inappproducts_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_insert(
    client: &SimpleHttpClient,
    packageName: &str,
    autoConvertMissingPrices: Option<bool>,
    body: &InAppProduct,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InAppProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_inappproducts_insert_builder(
        client,
        packageName,
        autoConvertMissingPrices,
        body,
    )?;
    androidpublisher_inappproducts_insert_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts
/// Lists all in-app products - both managed products and subscriptions. If an app has a large number of in-app products, the response may be paginated. In this case the response field `tokenPagination`.`nextPageToken` will be set and the caller should provide its value as a token request parameter to retrieve the next page. This method should no longer be used to retrieve subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_inappproducts_list_execute()` to send, or `androidpublisher_inappproducts_list` for simplest API.

pub fn androidpublisher_inappproducts_list_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    maxResults: Option<i32>,
    startIndex: Option<i32>,
    token: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/inappproducts",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startIndex",
            startIndex
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "token",
            token
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts
/// Lists all in-app products - both managed products and subscriptions. If an app has a large number of in-app products, the response may be paginated. In this case the response field `tokenPagination`.`nextPageToken` will be set and the caller should provide its value as a token request parameter to retrieve the next page. This method should no longer be used to retrieve subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_inappproducts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_inappproducts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InappproductsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InappproductsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts
/// Lists all in-app products - both managed products and subscriptions. If an app has a large number of in-app products, the response may be paginated. In this case the response field `tokenPagination`.`nextPageToken` will be set and the caller should provide its value as a token request parameter to retrieve the next page. This method should no longer be used to retrieve subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_inappproducts_list_builder()` + `androidpublisher_inappproducts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_list(
    client: &SimpleHttpClient,
    packageName: &str,
    maxResults: Option<i32>,
    startIndex: Option<i32>,
    token: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InappproductsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_inappproducts_list_builder(
        client,
        packageName,
        maxResults,
        startIndex,
        token,
    )?;
    androidpublisher_inappproducts_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Patches an in-app product (a managed product or a subscription). This method should no longer be used to update subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_inappproducts_patch_execute()` to send, or `androidpublisher_inappproducts_patch` for simplest API.

pub fn androidpublisher_inappproducts_patch_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    sku: &str,
    autoConvertMissingPrices: Option<bool>,
    latencyTolerance: Option<&str>,
    body: &InAppProduct,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/inappproducts/{}",
        packageName, sku,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "autoConvertMissingPrices",
            autoConvertMissingPrices
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "latencyTolerance",
            latencyTolerance
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Patches an in-app product (a managed product or a subscription). This method should no longer be used to update subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_inappproducts_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_inappproducts_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InAppProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InAppProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Patches an in-app product (a managed product or a subscription). This method should no longer be used to update subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_inappproducts_patch_builder()` + `androidpublisher_inappproducts_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_patch(
    client: &SimpleHttpClient,
    packageName: &str,
    sku: &str,
    autoConvertMissingPrices: Option<bool>,
    latencyTolerance: Option<&str>,
    body: &InAppProduct,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InAppProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_inappproducts_patch_builder(
        client,
        packageName,
        sku,
        autoConvertMissingPrices,
        latencyTolerance,
        body,
    )?;
    androidpublisher_inappproducts_patch_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Updates an in-app product (a managed product or a subscription). This method should no longer be used to update subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_inappproducts_update_execute()` to send, or `androidpublisher_inappproducts_update` for simplest API.

pub fn androidpublisher_inappproducts_update_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    sku: &str,
    allowMissing: Option<bool>,
    autoConvertMissingPrices: Option<bool>,
    latencyTolerance: Option<&str>,
    body: &InAppProduct,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/inappproducts/{}",
        packageName, sku,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "allowMissing",
            allowMissing
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "autoConvertMissingPrices",
            autoConvertMissingPrices
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "latencyTolerance",
            latencyTolerance
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Updates an in-app product (a managed product or a subscription). This method should no longer be used to update subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_inappproducts_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_inappproducts_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InAppProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InAppProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Updates an in-app product (a managed product or a subscription). This method should no longer be used to update subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_inappproducts_update_builder()` + `androidpublisher_inappproducts_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_update(
    client: &SimpleHttpClient,
    packageName: &str,
    sku: &str,
    allowMissing: Option<bool>,
    autoConvertMissingPrices: Option<bool>,
    latencyTolerance: Option<&str>,
    body: &InAppProduct,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InAppProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_inappproducts_update_builder(
        client,
        packageName,
        sku,
        allowMissing,
        autoConvertMissingPrices,
        latencyTolerance,
        body,
    )?;
    androidpublisher_inappproducts_update_execute(builder)
}

/// GET androidpublisher/v3/applications/internalappsharing/{packageName}/artifacts/apk
/// Uploads an APK to internal app sharing. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_internalappsharingartifacts_uploadapk_execute()` to send, or `androidpublisher_internalappsharingartifacts_uploadapk` for simplest API.

pub fn androidpublisher_internalappsharingartifacts_uploadapk_builder(
    client: &SimpleHttpClient,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/internalappsharing/{}/artifacts/apk",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/internalappsharing/{packageName}/artifacts/apk
/// Uploads an APK to internal app sharing. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_internalappsharingartifacts_uploadapk_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_internalappsharingartifacts_uploadapk_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_internalappsharingartifacts_uploadapk_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InternalAppSharingArtifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InternalAppSharingArtifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/internalappsharing/{packageName}/artifacts/apk
/// Uploads an APK to internal app sharing. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_internalappsharingartifacts_uploadapk_builder()` + `androidpublisher_internalappsharingartifacts_uploadapk_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_internalappsharingartifacts_uploadapk(
    client: &SimpleHttpClient,
    packageName: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InternalAppSharingArtifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_internalappsharingartifacts_uploadapk_builder(client, packageName)?;
    androidpublisher_internalappsharingartifacts_uploadapk_execute(builder)
}

/// GET androidpublisher/v3/applications/internalappsharing/{packageName}/artifacts/bundle
/// Uploads an app bundle to internal app sharing. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_internalappsharingartifacts_uploadbundle_execute()` to send, or `androidpublisher_internalappsharingartifacts_uploadbundle` for simplest API.

pub fn androidpublisher_internalappsharingartifacts_uploadbundle_builder(
    client: &SimpleHttpClient,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/internalappsharing/{}/artifacts/bundle",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/internalappsharing/{packageName}/artifacts/bundle
/// Uploads an app bundle to internal app sharing. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_internalappsharingartifacts_uploadbundle_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_internalappsharingartifacts_uploadbundle_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_internalappsharingartifacts_uploadbundle_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InternalAppSharingArtifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InternalAppSharingArtifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/internalappsharing/{packageName}/artifacts/bundle
/// Uploads an app bundle to internal app sharing. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_internalappsharingartifacts_uploadbundle_builder()` + `androidpublisher_internalappsharingartifacts_uploadbundle_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_internalappsharingartifacts_uploadbundle(
    client: &SimpleHttpClient,
    packageName: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InternalAppSharingArtifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_internalappsharingartifacts_uploadbundle_builder(client, packageName)?;
    androidpublisher_internalappsharingartifacts_uploadbundle_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/pricing:convertRegionPrices
/// Calculates the region prices, using today's exchange rate and country-specific pricing patterns, based on the price in the request for a set of regions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_convert_region_prices_execute()` to send, or `androidpublisher_monetization_convert_region_prices` for simplest API.

pub fn androidpublisher_monetization_convert_region_prices_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &ConvertRegionPricesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/pricing:convertRegionPrices",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/pricing:convertRegionPrices
/// Calculates the region prices, using today's exchange rate and country-specific pricing patterns, based on the price in the request for a set of regions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_convert_region_prices_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_convert_region_prices_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_convert_region_prices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ConvertRegionPricesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConvertRegionPricesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/pricing:convertRegionPrices
/// Calculates the region prices, using today's exchange rate and country-specific pricing patterns, based on the price in the request for a set of regions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_convert_region_prices_builder()` + `androidpublisher_monetization_convert_region_prices_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_convert_region_prices(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &ConvertRegionPricesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ConvertRegionPricesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_convert_region_prices_builder(client, packageName, body)?;
    androidpublisher_monetization_convert_region_prices_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchDelete
/// Deletes one or more one-time products.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_batch_delete_execute()` to send, or `androidpublisher_monetization_onetimeproducts_batch_delete` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_batch_delete_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &BatchDeleteOneTimeProductsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts:batchDelete",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchDelete
/// Deletes one or more one-time products.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_batch_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_batch_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchDelete
/// Deletes one or more one-time products.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_batch_delete_builder()` + `androidpublisher_monetization_onetimeproducts_batch_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_batch_delete(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &BatchDeleteOneTimeProductsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_onetimeproducts_batch_delete_builder(
        client,
        packageName,
        body,
    )?;
    androidpublisher_monetization_onetimeproducts_batch_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchGet
/// Reads one or more one-time products.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_batch_get_execute()` to send, or `androidpublisher_monetization_onetimeproducts_batch_get` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_batch_get_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productIds: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts:batchGet",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "productIds",
            productIds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchGet
/// Reads one or more one-time products.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetOneTimeProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetOneTimeProductsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchGet
/// Reads one or more one-time products.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_batch_get_builder()` + `androidpublisher_monetization_onetimeproducts_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_batch_get(
    client: &SimpleHttpClient,
    packageName: &str,
    productIds: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetOneTimeProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_onetimeproducts_batch_get_builder(
        client,
        packageName,
        productIds,
    )?;
    androidpublisher_monetization_onetimeproducts_batch_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchUpdate
/// Creates or updates one or more one-time products.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_batch_update_execute()` to send, or `androidpublisher_monetization_onetimeproducts_batch_update` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_batch_update_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &BatchUpdateOneTimeProductsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts:batchUpdate",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchUpdate
/// Creates or updates one or more one-time products.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateOneTimeProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateOneTimeProductsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchUpdate
/// Creates or updates one or more one-time products.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_batch_update_builder()` + `androidpublisher_monetization_onetimeproducts_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_batch_update(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &BatchUpdateOneTimeProductsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateOneTimeProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_onetimeproducts_batch_update_builder(
        client,
        packageName,
        body,
    )?;
    androidpublisher_monetization_onetimeproducts_batch_update_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}
/// Deletes a one-time product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_delete_execute()` to send, or `androidpublisher_monetization_onetimeproducts_delete` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_delete_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    latencyTolerance: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts/{}",
        packageName, productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "latencyTolerance",
            latencyTolerance
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}
/// Deletes a one-time product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}
/// Deletes a one-time product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_delete_builder()` + `androidpublisher_monetization_onetimeproducts_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_delete(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    latencyTolerance: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_onetimeproducts_delete_builder(
        client,
        packageName,
        productId,
        latencyTolerance,
    )?;
    androidpublisher_monetization_onetimeproducts_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}
/// Reads a single one-time product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_get_execute()` to send, or `androidpublisher_monetization_onetimeproducts_get` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_get_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts/{}",
        packageName, productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}
/// Reads a single one-time product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OneTimeProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}
/// Reads a single one-time product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_get_builder()` + `androidpublisher_monetization_onetimeproducts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_get(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_get_builder(client, packageName, productId)?;
    androidpublisher_monetization_onetimeproducts_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts
/// Lists all one-time products under a given app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_list_execute()` to send, or `androidpublisher_monetization_onetimeproducts_list` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_list_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts
/// Lists all one-time products under a given app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListOneTimeProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOneTimeProductsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts
/// Lists all one-time products under a given app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_list_builder()` + `androidpublisher_monetization_onetimeproducts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_list(
    client: &SimpleHttpClient,
    packageName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListOneTimeProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_onetimeproducts_list_builder(
        client,
        packageName,
        pageSize,
        pageToken,
    )?;
    androidpublisher_monetization_onetimeproducts_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/onetimeproducts/{productId}
/// Creates or updates a one-time product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_patch_execute()` to send, or `androidpublisher_monetization_onetimeproducts_patch` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_patch_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    allowMissing: Option<bool>,
    latencyTolerance: Option<&str>,
    regionsVersion_version: Option<&str>,
    updateMask: Option<&str>,
    body: &OneTimeProduct,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/onetimeproducts/{}",
        packageName, productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "allowMissing",
            allowMissing
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "latencyTolerance",
            latencyTolerance
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "regionsVersion_version",
            regionsVersion_version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/onetimeproducts/{productId}
/// Creates or updates a one-time product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OneTimeProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/onetimeproducts/{productId}
/// Creates or updates a one-time product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_patch_builder()` + `androidpublisher_monetization_onetimeproducts_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_patch(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    allowMissing: Option<bool>,
    latencyTolerance: Option<&str>,
    regionsVersion_version: Option<&str>,
    updateMask: Option<&str>,
    body: &OneTimeProduct,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_onetimeproducts_patch_builder(
        client,
        packageName,
        productId,
        allowMissing,
        latencyTolerance,
        regionsVersion_version,
        updateMask,
        body,
    )?;
    androidpublisher_monetization_onetimeproducts_patch_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions:batchDelete
/// Deletes purchase options across one or multiple one-time products. By default this operation will fail if there are any existing offers under the deleted purchase options. Use the force parameter to override the default behavior.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    body: &BatchDeletePurchaseOptionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions:batchDelete",
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions:batchDelete
/// Deletes purchase options across one or multiple one-time products. By default this operation will fail if there are any existing offers under the deleted purchase options. Use the force parameter to override the default behavior.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions:batchDelete
/// Deletes purchase options across one or multiple one-time products. By default this operation will fail if there are any existing offers under the deleted purchase options. Use the force parameter to override the default behavior.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    body: &BatchDeletePurchaseOptionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_builder(
            client,
            packageName,
            productId,
            body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions:batchUpdateStates
/// Activates or deactivates purchase options across one or multiple one-time products.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    body: &BatchUpdatePurchaseOptionStatesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions:batchUpdateStates",
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions:batchUpdateStates
/// Activates or deactivates purchase options across one or multiple one-time products.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdatePurchaseOptionStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdatePurchaseOptionStatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions:batchUpdateStates
/// Activates or deactivates purchase options across one or multiple one-time products.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    body: &BatchUpdatePurchaseOptionStatesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdatePurchaseOptionStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_builder(
            client,
            packageName,
            productId,
            body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_execute(
        builder,
    )
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:activate
/// Activates a one-time product offer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_builder(
    client: &SimpleHttpClient,
    offerId: &str,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    body: &ActivateOneTimeProductOfferRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers/{}:activate",
        offerId,
        packageName,
        productId,
        purchaseOptionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:activate
/// Activates a one-time product offer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProductOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OneTimeProductOffer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:activate
/// Activates a one-time product offer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate(
    client: &SimpleHttpClient,
    offerId: &str,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    body: &ActivateOneTimeProductOfferRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProductOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_builder(
            client,
            offerId,
            packageName,
            productId,
            purchaseOptionId,
            body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchDelete
/// Deletes one or more one-time product offers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    body: &BatchDeleteOneTimeProductOffersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers:batchDelete",
        packageName,
        productId,
        purchaseOptionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchDelete
/// Deletes one or more one-time product offers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchDelete
/// Deletes one or more one-time product offers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    body: &BatchDeleteOneTimeProductOffersRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_builder(
            client,
            packageName,
            productId,
            purchaseOptionId,
            body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_execute(
        builder,
    )
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchGet
/// Reads one or more one-time product offers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    body: &BatchGetOneTimeProductOffersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers:batchGet",
        packageName,
        productId,
        purchaseOptionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchGet
/// Reads one or more one-time product offers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetOneTimeProductOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetOneTimeProductOffersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchGet
/// Reads one or more one-time product offers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    body: &BatchGetOneTimeProductOffersRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetOneTimeProductOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_builder(
            client,
            packageName,
            productId,
            purchaseOptionId,
            body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchUpdate
/// Creates or updates one or more one-time product offers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    body: &BatchUpdateOneTimeProductOffersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers:batchUpdate",
        packageName,
        productId,
        purchaseOptionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchUpdate
/// Creates or updates one or more one-time product offers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateOneTimeProductOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateOneTimeProductOffersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchUpdate
/// Creates or updates one or more one-time product offers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    body: &BatchUpdateOneTimeProductOffersRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateOneTimeProductOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_builder(
            client,
            packageName,
            productId,
            purchaseOptionId,
            body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_execute(
        builder,
    )
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchUpdateStates
/// Updates a batch of one-time product offer states.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    body: &BatchUpdateOneTimeProductOfferStatesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers:batchUpdateStates",
        packageName,
        productId,
        purchaseOptionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchUpdateStates
/// Updates a batch of one-time product offer states.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateOneTimeProductOfferStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateOneTimeProductOfferStatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchUpdateStates
/// Updates a batch of one-time product offer states.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    body: &BatchUpdateOneTimeProductOfferStatesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateOneTimeProductOfferStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_builder(client, packageName, productId, purchaseOptionId, body)?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:cancel
/// Cancels a one-time product offer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_builder(
    client: &SimpleHttpClient,
    offerId: &str,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    body: &CancelOneTimeProductOfferRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers/{}:cancel",
        offerId,
        packageName,
        productId,
        purchaseOptionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:cancel
/// Cancels a one-time product offer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProductOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OneTimeProductOffer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:cancel
/// Cancels a one-time product offer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel(
    client: &SimpleHttpClient,
    offerId: &str,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    body: &CancelOneTimeProductOfferRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProductOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_builder(
            client,
            offerId,
            packageName,
            productId,
            purchaseOptionId,
            body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:deactivate
/// Deactivates a one-time product offer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_builder(
    client: &SimpleHttpClient,
    offerId: &str,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    body: &DeactivateOneTimeProductOfferRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers/{}:deactivate",
        offerId,
        packageName,
        productId,
        purchaseOptionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:deactivate
/// Deactivates a one-time product offer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProductOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OneTimeProductOffer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:deactivate
/// Deactivates a one-time product offer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate(
    client: &SimpleHttpClient,
    offerId: &str,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    body: &DeactivateOneTimeProductOfferRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProductOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_builder(
            client,
            offerId,
            packageName,
            productId,
            purchaseOptionId,
            body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_execute(
        builder,
    )
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers
/// Lists all offers under a given app, product, or purchase option.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers",
        packageName,
        productId,
        purchaseOptionId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers
/// Lists all offers under a given app, product, or purchase option.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListOneTimeProductOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOneTimeProductOffersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers
/// Lists all offers under a given app, product, or purchase option.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_list(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    purchaseOptionId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListOneTimeProductOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_builder(
            client,
            packageName,
            productId,
            purchaseOptionId,
            pageSize,
            pageToken,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}:archive
/// Deprecated: subscription archiving is not supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_archive_execute()` to send, or `androidpublisher_monetization_subscriptions_archive` for simplest API.

pub fn androidpublisher_monetization_subscriptions_archive_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    body: &ArchiveSubscriptionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}:archive",
        packageName, productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}:archive
/// Deprecated: subscription archiving is not supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_archive_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_archive_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_archive_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}:archive
/// Deprecated: subscription archiving is not supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_archive_builder()` + `androidpublisher_monetization_subscriptions_archive_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_archive(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    body: &ArchiveSubscriptionRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_archive_builder(
        client,
        packageName,
        productId,
        body,
    )?;
    androidpublisher_monetization_subscriptions_archive_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions:batchGet
/// Reads one or more subscriptions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_batch_get_execute()` to send, or `androidpublisher_monetization_subscriptions_batch_get` for simplest API.

pub fn androidpublisher_monetization_subscriptions_batch_get_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productIds: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions:batchGet",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "productIds",
            productIds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions:batchGet
/// Reads one or more subscriptions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetSubscriptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetSubscriptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions:batchGet
/// Reads one or more subscriptions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_batch_get_builder()` + `androidpublisher_monetization_subscriptions_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_batch_get(
    client: &SimpleHttpClient,
    packageName: &str,
    productIds: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetSubscriptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_batch_get_builder(
        client,
        packageName,
        productIds,
    )?;
    androidpublisher_monetization_subscriptions_batch_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions:batchUpdate
/// Updates a batch of subscriptions. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_batch_update_execute()` to send, or `androidpublisher_monetization_subscriptions_batch_update` for simplest API.

pub fn androidpublisher_monetization_subscriptions_batch_update_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &BatchUpdateSubscriptionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions:batchUpdate",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions:batchUpdate
/// Updates a batch of subscriptions. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateSubscriptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateSubscriptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions:batchUpdate
/// Updates a batch of subscriptions. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_batch_update_builder()` + `androidpublisher_monetization_subscriptions_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_batch_update(
    client: &SimpleHttpClient,
    packageName: &str,
    body: &BatchUpdateSubscriptionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateSubscriptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_batch_update_builder(
        client,
        packageName,
        body,
    )?;
    androidpublisher_monetization_subscriptions_batch_update_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions
/// Creates a new subscription. Newly added base plans will remain in draft state until activated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_create_execute()` to send, or `androidpublisher_monetization_subscriptions_create` for simplest API.

pub fn androidpublisher_monetization_subscriptions_create_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: Option<&str>,
    regionsVersion_version: Option<&str>,
    body: &Subscription,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "productId",
            productId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "regionsVersion_version",
            regionsVersion_version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions
/// Creates a new subscription. Newly added base plans will remain in draft state until activated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions
/// Creates a new subscription. Newly added base plans will remain in draft state until activated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_create_builder()` + `androidpublisher_monetization_subscriptions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_create(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: Option<&str>,
    regionsVersion_version: Option<&str>,
    body: &Subscription,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_create_builder(
        client,
        packageName,
        productId,
        regionsVersion_version,
        body,
    )?;
    androidpublisher_monetization_subscriptions_create_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}
/// Deletes a subscription. A subscription can only be deleted if it has never had a base plan published.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_delete_execute()` to send, or `androidpublisher_monetization_subscriptions_delete` for simplest API.

pub fn androidpublisher_monetization_subscriptions_delete_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}",
        packageName, productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}
/// Deletes a subscription. A subscription can only be deleted if it has never had a base plan published.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}
/// Deletes a subscription. A subscription can only be deleted if it has never had a base plan published.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_delete_builder()` + `androidpublisher_monetization_subscriptions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_delete(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_subscriptions_delete_builder(client, packageName, productId)?;
    androidpublisher_monetization_subscriptions_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}
/// Reads a single subscription.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_get_execute()` to send, or `androidpublisher_monetization_subscriptions_get` for simplest API.

pub fn androidpublisher_monetization_subscriptions_get_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}",
        packageName, productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}
/// Reads a single subscription.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}
/// Reads a single subscription.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_get_builder()` + `androidpublisher_monetization_subscriptions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_get(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_subscriptions_get_builder(client, packageName, productId)?;
    androidpublisher_monetization_subscriptions_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions
/// Lists all subscriptions under a given app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_list_execute()` to send, or `androidpublisher_monetization_subscriptions_list` for simplest API.

pub fn androidpublisher_monetization_subscriptions_list_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    showArchived: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "showArchived",
            showArchived
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions
/// Lists all subscriptions under a given app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSubscriptionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSubscriptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions
/// Lists all subscriptions under a given app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_list_builder()` + `androidpublisher_monetization_subscriptions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_list(
    client: &SimpleHttpClient,
    packageName: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    showArchived: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSubscriptionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_list_builder(
        client,
        packageName,
        pageSize,
        pageToken,
        showArchived,
    )?;
    androidpublisher_monetization_subscriptions_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}
/// Updates an existing subscription.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_patch_execute()` to send, or `androidpublisher_monetization_subscriptions_patch` for simplest API.

pub fn androidpublisher_monetization_subscriptions_patch_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    allowMissing: Option<bool>,
    latencyTolerance: Option<&str>,
    regionsVersion_version: Option<&str>,
    updateMask: Option<&str>,
    body: &Subscription,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}",
        packageName, productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "allowMissing",
            allowMissing
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "latencyTolerance",
            latencyTolerance
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "regionsVersion_version",
            regionsVersion_version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}
/// Updates an existing subscription.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}
/// Updates an existing subscription.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_patch_builder()` + `androidpublisher_monetization_subscriptions_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_patch(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    allowMissing: Option<bool>,
    latencyTolerance: Option<&str>,
    regionsVersion_version: Option<&str>,
    updateMask: Option<&str>,
    body: &Subscription,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_patch_builder(
        client,
        packageName,
        productId,
        allowMissing,
        latencyTolerance,
        regionsVersion_version,
        updateMask,
        body,
    )?;
    androidpublisher_monetization_subscriptions_patch_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:activate
/// Activates a base plan. Once activated, base plans will be available to new subscribers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_activate_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_activate` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_activate_builder(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    body: &ActivateBasePlanRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}:activate",
        basePlanId,
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:activate
/// Activates a base plan. Once activated, base plans will be available to new subscribers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_activate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_activate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:activate
/// Activates a base plan. Once activated, base plans will be available to new subscribers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_activate_builder()` + `androidpublisher_monetization_subscriptions_base_plans_activate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_activate(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    body: &ActivateBasePlanRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_activate_builder(
        client,
        basePlanId,
        packageName,
        productId,
        body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_activate_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans:batchMigratePrices
/// Batch variant of the MigrateBasePlanPrices endpoint. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    body: &BatchMigrateBasePlanPricesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans:batchMigratePrices",
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans:batchMigratePrices
/// Batch variant of the MigrateBasePlanPrices endpoint. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchMigrateBasePlanPricesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchMigrateBasePlanPricesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans:batchMigratePrices
/// Batch variant of the MigrateBasePlanPrices endpoint. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_builder()` + `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    body: &BatchMigrateBasePlanPricesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchMigrateBasePlanPricesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_builder(
            client,
            packageName,
            productId,
            body,
        )?;
    androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans:batchUpdateStates
/// Activates or deactivates base plans across one or multiple subscriptions. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_batch_update_states_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_batch_update_states` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_batch_update_states_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    body: &BatchUpdateBasePlanStatesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans:batchUpdateStates",
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans:batchUpdateStates
/// Activates or deactivates base plans across one or multiple subscriptions. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_batch_update_states_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_batch_update_states_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_batch_update_states_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateBasePlanStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateBasePlanStatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans:batchUpdateStates
/// Activates or deactivates base plans across one or multiple subscriptions. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_batch_update_states_builder()` + `androidpublisher_monetization_subscriptions_base_plans_batch_update_states_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_batch_update_states(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    body: &BatchUpdateBasePlanStatesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateBasePlanStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_subscriptions_base_plans_batch_update_states_builder(
            client,
            packageName,
            productId,
            body,
        )?;
    androidpublisher_monetization_subscriptions_base_plans_batch_update_states_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:deactivate
/// Deactivates a base plan. Once deactivated, the base plan will become unavailable to new subscribers, but existing subscribers will maintain their subscription
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_deactivate_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_deactivate` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_deactivate_builder(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    body: &DeactivateBasePlanRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}:deactivate",
        basePlanId,
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:deactivate
/// Deactivates a base plan. Once deactivated, the base plan will become unavailable to new subscribers, but existing subscribers will maintain their subscription
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_deactivate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_deactivate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_deactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:deactivate
/// Deactivates a base plan. Once deactivated, the base plan will become unavailable to new subscribers, but existing subscribers will maintain their subscription
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_deactivate_builder()` + `androidpublisher_monetization_subscriptions_base_plans_deactivate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_deactivate(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    body: &DeactivateBasePlanRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_deactivate_builder(
        client,
        basePlanId,
        packageName,
        productId,
        body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_deactivate_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}
/// Deletes a base plan. Can only be done for draft base plans. This action is irreversible.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_delete_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_delete` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_delete_builder(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}",
        basePlanId, packageName, productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}
/// Deletes a base plan. Can only be done for draft base plans. This action is irreversible.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}
/// Deletes a base plan. Can only be done for draft base plans. This action is irreversible.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_delete_builder()` + `androidpublisher_monetization_subscriptions_base_plans_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_delete(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_delete_builder(
        client,
        basePlanId,
        packageName,
        productId,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:migratePrices
/// Migrates subscribers from one or more legacy price cohorts to the current price. Requests result in Google Play notifying affected subscribers. Only up to 250 simultaneous legacy price cohorts are supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_migrate_prices_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_migrate_prices` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_migrate_prices_builder(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    body: &MigrateBasePlanPricesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}:migratePrices",
        basePlanId,
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:migratePrices
/// Migrates subscribers from one or more legacy price cohorts to the current price. Requests result in Google Play notifying affected subscribers. Only up to 250 simultaneous legacy price cohorts are supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_migrate_prices_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_migrate_prices_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_migrate_prices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MigrateBasePlanPricesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MigrateBasePlanPricesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:migratePrices
/// Migrates subscribers from one or more legacy price cohorts to the current price. Requests result in Google Play notifying affected subscribers. Only up to 250 simultaneous legacy price cohorts are supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_migrate_prices_builder()` + `androidpublisher_monetization_subscriptions_base_plans_migrate_prices_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_migrate_prices(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    body: &MigrateBasePlanPricesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MigrateBasePlanPricesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_migrate_prices_builder(
        client,
        basePlanId,
        packageName,
        productId,
        body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_migrate_prices_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}:activate
/// Activates a subscription offer. Once activated, subscription offers will be available to new subscribers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_activate_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_activate` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_activate_builder(
    client: &SimpleHttpClient,
    basePlanId: &str,
    offerId: &str,
    packageName: &str,
    productId: &str,
    body: &ActivateSubscriptionOfferRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers/{}:activate",
        basePlanId,
        offerId,
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}:activate
/// Activates a subscription offer. Once activated, subscription offers will be available to new subscribers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_activate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_offers_activate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionOffer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}:activate
/// Activates a subscription offer. Once activated, subscription offers will be available to new subscribers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_activate_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_activate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_activate(
    client: &SimpleHttpClient,
    basePlanId: &str,
    offerId: &str,
    packageName: &str,
    productId: &str,
    body: &ActivateSubscriptionOfferRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_offers_activate_builder(
        client,
        basePlanId,
        offerId,
        packageName,
        productId,
        body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_activate_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchGet
/// Reads one or more subscription offers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_builder(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    body: &BatchGetSubscriptionOffersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers:batchGet",
        basePlanId,
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchGet
/// Reads one or more subscription offers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetSubscriptionOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetSubscriptionOffersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchGet
/// Reads one or more subscription offers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_get(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    body: &BatchGetSubscriptionOffersRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetSubscriptionOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_builder(
        client,
        basePlanId,
        packageName,
        productId,
        body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchUpdate
/// Updates a batch of subscription offers. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_builder(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    body: &BatchUpdateSubscriptionOffersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers:batchUpdate",
        basePlanId,
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchUpdate
/// Updates a batch of subscription offers. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateSubscriptionOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateSubscriptionOffersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchUpdate
/// Updates a batch of subscription offers. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_update(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    body: &BatchUpdateSubscriptionOffersRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateSubscriptionOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_builder(
            client,
            basePlanId,
            packageName,
            productId,
            body,
        )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchUpdateStates
/// Updates a batch of subscription offer states. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_builder(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    body: &BatchUpdateSubscriptionOfferStatesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers:batchUpdateStates",
        basePlanId,
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchUpdateStates
/// Updates a batch of subscription offer states. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateSubscriptionOfferStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateSubscriptionOfferStatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchUpdateStates
/// Updates a batch of subscription offer states. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    body: &BatchUpdateSubscriptionOfferStatesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateSubscriptionOfferStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_builder(
            client,
            basePlanId,
            packageName,
            productId,
            body,
        )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_execute(
        builder,
    )
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers
/// Creates a new subscription offer. Only auto-renewing base plans can have subscription offers. The offer state will be DRAFT until it is activated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_create_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_create` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_create_builder(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    offerId: Option<&str>,
    regionsVersion_version: Option<&str>,
    body: &SubscriptionOffer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers",
        basePlanId,
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "offerId",
            offerId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "regionsVersion_version",
            regionsVersion_version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers
/// Creates a new subscription offer. Only auto-renewing base plans can have subscription offers. The offer state will be DRAFT until it is activated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_offers_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionOffer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers
/// Creates a new subscription offer. Only auto-renewing base plans can have subscription offers. The offer state will be DRAFT until it is activated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_create_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_create(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    offerId: Option<&str>,
    regionsVersion_version: Option<&str>,
    body: &SubscriptionOffer,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_offers_create_builder(
        client,
        basePlanId,
        packageName,
        productId,
        offerId,
        regionsVersion_version,
        body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_create_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}:deactivate
/// Deactivates a subscription offer. Once deactivated, existing subscribers will maintain their subscription, but the offer will become unavailable to new subscribers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_builder(
    client: &SimpleHttpClient,
    basePlanId: &str,
    offerId: &str,
    packageName: &str,
    productId: &str,
    body: &DeactivateSubscriptionOfferRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers/{}:deactivate",
        basePlanId,
        offerId,
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}:deactivate
/// Deactivates a subscription offer. Once deactivated, existing subscribers will maintain their subscription, but the offer will become unavailable to new subscribers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionOffer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}:deactivate
/// Deactivates a subscription offer. Once deactivated, existing subscribers will maintain their subscription, but the offer will become unavailable to new subscribers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_deactivate(
    client: &SimpleHttpClient,
    basePlanId: &str,
    offerId: &str,
    packageName: &str,
    productId: &str,
    body: &DeactivateSubscriptionOfferRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_builder(
        client,
        basePlanId,
        offerId,
        packageName,
        productId,
        body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}
/// Deletes a subscription offer. Can only be done for draft offers. This action is irreversible.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_delete_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_delete` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_delete_builder(
    client: &SimpleHttpClient,
    basePlanId: &str,
    offerId: &str,
    packageName: &str,
    productId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers/{}",
        basePlanId,
        offerId,
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}
/// Deletes a subscription offer. Can only be done for draft offers. This action is irreversible.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_offers_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}
/// Deletes a subscription offer. Can only be done for draft offers. This action is irreversible.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_delete_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_delete(
    client: &SimpleHttpClient,
    basePlanId: &str,
    offerId: &str,
    packageName: &str,
    productId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_offers_delete_builder(
        client,
        basePlanId,
        offerId,
        packageName,
        productId,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}
/// Reads a single offer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_get_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_get` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_get_builder(
    client: &SimpleHttpClient,
    basePlanId: &str,
    offerId: &str,
    packageName: &str,
    productId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers/{}",
        basePlanId,
        offerId,
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}
/// Reads a single offer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_offers_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionOffer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}
/// Reads a single offer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_get_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_get(
    client: &SimpleHttpClient,
    basePlanId: &str,
    offerId: &str,
    packageName: &str,
    productId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_offers_get_builder(
        client,
        basePlanId,
        offerId,
        packageName,
        productId,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers
/// Lists all offers under a given subscription.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_list_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_list` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_list_builder(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers",
        basePlanId,
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers
/// Lists all offers under a given subscription.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_offers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSubscriptionOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSubscriptionOffersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers
/// Lists all offers under a given subscription.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_list_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_list(
    client: &SimpleHttpClient,
    basePlanId: &str,
    packageName: &str,
    productId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListSubscriptionOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_offers_list_builder(
        client,
        basePlanId,
        packageName,
        productId,
        pageSize,
        pageToken,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}
/// Updates an existing subscription offer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_patch_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_patch` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_patch_builder(
    client: &SimpleHttpClient,
    basePlanId: &str,
    offerId: &str,
    packageName: &str,
    productId: &str,
    allowMissing: Option<bool>,
    latencyTolerance: Option<&str>,
    regionsVersion_version: Option<&str>,
    updateMask: Option<&str>,
    body: &SubscriptionOffer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers/{}",
        basePlanId,
        offerId,
        packageName,
        productId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "allowMissing",
            allowMissing
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "latencyTolerance",
            latencyTolerance
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "regionsVersion_version",
            regionsVersion_version
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}
/// Updates an existing subscription offer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_monetization_subscriptions_base_plans_offers_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionOffer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}
/// Updates an existing subscription offer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_patch_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_patch(
    client: &SimpleHttpClient,
    basePlanId: &str,
    offerId: &str,
    packageName: &str,
    productId: &str,
    allowMissing: Option<bool>,
    latencyTolerance: Option<&str>,
    regionsVersion_version: Option<&str>,
    updateMask: Option<&str>,
    body: &SubscriptionOffer,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_offers_patch_builder(
        client,
        basePlanId,
        offerId,
        packageName,
        productId,
        allowMissing,
        latencyTolerance,
        regionsVersion_version,
        updateMask,
        body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_patch_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/orders:batchGet
/// Get order details for a list of orders.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_orders_batchget_execute()` to send, or `androidpublisher_orders_batchget` for simplest API.

pub fn androidpublisher_orders_batchget_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    orderIds: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/orders:batchGet",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "orderIds",
            orderIds
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/orders:batchGet
/// Get order details for a list of orders.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_orders_batchget_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_orders_batchget_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_orders_batchget_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchGetOrdersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetOrdersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/orders:batchGet
/// Get order details for a list of orders.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_orders_batchget_builder()` + `androidpublisher_orders_batchget_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_orders_batchget(
    client: &SimpleHttpClient,
    packageName: &str,
    orderIds: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchGetOrdersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_orders_batchget_builder(client, packageName, orderIds)?;
    androidpublisher_orders_batchget_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/orders/{orderId}
/// Get order details for a single order.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_orders_get_execute()` to send, or `androidpublisher_orders_get` for simplest API.

pub fn androidpublisher_orders_get_builder(
    client: &SimpleHttpClient,
    orderId: &str,
    packageName: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/orders/{}",
        orderId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/orders/{orderId}
/// Get order details for a single order.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_orders_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_orders_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_orders_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Order>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Order = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/orders/{orderId}
/// Get order details for a single order.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_orders_get_builder()` + `androidpublisher_orders_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_orders_get(
    client: &SimpleHttpClient,
    orderId: &str,
    packageName: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Order>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_orders_get_builder(client, orderId, packageName)?;
    androidpublisher_orders_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/orders/{orderId}:refund
/// Refunds a user's subscription or in-app purchase order. Orders older than 3 years cannot be refunded.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_orders_refund_execute()` to send, or `androidpublisher_orders_refund` for simplest API.

pub fn androidpublisher_orders_refund_builder(
    client: &SimpleHttpClient,
    orderId: &str,
    packageName: &str,
    revoke: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/orders/{}:refund",
        orderId, packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "revoke",
            revoke
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/orders/{orderId}:refund
/// Refunds a user's subscription or in-app purchase order. Orders older than 3 years cannot be refunded.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_orders_refund_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_orders_refund_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_orders_refund_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/orders/{orderId}:refund
/// Refunds a user's subscription or in-app purchase order. Orders older than 3 years cannot be refunded.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_orders_refund_builder()` + `androidpublisher_orders_refund_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_orders_refund(
    client: &SimpleHttpClient,
    orderId: &str,
    packageName: &str,
    revoke: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_orders_refund_builder(client, orderId, packageName, revoke)?;
    androidpublisher_orders_refund_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}:acknowledge
/// Acknowledges a purchase of an inapp item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_products_acknowledge_execute()` to send, or `androidpublisher_purchases_products_acknowledge` for simplest API.

pub fn androidpublisher_purchases_products_acknowledge_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    token: &str,
    body: &ProductPurchasesAcknowledgeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/products/{}/tokens/{}:acknowledge",
        packageName,
        productId,
        token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}:acknowledge
/// Acknowledges a purchase of an inapp item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_products_acknowledge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_products_acknowledge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_products_acknowledge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}:acknowledge
/// Acknowledges a purchase of an inapp item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_products_acknowledge_builder()` + `androidpublisher_purchases_products_acknowledge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_products_acknowledge(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    token: &str,
    body: &ProductPurchasesAcknowledgeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_products_acknowledge_builder(
        client,
        packageName,
        productId,
        token,
        body,
    )?;
    androidpublisher_purchases_products_acknowledge_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}:consume
/// Consumes a purchase for an inapp item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_products_consume_execute()` to send, or `androidpublisher_purchases_products_consume` for simplest API.

pub fn androidpublisher_purchases_products_consume_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    token: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/products/{}/tokens/{}:consume",
        packageName,
        productId,
        token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}:consume
/// Consumes a purchase for an inapp item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_products_consume_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_products_consume_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_products_consume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}:consume
/// Consumes a purchase for an inapp item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_products_consume_builder()` + `androidpublisher_purchases_products_consume_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_products_consume(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    token: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_purchases_products_consume_builder(client, packageName, productId, token)?;
    androidpublisher_purchases_products_consume_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}
/// Checks the purchase and consumption status of an inapp item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_products_get_execute()` to send, or `androidpublisher_purchases_products_get` for simplest API.

pub fn androidpublisher_purchases_products_get_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    token: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/products/{}/tokens/{}",
        packageName,
        productId,
        token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}
/// Checks the purchase and consumption status of an inapp item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_products_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_products_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_products_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductPurchase>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductPurchase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}
/// Checks the purchase and consumption status of an inapp item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_products_get_builder()` + `androidpublisher_purchases_products_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_products_get(
    client: &SimpleHttpClient,
    packageName: &str,
    productId: &str,
    token: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductPurchase>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_purchases_products_get_builder(client, packageName, productId, token)?;
    androidpublisher_purchases_products_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/productsv2/tokens/{token}
/// Checks the purchase and consumption status of an inapp item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_productsv2_getproductpurchasev2_execute()` to send, or `androidpublisher_purchases_productsv2_getproductpurchasev2` for simplest API.

pub fn androidpublisher_purchases_productsv2_getproductpurchasev2_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    token: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/productsv2/tokens/{}",
        packageName, token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/productsv2/tokens/{token}
/// Checks the purchase and consumption status of an inapp item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_productsv2_getproductpurchasev2_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_productsv2_getproductpurchasev2_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_productsv2_getproductpurchasev2_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductPurchaseV2>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductPurchaseV2 = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/productsv2/tokens/{token}
/// Checks the purchase and consumption status of an inapp item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_productsv2_getproductpurchasev2_builder()` + `androidpublisher_purchases_productsv2_getproductpurchasev2_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_productsv2_getproductpurchasev2(
    client: &SimpleHttpClient,
    packageName: &str,
    token: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductPurchaseV2>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_productsv2_getproductpurchasev2_builder(
        client,
        packageName,
        token,
    )?;
    androidpublisher_purchases_productsv2_getproductpurchasev2_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:acknowledge
/// Acknowledges a subscription purchase.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptions_acknowledge_execute()` to send, or `androidpublisher_purchases_subscriptions_acknowledge` for simplest API.

pub fn androidpublisher_purchases_subscriptions_acknowledge_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    subscriptionId: &str,
    token: &str,
    body: &SubscriptionPurchasesAcknowledgeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/subscriptions/{}/tokens/{}:acknowledge",
        packageName,
        subscriptionId,
        token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:acknowledge
/// Acknowledges a subscription purchase.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_subscriptions_acknowledge_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_subscriptions_acknowledge_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_acknowledge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:acknowledge
/// Acknowledges a subscription purchase.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptions_acknowledge_builder()` + `androidpublisher_purchases_subscriptions_acknowledge_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_acknowledge(
    client: &SimpleHttpClient,
    packageName: &str,
    subscriptionId: &str,
    token: &str,
    body: &SubscriptionPurchasesAcknowledgeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptions_acknowledge_builder(
        client,
        packageName,
        subscriptionId,
        token,
        body,
    )?;
    androidpublisher_purchases_subscriptions_acknowledge_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:cancel
/// Cancels a user's subscription purchase. The subscription remains valid until its expiration time. Newer version is available at purchases.subscriptionsv2.cancel for better client library support.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptions_cancel_execute()` to send, or `androidpublisher_purchases_subscriptions_cancel` for simplest API.

pub fn androidpublisher_purchases_subscriptions_cancel_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    subscriptionId: &str,
    token: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/subscriptions/{}/tokens/{}:cancel",
        packageName,
        subscriptionId,
        token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:cancel
/// Cancels a user's subscription purchase. The subscription remains valid until its expiration time. Newer version is available at purchases.subscriptionsv2.cancel for better client library support.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_subscriptions_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_subscriptions_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:cancel
/// Cancels a user's subscription purchase. The subscription remains valid until its expiration time. Newer version is available at purchases.subscriptionsv2.cancel for better client library support.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptions_cancel_builder()` + `androidpublisher_purchases_subscriptions_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_cancel(
    client: &SimpleHttpClient,
    packageName: &str,
    subscriptionId: &str,
    token: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptions_cancel_builder(
        client,
        packageName,
        subscriptionId,
        token,
    )?;
    androidpublisher_purchases_subscriptions_cancel_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:defer
/// Defers a user's subscription purchase until a specified future expiration time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptions_defer_execute()` to send, or `androidpublisher_purchases_subscriptions_defer` for simplest API.

pub fn androidpublisher_purchases_subscriptions_defer_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    subscriptionId: &str,
    token: &str,
    body: &SubscriptionPurchasesDeferRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/subscriptions/{}/tokens/{}:defer",
        packageName,
        subscriptionId,
        token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:defer
/// Defers a user's subscription purchase until a specified future expiration time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_subscriptions_defer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_subscriptions_defer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_defer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubscriptionPurchasesDeferResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionPurchasesDeferResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:defer
/// Defers a user's subscription purchase until a specified future expiration time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptions_defer_builder()` + `androidpublisher_purchases_subscriptions_defer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_defer(
    client: &SimpleHttpClient,
    packageName: &str,
    subscriptionId: &str,
    token: &str,
    body: &SubscriptionPurchasesDeferRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubscriptionPurchasesDeferResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptions_defer_builder(
        client,
        packageName,
        subscriptionId,
        token,
        body,
    )?;
    androidpublisher_purchases_subscriptions_defer_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}
/// Deprecated: Use purchases.subscriptionsv2.get instead. Checks whether a user's subscription purchase is valid and returns its expiry time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptions_get_execute()` to send, or `androidpublisher_purchases_subscriptions_get` for simplest API.

pub fn androidpublisher_purchases_subscriptions_get_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    subscriptionId: &str,
    token: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/subscriptions/{}/tokens/{}",
        packageName,
        subscriptionId,
        token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}
/// Deprecated: Use purchases.subscriptionsv2.get instead. Checks whether a user's subscription purchase is valid and returns its expiry time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_subscriptions_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_subscriptions_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionPurchase>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionPurchase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}
/// Deprecated: Use purchases.subscriptionsv2.get instead. Checks whether a user's subscription purchase is valid and returns its expiry time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptions_get_builder()` + `androidpublisher_purchases_subscriptions_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_get(
    client: &SimpleHttpClient,
    packageName: &str,
    subscriptionId: &str,
    token: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionPurchase>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptions_get_builder(
        client,
        packageName,
        subscriptionId,
        token,
    )?;
    androidpublisher_purchases_subscriptions_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:refund
/// Deprecated: Use orders.refund instead. Refunds a user's subscription purchase, but the subscription remains valid until its expiration time and it will continue to recur.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptions_refund_execute()` to send, or `androidpublisher_purchases_subscriptions_refund` for simplest API.

pub fn androidpublisher_purchases_subscriptions_refund_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    subscriptionId: &str,
    token: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/subscriptions/{}/tokens/{}:refund",
        packageName,
        subscriptionId,
        token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:refund
/// Deprecated: Use orders.refund instead. Refunds a user's subscription purchase, but the subscription remains valid until its expiration time and it will continue to recur.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_subscriptions_refund_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_subscriptions_refund_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_refund_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:refund
/// Deprecated: Use orders.refund instead. Refunds a user's subscription purchase, but the subscription remains valid until its expiration time and it will continue to recur.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptions_refund_builder()` + `androidpublisher_purchases_subscriptions_refund_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_refund(
    client: &SimpleHttpClient,
    packageName: &str,
    subscriptionId: &str,
    token: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptions_refund_builder(
        client,
        packageName,
        subscriptionId,
        token,
    )?;
    androidpublisher_purchases_subscriptions_refund_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:revoke
/// Deprecated: Use purchases.subscriptionsv2.revoke instead. Refunds and immediately revokes a user's subscription purchase. Access to the subscription will be terminated immediately and it will stop recurring.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptions_revoke_execute()` to send, or `androidpublisher_purchases_subscriptions_revoke` for simplest API.

pub fn androidpublisher_purchases_subscriptions_revoke_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    subscriptionId: &str,
    token: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/subscriptions/{}/tokens/{}:revoke",
        packageName,
        subscriptionId,
        token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:revoke
/// Deprecated: Use purchases.subscriptionsv2.revoke instead. Refunds and immediately revokes a user's subscription purchase. Access to the subscription will be terminated immediately and it will stop recurring.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_subscriptions_revoke_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_subscriptions_revoke_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_revoke_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:revoke
/// Deprecated: Use purchases.subscriptionsv2.revoke instead. Refunds and immediately revokes a user's subscription purchase. Access to the subscription will be terminated immediately and it will stop recurring.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptions_revoke_builder()` + `androidpublisher_purchases_subscriptions_revoke_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_revoke(
    client: &SimpleHttpClient,
    packageName: &str,
    subscriptionId: &str,
    token: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptions_revoke_builder(
        client,
        packageName,
        subscriptionId,
        token,
    )?;
    androidpublisher_purchases_subscriptions_revoke_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:cancel
/// Cancel a subscription purchase for the user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptionsv2_cancel_execute()` to send, or `androidpublisher_purchases_subscriptionsv2_cancel` for simplest API.

pub fn androidpublisher_purchases_subscriptionsv2_cancel_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    token: &str,
    body: &CancelSubscriptionPurchaseRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/subscriptionsv2/tokens/{}:cancel",
        packageName,
        token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:cancel
/// Cancel a subscription purchase for the user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_subscriptionsv2_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_subscriptionsv2_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CancelSubscriptionPurchaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CancelSubscriptionPurchaseResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:cancel
/// Cancel a subscription purchase for the user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptionsv2_cancel_builder()` + `androidpublisher_purchases_subscriptionsv2_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_cancel(
    client: &SimpleHttpClient,
    packageName: &str,
    token: &str,
    body: &CancelSubscriptionPurchaseRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CancelSubscriptionPurchaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptionsv2_cancel_builder(
        client,
        packageName,
        token,
        body,
    )?;
    androidpublisher_purchases_subscriptionsv2_cancel_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:defer
/// Defers the renewal of a subscription.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptionsv2_defer_execute()` to send, or `androidpublisher_purchases_subscriptionsv2_defer` for simplest API.

pub fn androidpublisher_purchases_subscriptionsv2_defer_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    token: &str,
    body: &DeferSubscriptionPurchaseRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/subscriptionsv2/tokens/{}:defer",
        packageName,
        token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:defer
/// Defers the renewal of a subscription.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_subscriptionsv2_defer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_subscriptionsv2_defer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_defer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeferSubscriptionPurchaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeferSubscriptionPurchaseResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:defer
/// Defers the renewal of a subscription.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptionsv2_defer_builder()` + `androidpublisher_purchases_subscriptionsv2_defer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_defer(
    client: &SimpleHttpClient,
    packageName: &str,
    token: &str,
    body: &DeferSubscriptionPurchaseRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeferSubscriptionPurchaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_purchases_subscriptionsv2_defer_builder(client, packageName, token, body)?;
    androidpublisher_purchases_subscriptionsv2_defer_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}
/// Get metadata about a subscription
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptionsv2_get_execute()` to send, or `androidpublisher_purchases_subscriptionsv2_get` for simplest API.

pub fn androidpublisher_purchases_subscriptionsv2_get_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    token: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/subscriptionsv2/tokens/{}",
        packageName,
        token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}
/// Get metadata about a subscription
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_subscriptionsv2_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_subscriptionsv2_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionPurchaseV2>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionPurchaseV2 = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}
/// Get metadata about a subscription
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptionsv2_get_builder()` + `androidpublisher_purchases_subscriptionsv2_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_get(
    client: &SimpleHttpClient,
    packageName: &str,
    token: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionPurchaseV2>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_purchases_subscriptionsv2_get_builder(client, packageName, token)?;
    androidpublisher_purchases_subscriptionsv2_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:revoke
/// Revoke a subscription purchase for the user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptionsv2_revoke_execute()` to send, or `androidpublisher_purchases_subscriptionsv2_revoke` for simplest API.

pub fn androidpublisher_purchases_subscriptionsv2_revoke_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    token: &str,
    body: &RevokeSubscriptionPurchaseRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/subscriptionsv2/tokens/{}:revoke",
        packageName,
        token,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:revoke
/// Revoke a subscription purchase for the user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_subscriptionsv2_revoke_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_subscriptionsv2_revoke_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_revoke_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RevokeSubscriptionPurchaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RevokeSubscriptionPurchaseResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:revoke
/// Revoke a subscription purchase for the user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptionsv2_revoke_builder()` + `androidpublisher_purchases_subscriptionsv2_revoke_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_revoke(
    client: &SimpleHttpClient,
    packageName: &str,
    token: &str,
    body: &RevokeSubscriptionPurchaseRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RevokeSubscriptionPurchaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptionsv2_revoke_builder(
        client,
        packageName,
        token,
        body,
    )?;
    androidpublisher_purchases_subscriptionsv2_revoke_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/voidedpurchases
/// Lists the purchases that were canceled, refunded or charged-back.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_voidedpurchases_list_execute()` to send, or `androidpublisher_purchases_voidedpurchases_list` for simplest API.

pub fn androidpublisher_purchases_voidedpurchases_list_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    endTime: Option<&str>,
    includeQuantityBasedPartialRefund: Option<bool>,
    maxResults: Option<i32>,
    startIndex: Option<i32>,
    startTime: Option<&str>,
    token: Option<&str>,
    type_rs: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/purchases/voidedpurchases",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "endTime",
            endTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeQuantityBasedPartialRefund",
            includeQuantityBasedPartialRefund
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startIndex",
            startIndex
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startTime",
            startTime
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "token",
            token
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/voidedpurchases
/// Lists the purchases that were canceled, refunded or charged-back.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_purchases_voidedpurchases_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_purchases_voidedpurchases_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_voidedpurchases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VoidedPurchasesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VoidedPurchasesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/voidedpurchases
/// Lists the purchases that were canceled, refunded or charged-back.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_voidedpurchases_list_builder()` + `androidpublisher_purchases_voidedpurchases_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_voidedpurchases_list(
    client: &SimpleHttpClient,
    packageName: &str,
    endTime: Option<&str>,
    includeQuantityBasedPartialRefund: Option<bool>,
    maxResults: Option<i32>,
    startIndex: Option<i32>,
    startTime: Option<&str>,
    token: Option<&str>,
    type_rs: Option<i32>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VoidedPurchasesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_voidedpurchases_list_builder(
        client,
        packageName,
        endTime,
        includeQuantityBasedPartialRefund,
        maxResults,
        startIndex,
        startTime,
        token,
        type_rs,
    )?;
    androidpublisher_purchases_voidedpurchases_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/reviews/{reviewId}
/// Gets a single review.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_reviews_get_execute()` to send, or `androidpublisher_reviews_get` for simplest API.

pub fn androidpublisher_reviews_get_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    reviewId: &str,
    translationLanguage: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/reviews/{}",
        packageName, reviewId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "translationLanguage",
            translationLanguage
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/reviews/{reviewId}
/// Gets a single review.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_reviews_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_reviews_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_reviews_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Review>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Review = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/reviews/{reviewId}
/// Gets a single review.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_reviews_get_builder()` + `androidpublisher_reviews_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_reviews_get(
    client: &SimpleHttpClient,
    packageName: &str,
    reviewId: &str,
    translationLanguage: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Review>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_reviews_get_builder(client, packageName, reviewId, translationLanguage)?;
    androidpublisher_reviews_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/reviews
/// Lists all reviews.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_reviews_list_execute()` to send, or `androidpublisher_reviews_list` for simplest API.

pub fn androidpublisher_reviews_list_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    maxResults: Option<i32>,
    startIndex: Option<i32>,
    token: Option<&str>,
    translationLanguage: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/reviews",
        packageName,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "maxResults",
            maxResults
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startIndex",
            startIndex
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "token",
            token
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "translationLanguage",
            translationLanguage
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/applications/{packageName}/reviews
/// Lists all reviews.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_reviews_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_reviews_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_reviews_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReviewsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReviewsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/reviews
/// Lists all reviews.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_reviews_list_builder()` + `androidpublisher_reviews_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_reviews_list(
    client: &SimpleHttpClient,
    packageName: &str,
    maxResults: Option<i32>,
    startIndex: Option<i32>,
    token: Option<&str>,
    translationLanguage: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReviewsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_reviews_list_builder(
        client,
        packageName,
        maxResults,
        startIndex,
        token,
        translationLanguage,
    )?;
    androidpublisher_reviews_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/reviews/{reviewId}:reply
/// Replies to a single review, or updates an existing reply.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_reviews_reply_execute()` to send, or `androidpublisher_reviews_reply` for simplest API.

pub fn androidpublisher_reviews_reply_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    reviewId: &str,
    body: &ReviewsReplyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/reviews/{}:reply",
        packageName, reviewId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/reviews/{reviewId}:reply
/// Replies to a single review, or updates an existing reply.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_reviews_reply_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_reviews_reply_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_reviews_reply_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReviewsReplyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReviewsReplyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/reviews/{reviewId}:reply
/// Replies to a single review, or updates an existing reply.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_reviews_reply_builder()` + `androidpublisher_reviews_reply_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_reviews_reply(
    client: &SimpleHttpClient,
    packageName: &str,
    reviewId: &str,
    body: &ReviewsReplyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReviewsReplyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_reviews_reply_builder(client, packageName, reviewId, body)?;
    androidpublisher_reviews_reply_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants
/// Creates an APK which is suitable for inclusion in a system image from an already uploaded Android App Bundle.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_systemapks_variants_create_execute()` to send, or `androidpublisher_systemapks_variants_create` for simplest API.

pub fn androidpublisher_systemapks_variants_create_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    versionCode: &str,
    body: &Variant,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/systemApks/{}/variants",
        packageName, versionCode,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants
/// Creates an APK which is suitable for inclusion in a system image from an already uploaded Android App Bundle.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_systemapks_variants_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_systemapks_variants_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_systemapks_variants_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Variant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Variant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants
/// Creates an APK which is suitable for inclusion in a system image from an already uploaded Android App Bundle.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_systemapks_variants_create_builder()` + `androidpublisher_systemapks_variants_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_systemapks_variants_create(
    client: &SimpleHttpClient,
    packageName: &str,
    versionCode: &str,
    body: &Variant,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Variant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_systemapks_variants_create_builder(
        client,
        packageName,
        versionCode,
        body,
    )?;
    androidpublisher_systemapks_variants_create_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants/{variantId}:download
/// Downloads a previously created system APK which is suitable for inclusion in a system image.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_systemapks_variants_download_execute()` to send, or `androidpublisher_systemapks_variants_download` for simplest API.

pub fn androidpublisher_systemapks_variants_download_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    variantId: &str,
    versionCode: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/systemApks/{}/variants/{}:download",
        packageName,
        variantId,
        versionCode,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants/{variantId}:download
/// Downloads a previously created system APK which is suitable for inclusion in a system image.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_systemapks_variants_download_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_systemapks_variants_download_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_systemapks_variants_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants/{variantId}:download
/// Downloads a previously created system APK which is suitable for inclusion in a system image.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_systemapks_variants_download_builder()` + `androidpublisher_systemapks_variants_download_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_systemapks_variants_download(
    client: &SimpleHttpClient,
    packageName: &str,
    variantId: &str,
    versionCode: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_systemapks_variants_download_builder(
        client,
        packageName,
        variantId,
        versionCode,
    )?;
    androidpublisher_systemapks_variants_download_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants/{variantId}
/// Returns a previously created system APK variant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_systemapks_variants_get_execute()` to send, or `androidpublisher_systemapks_variants_get` for simplest API.

pub fn androidpublisher_systemapks_variants_get_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    variantId: &str,
    versionCode: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/systemApks/{}/variants/{}",
        packageName, variantId, versionCode,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants/{variantId}
/// Returns a previously created system APK variant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_systemapks_variants_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_systemapks_variants_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_systemapks_variants_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Variant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Variant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants/{variantId}
/// Returns a previously created system APK variant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_systemapks_variants_get_builder()` + `androidpublisher_systemapks_variants_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_systemapks_variants_get(
    client: &SimpleHttpClient,
    packageName: &str,
    variantId: &str,
    versionCode: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Variant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_systemapks_variants_get_builder(
        client,
        packageName,
        variantId,
        versionCode,
    )?;
    androidpublisher_systemapks_variants_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants
/// Returns the list of previously created system APK variants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_systemapks_variants_list_execute()` to send, or `androidpublisher_systemapks_variants_list` for simplest API.

pub fn androidpublisher_systemapks_variants_list_builder(
    client: &SimpleHttpClient,
    packageName: &str,
    versionCode: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/applications/{}/systemApks/{}/variants",
        packageName, versionCode,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants
/// Returns the list of previously created system APK variants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_systemapks_variants_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_systemapks_variants_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_systemapks_variants_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SystemApksListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SystemApksListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants
/// Returns the list of previously created system APK variants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_systemapks_variants_list_builder()` + `androidpublisher_systemapks_variants_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_systemapks_variants_list(
    client: &SimpleHttpClient,
    packageName: &str,
    versionCode: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SystemApksListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_systemapks_variants_list_builder(client, packageName, versionCode)?;
    androidpublisher_systemapks_variants_list_execute(builder)
}

/// GET androidpublisher/v3/{+parent}/users
/// Grant access for a user to the given developer account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_users_create_execute()` to send, or `androidpublisher_users_create` for simplest API.

pub fn androidpublisher_users_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &User,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/{+parent}/users",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/{+parent}/users
/// Grant access for a user to the given developer account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_users_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_users_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_users_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+parent}/users
/// Grant access for a user to the given developer account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_users_create_builder()` + `androidpublisher_users_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_users_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &User,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_users_create_builder(client, parent, body)?;
    androidpublisher_users_create_execute(builder)
}

/// GET androidpublisher/v3/{+name}
/// Removes all access for the user to the given developer account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_users_delete_execute()` to send, or `androidpublisher_users_delete` for simplest API.

pub fn androidpublisher_users_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comandroidpublisher/v3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+name}
/// Removes all access for the user to the given developer account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_users_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_users_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_users_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+name}
/// Removes all access for the user to the given developer account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_users_delete_builder()` + `androidpublisher_users_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_users_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_users_delete_builder(client, name)?;
    androidpublisher_users_delete_execute(builder)
}

/// GET androidpublisher/v3/{+parent}/users
/// Lists all users with access to a developer account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_users_list_execute()` to send, or `androidpublisher_users_list` for simplest API.

pub fn androidpublisher_users_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comandroidpublisher/v3/{+parent}/users",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET androidpublisher/v3/{+parent}/users
/// Lists all users with access to a developer account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_users_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_users_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUsersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+parent}/users
/// Lists all users with access to a developer account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_users_list_builder()` + `androidpublisher_users_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_users_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_users_list_builder(client, parent, pageSize, pageToken)?;
    androidpublisher_users_list_execute(builder)
}

/// GET androidpublisher/v3/{+name}
/// Updates access for the user to the developer account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_users_patch_execute()` to send, or `androidpublisher_users_patch` for simplest API.

pub fn androidpublisher_users_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &User,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comandroidpublisher/v3/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET androidpublisher/v3/{+name}
/// Updates access for the user to the developer account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `androidpublisher_users_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from androidpublisher_users_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_users_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/{+name}
/// Updates access for the user to the developer account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_users_patch_builder()` + `androidpublisher_users_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_users_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &User,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_users_patch_builder(client, name, updateMask, body)?;
    androidpublisher_users_patch_execute(builder)
}
