//! Auto-generated API clients for gcp/androidpublisher.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET androidpublisher/v3/applications/{packageName}/dataSafety
/// Writes the Safety Labels declaration of an app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_applications_data_safety_execute()` to send, or `androidpublisher_applications_data_safety` for simplest API.

pub fn androidpublisher_applications_data_safety_builder(
    client: &SimpleHttpClient,
    packageName: String,
    body: &SafetyLabelsUpdateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/dataSafety",
        packageName.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/dataSafety
/// Writes the Safety Labels declaration of an app.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_applications_data_safety_execute()` or `androidpublisher_applications_data_safety`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_applications_data_safety_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_data_safety_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SafetyLabelsUpdateResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SafetyLabelsUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/dataSafety
/// Writes the Safety Labels declaration of an app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_applications_data_safety_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_applications_data_safety_task()`.
/// For the simplest API, use `androidpublisher_applications_data_safety()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_applications_data_safety_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_applications_data_safety_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SafetyLabelsUpdateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_applications_data_safety_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_applications_data_safety`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherApplicationsDataSafetyArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Request body.
    pub body: SafetyLabelsUpdateRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/dataSafety
/// Writes the Safety Labels declaration of an app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_applications_data_safety_builder()` + `androidpublisher_applications_data_safety_execute()`.
/// For task-level control, use `androidpublisher_applications_data_safety_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_data_safety(
    client: &SimpleHttpClient,
    args: &AndroidpublisherApplicationsDataSafetyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SafetyLabelsUpdateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_applications_data_safety_builder(
        client,
        args.packageName.clone(),
        &args.body,
    )?;
    androidpublisher_applications_data_safety_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs
/// Creates a new device tier config for an app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_applications_device_tier_configs_create_execute()` to send, or `androidpublisher_applications_device_tier_configs_create` for simplest API.

pub fn androidpublisher_applications_device_tier_configs_create_builder(
    client: &SimpleHttpClient,
    packageName: String,
    allowUnknownDevices: Option<bool>,
    body: &DeviceTierConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/deviceTierConfigs",
        packageName.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowUnknownDevices {
        query_parts.push(format!("allowUnknownDevices={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs
/// Creates a new device tier config for an app.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_applications_device_tier_configs_create_execute()` or `androidpublisher_applications_device_tier_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_applications_device_tier_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_device_tier_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeviceTierConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeviceTierConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs
/// Creates a new device tier config for an app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_applications_device_tier_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_applications_device_tier_configs_create_task()`.
/// For the simplest API, use `androidpublisher_applications_device_tier_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_applications_device_tier_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_applications_device_tier_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceTierConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_applications_device_tier_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_applications_device_tier_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherApplicationsDeviceTierConfigsCreateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Query parameter: allowUnknownDevices
    pub allowUnknownDevices: Option<bool>,
    /// Request body.
    pub body: DeviceTierConfig,
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs
/// Creates a new device tier config for an app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_applications_device_tier_configs_create_builder()` + `androidpublisher_applications_device_tier_configs_create_execute()`.
/// For task-level control, use `androidpublisher_applications_device_tier_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_device_tier_configs_create(
    client: &SimpleHttpClient,
    args: &AndroidpublisherApplicationsDeviceTierConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceTierConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_applications_device_tier_configs_create_builder(
        client,
        args.packageName.clone(),
        args.allowUnknownDevices.clone(),
        &args.body,
    )?;
    androidpublisher_applications_device_tier_configs_create_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs/{deviceTierConfigId}
/// Returns a particular device tier config.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_applications_device_tier_configs_get_execute()` to send, or `androidpublisher_applications_device_tier_configs_get` for simplest API.

pub fn androidpublisher_applications_device_tier_configs_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    deviceTierConfigId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/deviceTierConfigs/{}",
        packageName.as_str(),
        deviceTierConfigId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs/{deviceTierConfigId}
/// Returns a particular device tier config.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_applications_device_tier_configs_get_execute()` or `androidpublisher_applications_device_tier_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_applications_device_tier_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_device_tier_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeviceTierConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeviceTierConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs/{deviceTierConfigId}
/// Returns a particular device tier config.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_applications_device_tier_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_applications_device_tier_configs_get_task()`.
/// For the simplest API, use `androidpublisher_applications_device_tier_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_applications_device_tier_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_applications_device_tier_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceTierConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_applications_device_tier_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_applications_device_tier_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherApplicationsDeviceTierConfigsGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: deviceTierConfigId
    pub deviceTierConfigId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/deviceTierConfigs/{deviceTierConfigId}
/// Returns a particular device tier config.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_applications_device_tier_configs_get_builder()` + `androidpublisher_applications_device_tier_configs_get_execute()`.
/// For task-level control, use `androidpublisher_applications_device_tier_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_applications_device_tier_configs_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherApplicationsDeviceTierConfigsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeviceTierConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_applications_device_tier_configs_get_builder(
        client,
        args.packageName.clone(),
        args.deviceTierConfigId.clone(),
    )?;
    androidpublisher_applications_device_tier_configs_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:addTargeting
/// Incrementally update targeting for a recovery action. Note that only the criteria selected during the creation of recovery action can be expanded.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_apprecovery_add_targeting_execute()` to send, or `androidpublisher_apprecovery_add_targeting` for simplest API.

pub fn androidpublisher_apprecovery_add_targeting_builder(
    client: &SimpleHttpClient,
    packageName: String,
    appRecoveryId: String,
    body: &AddTargetingRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/appRecoveries/{}:addTargeting",
        packageName.as_str(),
        appRecoveryId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:addTargeting
/// Incrementally update targeting for a recovery action. Note that only the criteria selected during the creation of recovery action can be expanded.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_apprecovery_add_targeting_execute()` or `androidpublisher_apprecovery_add_targeting`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_apprecovery_add_targeting_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_add_targeting_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddTargetingResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AddTargetingResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:addTargeting
/// Incrementally update targeting for a recovery action. Note that only the criteria selected during the creation of recovery action can be expanded.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_apprecovery_add_targeting_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_apprecovery_add_targeting_task()`.
/// For the simplest API, use `androidpublisher_apprecovery_add_targeting()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_apprecovery_add_targeting_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_apprecovery_add_targeting_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddTargetingResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_apprecovery_add_targeting_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_apprecovery_add_targeting`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherApprecoveryAddTargetingArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: appRecoveryId
    pub appRecoveryId: String,
    /// Request body.
    pub body: AddTargetingRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:addTargeting
/// Incrementally update targeting for a recovery action. Note that only the criteria selected during the creation of recovery action can be expanded.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_apprecovery_add_targeting_builder()` + `androidpublisher_apprecovery_add_targeting_execute()`.
/// For task-level control, use `androidpublisher_apprecovery_add_targeting_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_add_targeting(
    client: &SimpleHttpClient,
    args: &AndroidpublisherApprecoveryAddTargetingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AddTargetingResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_apprecovery_add_targeting_builder(
        client,
        args.packageName.clone(),
        args.appRecoveryId.clone(),
        &args.body,
    )?;
    androidpublisher_apprecovery_add_targeting_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:cancel
/// Cancel an already executing app recovery action. Note that this action changes status of the recovery action to CANCELED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_apprecovery_cancel_execute()` to send, or `androidpublisher_apprecovery_cancel` for simplest API.

pub fn androidpublisher_apprecovery_cancel_builder(
    client: &SimpleHttpClient,
    packageName: String,
    appRecoveryId: String,
    body: &CancelAppRecoveryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/appRecoveries/{}:cancel",
        packageName.as_str(),
        appRecoveryId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:cancel
/// Cancel an already executing app recovery action. Note that this action changes status of the recovery action to CANCELED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_apprecovery_cancel_execute()` or `androidpublisher_apprecovery_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_apprecovery_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CancelAppRecoveryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CancelAppRecoveryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:cancel
/// Cancel an already executing app recovery action. Note that this action changes status of the recovery action to CANCELED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_apprecovery_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_apprecovery_cancel_task()`.
/// For the simplest API, use `androidpublisher_apprecovery_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_apprecovery_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_apprecovery_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CancelAppRecoveryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_apprecovery_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_apprecovery_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherApprecoveryCancelArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: appRecoveryId
    pub appRecoveryId: String,
    /// Request body.
    pub body: CancelAppRecoveryRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:cancel
/// Cancel an already executing app recovery action. Note that this action changes status of the recovery action to CANCELED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_apprecovery_cancel_builder()` + `androidpublisher_apprecovery_cancel_execute()`.
/// For task-level control, use `androidpublisher_apprecovery_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_cancel(
    client: &SimpleHttpClient,
    args: &AndroidpublisherApprecoveryCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CancelAppRecoveryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_apprecovery_cancel_builder(
        client,
        args.packageName.clone(),
        args.appRecoveryId.clone(),
        &args.body,
    )?;
    androidpublisher_apprecovery_cancel_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries
/// Create an app recovery action with recovery status as DRAFT. Note that this action does not execute the recovery action.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_apprecovery_create_execute()` to send, or `androidpublisher_apprecovery_create` for simplest API.

pub fn androidpublisher_apprecovery_create_builder(
    client: &SimpleHttpClient,
    packageName: String,
    body: &CreateDraftAppRecoveryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/appRecoveries",
        packageName.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries
/// Create an app recovery action with recovery status as DRAFT. Note that this action does not execute the recovery action.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_apprecovery_create_execute()` or `androidpublisher_apprecovery_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_apprecovery_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AppRecoveryAction>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppRecoveryAction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries
/// Create an app recovery action with recovery status as DRAFT. Note that this action does not execute the recovery action.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_apprecovery_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_apprecovery_create_task()`.
/// For the simplest API, use `androidpublisher_apprecovery_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_apprecovery_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_apprecovery_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppRecoveryAction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_apprecovery_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_apprecovery_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherApprecoveryCreateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Request body.
    pub body: CreateDraftAppRecoveryRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries
/// Create an app recovery action with recovery status as DRAFT. Note that this action does not execute the recovery action.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_apprecovery_create_builder()` + `androidpublisher_apprecovery_create_execute()`.
/// For task-level control, use `androidpublisher_apprecovery_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_create(
    client: &SimpleHttpClient,
    args: &AndroidpublisherApprecoveryCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppRecoveryAction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_apprecovery_create_builder(client, args.packageName.clone(), &args.body)?;
    androidpublisher_apprecovery_create_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:deploy
/// Deploy an already created app recovery action with recovery status DRAFT. Note that this action activates the recovery action for all targeted users and changes its status to `ACTIVE`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_apprecovery_deploy_execute()` to send, or `androidpublisher_apprecovery_deploy` for simplest API.

pub fn androidpublisher_apprecovery_deploy_builder(
    client: &SimpleHttpClient,
    packageName: String,
    appRecoveryId: String,
    body: &DeployAppRecoveryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/appRecoveries/{}:deploy",
        packageName.as_str(),
        appRecoveryId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:deploy
/// Deploy an already created app recovery action with recovery status DRAFT. Note that this action activates the recovery action for all targeted users and changes its status to `ACTIVE`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_apprecovery_deploy_execute()` or `androidpublisher_apprecovery_deploy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_apprecovery_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_deploy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeployAppRecoveryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeployAppRecoveryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:deploy
/// Deploy an already created app recovery action with recovery status DRAFT. Note that this action activates the recovery action for all targeted users and changes its status to `ACTIVE`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_apprecovery_deploy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_apprecovery_deploy_task()`.
/// For the simplest API, use `androidpublisher_apprecovery_deploy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_apprecovery_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_apprecovery_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeployAppRecoveryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_apprecovery_deploy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_apprecovery_deploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherApprecoveryDeployArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: appRecoveryId
    pub appRecoveryId: String,
    /// Request body.
    pub body: DeployAppRecoveryRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/appRecoveries/{appRecoveryId}:deploy
/// Deploy an already created app recovery action with recovery status DRAFT. Note that this action activates the recovery action for all targeted users and changes its status to `ACTIVE`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_apprecovery_deploy_builder()` + `androidpublisher_apprecovery_deploy_execute()`.
/// For task-level control, use `androidpublisher_apprecovery_deploy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_apprecovery_deploy(
    client: &SimpleHttpClient,
    args: &AndroidpublisherApprecoveryDeployArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeployAppRecoveryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_apprecovery_deploy_builder(
        client,
        args.packageName.clone(),
        args.appRecoveryId.clone(),
        &args.body,
    )?;
    androidpublisher_apprecovery_deploy_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}:commit
/// Commits an app edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_commit_execute()` to send, or `androidpublisher_edits_commit` for simplest API.

pub fn androidpublisher_edits_commit_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
    changesInReviewBehavior: Option<String>,
    changesNotSentForReview: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}:commit",
        packageName.as_str(),
        editId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = changesInReviewBehavior {
        query_parts.push(format!("changesInReviewBehavior={}", val));
    }
    if let Some(val) = changesNotSentForReview {
        query_parts.push(format!("changesNotSentForReview={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}:commit
/// Commits an app edit.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_commit_execute()` or `androidpublisher_edits_commit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_commit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_commit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AppEdit>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppEdit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}:commit
/// Commits an app edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_commit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_commit_task()`.
/// For the simplest API, use `androidpublisher_edits_commit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_commit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_commit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppEdit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_commit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_commit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsCommitArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
    /// Query parameter: changesInReviewBehavior
    pub changesInReviewBehavior: Option<String>,
    /// Query parameter: changesNotSentForReview
    pub changesNotSentForReview: Option<bool>,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}:commit
/// Commits an app edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_commit_builder()` + `androidpublisher_edits_commit_execute()`.
/// For task-level control, use `androidpublisher_edits_commit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_commit(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsCommitArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppEdit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_commit_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
        args.changesInReviewBehavior.clone(),
        args.changesNotSentForReview.clone(),
    )?;
    androidpublisher_edits_commit_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}
/// Deletes an app edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_delete_execute()` to send, or `androidpublisher_edits_delete` for simplest API.

pub fn androidpublisher_edits_delete_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}",
        packageName.as_str(),
        editId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}
/// Deletes an app edit.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_delete_execute()` or `androidpublisher_edits_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}
/// Deletes an app edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_delete_task()`.
/// For the simplest API, use `androidpublisher_edits_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsDeleteArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}
/// Deletes an app edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_delete_builder()` + `androidpublisher_edits_delete_execute()`.
/// For task-level control, use `androidpublisher_edits_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_delete(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_delete_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
    )?;
    androidpublisher_edits_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits
/// Creates a new edit for an app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_insert_execute()` to send, or `androidpublisher_edits_insert` for simplest API.

pub fn androidpublisher_edits_insert_builder(
    client: &SimpleHttpClient,
    packageName: String,
    body: &AppEdit,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits",
        packageName.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits
/// Creates a new edit for an app.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_insert_execute()` or `androidpublisher_edits_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AppEdit>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppEdit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits
/// Creates a new edit for an app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_insert_task()`.
/// For the simplest API, use `androidpublisher_edits_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppEdit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsInsertArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Request body.
    pub body: AppEdit,
}

/// GET androidpublisher/v3/applications/{packageName}/edits
/// Creates a new edit for an app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_insert_builder()` + `androidpublisher_edits_insert_execute()`.
/// For task-level control, use `androidpublisher_edits_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_insert(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppEdit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_edits_insert_builder(client, args.packageName.clone(), &args.body)?;
    androidpublisher_edits_insert_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}:validate
/// Validates an app edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_validate_execute()` to send, or `androidpublisher_edits_validate` for simplest API.

pub fn androidpublisher_edits_validate_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}:validate",
        packageName.as_str(),
        editId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}:validate
/// Validates an app edit.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_validate_execute()` or `androidpublisher_edits_validate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_validate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AppEdit>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppEdit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}:validate
/// Validates an app edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_validate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_validate_task()`.
/// For the simplest API, use `androidpublisher_edits_validate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppEdit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_validate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_validate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsValidateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}:validate
/// Validates an app edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_validate_builder()` + `androidpublisher_edits_validate_execute()`.
/// For task-level control, use `androidpublisher_edits_validate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_validate(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsValidateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppEdit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_validate_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
    )?;
    androidpublisher_edits_validate_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/externallyHosted
/// Creates a new APK without uploading the APK itself to Google Play, instead hosting the APK at a specified URL. This function is only available to organizations using Managed Play whose application is configured to restrict distribution to the organizations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_apks_addexternallyhosted_execute()` to send, or `androidpublisher_edits_apks_addexternallyhosted` for simplest API.

pub fn androidpublisher_edits_apks_addexternallyhosted_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
    body: &ApksAddExternallyHostedRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}/apks/externallyHosted",
        packageName.as_str(),
        editId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/externallyHosted
/// Creates a new APK without uploading the APK itself to Google Play, instead hosting the APK at a specified URL. This function is only available to organizations using Managed Play whose application is configured to restrict distribution to the organizations.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_apks_addexternallyhosted_execute()` or `androidpublisher_edits_apks_addexternallyhosted`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_apks_addexternallyhosted_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_apks_addexternallyhosted_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApksAddExternallyHostedResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApksAddExternallyHostedResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/externallyHosted
/// Creates a new APK without uploading the APK itself to Google Play, instead hosting the APK at a specified URL. This function is only available to organizations using Managed Play whose application is configured to restrict distribution to the organizations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_apks_addexternallyhosted_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_apks_addexternallyhosted_task()`.
/// For the simplest API, use `androidpublisher_edits_apks_addexternallyhosted()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_apks_addexternallyhosted_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_apks_addexternallyhosted_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ApksAddExternallyHostedResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_apks_addexternallyhosted_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_apks_addexternallyhosted`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsApksAddexternallyhostedArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
    /// Request body.
    pub body: ApksAddExternallyHostedRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/externallyHosted
/// Creates a new APK without uploading the APK itself to Google Play, instead hosting the APK at a specified URL. This function is only available to organizations using Managed Play whose application is configured to restrict distribution to the organizations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_apks_addexternallyhosted_builder()` + `androidpublisher_edits_apks_addexternallyhosted_execute()`.
/// For task-level control, use `androidpublisher_edits_apks_addexternallyhosted_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_apks_addexternallyhosted(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsApksAddexternallyhostedArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ApksAddExternallyHostedResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_apks_addexternallyhosted_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
        &args.body,
    )?;
    androidpublisher_edits_apks_addexternallyhosted_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks
/// Lists all current APKs of the app and edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_apks_list_execute()` to send, or `androidpublisher_edits_apks_list` for simplest API.

pub fn androidpublisher_edits_apks_list_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}/apks",
        packageName.as_str(),
        editId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks
/// Lists all current APKs of the app and edit.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_apks_list_execute()` or `androidpublisher_edits_apks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_apks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_apks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApksListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ApksListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks
/// Lists all current APKs of the app and edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_apks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_apks_list_task()`.
/// For the simplest API, use `androidpublisher_edits_apks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_apks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_apks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApksListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_apks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_apks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsApksListArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks
/// Lists all current APKs of the app and edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_apks_list_builder()` + `androidpublisher_edits_apks_list_execute()`.
/// For task-level control, use `androidpublisher_edits_apks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_apks_list(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsApksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ApksListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_apks_list_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
    )?;
    androidpublisher_edits_apks_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/bundles
/// Lists all current Android App Bundles of the app and edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_bundles_list_execute()` to send, or `androidpublisher_edits_bundles_list` for simplest API.

pub fn androidpublisher_edits_bundles_list_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}/bundles",
        packageName.as_str(),
        editId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/bundles
/// Lists all current Android App Bundles of the app and edit.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_bundles_list_execute()` or `androidpublisher_edits_bundles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_bundles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_bundles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BundlesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BundlesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/bundles
/// Lists all current Android App Bundles of the app and edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_bundles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_bundles_list_task()`.
/// For the simplest API, use `androidpublisher_edits_bundles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_bundles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_bundles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BundlesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_bundles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_bundles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsBundlesListArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/bundles
/// Lists all current Android App Bundles of the app and edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_bundles_list_builder()` + `androidpublisher_edits_bundles_list_execute()`.
/// For task-level control, use `androidpublisher_edits_bundles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_bundles_list(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsBundlesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BundlesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_bundles_list_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
    )?;
    androidpublisher_edits_bundles_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/countryAvailability/{track}
/// Gets country availability.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_countryavailability_get_execute()` to send, or `androidpublisher_edits_countryavailability_get` for simplest API.

pub fn androidpublisher_edits_countryavailability_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
    track: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}/countryAvailability/{}",
        packageName.as_str(),
        editId.as_str(),
        track.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/countryAvailability/{track}
/// Gets country availability.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_countryavailability_get_execute()` or `androidpublisher_edits_countryavailability_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_countryavailability_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_countryavailability_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TrackCountryAvailability>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TrackCountryAvailability = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/countryAvailability/{track}
/// Gets country availability.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_countryavailability_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_countryavailability_get_task()`.
/// For the simplest API, use `androidpublisher_edits_countryavailability_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_countryavailability_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_countryavailability_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TrackCountryAvailability>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_countryavailability_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_countryavailability_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsCountryavailabilityGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
    /// Path parameter: track
    pub track: String,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/countryAvailability/{track}
/// Gets country availability.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_countryavailability_get_builder()` + `androidpublisher_edits_countryavailability_get_execute()`.
/// For task-level control, use `androidpublisher_edits_countryavailability_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_countryavailability_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsCountryavailabilityGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TrackCountryAvailability>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_countryavailability_get_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
        args.track.clone(),
    )?;
    androidpublisher_edits_countryavailability_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/deobfuscationFiles/{deobfuscationFileType}
/// Uploads a new deobfuscation file and attaches to the specified APK.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_deobfuscationfiles_upload_execute()` to send, or `androidpublisher_edits_deobfuscationfiles_upload` for simplest API.

pub fn androidpublisher_edits_deobfuscationfiles_upload_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
    apkVersionCode: String,
    deobfuscationFileType: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}/apks/{}/deobfuscationFiles/{}",
        packageName.as_str(),
        editId.as_str(),
        apkVersionCode.as_str(),
        deobfuscationFileType.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/deobfuscationFiles/{deobfuscationFileType}
/// Uploads a new deobfuscation file and attaches to the specified APK.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_deobfuscationfiles_upload_execute()` or `androidpublisher_edits_deobfuscationfiles_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_deobfuscationfiles_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_deobfuscationfiles_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeobfuscationFilesUploadResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeobfuscationFilesUploadResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/deobfuscationFiles/{deobfuscationFileType}
/// Uploads a new deobfuscation file and attaches to the specified APK.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_deobfuscationfiles_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_deobfuscationfiles_upload_task()`.
/// For the simplest API, use `androidpublisher_edits_deobfuscationfiles_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_deobfuscationfiles_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_deobfuscationfiles_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeobfuscationFilesUploadResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_deobfuscationfiles_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_deobfuscationfiles_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsDeobfuscationfilesUploadArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
    /// Path parameter: apkVersionCode
    pub apkVersionCode: String,
    /// Path parameter: deobfuscationFileType
    pub deobfuscationFileType: String,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/deobfuscationFiles/{deobfuscationFileType}
/// Uploads a new deobfuscation file and attaches to the specified APK.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_deobfuscationfiles_upload_builder()` + `androidpublisher_edits_deobfuscationfiles_upload_execute()`.
/// For task-level control, use `androidpublisher_edits_deobfuscationfiles_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_deobfuscationfiles_upload(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsDeobfuscationfilesUploadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeobfuscationFilesUploadResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_deobfuscationfiles_upload_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
        args.apkVersionCode.clone(),
        args.deobfuscationFileType.clone(),
    )?;
    androidpublisher_edits_deobfuscationfiles_upload_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/details
/// Gets details of an app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_details_get_execute()` to send, or `androidpublisher_edits_details_get` for simplest API.

pub fn androidpublisher_edits_details_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}/details",
        packageName.as_str(),
        editId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/details
/// Gets details of an app.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_details_get_execute()` or `androidpublisher_edits_details_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_details_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_details_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AppDetails>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppDetails = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/details
/// Gets details of an app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_details_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_details_get_task()`.
/// For the simplest API, use `androidpublisher_edits_details_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_details_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_details_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppDetails>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_details_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_details_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsDetailsGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/details
/// Gets details of an app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_details_get_builder()` + `androidpublisher_edits_details_get_execute()`.
/// For task-level control, use `androidpublisher_edits_details_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_details_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsDetailsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppDetails>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_details_get_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
    )?;
    androidpublisher_edits_details_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Fetches the expansion file configuration for the specified APK.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_expansionfiles_get_execute()` to send, or `androidpublisher_edits_expansionfiles_get` for simplest API.

pub fn androidpublisher_edits_expansionfiles_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
    apkVersionCode: String,
    expansionFileType: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}/apks/{}/expansionFiles/{}",
        packageName.as_str(),
        editId.as_str(),
        apkVersionCode.as_str(),
        expansionFileType.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Fetches the expansion file configuration for the specified APK.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_expansionfiles_get_execute()` or `androidpublisher_edits_expansionfiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_expansionfiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_expansionfiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExpansionFile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExpansionFile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Fetches the expansion file configuration for the specified APK.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_expansionfiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_expansionfiles_get_task()`.
/// For the simplest API, use `androidpublisher_edits_expansionfiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_expansionfiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_expansionfiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExpansionFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_expansionfiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_expansionfiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsExpansionfilesGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
    /// Path parameter: apkVersionCode
    pub apkVersionCode: String,
    /// Path parameter: expansionFileType
    pub expansionFileType: String,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/apks/{apkVersionCode}/expansionFiles/{expansionFileType}
/// Fetches the expansion file configuration for the specified APK.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_expansionfiles_get_builder()` + `androidpublisher_edits_expansionfiles_get_execute()`.
/// For task-level control, use `androidpublisher_edits_expansionfiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_expansionfiles_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsExpansionfilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExpansionFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_expansionfiles_get_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
        args.apkVersionCode.clone(),
        args.expansionFileType.clone(),
    )?;
    androidpublisher_edits_expansionfiles_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}/{imageId}
/// Deletes the image (specified by id) from the edit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_images_delete_execute()` to send, or `androidpublisher_edits_images_delete` for simplest API.

pub fn androidpublisher_edits_images_delete_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
    language: String,
    imageType: String,
    imageId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}/listings/{}/{}/{}",
        packageName.as_str(),
        editId.as_str(),
        language.as_str(),
        imageType.as_str(),
        imageId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}/{imageId}
/// Deletes the image (specified by id) from the edit.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_images_delete_execute()` or `androidpublisher_edits_images_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_images_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_images_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}/{imageId}
/// Deletes the image (specified by id) from the edit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_images_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_images_delete_task()`.
/// For the simplest API, use `androidpublisher_edits_images_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_images_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_images_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_images_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_images_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsImagesDeleteArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
    /// Path parameter: language
    pub language: String,
    /// Path parameter: imageType
    pub imageType: String,
    /// Path parameter: imageId
    pub imageId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}/{imageId}
/// Deletes the image (specified by id) from the edit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_images_delete_builder()` + `androidpublisher_edits_images_delete_execute()`.
/// For task-level control, use `androidpublisher_edits_images_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_images_delete(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsImagesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_images_delete_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
        args.language.clone(),
        args.imageType.clone(),
        args.imageId.clone(),
    )?;
    androidpublisher_edits_images_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}
/// Deletes all images for the specified language and image type. Returns an empty response if no images are found.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_images_deleteall_execute()` to send, or `androidpublisher_edits_images_deleteall` for simplest API.

pub fn androidpublisher_edits_images_deleteall_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
    language: String,
    imageType: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}/listings/{}/{}",
        packageName.as_str(),
        editId.as_str(),
        language.as_str(),
        imageType.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}
/// Deletes all images for the specified language and image type. Returns an empty response if no images are found.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_images_deleteall_execute()` or `androidpublisher_edits_images_deleteall`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_images_deleteall_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_images_deleteall_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ImagesDeleteAllResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImagesDeleteAllResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}
/// Deletes all images for the specified language and image type. Returns an empty response if no images are found.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_images_deleteall_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_images_deleteall_task()`.
/// For the simplest API, use `androidpublisher_edits_images_deleteall()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_images_deleteall_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_images_deleteall_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImagesDeleteAllResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_images_deleteall_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_images_deleteall`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsImagesDeleteallArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
    /// Path parameter: language
    pub language: String,
    /// Path parameter: imageType
    pub imageType: String,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}/{imageType}
/// Deletes all images for the specified language and image type. Returns an empty response if no images are found.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_images_deleteall_builder()` + `androidpublisher_edits_images_deleteall_execute()`.
/// For task-level control, use `androidpublisher_edits_images_deleteall_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_images_deleteall(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsImagesDeleteallArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ImagesDeleteAllResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_images_deleteall_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
        args.language.clone(),
        args.imageType.clone(),
    )?;
    androidpublisher_edits_images_deleteall_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Deletes a localized store listing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_listings_delete_execute()` to send, or `androidpublisher_edits_listings_delete` for simplest API.

pub fn androidpublisher_edits_listings_delete_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
    language: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}/listings/{}",
        packageName.as_str(),
        editId.as_str(),
        language.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Deletes a localized store listing.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_listings_delete_execute()` or `androidpublisher_edits_listings_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_listings_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Deletes a localized store listing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_listings_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_listings_delete_task()`.
/// For the simplest API, use `androidpublisher_edits_listings_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_listings_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_listings_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_listings_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_listings_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsListingsDeleteArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
    /// Path parameter: language
    pub language: String,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings/{language}
/// Deletes a localized store listing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_listings_delete_builder()` + `androidpublisher_edits_listings_delete_execute()`.
/// For task-level control, use `androidpublisher_edits_listings_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_delete(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsListingsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_listings_delete_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
        args.language.clone(),
    )?;
    androidpublisher_edits_listings_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings
/// Deletes all store listings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_listings_deleteall_execute()` to send, or `androidpublisher_edits_listings_deleteall` for simplest API.

pub fn androidpublisher_edits_listings_deleteall_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}/listings",
        packageName.as_str(),
        editId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings
/// Deletes all store listings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_listings_deleteall_execute()` or `androidpublisher_edits_listings_deleteall`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_listings_deleteall_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_deleteall_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings
/// Deletes all store listings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_listings_deleteall_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_listings_deleteall_task()`.
/// For the simplest API, use `androidpublisher_edits_listings_deleteall()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_listings_deleteall_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_listings_deleteall_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_listings_deleteall_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_listings_deleteall`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsListingsDeleteallArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/listings
/// Deletes all store listings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_listings_deleteall_builder()` + `androidpublisher_edits_listings_deleteall_execute()`.
/// For task-level control, use `androidpublisher_edits_listings_deleteall_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_listings_deleteall(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsListingsDeleteallArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_listings_deleteall_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
    )?;
    androidpublisher_edits_listings_deleteall_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/testers/{track}
/// Gets testers. Note: Testers resource does not support email lists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_testers_get_execute()` to send, or `androidpublisher_edits_testers_get` for simplest API.

pub fn androidpublisher_edits_testers_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
    track: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}/testers/{}",
        packageName.as_str(),
        editId.as_str(),
        track.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/testers/{track}
/// Gets testers. Note: Testers resource does not support email lists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_testers_get_execute()` or `androidpublisher_edits_testers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_testers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_testers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Testers>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Testers = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/testers/{track}
/// Gets testers. Note: Testers resource does not support email lists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_testers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_testers_get_task()`.
/// For the simplest API, use `androidpublisher_edits_testers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_testers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_testers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Testers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_testers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_testers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsTestersGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
    /// Path parameter: track
    pub track: String,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/testers/{track}
/// Gets testers. Note: Testers resource does not support email lists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_testers_get_builder()` + `androidpublisher_edits_testers_get_execute()`.
/// For task-level control, use `androidpublisher_edits_testers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_testers_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsTestersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Testers>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_testers_get_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
        args.track.clone(),
    )?;
    androidpublisher_edits_testers_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks
/// Creates a new track.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_tracks_create_execute()` to send, or `androidpublisher_edits_tracks_create` for simplest API.

pub fn androidpublisher_edits_tracks_create_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
    body: &TrackConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}/tracks",
        packageName.as_str(),
        editId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks
/// Creates a new track.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_tracks_create_execute()` or `androidpublisher_edits_tracks_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_tracks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_tracks_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Track>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Track = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks
/// Creates a new track.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_tracks_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_tracks_create_task()`.
/// For the simplest API, use `androidpublisher_edits_tracks_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_tracks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_tracks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Track>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_tracks_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_tracks_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsTracksCreateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
    /// Request body.
    pub body: TrackConfig,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks
/// Creates a new track.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_tracks_create_builder()` + `androidpublisher_edits_tracks_create_execute()`.
/// For task-level control, use `androidpublisher_edits_tracks_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_tracks_create(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsTracksCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Track>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_tracks_create_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
        &args.body,
    )?;
    androidpublisher_edits_tracks_create_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks/{track}
/// Gets a track.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_edits_tracks_get_execute()` to send, or `androidpublisher_edits_tracks_get` for simplest API.

pub fn androidpublisher_edits_tracks_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    editId: String,
    track: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/edits/{}/tracks/{}",
        packageName.as_str(),
        editId.as_str(),
        track.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks/{track}
/// Gets a track.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_edits_tracks_get_execute()` or `androidpublisher_edits_tracks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_tracks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_tracks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Track>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Track = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks/{track}
/// Gets a track.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_edits_tracks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_edits_tracks_get_task()`.
/// For the simplest API, use `androidpublisher_edits_tracks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_edits_tracks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_edits_tracks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Track>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_edits_tracks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_edits_tracks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherEditsTracksGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: editId
    pub editId: String,
    /// Path parameter: track
    pub track: String,
}

/// GET androidpublisher/v3/applications/{packageName}/edits/{editId}/tracks/{track}
/// Gets a track.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_edits_tracks_get_builder()` + `androidpublisher_edits_tracks_get_execute()`.
/// For task-level control, use `androidpublisher_edits_tracks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_edits_tracks_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherEditsTracksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Track>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_edits_tracks_get_builder(
        client,
        args.packageName.clone(),
        args.editId.clone(),
        args.track.clone(),
    )?;
    androidpublisher_edits_tracks_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{applicationsId}/externalTransactions
/// Creates a new external transaction.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_externaltransactions_createexternaltransaction_execute()` to send, or `androidpublisher_externaltransactions_createexternaltransaction` for simplest API.

pub fn androidpublisher_externaltransactions_createexternaltransaction_builder(
    client: &SimpleHttpClient,
    parent: String,
    externalTransactionId: Option<String>,
    body: &ExternalTransaction,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/externalTransactions",
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = externalTransactionId {
        query_parts.push(format!("externalTransactionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{applicationsId}/externalTransactions
/// Creates a new external transaction.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_externaltransactions_createexternaltransaction_execute()` or `androidpublisher_externaltransactions_createexternaltransaction`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_externaltransactions_createexternaltransaction_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_externaltransactions_createexternaltransaction_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExternalTransaction>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ExternalTransaction = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{applicationsId}/externalTransactions
/// Creates a new external transaction.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_externaltransactions_createexternaltransaction_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_externaltransactions_createexternaltransaction_task()`.
/// For the simplest API, use `androidpublisher_externaltransactions_createexternaltransaction()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_externaltransactions_createexternaltransaction_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_externaltransactions_createexternaltransaction_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_externaltransactions_createexternaltransaction_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_externaltransactions_createexternaltransaction`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherExternaltransactionsCreateexternaltransactionArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: externalTransactionId
    pub externalTransactionId: Option<String>,
    /// Request body.
    pub body: ExternalTransaction,
}

/// GET androidpublisher/v3/applications/{applicationsId}/externalTransactions
/// Creates a new external transaction.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_externaltransactions_createexternaltransaction_builder()` + `androidpublisher_externaltransactions_createexternaltransaction_execute()`.
/// For task-level control, use `androidpublisher_externaltransactions_createexternaltransaction_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_externaltransactions_createexternaltransaction(
    client: &SimpleHttpClient,
    args: &AndroidpublisherExternaltransactionsCreateexternaltransactionArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ExternalTransaction>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_externaltransactions_createexternaltransaction_builder(
        client,
        args.parent.clone(),
        args.externalTransactionId.clone(),
        &args.body,
    )?;
    androidpublisher_externaltransactions_createexternaltransaction_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/generatedApks/{versionCode}/downloads/{downloadId}:download
/// Downloads a single signed APK generated from an app bundle.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_generatedapks_download_execute()` to send, or `androidpublisher_generatedapks_download` for simplest API.

pub fn androidpublisher_generatedapks_download_builder(
    client: &SimpleHttpClient,
    packageName: String,
    versionCode: String,
    downloadId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/generatedApks/{}/downloads/{}:download",
        packageName.as_str(),
        versionCode.as_str(),
        downloadId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/generatedApks/{versionCode}/downloads/{downloadId}:download
/// Downloads a single signed APK generated from an app bundle.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_generatedapks_download_execute()` or `androidpublisher_generatedapks_download`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_generatedapks_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_generatedapks_download_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/generatedApks/{versionCode}/downloads/{downloadId}:download
/// Downloads a single signed APK generated from an app bundle.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_generatedapks_download_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_generatedapks_download_task()`.
/// For the simplest API, use `androidpublisher_generatedapks_download()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_generatedapks_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_generatedapks_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_generatedapks_download_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_generatedapks_download`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherGeneratedapksDownloadArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: versionCode
    pub versionCode: String,
    /// Path parameter: downloadId
    pub downloadId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/generatedApks/{versionCode}/downloads/{downloadId}:download
/// Downloads a single signed APK generated from an app bundle.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_generatedapks_download_builder()` + `androidpublisher_generatedapks_download_execute()`.
/// For task-level control, use `androidpublisher_generatedapks_download_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_generatedapks_download(
    client: &SimpleHttpClient,
    args: &AndroidpublisherGeneratedapksDownloadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_generatedapks_download_builder(
        client,
        args.packageName.clone(),
        args.versionCode.clone(),
        args.downloadId.clone(),
    )?;
    androidpublisher_generatedapks_download_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/generatedApks/{versionCode}
/// Returns download metadata for all APKs that were generated from a given app bundle.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_generatedapks_list_execute()` to send, or `androidpublisher_generatedapks_list` for simplest API.

pub fn androidpublisher_generatedapks_list_builder(
    client: &SimpleHttpClient,
    packageName: String,
    versionCode: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/generatedApks/{}",
        packageName.as_str(),
        versionCode.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/generatedApks/{versionCode}
/// Returns download metadata for all APKs that were generated from a given app bundle.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_generatedapks_list_execute()` or `androidpublisher_generatedapks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_generatedapks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_generatedapks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GeneratedApksListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GeneratedApksListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/generatedApks/{versionCode}
/// Returns download metadata for all APKs that were generated from a given app bundle.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_generatedapks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_generatedapks_list_task()`.
/// For the simplest API, use `androidpublisher_generatedapks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_generatedapks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_generatedapks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GeneratedApksListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_generatedapks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_generatedapks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherGeneratedapksListArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: versionCode
    pub versionCode: String,
}

/// GET androidpublisher/v3/applications/{packageName}/generatedApks/{versionCode}
/// Returns download metadata for all APKs that were generated from a given app bundle.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_generatedapks_list_builder()` + `androidpublisher_generatedapks_list_execute()`.
/// For task-level control, use `androidpublisher_generatedapks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_generatedapks_list(
    client: &SimpleHttpClient,
    args: &AndroidpublisherGeneratedapksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GeneratedApksListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_generatedapks_list_builder(
        client,
        args.packageName.clone(),
        args.versionCode.clone(),
    )?;
    androidpublisher_generatedapks_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchDelete
/// Deletes in-app products (managed products or subscriptions). Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput. This method should not be used to delete subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_inappproducts_batch_delete_execute()` to send, or `androidpublisher_inappproducts_batch_delete` for simplest API.

pub fn androidpublisher_inappproducts_batch_delete_builder(
    client: &SimpleHttpClient,
    packageName: String,
    body: &InappproductsBatchDeleteRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/inappproducts:batchDelete",
        packageName.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchDelete
/// Deletes in-app products (managed products or subscriptions). Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput. This method should not be used to delete subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_inappproducts_batch_delete_execute()` or `androidpublisher_inappproducts_batch_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_inappproducts_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_batch_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchDelete
/// Deletes in-app products (managed products or subscriptions). Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput. This method should not be used to delete subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_inappproducts_batch_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_inappproducts_batch_delete_task()`.
/// For the simplest API, use `androidpublisher_inappproducts_batch_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_inappproducts_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_inappproducts_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_inappproducts_batch_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_inappproducts_batch_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherInappproductsBatchDeleteArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Request body.
    pub body: InappproductsBatchDeleteRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchDelete
/// Deletes in-app products (managed products or subscriptions). Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput. This method should not be used to delete subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_inappproducts_batch_delete_builder()` + `androidpublisher_inappproducts_batch_delete_execute()`.
/// For task-level control, use `androidpublisher_inappproducts_batch_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_batch_delete(
    client: &SimpleHttpClient,
    args: &AndroidpublisherInappproductsBatchDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_inappproducts_batch_delete_builder(
        client,
        args.packageName.clone(),
        &args.body,
    )?;
    androidpublisher_inappproducts_batch_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchGet
/// Reads multiple in-app products, which can be managed products or subscriptions. This method should not be used to retrieve subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_inappproducts_batch_get_execute()` to send, or `androidpublisher_inappproducts_batch_get` for simplest API.

pub fn androidpublisher_inappproducts_batch_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    sku: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/inappproducts:batchGet",
        packageName.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sku {
        query_parts.push(format!("sku={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchGet
/// Reads multiple in-app products, which can be managed products or subscriptions. This method should not be used to retrieve subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_inappproducts_batch_get_execute()` or `androidpublisher_inappproducts_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_inappproducts_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InappproductsBatchGetResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InappproductsBatchGetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchGet
/// Reads multiple in-app products, which can be managed products or subscriptions. This method should not be used to retrieve subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_inappproducts_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_inappproducts_batch_get_task()`.
/// For the simplest API, use `androidpublisher_inappproducts_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_inappproducts_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_inappproducts_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InappproductsBatchGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_inappproducts_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_inappproducts_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherInappproductsBatchGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Query parameter: sku
    pub sku: Option<String>,
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchGet
/// Reads multiple in-app products, which can be managed products or subscriptions. This method should not be used to retrieve subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_inappproducts_batch_get_builder()` + `androidpublisher_inappproducts_batch_get_execute()`.
/// For task-level control, use `androidpublisher_inappproducts_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_batch_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherInappproductsBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InappproductsBatchGetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_inappproducts_batch_get_builder(
        client,
        args.packageName.clone(),
        args.sku.clone(),
    )?;
    androidpublisher_inappproducts_batch_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchUpdate
/// Updates or inserts one or more in-app products (managed products or subscriptions). Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput. This method should no longer be used to update subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_inappproducts_batch_update_execute()` to send, or `androidpublisher_inappproducts_batch_update` for simplest API.

pub fn androidpublisher_inappproducts_batch_update_builder(
    client: &SimpleHttpClient,
    packageName: String,
    body: &InappproductsBatchUpdateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/inappproducts:batchUpdate",
        packageName.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchUpdate
/// Updates or inserts one or more in-app products (managed products or subscriptions). Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput. This method should no longer be used to update subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_inappproducts_batch_update_execute()` or `androidpublisher_inappproducts_batch_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_inappproducts_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_batch_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InappproductsBatchUpdateResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InappproductsBatchUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchUpdate
/// Updates or inserts one or more in-app products (managed products or subscriptions). Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput. This method should no longer be used to update subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_inappproducts_batch_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_inappproducts_batch_update_task()`.
/// For the simplest API, use `androidpublisher_inappproducts_batch_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_inappproducts_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_inappproducts_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InappproductsBatchUpdateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_inappproducts_batch_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_inappproducts_batch_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherInappproductsBatchUpdateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Request body.
    pub body: InappproductsBatchUpdateRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts:batchUpdate
/// Updates or inserts one or more in-app products (managed products or subscriptions). Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput. This method should no longer be used to update subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_inappproducts_batch_update_builder()` + `androidpublisher_inappproducts_batch_update_execute()`.
/// For task-level control, use `androidpublisher_inappproducts_batch_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_batch_update(
    client: &SimpleHttpClient,
    args: &AndroidpublisherInappproductsBatchUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InappproductsBatchUpdateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_inappproducts_batch_update_builder(
        client,
        args.packageName.clone(),
        &args.body,
    )?;
    androidpublisher_inappproducts_batch_update_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Deletes an in-app product (a managed product or a subscription). This method should no longer be used to delete subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_inappproducts_delete_execute()` to send, or `androidpublisher_inappproducts_delete` for simplest API.

pub fn androidpublisher_inappproducts_delete_builder(
    client: &SimpleHttpClient,
    packageName: String,
    sku: String,
    latencyTolerance: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/inappproducts/{}",
        packageName.as_str(),
        sku.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = latencyTolerance {
        query_parts.push(format!("latencyTolerance={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Deletes an in-app product (a managed product or a subscription). This method should no longer be used to delete subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_inappproducts_delete_execute()` or `androidpublisher_inappproducts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_inappproducts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Deletes an in-app product (a managed product or a subscription). This method should no longer be used to delete subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_inappproducts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_inappproducts_delete_task()`.
/// For the simplest API, use `androidpublisher_inappproducts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_inappproducts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_inappproducts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_inappproducts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_inappproducts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherInappproductsDeleteArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: sku
    pub sku: String,
    /// Query parameter: latencyTolerance
    pub latencyTolerance: Option<String>,
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts/{sku}
/// Deletes an in-app product (a managed product or a subscription). This method should no longer be used to delete subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_inappproducts_delete_builder()` + `androidpublisher_inappproducts_delete_execute()`.
/// For task-level control, use `androidpublisher_inappproducts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_delete(
    client: &SimpleHttpClient,
    args: &AndroidpublisherInappproductsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_inappproducts_delete_builder(
        client,
        args.packageName.clone(),
        args.sku.clone(),
        args.latencyTolerance.clone(),
    )?;
    androidpublisher_inappproducts_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts
/// Creates an in-app product (a managed product or a subscription). This method should no longer be used to create subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_inappproducts_insert_execute()` to send, or `androidpublisher_inappproducts_insert` for simplest API.

pub fn androidpublisher_inappproducts_insert_builder(
    client: &SimpleHttpClient,
    packageName: String,
    autoConvertMissingPrices: Option<bool>,
    body: &InAppProduct,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/inappproducts",
        packageName.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = autoConvertMissingPrices {
        query_parts.push(format!("autoConvertMissingPrices={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts
/// Creates an in-app product (a managed product or a subscription). This method should no longer be used to create subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_inappproducts_insert_execute()` or `androidpublisher_inappproducts_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_inappproducts_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InAppProduct>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InAppProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts
/// Creates an in-app product (a managed product or a subscription). This method should no longer be used to create subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_inappproducts_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_inappproducts_insert_task()`.
/// For the simplest API, use `androidpublisher_inappproducts_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_inappproducts_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_inappproducts_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InAppProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_inappproducts_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_inappproducts_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherInappproductsInsertArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Query parameter: autoConvertMissingPrices
    pub autoConvertMissingPrices: Option<bool>,
    /// Request body.
    pub body: InAppProduct,
}

/// GET androidpublisher/v3/applications/{packageName}/inappproducts
/// Creates an in-app product (a managed product or a subscription). This method should no longer be used to create subscriptions. See [this article](<https://android-developers.googleblog.`com/2023/06/changes-to-google-play-developer-api-june-2023`.html>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_inappproducts_insert_builder()` + `androidpublisher_inappproducts_insert_execute()`.
/// For task-level control, use `androidpublisher_inappproducts_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_inappproducts_insert(
    client: &SimpleHttpClient,
    args: &AndroidpublisherInappproductsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InAppProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_inappproducts_insert_builder(
        client,
        args.packageName.clone(),
        args.autoConvertMissingPrices.clone(),
        &args.body,
    )?;
    androidpublisher_inappproducts_insert_execute(builder)
}

/// GET androidpublisher/v3/applications/internalappsharing/{packageName}/artifacts/apk
/// Uploads an APK to internal app sharing. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_internalappsharingartifacts_uploadapk_execute()` to send, or `androidpublisher_internalappsharingartifacts_uploadapk` for simplest API.

pub fn androidpublisher_internalappsharingartifacts_uploadapk_builder(
    client: &SimpleHttpClient,
    packageName: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/internalappsharing/{}/artifacts/apk",
        packageName.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/internalappsharing/{packageName}/artifacts/apk
/// Uploads an APK to internal app sharing. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_internalappsharingartifacts_uploadapk_execute()` or `androidpublisher_internalappsharingartifacts_uploadapk`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_internalappsharingartifacts_uploadapk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_internalappsharingartifacts_uploadapk_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InternalAppSharingArtifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InternalAppSharingArtifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/internalappsharing/{packageName}/artifacts/apk
/// Uploads an APK to internal app sharing. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_internalappsharingartifacts_uploadapk_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_internalappsharingartifacts_uploadapk_task()`.
/// For the simplest API, use `androidpublisher_internalappsharingartifacts_uploadapk()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_internalappsharingartifacts_uploadapk_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_internalappsharingartifacts_uploadapk_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InternalAppSharingArtifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_internalappsharingartifacts_uploadapk_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_internalappsharingartifacts_uploadapk`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherInternalappsharingartifactsUploadapkArgs {
    /// Path parameter: packageName
    pub packageName: String,
}

/// GET androidpublisher/v3/applications/internalappsharing/{packageName}/artifacts/apk
/// Uploads an APK to internal app sharing. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_internalappsharingartifacts_uploadapk_builder()` + `androidpublisher_internalappsharingartifacts_uploadapk_execute()`.
/// For task-level control, use `androidpublisher_internalappsharingartifacts_uploadapk_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_internalappsharingartifacts_uploadapk(
    client: &SimpleHttpClient,
    args: &AndroidpublisherInternalappsharingartifactsUploadapkArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InternalAppSharingArtifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_internalappsharingartifacts_uploadapk_builder(
        client,
        args.packageName.clone(),
    )?;
    androidpublisher_internalappsharingartifacts_uploadapk_execute(builder)
}

/// GET androidpublisher/v3/applications/internalappsharing/{packageName}/artifacts/bundle
/// Uploads an app bundle to internal app sharing. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_internalappsharingartifacts_uploadbundle_execute()` to send, or `androidpublisher_internalappsharingartifacts_uploadbundle` for simplest API.

pub fn androidpublisher_internalappsharingartifacts_uploadbundle_builder(
    client: &SimpleHttpClient,
    packageName: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/internalappsharing/{}/artifacts/bundle",
        packageName.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/internalappsharing/{packageName}/artifacts/bundle
/// Uploads an app bundle to internal app sharing. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_internalappsharingartifacts_uploadbundle_execute()` or `androidpublisher_internalappsharingartifacts_uploadbundle`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_internalappsharingartifacts_uploadbundle_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_internalappsharingartifacts_uploadbundle_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InternalAppSharingArtifact>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InternalAppSharingArtifact = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/internalappsharing/{packageName}/artifacts/bundle
/// Uploads an app bundle to internal app sharing. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_internalappsharingartifacts_uploadbundle_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_internalappsharingartifacts_uploadbundle_task()`.
/// For the simplest API, use `androidpublisher_internalappsharingartifacts_uploadbundle()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_internalappsharingartifacts_uploadbundle_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_internalappsharingartifacts_uploadbundle_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InternalAppSharingArtifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_internalappsharingartifacts_uploadbundle_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_internalappsharingartifacts_uploadbundle`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherInternalappsharingartifactsUploadbundleArgs {
    /// Path parameter: packageName
    pub packageName: String,
}

/// GET androidpublisher/v3/applications/internalappsharing/{packageName}/artifacts/bundle
/// Uploads an app bundle to internal app sharing. If you are using the Google API client libraries, please increase the timeout of the http request before calling this endpoint (a timeout of 2 minutes is recommended). See [Timeouts and Errors](<https://developers.google.`com/api-client-library/java/google-api-java-client/errors`>) for an example in java.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_internalappsharingartifacts_uploadbundle_builder()` + `androidpublisher_internalappsharingartifacts_uploadbundle_execute()`.
/// For task-level control, use `androidpublisher_internalappsharingartifacts_uploadbundle_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_internalappsharingartifacts_uploadbundle(
    client: &SimpleHttpClient,
    args: &AndroidpublisherInternalappsharingartifactsUploadbundleArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InternalAppSharingArtifact>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_internalappsharingartifacts_uploadbundle_builder(
        client,
        args.packageName.clone(),
    )?;
    androidpublisher_internalappsharingartifacts_uploadbundle_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/pricing:convertRegionPrices
/// Calculates the region prices, using today's exchange rate and country-specific pricing patterns, based on the price in the request for a set of regions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_convert_region_prices_execute()` to send, or `androidpublisher_monetization_convert_region_prices` for simplest API.

pub fn androidpublisher_monetization_convert_region_prices_builder(
    client: &SimpleHttpClient,
    packageName: String,
    body: &ConvertRegionPricesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/pricing:convertRegionPrices",
        packageName.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/pricing:convertRegionPrices
/// Calculates the region prices, using today's exchange rate and country-specific pricing patterns, based on the price in the request for a set of regions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_convert_region_prices_execute()` or `androidpublisher_monetization_convert_region_prices`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_convert_region_prices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_convert_region_prices_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConvertRegionPricesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConvertRegionPricesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/pricing:convertRegionPrices
/// Calculates the region prices, using today's exchange rate and country-specific pricing patterns, based on the price in the request for a set of regions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_convert_region_prices_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_convert_region_prices_task()`.
/// For the simplest API, use `androidpublisher_monetization_convert_region_prices()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_convert_region_prices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_convert_region_prices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ConvertRegionPricesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_convert_region_prices_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_convert_region_prices`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationConvertRegionPricesArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Request body.
    pub body: ConvertRegionPricesRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/pricing:convertRegionPrices
/// Calculates the region prices, using today's exchange rate and country-specific pricing patterns, based on the price in the request for a set of regions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_convert_region_prices_builder()` + `androidpublisher_monetization_convert_region_prices_execute()`.
/// For task-level control, use `androidpublisher_monetization_convert_region_prices_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_convert_region_prices(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationConvertRegionPricesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ConvertRegionPricesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_convert_region_prices_builder(
        client,
        args.packageName.clone(),
        &args.body,
    )?;
    androidpublisher_monetization_convert_region_prices_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchDelete
/// Deletes one or more one-time products.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_batch_delete_execute()` to send, or `androidpublisher_monetization_onetimeproducts_batch_delete` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_batch_delete_builder(
    client: &SimpleHttpClient,
    packageName: String,
    body: &BatchDeleteOneTimeProductsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts:batchDelete",
        packageName.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchDelete
/// Deletes one or more one-time products.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_batch_delete_execute()` or `androidpublisher_monetization_onetimeproducts_batch_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_batch_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchDelete
/// Deletes one or more one-time products.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_batch_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_batch_delete_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_batch_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_onetimeproducts_batch_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_batch_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsBatchDeleteArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Request body.
    pub body: BatchDeleteOneTimeProductsRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchDelete
/// Deletes one or more one-time products.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_batch_delete_builder()` + `androidpublisher_monetization_onetimeproducts_batch_delete_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_batch_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_batch_delete(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsBatchDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_onetimeproducts_batch_delete_builder(
        client,
        args.packageName.clone(),
        &args.body,
    )?;
    androidpublisher_monetization_onetimeproducts_batch_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchGet
/// Reads one or more one-time products.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_batch_get_execute()` to send, or `androidpublisher_monetization_onetimeproducts_batch_get` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_batch_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productIds: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts:batchGet",
        packageName.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = productIds {
        query_parts.push(format!("productIds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchGet
/// Reads one or more one-time products.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_batch_get_execute()` or `androidpublisher_monetization_onetimeproducts_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchGetOneTimeProductsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetOneTimeProductsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchGet
/// Reads one or more one-time products.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_batch_get_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetOneTimeProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_onetimeproducts_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsBatchGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Query parameter: productIds
    pub productIds: Option<String>,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchGet
/// Reads one or more one-time products.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_batch_get_builder()` + `androidpublisher_monetization_onetimeproducts_batch_get_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_batch_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetOneTimeProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_onetimeproducts_batch_get_builder(
        client,
        args.packageName.clone(),
        args.productIds.clone(),
    )?;
    androidpublisher_monetization_onetimeproducts_batch_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchUpdate
/// Creates or updates one or more one-time products.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_batch_update_execute()` to send, or `androidpublisher_monetization_onetimeproducts_batch_update` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_batch_update_builder(
    client: &SimpleHttpClient,
    packageName: String,
    body: &BatchUpdateOneTimeProductsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts:batchUpdate",
        packageName.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchUpdate
/// Creates or updates one or more one-time products.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_batch_update_execute()` or `androidpublisher_monetization_onetimeproducts_batch_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_batch_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchUpdateOneTimeProductsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateOneTimeProductsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchUpdate
/// Creates or updates one or more one-time products.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_batch_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_batch_update_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_batch_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateOneTimeProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_onetimeproducts_batch_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_batch_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsBatchUpdateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Request body.
    pub body: BatchUpdateOneTimeProductsRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts:batchUpdate
/// Creates or updates one or more one-time products.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_batch_update_builder()` + `androidpublisher_monetization_onetimeproducts_batch_update_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_batch_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_batch_update(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsBatchUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateOneTimeProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_onetimeproducts_batch_update_builder(
        client,
        args.packageName.clone(),
        &args.body,
    )?;
    androidpublisher_monetization_onetimeproducts_batch_update_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}
/// Deletes a one-time product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_delete_execute()` to send, or `androidpublisher_monetization_onetimeproducts_delete` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_delete_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    latencyTolerance: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts/{}",
        packageName.as_str(),
        productId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = latencyTolerance {
        query_parts.push(format!("latencyTolerance={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}
/// Deletes a one-time product.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_delete_execute()` or `androidpublisher_monetization_onetimeproducts_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}
/// Deletes a one-time product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_delete_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_onetimeproducts_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsDeleteArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Query parameter: latencyTolerance
    pub latencyTolerance: Option<String>,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}
/// Deletes a one-time product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_delete_builder()` + `androidpublisher_monetization_onetimeproducts_delete_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_delete(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_onetimeproducts_delete_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.latencyTolerance.clone(),
    )?;
    androidpublisher_monetization_onetimeproducts_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts
/// Lists all one-time products under a given app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_list_execute()` to send, or `androidpublisher_monetization_onetimeproducts_list` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_list_builder(
    client: &SimpleHttpClient,
    packageName: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts",
        packageName.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts
/// Lists all one-time products under a given app.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_list_execute()` or `androidpublisher_monetization_onetimeproducts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOneTimeProductsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOneTimeProductsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts
/// Lists all one-time products under a given app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_list_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListOneTimeProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_onetimeproducts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsListArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts
/// Lists all one-time products under a given app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_list_builder()` + `androidpublisher_monetization_onetimeproducts_list_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_list(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListOneTimeProductsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_onetimeproducts_list_builder(
        client,
        args.packageName.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    androidpublisher_monetization_onetimeproducts_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/onetimeproducts/{productId}
/// Creates or updates a one-time product.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_patch_execute()` to send, or `androidpublisher_monetization_onetimeproducts_patch` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_patch_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    allowMissing: Option<bool>,
    latencyTolerance: Option<String>,
    regionsVersion_version: Option<String>,
    updateMask: Option<String>,
    body: &OneTimeProduct,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/onetimeproducts/{}",
        packageName.as_str(),
        productId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = latencyTolerance {
        query_parts.push(format!("latencyTolerance={}", val));
    }
    if let Some(val) = regionsVersion_version {
        query_parts.push(format!("regionsVersion.version={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/onetimeproducts/{productId}
/// Creates or updates a one-time product.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_patch_execute()` or `androidpublisher_monetization_onetimeproducts_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OneTimeProduct>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OneTimeProduct = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/onetimeproducts/{productId}
/// Creates or updates a one-time product.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_patch_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_onetimeproducts_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsPatchArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<bool>,
    /// Query parameter: latencyTolerance
    pub latencyTolerance: Option<String>,
    /// Query parameter: regionsVersion_version
    pub regionsVersion_version: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: OneTimeProduct,
}

/// GET androidpublisher/v3/applications/{packageName}/onetimeproducts/{productId}
/// Creates or updates a one-time product.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_patch_builder()` + `androidpublisher_monetization_onetimeproducts_patch_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_patch(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProduct>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_onetimeproducts_patch_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.allowMissing.clone(),
        args.latencyTolerance.clone(),
        args.regionsVersion_version.clone(),
        args.updateMask.clone(),
        &args.body,
    )?;
    androidpublisher_monetization_onetimeproducts_patch_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions:batchDelete
/// Deletes purchase options across one or multiple one-time products. By default this operation will fail if there are any existing offers under the deleted purchase options. Use the force parameter to override the default behavior.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    body: &BatchDeletePurchaseOptionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions:batchDelete",
        packageName.as_str(),
        productId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions:batchDelete
/// Deletes purchase options across one or multiple one-time products. By default this operation will fail if there are any existing offers under the deleted purchase options. Use the force parameter to override the default behavior.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_execute()` or `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions:batchDelete
/// Deletes purchase options across one or multiple one-time products. By default this operation will fail if there are any existing offers under the deleted purchase options. Use the force parameter to override the default behavior.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsBatchDeleteArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Request body.
    pub body: BatchDeletePurchaseOptionsRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions:batchDelete
/// Deletes purchase options across one or multiple one-time products. By default this operation will fail if there are any existing offers under the deleted purchase options. Use the force parameter to override the default behavior.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsBatchDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_builder(
            client,
            args.packageName.clone(),
            args.productId.clone(),
            &args.body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_batch_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions:batchUpdateStates
/// Activates or deactivates purchase options across one or multiple one-time products.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    body: &BatchUpdatePurchaseOptionStatesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions:batchUpdateStates",
        packageName.as_str(),
        productId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions:batchUpdateStates
/// Activates or deactivates purchase options across one or multiple one-time products.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_execute()` or `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchUpdatePurchaseOptionStatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdatePurchaseOptionStatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions:batchUpdateStates
/// Activates or deactivates purchase options across one or multiple one-time products.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdatePurchaseOptionStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsBatchUpdateStatesArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Request body.
    pub body: BatchUpdatePurchaseOptionStatesRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions:batchUpdateStates
/// Activates or deactivates purchase options across one or multiple one-time products.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsBatchUpdateStatesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdatePurchaseOptionStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_builder(
            client,
            args.packageName.clone(),
            args.productId.clone(),
            &args.body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_batch_update_states_execute(
        builder,
    )
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:activate
/// Activates a one-time product offer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    purchaseOptionId: String,
    offerId: String,
    body: &ActivateOneTimeProductOfferRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers/{}:activate",
        packageName.as_str(),
        productId.as_str(),
        purchaseOptionId.as_str(),
        offerId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:activate
/// Activates a one-time product offer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_execute()` or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OneTimeProductOffer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OneTimeProductOffer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:activate
/// Activates a one-time product offer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProductOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersActivateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: purchaseOptionId
    pub purchaseOptionId: String,
    /// Path parameter: offerId
    pub offerId: String,
    /// Request body.
    pub body: ActivateOneTimeProductOfferRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:activate
/// Activates a one-time product offer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersActivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProductOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_builder(
            client,
            args.packageName.clone(),
            args.productId.clone(),
            args.purchaseOptionId.clone(),
            args.offerId.clone(),
            &args.body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_activate_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchDelete
/// Deletes one or more one-time product offers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    purchaseOptionId: String,
    body: &BatchDeleteOneTimeProductOffersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers:batchDelete",
        packageName.as_str(),
        productId.as_str(),
        purchaseOptionId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchDelete
/// Deletes one or more one-time product offers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_execute()` or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchDelete
/// Deletes one or more one-time product offers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersBatchDeleteArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: purchaseOptionId
    pub purchaseOptionId: String,
    /// Request body.
    pub body: BatchDeleteOneTimeProductOffersRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchDelete
/// Deletes one or more one-time product offers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersBatchDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_builder(
            client,
            args.packageName.clone(),
            args.productId.clone(),
            args.purchaseOptionId.clone(),
            &args.body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_delete_execute(
        builder,
    )
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchGet
/// Reads one or more one-time product offers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    purchaseOptionId: String,
    body: &BatchGetOneTimeProductOffersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers:batchGet",
        packageName.as_str(),
        productId.as_str(),
        purchaseOptionId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchGet
/// Reads one or more one-time product offers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_execute()` or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchGetOneTimeProductOffersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetOneTimeProductOffersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchGet
/// Reads one or more one-time product offers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetOneTimeProductOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersBatchGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: purchaseOptionId
    pub purchaseOptionId: String,
    /// Request body.
    pub body: BatchGetOneTimeProductOffersRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchGet
/// Reads one or more one-time product offers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetOneTimeProductOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_builder(
            client,
            args.packageName.clone(),
            args.productId.clone(),
            args.purchaseOptionId.clone(),
            &args.body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchUpdate
/// Creates or updates one or more one-time product offers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    purchaseOptionId: String,
    body: &BatchUpdateOneTimeProductOffersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers:batchUpdate",
        packageName.as_str(),
        productId.as_str(),
        purchaseOptionId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchUpdate
/// Creates or updates one or more one-time product offers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_execute()` or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchUpdateOneTimeProductOffersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateOneTimeProductOffersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchUpdate
/// Creates or updates one or more one-time product offers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateOneTimeProductOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersBatchUpdateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: purchaseOptionId
    pub purchaseOptionId: String,
    /// Request body.
    pub body: BatchUpdateOneTimeProductOffersRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchUpdate
/// Creates or updates one or more one-time product offers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersBatchUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateOneTimeProductOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_builder(
            client,
            args.packageName.clone(),
            args.productId.clone(),
            args.purchaseOptionId.clone(),
            &args.body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_execute(
        builder,
    )
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchUpdateStates
/// Updates a batch of one-time product offer states.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    purchaseOptionId: String,
    body: &BatchUpdateOneTimeProductOfferStatesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers:batchUpdateStates",
        packageName.as_str(),
        productId.as_str(),
        purchaseOptionId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchUpdateStates
/// Updates a batch of one-time product offer states.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_execute()` or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchUpdateOneTimeProductOfferStatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateOneTimeProductOfferStatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchUpdateStates
/// Updates a batch of one-time product offer states.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateOneTimeProductOfferStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersBatchUpdateStatesArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: purchaseOptionId
    pub purchaseOptionId: String,
    /// Request body.
    pub body: BatchUpdateOneTimeProductOfferStatesRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers:batchUpdateStates
/// Updates a batch of one-time product offer states.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersBatchUpdateStatesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateOneTimeProductOfferStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_builder(client, args.packageName.clone(), args.productId.clone(), args.purchaseOptionId.clone(), &args.body)?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_batch_update_states_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:cancel
/// Cancels a one-time product offer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    purchaseOptionId: String,
    offerId: String,
    body: &CancelOneTimeProductOfferRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers/{}:cancel",
        packageName.as_str(),
        productId.as_str(),
        purchaseOptionId.as_str(),
        offerId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:cancel
/// Cancels a one-time product offer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_execute()` or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OneTimeProductOffer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OneTimeProductOffer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:cancel
/// Cancels a one-time product offer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProductOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersCancelArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: purchaseOptionId
    pub purchaseOptionId: String,
    /// Path parameter: offerId
    pub offerId: String,
    /// Request body.
    pub body: CancelOneTimeProductOfferRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:cancel
/// Cancels a one-time product offer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProductOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_builder(
            client,
            args.packageName.clone(),
            args.productId.clone(),
            args.purchaseOptionId.clone(),
            args.offerId.clone(),
            &args.body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_cancel_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:deactivate
/// Deactivates a one-time product offer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    purchaseOptionId: String,
    offerId: String,
    body: &DeactivateOneTimeProductOfferRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers/{}:deactivate",
        packageName.as_str(),
        productId.as_str(),
        purchaseOptionId.as_str(),
        offerId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:deactivate
/// Deactivates a one-time product offer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_execute()` or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OneTimeProductOffer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OneTimeProductOffer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:deactivate
/// Deactivates a one-time product offer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProductOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersDeactivateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: purchaseOptionId
    pub purchaseOptionId: String,
    /// Path parameter: offerId
    pub offerId: String,
    /// Request body.
    pub body: DeactivateOneTimeProductOfferRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers/{offerId}:deactivate
/// Deactivates a one-time product offer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersDeactivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OneTimeProductOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_builder(
            client,
            args.packageName.clone(),
            args.productId.clone(),
            args.purchaseOptionId.clone(),
            args.offerId.clone(),
            &args.body,
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_deactivate_execute(
        builder,
    )
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers
/// Lists all offers under a given app, product, or purchase option.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_execute()` to send, or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list` for simplest API.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    purchaseOptionId: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/oneTimeProducts/{}/purchaseOptions/{}/offers",
        packageName.as_str(),
        productId.as_str(),
        purchaseOptionId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers
/// Lists all offers under a given app, product, or purchase option.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_execute()` or `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOneTimeProductOffersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOneTimeProductOffersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers
/// Lists all offers under a given app, product, or purchase option.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_task()`.
/// For the simplest API, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListOneTimeProductOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_onetimeproducts_purchase_options_offers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersListArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: purchaseOptionId
    pub purchaseOptionId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET androidpublisher/v3/applications/{packageName}/oneTimeProducts/{productId}/purchaseOptions/{purchaseOptionId}/offers
/// Lists all offers under a given app, product, or purchase option.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_builder()` + `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_execute()`.
/// For task-level control, use `androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_onetimeproducts_purchase_options_offers_list(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationOnetimeproductsPurchaseOptionsOffersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListOneTimeProductOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_builder(
            client,
            args.packageName.clone(),
            args.productId.clone(),
            args.purchaseOptionId.clone(),
            args.pageSize.clone(),
            args.pageToken.clone(),
        )?;
    androidpublisher_monetization_onetimeproducts_purchase_options_offers_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}:archive
/// Deprecated: subscription archiving is not supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_archive_execute()` to send, or `androidpublisher_monetization_subscriptions_archive` for simplest API.

pub fn androidpublisher_monetization_subscriptions_archive_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    body: &ArchiveSubscriptionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}:archive",
        packageName.as_str(),
        productId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}:archive
/// Deprecated: subscription archiving is not supported.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_archive_execute()` or `androidpublisher_monetization_subscriptions_archive`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_archive_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_archive_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}:archive
/// Deprecated: subscription archiving is not supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_archive_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_archive_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_archive()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_archive_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_archive_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_subscriptions_archive_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_archive`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsArchiveArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Request body.
    pub body: ArchiveSubscriptionRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}:archive
/// Deprecated: subscription archiving is not supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_archive_builder()` + `androidpublisher_monetization_subscriptions_archive_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_archive_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_archive(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsArchiveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_archive_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        &args.body,
    )?;
    androidpublisher_monetization_subscriptions_archive_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions:batchGet
/// Reads one or more subscriptions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_batch_get_execute()` to send, or `androidpublisher_monetization_subscriptions_batch_get` for simplest API.

pub fn androidpublisher_monetization_subscriptions_batch_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productIds: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions:batchGet",
        packageName.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = productIds {
        query_parts.push(format!("productIds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions:batchGet
/// Reads one or more subscriptions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_batch_get_execute()` or `androidpublisher_monetization_subscriptions_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchGetSubscriptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetSubscriptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions:batchGet
/// Reads one or more subscriptions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_batch_get_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetSubscriptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_subscriptions_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBatchGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Query parameter: productIds
    pub productIds: Option<String>,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions:batchGet
/// Reads one or more subscriptions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_batch_get_builder()` + `androidpublisher_monetization_subscriptions_batch_get_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_batch_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetSubscriptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_batch_get_builder(
        client,
        args.packageName.clone(),
        args.productIds.clone(),
    )?;
    androidpublisher_monetization_subscriptions_batch_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions:batchUpdate
/// Updates a batch of subscriptions. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_batch_update_execute()` to send, or `androidpublisher_monetization_subscriptions_batch_update` for simplest API.

pub fn androidpublisher_monetization_subscriptions_batch_update_builder(
    client: &SimpleHttpClient,
    packageName: String,
    body: &BatchUpdateSubscriptionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions:batchUpdate",
        packageName.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions:batchUpdate
/// Updates a batch of subscriptions. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_batch_update_execute()` or `androidpublisher_monetization_subscriptions_batch_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_batch_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchUpdateSubscriptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateSubscriptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions:batchUpdate
/// Updates a batch of subscriptions. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_batch_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_batch_update_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_batch_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateSubscriptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_subscriptions_batch_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_batch_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBatchUpdateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Request body.
    pub body: BatchUpdateSubscriptionsRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions:batchUpdate
/// Updates a batch of subscriptions. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_batch_update_builder()` + `androidpublisher_monetization_subscriptions_batch_update_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_batch_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_batch_update(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBatchUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateSubscriptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_batch_update_builder(
        client,
        args.packageName.clone(),
        &args.body,
    )?;
    androidpublisher_monetization_subscriptions_batch_update_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions
/// Creates a new subscription. Newly added base plans will remain in draft state until activated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_create_execute()` to send, or `androidpublisher_monetization_subscriptions_create` for simplest API.

pub fn androidpublisher_monetization_subscriptions_create_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: Option<String>,
    regionsVersion_version: Option<String>,
    body: &Subscription,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions",
        packageName.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = productId {
        query_parts.push(format!("productId={}", val));
    }
    if let Some(val) = regionsVersion_version {
        query_parts.push(format!("regionsVersion.version={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions
/// Creates a new subscription. Newly added base plans will remain in draft state until activated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_create_execute()` or `androidpublisher_monetization_subscriptions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions
/// Creates a new subscription. Newly added base plans will remain in draft state until activated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_create_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_subscriptions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsCreateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Query parameter: productId
    pub productId: Option<String>,
    /// Query parameter: regionsVersion_version
    pub regionsVersion_version: Option<String>,
    /// Request body.
    pub body: Subscription,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions
/// Creates a new subscription. Newly added base plans will remain in draft state until activated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_create_builder()` + `androidpublisher_monetization_subscriptions_create_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_create(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_create_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.regionsVersion_version.clone(),
        &args.body,
    )?;
    androidpublisher_monetization_subscriptions_create_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}
/// Deletes a subscription. A subscription can only be deleted if it has never had a base plan published.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_delete_execute()` to send, or `androidpublisher_monetization_subscriptions_delete` for simplest API.

pub fn androidpublisher_monetization_subscriptions_delete_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}",
        packageName.as_str(),
        productId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}
/// Deletes a subscription. A subscription can only be deleted if it has never had a base plan published.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_delete_execute()` or `androidpublisher_monetization_subscriptions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}
/// Deletes a subscription. A subscription can only be deleted if it has never had a base plan published.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_delete_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_subscriptions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsDeleteArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}
/// Deletes a subscription. A subscription can only be deleted if it has never had a base plan published.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_delete_builder()` + `androidpublisher_monetization_subscriptions_delete_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_delete(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_delete_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
    )?;
    androidpublisher_monetization_subscriptions_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:activate
/// Activates a base plan. Once activated, base plans will be available to new subscribers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_activate_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_activate` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_activate_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    basePlanId: String,
    body: &ActivateBasePlanRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}:activate",
        packageName.as_str(),
        productId.as_str(),
        basePlanId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:activate
/// Activates a base plan. Once activated, base plans will be available to new subscribers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_base_plans_activate_execute()` or `androidpublisher_monetization_subscriptions_base_plans_activate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_activate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:activate
/// Activates a base plan. Once activated, base plans will be available to new subscribers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_base_plans_activate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_activate_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_base_plans_activate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_base_plans_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_subscriptions_base_plans_activate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_base_plans_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBasePlansActivateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: basePlanId
    pub basePlanId: String,
    /// Request body.
    pub body: ActivateBasePlanRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:activate
/// Activates a base plan. Once activated, base plans will be available to new subscribers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_activate_builder()` + `androidpublisher_monetization_subscriptions_base_plans_activate_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_activate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_activate(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBasePlansActivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_activate_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.basePlanId.clone(),
        &args.body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_activate_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans:batchMigratePrices
/// Batch variant of the MigrateBasePlanPrices endpoint. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    body: &BatchMigrateBasePlanPricesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}/basePlans:batchMigratePrices",
        packageName.as_str(),
        productId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans:batchMigratePrices
/// Batch variant of the MigrateBasePlanPrices endpoint. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_execute()` or `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchMigrateBasePlanPricesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchMigrateBasePlanPricesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans:batchMigratePrices
/// Batch variant of the MigrateBasePlanPrices endpoint. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchMigrateBasePlanPricesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBasePlansBatchMigratePricesArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Request body.
    pub body: BatchMigrateBasePlanPricesRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans:batchMigratePrices
/// Batch variant of the MigrateBasePlanPrices endpoint. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_builder()` + `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBasePlansBatchMigratePricesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchMigrateBasePlanPricesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_builder(
            client,
            args.packageName.clone(),
            args.productId.clone(),
            &args.body,
        )?;
    androidpublisher_monetization_subscriptions_base_plans_batch_migrate_prices_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans:batchUpdateStates
/// Activates or deactivates base plans across one or multiple subscriptions. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_batch_update_states_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_batch_update_states` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_batch_update_states_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    body: &BatchUpdateBasePlanStatesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}/basePlans:batchUpdateStates",
        packageName.as_str(),
        productId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans:batchUpdateStates
/// Activates or deactivates base plans across one or multiple subscriptions. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_base_plans_batch_update_states_execute()` or `androidpublisher_monetization_subscriptions_base_plans_batch_update_states`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_batch_update_states_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_batch_update_states_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchUpdateBasePlanStatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateBasePlanStatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans:batchUpdateStates
/// Activates or deactivates base plans across one or multiple subscriptions. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_base_plans_batch_update_states_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_batch_update_states_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_base_plans_batch_update_states()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_batch_update_states_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_base_plans_batch_update_states_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateBasePlanStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_subscriptions_base_plans_batch_update_states_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_base_plans_batch_update_states`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBasePlansBatchUpdateStatesArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Request body.
    pub body: BatchUpdateBasePlanStatesRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans:batchUpdateStates
/// Activates or deactivates base plans across one or multiple subscriptions. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_batch_update_states_builder()` + `androidpublisher_monetization_subscriptions_base_plans_batch_update_states_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_batch_update_states_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_batch_update_states(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBasePlansBatchUpdateStatesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateBasePlanStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_subscriptions_base_plans_batch_update_states_builder(
            client,
            args.packageName.clone(),
            args.productId.clone(),
            &args.body,
        )?;
    androidpublisher_monetization_subscriptions_base_plans_batch_update_states_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:deactivate
/// Deactivates a base plan. Once deactivated, the base plan will become unavailable to new subscribers, but existing subscribers will maintain their subscription
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_deactivate_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_deactivate` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_deactivate_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    basePlanId: String,
    body: &DeactivateBasePlanRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}:deactivate",
        packageName.as_str(),
        productId.as_str(),
        basePlanId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:deactivate
/// Deactivates a base plan. Once deactivated, the base plan will become unavailable to new subscribers, but existing subscribers will maintain their subscription
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_base_plans_deactivate_execute()` or `androidpublisher_monetization_subscriptions_base_plans_deactivate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_deactivate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_deactivate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Subscription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:deactivate
/// Deactivates a base plan. Once deactivated, the base plan will become unavailable to new subscribers, but existing subscribers will maintain their subscription
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_base_plans_deactivate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_deactivate_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_base_plans_deactivate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_deactivate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_base_plans_deactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_subscriptions_base_plans_deactivate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_base_plans_deactivate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBasePlansDeactivateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: basePlanId
    pub basePlanId: String,
    /// Request body.
    pub body: DeactivateBasePlanRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:deactivate
/// Deactivates a base plan. Once deactivated, the base plan will become unavailable to new subscribers, but existing subscribers will maintain their subscription
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_deactivate_builder()` + `androidpublisher_monetization_subscriptions_base_plans_deactivate_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_deactivate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_deactivate(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBasePlansDeactivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Subscription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_deactivate_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.basePlanId.clone(),
        &args.body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_deactivate_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}
/// Deletes a base plan. Can only be done for draft base plans. This action is irreversible.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_delete_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_delete` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_delete_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    basePlanId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}",
        packageName.as_str(),
        productId.as_str(),
        basePlanId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}
/// Deletes a base plan. Can only be done for draft base plans. This action is irreversible.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_base_plans_delete_execute()` or `androidpublisher_monetization_subscriptions_base_plans_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}
/// Deletes a base plan. Can only be done for draft base plans. This action is irreversible.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_base_plans_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_delete_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_base_plans_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_base_plans_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_subscriptions_base_plans_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_base_plans_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBasePlansDeleteArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: basePlanId
    pub basePlanId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}
/// Deletes a base plan. Can only be done for draft base plans. This action is irreversible.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_delete_builder()` + `androidpublisher_monetization_subscriptions_base_plans_delete_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_delete(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBasePlansDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_delete_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.basePlanId.clone(),
    )?;
    androidpublisher_monetization_subscriptions_base_plans_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:migratePrices
/// Migrates subscribers from one or more legacy price cohorts to the current price. Requests result in Google Play notifying affected subscribers. Only up to 250 simultaneous legacy price cohorts are supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_migrate_prices_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_migrate_prices` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_migrate_prices_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    basePlanId: String,
    body: &MigrateBasePlanPricesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}:migratePrices",
        packageName.as_str(),
        productId.as_str(),
        basePlanId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:migratePrices
/// Migrates subscribers from one or more legacy price cohorts to the current price. Requests result in Google Play notifying affected subscribers. Only up to 250 simultaneous legacy price cohorts are supported.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_base_plans_migrate_prices_execute()` or `androidpublisher_monetization_subscriptions_base_plans_migrate_prices`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_migrate_prices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_migrate_prices_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MigrateBasePlanPricesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MigrateBasePlanPricesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:migratePrices
/// Migrates subscribers from one or more legacy price cohorts to the current price. Requests result in Google Play notifying affected subscribers. Only up to 250 simultaneous legacy price cohorts are supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_base_plans_migrate_prices_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_migrate_prices_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_base_plans_migrate_prices()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_migrate_prices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_base_plans_migrate_prices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MigrateBasePlanPricesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_subscriptions_base_plans_migrate_prices_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_base_plans_migrate_prices`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBasePlansMigratePricesArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: basePlanId
    pub basePlanId: String,
    /// Request body.
    pub body: MigrateBasePlanPricesRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}:migratePrices
/// Migrates subscribers from one or more legacy price cohorts to the current price. Requests result in Google Play notifying affected subscribers. Only up to 250 simultaneous legacy price cohorts are supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_migrate_prices_builder()` + `androidpublisher_monetization_subscriptions_base_plans_migrate_prices_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_migrate_prices_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_migrate_prices(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBasePlansMigratePricesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MigrateBasePlanPricesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_migrate_prices_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.basePlanId.clone(),
        &args.body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_migrate_prices_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}:activate
/// Activates a subscription offer. Once activated, subscription offers will be available to new subscribers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_activate_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_activate` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_activate_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    basePlanId: String,
    offerId: String,
    body: &ActivateSubscriptionOfferRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers/{}:activate",
        packageName.as_str(),
        productId.as_str(),
        basePlanId.as_str(),
        offerId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}:activate
/// Activates a subscription offer. Once activated, subscription offers will be available to new subscribers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_base_plans_offers_activate_execute()` or `androidpublisher_monetization_subscriptions_base_plans_offers_activate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_offers_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_activate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubscriptionOffer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionOffer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}:activate
/// Activates a subscription offer. Once activated, subscription offers will be available to new subscribers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_activate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_offers_activate_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_base_plans_offers_activate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_offers_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_subscriptions_base_plans_offers_activate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_base_plans_offers_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBasePlansOffersActivateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: basePlanId
    pub basePlanId: String,
    /// Path parameter: offerId
    pub offerId: String,
    /// Request body.
    pub body: ActivateSubscriptionOfferRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}:activate
/// Activates a subscription offer. Once activated, subscription offers will be available to new subscribers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_activate_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_activate_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_offers_activate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_activate(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBasePlansOffersActivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_offers_activate_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.basePlanId.clone(),
        args.offerId.clone(),
        &args.body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_activate_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchGet
/// Reads one or more subscription offers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    basePlanId: String,
    body: &BatchGetSubscriptionOffersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers:batchGet",
        packageName.as_str(),
        productId.as_str(),
        basePlanId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchGet
/// Reads one or more subscription offers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_execute()` or `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchGetSubscriptionOffersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetSubscriptionOffersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchGet
/// Reads one or more subscription offers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetSubscriptionOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_base_plans_offers_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBasePlansOffersBatchGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: basePlanId
    pub basePlanId: String,
    /// Request body.
    pub body: BatchGetSubscriptionOffersRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchGet
/// Reads one or more subscription offers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBasePlansOffersBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetSubscriptionOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.basePlanId.clone(),
        &args.body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_batch_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchUpdate
/// Updates a batch of subscription offers. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    basePlanId: String,
    body: &BatchUpdateSubscriptionOffersRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers:batchUpdate",
        packageName.as_str(),
        productId.as_str(),
        basePlanId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchUpdate
/// Updates a batch of subscription offers. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_execute()` or `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchUpdateSubscriptionOffersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateSubscriptionOffersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchUpdate
/// Updates a batch of subscription offers. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateSubscriptionOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_base_plans_offers_batch_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBasePlansOffersBatchUpdateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: basePlanId
    pub basePlanId: String,
    /// Request body.
    pub body: BatchUpdateSubscriptionOffersRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchUpdate
/// Updates a batch of subscription offers. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_update(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBasePlansOffersBatchUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateSubscriptionOffersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_builder(
            client,
            args.packageName.clone(),
            args.productId.clone(),
            args.basePlanId.clone(),
            &args.body,
        )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchUpdateStates
/// Updates a batch of subscription offer states. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    basePlanId: String,
    body: &BatchUpdateSubscriptionOfferStatesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers:batchUpdateStates",
        packageName.as_str(),
        productId.as_str(),
        basePlanId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchUpdateStates
/// Updates a batch of subscription offer states. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_execute()` or `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchUpdateSubscriptionOfferStatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateSubscriptionOfferStatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchUpdateStates
/// Updates a batch of subscription offer states. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateSubscriptionOfferStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBasePlansOffersBatchUpdateStatesArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: basePlanId
    pub basePlanId: String,
    /// Request body.
    pub body: BatchUpdateSubscriptionOfferStatesRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers:batchUpdateStates
/// Updates a batch of subscription offer states. Set the `latencyTolerance` field on nested requests to PRODUCT_UPDATE_LATENCY_TOLERANCE_LATENCY_TOLERANT to achieve maximum update throughput.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBasePlansOffersBatchUpdateStatesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateSubscriptionOfferStatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_builder(
            client,
            args.packageName.clone(),
            args.productId.clone(),
            args.basePlanId.clone(),
            &args.body,
        )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_batch_update_states_execute(
        builder,
    )
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers
/// Creates a new subscription offer. Only auto-renewing base plans can have subscription offers. The offer state will be DRAFT until it is activated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_create_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_create` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_create_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    basePlanId: String,
    offerId: Option<String>,
    regionsVersion_version: Option<String>,
    body: &SubscriptionOffer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers",
        packageName.as_str(),
        productId.as_str(),
        basePlanId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = offerId {
        query_parts.push(format!("offerId={}", val));
    }
    if let Some(val) = regionsVersion_version {
        query_parts.push(format!("regionsVersion.version={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers
/// Creates a new subscription offer. Only auto-renewing base plans can have subscription offers. The offer state will be DRAFT until it is activated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_base_plans_offers_create_execute()` or `androidpublisher_monetization_subscriptions_base_plans_offers_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_offers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubscriptionOffer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionOffer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers
/// Creates a new subscription offer. Only auto-renewing base plans can have subscription offers. The offer state will be DRAFT until it is activated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_offers_create_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_base_plans_offers_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_offers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_subscriptions_base_plans_offers_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_base_plans_offers_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBasePlansOffersCreateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: basePlanId
    pub basePlanId: String,
    /// Query parameter: offerId
    pub offerId: Option<String>,
    /// Query parameter: regionsVersion_version
    pub regionsVersion_version: Option<String>,
    /// Request body.
    pub body: SubscriptionOffer,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers
/// Creates a new subscription offer. Only auto-renewing base plans can have subscription offers. The offer state will be DRAFT until it is activated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_create_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_create_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_offers_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_create(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBasePlansOffersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_offers_create_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.basePlanId.clone(),
        args.offerId.clone(),
        args.regionsVersion_version.clone(),
        &args.body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_create_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}:deactivate
/// Deactivates a subscription offer. Once deactivated, existing subscribers will maintain their subscription, but the offer will become unavailable to new subscribers.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    basePlanId: String,
    offerId: String,
    body: &DeactivateSubscriptionOfferRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers/{}:deactivate",
        packageName.as_str(),
        productId.as_str(),
        basePlanId.as_str(),
        offerId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}:deactivate
/// Deactivates a subscription offer. Once deactivated, existing subscribers will maintain their subscription, but the offer will become unavailable to new subscribers.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_execute()` or `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubscriptionOffer>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionOffer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}:deactivate
/// Deactivates a subscription offer. Once deactivated, existing subscribers will maintain their subscription, but the offer will become unavailable to new subscribers.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_base_plans_offers_deactivate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBasePlansOffersDeactivateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: basePlanId
    pub basePlanId: String,
    /// Path parameter: offerId
    pub offerId: String,
    /// Request body.
    pub body: DeactivateSubscriptionOfferRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}:deactivate
/// Deactivates a subscription offer. Once deactivated, existing subscribers will maintain their subscription, but the offer will become unavailable to new subscribers.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_deactivate(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBasePlansOffersDeactivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionOffer>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.basePlanId.clone(),
        args.offerId.clone(),
        &args.body,
    )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_deactivate_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}
/// Deletes a subscription offer. Can only be done for draft offers. This action is irreversible.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_monetization_subscriptions_base_plans_offers_delete_execute()` to send, or `androidpublisher_monetization_subscriptions_base_plans_offers_delete` for simplest API.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_delete_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    basePlanId: String,
    offerId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/subscriptions/{}/basePlans/{}/offers/{}",
        packageName.as_str(),
        productId.as_str(),
        basePlanId.as_str(),
        offerId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}
/// Deletes a subscription offer. Can only be done for draft offers. This action is irreversible.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_monetization_subscriptions_base_plans_offers_delete_execute()` or `androidpublisher_monetization_subscriptions_base_plans_offers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_offers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}
/// Deletes a subscription offer. Can only be done for draft offers. This action is irreversible.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_offers_delete_task()`.
/// For the simplest API, use `androidpublisher_monetization_subscriptions_base_plans_offers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_monetization_subscriptions_base_plans_offers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_monetization_subscriptions_base_plans_offers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_monetization_subscriptions_base_plans_offers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherMonetizationSubscriptionsBasePlansOffersDeleteArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: basePlanId
    pub basePlanId: String,
    /// Path parameter: offerId
    pub offerId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/subscriptions/{productId}/basePlans/{basePlanId}/offers/{offerId}
/// Deletes a subscription offer. Can only be done for draft offers. This action is irreversible.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_monetization_subscriptions_base_plans_offers_delete_builder()` + `androidpublisher_monetization_subscriptions_base_plans_offers_delete_execute()`.
/// For task-level control, use `androidpublisher_monetization_subscriptions_base_plans_offers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_monetization_subscriptions_base_plans_offers_delete(
    client: &SimpleHttpClient,
    args: &AndroidpublisherMonetizationSubscriptionsBasePlansOffersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_monetization_subscriptions_base_plans_offers_delete_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.basePlanId.clone(),
        args.offerId.clone(),
    )?;
    androidpublisher_monetization_subscriptions_base_plans_offers_delete_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/orders:batchGet
/// Get order details for a list of orders.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_orders_batchget_execute()` to send, or `androidpublisher_orders_batchget` for simplest API.

pub fn androidpublisher_orders_batchget_builder(
    client: &SimpleHttpClient,
    packageName: String,
    orderIds: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/orders:batchGet",
        packageName.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderIds {
        query_parts.push(format!("orderIds={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/orders:batchGet
/// Get order details for a list of orders.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_orders_batchget_execute()` or `androidpublisher_orders_batchget`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_orders_batchget_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_orders_batchget_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchGetOrdersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetOrdersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/orders:batchGet
/// Get order details for a list of orders.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_orders_batchget_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_orders_batchget_task()`.
/// For the simplest API, use `androidpublisher_orders_batchget()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_orders_batchget_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_orders_batchget_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchGetOrdersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_orders_batchget_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_orders_batchget`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherOrdersBatchgetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Query parameter: orderIds
    pub orderIds: Option<String>,
}

/// GET androidpublisher/v3/applications/{packageName}/orders:batchGet
/// Get order details for a list of orders.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_orders_batchget_builder()` + `androidpublisher_orders_batchget_execute()`.
/// For task-level control, use `androidpublisher_orders_batchget_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_orders_batchget(
    client: &SimpleHttpClient,
    args: &AndroidpublisherOrdersBatchgetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchGetOrdersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_orders_batchget_builder(
        client,
        args.packageName.clone(),
        args.orderIds.clone(),
    )?;
    androidpublisher_orders_batchget_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/orders/{orderId}
/// Get order details for a single order.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_orders_get_execute()` to send, or `androidpublisher_orders_get` for simplest API.

pub fn androidpublisher_orders_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    orderId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/orders/{}",
        packageName.as_str(),
        orderId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/orders/{orderId}
/// Get order details for a single order.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_orders_get_execute()` or `androidpublisher_orders_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_orders_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_orders_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Order>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Order = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/orders/{orderId}
/// Get order details for a single order.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_orders_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_orders_get_task()`.
/// For the simplest API, use `androidpublisher_orders_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_orders_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_orders_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Order>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_orders_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_orders_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherOrdersGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: orderId
    pub orderId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/orders/{orderId}
/// Get order details for a single order.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_orders_get_builder()` + `androidpublisher_orders_get_execute()`.
/// For task-level control, use `androidpublisher_orders_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_orders_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherOrdersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Order>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_orders_get_builder(
        client,
        args.packageName.clone(),
        args.orderId.clone(),
    )?;
    androidpublisher_orders_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/orders/{orderId}:refund
/// Refunds a user's subscription or in-app purchase order. Orders older than 3 years cannot be refunded.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_orders_refund_execute()` to send, or `androidpublisher_orders_refund` for simplest API.

pub fn androidpublisher_orders_refund_builder(
    client: &SimpleHttpClient,
    packageName: String,
    orderId: String,
    revoke: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/orders/{}:refund",
        packageName.as_str(),
        orderId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = revoke {
        query_parts.push(format!("revoke={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/orders/{orderId}:refund
/// Refunds a user's subscription or in-app purchase order. Orders older than 3 years cannot be refunded.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_orders_refund_execute()` or `androidpublisher_orders_refund`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_orders_refund_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_orders_refund_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/orders/{orderId}:refund
/// Refunds a user's subscription or in-app purchase order. Orders older than 3 years cannot be refunded.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_orders_refund_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_orders_refund_task()`.
/// For the simplest API, use `androidpublisher_orders_refund()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_orders_refund_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_orders_refund_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_orders_refund_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_orders_refund`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherOrdersRefundArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: orderId
    pub orderId: String,
    /// Query parameter: revoke
    pub revoke: Option<bool>,
}

/// GET androidpublisher/v3/applications/{packageName}/orders/{orderId}:refund
/// Refunds a user's subscription or in-app purchase order. Orders older than 3 years cannot be refunded.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_orders_refund_builder()` + `androidpublisher_orders_refund_execute()`.
/// For task-level control, use `androidpublisher_orders_refund_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_orders_refund(
    client: &SimpleHttpClient,
    args: &AndroidpublisherOrdersRefundArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_orders_refund_builder(
        client,
        args.packageName.clone(),
        args.orderId.clone(),
        args.revoke.clone(),
    )?;
    androidpublisher_orders_refund_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}:acknowledge
/// Acknowledges a purchase of an inapp item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_products_acknowledge_execute()` to send, or `androidpublisher_purchases_products_acknowledge` for simplest API.

pub fn androidpublisher_purchases_products_acknowledge_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    token: String,
    body: &ProductPurchasesAcknowledgeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/products/{}/tokens/{}:acknowledge",
        packageName.as_str(),
        productId.as_str(),
        token.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}:acknowledge
/// Acknowledges a purchase of an inapp item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_products_acknowledge_execute()` or `androidpublisher_purchases_products_acknowledge`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_products_acknowledge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_products_acknowledge_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}:acknowledge
/// Acknowledges a purchase of an inapp item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_products_acknowledge_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_products_acknowledge_task()`.
/// For the simplest API, use `androidpublisher_purchases_products_acknowledge()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_products_acknowledge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_products_acknowledge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_products_acknowledge_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_products_acknowledge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesProductsAcknowledgeArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: token
    pub token: String,
    /// Request body.
    pub body: ProductPurchasesAcknowledgeRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}:acknowledge
/// Acknowledges a purchase of an inapp item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_products_acknowledge_builder()` + `androidpublisher_purchases_products_acknowledge_execute()`.
/// For task-level control, use `androidpublisher_purchases_products_acknowledge_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_products_acknowledge(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesProductsAcknowledgeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_products_acknowledge_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.token.clone(),
        &args.body,
    )?;
    androidpublisher_purchases_products_acknowledge_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}:consume
/// Consumes a purchase for an inapp item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_products_consume_execute()` to send, or `androidpublisher_purchases_products_consume` for simplest API.

pub fn androidpublisher_purchases_products_consume_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    token: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/products/{}/tokens/{}:consume",
        packageName.as_str(),
        productId.as_str(),
        token.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}:consume
/// Consumes a purchase for an inapp item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_products_consume_execute()` or `androidpublisher_purchases_products_consume`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_products_consume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_products_consume_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}:consume
/// Consumes a purchase for an inapp item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_products_consume_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_products_consume_task()`.
/// For the simplest API, use `androidpublisher_purchases_products_consume()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_products_consume_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_products_consume_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_products_consume_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_products_consume`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesProductsConsumeArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: token
    pub token: String,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}:consume
/// Consumes a purchase for an inapp item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_products_consume_builder()` + `androidpublisher_purchases_products_consume_execute()`.
/// For task-level control, use `androidpublisher_purchases_products_consume_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_products_consume(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesProductsConsumeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_products_consume_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.token.clone(),
    )?;
    androidpublisher_purchases_products_consume_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}
/// Checks the purchase and consumption status of an inapp item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_products_get_execute()` to send, or `androidpublisher_purchases_products_get` for simplest API.

pub fn androidpublisher_purchases_products_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    productId: String,
    token: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/products/{}/tokens/{}",
        packageName.as_str(),
        productId.as_str(),
        token.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}
/// Checks the purchase and consumption status of an inapp item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_products_get_execute()` or `androidpublisher_purchases_products_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_products_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_products_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductPurchase>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductPurchase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}
/// Checks the purchase and consumption status of an inapp item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_products_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_products_get_task()`.
/// For the simplest API, use `androidpublisher_purchases_products_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_products_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_products_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductPurchase>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_products_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_products_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesProductsGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: productId
    pub productId: String,
    /// Path parameter: token
    pub token: String,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/products/{productId}/tokens/{token}
/// Checks the purchase and consumption status of an inapp item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_products_get_builder()` + `androidpublisher_purchases_products_get_execute()`.
/// For task-level control, use `androidpublisher_purchases_products_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_products_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesProductsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductPurchase>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_products_get_builder(
        client,
        args.packageName.clone(),
        args.productId.clone(),
        args.token.clone(),
    )?;
    androidpublisher_purchases_products_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/productsv2/tokens/{token}
/// Checks the purchase and consumption status of an inapp item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_productsv2_getproductpurchasev2_execute()` to send, or `androidpublisher_purchases_productsv2_getproductpurchasev2` for simplest API.

pub fn androidpublisher_purchases_productsv2_getproductpurchasev2_builder(
    client: &SimpleHttpClient,
    packageName: String,
    token: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/productsv2/tokens/{}",
        packageName.as_str(),
        token.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/productsv2/tokens/{token}
/// Checks the purchase and consumption status of an inapp item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_productsv2_getproductpurchasev2_execute()` or `androidpublisher_purchases_productsv2_getproductpurchasev2`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_productsv2_getproductpurchasev2_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_productsv2_getproductpurchasev2_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductPurchaseV2>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProductPurchaseV2 = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/productsv2/tokens/{token}
/// Checks the purchase and consumption status of an inapp item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_productsv2_getproductpurchasev2_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_productsv2_getproductpurchasev2_task()`.
/// For the simplest API, use `androidpublisher_purchases_productsv2_getproductpurchasev2()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_productsv2_getproductpurchasev2_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_productsv2_getproductpurchasev2_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductPurchaseV2>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_productsv2_getproductpurchasev2_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_productsv2_getproductpurchasev2`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesProductsv2Getproductpurchasev2Args {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: token
    pub token: String,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/productsv2/tokens/{token}
/// Checks the purchase and consumption status of an inapp item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_productsv2_getproductpurchasev2_builder()` + `androidpublisher_purchases_productsv2_getproductpurchasev2_execute()`.
/// For task-level control, use `androidpublisher_purchases_productsv2_getproductpurchasev2_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_productsv2_getproductpurchasev2(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesProductsv2Getproductpurchasev2Args,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProductPurchaseV2>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_productsv2_getproductpurchasev2_builder(
        client,
        args.packageName.clone(),
        args.token.clone(),
    )?;
    androidpublisher_purchases_productsv2_getproductpurchasev2_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:acknowledge
/// Acknowledges a subscription purchase.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptions_acknowledge_execute()` to send, or `androidpublisher_purchases_subscriptions_acknowledge` for simplest API.

pub fn androidpublisher_purchases_subscriptions_acknowledge_builder(
    client: &SimpleHttpClient,
    packageName: String,
    subscriptionId: String,
    token: String,
    body: &SubscriptionPurchasesAcknowledgeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/subscriptions/{}/tokens/{}:acknowledge",
        packageName.as_str(),
        subscriptionId.as_str(),
        token.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:acknowledge
/// Acknowledges a subscription purchase.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_subscriptions_acknowledge_execute()` or `androidpublisher_purchases_subscriptions_acknowledge`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptions_acknowledge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_acknowledge_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:acknowledge
/// Acknowledges a subscription purchase.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_subscriptions_acknowledge_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_subscriptions_acknowledge_task()`.
/// For the simplest API, use `androidpublisher_purchases_subscriptions_acknowledge()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptions_acknowledge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_subscriptions_acknowledge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_subscriptions_acknowledge_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_subscriptions_acknowledge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesSubscriptionsAcknowledgeArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: subscriptionId
    pub subscriptionId: String,
    /// Path parameter: token
    pub token: String,
    /// Request body.
    pub body: SubscriptionPurchasesAcknowledgeRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:acknowledge
/// Acknowledges a subscription purchase.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptions_acknowledge_builder()` + `androidpublisher_purchases_subscriptions_acknowledge_execute()`.
/// For task-level control, use `androidpublisher_purchases_subscriptions_acknowledge_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_acknowledge(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesSubscriptionsAcknowledgeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptions_acknowledge_builder(
        client,
        args.packageName.clone(),
        args.subscriptionId.clone(),
        args.token.clone(),
        &args.body,
    )?;
    androidpublisher_purchases_subscriptions_acknowledge_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:cancel
/// Cancels a user's subscription purchase. The subscription remains valid until its expiration time. Newer version is available at purchases.subscriptionsv2.cancel for better client library support.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptions_cancel_execute()` to send, or `androidpublisher_purchases_subscriptions_cancel` for simplest API.

pub fn androidpublisher_purchases_subscriptions_cancel_builder(
    client: &SimpleHttpClient,
    packageName: String,
    subscriptionId: String,
    token: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/subscriptions/{}/tokens/{}:cancel",
        packageName.as_str(),
        subscriptionId.as_str(),
        token.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:cancel
/// Cancels a user's subscription purchase. The subscription remains valid until its expiration time. Newer version is available at purchases.subscriptionsv2.cancel for better client library support.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_subscriptions_cancel_execute()` or `androidpublisher_purchases_subscriptions_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptions_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:cancel
/// Cancels a user's subscription purchase. The subscription remains valid until its expiration time. Newer version is available at purchases.subscriptionsv2.cancel for better client library support.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_subscriptions_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_subscriptions_cancel_task()`.
/// For the simplest API, use `androidpublisher_purchases_subscriptions_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptions_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_subscriptions_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_subscriptions_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_subscriptions_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesSubscriptionsCancelArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: subscriptionId
    pub subscriptionId: String,
    /// Path parameter: token
    pub token: String,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:cancel
/// Cancels a user's subscription purchase. The subscription remains valid until its expiration time. Newer version is available at purchases.subscriptionsv2.cancel for better client library support.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptions_cancel_builder()` + `androidpublisher_purchases_subscriptions_cancel_execute()`.
/// For task-level control, use `androidpublisher_purchases_subscriptions_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_cancel(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesSubscriptionsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptions_cancel_builder(
        client,
        args.packageName.clone(),
        args.subscriptionId.clone(),
        args.token.clone(),
    )?;
    androidpublisher_purchases_subscriptions_cancel_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:defer
/// Defers a user's subscription purchase until a specified future expiration time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptions_defer_execute()` to send, or `androidpublisher_purchases_subscriptions_defer` for simplest API.

pub fn androidpublisher_purchases_subscriptions_defer_builder(
    client: &SimpleHttpClient,
    packageName: String,
    subscriptionId: String,
    token: String,
    body: &SubscriptionPurchasesDeferRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/subscriptions/{}/tokens/{}:defer",
        packageName.as_str(),
        subscriptionId.as_str(),
        token.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:defer
/// Defers a user's subscription purchase until a specified future expiration time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_subscriptions_defer_execute()` or `androidpublisher_purchases_subscriptions_defer`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptions_defer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_defer_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubscriptionPurchasesDeferResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionPurchasesDeferResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:defer
/// Defers a user's subscription purchase until a specified future expiration time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_subscriptions_defer_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_subscriptions_defer_task()`.
/// For the simplest API, use `androidpublisher_purchases_subscriptions_defer()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptions_defer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_subscriptions_defer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubscriptionPurchasesDeferResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_subscriptions_defer_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_subscriptions_defer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesSubscriptionsDeferArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: subscriptionId
    pub subscriptionId: String,
    /// Path parameter: token
    pub token: String,
    /// Request body.
    pub body: SubscriptionPurchasesDeferRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:defer
/// Defers a user's subscription purchase until a specified future expiration time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptions_defer_builder()` + `androidpublisher_purchases_subscriptions_defer_execute()`.
/// For task-level control, use `androidpublisher_purchases_subscriptions_defer_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_defer(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesSubscriptionsDeferArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubscriptionPurchasesDeferResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptions_defer_builder(
        client,
        args.packageName.clone(),
        args.subscriptionId.clone(),
        args.token.clone(),
        &args.body,
    )?;
    androidpublisher_purchases_subscriptions_defer_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}
/// Deprecated: Use purchases.subscriptionsv2.get instead. Checks whether a user's subscription purchase is valid and returns its expiry time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptions_get_execute()` to send, or `androidpublisher_purchases_subscriptions_get` for simplest API.

pub fn androidpublisher_purchases_subscriptions_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    subscriptionId: String,
    token: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/subscriptions/{}/tokens/{}",
        packageName.as_str(),
        subscriptionId.as_str(),
        token.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}
/// Deprecated: Use purchases.subscriptionsv2.get instead. Checks whether a user's subscription purchase is valid and returns its expiry time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_subscriptions_get_execute()` or `androidpublisher_purchases_subscriptions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubscriptionPurchase>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionPurchase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}
/// Deprecated: Use purchases.subscriptionsv2.get instead. Checks whether a user's subscription purchase is valid and returns its expiry time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_subscriptions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_subscriptions_get_task()`.
/// For the simplest API, use `androidpublisher_purchases_subscriptions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_subscriptions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionPurchase>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_subscriptions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_subscriptions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesSubscriptionsGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: subscriptionId
    pub subscriptionId: String,
    /// Path parameter: token
    pub token: String,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}
/// Deprecated: Use purchases.subscriptionsv2.get instead. Checks whether a user's subscription purchase is valid and returns its expiry time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptions_get_builder()` + `androidpublisher_purchases_subscriptions_get_execute()`.
/// For task-level control, use `androidpublisher_purchases_subscriptions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesSubscriptionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionPurchase>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptions_get_builder(
        client,
        args.packageName.clone(),
        args.subscriptionId.clone(),
        args.token.clone(),
    )?;
    androidpublisher_purchases_subscriptions_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:refund
/// Deprecated: Use orders.refund instead. Refunds a user's subscription purchase, but the subscription remains valid until its expiration time and it will continue to recur.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptions_refund_execute()` to send, or `androidpublisher_purchases_subscriptions_refund` for simplest API.

pub fn androidpublisher_purchases_subscriptions_refund_builder(
    client: &SimpleHttpClient,
    packageName: String,
    subscriptionId: String,
    token: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/subscriptions/{}/tokens/{}:refund",
        packageName.as_str(),
        subscriptionId.as_str(),
        token.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:refund
/// Deprecated: Use orders.refund instead. Refunds a user's subscription purchase, but the subscription remains valid until its expiration time and it will continue to recur.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_subscriptions_refund_execute()` or `androidpublisher_purchases_subscriptions_refund`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptions_refund_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_refund_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:refund
/// Deprecated: Use orders.refund instead. Refunds a user's subscription purchase, but the subscription remains valid until its expiration time and it will continue to recur.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_subscriptions_refund_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_subscriptions_refund_task()`.
/// For the simplest API, use `androidpublisher_purchases_subscriptions_refund()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptions_refund_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_subscriptions_refund_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_subscriptions_refund_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_subscriptions_refund`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesSubscriptionsRefundArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: subscriptionId
    pub subscriptionId: String,
    /// Path parameter: token
    pub token: String,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:refund
/// Deprecated: Use orders.refund instead. Refunds a user's subscription purchase, but the subscription remains valid until its expiration time and it will continue to recur.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptions_refund_builder()` + `androidpublisher_purchases_subscriptions_refund_execute()`.
/// For task-level control, use `androidpublisher_purchases_subscriptions_refund_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_refund(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesSubscriptionsRefundArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptions_refund_builder(
        client,
        args.packageName.clone(),
        args.subscriptionId.clone(),
        args.token.clone(),
    )?;
    androidpublisher_purchases_subscriptions_refund_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:revoke
/// Deprecated: Use purchases.subscriptionsv2.revoke instead. Refunds and immediately revokes a user's subscription purchase. Access to the subscription will be terminated immediately and it will stop recurring.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptions_revoke_execute()` to send, or `androidpublisher_purchases_subscriptions_revoke` for simplest API.

pub fn androidpublisher_purchases_subscriptions_revoke_builder(
    client: &SimpleHttpClient,
    packageName: String,
    subscriptionId: String,
    token: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/subscriptions/{}/tokens/{}:revoke",
        packageName.as_str(),
        subscriptionId.as_str(),
        token.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:revoke
/// Deprecated: Use purchases.subscriptionsv2.revoke instead. Refunds and immediately revokes a user's subscription purchase. Access to the subscription will be terminated immediately and it will stop recurring.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_subscriptions_revoke_execute()` or `androidpublisher_purchases_subscriptions_revoke`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptions_revoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_revoke_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:revoke
/// Deprecated: Use purchases.subscriptionsv2.revoke instead. Refunds and immediately revokes a user's subscription purchase. Access to the subscription will be terminated immediately and it will stop recurring.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_subscriptions_revoke_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_subscriptions_revoke_task()`.
/// For the simplest API, use `androidpublisher_purchases_subscriptions_revoke()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptions_revoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_subscriptions_revoke_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_subscriptions_revoke_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_subscriptions_revoke`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesSubscriptionsRevokeArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: subscriptionId
    pub subscriptionId: String,
    /// Path parameter: token
    pub token: String,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptions/{subscriptionId}/tokens/{token}:revoke
/// Deprecated: Use purchases.subscriptionsv2.revoke instead. Refunds and immediately revokes a user's subscription purchase. Access to the subscription will be terminated immediately and it will stop recurring.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptions_revoke_builder()` + `androidpublisher_purchases_subscriptions_revoke_execute()`.
/// For task-level control, use `androidpublisher_purchases_subscriptions_revoke_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptions_revoke(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesSubscriptionsRevokeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptions_revoke_builder(
        client,
        args.packageName.clone(),
        args.subscriptionId.clone(),
        args.token.clone(),
    )?;
    androidpublisher_purchases_subscriptions_revoke_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:cancel
/// Cancel a subscription purchase for the user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptionsv2_cancel_execute()` to send, or `androidpublisher_purchases_subscriptionsv2_cancel` for simplest API.

pub fn androidpublisher_purchases_subscriptionsv2_cancel_builder(
    client: &SimpleHttpClient,
    packageName: String,
    token: String,
    body: &CancelSubscriptionPurchaseRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/subscriptionsv2/tokens/{}:cancel",
        packageName.as_str(),
        token.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:cancel
/// Cancel a subscription purchase for the user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_subscriptionsv2_cancel_execute()` or `androidpublisher_purchases_subscriptionsv2_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptionsv2_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CancelSubscriptionPurchaseResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CancelSubscriptionPurchaseResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:cancel
/// Cancel a subscription purchase for the user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_subscriptionsv2_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_subscriptionsv2_cancel_task()`.
/// For the simplest API, use `androidpublisher_purchases_subscriptionsv2_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptionsv2_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_subscriptionsv2_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CancelSubscriptionPurchaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_subscriptionsv2_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_subscriptionsv2_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesSubscriptionsv2CancelArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: token
    pub token: String,
    /// Request body.
    pub body: CancelSubscriptionPurchaseRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:cancel
/// Cancel a subscription purchase for the user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptionsv2_cancel_builder()` + `androidpublisher_purchases_subscriptionsv2_cancel_execute()`.
/// For task-level control, use `androidpublisher_purchases_subscriptionsv2_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_cancel(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesSubscriptionsv2CancelArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CancelSubscriptionPurchaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptionsv2_cancel_builder(
        client,
        args.packageName.clone(),
        args.token.clone(),
        &args.body,
    )?;
    androidpublisher_purchases_subscriptionsv2_cancel_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:defer
/// Defers the renewal of a subscription.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptionsv2_defer_execute()` to send, or `androidpublisher_purchases_subscriptionsv2_defer` for simplest API.

pub fn androidpublisher_purchases_subscriptionsv2_defer_builder(
    client: &SimpleHttpClient,
    packageName: String,
    token: String,
    body: &DeferSubscriptionPurchaseRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/subscriptionsv2/tokens/{}:defer",
        packageName.as_str(),
        token.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:defer
/// Defers the renewal of a subscription.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_subscriptionsv2_defer_execute()` or `androidpublisher_purchases_subscriptionsv2_defer`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptionsv2_defer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_defer_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeferSubscriptionPurchaseResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeferSubscriptionPurchaseResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:defer
/// Defers the renewal of a subscription.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_subscriptionsv2_defer_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_subscriptionsv2_defer_task()`.
/// For the simplest API, use `androidpublisher_purchases_subscriptionsv2_defer()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptionsv2_defer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_subscriptionsv2_defer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeferSubscriptionPurchaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_subscriptionsv2_defer_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_subscriptionsv2_defer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesSubscriptionsv2DeferArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: token
    pub token: String,
    /// Request body.
    pub body: DeferSubscriptionPurchaseRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:defer
/// Defers the renewal of a subscription.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptionsv2_defer_builder()` + `androidpublisher_purchases_subscriptionsv2_defer_execute()`.
/// For task-level control, use `androidpublisher_purchases_subscriptionsv2_defer_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_defer(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesSubscriptionsv2DeferArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DeferSubscriptionPurchaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptionsv2_defer_builder(
        client,
        args.packageName.clone(),
        args.token.clone(),
        &args.body,
    )?;
    androidpublisher_purchases_subscriptionsv2_defer_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}
/// Get metadata about a subscription
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptionsv2_get_execute()` to send, or `androidpublisher_purchases_subscriptionsv2_get` for simplest API.

pub fn androidpublisher_purchases_subscriptionsv2_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    token: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/subscriptionsv2/tokens/{}",
        packageName.as_str(),
        token.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}
/// Get metadata about a subscription
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_subscriptionsv2_get_execute()` or `androidpublisher_purchases_subscriptionsv2_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptionsv2_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubscriptionPurchaseV2>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubscriptionPurchaseV2 = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}
/// Get metadata about a subscription
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_subscriptionsv2_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_subscriptionsv2_get_task()`.
/// For the simplest API, use `androidpublisher_purchases_subscriptionsv2_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptionsv2_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_subscriptionsv2_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionPurchaseV2>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_subscriptionsv2_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_subscriptionsv2_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesSubscriptionsv2GetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: token
    pub token: String,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}
/// Get metadata about a subscription
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptionsv2_get_builder()` + `androidpublisher_purchases_subscriptionsv2_get_execute()`.
/// For task-level control, use `androidpublisher_purchases_subscriptionsv2_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesSubscriptionsv2GetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SubscriptionPurchaseV2>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptionsv2_get_builder(
        client,
        args.packageName.clone(),
        args.token.clone(),
    )?;
    androidpublisher_purchases_subscriptionsv2_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:revoke
/// Revoke a subscription purchase for the user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_subscriptionsv2_revoke_execute()` to send, or `androidpublisher_purchases_subscriptionsv2_revoke` for simplest API.

pub fn androidpublisher_purchases_subscriptionsv2_revoke_builder(
    client: &SimpleHttpClient,
    packageName: String,
    token: String,
    body: &RevokeSubscriptionPurchaseRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/subscriptionsv2/tokens/{}:revoke",
        packageName.as_str(),
        token.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:revoke
/// Revoke a subscription purchase for the user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_subscriptionsv2_revoke_execute()` or `androidpublisher_purchases_subscriptionsv2_revoke`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptionsv2_revoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_revoke_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RevokeSubscriptionPurchaseResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RevokeSubscriptionPurchaseResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:revoke
/// Revoke a subscription purchase for the user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_subscriptionsv2_revoke_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_subscriptionsv2_revoke_task()`.
/// For the simplest API, use `androidpublisher_purchases_subscriptionsv2_revoke()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_subscriptionsv2_revoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_subscriptionsv2_revoke_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RevokeSubscriptionPurchaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_subscriptionsv2_revoke_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_subscriptionsv2_revoke`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesSubscriptionsv2RevokeArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: token
    pub token: String,
    /// Request body.
    pub body: RevokeSubscriptionPurchaseRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/subscriptionsv2/tokens/{token}:revoke
/// Revoke a subscription purchase for the user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_subscriptionsv2_revoke_builder()` + `androidpublisher_purchases_subscriptionsv2_revoke_execute()`.
/// For task-level control, use `androidpublisher_purchases_subscriptionsv2_revoke_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_subscriptionsv2_revoke(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesSubscriptionsv2RevokeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<RevokeSubscriptionPurchaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_subscriptionsv2_revoke_builder(
        client,
        args.packageName.clone(),
        args.token.clone(),
        &args.body,
    )?;
    androidpublisher_purchases_subscriptionsv2_revoke_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/voidedpurchases
/// Lists the purchases that were canceled, refunded or charged-back.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_purchases_voidedpurchases_list_execute()` to send, or `androidpublisher_purchases_voidedpurchases_list` for simplest API.

pub fn androidpublisher_purchases_voidedpurchases_list_builder(
    client: &SimpleHttpClient,
    packageName: String,
    endTime: Option<String>,
    includeQuantityBasedPartialRefund: Option<bool>,
    maxResults: Option<i32>,
    startIndex: Option<i32>,
    startTime: Option<String>,
    token: Option<String>,
    type_rs: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/purchases/voidedpurchases",
        packageName.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endTime {
        query_parts.push(format!("endTime={}", val));
    }
    if let Some(val) = includeQuantityBasedPartialRefund {
        query_parts.push(format!("includeQuantityBasedPartialRefund={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = startIndex {
        query_parts.push(format!("startIndex={}", val));
    }
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/voidedpurchases
/// Lists the purchases that were canceled, refunded or charged-back.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_purchases_voidedpurchases_list_execute()` or `androidpublisher_purchases_voidedpurchases_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_voidedpurchases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_voidedpurchases_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VoidedPurchasesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VoidedPurchasesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/voidedpurchases
/// Lists the purchases that were canceled, refunded or charged-back.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_purchases_voidedpurchases_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_purchases_voidedpurchases_list_task()`.
/// For the simplest API, use `androidpublisher_purchases_voidedpurchases_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_purchases_voidedpurchases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_purchases_voidedpurchases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VoidedPurchasesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_purchases_voidedpurchases_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_purchases_voidedpurchases_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherPurchasesVoidedpurchasesListArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Query parameter: endTime
    pub endTime: Option<String>,
    /// Query parameter: includeQuantityBasedPartialRefund
    pub includeQuantityBasedPartialRefund: Option<bool>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: startIndex
    pub startIndex: Option<i32>,
    /// Query parameter: startTime
    pub startTime: Option<String>,
    /// Query parameter: token
    pub token: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<i32>,
}

/// GET androidpublisher/v3/applications/{packageName}/purchases/voidedpurchases
/// Lists the purchases that were canceled, refunded or charged-back.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_purchases_voidedpurchases_list_builder()` + `androidpublisher_purchases_voidedpurchases_list_execute()`.
/// For task-level control, use `androidpublisher_purchases_voidedpurchases_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_purchases_voidedpurchases_list(
    client: &SimpleHttpClient,
    args: &AndroidpublisherPurchasesVoidedpurchasesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<VoidedPurchasesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_purchases_voidedpurchases_list_builder(
        client,
        args.packageName.clone(),
        args.endTime.clone(),
        args.includeQuantityBasedPartialRefund.clone(),
        args.maxResults.clone(),
        args.startIndex.clone(),
        args.startTime.clone(),
        args.token.clone(),
        args.type_rs.clone(),
    )?;
    androidpublisher_purchases_voidedpurchases_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/reviews/{reviewId}
/// Gets a single review.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_reviews_get_execute()` to send, or `androidpublisher_reviews_get` for simplest API.

pub fn androidpublisher_reviews_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    reviewId: String,
    translationLanguage: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/reviews/{}",
        packageName.as_str(),
        reviewId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = translationLanguage {
        query_parts.push(format!("translationLanguage={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/reviews/{reviewId}
/// Gets a single review.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_reviews_get_execute()` or `androidpublisher_reviews_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_reviews_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_reviews_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Review>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Review = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/reviews/{reviewId}
/// Gets a single review.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_reviews_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_reviews_get_task()`.
/// For the simplest API, use `androidpublisher_reviews_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_reviews_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_reviews_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Review>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_reviews_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_reviews_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherReviewsGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: reviewId
    pub reviewId: String,
    /// Query parameter: translationLanguage
    pub translationLanguage: Option<String>,
}

/// GET androidpublisher/v3/applications/{packageName}/reviews/{reviewId}
/// Gets a single review.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_reviews_get_builder()` + `androidpublisher_reviews_get_execute()`.
/// For task-level control, use `androidpublisher_reviews_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_reviews_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherReviewsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Review>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_reviews_get_builder(
        client,
        args.packageName.clone(),
        args.reviewId.clone(),
        args.translationLanguage.clone(),
    )?;
    androidpublisher_reviews_get_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/reviews
/// Lists all reviews.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_reviews_list_execute()` to send, or `androidpublisher_reviews_list` for simplest API.

pub fn androidpublisher_reviews_list_builder(
    client: &SimpleHttpClient,
    packageName: String,
    maxResults: Option<i32>,
    startIndex: Option<i32>,
    token: Option<String>,
    translationLanguage: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/reviews",
        packageName.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = startIndex {
        query_parts.push(format!("startIndex={}", val));
    }
    if let Some(val) = token {
        query_parts.push(format!("token={}", val));
    }
    if let Some(val) = translationLanguage {
        query_parts.push(format!("translationLanguage={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/reviews
/// Lists all reviews.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_reviews_list_execute()` or `androidpublisher_reviews_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_reviews_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_reviews_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReviewsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReviewsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/reviews
/// Lists all reviews.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_reviews_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_reviews_list_task()`.
/// For the simplest API, use `androidpublisher_reviews_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_reviews_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_reviews_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReviewsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_reviews_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_reviews_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherReviewsListArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: startIndex
    pub startIndex: Option<i32>,
    /// Query parameter: token
    pub token: Option<String>,
    /// Query parameter: translationLanguage
    pub translationLanguage: Option<String>,
}

/// GET androidpublisher/v3/applications/{packageName}/reviews
/// Lists all reviews.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_reviews_list_builder()` + `androidpublisher_reviews_list_execute()`.
/// For task-level control, use `androidpublisher_reviews_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_reviews_list(
    client: &SimpleHttpClient,
    args: &AndroidpublisherReviewsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReviewsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_reviews_list_builder(
        client,
        args.packageName.clone(),
        args.maxResults.clone(),
        args.startIndex.clone(),
        args.token.clone(),
        args.translationLanguage.clone(),
    )?;
    androidpublisher_reviews_list_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/reviews/{reviewId}:reply
/// Replies to a single review, or updates an existing reply.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_reviews_reply_execute()` to send, or `androidpublisher_reviews_reply` for simplest API.

pub fn androidpublisher_reviews_reply_builder(
    client: &SimpleHttpClient,
    packageName: String,
    reviewId: String,
    body: &ReviewsReplyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/reviews/{}:reply",
        packageName.as_str(),
        reviewId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/reviews/{reviewId}:reply
/// Replies to a single review, or updates an existing reply.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_reviews_reply_execute()` or `androidpublisher_reviews_reply`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_reviews_reply_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_reviews_reply_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReviewsReplyResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReviewsReplyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/reviews/{reviewId}:reply
/// Replies to a single review, or updates an existing reply.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_reviews_reply_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_reviews_reply_task()`.
/// For the simplest API, use `androidpublisher_reviews_reply()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_reviews_reply_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_reviews_reply_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReviewsReplyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = androidpublisher_reviews_reply_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_reviews_reply`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherReviewsReplyArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: reviewId
    pub reviewId: String,
    /// Request body.
    pub body: ReviewsReplyRequest,
}

/// GET androidpublisher/v3/applications/{packageName}/reviews/{reviewId}:reply
/// Replies to a single review, or updates an existing reply.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_reviews_reply_builder()` + `androidpublisher_reviews_reply_execute()`.
/// For task-level control, use `androidpublisher_reviews_reply_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_reviews_reply(
    client: &SimpleHttpClient,
    args: &AndroidpublisherReviewsReplyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReviewsReplyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = androidpublisher_reviews_reply_builder(
        client,
        args.packageName.clone(),
        args.reviewId.clone(),
        &args.body,
    )?;
    androidpublisher_reviews_reply_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants
/// Creates an APK which is suitable for inclusion in a system image from an already uploaded Android App Bundle.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_systemapks_variants_create_execute()` to send, or `androidpublisher_systemapks_variants_create` for simplest API.

pub fn androidpublisher_systemapks_variants_create_builder(
    client: &SimpleHttpClient,
    packageName: String,
    versionCode: String,
    body: &Variant,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/systemApks/{}/variants",
        packageName.as_str(),
        versionCode.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants
/// Creates an APK which is suitable for inclusion in a system image from an already uploaded Android App Bundle.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_systemapks_variants_create_execute()` or `androidpublisher_systemapks_variants_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_systemapks_variants_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_systemapks_variants_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Variant>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Variant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants
/// Creates an APK which is suitable for inclusion in a system image from an already uploaded Android App Bundle.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_systemapks_variants_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_systemapks_variants_create_task()`.
/// For the simplest API, use `androidpublisher_systemapks_variants_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_systemapks_variants_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_systemapks_variants_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Variant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_systemapks_variants_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_systemapks_variants_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherSystemapksVariantsCreateArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: versionCode
    pub versionCode: String,
    /// Request body.
    pub body: Variant,
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants
/// Creates an APK which is suitable for inclusion in a system image from an already uploaded Android App Bundle.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_systemapks_variants_create_builder()` + `androidpublisher_systemapks_variants_create_execute()`.
/// For task-level control, use `androidpublisher_systemapks_variants_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_systemapks_variants_create(
    client: &SimpleHttpClient,
    args: &AndroidpublisherSystemapksVariantsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Variant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_systemapks_variants_create_builder(
        client,
        args.packageName.clone(),
        args.versionCode.clone(),
        &args.body,
    )?;
    androidpublisher_systemapks_variants_create_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants/{variantId}:download
/// Downloads a previously created system APK which is suitable for inclusion in a system image.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_systemapks_variants_download_execute()` to send, or `androidpublisher_systemapks_variants_download` for simplest API.

pub fn androidpublisher_systemapks_variants_download_builder(
    client: &SimpleHttpClient,
    packageName: String,
    versionCode: String,
    variantId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/systemApks/{}/variants/{}:download",
        packageName.as_str(),
        versionCode.as_str(),
        variantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants/{variantId}:download
/// Downloads a previously created system APK which is suitable for inclusion in a system image.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_systemapks_variants_download_execute()` or `androidpublisher_systemapks_variants_download`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_systemapks_variants_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_systemapks_variants_download_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants/{variantId}:download
/// Downloads a previously created system APK which is suitable for inclusion in a system image.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_systemapks_variants_download_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_systemapks_variants_download_task()`.
/// For the simplest API, use `androidpublisher_systemapks_variants_download()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_systemapks_variants_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_systemapks_variants_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_systemapks_variants_download_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_systemapks_variants_download`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherSystemapksVariantsDownloadArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: versionCode
    pub versionCode: String,
    /// Path parameter: variantId
    pub variantId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants/{variantId}:download
/// Downloads a previously created system APK which is suitable for inclusion in a system image.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_systemapks_variants_download_builder()` + `androidpublisher_systemapks_variants_download_execute()`.
/// For task-level control, use `androidpublisher_systemapks_variants_download_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_systemapks_variants_download(
    client: &SimpleHttpClient,
    args: &AndroidpublisherSystemapksVariantsDownloadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_systemapks_variants_download_builder(
        client,
        args.packageName.clone(),
        args.versionCode.clone(),
        args.variantId.clone(),
    )?;
    androidpublisher_systemapks_variants_download_execute(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants/{variantId}
/// Returns a previously created system APK variant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_systemapks_variants_get_execute()` to send, or `androidpublisher_systemapks_variants_get` for simplest API.

pub fn androidpublisher_systemapks_variants_get_builder(
    client: &SimpleHttpClient,
    packageName: String,
    versionCode: String,
    variantId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{}/systemApks/{}/variants/{}",
        packageName.as_str(),
        versionCode.as_str(),
        variantId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants/{variantId}
/// Returns a previously created system APK variant.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_systemapks_variants_get_execute()` or `androidpublisher_systemapks_variants_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_systemapks_variants_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_systemapks_variants_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Variant>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Variant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants/{variantId}
/// Returns a previously created system APK variant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_systemapks_variants_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_systemapks_variants_get_task()`.
/// For the simplest API, use `androidpublisher_systemapks_variants_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_systemapks_variants_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_systemapks_variants_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Variant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_systemapks_variants_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_systemapks_variants_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherSystemapksVariantsGetArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Path parameter: versionCode
    pub versionCode: String,
    /// Path parameter: variantId
    pub variantId: String,
}

/// GET androidpublisher/v3/applications/{packageName}/systemApks/{versionCode}/variants/{variantId}
/// Returns a previously created system APK variant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_systemapks_variants_get_builder()` + `androidpublisher_systemapks_variants_get_execute()`.
/// For task-level control, use `androidpublisher_systemapks_variants_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_systemapks_variants_get(
    client: &SimpleHttpClient,
    args: &AndroidpublisherSystemapksVariantsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Variant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_systemapks_variants_get_builder(
        client,
        args.packageName.clone(),
        args.versionCode.clone(),
        args.variantId.clone(),
    )?;
    androidpublisher_systemapks_variants_get_execute(builder)
}

/// GET androidpublisher/v3/developers/{developersId}/users
/// Grant access for a user to the given developer account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `androidpublisher_users_create_execute()` to send, or `androidpublisher_users_create` for simplest API.

pub fn androidpublisher_users_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &User,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://androidpublisher.googleapis.com/androidpublisher/v3/developers/{}/users",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET androidpublisher/v3/developers/{developersId}/users
/// Grant access for a user to the given developer account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `androidpublisher_users_create_execute()` or `androidpublisher_users_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_users_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_users_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<User>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET androidpublisher/v3/developers/{developersId}/users
/// Grant access for a user to the given developer account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `androidpublisher_users_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `androidpublisher_users_create_task()`.
/// For the simplest API, use `androidpublisher_users_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `androidpublisher_users_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn androidpublisher_users_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = androidpublisher_users_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`androidpublisher_users_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidpublisherUsersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: User,
}

/// GET androidpublisher/v3/developers/{developersId}/users
/// Grant access for a user to the given developer account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `androidpublisher_users_create_builder()` + `androidpublisher_users_create_execute()`.
/// For task-level control, use `androidpublisher_users_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn androidpublisher_users_create(
    client: &SimpleHttpClient,
    args: &AndroidpublisherUsersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = androidpublisher_users_create_builder(client, args.parent.clone(), &args.body)?;
    androidpublisher_users_create_execute(builder)
}
